---
phase: quick/260914-a2y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/quick-health-check.sh
autonomous: true
requirements: [CR-01, WR-01]
user_setup: []

must_haves:
  truths:
    - "A wrong or absent requireBeetsMatch in the live extended.conf turns quick-health-check.sh red and exits 1"
    - "A ConversionFormat that is neither FLAC nor OPUS turns quick-health-check.sh red and exits 1"
    - "An unreadable extended.conf, an absent container, a bound expiry or an unreachable host each print their own UNKNOWN and exit 1 — never a tick, never silence"
    - "On the healthy estate the script still exits 0 with the new assertion green"
    - "No byte of extended.conf is copied into this repo and no whole-file content reaches script output"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "the extended.conf destructive-switch assertion block plus its EXIT-CODE notice"
      contains: "EXTCONF_"
  key_links:
    - from: "scripts/quick-health-check.sh"
      to: "sabnzbd container /config/extended.conf"
      via: "ssh -n + docker exec, bounded remote-side by REMOTE_TIMEOUT"
      pattern: "docker exec sabnzbd"
---

<objective>
Close code-review findings CR-01 (Critical) and WR-01 by adding one fail-closed assertion block to
`scripts/quick-health-check.sh` covering the two `/config/extended.conf` values that disarm the
destructive branches in `stacks/selfhosted/arrs/sabnzbd/audio.bash`.

Purpose: Phase 4 stripped the `beet` call from `audio.bash`. `beets()` (audio.bash:281-295) touches a
sentinel then tests for audio files *newer* than it; the removed `beet` call was the only writer that
could make one newer. `SUCCESS: Matched with beets!` (:286) is now structurally unreachable and the
`else` at :287 is taken on EVERY music job — corroborated, not theorised, by plan 04-15's window-2
block recording `matching_delta=3 error_delta=3` over 3 real jobs. The only thing standing between
that branch and `rm -rf "$1"/*` at :290 is `requireBeetsMatch="false"` in a host file that is not
vendored, not in the D-13 drift set, and asserted nowhere. `conversion()` has the same shape (WR-01):
`ConversionFormat=FLAC` short-circuits at :220 and `OPUS` is handled at :226, but every other value
falls to `rm -rf "$1"/*` at :237, making the `ffmpeg` block at :242-255 unreachable dead code.

Output: one new assertion block, one new `EXIT-CODE BEHAVIOUR CHANGED` notice, one updated tail
pointer, and an executed negative-control transcript covering all four failure branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@scripts/quick-health-check.sh
@stacks/selfhosted/arrs/sabnzbd/audio.bash
@.planning/phases/04-collapse-to-one-tagger/04-REVIEW.md
</context>

<measured_facts>
These were measured during planning. Do not re-derive them; do not contradict them without
re-measuring and saying so.

1. **The brief's "fourth notice" ordinal is WRONG, and writing "fourth" would make the file lie.**
   `grep -n 'EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh` returns **five headers**
   (lines 4, 29, 55, 135, 156); `grep -c` returns **6**, because the 02-09 notice quotes the phrase
   inside its own body (the file says so at :140 and declines to "fix" it). The notice you add is
   therefore the **SIXTH header**, and the post-edit `grep -c` is **7**. Use those numbers.

2. **`extended.conf` carries trailing comments on the same line as a value.** Plan 04-12 measured
   `grep -cx '^ReplaygainTagging="false"'` returning **0** for exactly this reason. Any pattern that
   anchors the end of the line, or uses `grep -x`, FAILS ON A CORRECT VALUE. Anchor the start only.

3. **Live values, verified by the orchestrator 2026-09-14:** `requireBeetsMatch="false"`,
   `ConversionFormat="FLAC"`; file sha256 `54c5433bba38c61e372425c34cc0c8aad2001922ca1fb6bd55e98dc1ada1b49d`,
   2955 bytes. That sha256 also matches the § 3 baseline plan 04-15 recorded, so the file has not moved.

4. **Container name is `sabnzbd`** (`stacks/selfhosted/arrs/sabnzbd.yaml:31`).

5. **The file holds `radarrArrApiKey`, `radarr4kArrApiKey`, `sonarrArrApiKey`, `sonarr4kArrApiKey`,
   `sonarranimeArrApiKey`.** Empty today. THIS REPO IS PUBLIC.

6. **THREE OF THIS PLAN'S OWN ACCEPTANCE GATES WERE DEFECTIVE AND WERE FIXED BEFORE IT SHIPPED.**
   Recorded because this estate's most-repeated defect class is a gate that cannot discriminate, and
   a planner that quietly repairs its own gates teaches nothing. Each was found by DRIVING it against
   a control, not by reading it:
   - **A FALSE-RED, and the worst of the three.** The prohibited-pattern gate was first written
     `grep -c 'grep -x\|requireBeetsMatch=\"\?false\"\?$'`. Driven against a control file it returned
     **2** — it matched the `grep -x` token AND a perfectly legitimate `requireBeetsMatch="false"`, a
     string the new block's own match pattern necessarily contains. It would have failed Task 1
     against CORRECT code. Replaced with the `-E` form now in the task, which requires a literal
     trailing `$` character: driven at **1** on the bad token and **0** on clean text.
   - **A SECOND FALSE-RED, from a platform difference this repo already documents.**
     `git diff --stat -- audio.bash | wc -l | grep -q '^0$'` DID NOT MATCH on an unchanged file,
     because macOS `wc` pads its output with leading spaces so `^0$` can never match. Same family as
     the `timeout N cmd | wc -l` trap at :488-496. Replaced with
     `test -z "$(git diff --name-only -- …)"`, driven clean. Every surviving `wc -l` in this plan
     pipes through `tr -d ' '`.
   - **A VACUOUS GATE.** `git show --name-only HEAD | grep -qv 'extended.conf'` succeeds whenever ANY
     line does not match — which is every real commit, so it passes even when `extended.conf` IS
     present. Driven and confirmed, then deleted. The adjacent `grep -c … -eq 0` clause is the one
     that actually tests the property.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Add the extended.conf destructive-switch assertion block, its notice, and the tail pointer</name>
  <files>scripts/quick-health-check.sh</files>
  <action>
Three edits to one file. Do NOT create any other file, and do NOT touch `audio.bash` — byte-identity
against the host copy is an asserted property of the D-13 drift block directly above your insertion
point.

**Edit A — the override constants.** Immediately after the `DRIFT_EXPECT_*` block (currently ending
~:324), add two `${VAR:-default}` constants in the established style, with a comment stating the
DRIFT_APPDATA_ROOT precedent explicitly: every override here can only make the block REDDER, and
there is deliberately no success-producing override.
  - `EXTCONF_HOST="${EXTCONF_HOST:-root@172.16.1.159}"`
  - `EXTCONF_PATH="${EXTCONF_PATH:-/config/extended.conf}"` (a path INSIDE the sabnzbd container)
Any non-default value of either MUST force `EXIT_CODE=1` regardless of what the comparison finds,
exactly as `DRIFT_APPDATA_ROOT` does at :615-619. That is what makes the negative controls in Task 2
executable without ever being able to launder a red run into a green one.

**Edit B — the assertion block.** Insert AFTER the vendored-drift block's closing `fi` (currently
:682) and BEFORE the `# Music freeze harness (D-29)` comment. Copy the drift block's idiom exactly:

  - Build the remote command in a variable whose string STARTS with `set -o pipefail`.
  - Capture with `OUT=$(ssh -n $SSH_OPTS "$EXTCONF_HOST" "$CMD")` and take `RC=$?` on the VERY NEXT
    LINE with NO local pipe in the assignment. `timeout N cmd | grep -c` silently exits 0 — that trap
    is the entire reason for this rule; see the container-count note at :503-508.
  - Bound the remote command LINUX-SIDE with `timeout $REMOTE_TIMEOUT` inside the command string.
    macOS has no GNU `timeout`; a workstation-side wrapper is an undeclared dependency on the one
    machine this file is meant to be typed on.
  - Branch order is the house S1 order and is not negotiable: empty output first (deferring when
    `RC -eq 124`, because a killed command usually produces no output either) → 124 → any other
    non-zero → only then assert. Every non-assert branch prints `⚠️  UNKNOWN`, says in words
    "Nothing was asserted. This is NOT 'the destructive switches are safe'", and sets `EXIT_CODE=1`.

The remote command reads the file via `docker exec sabnzbd sh -c "cat $EXTCONF_PATH"` and does ALL
matching REMOTE-SIDE, emitting only labels. Distinct exit codes so the UNKNOWN branch can name the
cause: 3 = `docker exec` failed (container absent, docker unavailable), 4 = the `cat` failed
(unreadable/missing path), 6 = the read succeeded but produced no bytes.

Emit exactly two label lines, `requireBeetsMatch=<ok|BAD>` and `ConversionFormat=<ok|BAD>`. On BAD
you MAY echo the offending line for those two keys only — neither is a secret and diagnosis needs it.
NEVER echo the whole file, and never any line matching the five `*ArrApiKey` names.

Match patterns — start-anchored, tolerant of whitespace and an optional quote, and with NO end anchor
and NO `grep -x` (see measured fact 2, which is a defect this estate has already paid for):
  - `grep -qE '^[[:space:]]*requireBeetsMatch="?false"?'`
  - `grep -qE '^[[:space:]]*ConversionFormat="?(FLAC|OPUS)"?'`
Absence is a FAILURE, not a pass — an absent `requireBeetsMatch` makes `audio.bash`'s unquoted
`[ $requireBeetsMatch = true ]` error with rc 2 and land in the safe direction only by accident, which
is not a property to depend on.

Then assert a short-answer guard in the drift block's own shape: count the emitted label lines and, if
they are not exactly 2, print UNKNOWN and set `EXIT_CODE=1` — "a short answer is 'could not look', NOT
'the switch that did report is fine'". An unrecognised label gets the same treatment.

On the all-clear path print one identifying line containing the literal token `extended.conf switches`
so Task 3 can quote it, and name both switches on it. Print it ONLY when both are ok AND neither
override is in effect.

In the block's header comment, state the four things a future reader will otherwise get wrong:
(a) WHY it exists — the `beets()` success branch is structurally unreachable after the Phase 4 strip,
    so the `else` with `rm -rf` at audio.bash:290 runs on every music job and this value is the only
    guard; cite the 04-15 `matching_delta=3 error_delta=3` observation as the corroboration.
(b) WHY the fix is here and not in `audio.bash` — the drift guard asserts byte-identity, so patching
    the script in place would turn this repo red against the host.
(c) WHY `extended.conf` is NOT vendored and must not be added to the D-13 set — the repo is PUBLIC and
    the file carries five `*ArrApiKey` fields, empty today. Assert the values; never copy the file.
(d) That `ReplaygainTagging` is DELIBERATELY out of scope here. The review's snippet names three
    switches; this block asserts the two that gate a `rm -rf`. Say so, so the omission reads as a
    decision rather than an oversight.

**Edit C — two pointer updates, same commit.**
  - Add the SIXTH `EXIT-CODE BEHAVIOUR CHANGED` notice at the top, after the one at :156, in the
    established style and carrying the shared greppable phrase literally. State the new conditions
    that can exit 1: a wrong/absent `requireBeetsMatch`; a `ConversionFormat` outside {FLAC, OPUS};
    the config unreadable or the container absent; the read bounded out at 124; the host unreachable;
    a short or unrecognised answer; and either override being non-default. Number it SIXTH, not
    fourth — see measured fact 1, and record the `grep -c` going 6 → 7 in the notice itself so the
    next reader is not surprised by the over-count the file already documents at :140.
  - Extend the tail message at :962-964, which enumerates the fatal blocks, to name this one. Plan
    02.1-15 caught exactly this: a run that failed on a block the tail did not list sent the reader
    to the green ones.
  </action>
  <verify>
    <automated>bash -n scripts/quick-health-check.sh && test "$(grep -c 'EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh)" -eq 7 && test "$(grep -v '^#' scripts/quick-health-check.sh | grep -c 'EXTCONF_HOST\|EXTCONF_PATH')" -ge 4 && test "$(grep -v '^#' scripts/quick-health-check.sh | grep -c 'docker exec sabnzbd')" -ge 1 && test "$(grep -v '^#' scripts/quick-health-check.sh | grep -cE 'grep -x|requireBeetsMatch="?false"?\$')" -eq 0 && test -z "$(git -C . diff --name-only -- stacks/selfhosted/arrs/sabnzbd/audio.bash)"</automated>
  </verify>
  <done>
`bash -n` passes. The notice count is 7. Both overrides and the `docker exec sabnzbd` read exist in
EXECUTABLE lines (comment-stripped — the tokens also appear in the new prose, and a grep satisfied by
a comment about the thing rather than the thing is this estate's most-repeated defect). No `grep -x`
and no end-anchored value pattern anywhere. `audio.bash` is untouched. No new file was created, and
nothing resembling `extended.conf` content entered the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drive all four failure branches, then restore and prove the restore</name>
  <files>.planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt</files>
  <action>
This repo's standard is proofs EXECUTED, not asserted. A branch that has only been read has not been
tested, and the estate has twice shipped a control that was structurally unable to fail. Capture every
command and its full output into the artifact file as you go.

**Before anything else**, record `sha256sum` of the live `/config/extended.conf` via
`docker exec sabnzbd sh -c 'sha256sum /config/extended.conf'` and assert it equals
`54c5433bba38c61e372425c34cc0c8aad2001922ca1fb6bd55e98dc1ada1b49d` (measured fact 3). Re-assert it at
the END of this task. **Do not mutate the real `/config/extended.conf` at any point** — its value is
the only thing standing between the estate and data loss, and nothing here needs it changed.

Drive these four, each with the full command line and its exit code recorded. Each must produce its
OWN distinct red or UNKNOWN and set exit 1 — a shared verdict is the defect, not the fix:

  1. **Wrong/absent `requireBeetsMatch`.** Write a fixture into the container's writable layer only —
     `docker exec sabnzbd sh -c 'printf ... > /tmp/ec-fixture-a2y.conf'` carrying
     `requireBeetsMatch="true"` and a valid `ConversionFormat="FLAC"` — then run with
     `EXTCONF_PATH=/tmp/ec-fixture-a2y.conf`. Expect: `requireBeetsMatch` red naming the rm -rf,
     `ConversionFormat` NOT red, exit 1. The second half is what proves the assertion is per-switch
     rather than a single flag that reds the whole block — that per-field property is what plan
     02.1-07's most informative transcript line turned on. Run a second variant with the key absent
     entirely and confirm absence is treated as failure, not as a pass.
  2. **Wrong `ConversionFormat`.** Same device, a fixture carrying `ConversionFormat="MP3"` and a
     valid `requireBeetsMatch="false"`. Expect: `ConversionFormat` red citing WR-01, the other green,
     exit 1. `MP3` is the right value to pick — it is precisely the format whose `ffmpeg` path at
     audio.bash:242 is unreachable dead code behind the `rm -rf` at :237.
  3. **Unreadable config.** `EXTCONF_PATH=/config/does-not-exist-a2y.conf`. Expect the exit-4 UNKNOWN
     branch, in words "could not look", NOT a green tick and NOT silence, exit 1.
  4. **Unreachable host.** `EXTCONF_HOST=root@192.0.2.1` (RFC 5737 TEST-NET-1, the same device row 21
     used). Expect an UNKNOWN naming the unreachable/ssh-failure case, exit 1.

Note in the artifact that controls 1-4 all run with a non-default override, which by Edit A forces
`EXIT_CODE=1` on its own. So for each one, ALSO record the block's own label lines, and state plainly
that the exit code alone is not the evidence here — the label lines are. A control whose verdict is
guaranteed by the override would be uninformative, and saying so is the difference between a proof
and a transcript that looks like one.

**Clean up under a `trap`, not on the happy path.** `docker exec sabnzbd sh -c 'rm -f
/tmp/ec-fixture-a2y.conf'`. Codex caught "restore on failure with no `trap`" in this estate once
already. Afterwards assert the fixtures are gone and re-assert the live file's sha256 unchanged.

If any branch does NOT behave as described, record what it actually did and FIX THE CODE — do not
adjust the expectation to match the behaviour, and do not delete a control that failed.
  </action>
  <verify>
    <automated>test -s .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt && test "$(grep -c '54c5433bba38c61e372425c34cc0c8aad2001922ca1fb6bd55e98dc1ada1b49d' .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt)" -ge 2 && test "$(grep -c '192.0.2.1' .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt)" -ge 1 && test "$(grep -c 'ec-fixture-a2y' .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt)" -ge 3 && test "$(grep -cE 'radarrArrApiKey|sonarrArrApiKey|radarr4kArrApiKey|sonarr4kArrApiKey|sonarranimeArrApiKey' .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt)" -eq 0</automated>
  </verify>
  <done>
All four branches driven with commands and outputs recorded; each produced its own distinct
red/UNKNOWN and exit 1. The per-switch independence of controls 1 and 2 is demonstrated, not claimed.
The live `extended.conf` sha256 appears at least twice (open and close) and is unchanged. Fixtures are
removed. No `*ArrApiKey` name and no whole-file content appears anywhere in the artifact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run the health check green and commit</name>
  <files>scripts/quick-health-check.sh, .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/artifacts/260914-a2y-negative-controls.txt</files>
  <action>
Run `bash scripts/quick-health-check.sh` with NO environment overrides — the routine invocation, which
is the only one that can report green. Confirm it exits **0** and that the new assertion is green,
quoting its identifying line (the one carrying `extended.conf switches`) verbatim into the artifact
file along with the observed exit code.

`scripts/quick-health-check.sh` is the estate's only test suite and there is no unit-test framework
here; it exited 0 before this change and must exit 0 after. If it does not, the change is wrong —
diagnose and fix it, and do not paper over a red with an override.

Then commit. The message takes its text as a POSITIONAL argument:

  gsd-sdk query commit "fix(health): assert extended.conf destructive switches (CR-01, WR-01)" \
    --files scripts/quick-health-check.sh \
    .planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/

Do NOT write to `.planning/STATE.md` or `.planning/ROADMAP.md`. Quick tasks are not tracked in the
roadmap, and the `state.*`/`roadmap.*` SDK verbs corrupted unrelated lines three times during phase 4.
If a STATE.md write becomes genuinely necessary, `git diff .planning/STATE.md` must be read line by
line before the commit.

Confirm the commit contains no `extended.conf` file and no new file under `stacks/`.
  </action>
  <verify>
    <automated>bash scripts/quick-health-check.sh; test $? -eq 0 && test "$(git -C . show --name-only --format= HEAD | grep -c 'extended.conf')" -eq 0 && test "$(git -C . status --porcelain | wc -l | tr -d ' ')" -eq 0</automated>
  </verify>
  <done>
`bash scripts/quick-health-check.sh` exits 0 with the new assertion green and its identifying line
quoted in the artifact. The commit landed, carries no `extended.conf`, and the working tree is clean.
STATE.md and ROADMAP.md are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sabnzbd container → health-check transcript | File content crosses out of a secrets-bearing config |
| local working tree → public GitHub repo | Anything committed here is world-readable, permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a2y-01 | Information disclosure | `extended.conf` five `*ArrApiKey` fields | mitigate | Never vendor the file and never add it to the D-13 drift set; match remote-side and emit only labels plus the two non-secret switch values. Task 2 verify greps the artifact for all five key names and requires 0. |
| T-a2y-02 | Tampering | live `/config/extended.conf` | mitigate | Fixtures go to the container's `/tmp` only; sha256 asserted open and close of Task 2; cleanup under `trap`. |
| T-a2y-03 | Denial of service | `quick-health-check.sh`, the estate's only test suite | mitigate | `bash -n` in Task 1; a full green run required in Task 3; every new remote call bounded by `$REMOTE_TIMEOUT` with its own 124 branch. |
| T-a2y-04 | Repudiation | a green tick that measured nothing | mitigate | Fail-closed on every "could not look" branch; overrides can only force red; all four failure branches driven in Task 2 rather than read. |
| T-a2y-SC | Tampering | package installs | accept | No package manager runs in this plan; nothing is installed on host or workstation. |
</threat_model>

<verification>
- `bash -n scripts/quick-health-check.sh` passes.
- `grep -c 'EXIT-CODE BEHAVIOUR CHANGED'` returns 7.
- All four failure branches have an executed transcript, each with a distinct verdict and exit 1.
- `bash scripts/quick-health-check.sh` exits 0 with the new assertion green.
- `git log -p -1` contains no `extended.conf` content and no new file under `stacks/`.
- `stacks/selfhosted/arrs/sabnzbd/audio.bash` is byte-unchanged, so the D-13 drift block stays green.
</verification>

<success_criteria>
CR-01 and WR-01 are closed at the guard layer: the two `extended.conf` values that disarm the
`rm -rf` branches in `audio.bash` are asserted on every routine run of the estate's health check, a
wrong or unreadable value is red and exits 1, "could not look" never shares a verdict with "nothing is
wrong", every failure branch has been driven rather than read, and the script still exits 0 on the
healthy estate — with no byte of a secrets-bearing file copied into a public repository.
</success_criteria>

<output>
Create `.planning/quick/260914-a2y-assert-extended-conf-guards-for-audio-ba/260914-a2y-SUMMARY.md` when done
</output>
