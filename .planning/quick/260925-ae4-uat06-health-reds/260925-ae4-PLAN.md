---
phase: quick/260925-ae4-uat06-health-reds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-music-freeze.sh
  - .planning/quick/260925-ae4-uat06-health-reds/artifacts/
autonomous: true
requirements:
  - gap-1-config-drift
  - gap-2-pinned-count
user_setup: []

must_haves:
  truths:
    - "scripts/check-music-freeze.sh's §2 interpolated-host-path pin equals the measured inventory (13), so §2's pass is a claim about a fully-read tree"
    - "The 13th interpolated line is DECLARED in band beside the constant — which line, which file, which commit added it, and why it is safe"
    - "The beets config the container actually bind-mounts is byte-identical to the repo copy, so a grep of the RUNNING config returns plan 06-07's retraction and not the superseded wrong explanation"
    - "quick-health-check.sh's vendored-file drift block reports all four files matching"
    - "quick-health-check.sh's Music freeze harness block reports ✅ Intact"
    - "quick-health-check.sh's consumers block still reports CONF-04 MEASURED AND OPEN at exit 3 — the by-design ⚠ is untouched"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "the moved pin and its in-band declaration"
      contains: "DECLARED_INTERP_EXPECTED:-13"
    - path: ".planning/quick/260925-ae4-uat06-health-reds/artifacts/qhc-after.txt"
      provides: "the captured acceptance run, kept as evidence rather than eyeballed"
  key_links:
    - from: "scripts/check-music-freeze.sh (DECLARED_INTERP_EXPECTED)"
      to: "stacks/selfhosted/**/*.yaml interpolated volume lines"
      via: "the script's own driving grep, counted on BOTH the workstation repo and the LXC 100 checkout"
      pattern: "DECLARED_INTERP_EXPECTED:-13"
    - from: "/mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml (host checkout, clean)"
      to: "/mnt/fast/appdata/arrs/beets/config/config.yaml (the bind-mounted copy)"
      via: "host-side cp, owner/mode preserved by writing through the existing inode"
      pattern: "661c729738a12be61d94dcf0cf0bfb6b0cdaa9c996d2ed9fffb9bc494394668f"
---

<objective>
Close the two gaps diagnosed in `.planning/phases/06-tagger-configuration-and-dry-run/06-UAT.md`
§ Gaps. Both were introduced OUTSIDE Phase 6 and both surface as reds in
`scripts/quick-health-check.sh`.

- **Gap 2 (major, repo change):** `DECLARED_INTERP_EXPECTED` is pinned at 12; the tree measures 13.
  Commit `2f19870` added the 13th line without moving the pin in the same commit. This is
  CONVENTIONS.md convention 5 firing exactly as designed — the instrument is right and the commit
  was wrong.
- **Gap 1 (minor, deploy action):** the deployed beets config at
  `/mnt/fast/appdata/arrs/beets/config/config.yaml` is a revision behind the repo. The diff is
  comments only, so this is not a config-correctness fix — it is that plan 06-07's retraction never
  reached the running copy, and 06-07 wrote that retraction *specifically* so a grep for it returns
  the retraction.

Purpose: the estate's single health entry point has carried two unrelated reds for a week. That is
precisely how an operator learns to tune out a red — including the one red that matters.

Output: one repo commit (pin + in-band declaration, together), two refreshed deployed copies on
LXC 100, and a captured acceptance run with the three block verdicts ASSERTED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CONVENTIONS.md
@CLAUDE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-UAT.md
@scripts/check-music-freeze.sh
</context>

<measured_state>
Every number below was MEASURED during planning on 2026-09-25, not inferred. Where a briefing
claim was measured false, that is stated. Re-measure before leaning on any of it.

**Interpolated-line inventory — 13 on BOTH trees, identically distributed.**
Driving grep is the script's own:
`/usr/bin/grep -rnE '^[[:space:]]*-[[:space:]]*"?\$\{?[A-Za-z_][^:]*:/' stacks --include='*.yaml' --include='*.yml'`

| file | lines |
|---|---|
| `stacks/selfhosted/arrs/listenarr.yaml` | 3 |
| `stacks/selfhosted/media/audiobookshelf.yaml` | 4 |
| `stacks/selfhosted/immich/compose.yaml` | 2 |
| `stacks/selfhosted/automation/n8n-postgres.yaml` | 1 |
| `stacks/selfhosted/monitoring/grafana.yaml` | 1 |
| `stacks/selfhosted/monitoring/prometheus.yaml` | 1 |
| `stacks/selfhosted/monitoring/node-exporter.yaml` | 1 ← the 13th, added by `2f19870` |
| **total** | **13** |

The same count and the same per-file distribution were re-measured on LXC 100's
`/mnt/fast/stacks`. So `13` is correct on both the tree a contributor runs against and the tree the
health check actually scans. The host's one extra commit did **not** add an interpolated line.

**The 13th line, read by hand:**
`      - ${APPDATA_DIR}/monitoring/node-exporter-textfile:/var/lib/node_exporter/textfile_collector:ro`
It is `:ro`, its host side is a **monitoring** appdata directory, and it does not reach
`/mnt/tank/media/Music`. §2's actual concern — a rw path reaching the library — is unaffected.

**⚠ WHERE THE FREEZE CHECK ACTUALLY RUNS — this is load-bearing and the briefing did not say it.**
`scripts/quick-health-check.sh` does **not** run the workstation's copy. It runs
`bash /mnt/fast/stacks/scripts/check-music-freeze.sh` over ssh on LXC 100, scanning the HOST
checkout's `stacks/` tree. **A repo-only edit therefore does not turn the block green.** The edited
script must reach `/mnt/fast/stacks` or gap 2 stays red.

**⚠ THE HOST CHECKOUT HAS DIVERGED — the briefing's claim that it "is at the same commit as local
HEAD" is MEASURED FALSE.**

| | commit |
|---|---|
| workstation `HEAD` (== `origin/main`) | `099bd5f` |
| LXC 100 `/mnt/fast/stacks` `HEAD` (branch `main`, upstream `origin/main`) | `745611e` |
| common ancestor | `c406259` |

`745611e` is **"feat(neocortex-memory): a private tailnet route via tsbridge (TODO-317)"** — a real
commit that exists **only on the host** and is **not pushed**. `git cat-file -t 745611e` fails in
the workstation repo. So the host is 1 ahead and 1 behind: **`git pull --ff-only` cannot succeed.**
The host working tree is otherwise **clean** (`git status --porcelain` empty).

Resolving that divergence is **OUT OF SCOPE** — it is someone else's unpushed work and rebasing or
merging it is not this quick task's call. See task 3 for the mechanism chosen instead, and surface
the divergence to the operator rather than silently fixing it.

**Deploy targets, owner and mode as they stand today:**

| path | owner:group | mode | sha256 now |
|---|---|---|---|
| `/mnt/fast/appdata/arrs/beets/config/config.yaml` | `apps:apps` | `660` | `96a7c622…e3e1f7` (stale) |
| `/mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml` | `root:apps` | `644` | `661c7297…4668f` (correct) |
| `/mnt/fast/stacks/scripts/check-music-freeze.sh` | `root:apps` | `755` | `b072301…e5d92` (== workstation copy today) |

The host checkout's `config.yaml` working-tree copy already hashes `661c7297…`, and the tree is
clean, so `git show HEAD:…` — which is the half the drift block actually reads — yields the same
bytes. **Copy from the host checkout, host-side.** No mac→linux transfer of config bytes is needed.

**`tank` ACL constraints do not apply to any path in this plan.** All of them are on `fast`.
The `rsync -a` `EPERM` trap is a `tank`-only problem.

**⚠ THE OVERALL EXIT CODE WILL STILL BE 1, AND THAT IS CORRECT.** The consumers fold-in's `exit 3`
arm sets `EXIT_CODE=1` deliberately; its own comment calls this "the load-bearing half" and says
"Do not add an override". After both gaps close, CONF-04 pending is the **sole** remaining holder of
the non-zero exit. **Acceptance is therefore block-level, not exit-code-level.** Do not chase
exit 0, do not tune the consumers block out, and do not present the overall code as a CONF-04
signal.
</measured_state>

<tasks>

<task type="auto">
  <name>Task 1: Move the pin 12 → 13 and declare the 13th line in band</name>
  <files>scripts/check-music-freeze.sh</files>
  <action>
Two edits in ONE commit, per CONVENTIONS.md convention 5.

**Edit A — the constant.** Change the default in
`DECLARED_INTERP_EXPECTED="${DECLARED_INTERP_EXPECTED:-12}"` from `12` to `13`. Change nothing else
on that line; the `${VAR:-default}` form stays.

**Edit B — the in-band declaration.** The constant already carries a multi-line header comment (the
block that opens "WHY (3) IS A PINNED INVENTORY AND NOT A BLANKET REFUSAL" and runs down to the
`DECLARED_INTERP_EXPECTED` gloss). **Extend that existing block in its established voice** — do not
bolt a detached note on beside it, and do not restate what it already argues.

Two things must land inside it:

1. The sentence "Twelve such lines exist right now and none of them resolves into the library" is
   now wrong. Correct it to thirteen. Write the number **as a word**, matching the existing voice —
   the surrounding prose already spells it out, and a numeral in prose beside a pinned numeral is
   the ambiguity convention 8 exists to avoid.
2. A short declaration of the 13th line, in the terms the block's own remediation text demands:
   **which line** (`${APPDATA_DIR}/monitoring/node-exporter-textfile:/var/lib/node_exporter/textfile_collector:ro`),
   **which file** (`stacks/selfhosted/monitoring/node-exporter.yaml`), **which commit added it**
   (`2f19870`, 2026-09-18, "feat(260918-c12): textfile collector, drift timer units and Grafana
   Telegram alerting"), and **why it is safe** — it is `:ro`, its host side is a monitoring appdata
   directory, and it does not reach `/mnt/tank/media/Music`, so §2's actual concern is unaffected.

State plainly, in one clause, that `2f19870` did **not** move the pin in its own commit and that
this commit is therefore the *remediation*, not the same-commit move convention 5 asks for. A reader
who greps `2f19870` must land on an explanation that does not pretend the convention was honoured.

Per convention 12, keep it to durable rationale: **why this line is declared safe and what would
falsify that** — not a round-by-round history of the UAT that found it.

**PROHIBITED:** do not export or otherwise set the `DECLARED_INTERP_EXPECTED` environment override
to silence this. The script's own remediation arm states its only sanctioned use is making the check
REDDER, and convention 4 names this knob as the repository's one policy-governed (not
mechanism-governed) exception. Using it here would be the precise misuse both documents prohibit.

**Do not touch** `stacks/selfhosted/monitoring/node-exporter.yaml` or any other stack file. The
count is correct; the pin is what is wrong.

Before finishing, confirm by grep that no published recipe elsewhere in the repo counts tokens
inside `scripts/check-music-freeze.sh` (convention 8: writing a number into a file that something
greps moves that number). Planning measured that `quick-health-check.sh` greps this script's
**output**, never its bytes — re-confirm rather than assume, and record the recipe used.
  </action>
  <verify>
    <automated>PIN=$(/usr/bin/grep -oE 'DECLARED_INTERP_EXPECTED:-[0-9]+' scripts/check-music-freeze.sh | /usr/bin/grep -oE '[0-9]+$') && CNT=$(/usr/bin/grep -rnE '^[[:space:]]*-[[:space:]]*"?\$\{?[A-Za-z_][^:]*:/' stacks --include='*.yaml' --include='*.yml' | /usr/bin/grep -c .) && echo "PIN=$PIN CNT=$CNT" && test "$PIN" = "13" && test "$CNT" = "13" && /usr/bin/grep -q '2f19870' scripts/check-music-freeze.sh && /usr/bin/grep -q 'node-exporter-textfile' scripts/check-music-freeze.sh && ! /usr/bin/grep -q 'Twelve such lines exist' scripts/check-music-freeze.sh && echo PIN-DECLARATION-OK</automated>
  </verify>
  <done>
`DECLARED_INTERP_EXPECTED` defaults to 13; the tree measures 13; the header names the node-exporter
line, its file and commit `2f19870`, and declares it safe; the stale "Twelve" sentence is gone. Both
edits are in a single commit. No stack file was modified. The environment override was not used.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh the deployed beets config on LXC 100 (gap 1)</name>
  <files>/mnt/fast/appdata/arrs/beets/config/config.yaml (on LXC 100 — NOT a repo file)</files>
  <action>
This is a **deploy action, not a repo edit.** Nothing in the repository changes in this task.

Bound every remote command **Linux-side** (`timeout`), per convention 2 — macOS has no GNU
`timeout`, and `timeout N cmd | wc -l` silently exits 0, so any remote pipeline needs
`set -o pipefail` and the ssh return code must be read.

**Step 1 — preserve the old bytes before overwriting.** Pull the current deployed file down to
`.planning/quick/260925-ae4-uat06-health-reds/artifacts/config.yaml.deployed-pre` on the workstation
(`ssh … cat` into the file). Keep the backup on the workstation, not on the host: a stray `.bak`
beside a config the container bind-mounts is untidy, and the workstation copy is fully sufficient
for reversal. Assert the downloaded copy hashes `96a7c622779f95a13cd858c09b34072bcfc01405fecccd00b361ecdb5cb3e1f7`;
if it does not, STOP — the deployed state moved since planning and the diagnosis must be re-read
before anything is overwritten.

**Step 2 — copy, host-side, from the host checkout.** On LXC 100:
`cp /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml /mnt/fast/appdata/arrs/beets/config/config.yaml`

Source it from the **host checkout**, not from the workstation. That checkout's working tree is
clean and its copy already hashes `661c7297…4668f`, which is exactly the bytes the drift block reads
via `git show HEAD:…`. Copying from the same tree the comparison reads removes a whole class of
"which copy did I actually ship" error.

Plain `cp` is correct here and the mode/owner are preserved **by mechanism**: the destination
already exists, so `cp` writes through the existing inode and does not chown or chmod it. Do **not**
reach for `rsync -a` — and note the `tank` `EPERM` trap does not apply on this path anyway, because
this is `fast`.

**Step 3 — assert, do not report** (convention 3). Re-read the deployed file's sha256 and its
`stat -c '%U:%G %a'` and assert both: sha256 == `661c7297…4668f`, owner/mode == `apps:apps 660`.
A printed value that nothing compares cannot fail the run.

**Do NOT restart or recreate the beets-flask container.** It bind-mounts this path `:ro` and the
change is comments-only; there is no reason to bounce it and a restart is a side effect this task
has not justified.
  </action>
  <verify>
    <automated>ssh -n -o BatchMode=yes -o ConnectTimeout=8 root@172.16.1.159 "timeout 60 bash -c 'set -o pipefail; H=\$(sha256sum /mnt/fast/appdata/arrs/beets/config/config.yaml | cut -d\" \" -f1); S=\$(stat -c \"%U:%G %a\" /mnt/fast/appdata/arrs/beets/config/config.yaml); echo \"H=\$H S=\$S\"; [ \"\$H\" = 661c729738a12be61d94dcf0cf0bfb6b0cdaa9c996d2ed9fffb9bc494394668f ] && [ \"\$S\" = \"apps:apps 660\" ]'" && echo DEPLOYED-CONFIG-OK</automated>
  </verify>
  <done>
The deployed config hashes `661c7297…4668f`, still owned `apps:apps` mode `660`. The pre-change
bytes are preserved on the workstation under the quick task's `artifacts/`. No container was
restarted. No repo file changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy the edited freeze script to LXC 100, then assert the acceptance run</name>
  <files>/mnt/fast/stacks/scripts/check-music-freeze.sh (on LXC 100), .planning/quick/260925-ae4-uat06-health-reds/artifacts/qhc-after.txt</files>
  <action>
**Part A — get the edited script onto the host.**

`quick-health-check.sh` runs `/mnt/fast/stacks/scripts/check-music-freeze.sh` over ssh; the
workstation copy is never executed by it. So task 1's edit must reach the host.

**Mechanism: `scp` the single file.** Do **not** `git pull` on the host and do **not** rebase or
merge it. Planning measured that `/mnt/fast/stacks` sits on `main` at `745611e`
("feat(neocortex-memory): a private tailnet route via tsbridge (TODO-317)"), an **unpushed commit
that does not exist in the workstation repo**, diverged from `origin/main` at common ancestor
`c406259`. `git pull --ff-only` cannot succeed, and rewriting or merging someone else's unpushed
work is not this task's call.

Before copying, record the target's current `stat -c '%U:%G %a'` (planning measured `root:apps 755`).
`scp` recreates the destination file, so owner and mode can move. **After the copy, re-assert
`root:apps 755` and restore it with `chown`/`chmod` if it moved.** A deploy that silently changes a
file's mode is a change nobody asked for.

Then assert sha256 equality between the workstation copy and the host copy. That is the invariant
plan `06-51` established ("6 of 6 instrument scripts sha256-MATCH between repo and host") and this
task must preserve it, not just satisfy the health check.

**Record the cost honestly in the summary, and do not hide it:** this leaves the host checkout's
working tree carrying **one modified file** relative to its own `HEAD`. That dirt is transient and
self-healing — once this task's commit reaches `origin/main` and the operator resolves the
divergence, the working file already matches the committed bytes and git reads clean again. The
divergence itself is **an operator action this task deliberately did not take** and must be
surfaced as such, by commit hash and subject.

**Part B — the acceptance run, ASSERTED.**

Run from the workstation, capturing to an artifact rather than eyeballing scrollback:

`bash scripts/quick-health-check.sh > .planning/quick/260925-ae4-uat06-health-reds/artifacts/qhc-after.txt 2>&1`

Capture the exit code immediately, before any pipe. Then assert, per convention 3 — and use
`/usr/bin/grep` by absolute path throughout, per convention 9, because the operator's zsh aliases
`grep` to `ugrep` and a shell function answering "no matches" when it was never consulted is a false
green.

Three block verdicts, each asserted as a **positive token AND a negative token**. The positive half
is the anti-vacuous guard: a zero count of a failure string is indistinguishable from a block that
never ran, which is convention 8's sub-rule 3 and convention 1's "could not look" arriving in a new
costume.

| block | must be PRESENT | must be ABSENT |
|---|---|---|
| Vendored-file drift | `vendored files match (4)` | `survivor-config.yaml DRIFTED` |
| Music freeze harness | `Music freeze harness: ✅ Intact` | `interpolated-host-path inventory MOVED` |
| Music consumers audit | `CONF-04 MEASURED AND OPEN` | — (this ⚠ is BY DESIGN and stays) |

Before any of those, assert the captured file is **non-empty**. An empty capture is COULD NOT LOOK,
a distinct verdict from "nothing is wrong" (convention 1) — report it as such and do not proceed to
the token assertions.

`REMOTE_TIMEOUT` defaults to 120 s and the run makes several bounded ssh round trips; give it room
rather than assuming a hang. If any block reports `⚠️ UNKNOWN`, that is COULD NOT LOOK, not a pass
and not a failure — report it in those words.

**Part C — report the overall exit code honestly.**

Expect the run to **still exit 1**. The consumers fold-in's `exit 3` arm sets `EXIT_CODE=1`
deliberately — its own comment calls this "the load-bearing half" and forbids an override. After
both gaps close, **CONF-04 pending is the sole remaining holder of the non-zero exit**. State that
plainly: which blocks went green, and exactly what still holds the overall code non-zero.

Do **not** present the overall exit code as a CONF-04 signal, do not tune the consumers block out,
and do not tick anything. The hard exclusions stand: `CONF-04` stays unticked,
`.planning/REQUIREMENTS.md` is untouched, the ROADMAP Phase 6 status row stays "In Progress", the
ZFS snapshot `tank/media/Music@pre-06-41-conf04-reprobe` is neither released nor destroyed, and
`scripts/check-music-consumers.sh` is not modified.
  </action>
  <verify>
    <automated>F=.planning/quick/260925-ae4-uat06-health-reds/artifacts/qhc-after.txt && test -s "$F" && /usr/bin/grep -q 'vendored files match (4)' "$F" && test "$(/usr/bin/grep -c 'survivor-config.yaml DRIFTED' "$F")" = "0" && /usr/bin/grep -q 'Music freeze harness: ✅ Intact' "$F" && test "$(/usr/bin/grep -c 'interpolated-host-path inventory MOVED' "$F")" = "0" && /usr/bin/grep -q 'CONF-04 MEASURED AND OPEN' "$F" && echo ACCEPTANCE-OK</automated>
  </verify>
  <done>
The host copy of `check-music-freeze.sh` is sha256-identical to the workstation copy and still
`root:apps 755`. The captured acceptance run is non-empty and carries all three positive tokens and
neither failure token. The summary states the overall exit code, names CONF-04 pending as its sole
remaining cause, and surfaces the unresolved host-checkout divergence at `745611e` as an operator
action not taken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 (ssh, root) | Every task 2/3 command crosses it; all inputs are operator-authored, none are network-derived |
| repo → running container config | Task 2 writes a file beets-flask bind-mounts `:ro` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ae4-01 | Tampering | `/mnt/fast/appdata/arrs/beets/config/config.yaml` — wrong bytes shipped to a live config | mitigate | Source from the host checkout (clean tree, already `661c7297…`); assert the post-copy sha256 against that literal; pre-change bytes preserved on the workstation before the write |
| T-ae4-02 | Elevation of Privilege | same file — a deploy that silently widens owner/mode | mitigate | `cp` through the existing inode preserves both by mechanism; post-copy `stat` asserted `apps:apps 660` |
| T-ae4-03 | Elevation of Privilege | `/mnt/fast/stacks/scripts/check-music-freeze.sh` — `scp` recreates the file and can move its mode | mitigate | Pre-copy `stat` recorded; post-copy `root:apps 755` asserted and restored if moved |
| T-ae4-04 | Repudiation | a health check silently made green rather than actually green | mitigate | The override is prohibited by name in task 1; every acceptance assertion pairs a negative token with a positive one so a block that never ran cannot pass |
| T-ae4-05 | Denial of Service | unbounded ssh command hanging the run | mitigate | `timeout` inside every remote string (convention 2); `set -o pipefail` on any remote pipeline; ssh RC read before piping |
| T-ae4-06 | Tampering | host git history rewritten to force a deploy | accept-by-refusal | No `pull`/`rebase`/`merge` is issued on `/mnt/fast/stacks`; the unpushed `745611e` divergence is surfaced to the operator, not resolved |
| T-ae4-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager install in this plan |
</threat_model>

<verification>
Single acceptance test, from the workstation:

    bash scripts/quick-health-check.sh

Asserted (not eyeballed), against the captured
`.planning/quick/260925-ae4-uat06-health-reds/artifacts/qhc-after.txt`:

1. The file is non-empty — an empty capture is COULD NOT LOOK, not a pass.
2. Vendored-file drift block: `vendored files match (4)` present **and**
   `survivor-config.yaml DRIFTED` absent.
3. Music freeze harness: `Music freeze harness: ✅ Intact` present **and**
   `interpolated-host-path inventory MOVED` absent.
4. Music consumers audit: `CONF-04 MEASURED AND OPEN` present — the by-design ⚠ and its exit 3
   survive untouched.

The overall exit code is expected to remain **1**, held there solely by CONF-04 pending. That is the
correct and deliberate behaviour of the consumers `exit 3` arm and must be reported as such, never
tuned out and never read as a CONF-04 signal.
</verification>

<success_criteria>
- `DECLARED_INTERP_EXPECTED` defaults to 13; the workstation tree and the LXC 100 checkout both
  measure 13.
- The 13th line is declared in band beside the constant — line, file, commit `2f19870`, and why it
  is safe — as an extension of the existing header, in its voice.
- The `DECLARED_INTERP_EXPECTED` environment override was **not** used anywhere.
- `/mnt/fast/appdata/arrs/beets/config/config.yaml` hashes `661c7297…4668f`, owner `apps:apps`,
  mode `660`. No container restarted.
- `/mnt/fast/stacks/scripts/check-music-freeze.sh` is sha256-identical to the workstation copy,
  owner `root:apps`, mode `755`.
- The two named blocks assert green; the consumers block still asserts its by-design pending.
- The overall exit code is reported honestly with its sole cause named.
- The unresolved host-checkout divergence at `745611e` is surfaced to the operator as an action
  this task deliberately did not take.
- Hard exclusions respected: no `CONF-04` tick, `.planning/REQUIREMENTS.md` untouched, ROADMAP
  Phase 6 status row untouched, `tank/media/Music@pre-06-41-conf04-reprobe` neither released nor
  destroyed, `scripts/check-music-consumers.sh` unmodified, no Phase 6 plans created.
</success_criteria>

<output>
Create `.planning/quick/260925-ae4-uat06-health-reds/SUMMARY.md` when done.
</output>
