---
phase: quick-260916-062
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md
  - .planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [TAGR-04]
must_haves:
  truths:
    - "04-VERIFICATION.md's frontmatter carries an overrides: entry bound verbatim to the existing gaps[0].truth string, with accepted_by/accepted_at left as literal placeholders"
    - "status: gaps_found and overrides_applied: 0 are unchanged, and the entire document body (including the prose overrides: draft near line 211) is byte-identical to HEAD"
    - "ROADMAP § Phase 4 records 04-18 and 04-19 as PLANNED BUT DELIBERATELY NOT EXECUTED, dated 2026-09-16, rows still present and still unticked"
    - "STATE.md's Current Position and last_activity say the same thing, and git diff shows only the intended lines changed"
    - "The four external review outputs survive the session as a committed file, reproduced verbatim, labelled by tool and model"
    - "No credential reaches the public repo, proven by a screen that was shown to catch a planted positive control"
    - "The estate is not touched: no ssh, no docker, no container command runs at any point"
  artifacts:
    - path: ".planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md"
      provides: "Durable verbatim record of the four external model reviews of plan 04-18"
      contains: "## Review"
    - path: ".planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md"
      provides: "Unsigned criterion-3 override in frontmatter"
      contains: "overrides:"
  key_links:
    - from: ".planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md (overrides[0].must_have)"
      to: ".planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md (gaps[0].truth)"
      via: "verbatim string equality, asserted by a YAML parse"
      pattern: "overrides"
    - from: ".planning/ROADMAP.md § Phase 4 Wave 13/14 rows"
      to: ".planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md"
      via: "explicit filename pointer in the not-executed note"
      pattern: "04-18-EXTERNAL-REVIEWS"
---

<objective>
Prepare — but do not sign — the criterion-3 override in `04-VERIFICATION.md`, record plans 04-18
and 04-19 as deliberately not executed, and preserve the four external model reviews that justify
that decision before the session scratchpad evaporates.

Purpose: Phase 4's criterion 3 has been OPEN across two observation windows and five real music
jobs. A third capture design was built (04-17) and all 14 synthetic controls passed, but external
review of the plan that would arm it (04-18) found roughly 30 defects — including a judge binary
that self-reports every PASS condition. A PASS from that instrument would not have been
trustworthy, so window 3 is being closed unrun and the disposition handed to the operator as an
unsigned override. This plan records that honestly and preserves the evidence it rests on.

Output: one new file (`04-18-EXTERNAL-REVIEWS.md`), a frontmatter-only append to
`04-VERIFICATION.md`, and honest not-executed records in `ROADMAP.md` and `STATE.md`.

**THIS IS DOCUMENTATION ONLY. THE ESTATE MUST NOT BE TOUCHED.** No `ssh`, no `docker`, no
container command, at any point, in any task, including verification. `nscript_enable` stays 0 and
`direct_unpack` stays 1 because nothing in this plan goes near them. If a step seems to require
reading live estate state, it is the wrong step — stop and report instead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@./CLAUDE.md

@.planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md

**Structural facts about `04-VERIFICATION.md` that will bite you if you skip them:**

- The file contains `---` at column 1 on **three** lines: 1, 68 and 224. The YAML frontmatter is
  between the **first and second** only (lines 1–68). Line 224 is a prose horizontal rule above
  the sign-off block.
- A **prose draft** of an `overrides:` block sits at column 1 near line 211, inside a
  ```` ```yaml ```` fence in § Gaps Summary. It is illustrative text, **not** frontmatter. It must
  be left byte-identical. Any check that greps `^overrides:` across the whole file will match it
  and be wrong — every gate in this plan is frontmatter-scoped by parsing, not by grep.
- The string the override must bind to is `gaps[0].truth` at line 15. It must be copied
  **verbatim**, not retyped.

**Shell variable used throughout (session scratchpad — does not survive this session):**

```
SCRATCH=/private/tmp/claude-501/-Users-damian-Development-damianflynn-selfhost-stacks/e978430b-edf0-415c-92ac-f2b6b679a5d1/scratchpad
```

Four files there are the evidence: `pi-0418.txt`, `pi-gpt55.txt`, `pi-kimi.txt`,
`copilot-0418.txt`. A fifth, `codex-0418.txt`, is from an earlier review round and is **out of
scope** — do not include it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the four external reviews as a committed, verbatim, credential-screened record</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md</files>
  <action>
Create `.planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md`. This file is the
only durable copy of the evidence the D1 override rests on — the scratchpad does not survive the
session. Build it first, because tasks 2 and 3 both point at it by filename.

Read all four source files in full from `$SCRATCH` before writing anything.

**Preamble** (before the first `## Review` heading). State plainly:

- These are **external-model outputs reproduced verbatim for the record**. They are **model
  output, not verified fact** — no finding below has been independently confirmed against the text
  of `04-18-PLAN.md` by this repo's own process, and at least one same-family reviewer has been
  confidently wrong before on this project.
- They reviewed `.planning/phases/04-collapse-to-one-tagger/04-18-PLAN.md` on 2026-09-15.
- Be accurate about the count. There are **four external outputs spanning three non-Anthropic
  model families** (Google Gemini, GPT-5.5 — twice, through two different tools — and Kimi-K3).
  With the in-session Claude review, which is not reproduced here, that is four model families
  total. Do not write "four external families"; that would be the wrong number.
- Why they matter: they are the stated reason window 3 was closed unrun, so the override in
  `04-VERIFICATION.md` cites this file.

**One section per output**, each an `## Review N — <tool>, <model>` heading, numbered 1–4, naming
the tool and the model explicitly:

1. `## Review 1 — pi, Google Gemini` — body of `$SCRATCH/pi-0418.txt`, verbatim, complete.
2. `## Review 2 — pi, github-copilot gpt-5.5` — body of `$SCRATCH/pi-gpt55.txt`, verbatim, complete.
3. `## Review 3 — pi, ollama-cloud kimi-k3` — body of `$SCRATCH/pi-kimi.txt`, verbatim, complete.
4. `## Review 4 — GitHub Copilot CLI, gpt-5.5` — body of `$SCRATCH/copilot-0418.txt`, **trimmed**,
   see below.

Reproduce bodies as plain markdown, **not** inside code fences. This is safe and was checked: none
of the four sources contains a ```` ``` ```` fence, a `---` at column 1, or a `#` heading at
column 1, so nothing can collide with this document's own structure or be mistaken for
frontmatter.

**The `copilot-0418.txt` trim.** The file is 109 lines and its findings are buried. Correct the
brief's wording while you are here: the chatter is **not** ANSI escape sequences — `grep` for
`\x1b` returns zero. It is a UTF-8 tool-call transcript drawn with box-drawing and bullet glyphs
(`●`, `│`, `└`, `/ Search (grep)`). Keep only the numbered findings block: from the line beginning
`1. L455-L458` through to the line immediately before the one beginning `Changes    +0 -0`.

Directly under the Review 4 heading, add a one-line note saying exactly what was trimmed and why,
naming both ends: (i) a leading tool-call transcript of `Read`/`Search (grep)` calls, dropped as
noise; (ii) a trailing telemetry footer (`Changes`, `AI Credits`, `Tokens`, `Resume`), dropped
because it carries a `copilot --resume=` session identifier that has no place in a public repo.
Do not reproduce that identifier anywhere, including in the note.

**Do not edit, summarise, reorder, renumber, correct or "clean up" any finding.** Verbatim means
verbatim, glyphs included.
  </action>
  <verify>
    <automated>
SCRATCH=/private/tmp/claude-501/-Users-damian-Development-damianflynn-selfhost-stacks/e978430b-edf0-415c-92ac-f2b6b679a5d1/scratchpad
T=.planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md

# Gate A — exactly four labelled review sections
echo "SECTIONS=$(grep -cE '^## Review [1-4] — ' "$T")"   # must be 4

# Gate B — verbatim fidelity of every non-blank source line, and the trim actually happened
python3 - "$SCRATCH" "$T" <<'PY'
import sys, pathlib
S = pathlib.Path(sys.argv[1]); T = pathlib.Path(sys.argv[2])
tgt = set(T.read_text().splitlines())
missing = 0
for name in ("pi-0418.txt", "pi-gpt55.txt", "pi-kimi.txt"):
    for ln in (S / name).read_text().splitlines():
        if ln.strip() and ln not in tgt:
            print("MISSING", name, repr(ln)[:100]); missing += 1
cp = (S / "copilot-0418.txt").read_text().splitlines()
a = next(i for i, l in enumerate(cp) if l.startswith("1. L455-L458"))
b = next(i for i, l in enumerate(cp) if l.startswith("Changes "))
for ln in cp[a:b]:
    if ln.strip() and ln not in tgt:
        print("MISSING copilot", repr(ln)[:100]); missing += 1
print("MISSING_TOTAL=", missing)
sys.exit(1 if missing else 0)
PY
echo "FIDELITY_RC=$?"   # must be 0, with MISSING_TOTAL= 0

# Gate C — the trimmed material is genuinely absent (chatter, telemetry, session id)
echo "CHATTER=$(grep -c 'Search (grep)' "$T")"                 # must be 0
echo "TELEMETRY=$(grep -cE '^(Changes|AI Credits|Tokens|Resume) ' "$T")"  # must be 0
echo "RESUMEID=$(grep -c -- '--resume=' "$T")"                 # must be 0

# Gate D — trim is disclosed, and codex was not pulled in
echo "TRIMNOTE=$(grep -ci 'trimmed' "$T")"                     # must be >= 1
echo "CODEX=$(grep -ci 'codex' "$T")"                          # must be 0

# Gate E — the estate was not touched by this task
echo "ESTATE=$(grep -cE '(^|[^a-z])(ssh|docker) ' "$T")"       # must be 0 (no estate commands authored)
    </automated>
  </verify>
  <done>
`04-18-EXTERNAL-REVIEWS.md` exists with SECTIONS=4, MISSING_TOTAL=0 and FIDELITY_RC=0;
CHATTER/TELEMETRY/RESUMEID/CODEX/ESTATE all 0; TRIMNOTE >= 1. Every non-blank line of the three
`pi` outputs and of the kept `copilot` findings block is present byte-for-byte.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the UNSIGNED override to 04-VERIFICATION.md's frontmatter, body untouched</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md</files>
  <action>
Append one `overrides:` key to the **frontmatter** of
`.planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md`, immediately after
`human_verification: []` (line 67) and before the closing `---` at line 68.

**Use the Edit tool.** Do not rewrite the file, do not use a heredoc, and do not use any
`gsd-sdk` verb that writes to this file.

**The agent must NOT sign this override.** `accepted_by` and `accepted_at` stay as the literal
placeholder strings `"<developer name>"` and `"<ISO timestamp>"`. Do not substitute a name, a
date, a timestamp, an email, or any wording implying the operator has accepted anything.
`status: gaps_found` and `overrides_applied: 0` stay exactly as they are — the operator flips both
when they sign. Nothing else in the frontmatter changes.

Copy `gaps[0].truth` (line 15) into `must_have` **verbatim**, by copy, not by retyping. A parser
asserts equality in `<verify>`; a single character of drift fails the task.

The `reason:` must state the real rationale, which is **not** what 04-19 assumed. Use a `>` folded
scalar, blank-line-separated so (a)–(d) stay distinct paragraphs, all lines at the same
indentation (extra indent inside a folded scalar is preserved literally and will look wrong). Keep
each parenthesised label and each short token named below on a single source line, so folding
cannot split it. Content, in this order:

- An opening sentence: accepting the threefold unanimous side-effect evidence in place of the byte
  proof, after three distinct capture designs were built, measured and exhausted.
- **(a)** Three capture designs built and measured exhausted: the destination-tree poll (window
  1), the incomplete-tree poll (window 2), and SABnzbd's own `pp` notification hook (window 3 —
  built and self-tested, but never armed).
- **(b)** Five real music jobs across two observation windows all returned
  `STATUS=UNPROVEN reason=no-attributed-pre`; not one produced a valid PRE-HOOK snapshot.
- **(c)** Side-effect evidence threefold and unanimous across those five jobs: 0 `library.blb`,
  0 new `.bak` beyond the pre-declared SABnzbd exception, 0 `beets.log`, 0
  `SUCCESS: Matched with beets`, and `extended.conf` provably never written.
- **(d)** Window 3 was **deliberately NOT run**. Plan 04-17 built the instrument and all 14
  synthetic controls passed, but four independent AI model families (Claude, Google Gemini,
  GPT-5.5, Kimi-K3) reviewing plan 04-18 found roughly 30 defects between them — including an
  unverified judge binary that self-reports every PASS condition, an OPEN branch that can swallow
  a FAIL, and a restore contract that is count-checked rather than diffed and so could leave the
  estate re-armed. A PASS from that instrument would not have been trustworthy, which is the
  entire reason for running it. The window was therefore closed unrun rather than run for the
  appearance of measurement. Cite the record by path:
  `.planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md`.
- A closing sentence: this override is **UNSIGNED** — prepared for the operator, not taken by the
  agent; `status` stays `gaps_found` and `overrides_applied` stays 0 until the operator fills
  `accepted_by` and `accepted_at`.

**The prose `overrides:` draft near line 211 must be left byte-identical**, along with every other
byte of the document body. The verify proves this by comparing a hash of everything after the
frontmatter against `HEAD`.
  </action>
  <verify>
    <automated>
python3 - <<'PY'
import subprocess, hashlib, sys, yaml
p = ".planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md"
new = open(p).read()
old = subprocess.run(["git", "show", "HEAD:" + p], capture_output=True, text=True).stdout

def split(t):
    assert t.startswith("---\n"), "no leading frontmatter delimiter"
    i = t.index("\n---\n", 3)
    return t[4:i + 1], t[i + 5:]

nf, nb = split(new); of, ob = split(old)
ok = True

h = lambda s: hashlib.sha256(s.encode()).hexdigest()
body_same = h(nb) == h(ob)
print("BODY_BYTE_IDENTICAL=", body_same); ok &= body_same

d = yaml.safe_load(nf); o = yaml.safe_load(of)

print("OLD_HAD_OVERRIDES=", "overrides" in o)          # must be False
ok &= "overrides" not in o

ov = d.get("overrides")
print("N_OVERRIDES=", len(ov) if ov else 0); ok &= bool(ov) and len(ov) == 1
e = ov[0]

verbatim = e["must_have"] == d["gaps"][0]["truth"] == o["gaps"][0]["truth"]
print("MUST_HAVE_VERBATIM=", verbatim); ok &= verbatim

unsigned = e["accepted_by"] == "<developer name>" and e["accepted_at"] == "<ISO timestamp>"
print("UNSIGNED=", unsigned); ok &= unsigned

print("STATUS=", repr(d["status"]), "OVERRIDES_APPLIED=", repr(d["overrides_applied"]))
ok &= d["status"] == "gaps_found" and d["overrides_applied"] == 0

rest_same = {k: v for k, v in d.items() if k != "overrides"} == o
print("ALL_OTHER_FRONTMATTER_UNCHANGED=", rest_same); ok &= rest_same

r = " ".join(str(e["reason"]).split())
need = ["(a)", "(b)", "(c)", "(d)", "window 1", "window 2", "window 3",
        "deliberately NOT run", "four independent AI model families",
        "04-18-EXTERNAL-REVIEWS.md", "UNSIGNED", "no-attributed-pre"]
for t in need:
    hit = t in r
    print("REASON_HAS", repr(t), "=", hit); ok &= hit

bad = ["accepted by the operator", "operator accepted", "Damian", "anthropic@damianflynn.com"]
for t in bad:
    hit = t in r
    print("REASON_MUST_NOT_HAVE", repr(t), "=", hit); ok &= not hit

print("TASK2_OK=", ok)
sys.exit(0 if ok else 1)
PY
echo "RC=$?"   # must be 0, and every printed gate must read as specified

# Independent confirmation that only the frontmatter region changed
git diff -U0 -- .planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md | grep -E '^@@'
    </automated>
  </verify>
  <done>
RC=0 with BODY_BYTE_IDENTICAL=True, OLD_HAD_OVERRIDES=False, N_OVERRIDES=1,
MUST_HAVE_VERBATIM=True, UNSIGNED=True, STATUS='gaps_found', OVERRIDES_APPLIED=0,
ALL_OTHER_FRONTMATTER_UNCHANGED=True, every REASON_HAS True and every REASON_MUST_NOT_HAVE False.
The single `git diff` hunk header lies inside the frontmatter (a `+` start below line 68).
  </done>
</task>

<task type="auto">
  <name>Task 3: Honest not-executed records in ROADMAP and STATE, credential-screen the diff, commit</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**3a — ROADMAP § Phase 4.** Edit `.planning/ROADMAP.md`. The two rows are at lines 633 (`04-18`)
and 637 (`04-19`), under the `**Wave 13**` and `**Wave 14**` headers.

For each row: **keep the row, keep it unticked (`- [ ]`), keep its existing text.** Append — or
add on the line directly beneath — a bold marker reading `**PLANNED BUT DELIBERATELY NOT EXECUTED
(2026-09-16)**`, followed by one line of reason and a pointer. Suggested reason text, one line
each:

- 04-18: window 3 was never armed — external review by four independent AI model families found
  roughly 30 defects in this plan, including a judge binary that self-reports every PASS
  condition; a PASS from it would not have been trustworthy. Record:
  `04-18-EXTERNAL-REVIEWS.md`.
- 04-19: superseded — its disposition step was performed directly by quick task 260916-062, which
  prepared the unsigned override without running window 3. Record: `04-18-EXTERNAL-REVIEWS.md`.

Do **not** delete either row, do **not** tick either box, and do **not** touch rows 04-14 through
04-17 (04-15/04-16/04-17 are already-known unticked-but-complete rows — that pre-existing
inconsistency is not this task's to fix). Do **not** update ROADMAP's quick-task tracking section;
quick tasks are tracked separately.

**3b — STATE.md.** Edit by hand with the **Edit tool**. This repo has a recorded trap: `gsd-sdk`
`state.*` verbs corrupt unrelated STATE.md lines. Do not use them here, for any reason.

Change exactly three regions, all in the first ~45 lines:

1. `last_updated` (line 6) — the execution timestamp, same `"YYYY-MM-DDTHH:MM:SS.000Z"` format.
2. `last_activity` (line 7) — replace with a single line recording: quick 260916-062 prepared the
   criterion-3 override **UNSIGNED** in `04-VERIFICATION.md` frontmatter (`status` stays
   `gaps_found`, `overrides_applied` stays 0 — the operator signs); plans 04-18 and 04-19 marked
   PLANNED BUT DELIBERATELY NOT EXECUTED because window 3 was never armed after four AI model
   families found ~30 defects in 04-18; record at `04-18-EXTERNAL-REVIEWS.md`; and
   `ESTATE UNTOUCHED -- nscript_enable still 0, direct_unpack still 1`.
3. The `## Current Position` block (lines 31–43) — update the `Plan:` line and the narrative so
   they say 04-18 and 04-19 are planned and deliberately not executed, window 3 never ran, and the
   disposition now sits with the operator as an unsigned override. Keep it short.

Leave the `progress:` block alone — `completed_plans: 61` is still correct, because no plan was
executed. Do not touch anything below line ~45 (the file is 1,420 lines and the tail is stale by
design).

**3c — credential screen, with a planted positive control.** This repo is **public**. SABnzbd's
API key is a bare 32-character hex string, which label-greps (`api_key|apikey|token=`) and
40+-character-run heuristics both miss. The literal value is **not available on this workstation**
— there is no local `.env` (gitignored, absent) and no `~/.claude/secrets/` entry for SABnzbd, and
reading it from the container is forbidden by this plan's estate ban. So the screen matches the
literal's **exact shape** (a bare 32-hex run), and a runtime-generated random 32-hex control
proves the screen discriminates. Record that limitation in the SUMMARY; do not work around it by
touching the estate.

Write the screen to `$SCRATCH/screen-062.sh` (scratchpad, never the repo). Two tiers:

- **Tier A (hard gate):** a bare 32-hex run not embedded in a longer hex string, i.e.
  `(^|[^0-9a-fA-F])[0-9a-fA-F]{32}([^0-9a-fA-F]|$)`; plus label-form
  `(api[_-]?key|apikey|token|password|passwd|secret|bearer)[[:space:]]*[:=][[:space:]]*\S{8,}`.
  Must return **no hits** on the staged added lines.
- **Tier B (advisory):** any `[A-Za-z0-9+/=_-]{40,}` run. Expect benign hits (this plan file
  embeds the long scratchpad path). Print them and triage each one in writing in the SUMMARY.

Generate the control with `openssl rand -hex 16` at runtime. **Never write a literal 32-hex string
into any repo file**, including this plan and the SUMMARY.

**3d — commit.** Stage exactly: `.planning/phases/04-collapse-to-one-tagger/04-18-EXTERNAL-REVIEWS.md`,
`.planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md`, `.planning/ROADMAP.md`,
`.planning/STATE.md`, and this quick task's directory. Run the screen against the staged diff
**before** committing. Commit message:

`docs(04): prepare the unsigned criterion-3 override; record window 3 as deliberately not run`

Do not modify commit `8646432` (04-17's contract amendment) or any other historical record in
`.planning/**`.
  </action>
  <verify>
    <automated>
SCRATCH=/private/tmp/claude-501/-Users-damian-Development-damianflynn-selfhost-stacks/e978430b-edf0-415c-92ac-f2b6b679a5d1/scratchpad

# Gate A — ROADMAP: both rows marked, both still present, both still unticked, pointer present
echo "MARKED=$(grep -c 'PLANNED BUT DELIBERATELY NOT EXECUTED (2026-09-16)' .planning/ROADMAP.md)"   # must be 2
echo "UNTICKED=$(grep -cE '^- \[ \] 04-1[89]-PLAN\.md' .planning/ROADMAP.md)"                        # must be 2
echo "NOT_TICKED=$(grep -cE '^- \[x\] 04-1[89]-PLAN\.md' .planning/ROADMAP.md)"                      # must be 0
echo "POINTER=$(grep -c '04-18-EXTERNAL-REVIEWS.md' .planning/ROADMAP.md)"                           # must be >= 2

# Gate B — STATE: every changed line is in the head region, nothing in the stale tail
python3 - <<'PY'
import subprocess, re, sys
d = subprocess.run(["git", "diff", "-U0", "--", ".planning/STATE.md"],
                   capture_output=True, text=True).stdout
print(d)
h = re.findall(r'^@@ -(\d+)(?:,\d+)? \+(\d+)(?:,\d+)? @@', d, re.M)
print("HUNK_STARTS=", h)
outside = [x for x in h if int(x[0]) > 50]
print("HUNKS_OUTSIDE_HEAD=", len(outside))
sys.exit(1 if outside or not h else 0)
PY
echo "STATE_DIFF_RC=$?"   # must be 0; read the printed diff and confirm only lines 6, 7 and the Current Position block changed
echo "PROGRESS_UNCHANGED=$(git diff -U0 -- .planning/STATE.md | grep -cE '^[+-] *(completed_plans|total_plans|percent|completed_phases):')"  # must be 0

# Gate C — credential screen, control first (proves it discriminates), then the real diff
CTL=$(openssl rand -hex 16)
TIER_A='(^|[^0-9a-fA-F])[0-9a-fA-F]{32}([^0-9a-fA-F]|$)|(api[_-]?key|apikey|token|password|passwd|secret|bearer)[[:space:]]*[:=][[:space:]]*[^[:space:]]{8,}'
printf 'a harmless line\nsab api key line %s\n' "$CTL" | grep -nEi "$TIER_A"
echo "CONTROL_RC=$?"   # must be 0 — the planted 32-hex control WAS caught; a non-zero here means the screen is blind and proves nothing

git diff --cached -U0 | grep -E '^\+' | grep -v '^\+\+\+' | grep -nEi "$TIER_A"
echo "TIER_A_RC=$?"    # must be 1 — no credential-shaped string in any added line

git diff --cached -U0 | grep -E '^\+' | grep -v '^\+\+\+' | grep -nE '[A-Za-z0-9+/=_-]{40,}'
echo "TIER_B_RC=$?"    # advisory: read every hit and triage it in the SUMMARY

# Gate D — the estate was never touched by this plan
echo "ESTATE_CMDS=$(git diff --cached -U0 | grep -E '^\+' | grep -v '^\+\+\+' | grep -cE '(ssh root@|docker (exec|restart|compose)|nscript_enable=1|direct_unpack=0)')"   # must be 0

# Gate E — no historical record rewritten
git diff --cached --name-only
echo "HISTORY_TOUCHED=$(git diff --cached --name-only | grep -cE '\.planning/phases/04-collapse-to-one-tagger/04-(0[1-9]|1[0-7])-(PLAN|SUMMARY)\.md|04-D12-EVIDENCE\.md')"  # must be 0
    </automated>
  </verify>
  <done>
MARKED=2, UNTICKED=2, NOT_TICKED=0, POINTER>=2. STATE_DIFF_RC=0 with every hunk start <= 50 and
PROGRESS_UNCHANGED=0, and the printed diff shows only `last_updated`, `last_activity` and the
Current Position block. CONTROL_RC=0 (screen caught the planted control) **and** TIER_A_RC=1
(staged diff clean) — both are required; TIER_A_RC=1 alone proves nothing. Tier B hits are each
triaged in writing. ESTATE_CMDS=0, HISTORY_TOUCHED=0. Commit made with the specified message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session scratchpad → public git repo | Untrusted external-model output, quoting plan text, crosses into a world-readable repo |
| agent → `04-VERIFICATION.md` frontmatter | An agent writing a field whose meaning is "a human accepted this" |
| plan text → live estate | The reviewed plan (04-18) would arm the estate; this plan must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-062-01 | Information disclosure | `04-18-EXTERNAL-REVIEWS.md` in a public repo | mitigate | Two-tier screen over staged added lines; Tier A matches a bare 32-hex run (SABnzbd API key shape) and label-form secrets; proven to discriminate by a runtime-generated 32-hex positive control that must be caught (`CONTROL_RC=0`) before the clean result (`TIER_A_RC=1`) is believed |
| T-062-02 | Information disclosure | Copilot telemetry footer | mitigate | `copilot --resume=<uuid>` session identifier trimmed and gated absent (`RESUMEID=0`), never reproduced in the trim note |
| T-062-03 | Spoofing / Repudiation | `overrides[0].accepted_by` / `accepted_at` | mitigate | Literal placeholders asserted by YAML parse (`UNSIGNED=True`); `status`/`overrides_applied` asserted unchanged; reason gated against operator-acceptance wording and the developer's name/email |
| T-062-04 | Tampering | `04-VERIFICATION.md` body, incl. the prose `overrides:` draft at ~line 211 | mitigate | Whole-body sha256 compared against `HEAD` (`BODY_BYTE_IDENTICAL=True`); every gate is frontmatter-scoped by YAML parse, never by a whole-file `^overrides:` grep that the prose draft would satisfy |
| T-062-05 | Tampering | `.planning/STATE.md` (recorded `gsd-sdk state.*` corruption trap) | mitigate | Edit tool only; `git diff -U0` hunk starts asserted <= 50 and `progress:` keys asserted unchanged |
| T-062-06 | Elevation of privilege | Live estate (`nscript_enable`, `direct_unpack`, SABnzbd config) | mitigate | Plan authorises no `ssh`/`docker`/container command in any task or verify; staged diff gated against arming strings (`ESTATE_CMDS=0`) |
| T-062-07 | Repudiation | Historical `.planning/**` records, commit `8646432` | mitigate | Staged file list gated against 04-01..04-17 plans/summaries and `04-D12-EVIDENCE.md` (`HISTORY_TOUCHED=0`) |
| T-062-SC | Tampering | package installs | accept | No package manager runs; nothing is installed |
</threat_model>

<verification>
Read the output of every gate — none of these is satisfied by "the command exited 0".

1. The override parses, binds verbatim to `gaps[0].truth`, and is **unsigned**; `status` and
   `overrides_applied` are untouched; the entire body is byte-identical to `HEAD`.
2. The four reviews are present verbatim, labelled by tool and model, with the copilot trim
   disclosed and its session identifier gone.
3. ROADMAP's 04-18 and 04-19 rows still exist, still unticked, now marked not-executed with a
   date, a reason and a pointer.
4. STATE's diff is confined to the head region and says the same thing as ROADMAP.
5. The credential screen caught its planted control **and** found nothing in the staged diff.
6. No estate command was run at any point in this plan.
</verification>

<success_criteria>
- `overrides:` exists in `04-VERIFICATION.md`'s frontmatter with placeholder `accepted_by` /
  `accepted_at`, and `status: gaps_found` / `overrides_applied: 0` are unchanged
- The document body, including the prose draft near line 211, hashes identically to `HEAD`
- `04-18-EXTERNAL-REVIEWS.md` reproduces all four outputs verbatim with zero missing non-blank
  source lines
- ROADMAP § Phase 4 and STATE.md both record 04-18/04-19 as planned-but-deliberately-not-executed,
  dated 2026-09-16, pointing at the review record
- The public-repo credential screen demonstrably discriminates and reports the staged diff clean
- `nscript_enable` is still 0 and `direct_unpack` is still 1, trivially, because nothing in this
  plan contacted the estate
</success_criteria>

<output>
Create `.planning/quick/260916-062-prepare-the-unsigned-criterion-3-overrid/260916-062-SUMMARY.md`
when done. It must record: the Tier B advisory hits and their triage; the stated limitation that
the SABnzbd API key's literal value was unavailable on this workstation so the screen matched its
shape instead; and an explicit statement that the override is unsigned and awaits the operator.
</output>
