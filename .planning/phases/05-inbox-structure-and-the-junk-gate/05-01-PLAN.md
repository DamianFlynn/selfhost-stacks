---
phase: 05-inbox-structure-and-the-junk-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md
  - host:/mnt/tank/downloads/complete/nzb/_inbox
  - host:/mnt/fast/safety/phase05
autonomous: true
requirements: [INBX-01]

must_haves:
  truths:
    - "A ZFS snapshot named tank/downloads@pre-phase5 exists, and its creation time precedes every Phase 5 move, delete and tag write (D-01, D-27)"
    - "/mnt/tank/downloads/complete/nzb/_inbox holds exactly six directories: 01-auto, 02-review, 03-asis, 04-hold, 99-quarantine, _done (D-17, INBX-01)"
    - "All six directories are owned 568:568 as read from atlantis, not from LXC 100 (D-23)"
    - "_inbox is a plain directory, not a ZFS dataset, and reports the same devid as unsorted/ and dj-mixes/ (D-18)"
    - "Moving a fixture between two _inbox directories leaves st_ino unchanged; moving the same fixture across a devid boundary changes it (D-21, criterion 1)"
    - "Nothing equivalent to the staging tree exists under /mnt/tank/media (criterion 1, second clause)"
    - "The exact inbox paths are recorded in the repo for Phase 6 to configure against (D-16), with D-19's _done policy and D-26's no-chown-per-move statement"
  artifacts:
    - path: ".planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md"
      provides: "The six exact absolute inbox paths, the devid measurement, the D-18 prohibition, the D-19 _done policy, the D-26 statement, and the D-21 inode evidence"
      contains: "99-quarantine"
    - path: "host:/mnt/fast/safety/phase05/snapshot-proof.txt"
      provides: "Proof file naming tank/downloads@pre-phase5, consumed by every later mutating tool in this phase"
      contains: "tank/downloads@pre-phase5"
    - path: "host:/mnt/fast/safety/phase05/inode-proof.txt"
      provides: "The D-21 positive control and its cross-dataset negative control, both driven"
  key_links:
    - from: "/mnt/tank/downloads/complete/nzb/_inbox"
      to: "tank/downloads dataset"
      via: "plain directory, never its own dataset"
      pattern: "same st_dev as unsorted/"
    - from: "05-INBOX-PATHS.md"
      to: "Phase 6 CONF-01..CONF-06"
      via: "recorded measured paths, not intended ones"
      pattern: "_inbox/01-auto"
---

<objective>
Take the reversibility fence, then create the staging tree, then prove the property criterion 1
actually rests on.

This plan is the first act of Phase 5. D-01 makes `tank/downloads@pre-phase5` the first thing that
happens, before anything moves, is deleted, is chowned or has a tag written — `mv` has no undo,
`chown` has no undo, beets has no `undo`, and D-10 adds an in-place tag write to the list. Every
later mutating plan in this phase refuses to act without a proof file naming that snapshot, so taking
it here is not procedural politeness: it is the mechanical precondition for the rest of the phase
(D-27).

Purpose: satisfy INBX-01 and ROADMAP criterion 1, and hand Phase 6 the measured paths (D-16) rather
than intended ones.
Output: six directories under `/mnt/tank/downloads/complete/nzb/_inbox` owned `568:568`, a snapshot
proof file, a driven inode proof with its negative control, and `05-INBOX-PATHS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Take tank/downloads@pre-phase5 and create the six inbox directories from atlantis</name>

  <read_first>
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-01, D-17, D-18, D-19, D-23, D-26, D-27
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md` sections 1, 2 and 7
    - `scripts/freeze-music-apply.sh` lines 60-160 — constants block, detect_zfs_route, zfs_exec, snapshot_exists, snapshot_creation. This is Phase 1's method and the analog for every privileged delegation in this phase.
    - `scripts/freeze-music-apply.sh` lines 225-250 — the zfs snapshot call sites
    - `.planning/research/ARCHITECTURE.md` section "Recommended Structure" — the diagram that fixes the inbox location under complete/nzb/
    - `CLAUDE.md` sections "Storage and Permissions" and "Infrastructure Warnings"
  </read_first>

  <files>
    /mnt/tank/downloads/complete/nzb/_inbox/01-auto
    /mnt/tank/downloads/complete/nzb/_inbox/02-review
    /mnt/tank/downloads/complete/nzb/_inbox/03-asis
    /mnt/tank/downloads/complete/nzb/_inbox/04-hold
    /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine
    /mnt/tank/downloads/complete/nzb/_inbox/_done
    /mnt/fast/safety/phase05/snapshot-proof.txt
  </files>

  <action>
    Run every privileged step in this task from atlantis over
    `ssh -o BatchMode=yes -o ConnectTimeout=15 root@172.16.1.158`. `zfs` does not exist on LXC 100,
    and `chown` fails there for a different reason — a sparse idmap in an unprivileged container, not
    the aclmode problem. Atlantis is the only place either works.

    STEP 1 — re-verify the devid finding. `05-PREMEASURE.md` section 1 is the largest single claim
    the phase rests on and its own Limits section flags it as most deserving a re-check. Read
    `stat -c '%d %i %u:%g'` on /mnt/tank/downloads, /mnt/tank/downloads/complete/nzb,
    /mnt/tank/downloads/complete/nzb/unsorted, /mnt/tank/downloads/complete/nzb/dj-mixes and
    /mnt/tank/media/Music. The first four must report the same %d value; /mnt/tank/media/Music must
    report a different one. If the first four are not identical, STOP and report: criterion 1's
    atomic-rename property does not hold and the phase design changes.

    STEP 2 — take the snapshot, before creating anything. Assert tank/downloads@pre-phase5 does not
    already exist, then run `zfs snapshot tank/downloads@pre-phase5`, then read it back with
    `zfs list -t snapshot -H -o name tank/downloads@pre-phase5` and write that output to
    /mnt/fast/safety/phase05/snapshot-proof.txt on LXC 100. Also record its creation property in epoch
    seconds via `zfs get -H -p -o value creation tank/downloads@pre-phase5` into the same directory as
    snapshot-creation.txt; later plans compare their own mtimes against that number to show D-27's
    ordering held. Do not reuse @pre-project or @pre-chown: both are dated 2026-08-18, a month of
    unrelated downloads has landed since, and rolling back to them would discard content this project
    never touched (05-PREMEASURE.md section 2).

    STEP 3 — create the tree at /mnt/tank/downloads/complete/nzb/_inbox with exactly six children:
    01-auto, 02-review, 03-asis, 04-hold, 99-quarantine, _done. The location is complete/nzb/_inbox,
    not /mnt/tank/downloads/_inbox, per ARCHITECTURE.md "Recommended Structure", which places it
    beside music/, unsorted/ and dj-mixes/; D-12 lists it in exactly that company. It is still "under
    tank/downloads" as ROADMAP criterion 1 requires, because tank/downloads is the dataset. Record
    that reasoning in Task 3's artifact so nobody re-litigates it.

    Create with `mkdir -p`, then `chown 568:568` each of the seven directories — _inbox itself plus
    its six children — from atlantis, per D-23. Do not attempt any chmod: it returns EPERM on tank
    even as real root under aclmode=restricted plus aclinherit=passthrough, and everything on the pool
    is 0777 and stays that way.

    STEP 4 — assert D-18 mechanically. `zfs list -H -o name -t filesystem -r tank/downloads` must
    contain no dataset whose name ends in _inbox, and `stat -c %d` on _inbox must equal the %d
    measured on unsorted/. A dataset here would place _inbox on a different devid and destroy
    criterion 1's atomic-rename property outright. It looks like a tidy improvement — quota and
    independent snapshots, exactly what fast/transcode got in Phase 02.1 — which is why the
    prohibition is asserted rather than assumed.

    STEP 5 — assert the second clause of criterion 1. A find over /mnt/tank/media to depth 3 for any
    directory named _inbox, 01-auto, 02-review, 03-asis, 04-hold, 99-quarantine or _done must return
    zero lines.

    Chown nothing else. D-24's tree-wide sweep is plan 05-10 and runs last (D-27), so it also
    normalises these six directories; this task's chown only makes the tree correct from the moment it
    exists.

    Every remote command containing a pipe must carry `set -o pipefail` inside the remote command
    string, and the ssh exit status must be read with no local pipe in front of it — otherwise
    `timeout N cmd | wc -l` silently exits 0 (quick-health-check.sh lines 796-839).
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=15 root@172.16.1.158 "set -o pipefail; zfs list -t snapshot -H -o name tank/downloads@pre-phase5 && ls -1 /mnt/tank/downloads/complete/nzb/_inbox | sort | tr '\n' ' ' && stat -c '%u:%g' /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine"</automated>
  </verify>

  <acceptance_criteria>
    - `zfs list -t snapshot -H -o name tank/downloads@pre-phase5` on atlantis exits 0 and prints exactly `tank/downloads@pre-phase5`
    - /mnt/fast/safety/phase05/snapshot-proof.txt exists on LXC 100 and contains the literal string `tank/downloads@pre-phase5`
    - /mnt/fast/safety/phase05/snapshot-creation.txt contains a bare integer epoch timestamp
    - `ls -1 /mnt/tank/downloads/complete/nzb/_inbox | sort` prints exactly six lines: 01-auto, 02-review, 03-asis, 04-hold, 99-quarantine, _done
    - `find /mnt/tank/downloads/complete/nzb/_inbox -mindepth 1 -maxdepth 1 ! -type d` returns zero lines — the tree is created empty (D-17)
    - `stat -c '%u:%g'` read FROM ATLANTIS on all seven directories returns `568:568` on every one. A container-side listing showing 65534 is LXC 100's view, not the disk; do not accept it as evidence either way
    - `stat -c %d` is identical for _inbox, unsorted and dj-mixes under complete/nzb, and differs from `stat -c %d /mnt/tank/media/Music`
    - `zfs list -H -o name -t filesystem -r tank/downloads` contains zero lines ending in `_inbox`
    - the criterion-1 find over /mnt/tank/media returns zero lines
    - no chmod was attempted anywhere in this task
  </acceptance_criteria>

  <done>
    The fence exists and is proven by a file; the six directories exist, are empty, are 568:568 on
    disk, and sit on the same devid as the content that will move into them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drive the D-21 inode proof — positive control and cross-dataset negative control</name>

  <read_first>
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-21 in full, including its warning about Phase 1's D-20
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md` section 7
    - `scripts/quick-health-check.sh` lines 463-487 — the VENDORED_DRIFT_PROMOTED block, the estate's clearest written example of "prove the instrument can fail"
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md` sections 1 and 3
  </read_first>

  <files>
    /mnt/fast/safety/phase05/inode-proof.txt
  </files>

  <action>
    Run from LXC 100 (root@172.16.1.159). No zfs and no chown are needed, so atlantis is not required
    and Phase 1's D-20 is not touched.

    Create a throwaway fixture directory
    /mnt/tank/downloads/complete/nzb/_inbox/01-auto/.phase05-inode-fixture containing one small file.
    The leading dot keeps it invisible to anything that later globs the tree, and it is removed by
    this task before it exits.

    POSITIVE CONTROL: read `stat -c '%d %i'` on the fixture, move it from _inbox/01-auto/ to
    _inbox/02-review/ with mv, read `stat -c '%d %i'` again. The inode must be unchanged and the devid
    must be unchanged. This is the property ROADMAP criterion 1 demands be verified by inode rather
    than by watching it look fast.

    NEGATIVE CONTROL — the more interesting half, and the reason the task exists. An inode that has
    only ever been observed staying the same is an instrument nobody has shown can distinguish
    anything. Move the SAME fixture from _inbox/02-review/ to a scratch directory under
    /mnt/fast/safety/phase05/inode-negative/, which is on a different filesystem entirely: note that
    /mnt/fast/safety is not on the fast ZFS pool at all, it is LXC 100's ext4 root (normalise-dj-tags.py
    HAZARD NOTES — only the mp entries in /etc/pve/lxc/100.conf are real bind mounts), which makes it
    an unambiguously different devid. Read `stat -c '%d %i'` again. The devid must differ and the inode
    must change. If the inode collides numerically across the two filesystems, the devid difference
    carries the proof — assert the pair (%d, %i), never %i alone.

    The negative control is deliberately NOT run against /mnt/tank/media/Music. Phase 1's D-20 says
    nobody holds rw there until Phase 6, and D-21 states the property under test is "a different devid
    changes the inode", not "Music specifically". Record that choice in the evidence file.

    Write both controls, with the literal %d and %i values before and after each move, to
    /mnt/fast/safety/phase05/inode-proof.txt, tagging the positive result with the literal string
    `INODE UNCHANGED` and the negative result with `INODE CHANGED`. Then remove the fixture and the
    inode-negative scratch directory, and assert both are gone — D-17 says the tree is created empty
    and it must be empty when this task finishes.

    Use `set -euo pipefail` and an EXIT trap that removes the fixture from whichever of the three
    locations it currently occupies, so an interrupted run does not strand it in the inbox.
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; grep -c 'INODE UNCHANGED' /mnt/fast/safety/phase05/inode-proof.txt && grep -c 'INODE CHANGED' /mnt/fast/safety/phase05/inode-proof.txt && find /mnt/tank/downloads/complete/nzb/_inbox -mindepth 1 -maxdepth 2 | wc -l"</automated>
  </verify>

  <acceptance_criteria>
    - inode-proof.txt records a positive control whose %i is byte-identical before and after the _inbox/01-auto to _inbox/02-review move, and whose %d is also identical
    - inode-proof.txt records a negative control whose %d differs and whose %i differs after the move to /mnt/fast/safety/phase05/inode-negative/
    - the negative control is recorded as DRIVEN, with both observed value pairs printed — not asserted from the devid table alone
    - `find /mnt/tank/downloads/complete/nzb/_inbox -mindepth 1 -maxdepth 2 | wc -l` returns 6, the six directories with no fixture residue
    - `test -e /mnt/fast/safety/phase05/inode-negative` is false
    - /mnt/tank/media/Music appears in none of the commands run by this task
  </acceptance_criteria>

  <done>
    Criterion 1's inode claim is backed by an instrument that has been observed both passing and
    failing, and the inbox is empty again.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the measured inbox paths and the D-18/D-19/D-26 policy for Phase 6</name>

  <read_first>
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-16, D-17, D-18, D-19, D-23, D-26
    - `.planning/research/ARCHITECTURE.md` sections "Recommended Structure" and "Pattern 5: Filesystem-as-Queue"
    - `/mnt/fast/safety/phase05/inode-proof.txt` produced by Task 2
    - `.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md` section D-01 — the objection D-16 amends
  </read_first>

  <files>
    .planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md
  </files>

  <action>
    Write 05-INBOX-PATHS.md. This is D-16's deliverable toward Phase 6: Phase 6 must configure
    beets-flask against measured directories, not intended ones, and beets-flask itself is explicitly
    out of scope here — D-16 amends Phase 4's D-01, which had deferred it TO Phase 5.

    Carry the values actually observed in Tasks 1 and 2, not the values this plan expected:

    1. The six absolute paths, one per line, under /mnt/tank/downloads/complete/nzb/_inbox/, each with
       its one-line policy from ARCHITECTURE.md "Recommended Structure": 01-auto is bucket A,
       autotag and import if the score passes; 02-review is bucket B, tag then present candidates and
       wait for a human; 03-asis is bucket C, import from existing tags with no autotag; 04-hold needs
       external artwork or tracklist sourcing first; 99-quarantine is bucket D; _done holds
       post-import receipts.
    2. Why the tree is at complete/nzb/_inbox and not /mnt/tank/downloads/_inbox — the ARCHITECTURE
       diagram places it beside music/, unsorted/ and dj-mixes/, and D-12 lists it in that same
       company. It is still under the tank/downloads dataset, which is what criterion 1 requires.
    3. The measured devid values from Task 1, and D-18 stated as a prohibition: _inbox must NEVER
       become its own ZFS dataset, because a dataset places it on a different devid from unsorted/ and
       dj-mixes/, turns every move into copy-then-unlink, and destroys criterion 1's atomic-rename
       property outright. Note that it looks like a tidy improvement — quota and independent
       snapshots, exactly what fast/transcode got in Phase 02.1 — which is why the prohibition is
       written down rather than assumed.
    4. D-19's _done/ policy: _done/ holds the whole source folder, pruned per release only after
       CONS-04 passes, verified in BOTH Jellyfin and Music Assistant, never on a timer. The release
       migrates unsorted/ to 01-auto/ to _done/ as atomic renames, so reaching _done/ costs no extra
       space; and because Phase 6 sets import.copy yes, the library holds a copy while _done/ holds the
       original — that original is the undo path, and it matters because beets has no undo command.
    5. D-26 stated explicitly: content moved into _inbox is NOT chowned per-move. A rename preserves
       ownership, and adding a privileged chown to every move would contradict the one-atomic-rename
       property the design rests on. Staging therefore carries mixed ownership by design. The statement
       is recorded so nobody later writes an "everything under _inbox is 568:568" assertion that D-25
       guarantees will drift.
    6. The D-21 inode evidence, both controls, quoting the observed values from inode-proof.txt.

    Keep it under 100 lines. It is a reference card for Phase 6, not a narrative.
  </action>

  <verify>
    <automated>test -f .planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md && grep -c '_inbox/99-quarantine' .planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md && grep -c 'never become its own ZFS dataset' -i .planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md</automated>
  </verify>

  <acceptance_criteria>
    - the file lists all six absolute paths with the `/mnt/tank/downloads/complete/nzb/_inbox/` prefix, each with a one-line policy
    - the file states the D-18 prohibition using the words "never become its own ZFS dataset" (any case) and gives the devid reason
    - the file records the D-19 _done policy including "verified in both Jellyfin and Music Assistant" and "never on a timer"
    - the file records D-26's no-chown-per-move statement
    - the file quotes the observed %d and %i pairs from inode-proof.txt for both controls
    - the file is 100 lines or fewer
  </acceptance_criteria>

  <done>
    Phase 6 can configure against measured paths, and the three prohibitions most likely to be
    reversed by a well-meaning later edit are written down with their reasons.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 (unprivileged container root) to atlantis (real root on the hypervisor) via ssh key | Everything on the far side runs with hypervisor-wide authority. A wrong path constant is not contained by a namespace. |
| Phase-5 tooling to the live download tree | SABnzbd writes into tank/downloads at roughly one music job per 72 s; any read is a point-in-time view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Elevation of Privilege | `mkdir`/`chown` delegated to atlantis as real root | mitigate | Both commands take literal absolute paths under /mnt/tank/downloads/complete/nzb/_inbox with no variable interpolation from measured data; no recursive flag is used anywhere in this plan |
| T-05-01-02 | Tampering | D-21 fixture left behind inside a policy directory | mitigate | Fixture name is dot-prefixed, removed by an EXIT trap, and its absence is an acceptance criterion (`find ... -maxdepth 2 | wc -l` equals 6) |
| T-05-01-03 | Tampering | The negative control is run against /mnt/tank/media/Music, breaching Phase 1 D-20 | mitigate | Target is /mnt/fast/safety/phase05/inode-negative, a different filesystem entirely; an acceptance criterion asserts /mnt/tank/media/Music appears in no command |
| T-05-01-04 | Repudiation | The snapshot is claimed but not taken, so later plans believe a rollback exists that does not | mitigate | The snapshot is read back and written to a proof file; every later mutating plan refuses to act unless that file names `tank/downloads@pre-phase5` |
| T-05-01-05 | Information Disclosure | None — no credentials, tokens or user data are read or written by this plan | accept | Snapshot names and paths are already public in this repo |
| T-05-01-06 | Denial of Service | A large file written to /tmp on LXC 100 consumes host RAM | mitigate | All output lands under /mnt/fast/safety/phase05; nothing in this plan writes to the system temp directory |
| T-05-01-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | This plan installs no packages from any package manager. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- `tank/downloads@pre-phase5` exists on atlantis and its `creation` epoch precedes every later
  Phase 5 mutation.
- The six inbox directories exist, are empty, are `568:568` as read from atlantis, and share a devid
  with `unsorted/` and `dj-mixes/`.
- No dataset under `tank/downloads` is named `_inbox`.
- `/mnt/tank/media` contains no directory named `_inbox`, `01-auto`, `02-review`, `03-asis`,
  `04-hold`, `99-quarantine` or `_done` to depth 3.
- `inode-proof.txt` shows the positive control unchanged and the negative control changed, both
  driven.
- `05-INBOX-PATHS.md` exists and records the six paths, D-18, D-19, D-26 and the inode evidence.
</verification>

<success_criteria>
ROADMAP criterion 1 is TRUE and proven by an instrument that has been seen to fail: the tree exists
under `tank/downloads`, nothing equivalent exists under `media/`, and an `_inbox`-to-`_inbox` move is
an atomic same-dataset rename verified by unchanged inode. INBX-01 is satisfied. The D-01 fence is in
place with a proof file the rest of the phase consumes.
</success_criteria>

<output>
Create `.planning/phases/05-inbox-structure-and-the-junk-gate/05-01-SUMMARY.md` when done.
</output>
