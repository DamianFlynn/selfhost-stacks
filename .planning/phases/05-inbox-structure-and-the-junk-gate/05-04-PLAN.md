---
phase: 05-inbox-structure-and-the-junk-gate
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - host:/mnt/tank/downloads/complete/nzb/_inbox/99-quarantine
  - host:/mnt/tank/downloads/lidarr-import
  - host:/mnt/fast/safety/phase05/junk-sweep-result.txt
autonomous: false
requirements: [INBX-02]

must_haves:
  truths:
    - "The operator has read the candidate file and approved it by name; nothing was moved or deleted before that approval (D-13)"
    - "Searching the five music paths returns zero _FAILED_, _UNPACK_ or stray .rar items outside 99-quarantine (ROADMAP criterion 2 as amended by D-12)"
    - "The Harry Potter rip is gone from unsorted/ — ordinary junk taken through the same gate as everything else (D-14)"
    - "The empty 512-byte dj-mixes decoy is gone (D-07 closing clause, D-11)"
    - "Approved junk is DELETED, not archived — 99-quarantine ends empty of swept items or holds only what the operator deliberately kept (D-11)"
    - "The nine low-value sidecars are gone and all five value-bearing sidecars remain in place with unchanged inodes (D-06)"
    - "/mnt/tank/downloads/lidarr-import no longer exists, closing Phase 1's D-23 (D-20)"
    - "Nothing under /mnt/tank/downloads/incomplete was touched (D-15)"
  artifacts:
    - path: "host:/mnt/fast/safety/phase05/junk-sweep-result.txt"
      provides: "Per-row outcome of the sweep — moved, deleted, skipped, failed — with reclaimed bytes reported as informational"
  key_links:
    - from: "operator approval of junk-candidates.tsv"
      to: "scripts/phase05-junk-sweep.sh sweep"
      via: "the file on disk is the authorization token; sweep refuses without it"
      pattern: "APPROVED_LIST"
---

<objective>
Run the gate's destructive half, once the operator has said yes.

D-11 corrected an earlier position: `99-quarantine` is a REVIEW-AND-DELETE queue, not an archive.
Deletion is the expected terminal state for junk, not a deferred maybe. The earlier "delete nothing"
position was rejected for a reason that still holds — nothing in this estate is scheduled,
`quick-health-check.sh` is manual-only by decision, so a dwell-then-purge policy would never fire. The
fix is to do the review now, not to keep the content forever.

This plan is `autonomous: false` by design. D-13's gate blocks BOTH the move and the delete, and the
gate is a read-and-approve-a-file step, never a sit-at-a-prompt step.

Purpose: satisfy INBX-02 and ROADMAP criterion 2 as amended, and close Phase 1's D-23.
Output: a swept and emptied junk set, a retired `lidarr-import`, and a per-row result record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-03-SUMMARY.md
@scripts/phase05-junk-sweep.sh
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator decides whether the junk candidate file is approved to act on</name>

  <read_first>
    - `/mnt/fast/safety/phase05/junk-candidates.tsv` — the file under review
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-03-SUMMARY.md` — the pre-declared expectations beside what was actually found, and the cd2.bmp finding
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-06, D-11, D-13, D-14, D-20
  </read_first>

  <action>
    Halt here. Present the candidate file and the two options below, and do not run the sweep subcommand
    until the operator selects one. Record the file's line count and sha256 at the moment of approval so
    the yes is bound to a specific content, and re-check that hash immediately before the sweep runs.

    This gate is `checkpoint:decision` and not `checkpoint:human-verify` deliberately.
    `.planning/config.json` sets `workflow.human_verify_mode: "end-of-phase"`, under which
    `checkpoint:human-verify` tasks do not halt mid-flight at all — they are harvested into a batch
    review AFTER the phase has already run. That is correct for post-hoc verification and wrong here,
    because this gate decides whether the work happens at all: D-13 is explicit that the approval blocks
    BOTH the move and the delete. `checkpoint:decision` is unaffected by `human_verify_mode` and still
    halts.
  </action>

  <decision>
    Approve `/mnt/fast/safety/phase05/junk-candidates.tsv` exactly as it stands, so the sweep may move
    every row to `99-quarantine` and then DELETE it?
  </decision>

  <context>
    Plan 05-03 wrote `scripts/phase05-junk-sweep.sh` and ran its read-only `enumerate` half against the
    five music paths. The result is one tab-separated file,
    `/mnt/fast/safety/phase05/junk-candidates.tsv`, with one row per candidate carrying its rule, its
    absolute path, whether it is a file or a directory, its size, how many audio files live beneath it,
    and the reason it was proposed. Nothing has been moved. Nothing has been deleted. The `sweep`
    subcommand refuses to run at all until this file has been approved, and refuses again unless
    `tank/downloads@pre-phase5` is proven to exist.

    What to read before deciding:

    1. Read the file on LXC 100, one row per line, columns aligned:
       ssh root@172.16.1.159 then run column -t on /mnt/fast/safety/phase05/junk-candidates.tsv
    2. Confirm the rows you expect are present: four music _FAILED_ / _UNPACK_ directories, the stray
       .rar files, the empty 512-byte dj-mixes/VA-Now_That.s decoy, the Harry Potter rip under
       unsorted/ — ordinary junk through the same gate as everything else, per D-14 — and the nine named
       low-value sidecars inside the Now! folder, seven EAC .log files plus play.m3u and
       "00. play.m3u" (D-06).
    3. Confirm the rows you expect to be ABSENT are absent: nothing under
       /mnt/tank/downloads/incomplete (D-15), nothing under /mnt/tank/media, none of the six TV/movie
       _FAILED_ directories, and none of the five kept sidecars — the m3u map, back.bmp, cd1.bmp,
       cd2.bmp and the volume-115 cue (D-06).
    4. Look at the two R8 PROPOSAL rows for lidarr-import/Madonna and lidarr-import/Michael Jackson.
       These are content, not junk. Their destination column says where they will go — change it, or
       delete the row entirely if you want them left alone. The lidarr-import directory itself is
       removed only if it ends up empty (D-20).
    5. To REMOVE something from the sweep, delete its line from the file. To keep a row, leave it. The
       file as it stands when you approve is exactly what will be acted on — nothing is added back and
       nothing is inferred.
    6. Everything approved is MOVED to 99-quarantine under a timestamp directory and then DELETED in
       the same run per D-11, except PROPOSAL rows, which are moved to their destination and never
       deleted. This is irreversible except by rolling back tank/downloads@pre-phase5, which would also
       discard everything downloaded since that snapshot was taken.

    How the approval is bound: the file's line count and sha256 are recorded at the moment of approval,
    and that hash is re-checked immediately before the sweep acts. A mismatch aborts with nothing moved
    and nothing deleted. If the script's own re-validation stage aborts because the live tree moved under
    the approved list, re-run enumerate into a NEW candidate file and return to this checkpoint — do NOT
    edit the approved file to make the abort go away.

    HAZARD, recorded rather than assumed away: `references/checkpoints.md` golden rule 5 states that when
    `workflow._auto_chain_active` or `workflow.auto_advance` is true, a `checkpoint:decision`
    AUTO-SELECTS THE FIRST OPTION. Both are `false` in `.planning/config.json` today, so this is not
    active — but the first option here is "proceed with the delete", so it must be written down: this
    phase must not be executed with `--auto` or `--chain`. The real protection is not the checkpoint
    type. It is D-13's file-based refusal — the sweep refuses to run without the approved file on disk
    and re-checks its sha256 immediately before acting. That control is load-bearing, is correctly
    implemented today, and must not be weakened or removed: a branch can be skipped by a flag, an env var
    or a future edit; a missing file cannot.
  </context>

  <options>
    <option id="approve-as-is">
      <name>Approve the candidate file exactly as it stands and run the sweep</name>
      <pros>The junk that would corrupt every later measurement is gone rather than parked; ROADMAP criterion 2 as amended becomes assertable; D-11's review-and-delete terminal state is reached now, while someone is looking, rather than deferred to a purge nothing in this estate schedules.</pros>
      <cons>Irreversible except by rolling back tank/downloads@pre-phase5, which also discards everything downloaded since that snapshot was taken. A wrong row deletes real content.</cons>
    </option>
    <option id="stop-and-revise">
      <name>Stop — the file needs editing or re-enumerating first</name>
      <pros>Nothing is moved and nothing is deleted. Rows can be removed or their destinations changed, or enumerate can be re-run to produce a fresh candidate file, and this checkpoint re-presented against it.</pros>
      <cons>The phase stalls here; later plans depend on a swept tree, so the junk keeps skewing every count taken in the meantime.</cons>
    </option>
  </options>

  <resume-signal>Select: approve-as-is or stop-and-revise. If stopping, describe what to change.</resume-signal>

  <acceptance_criteria>
    - the operator has explicitly selected an option, and an `approve-as-is` selection is recorded in the plan summary with the file's line count and sha256 at the moment of approval
    - the recorded sha256 is re-checked immediately before sweep runs, and a mismatch aborts with nothing moved and nothing deleted
    - on `stop-and-revise`, Task 2 did not run and nothing under the five music paths was moved or deleted
  </acceptance_criteria>

  <done>
    An explicit human decision exists, bound to a specific file content by hash, taken before anything was
    moved or deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run sweep — move approved rows to 99-quarantine, then delete them</name>

  <read_first>
    - `scripts/phase05-junk-sweep.sh` — its sweep refusals, its re-validation stage and its per-row loop
    - `/mnt/fast/safety/phase05/junk-candidates.tsv` as approved
    - `/mnt/fast/safety/phase05/snapshot-proof.txt`
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-11, D-13, D-15
    - `CLAUDE.md` section "Storage and Permissions" — the ZFS async-free note
  </read_first>

  <files>
    /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine
    /mnt/fast/safety/phase05/junk-sweep-result.txt
  </files>

  <action>
    Re-check the approved file's sha256 against the value recorded at approval. A mismatch means the
    file changed after the operator read it; abort with nothing moved and nothing deleted.

    Run the sweep subcommand of scripts/phase05-junk-sweep.sh from /mnt/fast/stacks on LXC 100 and
    capture its full output to /mnt/fast/safety/phase05/junk-sweep-result.txt.

    Expect the script's own re-validation stage to fire first. It re-derives each listed row against
    live state and aborts the WHOLE sweep on any conflict — a row that no longer matches its rule, has
    gained content, has changed size or audio count, or now sits under incomplete/. That is correct
    behaviour, not a bug: the tree is written at roughly one music job per 72 seconds, and the approval
    says which items the operator is willing to lose, not that they are still junk. If it aborts,
    re-run enumerate into a NEW candidate file and return to Task 1, rather than editing the approved
    file to make the abort go away.

    Do not assert a reclaimed-space floor after the delete. ZFS frees space asynchronously and
    zfs list can lag a large delete by about 20 seconds; report reclaimed bytes as informational and
    name the lag in the record. What IS asserted is the absence of each deleted path.

    After the run, assert ROADMAP criterion 2 as amended, from LXC 100, bounded Linux-side and with
    set -o pipefail inside each remote string. Across the five music paths only — complete/nzb/music,
    complete/nzb/unsorted, complete/nzb/dj-mixes, lidarr-import if it still exists, and
    complete/nzb/_inbox excluding 99-quarantine — a find for directories whose basename begins _FAILED_
    or _UNPACK_ must return zero, and a find for files matching the .rar and multipart forms must
    return zero. Assert separately that the Potter rip path no longer exists and that the empty
    dj-mixes/VA-Now_That.s_What_I_Call_Music__1-115_2023 decoy no longer exists.

    Assert the five value-bearing sidecars are all still present inside
    unsorted/VA-Now_That.s_What_I_Call_Music__1-115_2023, by name, with their inodes unchanged from the
    values plan 05-03 recorded. A sweep that quietly took a tracklist scan with it is the failure this
    whole triage exists to prevent.

    Assert nothing under /mnt/tank/downloads/incomplete was touched: record its entry count and total
    byte size before and after the run. It is a live directory so exact equality is not expected; what
    is asserted is that no path the sweep acted on resolves under it, which the script's fence already
    guarantees and this re-checks independently from the result file.
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; timeout 300 find /mnt/tank/downloads/complete/nzb/music /mnt/tank/downloads/complete/nzb/unsorted /mnt/tank/downloads/complete/nzb/dj-mixes -maxdepth 3 -type d -name '_FAILED_*' -o -maxdepth 3 -type d -name '_UNPACK_*' | wc -l"</automated>
  </verify>

  <acceptance_criteria>
    - the sweep exited 0, or exited 1 with every failing row named and explained in junk-sweep-result.txt
    - a find over the five music paths for directories named `_FAILED_*` or `_UNPACK_*`, excluding 99-quarantine, returns 0
    - a find over the same paths for files matching `*.rar`, `*.r[0-9][0-9]` and `*.part[0-9]*.rar`, excluding 99-quarantine, returns 0
    - `test -e /mnt/tank/downloads/complete/nzb/unsorted/Harry.Potter.And.The.Deathly.Hallows.Part.1.2010.PROPER.1080p.BluRay.x264-MOOVEE` is false
    - `test -e /mnt/tank/downloads/complete/nzb/dj-mixes/VA-Now_That.s_What_I_Call_Music__1-115_2023` is false
    - all five kept sidecars still exist inside the real Now! folder and `stat -c %i` on each matches the value recorded in 05-03-SUMMARY.md
    - every path listed in the approved file as a delete row returns false from `test -e`
    - junk-sweep-result.txt names every row with one of the outcomes moved, deleted, skipped or failed, with a reason under every failure
    - no path acted on in junk-sweep-result.txt begins with `/mnt/tank/downloads/incomplete` or `/mnt/tank/media`
  </acceptance_criteria>

  <done>
    ROADMAP criterion 2, as amended, is TRUE across the music paths, and the value-bearing sidecars are
    provably untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retire /mnt/tank/downloads/lidarr-import and leave 99-quarantine empty</name>

  <read_first>
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-11, D-17, D-20
    - `/mnt/fast/safety/phase05/junk-sweep-result.txt` from Task 2
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/` — D-23, the open item this closes
  </read_first>

  <files>
    /mnt/tank/downloads/lidarr-import
    /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine
  </files>

  <action>
    The two lidarr-import artist folders, Madonna and Michael Jackson, were routed by the sweep to
    whatever destination the operator approved in the R8 PROPOSAL rows. Confirm where each landed and
    record it. If both moved out, remove the now-empty /mnt/tank/downloads/lidarr-import directory with
    rmdir — the empty-only form, never a recursive removal — so the estate does not carry a fourth
    staging location nobody owns. This closes Phase 1's D-23 here rather than passing it to Phase 8.

    If either folder was left in place by operator choice, do NOT remove the directory; record that
    lidarr-import survives with its remaining contents named, and note that D-23 stays open. A partial
    close recorded honestly is worth more than a claimed one.

    Note the tension and record it rather than hiding it: D-17 says the tree is created empty and only
    99-quarantine receives content, while D-20 explicitly permits routing these two folders to the
    inbox. D-20 is the more specific decision and it governs. State in the summary which inbox folder
    each landed in, so Phase 6 and Phase 7 are not surprised by pre-seeded content.

    Then bring 99-quarantine to its terminal state per D-11. Every timestamp directory the sweep created
    should now be empty of deleted rows. Remove empty timestamp directories with rmdir. If anything
    remains, it is only what the operator deliberately kept — enumerate it by name in the summary, with
    the reason, so the phase does not close with an unexplained residue.

    Do not attempt any chmod anywhere in this task. Do not chown anything; plan 05-10's tree-wide sweep
    normalises everything last, per D-27.
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; test ! -e /mnt/tank/downloads/lidarr-import && find /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine -mindepth 1 | wc -l"</automated>
  </verify>

  <acceptance_criteria>
    - `test -e /mnt/tank/downloads/lidarr-import` is false, OR the summary names exactly what remains in it and states that Phase 1's D-23 stays open with the reason
    - no recursive removal was used on lidarr-import — only rmdir on an empty directory
    - `find /mnt/tank/downloads/complete/nzb/_inbox/99-quarantine -mindepth 1 | wc -l` returns 0, OR every remaining entry is named in the summary with the operator's reason for keeping it
    - the summary states which inbox folder each of Madonna and Michael Jackson landed in, and notes the D-17 / D-20 tension explicitly
    - no chmod and no chown was attempted in this task
  </acceptance_criteria>

  <done>
    The estate carries one fewer unowned staging location, and 99-quarantine is at its terminal state
    with any residue explained.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator approval file to irreversible deletion | The file content at approval time is the authorization token; its hash binds the yes to a specific list. |
| Live download tree to a list approved minutes earlier | SABnzbd writes at roughly one music job per 72 s, so an approved path can become a live job before the sweep runs (TOCTOU). |
| Music project scope to SABnzbd's in-flight work and to the library | `incomplete/` and `/mnt/tank/media` sit on the same pool and one bad path constant reaches both. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Irreversible deletion of operator data with no recovery | mitigate | `tank/downloads@pre-phase5` taken in plan 05-01 and proven by a file the sweep refuses to run without; deletion is per-row by name from the approved file, never over a glob |
| T-05-04-02 | Tampering | The approved list is edited between the operator's yes and the sweep | mitigate | sha256 recorded at approval and re-checked immediately before the sweep; a mismatch aborts |
| T-05-04-03 | Tampering | TOCTOU — an approved path becomes a live download before it is deleted | mitigate | The script re-derives every row against fresh state immediately before acting and aborts the whole sweep on any conflict, rather than skipping the row |
| T-05-04-04 | Tampering | A value-bearing sidecar is swept with the junk | mitigate | Hard KEEP list in the enumerator; post-sweep acceptance asserts all five by name with unchanged inodes |
| T-05-04-05 | Denial of Service | A deletion under `incomplete/` breaks an in-flight SABnzbd job | mitigate | `incomplete/` refused by name in the scope fence; post-run acceptance asserts no acted-on path begins with that prefix |
| T-05-04-06 | Elevation of Privilege | A recursive removal escapes into `/mnt/tank/media/Music`, breaching Phase 1 D-20 | mitigate | Scope fence refuses `/mnt/tank/media` by name; exactly one removal call exists and it takes a single quoted absolute path with a `--` guard; `rmdir` only on the empty `lidarr-import` |
| T-05-04-07 | Repudiation | The phase claims junk was deleted when it was merely moved | mitigate | Acceptance asserts `test -e` is false for every delete row and that 99-quarantine ends empty or with named, explained residue |
| T-05-04-08 | Elevation of Privilege | Auto-mode auto-selects the first option of the Task 1 decision gate and the delete proceeds unread | mitigate | `workflow.auto_advance` and `workflow._auto_chain_active` are both `false`; the task states the phase must not be run with `--auto` or `--chain`; independently, D-13's file-based refusal means the sweep still cannot act without the approved file and a matching sha256 |
| T-05-04-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | This plan installs no packages from any package manager. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- The operator approved a specific file content, bound by sha256, and the hash was re-checked before
  any action.
- A find over the five music paths returns zero `_FAILED_`, `_UNPACK_` and stray `.rar` items outside
  `99-quarantine`.
- The Potter rip and the empty `dj-mixes` decoy no longer exist.
- All five value-bearing sidecars survive with unchanged inodes.
- `lidarr-import` is gone, or its survival is recorded with reasons and D-23 is explicitly left open.
- `99-quarantine` is empty, or its residue is named with the operator's reason.
</verification>

<success_criteria>
INBX-02 is satisfied and ROADMAP criterion 2, as amended by D-12 with D-14 folded in, is TRUE. The junk
that would have corrupted every later measurement is gone rather than parked, and the content worth
keeping is provably still there.
</success_criteria>

<output>
Create `.planning/phases/05-inbox-structure-and-the-junk-gate/05-04-SUMMARY.md` when done.
</output>
