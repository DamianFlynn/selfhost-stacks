---
phase: 05-inbox-structure-and-the-junk-gate
plan: 10
type: execute
wave: 9
depends_on: ["05-09"]
files_modified:
  - scripts/phase05-downloads-chown.sh
  - host:/mnt/tank/downloads
  - host:/mnt/fast/safety/phase05/chown-verification.txt
autonomous: false
requirements: [INBX-01]

must_haves:
  truths:
    - "All of tank/downloads is owned 568:568, incomplete/ included, verified from atlantis rather than from a container view (D-24)"
    - "The operator was presented with the scope guard, the runner design, the snapshot state and the recovery story, and decided BEFORE the chown ran — a discretionary hardening, not a D-13 derivation"
    - "The chown ran LAST, after the junk sweep, the split and the tag repair, so it also normalises the 115 volume folders and the six _inbox directories (D-27)"
    - "A fresh snapshot tank/downloads@pre-phase5-chown was taken immediately before the chown, so zfs diff has a clean baseline instead of drowning in the split's renames"
    - "zfs diff against that fresh baseline shows only M lines, with zero plus, minus or R lines"
    - "tank/media/Music was NOT reached: a fresh snapshot of it taken at the same moment shows zero diff lines afterwards, proving the scope guard held and Phase 1's D-20 is intact"
    - "The run survives a dropped ssh session and is safely re-runnable, because chown is idempotent and the runner is detached with a completion sentinel"
    - "No standing health check asserting 568:568 across tank/downloads was added — the measurement and its date are the deliverable (D-25)"
    - "No chmod was attempted anywhere; everything on tank is 0777 and stays that way"
  artifacts:
    - path: "scripts/phase05-downloads-chown.sh"
      provides: "Snapshot preconditions, ALL-CAPS literal scope guard, detached atlantis runner with a sentinel, before and after census, zfs diff assertions"
      min_lines: 200
    - path: "host:/mnt/fast/safety/phase05/chown-verification.txt"
      provides: "Before and after census, the zfs diff line-class counts, the elapsed time, and the untouched-proof result for tank/media/Music"
  key_links:
    - from: "scripts/phase05-downloads-chown.sh"
      to: "atlantis 172.16.1.158 as real root"
      via: "detached setsid runner writing a sentinel, polled from the caller"
      pattern: "sentinel"
    - from: "tank/downloads@pre-phase5-chown"
      to: "the chown verification"
      via: "zfs diff against a baseline taken after the split, so the chown signal is not drowned by 4,746 renames"
      pattern: "pre-phase5-chown"
---

<objective>
Normalise ownership across the whole download tree, last, with an instrument that can actually see the
change and a second one that proves it did not reach the library.

D-24 is an operator decision taken against the stated recommendation, and the counter-argument is
preserved so a reviewer meets reasoning rather than silence: Phase 1 normalised the LIBRARY because it
is exported over NFS, where ZFS `acltype=nfsv4` ACLs are not exported and mode bits are the only lever
a client sees. `tank/downloads` is not exported, is 0777, and uid 3000 is the download client
legitimately owning what it wrote. The operator chose to normalise it anyway, all 209,039 entries,
`incomplete/` included.

Two things make this harder than Phase 1's equivalent. It is about 74 times the entry count — 197,776
of those entries are currently uid 3000 — so a synchronous `chown -R` over an ssh session that may drop
is a real exposure with no resume ledger. And D-27 puts the chown last, so `@pre-phase5` already carries
the split's roughly 4,746 renames and a `zfs diff` against it would drown the chown signal in expected
lines. Phase 1 hit exactly that and took a separate `@pre-chown` baseline; the same is done here.

A pre-execution operator gate sits in front of the chown (Task 2). It is DISCRETIONARY HARDENING, added
deliberately by the planner and not derived from any decision: CONTEXT.md's Discretion section does not
mandate it, and D-13's gate is scoped to the junk sweep. It is here because every other destructive
operation in this phase gates approval before acting — 05-04's decision precedes its sweep, 05-07's
precedes its rename — while this plan, whose own threat model calls the chown the most dangerous command
in the phase, would otherwise be guarded only by a literal inside the script. D-13's stated rationale
applies with equal force to a hardcoded literal: a branch can be skipped by a flag, an env var or a
future edit. A human reading the guard value before it fires is exactly the defence that catches a
code-level defect in it.

Purpose: satisfy the ownership half of the phase and close the uid-3000 carry-in recorded in STATE.md.
Output: a verified tree-wide chown, with the untouched-proof for the library taken alongside it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-09-SUMMARY.md
@scripts/freeze-music-apply.sh
@CLAUDE.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase05-downloads-chown.sh with its guards and its detached runner</name>

  <read_first>
    - `scripts/freeze-music-apply.sh` lines 60-160 — the constants block, detect_zfs_route, zfs_exec, snapshot_exists, snapshot_creation
    - `scripts/freeze-music-apply.sh` lines 536-612 — do_ownership in full: the snapshot precondition, the ALL-CAPS literal scope guard, the before census, the remote test-d re-assertion, the chown, and the deliberately-not-run mode pass with its reasons
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/01-08-PLAN.md` lines 14-25 — why a fresh pre-chown snapshot is needed and why the stat manifest cannot see a chown
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-23, D-24, D-25, D-26, D-27
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md` section 6 and the No Analog Found table row on session survival
  </read_first>

  <files>
    scripts/phase05-downloads-chown.sh
  </files>

  <action>
    Write a new bash script in house style with a header that doubles as help, `set -euo pipefail`,
    ALL-CAPS constants and the counter-incrementing failure helper.

    CONSTANTS. `DOWNLOADS=/mnt/tank/downloads`; `LIBRARY=/mnt/tank/media/Music`; `UIDGID=568:568`;
    `ZFS_HOST=172.16.1.158`; `SNAPTAG=pre-phase5-chown`;
    `OUT_DIR=${OUT_DIR:-/mnt/fast/safety/phase05}`. Copy the route-detection and delegation helpers from
    freeze-music-apply.sh wholesale in shape: read-only detection first so the route is known and printed
    before anything mutates, then a single exec helper that runs locally when zfs exists and over ssh to
    atlantis otherwise.

    PRECONDITIONS, each a refusal and none a skip. Refuse if the zfs route is unavailable — an
    unreachable atlantis means the snapshot state is UNKNOWN, and the snapshot is the only rollback for a
    bad recursive chown. Refuse if `tank/downloads@pre-phase5` does not exist, since D-01's fence must
    still be there. Then take the fresh baseline `tank/downloads@pre-phase5-chown` and refuse to proceed
    if it cannot be created or read back. Also take `tank/media/Music@pre-phase5-chown` at the same
    moment: it is the untouched-proof instrument for the library half and it must be fresh, because
    Phase 1's month-old `tank/media/Music@pre-chown` could carry unrelated drift and would make a
    non-zero diff ambiguous.

    WHY A SECOND SNAPSHOT AT ALL, stated in a comment so it is not optimised away: D-27 orders the chown
    last, so `@pre-phase5` already carries the split's roughly 4,746 renames plus the junk sweep's
    deletions plus the tag write's modifications. A `zfs diff` against it would show all of those and
    drown the chown's signal. Phase 1's plan 01-08 hit exactly this and took a separate `@pre-chown` for
    the same reason. Note also that the stat manifest cannot substitute: it keys on mtime, and chown
    moves ctime not mtime, so a zero from a stat manifest afterwards is EXPECTED and not reassuring —
    plan 01-06 proved that when its gate passed while `zfs diff` showed a real metadata change.
    `zfs diff` against the fresh snapshot is the only instrument that can verify this.

    SCOPE GUARD, ALL-CAPS and literal, before anything is delegated. The chown runs as REAL root on the
    Proxmox host, not as a namespaced container root, so a wrong constant here is a hypervisor-wide
    recursive chown rather than a contained mistake. Assert the literal: if `DOWNLOADS` is not exactly
    `/mnt/tank/downloads`, refuse. Re-assert the path on the FAR SIDE with a directory test before
    chowning, because a bind mount present here but absent there would otherwise make the recursive chown
    a silent no-op against a path the remote auto-creates.

    PRINT THE GUARD VALUE. Task 2 is a human gate that reads the literal the script will compare against,
    so the script must be able to show it without running the chown: support a dry inspection mode, or
    print the resolved `DOWNLOADS`, `LIBRARY` and `UIDGID` values and the detected zfs route as the first
    thing it does, before any mutation. A guard nobody can read before it fires is not reviewable.

    USE THE SYMLINK-SAFE FORM. Run the chown with the flag that affects symbolic links themselves rather
    than their referents. Without it a symlink under `tank/downloads` pointing into
    `/mnt/tank/media/Music` would have its TARGET chowned, silently reaching the library and breaching
    Phase 1's D-20. This is not hypothetical for this estate: `normalise-dj-tags.py`'s CR-04 records that
    exactly one symlink under a scratch tree defeated a path fence silently, and a hardlink did the same
    with no symlink involved. State the reason in a comment beside the flag.

    SESSION SURVIVAL. This is 209,039 entries, about 74 times the Phase 1 operation, and the only analog
    in the estate runs the chown synchronously over ssh with no timeout — untested at this scale. A
    dropped ssh mid-chown leaves a partially normalised tree with no resume ledger. Solve it by
    detaching: write a small runner onto atlantis that performs the chown and then writes its exit status
    to a sentinel file, and launch it in a new session, immune to a hangup, with stdin closed and stdout
    and stderr redirected to a log file on atlantis. The ssh call returns immediately; the caller then
    polls for the sentinel on a bounded loop with a stated maximum wait and prints progress. Prefer the
    new-session launcher from util-linux, which is present on Debian-based Proxmox; probe for it and fall
    back to a hangup-immune launcher, and refuse rather than silently running synchronously if neither is
    available. Note in the header that `tmux` and `screen` are not installed on LXC 100 and that
    atlantis's toolset was not previously measured, so the probe is the point.

    RESUME STORY, stated explicitly because there is no ledger: a recursive chown is idempotent, so an
    interrupted run is recovered by re-running the same command. That is the recovery path. The sentinel
    distinguishes "finished with status N" from "still running" from "the runner died without writing a
    sentinel", and the third case is a could-not-look, not a pass.

    CENSUS. Before the chown, count entries under `DOWNLOADS` grouped by owning uid and gid, from
    atlantis, and record the totals. After the chown, repeat. The 2026-09-18 measurement was 209,039
    entries with 197,776 at uid 3000; state the fresh numbers beside those. The before census must be
    obtainable on its own, without mutating anything, because Task 2 presents it to the operator.

    NO MODE PASS. Do not run chmod, do not attempt it, do not swallow a failure with a fallback. chmod is
    impossible on this pool: it returns EPERM as real root for every mode, including a no-op chmod on a
    file already at 0777, because aclmode=restricted plus aclinherit=passthrough gives even new files a
    non-trivial NFSv4 ACL. Everything on tank is 0777 and stays that way. Say so in a comment rather than
    leaving the absence to be read as an oversight.

    NO STANDING ASSERTION. D-25: do not add, and do not suggest adding, a health check asserting 568:568
    across tank/downloads. The download client keeps writing as uid 3000 at roughly one job per 72
    seconds, so new 3000-owned files appear almost immediately after the chown completes. Such a check
    would go red on the next download and train everyone to ignore it — the precise failure mode Phase
    02.1's CR-01 spent four gap-closure plans repairing in the other direction. The measurement and its
    date are the deliverable. Put that in the header.

    EXIT-CODE CONVENTION, stated: 0 when the chown completed, the after census shows every entry at
    568:568, the zfs diff shows only M lines, and the library untouched-proof shows zero lines; 1 when
    the chown ran but a verification failed; 2 on usage error or an unmet precondition.
  </action>

  <verify>
    <automated>bash -n scripts/phase05-downloads-chown.sh && bash scripts/phase05-downloads-chown.sh --help >/dev/null && grep -v '^[[:space:]]*#' scripts/phase05-downloads-chown.sh | grep -c 'chmod'; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/phase05-downloads-chown.sh` exits 0 and `--help` prints the header
    - `grep -v '^[[:space:]]*#' scripts/phase05-downloads-chown.sh | grep -c 'chmod'` returns 0 — no mode change is attempted anywhere
    - the script contains the literal scope-guard comparison against `/mnt/tank/downloads` and refuses on any other value; driven by running it with the constant overridden to another path and observing exit 2
    - the script can print its resolved constants and detected route, and produce the before census, without mutating anything — the inputs Task 2's gate presents
    - the chown invocation uses the symlink-safe flag, and a comment beside it states the library-reachability reason
    - the script refuses with exit 2 when `tank/downloads@pre-phase5` is absent
    - the script probes for a detached-session launcher and refuses rather than falling back to a synchronous run when none is found
    - the header states the D-25 prohibition against a standing ownership assertion and the reason
    - the header states the resume story: the chown is idempotent and an interrupted run is recovered by re-running
    - `grep -v '^[[:space:]]*#' scripts/phase05-downloads-chown.sh | grep -c '/tmp'` returns 0 for LXC-side paths — the atlantis-side log and sentinel are named explicitly and are not in the container's tmpfs
  </acceptance_criteria>

  <done>
    The most dangerous command in the phase is wrapped in a guard that asserts its own path literal,
    cannot reach the library through a symlink, survives a dropped session, refuses rather than degrading
    when a precondition is unmet, and can be read by a human before it fires.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator decides whether to run the 209,039-entry chown as real root</name>

  <read_first>
    - `scripts/phase05-downloads-chown.sh` as written in Task 1 — the constants block, the scope guard, the symlink-safe flag and the detached runner
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-24's preserved counter-argument, D-25, D-27
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/01-08-SUMMARY.md` — the Phase 1 equivalent at 1/74th the scale
  </read_first>

  <action>
    Halt here, BEFORE the chown. Produce the read-only inputs the decision needs and present them with
    the two options below: run the script's non-mutating inspection so the resolved `DOWNLOADS`,
    `LIBRARY` and `UIDGID` constants and the detected zfs route are printed verbatim, and take the before
    census from atlantis. Do not run the chown until the operator selects.

    This gate is DISCRETIONARY HARDENING and is recorded as such so a later reader knows where it came
    from: CONTEXT.md's Discretion section does not mandate a pre-chown approval, and D-13's gate is
    scoped to the junk sweep. It was added because every other destructive step in this phase gates
    approval before acting, and because the chown's only other guard is a literal inside a script — which
    D-13's own rationale warns is skippable by a flag, an env var or a future edit in a way a missing
    file is not.

    This gate is `checkpoint:decision` and not `checkpoint:human-verify` deliberately.
    `.planning/config.json` sets `workflow.human_verify_mode: "end-of-phase"`, under which
    `checkpoint:human-verify` does not halt mid-flight — it is harvested into a batch review AFTER the
    phase has run, which for a pre-execution gate is no gate at all. `checkpoint:decision` is unaffected
    by `human_verify_mode` and still halts.

    Record which option was selected, and on a `run-it` selection record the before-census figures and
    the guard value the decision was taken on.
  </action>

  <decision>
    Run `chown -R 568:568` as real root on atlantis across all of `/mnt/tank/downloads` — about 209,039
    entries, `incomplete/` included (D-24)?
  </decision>

  <context>
    What is about to happen, and what stands between it and a mistake:

    1. BEFORE CENSUS. Entries under `/mnt/tank/downloads` counted from atlantis and grouped by owning uid
       and gid. The 2026-09-18 measurement was 209,039 entries with 197,776 at uid 3000; the fresh figures
       are printed beside those. Any large divergence from that baseline is itself worth a pause.
    2. THE SCOPE GUARD. The script compares its `DOWNLOADS` constant against the literal
       `/mnt/tank/downloads` and refuses on any other value, before anything is delegated. The resolved
       value is printed above for you to read. This is the single most important thing on this screen:
       the chown runs as REAL root on the hypervisor, not as a namespaced container root, so a wrong
       constant is a hypervisor-wide recursive chown rather than a contained mistake (T-05-10-01). The
       path is also re-asserted on the far side with a directory test, so a missing bind mount cannot turn
       the chown into a silent no-op against a path the remote auto-creates.
    3. SYMLINK SAFETY. The chown uses the flag that affects symbolic links themselves rather than their
       referents, so a symlink under `tank/downloads` pointing into `/mnt/tank/media/Music` cannot carry
       the change into the library and breach Phase 1's D-20. This estate has been bitten by exactly that
       shape before — `normalise-dj-tags.py`'s CR-04 records one symlink defeating a path fence silently.
    4. THE RUNNER. A detached runner is written onto atlantis, launched in a new session with stdin
       closed and output redirected to a log there, and it writes its exit status to a sentinel file the
       caller polls on a bounded loop. If no detached-session launcher is available the script REFUSES
       rather than falling back to a synchronous run — refusal, not degradation. Nothing in this estate
       has run an operation at this scale, so the elapsed time is unknown and is part of the deliverable.
    5. SNAPSHOT STATE AT THE MOMENT OF THE CHOWN. `tank/downloads@pre-phase5` already exists — D-01 took
       it as the first act of the phase, and the script refuses if it is gone. The script then creates
       `tank/downloads@pre-phase5-chown` as a refusing precondition immediately before the chown, plus
       `tank/media/Music@pre-phase5-chown` at the same moment as the library untouched-proof instrument.
       So at the instant the chown fires, both baselines exist: the phase-wide one and the fresh one that
       makes the chown's signal visible instead of drowned in the split's 4,746 renames (D-27).
    6. RECOVERY. There is no resume ledger and none is needed: a recursive chown is IDEMPOTENT, so an
       interrupted or partial run is recovered by re-running the same command. The sentinel distinguishes
       "finished with status N" from "still running" from "the runner died without writing a sentinel",
       and the third is a could-not-look rather than a pass. A rollback of `@pre-phase5-chown` is
       available but is not the expected recovery; re-running is.

    HAZARD, recorded rather than assumed away: `references/checkpoints.md` golden rule 5 states that when
    `workflow._auto_chain_active` or `workflow.auto_advance` is true, a `checkpoint:decision`
    AUTO-SELECTS THE FIRST OPTION. Both are `false` in `.planning/config.json` today, so this is not
    active — but the first option here runs a hypervisor-root recursive chown, so it must be written
    down: this phase must not be executed with `--auto` or `--chain`. Note also what this gate is NOT: it
    does not replace the script's refusals. The literal scope guard, the `@pre-phase5` precondition, the
    far-side directory test and the launcher probe all still fire regardless of what is selected here,
    and none of them may be weakened or removed.
  </context>

  <options>
    <option id="run-it">
      <name>Run the chown now</name>
      <pros>Delivers D-24, which the operator took against the stated recommendation and which is the ownership half of this phase; closes the uid-3000 carry-in recorded in STATE.md; running it last (D-27) means it also normalises the 115 new volume folders and the six _inbox directories in the same pass.</pros>
      <cons>A recursive chown as real root over 209,039 entries, at ~74× the only comparable operation this estate has performed. Unknown elapsed time. The change is not undone by a file delete — recovery is a re-run or a snapshot rollback.</cons>
    </option>
    <option id="stop">
      <name>Stop — do not run the chown</name>
      <pros>Nothing privileged executes. The tree stays as it is, which is a working state: tank/downloads is not NFS-exported, is 0777, and uid 3000 is the download client legitimately owning what it wrote — the counter-argument D-24 was taken against.</pros>
      <cons>D-24 goes unmet and INBX-01's ownership half stays open; the uid-3000 carry-in in STATE.md is not closed and would have to be re-planned later, when the tree is larger.</cons>
    </option>
  </options>

  <resume-signal>Select: run-it or stop. If stopping, say what needs to change first.</resume-signal>

  <acceptance_criteria>
    - the operator has explicitly selected an option, and the selection is recorded with the before-census figures and the printed scope-guard value it was taken on
    - the inspection that produced those figures mutated nothing — no snapshot was created and no chown ran before the selection
    - on `stop`, Task 3 did not run and no entry under `/mnt/tank/downloads` changed ownership
  </acceptance_criteria>

  <done>
    The largest privileged operation in the phase has been read and authorised by a human BEFORE it ran,
    not after.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the chown and verify it with zfs diff plus the library untouched-proof</name>

  <read_first>
    - `scripts/phase05-downloads-chown.sh` as written in Task 1
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/01-08-SUMMARY.md` lines 240-285 — the executed Phase 1 result and the zfs diff figures it reported
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md` section 6, especially the scale warning
    - `.planning/STATE.md` around line 1115 — the uid-3000 carry-in this closes
  </read_first>

  <files>
    /mnt/tank/downloads
    /mnt/fast/safety/phase05/chown-verification.txt
  </files>

  <action>
    Run only after Task 2 returned `run-it`. Commit and push, `git pull --ff-only` on LXC 100, and run
    the script. Record the wall-clock elapsed time — D-24 requires this step to carry its own timing
    expectation, and no analog exists at this scale, so the measurement is the deliverable for anyone who
    has to repeat it.

    The zfs diff output will be roughly 200,000 lines. DO NOT print it raw. Assert on line-class counts:
    the count of lines beginning with M, the count beginning with plus, the count beginning with minus,
    and the count beginning with R. Expect M to be close to the entry count and the other three to be
    zero. A plus, minus or R line means something was created, removed or renamed during the run, which
    for a live tree is possible — the download client writes at roughly one job per 72 seconds — so
    report any such line individually with its path rather than failing blindly, and judge it against
    whether it sits under `incomplete/` or a download target.

    THE LIBRARY UNTOUCHED-PROOF. Run a diff of `tank/media/Music` against the fresh
    `tank/media/Music@pre-phase5-chown` taken in the same step, and assert ZERO lines. This is the
    negative half of the instrument and it replaces the role Phase 1 gave to `tank/downloads@pre-chown` —
    which cannot serve here, because in Phase 5 `tank/downloads` is the target. Zero lines proves the
    scope guard held, that no symlink carried the chown into the library, and that Phase 1's D-20 is
    still intact. Note that this is a strictly better negative control than Phase 1's, because it tests
    the thing that could actually go wrong rather than a dataset nothing was aimed at.

    Read ownership FROM ATLANTIS. A container-side listing showing 65534 is LXC 100's view, not the disk:
    the container is unprivileged with a sparse idmap, so unmapped on-disk ids surface as 65534 and both
    0:545 and 568:545 collapse to it. Phase 1 lost time to exactly this. If 65534 appears in any
    container-side listing, check from atlantis before believing it.

    Record everything in chown-verification.txt: the before census by uid and gid, the after census, the
    elapsed time, the four zfs diff line-class counts with any non-M line named, the library
    untouched-proof result, the date, and the operator's Task 2 selection with the guard value it was
    taken on. That record IS the deliverable per D-25 — there is no standing check and there will not
    be one.

    State D-26 in the record while the context is fresh: content moved into `_inbox` is not chowned
    per-move, staging carries mixed ownership by design, and this tree-wide sweep makes that largely moot
    at phase end but does not make it false. The statement stops someone later writing an "everything
    under _inbox is 568:568" assertion that D-25 guarantees will drift.
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=15 root@172.16.1.158 "set -o pipefail; timeout 600 find /mnt/tank/downloads -not -uid 568 -o -not -gid 568 | head -20 | wc -l"</automated>
  </verify>

  <acceptance_criteria>
    - the chown runner completed and its sentinel records exit status 0
    - a find from atlantis for any entry under /mnt/tank/downloads not owned 568:568 returns either 0, or only entries created after the chown started, each named with its mtime
    - the after census shows zero entries at uid 3000, or names the ones that appeared after the run started
    - the zfs diff against `tank/downloads@pre-phase5-chown` shows only M lines; the counts of plus, minus and R lines are each 0, or each such line is named with its path and judged
    - the diff of `tank/media/Music` against `tank/media/Music@pre-phase5-chown` returns ZERO lines
    - the raw zfs diff output was never printed in full — only line-class counts and named exceptions
    - elapsed wall-clock time is recorded in chown-verification.txt
    - the before and after censuses are recorded with the fresh entry count stated beside the 2026-09-18 figures of 209,039 entries and 197,776 at uid 3000
    - the record names the operator's Task 2 selection and the scope-guard value it was taken on
    - all ownership readings in the record were taken from atlantis, and the record says so
    - no chmod was attempted
  </acceptance_criteria>

  <done>
    The tree is normalised, the change is visible to the only instrument that can see it, and the library
    is provably untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator reviews the chown verification record</name>

  <read_first>
    - `/mnt/fast/safety/phase05/chown-verification.txt`
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-24's preserved counter-argument and D-25
  </read_first>

  <action>
    Present the chown verification record to the operator for review. This one is genuinely POST-HOC —
    the work is already done and authorised by Task 2's gate, and this is a read of the evidence. It is
    therefore correctly `checkpoint:human-verify`, which under this project's
    `workflow.human_verify_mode: "end-of-phase"` is harvested into the batch HUMAN-UAT review at phase
    end rather than halting here. That is the intended behaviour: the pre-execution decision has already
    been taken at Task 2, so nothing is gated on this review.

    Record the library untouched-proof line count and the after-census uid-3000 count the review is
    presented against.
  </action>

  <what-built>
    All 209,039-odd entries under `/mnt/tank/downloads` have been chowned to 568:568 from atlantis as
    real root, including `incomplete/`, the 115 new volume folders and the six `_inbox` directories. A
    fresh snapshot was taken immediately before the run so `zfs diff` had a clean baseline, and a fresh
    snapshot of the library was taken at the same moment as an untouched-proof. The verification record
    holds the before and after censuses, the elapsed time, the diff line-class counts and the library
    proof.
  </what-built>

  <how-to-verify>
    1. Read `/mnt/fast/safety/phase05/chown-verification.txt`.
    2. The number that matters most is the library untouched-proof: a diff of `tank/media/Music` against
       its fresh snapshot should be ZERO lines. Non-zero would mean the chown reached the library, which
       Phase 1's D-20 forbids until Phase 6.
    3. The download tree diff should be almost entirely M lines. Any plus, minus or R line is named
       individually — those are near-certainly the download client working during the run, since it
       writes about one job every 72 seconds, but read them.
    4. The after census should show zero entries at uid 3000, or only entries created after the run
       began. Some will already have appeared: that is expected and it is exactly why D-25 forbids adding
       a standing check for this.
    5. Note the elapsed time. Nothing in the estate had run an operation at this scale before, so this
       figure is the estimate anyone repeating it will use.
    6. Confirm you are content that there is NO standing health check for this. Per D-25 the measurement
       and its date are the deliverable; a check would go red on the next download.
  </how-to-verify>

  <resume-signal>Type "approved", or describe what in the record needs explaining.</resume-signal>

  <acceptance_criteria>
    - the review is presented with the library untouched-proof line count and the after-census uid-3000 count stated
    - if the library untouched-proof was non-zero, the record names that as a violation of Phase 1's D-20 rather than accepting it
  </acceptance_criteria>

  <done>
    The evidence for the largest privileged operation in the phase is on durable storage and in front of
    a human.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 to atlantis real root | The chown runs with hypervisor-wide authority; a wrong path constant is not contained by a namespace. |
| `tank/downloads` to `tank/media/Music` | A symlink or a wrong constant is the only thing between a download-tree operation and the single-writer library Phase 1 fenced. |
| A long privileged batch to an unreliable transport | ssh can drop mid-run over 209,039 entries with no resume ledger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Elevation of Privilege | A recursive chown as real root against an unexpected path becomes hypervisor-wide | mitigate | ALL-CAPS literal scope guard comparing the constant to `/mnt/tank/downloads` before any delegation, driven by an override test expecting exit 2; remote directory re-assertion so a missing bind mount cannot make the chown a silent no-op; and a blocking pre-execution operator decision (Task 2) that prints the resolved guard value for a human to read, so a defect in the literal itself is caught before it fires |
| T-05-10-02 | Tampering | A symlink under `tank/downloads` pointing into the library causes its target to be chowned, breaching Phase 1 D-20 | mitigate | The chown uses the symlink-safe flag that affects links rather than referents; a fresh `tank/media/Music@pre-phase5-chown` is diffed afterwards and asserted to be zero lines |
| T-05-10-03 | Denial of Service | A dropped ssh mid-chown leaves a partially normalised tree with no ledger | mitigate | Detached runner in a new session with stdin closed, a completion sentinel carrying the exit status, a bounded polling loop, and a stated resume story — chown is idempotent, so re-running is the recovery |
| T-05-10-04 | Repudiation | The verification passes because the baseline was too old to show the signal | mitigate | A fresh `tank/downloads@pre-phase5-chown` is taken immediately before the run, because `@pre-phase5` already carries the split's renames; the stat manifest is explicitly rejected as an instrument because chown moves ctime not mtime |
| T-05-10-05 | Repudiation | A container-side ownership reading showing 65534 is mistaken for the on-disk truth | mitigate | Every ownership reading is taken from atlantis and the record states so; the sparse-idmap cause is documented beside it |
| T-05-10-06 | Denial of Service | A standing ownership check goes red on the next download and trains everyone to ignore the health script | mitigate | D-25: no such check is added; the prohibition and its reason are in the script header; the measurement and its date are the deliverable |
| T-05-10-07 | Tampering | An attempted mode change fails EPERM and is silently swallowed, leaving a false belief about permissions | mitigate | No chmod is attempted anywhere; its impossibility on this pool is documented in a comment; acceptance greps the comment-stripped script and requires zero matches |
| T-05-10-08 | Elevation of Privilege | Auto-mode auto-selects the first option of the Task 2 decision gate and the chown runs unread | mitigate | `workflow.auto_advance` and `workflow._auto_chain_active` are both `false`; Task 2 states the phase must not be run with `--auto` or `--chain`; independently, the script's literal scope guard, `@pre-phase5` precondition, far-side directory test and launcher probe all still refuse regardless of the checkpoint outcome |
| T-05-10-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | This plan installs no packages from any package manager. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- The operator was shown the before census and the resolved scope-guard literal and decided BEFORE the
  chown ran; the selection and the values it was taken on are in the record.
- `tank/downloads@pre-phase5-chown` and `tank/media/Music@pre-phase5-chown` both exist and were created
  immediately before the chown.
- The chown runner's sentinel records exit status 0 and the elapsed time is recorded.
- A find from atlantis for entries not owned `568:568` returns zero, or only post-run arrivals, each
  named.
- `zfs diff` against the fresh download baseline shows only M lines; any other line class is named.
- `zfs diff` of `tank/media/Music` against its fresh snapshot returns zero lines.
- No chmod was attempted; no standing ownership check was added.
- The verification record is in front of the operator for post-hoc review.
</verification>

<success_criteria>
`tank/downloads` is uniformly `568:568` as read from the hypervisor, the change was authorised by a human
before it ran, is verified by the only instrument that can see it, the library is provably untouched, and
the operation's real cost is recorded for whoever repeats it. D-25's position holds: there is no standing
check and there will not be one.
</success_criteria>

<output>
Create `.planning/phases/05-inbox-structure-and-the-junk-gate/05-10-SUMMARY.md` when done.
</output>
