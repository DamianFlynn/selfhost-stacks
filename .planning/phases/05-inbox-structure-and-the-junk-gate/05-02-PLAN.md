---
phase: 05-inbox-structure-and-the-junk-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - scripts/quick-health-check.sh
autonomous: true
requirements: [INBX-01, INBX-02, INBX-03]

must_haves:
  truths:
    - "ROADMAP criterion 2 carries a dated in-band amendment narrowing the sweep to the music paths, with D-14's Potter factual correction folded into it as a one-line note (D-12, D-14)"
    - "The original wording of every amended criterion is still readable — nothing is replaced, only annotated"
    - "ROADMAP criteria 3 and 4 carry dated notes reconciling their wording with the measurements: criterion 3's target has zero subdirectories and its reconciliation number is 4,746 mp3 plus 14 sidecars, not 4760; criterion 4 is already green and becomes an assertion (D-08, D-22)"
    - "REQUIREMENTS INBX-01, INBX-02 and INBX-03 carry inline ADDENDUM entries in the TAGR-05 shape, each stating it does not reduce the requirement"
    - "quick-health-check.sh asserts zero underscore-prefixed directories under /mnt/tank/media/Music with three branches — could-not-look, none, and violation (D-22)"
    - "That assertion has been driven red by a deliberately created probe directory and driven green again after its removal"
    - "No tank/downloads ownership assertion was added to quick-health-check.sh (D-25)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 criteria 2, 3 and 4 amended in band and dated, substance preserved"
      contains: "Amended 2026-09-18 by plan 05-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INBX-01/02/03 addenda in the TAGR-05 inline shape"
      contains: "ADDENDUM"
    - path: "scripts/quick-health-check.sh"
      provides: "The criterion-4 standing assertion, bounded Linux-side, three branches"
      contains: "mnt/tank/media/Music"
  key_links:
    - from: "scripts/quick-health-check.sh"
      to: "atlantis 172.16.1.158"
      via: "bounded ssh with set -o pipefail inside the remote string and the ssh status read with no local pipe"
      pattern: "pipefail"
    - from: ".planning/ROADMAP.md criterion 2"
      to: ".planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md sections 4 and 5"
      via: "cited measurement inside the amendment"
      pattern: "05-PREMEASURE"
---

<objective>
Make the written criteria say what the measurements support, and turn criterion 4 from a claim into a
standing assertion that has been seen to fail.

Three of Phase 5's four criteria drift from what was measured. Criterion 2 as phrased quarantines
Sonarr's and Radarr's failed jobs into a music-project folder and names `dj-mixes` as the Potter
rip's location when `dj-mixes` contains no video at all. Criterion 3 says "split into per-volume
folders" against a folder with zero subdirectories, and states 4760 where the measured shape is 4,746
mp3 plus 14 sidecars. Criterion 4 is already green. D-12 requires the first of these be corrected by a
dated in-band amendment in the 02.1-11 shape — quote the original, state the measurement, name the
change, preserve substance — with D-14's correction folded in rather than given an amendment of its
own.

Purpose: a verifier at the end of this phase should meet criteria that are satisfiable and instruments
that are honest, not a discrepancy.
Output: amended `ROADMAP.md`, addended `REQUIREMENTS.md`, and a criterion-4 assertion inside
`quick-health-check.sh` proven capable of going red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md
@scripts/quick-health-check.sh
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP Phase 5 criteria 2, 3 and 4 in band, and addend INBX-01/02/03</name>

  <read_first>
    - `.planning/ROADMAP.md` lines 648-674 — the Phase 5 block being amended
    - `.planning/ROADMAP.md` lines 262-272 — the 02.1-11 amendment, the shorter and cleaner template
    - `.planning/ROADMAP.md` lines 526-549 — the 04-04 / D-20 amendment, the longer variant
    - `.planning/REQUIREMENTS.md` lines 155-165 — INBX-01, INBX-02, INBX-03 as written
    - `.planning/REQUIREMENTS.md` line 276 — the TAGR-05 inline ADDENDUM, the requirements-table shape
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-08, D-12, D-14, D-22
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PREMEASURE.md` sections 3, 4, 5 and 6
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md` sections 9b and 9c
  </read_first>

  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>

  <action>
    Add THREE blockquoted amendments to `.planning/ROADMAP.md`, each placed immediately under the
    criterion it annotates, never replacing the criterion text. Each opens with
    `> **Amended 2026-09-18 by plan 05-02 (<decision refs>).**` and follows the four moves visible in
    both exemplars: quote the original wording, state the measurement that forces the change and cite
    the artifact, name the change, and assert substance is preserved with the literal phrase
    "The substance is kept, not reduced" plus the rejected alternative and why.

    AMENDMENT A — criterion 2 (ROADMAP.md lines 663-664), decision refs D-12 and D-14. Original
    wording to quote: "Searching the download tree returns zero `_FAILED_`, `_UNPACK_` or stray `.rar`
    items outside `99-quarantine`, and the Harry Potter BluRay rip is out of `dj-mixes`."
    Measurement: `05-PREMEASURE.md` section 5 — ten `_FAILED_`/`_UNPACK_` directories exist under
    `complete/nzb/`, of which only four are music (`_UNPACK_Ed Sheeran…`, `_UNPACK_Katy Perry - Prism
    (2013) FLAC`, `_FAILED_Garth.Brooks-Ropin.The.Wind…`, `_FAILED_Garth.Brooks-The.Ultimate.Hits…`);
    the other six are TV and movies (`Chicago.PD.S13E02`, two `Goldie.and.Bear.S02E26E27` variants,
    two `Prep.and.Landing.2009` variants, `Disclosure.Day.2026`). Change: the sweep's scope narrows to
    the music paths — `complete/nzb/music/`, `complete/nzb/unsorted/`, `complete/nzb/dj-mixes/`,
    `/mnt/tank/downloads/lidarr-import/` and the new `complete/nzb/_inbox/`. Substance: the six
    TV/movie items are not lost — they are named in `05-PREMEASURE.md` section 5 and belong to Sonarr's
    and Radarr's own failure handling. Rejected alternative: quarantining another service's failed jobs
    into a music-project folder, which would also go red again the moment those services fail anything.
    Also note that `incomplete/` is out of scope entirely (D-15): it is SABnzbd's live working
    directory, drained at roughly one music job per 72 seconds, and moving or deleting anything under
    it can break an active download.

    Fold D-14 in as a ONE-LINE factual note inside Amendment A, not as an amendment of its own: the
    Potter clause names the wrong tree — a `find` over `dj-mixes` to depth 3 for `*potter*` returns
    zero (`05-PREMEASURE.md` section 4) — and the rip that exists is
    `complete/nzb/unsorted/Harry.Potter.And.The.Deathly.Hallows.Part.1.2010.PROPER.1080p.BluRay.x264-MOOVEE`,
    which is ordinary junk: a video in a music folder, taken through D-13's approval gate with
    everything else and deleted. Refiling it to the movies tree was rejected — this phase does not hand
    content to another service's import path uninvited. The second rip,
    `incomplete/Harry.Potter…hallowed` with its `__ADMIN__` directory, is SABnzbd's to resolve.

    AMENDMENT B — criterion 3 (ROADMAP.md lines 666-667), decision refs D-02, D-05, D-08.
    Original wording to quote: "The 45 GB `Now! 1-115` folder is split into per-volume folders, each
    independently importable and abortable, with the per-volume file counts summing back to the
    original count." Measurement: `05-PREMEASURE.md` section 3 — the real folder
    `complete/nzb/unsorted/VA-Now_That.s_What_I_Call_Music__1-115_2023` holds 4,760 files and ZERO
    subdirectories, so the split cannot move existing subfolders because there are none; volume
    boundaries must be derived. The 4,760 resolves as 4,746 mp3 plus 14 sidecars. Changes, all
    clarifications: (1) the per-volume folders are CREATED, not moved, and are created inside the
    existing folder as atomic same-dataset renames (D-07); (2) "the original count" is 4,746 mp3 —
    sidecars are accounted separately and the 24 manifest-only entries in the m3u are reported on their
    own line and never folded into the total (D-08); (3) the headline total is backed by the strictly
    stronger per-volume identity `files_present == sum of tracktotal across that volume's discs`,
    because a bare total of 4,746 would pass even if every file landed in the wrong folder; (4) all
    discs of a volume live in ONE folder — the CD1/CD2 split is not restored, because beets treats one
    directory as one album candidate and handing it two would risk matching one volume as two albums
    (D-05). Substance: the split is kept, not reduced, and the reconciliation is strengthened rather
    than relaxed. Rejected alternative: reconciling against the m3u manifest's 4,770 entries, which
    would fail by 24 on every run and train everyone to ignore it.

    AMENDMENT C — criterion 4 (ROADMAP.md lines 669-671), decision refs D-22 and D-25. Original
    wording stands unchanged and is quoted. Measurement: `05-PREMEASURE.md` section 6 — zero
    underscore-prefixed directories exist anywhere under `/mnt/tank/media/Music`. Change: criterion 4
    is a guard to maintain, not work to perform. It becomes a standing assertion in
    `scripts/quick-health-check.sh`, run before and after this phase's moves, and is proven capable of
    failing by a driven probe. Substance: nothing is reduced; an unasserted green claim becomes an
    asserted one. State D-25's prohibition here too, because it is the adjacent temptation: no
    `tank/downloads` ownership assertion is added to the health check, since the download client keeps
    writing as uid 3000 at roughly one job per 72 seconds and such a check would go red on the next
    download — the exact failure mode Phase 02.1's CR-01 spent four gap-closure plans repairing in the
    other direction.

    Then update the ROADMAP Phase 5 `**Research**:` line to record that research was deliberately
    skipped and that `05-CONTEXT.md` and `05-PREMEASURE.md` carry the measurements instead.

    In `.planning/REQUIREMENTS.md`, add inline ADDENDUM text to the INBX-01, INBX-02 and INBX-03 rows
    of the status table (around line 277), following TAGR-05's shape at line 276 exactly: open with
    `**ADDENDUM 2026-09-18 (05-02, D-XX)**`, use the phrase "neither of which reduces it" (or "none of
    which reduce it" for three points), and close with "The text above is deliberately not rewritten."
    INBX-01 records the measured path `/mnt/tank/downloads/complete/nzb/_inbox` and D-18's
    never-a-dataset prohibition. INBX-02 records the D-12 scope narrowing and the D-14 Potter
    correction. INBX-03 records the flat-folder finding, the 4,746 denominator and the per-volume
    `files == sum(tracktotal)` instrument. Do not edit the requirement text itself in lines 155-165.
  </action>

  <verify>
    <automated>grep -c 'Amended 2026-09-18 by plan 05-02' .planning/ROADMAP.md && grep -c 'ADDENDUM 2026-09-18 (05-02' .planning/REQUIREMENTS.md && grep -c 'The substance is kept, not reduced' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Amended 2026-09-18 by plan 05-02' .planning/ROADMAP.md` returns 3
    - `grep -c 'The substance is kept, not reduced' .planning/ROADMAP.md` returns at least 5 (two pre-existing plus the three added)
    - the original text of criteria 2, 3 and 4 is byte-identical to its pre-plan state — verified with `git diff .planning/ROADMAP.md` showing only added lines within the Phase 5 block, no deletions or modifications of criterion lines 663-671
    - each amendment cites `05-PREMEASURE.md` by section number
    - Amendment A names all four music `_FAILED_`/`_UNPACK_` items and states that the six TV/movie items are named in `05-PREMEASURE.md` section 5
    - Amendment A contains the Potter correction as a single note and does not introduce a separate amendment block for it
    - Amendment B states the number 4,746 and the phrase `files_present == sum of tracktotal` (or the identical identity in words)
    - Amendment C states the D-25 prohibition against a tank/downloads ownership assertion
    - `grep -c 'ADDENDUM 2026-09-18 (05-02' .planning/REQUIREMENTS.md` returns 3
    - `grep -c 'The text above is deliberately not rewritten' .planning/REQUIREMENTS.md` returns at least 4
    - `git diff .planning/REQUIREMENTS.md` shows no change to lines 155-165
  </acceptance_criteria>

  <done>
    Every Phase 5 criterion is satisfiable as written plus its amendment, the original wording is still
    readable, and the three INBX requirements carry addenda that clarify without reducing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fold the criterion-4 assertion into quick-health-check.sh and drive it red</name>

  <read_first>
    - `scripts/quick-health-check.sh` lines 410-500 — SSH_OPTS, REMOTE_TIMEOUT, and the VENDORED_DRIFT_PROMOTED negative-control block
    - `scripts/quick-health-check.sh` lines 796-871 — the canonical bounded-remote-count block with its two-part pipefail fix and three-branch verdict
    - `scripts/quick-health-check.sh` lines 930-950 and 1200-1215 — the existing host-override guard shape for reaching a host other than 172.16.1.159
    - `README.md` section "Health Checks" — the three rules
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — D-22 and D-25
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-PATTERNS.md` section 8
  </read_first>

  <files>
    scripts/quick-health-check.sh
  </files>

  <action>
    Add one assertion block to `scripts/quick-health-check.sh`: zero underscore-prefixed directories
    exist anywhere under `/mnt/tank/media/Music`. Music Assistant silently ignores underscore-prefixed
    folders and Jellyfin does not, so a staging-style name leaking into the library makes the two
    consumers diverge by design and neither reports an error — that divergence is what this asserts
    against.

    Copy the shape of the canonical block at lines 796-871, not its content. Both parts of the two-part
    fix are necessary and both must be present: `set -o pipefail` INSIDE the remote command string, and
    the ssh exit status captured with NO local pipe in front of it, then branched on 124 separately
    from other non-zero statuses. `VAR=$(ssh ... | tr -d ' ')` makes `$?` the tr's status and
    `${PIPESTATUS[0]}` does not rescue it.

    The read targets atlantis at 172.16.1.158, not LXC 100. The file already ssh's almost everywhere to
    .159, and already names atlantis unreachability as a fatal could-not-look at line 81, and already
    carries a host-override guard shape at lines 930-950 and 1200-1215 — reuse that shape rather than
    inventing a second convention. Bound the remote command Linux-side with `timeout $REMOTE_TIMEOUT`
    inside the remote string, because macOS has no GNU `timeout`.

    The remote command counts directories: a find over /mnt/tank/media/Music for type d whose basename
    begins with an underscore, piped to `wc -l`.

    Three branches, never two — "could not look", "there are none" and "BROKEN" are three different
    answers and must not share a verdict:
      - ssh status 124, or a non-zero status, or output that is not a bare integer: print an UNKNOWN
        line that says explicitly that nothing was counted and that this is NOT "zero underscore
        directories", and increment the failure counter.
      - a numeric count greater than zero: print a violation line naming the count, and increment the
        failure counter through the file's existing `fail()` helper so `FAILURES` actually moves.
        `info()` does not increment, and printing through `info()` is precisely the defect plan
        02.1-11 amended a ROADMAP criterion over.
      - a genuine numeric zero: pass.

    Register the new check in the script's selector alongside the existing checks so it runs from the
    single entry point, and add it to the header comment block, which is also the `--help` text.

    DO NOT add a `tank/downloads` ownership assertion. D-25 forbids it: the download client keeps
    writing as uid 3000 at about one job per 72 seconds, so such a check would go red on the next
    download and train everyone to ignore it. Put that prohibition in a comment beside the new block so
    the next person meets the reason rather than the temptation.

    DRIVE THE NEGATIVE CONTROL. `05-PREMEASURE.md` section 6 says the true count today is zero, which
    makes the green path the default and leaves this an assertion nobody has seen fail. From ATLANTIS
    over ssh as real root — not from a container, because Phase 1's D-20 says nobody holds rw on the
    library until Phase 6 and atlantis is the sanctioned route D-21 names — create
    `/mnt/tank/media/Music/_probe`, run the health check, confirm it goes red and that `FAILURES` is
    non-zero, then remove `_probe` and run the check again and confirm green. Guard the probe with an
    EXIT trap that removes it, and assert its absence afterwards. Do not attempt a chmod on it; mkdir
    and rmdir both work on tank, chmod does not. Jellyfin's real-time monitoring and metadata savers
    are off for the Music library (Phase 1, 01-06), so an empty directory present for seconds writes
    nothing.

    Record the driven control in the block's comment in the VENDORED_DRIFT_PROMOTED style: state that
    the check was seen red on a deliberately created probe and green after its removal, with the date,
    so the history is kept and the next reader knows the instrument can distinguish something.
  </action>

  <verify>
    <automated>bash -n scripts/quick-health-check.sh && grep -c 'mnt/tank/media/Music' scripts/quick-health-check.sh && grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -c 'tank/downloads.*568'</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/quick-health-check.sh` exits 0
    - the new block's remote command string contains `set -o pipefail` and `timeout $REMOTE_TIMEOUT`
    - the ssh status is assigned to its own variable on the line immediately after the ssh call, with no local pipe on the ssh line
    - the block has exactly three verdict branches, and the violation branch calls the failure helper that increments `FAILURES`, not `info()`
    - `grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -c 'tank/downloads.*568'` returns 0 — no tank/downloads ownership assertion was added (D-25)
    - running the check with `/mnt/tank/media/Music/_probe` present exits non-zero and prints a violation naming the count 1
    - running the check after `_probe` is removed exits 0 for that block and prints a pass
    - `ssh root@172.16.1.158 "find /mnt/tank/media/Music -maxdepth 1 -type d -name '_*' | wc -l"` returns 0 at task end
    - the block's comment records the driven negative control with its date
  </acceptance_criteria>

  <done>
    Criterion 4 is asserted by the estate's standing check, the assertion has been observed both red
    and green on real state, and the adjacent D-25 temptation is documented as forbidden rather than
    merely omitted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstation / LXC 100 to atlantis (real root) via ssh | The negative-control probe is created inside the library tree as real root, crossing Phase 1's D-20 single-writer fence deliberately and transiently. |
| Planning records to future executors | An amendment that silently reduces scope is a durable, invisible loss of requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | The `_probe` directory is left inside `/mnt/tank/media/Music` | mitigate | EXIT trap removes it; an acceptance criterion asserts a find for `_*` under the library returns 0 at task end; the health check itself re-asserts it |
| T-05-02-02 | Tampering | Amendment silently replaces criterion text instead of annotating it | mitigate | Acceptance criterion requires `git diff .planning/ROADMAP.md` to show only added lines within the Phase 5 block, with no deletions on criterion lines 663-671 |
| T-05-02-03 | Repudiation | The criterion-4 check reports green because it could not look | mitigate | Three-branch verdict with UNKNOWN distinguished from zero; ssh status branched on 124; `set -o pipefail` inside the remote string |
| T-05-02-04 | Denial of Service | A permanently red check trains the reader to ignore the whole file | mitigate | D-25's tank/downloads ownership assertion is explicitly not added, and its absence is asserted by grep |
| T-05-02-05 | Elevation of Privilege | Writing into the library from a container, breaching Phase 1 D-20 | mitigate | The probe is created from atlantis as real root, the sanctioned route; no container gains an rw mount and no mount is changed |
| T-05-02-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | This plan installs no packages from any package manager. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- Three dated amendments exist in `.planning/ROADMAP.md` under Phase 5 criteria 2, 3 and 4, each
  quoting its original, citing `05-PREMEASURE.md`, naming the change and preserving substance.
- Three ADDENDUM entries exist on INBX-01/02/03 in `.planning/REQUIREMENTS.md`, none of which rewrite
  the requirement text.
- `scripts/quick-health-check.sh` passes `bash -n`, carries the criterion-4 assertion with three
  branches, and has been observed red with a probe present and green with it removed.
- No `tank/downloads` ownership assertion exists in the health check.
</verification>

<success_criteria>
The written criteria match the measured reality without losing any substance, and criterion 4 is
carried by an instrument that has been driven to fail. A verifier reading `ROADMAP.md` at phase end
meets a satisfiable criterion set with its history intact.
</success_criteria>

<output>
Create `.planning/phases/05-inbox-structure-and-the-junk-gate/05-02-SUMMARY.md` when done.
</output>
