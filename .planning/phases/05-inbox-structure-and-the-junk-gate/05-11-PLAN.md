---
phase: 05-inbox-structure-and-the-junk-gate
plan: 11
type: execute
wave: 10
depends_on: ["05-02", "05-04", "05-07", "05-09", "05-10"]
files_modified:
  - stacks/selfhosted/arrs/beets.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [INBX-01, INBX-02, INBX-03]

must_haves:
  truths:
    - "All four ROADMAP Phase 5 criteria are re-asserted at phase end, from live state, after every move this phase made"
    - "Criterion 4 is asserted AFTER the phase's moves as well as before, so a staging-style name could not have leaked into the library (D-22)"
    - "stacks/selfhosted/arrs/beets.md carries a dated Phase 5 closure section beside the stack, as Phases 1, 2, 02.1 and 4 each did"
    - "The closure records the traps that will mislead the next person, including D-03's Vol.36 CD1 and Vol.36 double-space CD2 verbatim"
    - "The closure records what was recorded but not fixed: volumes 4/8/9's tracktotal surplus, the 24 located missing tracks, and the per-file Discogs release-id tag"
    - "INBX-01, INBX-02 and INBX-03 are marked Complete in REQUIREMENTS.md and the ROADMAP phase entry reflects the executed plans"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "The durable estate record of Phase 5 beside the stack it concerns"
      contains: "Phase 5"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INBX-01/02/03 marked Complete with their addenda intact"
  key_links:
    - from: "stacks/selfhosted/arrs/beets.md Phase 5 section"
      to: ".planning/phases/05-inbox-structure-and-the-junk-gate/"
      via: "verdicts quoted from the named evidence artifact, not paraphrased"
      pattern: "05-"
---

<objective>
Close the phase where the next person will look: beside the stack, dated, with the traps named.

`stacks/selfhosted/arrs/beets.md` is the durable estate record; Phases 1, 2, 02.1 and 4 each landed
their closure there. This phase adds its section, and re-asserts all four criteria from live state
first — because a closure written from plan summaries records what was intended, and the point of the
record is what is true.

Criterion 4 in particular is asserted a second time here. D-22 requires it before AND after the phase's
moves, so that a staging-style name cannot have leaked into the library during the split, the sweep or
the chown. Music Assistant silently ignores underscore-prefixed folders and Jellyfin does not, so the
two consumers would diverge by design and neither would report an error.

Purpose: leave a record that is checkable, and a requirement set that matches reality.
Output: a Phase 5 section in `beets.md`, updated requirement statuses, and an updated roadmap entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-NOW-INVENTORY.md
@.planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md
@stacks/selfhosted/arrs/beets.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-assert all four criteria from live state at phase end</name>

  <read_first>
    - `.planning/ROADMAP.md` Phase 5, criteria 1 to 4 and the three amendments plan 05-02 added
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-INBOX-PATHS.md` — the measured paths
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-NOW-INVENTORY.md` — the pre-declared exception table
    - `scripts/quick-health-check.sh` — the criterion-4 block plan 05-02 added
    - the SUMMARY files from plans 05-01, 05-04, 05-07, 05-09 and 05-10
  </read_first>

  <files>
    /mnt/fast/safety/phase05/phase05-final-assertions.txt
  </files>

  <action>
    Assert each criterion from live state, not from the plan summaries, and write the results to
    /mnt/fast/safety/phase05/phase05-final-assertions.txt. Bound every remote command Linux-side and put
    `set -o pipefail` inside any remote string containing a pipe, reading the ssh status with no local
    pipe in front of it.

    CRITERION 1. From atlantis: the six directories exist under
    /mnt/tank/downloads/complete/nzb/_inbox, `_inbox` shares a devid with unsorted/ and dj-mixes/, no
    dataset under tank/downloads is named _inbox, and nothing equivalent exists under /mnt/tank/media to
    depth 3. Re-run the D-21 inode proof once more now that the chown has happened — both the positive
    control between two inbox directories and the cross-dataset negative control — and clean up after
    itself. The property should be unchanged by ownership normalisation; asserting it again is cheap and
    it closes the loop on the one criterion that is about a filesystem property rather than a state.

    CRITERION 2. From LXC 100 across the five music paths, excluding 99-quarantine: zero directories
    whose basename begins `_FAILED_` or `_UNPACK_`, and zero files matching the `.rar` and multipart
    forms. Confirm the Potter rip path and the empty dj-mixes decoy are both absent. Confirm
    /mnt/tank/downloads/lidarr-import is absent, or record what remains and that Phase 1's D-23 stays
    open.

    CRITERION 3. Inside the collection folder: exactly 115 directories at depth 1 named Vol 001 through
    Vol 115, zero directories at depth 2, the sum of per-volume mp3 counts equal to the freshly measured
    collection total, and the per-volume `files == sum(tracktotal)` exception set matching
    05-NOW-INVENTORY.md's pre-declared list. Report the manifest-only entry count on its own line. Also
    assert the tag deliverable that made the split coherent: each of the 115 folders contains exactly one
    distinct album value.

    CRITERION 4. Run `scripts/quick-health-check.sh` and confirm its criterion-4 block passes on live
    state. This is the AFTER half of D-22's before-and-after pair. The BEFORE half was run in plan 05-02
    along with the driven negative control; do not re-drive the negative control here — creating a probe
    inside the library at phase close would be gratuitous. Record that the negative control was driven
    once, in plan 05-02, with its date, so the instrument's credibility travels with the result.

    Additionally confirm two decisions held rather than merely being stated. D-25: grep the
    comment-stripped health check and confirm it carries no tank/downloads ownership assertion. D-18:
    confirm no ZFS dataset named `_inbox` exists under tank/downloads.

    Report any criterion that is not TRUE plainly. OPEN is not FAIL: a FAIL needs a violated condition.
    Say which it is.
  </action>

  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; test -s /mnt/fast/safety/phase05/phase05-final-assertions.txt && grep -c 'CRITERION' /mnt/fast/safety/phase05/phase05-final-assertions.txt"</automated>
  </verify>

  <acceptance_criteria>
    - the file records a verdict line for each of criteria 1, 2, 3 and 4, each with the command output it rests on
    - criterion 1: six directories present, `_inbox` devid equal to unsorted/'s, zero `_inbox` datasets, zero equivalent directories under /mnt/tank/media, and a fresh inode proof showing unchanged inode on the same-dataset move and changed inode across the boundary
    - criterion 2: zero `_FAILED_`/`_UNPACK_` directories and zero `.rar`-form files across the five music paths outside 99-quarantine; Potter rip absent; dj-mixes decoy absent
    - criterion 3: exactly 115 depth-1 directories, zero depth-2 directories, the per-volume sum equal to the collection total, and the exception set matching the pre-declared list with every difference named
    - criterion 3 also: each of the 115 folders contains exactly 1 distinct album value
    - criterion 4: `scripts/quick-health-check.sh`'s criterion-4 block passes, and the record notes the negative control was driven in plan 05-02 with its date
    - `grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -c 'tank/downloads.*568'` returns 0
    - any criterion not TRUE is stated as OPEN or FAIL with the distinction made explicitly
  </acceptance_criteria>

  <done>
    The phase's claims are re-derived from the estate as it now stands, not carried forward from the
    plans that intended them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 5 closure section into beets.md and update the requirement statuses</name>

  <read_first>
    - `stacks/selfhosted/arrs/beets.md` lines 1126-1165 — the Phase 4 closure section, the closest template: a heading carrying the verdict and the date, verdicts quoted from a named evidence artifact rather than paraphrased, superseded verdicts kept beside current ones, distinctions stated plainly, later developments added in band and dated
    - `stacks/selfhosted/arrs/beets.md` line 974 — the Phase 3 section heading style
    - `/mnt/fast/safety/phase05/phase05-final-assertions.txt` from Task 1
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-NOW-INVENTORY.md` and `05-INBOX-PATHS.md`
    - `.planning/REQUIREMENTS.md` status table around line 277
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/05-CONTEXT.md` — the Deferred Ideas section, which is the source for what goes under Recorded, not fixed and Still open
  </read_first>

  <files>
    stacks/selfhosted/arrs/beets.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>

  <action>
    Add a Phase 5 section to `stacks/selfhosted/arrs/beets.md` following the house conventions visible in
    the Phase 4 and Phase 3 sections. The heading carries the verdict and the date. Verdicts are QUOTED
    from the named evidence artifact, not paraphrased — cite
    /mnt/fast/safety/phase05/phase05-final-assertions.txt and the plan summaries by number. Distinctions
    are stated plainly: OPEN is not FAIL, and a FAIL needs a violated condition.

    Sub-headings to write, modelled on Phase 4's:

    THE FOUR CRITERIA — one line each with its verdict from Task 1. Record criterion 4 as it actually is:
    already green before the phase started, now ASSERTED by the standing health check, with the negative
    control driven once in plan 05-02 on a deliberately created probe. Record criterion 2 and criterion 3
    as satisfied against their dated amendments, naming the amendment.

    WHAT KEEPS THESE TRUE — the standing mechanisms, not the one-off actions: the criterion-4 assertion
    in quick-health-check.sh; the two approval-gated scripts, which leave the rules written down for
    Phase 8's new inflow; and D-18's prohibition on `_inbox` ever becoming a dataset, which is what keeps
    criterion 1's atomic-rename property alive.

    TRAPS THAT WILL MISLEAD THE NEXT PERSON — at minimum: D-03's volume-number trap VERBATIM, naming
    `Vol.36 CD1` and `Vol.36  CD2` with the double space intact, and stating that a trailing-number regex
    exits 0 and silently misfiles 36 tracks into volumes 1 and 2; D-18's dataset trap, which looks like a
    tidy improvement; the 65534 reading, which is LXC 100's view and not the disk; the fact that the stat
    manifest cannot see a chown because it keys on mtime while chown moves ctime; and that `ffprobe`
    takes one input file, so a batched call exits clean and produces nothing.

    RECORDED, NOT FIXED — volumes 4, 8 and 9's tracktotal surplus with whatever plan 05-05 concluded
    about it, handed to Phase 6; the 24 manifest-only entries, located per volume and not chased,
    because re-acquisition is not scoped by this project; and the per-file `LOCATION` tag carrying a
    direct Discogs release id on every track in this collection, which is potentially a large shortcut
    for Phase 6's CONF-05 disambiguation and is recorded rather than acted on.

    STILL OPEN — D-19's `_done/` pruning mechanics at backlog scale, which Phase 9's batch cadence makes
    operationally real; `dj-mixes`'s 84 inconsistent top-level names, carried to Phase 6; and anything
    Task 1 reported as OPEN.

    HOW TO RE-RUN — the three new scripts by name with their subcommands, the fact that both destructive
    ones refuse without an approved file and a snapshot proof, and the note that delivery is git so a
    commit must reach origin before LXC 100 can pull.

    ROLLBACK — name `tank/downloads@pre-phase5` and state honestly what rolling back would cost: it
    predates the junk sweep, the split and the tag write, and also predates everything downloaded since
    it was taken. Do not describe it as a clean undo, because it is not one.

    Then update `.planning/REQUIREMENTS.md`: mark INBX-01, INBX-02 and INBX-03 Complete in the status
    table, keeping their plan-05-02 addenda intact and not rewriting the requirement text at lines
    155-165. Also tick the three INBX checkboxes in the requirement list itself.

    Then update `.planning/ROADMAP.md`'s Phase 5 entry: set the Plans line to the executed plan list with
    each plan's one-line objective, and add the executed verdict beside each criterion in the style
    Phase 1 uses at lines 73-74 and 87-88 — a bold TRUE-as-of marker naming the plan that made it true.
    Do not remove or rewrite the three amendments plan 05-02 added.
  </action>

  <verify>
    <automated>grep -c 'Phase 5' stacks/selfhosted/arrs/beets.md && grep -c 'Vol.36  CD2' stacks/selfhosted/arrs/beets.md && grep -c 'INBX-01 | Phase 5 | Complete' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `stacks/selfhosted/arrs/beets.md` contains a Phase 5 section whose heading carries a verdict and the date
    - the section contains the literal string `Vol.36  CD2` with two spaces
    - the section has sub-headings covering the four criteria, what keeps these true, traps, recorded-not-fixed, still open, how to re-run, and rollback
    - the section quotes at least one verdict from `/mnt/fast/safety/phase05/phase05-final-assertions.txt` and names that artifact
    - the rollback sub-heading states that `tank/downloads@pre-phase5` predates the sweep, the split, the tag write and a month of downloads, and does not describe it as a clean undo
    - INBX-01, INBX-02 and INBX-03 are marked Complete in the REQUIREMENTS status table and their plan-05-02 addenda are still present
    - `git diff .planning/REQUIREMENTS.md` shows no change to lines 155-165 other than the three checkbox ticks
    - the ROADMAP Phase 5 Plans line names the executed plans, and each criterion carries a TRUE-as-of marker naming the plan that made it true
    - the three amendments plan 05-02 added to ROADMAP Phase 5 are unchanged
  </acceptance_criteria>

  <done>
    The phase is closed in the place the estate keeps its operational truth, with the traps named where
    the next person will meet them and the rollback described honestly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan summaries to the durable record | A closure written from intentions rather than from live state launders unverified claims into looking twice-confirmed. |
| Estate documentation to future operators | A record that describes a snapshot as a clean undo would be acted on as one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Repudiation | The closure records what was planned rather than what is true | mitigate | Task 1 re-asserts all four criteria from live state first, and the closure quotes the resulting artifact by name rather than the plan summaries |
| T-05-11-02 | Tampering | The rollback is described as a clean undo and is later acted on as one | mitigate | The rollback sub-heading states explicitly that the snapshot predates the sweep, the split, the tag write and a month of unrelated downloads |
| T-05-11-03 | Tampering | The plan-05-02 amendments or the requirement text are overwritten while marking things Complete | mitigate | Acceptance asserts the amendments are unchanged and that `git diff` shows no change to the requirement text lines beyond the checkbox ticks |
| T-05-11-04 | Repudiation | Criterion 4's green result is recorded without the negative control that makes it credible | mitigate | The closure records that the control was driven once in plan 05-02 on a deliberately created probe, with its date |
| T-05-11-05 | Elevation of Privilege | Re-driving the criterion-4 negative control at close writes into the library again for no benefit | accept | The control is deliberately NOT re-driven here; the plan says so and cites the plan-05-02 run instead |
| T-05-11-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | This plan installs no packages from any package manager. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- All four criteria carry a verdict derived from live state at phase end, recorded in
  `phase05-final-assertions.txt`.
- Criterion 4 is asserted after the phase's moves as well as before.
- `beets.md` carries a dated Phase 5 section with the traps, the recorded-not-fixed items, the still-open
  items, the re-run instructions and an honest rollback description.
- INBX-01, INBX-02 and INBX-03 are Complete with their addenda intact.
- The ROADMAP Phase 5 entry names the executed plans and carries per-criterion TRUE-as-of markers, with
  the three amendments unchanged.
</verification>

<success_criteria>
The phase closes with claims that were re-measured rather than restated, a record beside the stack that
names the traps a future reader will otherwise rediscover, and a requirement set that matches the estate.
</success_criteria>

<output>
Create `.planning/phases/05-inbox-structure-and-the-junk-gate/05-11-SUMMARY.md` when done.
</output>
