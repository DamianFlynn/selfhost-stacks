---
phase: 01-safety-harness-and-freeze-the-writers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-music-freeze.sh
autonomous: true
requirements: [WRIT-01, WRIT-04, SAFE-05]

must_haves:
  truths:
    - "The WRIT-01 audit reports running-container rw holders on /mnt/tank/media/Music AND declared rw mounts across every stack YAML, as two separately labelled classes (D-08, D-26, and the false-pass finding: beets.yaml and soulbeet.yaml are commented out of arrs/compose.yaml so no running container exists for them)"
    - "The audit classifies rw holders as tagger-class versus consumer-class, so Jellyfin's documented exception (D-21) cannot be misread as a failure"
    - "The real directory names under /mnt/tank/media are read from the host and recorded, before any mount is narrowed (D-19 depends on this; a bind mount to a non-existent source silently creates an empty directory)"
    - "A stat census over all 13 artist folders under /mnt/tank/media/Music is recorded, showing today's mixed 568:65534 / 568:568 state (D-11 baseline)"
    - "A .nfo/.jpg/.lrc sidecar inventory across the 13 artist folders exists as the durable D-18 artifact and as the baseline the SAFE-05 one-hour test measures against"
    - "The audit script exits 1 on any failed assertion in its default mode, breaking the estate's silent-green precedent (the docker created-state blind spot that hid two down containers for six weeks) — and exits 0 in --baseline mode so it can report before the harness exists (D-27 verify half, D-29 consumer)"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "Read-only harness audit: mount classes, media layout, ownership census, sidecar inventory, fence spot-check"
      contains: "--baseline"
    - path: "/mnt/fast/safety/music-pre-project/audit/baseline-<date>.txt"
      provides: "The recorded before-state of every assertion this phase will later make true"
    - path: "/mnt/fast/safety/music-pre-project/audit/sidecars-baseline.txt"
      provides: "D-18 sidecar inventory — the SAFE-05 one-hour test baseline"
  key_links:
    - from: "scripts/check-music-freeze.sh"
      to: "docker inspect .Mounts on LXC 100"
      via: "running-container mount enumeration (D-26)"
      pattern: "range \\.Mounts"
    - from: "scripts/check-music-freeze.sh"
      to: "stacks/selfhosted/**/*.yaml"
      via: "declared-mount grep — the class a running-container-only audit misses"
      pattern: "mnt/tank/media"
---

<objective>
Build the read-only audit that every later plan in this phase is graded by, then run it once to
record the before-state.

This plan is deliberately first and deliberately non-mutating. The phase's six roadmap criteria are
all assertions about host state; writing the assertion mechanism before making any change means
each subsequent plan turns a red line green, and the final plan re-runs the same script rather than
inventing a fresh check that happens to pass.

Purpose: WRIT-01 is unmeetable without a correct audit, and a naive audit reports a **false pass**.
`stacks/selfhosted/arrs/beets/beets.yaml` and `stacks/selfhosted/arrs/soulbeet.yaml` are both
commented out of `stacks/selfhosted/arrs/compose.yaml` (lines 39 and 36), so no running container
exists for either regardless of the rw mounts their YAML declares. D-08/D-26 specify
running-container inspection; that is necessary but not sufficient. The audit reports both classes,
separately.

Purpose (second): D-19 hard-codes `/mnt/tank/media/Movies`, `/TV`, `/Books` as narrowed mount
sources. Those names have never been verified against the host. A Docker bind mount whose source
path does not exist is silently created as an empty directory rather than erroring — narrowing
Radarr to a typo would quietly break it. This plan reads the real listing first.

Output: `scripts/check-music-freeze.sh` committed and executable, plus a baseline report, an
ownership census and a sidecar inventory under the fence directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-harness-and-freeze-the-writers/01-CONTEXT.md
@.planning/phases/01-safety-harness-and-freeze-the-writers/01-PATTERNS.md
@scripts/check-renovate.sh
@scripts/check-server-health.sh
@stacks/selfhosted/arrs/compose.yaml
@CLAUDE.md
@STANDARDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-music-freeze.sh — the read-only harness audit</name>

  <read_first>
    - `scripts/check-renovate.sh` — the estate's most developed read-only audit. Copy its exact
      skeleton: `#!/usr/bin/env bash`, the `# name.sh - purpose` + `# Usage:` header pair,
      `set -euo pipefail`, `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"; cd "$REPO_ROOT"`,
      the five colour constants (RED/GREEN/YELLOW/BLUE/NC), the `# N. Description` numbered-section
      pattern with an emoji heading and a `━` box-drawing rule, the pass/warn `if … else` branch
      shape, and the trailing `📊 Summary` counts block.
    - `scripts/check-server-health.sh` — its lines 8-11 reachability guard and `exit 1` convention.
    - `scripts/setup-mpe.sh` — confirms the host-resident script convention (`#!/usr/bin/env bash`,
      `set -euo pipefail`, ALL-CAPS constants at top) that this script follows.
    - `scripts/quick-health-check.sh` — read to understand what plan 01-09 will later fold this
      script into; do NOT edit it in this plan and do NOT adopt its `#!/bin/bash` / no-strict-mode
      style here.
    - `stacks/selfhosted/arrs/compose.yaml` — confirm with your own eyes that `beets.yaml` (line 39)
      and `soulbeet.yaml` (line 36) are commented out of the `include:` list. This is why the
      declared-mount scan exists.
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/01-CONTEXT.md` § "The rw-mount
      inventory" — the eleven containers currently holding a rw path containing Music.
    - `CLAUDE.md` § Platform and § Storage and Permissions — host addresses, repo and appdata paths.
  </read_first>

  <files>scripts/check-music-freeze.sh</files>

  <action>
Create `scripts/check-music-freeze.sh` as a **host-resident** script: it runs on LXC 100
(`root@172.16.1.159`) from `/mnt/fast/stacks` after a `git pull`, in the `setup-mpe.sh` /
`pg-migrate.sh` convention — `#!/usr/bin/env bash`, `set -euo pipefail`, ALL-CAPS constants. It is
host-resident rather than workstation-resident because `stat` on `/mnt/tank`, `zfs list` and
`find` over the library cannot be expressed as one ssh'd command each. Plan 01-09 folds it into the
workstation-resident `scripts/quick-health-check.sh` by invoking it over a single
`ssh root@172.16.1.159 'bash /mnt/fast/stacks/scripts/check-music-freeze.sh …'`, which is how both
conventions are honoured without mixing them inside one file.

Constants at the top: `LIBRARY="/mnt/tank/media/Music"`, `MEDIA_ROOT="/mnt/tank/media"`,
`FENCE="/mnt/fast/safety/music-pre-project"`, `STACKS="/mnt/fast/stacks/stacks/selfhosted"`,
`UIDGID="568:568"`, `DIR_MODE="755"`, `FILE_MODE="644"`.

Accept one optional flag, `--baseline`. In `--baseline` mode the script prints every finding and
always exits 0 — this is how it is run before the harness exists. In default mode every check is an
assertion: it increments a `FAILURES` counter on a red finding and the script ends `exit 1` if
`FAILURES` is non-zero. **Establish this exit convention explicitly in the header comment**, noting
that `check-renovate.sh` exits 0 on every finding except a missing config and
`quick-health-check.sh` never exits non-zero at all, and that inheriting that silence is what let
the docker `created`-state blind spot hide two down containers for six weeks.

Implement these numbered sections:

1. **Running-container rw holders on the library (WRIT-01, D-26).** Enumerate mounts of every
   running container with
   `docker ps -q` piped into `docker inspect` using the Go template
   `{{.Name}} {{range .Mounts}}{{.Source}}:{{.Destination}}:{{if .RW}}rw{{else}}ro{{end}} {{end}}`.
   This template surface has no analog in the repo — it is new logic; only the surrounding block
   shape is copied. Select rows whose `.Source` is `/mnt/tank/media/Music` or a prefix of it
   (`/mnt/tank/media`, `/mnt/tank`) and whose flag is `rw`. Classify each hit:
   **tagger-class** if the container name matches `beets|soulbeet|wrtag|lidarr`; **consumer-class**
   if it is `jellyfin`; **unclassified** otherwise. Report as
   `tagger-class writers: N` / `consumer-class writers: <names> (documented exception, D-21)` /
   `unclassified writers: <names>`. Assert `tagger-class == 0` and `unclassified == 0`.
   Do NOT assert a bare total of 1 — the one is Jellyfin, and a bare count is misleading (D-21).

2. **Declared rw mounts across stack YAML (the false-pass guard).** Grep every `*.yaml` under
   `$STACKS` for volume lines whose host side contains `/mnt/tank/media`, and report each with its
   file, its host:container mapping and its explicit `:ro`/`:rw` suffix or the literal token
   `NO-SUFFIX` where the line carries neither (a missing suffix means rw). Report this as a
   separate labelled class from section 1 and state in the output why: a container that is
   commented out of `arrs/compose.yaml` is invisible to section 1 while its YAML still declares a
   rw path, so section 1 alone reports a false pass. Assert that no file other than
   `media/jellyfin.yaml` declares a rw or unsuffixed host path that contains
   `/mnt/tank/media/Music`.

3. **`/mnt/tank/media` layout (D-19 precondition).** Print `ls -1` of `$MEDIA_ROOT` with a per-entry
   `stat -c '%F %n'`. This section never asserts — it is the source of truth that plan 01-05 reads
   before hard-coding any narrowed mount source. Print an explicit line
   `D-19 mount sources must be chosen only from the names above.`

4. **Ownership census (WRIT-04, D-11/D-12).** Run `stat -c '%u:%g %a %n'` over the immediate
   children of `$LIBRARY` (the 13 artist folders), then count files under `$LIBRARY` whose
   `%u:%g` differs from `$UIDGID`, whose directory mode differs from `$DIR_MODE`, and whose file
   mode differs from `$FILE_MODE`. Report the counts. Assert all three counts are 0.

5. **Sidecar inventory (SAFE-05, D-18).** `find "$LIBRARY" -type f \( -name '*.nfo' -o -name '*.jpg'
   -o -name '*.lrc' \) -printf '%p\n' | sort`, and report the total plus a per-extension count. Emit
   the sorted path list to stdout only when `--sidecars` is also passed, so the list can be
   redirected to a file without the surrounding report. This section never asserts — sidecars are
   inventoried and left in place per D-18.

6. **Fence presence and spot-check (SAFE-02/03/04, D-06/D-08/D-10).** Assert `$FENCE` exists; assert
   `zfs list -t snapshot tank/media/Music@pre-project` and `tank/downloads@pre-project` both exit 0;
   assert the fence subdirectories `library-db/`, `dj-mixes-ffprobe/`, `cover-scans/`, `tags/`
   are non-empty; and assert **no running container mounts any path under `/mnt/fast/safety`** by
   reusing section 1's enumeration (D-08 — "no tagger can write here" is proven, not asserted, and
   it is the same check WRIT-01 needs).

7. **Summary.** A `📊 Summary` counts block in `check-renovate.sh:141-150` shape listing:
   tagger-class writers, consumer-class writers, declared rw Music mounts, media subtrees found,
   ownership mismatches, sidecars found, fence assertions failed, and `FAILURES` total.

Do not add any check that writes, moves, chowns or deletes. This script is read-only by contract;
plan 01-02 and 01-08 own every mutation in this phase.

Commit the file with mode 0755 — `check-renovate.sh`, `check-server-health.sh`, `pg-migrate.sh`,
`setup-mpe.sh` and `setup-mpe-cloudflare.sh` are all 0755; the 0644 scripts are drift, not a
convention.
  </action>

  <verify>
    <automated>bash -n scripts/check-music-freeze.sh && test -x scripts/check-music-freeze.sh && grep -q 'set -euo pipefail' scripts/check-music-freeze.sh && grep -q -- '--baseline' scripts/check-music-freeze.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/check-music-freeze.sh` exits 0.
    - `test -x scripts/check-music-freeze.sh` exits 0 (committed with the exec bit).
    - `head -1 scripts/check-music-freeze.sh` is exactly `#!/usr/bin/env bash`.
    - `grep -c 'set -euo pipefail' scripts/check-music-freeze.sh` returns 1.
    - `grep -v '^#' scripts/check-music-freeze.sh | grep -c 'range \.Mounts'` returns at least 1.
    - `grep -v '^#' scripts/check-music-freeze.sh | grep -c 'tagger-class'` returns at least 1, and
      the same for `consumer-class`.
    - `grep -v '^#' scripts/check-music-freeze.sh | grep -c '/mnt/fast/safety'` returns at least 1.
    - `grep -v '^#' scripts/check-music-freeze.sh | grep -c 'exit 1'` returns at least 1.
    - `grep -cE '^(chown|chmod|rm|mv|zfs (snapshot|rollback|destroy))' scripts/check-music-freeze.sh`
      returns 0 — the script performs no mutation.
  </acceptance_criteria>

  <done>`scripts/check-music-freeze.sh` exists, is syntactically valid, is mode 0755, implements all seven numbered sections, distinguishes tagger-class from consumer-class writers, scans declared mounts as a separate class, and mutates nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Run the baseline audit on LXC 100 and record the before-state</name>

  <read_first>
    - `scripts/check-music-freeze.sh` — the script created in Task 1.
    - `CLAUDE.md` § Platform — LXC 100 is `172.16.1.159`, repo at `/mnt/fast/stacks`.
    - `.planning/PROJECT.md` § Constraints — "Ownership is undecided": 12 of 13 artist folders are
      `568:65534`, 1 is `568:568`. Task 2's census must either confirm this or record that it has
      changed since 2026-08-17.
    - `.planning/phases/01-safety-harness-and-freeze-the-writers/01-CONTEXT.md` § "The rw-mount
      inventory" — the eleven-container table to compare the live audit against.
  </read_first>

  <files>/mnt/fast/safety/music-pre-project/audit/ (created on LXC 100, not in git)</files>

  <action>
Push the new script to the host and run it in reporting mode.

On the workstation, confirm the commit is present, then on LXC 100
(`ssh root@172.16.1.159`): `cd /mnt/fast/stacks && git pull`.

Create the fence root and its audit subdirectory with `mkdir -p /mnt/fast/safety/music-pre-project/audit`.
Creating the directory is the only filesystem effect of this plan and is non-destructive — the
fence *contents* are plan 01-02's job. Set ownership `568:568` and mode `0755` on the created
directories to match `STANDARDS.md` § Volume Mounting. Note `/mnt/fast` is a different physical
device from `tank` (D-06) and is not subject to the `acltype=nfsv4` rsync failure; no rsync is used
here regardless.

Run `bash scripts/check-music-freeze.sh --baseline` and tee the full output to
`/mnt/fast/safety/music-pre-project/audit/baseline-$(date +%Y%m%d).txt`. Expect a large number of
red findings — the harness does not exist yet. That is the point: this file is the recorded
before-state.

Run `bash scripts/check-music-freeze.sh --baseline --sidecars` and redirect the sorted sidecar path
list to `/mnt/fast/safety/music-pre-project/audit/sidecars-baseline.txt`. This file is the durable
D-18 inventory artifact and is the exact baseline the SAFE-05 one-hour watched-folder test in plan
01-06 diffs against — without it a pre-existing `.nfo` is indistinguishable from a newly written one.

Do **not** stage any of this under `/tmp`: `/tmp` on LXC 100 is tmpfs backed by host RAM, and a
large file there has previously taken the whole 28 GB box down.

From the report, extract and carry into this plan's SUMMARY, verbatim:
  (a) the exact `ls -1 /mnt/tank/media` entry names from section 3 — plan 01-05 hard-codes narrowed
      mount sources from this list and from nothing else;
  (b) the section 4 ownership census, per artist folder, as the WRIT-04 before-state;
  (c) the section 5 sidecar totals per extension;
  (d) the section 1 and section 2 writer lists, and a note of any container in the CONTEXT.md
      eleven-container table that section 1 did not see (expected: `beets`, `soulbeet` — both
      commented out of `arrs/compose.yaml`).
  </action>

  <verify>
    <automated>ssh root@172.16.1.159 'test -s /mnt/fast/safety/music-pre-project/audit/sidecars-baseline.txt || test -f /mnt/fast/safety/music-pre-project/audit/sidecars-baseline.txt; ls -1 /mnt/fast/safety/music-pre-project/audit/ | grep -c "^baseline-"'</automated>
  </verify>

  <acceptance_criteria>
    - `ssh root@172.16.1.159 'ls -1 /mnt/fast/safety/music-pre-project/audit/ | grep -c "^baseline-"'`
      returns at least 1.
    - `ssh root@172.16.1.159 'test -f /mnt/fast/safety/music-pre-project/audit/sidecars-baseline.txt'`
      exits 0.
    - `ssh root@172.16.1.159 'stat -c "%u:%g %a" /mnt/fast/safety/music-pre-project'` returns
      `568:568 755`.
    - `ssh root@172.16.1.159 'bash /mnt/fast/stacks/scripts/check-music-freeze.sh --baseline >/dev/null'`
      exits 0 (baseline mode never fails on findings).
    - The baseline report contains a line matching `tagger-class writers:` and a line matching
      `consumer-class writers:`.
    - The baseline report's section 3 lists at least one entry under `/mnt/tank/media`, and the
      SUMMARY records those names verbatim.
    - The SUMMARY records, per artist folder, the `uid:gid` returned by section 4.
    - No file was written under `/tmp` on LXC 100 by this task.
  </acceptance_criteria>

  <done>A dated baseline report and a sidecar inventory exist under `/mnt/fast/safety/music-pre-project/audit/`; the verified `/mnt/tank/media` subtree names, the per-folder ownership census, the sidecar totals and both writer classes are recorded verbatim in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 over ssh as root | Every command in this plan runs with full root authority on the Docker host and on the ZFS mountpoints |
| script → `/mnt/tank/media/Music` | 34 GB of already-tagged library with no other field-level record until plan 01-04 completes |
| script → `/mnt/fast` | Fence device; a runaway write here has no ZFS snapshot behind it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `scripts/check-music-freeze.sh` | mitigate | The script is read-only by contract. Acceptance criterion asserts `grep -cE '^(chown\|chmod\|rm\|mv\|zfs (snapshot\|rollback\|destroy))'` returns 0. Mutation is confined to plans 01-02 and 01-08 |
| T-01-02 | Denial of Service | `/tmp` on LXC 100 (tmpfs on host RAM) | mitigate | All output staged under `/mnt/fast/safety/…`. Prior incident: 1.1 GB in `/tmp` took the 28 GB box down and killed ssh on host and LXC |
| T-01-03 | Information Disclosure | baseline audit report | accept | Report contains container names, mount paths and uid:gid only. No credentials are enumerated. It is written to `/mnt/fast`, not committed to git |
| T-01-04 | Elevation of Privilege | root ssh session | accept | Estate convention; `CLAUDE.md` § Common Commands already prescribes `ssh root@172.16.1.159`. No new credential is created or stored by this plan |
| T-01-05 | Repudiation | false-pass audit | mitigate | The declared-mount scan (section 2) exists specifically because a running-container-only audit reports a green result for `beets.yaml` and `soulbeet.yaml` while their YAML still declares rw paths to Music |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. `bash`, `docker`, `stat`, `find`, `zfs` are all already present on LXC 100 |
</threat_model>

<verification>
- `bash -n scripts/check-music-freeze.sh` exits 0 and the file is mode 0755.
- `ssh root@172.16.1.159 'bash /mnt/fast/stacks/scripts/check-music-freeze.sh --baseline'` exits 0
  and prints all seven numbered sections.
- The same command without `--baseline` exits 1 at this point in the phase — the harness does not
  exist yet. Confirming the non-zero exit here is what proves the assertion mode works.
- `ssh root@172.16.1.159 'ls /mnt/tank/media'` output matches section 3 of the report exactly.
</verification>

<success_criteria>
- A read-only audit script exists in the repo that measures every WRIT-01, WRIT-04 and SAFE-05
  assertion this phase must later satisfy.
- The audit distinguishes running-container mounts from declared YAML mounts, and tagger-class
  writers from Jellyfin's consumer-class documented exception.
- The real `/mnt/tank/media` subtree names are recorded from the host, unblocking D-19.
- Today's mixed library ownership and today's sidecar population are both recorded before anything
  changes.
</success_criteria>

<output>
Create `.planning/phases/01-safety-harness-and-freeze-the-writers/01-01-SUMMARY.md` when done.
It MUST contain, verbatim from the host: the `/mnt/tank/media` entry names, the per-artist-folder
ownership census, the per-extension sidecar counts, and both writer-class lists.
</output>
