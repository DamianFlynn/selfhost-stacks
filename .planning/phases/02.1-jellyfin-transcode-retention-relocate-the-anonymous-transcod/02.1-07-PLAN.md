---
phase: 02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod
plan: 07
type: execute
wave: 3
depends_on: ["02.1-06"]
files_modified:
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-mounts.txt
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-encoding-diff.txt
autonomous: true
requirements: [TRAN-01, TRAN-02, TRAN-03, TRAN-04]

must_haves:
  truths:
    - "The running Jellyfin container holds zero Type: volume mounts, a /cache bind to /mnt/fast/appdata/media/jellyfin/cache and a /cache/transcodes bind to /mnt/fast/transcode"
    - "The /dev/shm:/data/transcode mount is gone from the running container, not only from the file"
    - "A file written into /cache/transcodes from inside the container appears on /mnt/fast/transcode on the host — the nested bind mounted in the right order"
    - "The ZFS quota still reads 50G after the container was recreated"
    - "Jellyfin's five retention values are set, and the amdgpu mitigation values that a full-object POST would have clobbered are provably still in place"
  artifacts:
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-mounts.txt"
      provides: "the post-recreate docker inspect mount table, the write-through probe and the post-recreate quota reading"
      contains: "/cache/transcodes"
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-encoding-diff.txt"
      provides: "the full jq -S diff of the encoding object before and after the POST"
      contains: "EnableSegmentDeletion"
  key_links:
    - from: "Jellyfin's EncodingOptions.TranscodingTempPath"
      to: "/cache/transcodes, which is a bind of the quota-bounded fast/transcode dataset"
      via: "POST /System/Configuration/encoding, read-modify-write"
      pattern: "TranscodingTempPath"
    - from: "the POST body"
      to: "HardwareAccelerationType: none and EnableHardwareEncoding: false"
      via: "values carried through from the GET response and asserted after the write"
      pattern: "EnableHardwareEncoding"
---

<objective>
Rebuild the container from the delivered compose file, prove the mount shape, and turn on Jellyfin's
own retention levers without re-arming a six-hour outage.

D-27 waives an approval gate on the recreate — it is treated as a routine compose change, accepting
that in-flight playback is interrupted. That waiver covers **only** the recreate. The `docker volume
rm` remains ordered after Jellyfin is *proven* running on the new path (D-10), which is plan 08.

The recreate is what makes the file change real. Deleting the `/dev/shm` line from `jellyfin.yaml`
does not remove the mount from the *running* container; only the recreate does. Docker sorts
container mounts by the **number of path segments** in the destination, so `/cache` (1) mounts before
`/cache/transcodes` (2) regardless of declaration order — that is what `sortMounts` exists for. That
behaviour was read at moby v28.3.2 while this estate runs 29.2.1, which is assumption **A4**, and it
is cheap to falsify: write one file into `/cache/transcodes` from inside the container and check it
appears on `/mnt/fast/transcode` on the host. Do that rather than assuming it.

Then the settings, and this is where the phase's sharpest hazard lives.
`POST /System/Configuration/encoding` is a **full-object replace**: `UpdateNamedConfiguration`
deserialises the entire body into `EncodingOptions` and saves it wholesale, so every property absent
from the body reverts to its C# constructor default. `EnableHardwareEncoding`'s constructor default
is **`true`**; its live value is **`false`**, written at 2026-08-31 18:27 by
`scripts/disable-jellyfin-hwaccel.sh` as the mitigation for the amdgpu HMM kernel oops that took the
whole estate down for six hours. A partial POST silently re-arms it. Read-modify-write is not a
nicety here.

And asserting only the five fields you meant to change is the same shape as 01-06's path-set diff: it
passes while something else was rewritten. So the assertion covers the **non**-changes too, and a
full sorted diff makes anything else that moved visible rather than merely unasserted.

Purpose: TRAN-01's mount shape and TRAN-04's settings, both established with the amdgpu mitigation
provably intact.
Output: a recreated container on the new binds, and the encoding round-trip's before/after evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-CONTEXT.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-RESEARCH.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recreate the container and prove the mount shape, the write-through and the surviving quota</name>
  <files>.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-mounts.txt</files>
  <read_first>
    - `stacks/selfhosted/media/compose.yaml` — the project name (`name: media`) and the `include:` of
      `jellyfin.yaml`, so the compose invocation targets the right project.
    - `stacks/selfhosted/media/jellyfin.yaml` — as delivered by plan 06, so the mounts asserted match
      the mounts declared.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pattern 2* — moby's `mountSort.Less` compares
      `strings.Count(filepath.Clean(destination), "/")`; the sort is on **separator count**, not path
      length, and not declaration order. Also the note that runc creates a missing bind destination
      inside the rootfs, so a real-but-permanently-shadowed empty
      `/mnt/fast/appdata/media/jellyfin/cache/transcodes` will appear on the host and is **inert** —
      do not delete it while the container is running.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Assumptions Log* **A4** — the moby version delta
      and the exact falsification test.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pitfall 3* — `CreateAndCheckMarker` enumerates
      `.jellyfin-*` recursively under the transcode path and throws `InvalidOperationException` on
      any foreign marker, which is a start-refusal.
    - `.planning/phases/02.1-…/02.1-VALIDATION.md` rows **1, 2, 3, 6, 7, 9, 10**.
    - `.planning/phases/02.1-…/artifacts/02.1-06-cache-census.txt` — the captured volume id and the
      pre-copy census, needed for the row-6 re-assertion.
  </read_first>
  <action>
    Recreate the container from the host checkout:
    `cd /mnt/fast/stacks && docker compose -f stacks/selfhosted/media/compose.yaml up -d --force-recreate jellyfin`.
    Run it from `/mnt/fast/stacks` per the estate's stack-ops convention. D-27 accepts that in-flight
    playback is interrupted; confirm the container reaches a running state and record
    `docker inspect jellyfin --format '{{.State.Status}} {{.State.StartedAt}}'`.

    If Jellyfin refuses to start, check for the marker-file guard before anything else:
    `find /mnt/fast/transcode /mnt/fast/appdata/media/jellyfin/cache -name '.jellyfin-*'` and remove
    a foreign one. `GetTranscodePath()` calls `CreateAndCheckMarker(path, "transcode",
    recursive: true)`, which throws on finding any marker other than `.jellyfin-transcode` beneath
    the transcode path.

    Capture the whole mount table into the artifact using the `.Mounts` enumeration form from
    `check-music-freeze.sh:188-199`, with `{{.Type}}` added — `|` is the field separator because
    bind-mount sources are absolute paths containing no `:`, while `:` is compose's own mapping
    separator and would be ambiguous parsed back out.

    Then assert, each separately and each with its own message:

    - VALIDATION.md row **7** (the invariant, D-18): `docker inspect jellyfin --format
      '{{range .Mounts}}{{if eq .Type "volume"}}x{{end}}{{end}}'` is the **empty string** — zero
      `Type: volume` mounts. Assert the invariant, not the id: asserting `d98b2ff9…` is absent would
      pass forever once deleted while a fresh anonymous volume grew unnoticed.
    - VALIDATION.md row **1**: the `/cache/transcodes` destination reports `bind /mnt/fast/transcode`.
    - VALIDATION.md row **2**: the `/cache` destination reports
      `bind /mnt/fast/appdata/media/jellyfin/cache`.
    - VALIDATION.md row **3** (D-02): `/data/transcode` appears in **no** destination — the
      `/dev/shm` mount is gone from the *running* container, not only from the file.

    Falsify or confirm **A4** with a write-through probe rather than trusting the sort: write a
    uniquely named file into `/cache/transcodes` from **inside** the container
    (`docker exec jellyfin …`), then confirm it appears at `/mnt/fast/transcode/<name>` on the host,
    and confirm it does **not** appear under `/mnt/fast/appdata/media/jellyfin/cache/transcodes/`.
    Record both readings. Then remove the probe file. Note in the artifact that an empty,
    permanently-shadowed `cache/transcodes` directory now exists on the host because runc creates a
    missing bind destination — it is inert, and it must not be deleted while the container runs.

    Re-assert VALIDATION.md row **6** on the running system: run the identical `find … -printf` used
    in plan 06 against `/mnt/fast/appdata/media/jellyfin/cache/images` and confirm the file count and
    byte total still match the recorded pre-copy figures. The recreate should not have touched them;
    proving it costs one command and catches a Jellyfin startup that decided to rebuild the cache.

    Assert VALIDATION.md row **10** — the quota **survives a container recreate**, which is the whole
    reason D-12 makes the ZFS property the load-bearing bound rather than an application setting.
    Re-run `ssh root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode'` and assert it still
    equals `53687091200`. Record it in the artifact next to the recreate timestamp so the ordering is
    unambiguous — a quota reading taken before the recreate does not discharge this row.

    Finally run the standing check in **default** mode (not `--baseline`) and record its output. At
    this point rows 1, 2, 3, 7, 9 and 12 should be green while the encoding values (assertion E) are
    still pre-phase and the old-volume assertion is still red, because the volume is deliberately not
    deleted yet.
  </action>
  <verify>
    <automated>ssh -n -o BatchMode=yes root@172.16.1.159 'test -z "$(docker inspect jellyfin --format "{{range .Mounts}}{{if eq .Type \"volume\"}}x{{end}}{{end}}")" && docker inspect jellyfin --format "{{range .Mounts}}{{if eq .Destination \"/cache/transcodes\"}}{{.Type}} {{.Source}}{{end}}{{end}}" | grep -qx "bind /mnt/fast/transcode" && docker inspect jellyfin --format "{{range .Mounts}}{{if eq .Destination \"/cache\"}}{{.Type}} {{.Source}}{{end}}{{end}}" | grep -qx "bind /mnt/fast/appdata/media/jellyfin/cache" && ! docker inspect jellyfin --format "{{range .Mounts}}{{.Destination}}{{\"\n\"}}{{end}}" | grep -qx /data/transcode' && test "$(ssh -n -o BatchMode=yes root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode')" = "53687091200" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker inspect jellyfin --format '{{.State.Status}}'` returns `running`, and `StartedAt` is later than the recreate command's timestamp — recorded in the artifact.
    - `docker inspect jellyfin --format '{{range .Mounts}}{{if eq .Type "volume"}}x{{end}}{{end}}'` returns the **empty string** (VALIDATION.md row 7).
    - The `/cache/transcodes` destination matches `bind /mnt/fast/transcode` exactly under `grep -qx` (row 1), and `/cache` matches `bind /mnt/fast/appdata/media/jellyfin/cache` exactly (row 2).
    - `docker inspect jellyfin --format '{{range .Mounts}}{{.Destination}}{{"\n"}}{{end}}' | grep -qx /data/transcode` exits **1** (row 3).
    - The write-through probe is recorded: a uniquely named file written from inside the container into `/cache/transcodes` appeared at `/mnt/fast/transcode/<name>` on the host and **not** under `/mnt/fast/appdata/media/jellyfin/cache/transcodes/`. Assumption **A4** is recorded as CONFIRMED or FALSIFIED in words. The probe file was removed afterwards.
    - The post-recreate `find … -printf` on `…/cache/images` returns the same file count and byte total as plan 06's pre-copy census (row 6), both figures quoted side by side.
    - `ssh root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode'` returns `53687091200`, taken **after** the recreate, with the ordering recorded (row 10 — a **fires** row).
    - The standing check's output is recorded. Rows 1/2/3/7/9 read green; the old-volume assertion is expected red because the volume is deliberately still present at this point.
    - The anonymous volume still exists: `docker volume ls -q | grep -qx "$(cat /mnt/fast/spike-021/old-vol.txt)"` exits **0**. D-10's ordering is intact.
  </acceptance_criteria>
  <done>The recreated container holds zero volume mounts, both binds resolve to the intended sources, `/data/transcode` is gone from the runtime, a write into `/cache/transcodes` lands on `fast/transcode`, the image cache is intact, and the quota survived the recreate.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the five encoding settings by read-modify-write, and assert what you did not mean to change</name>
  <files>.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-encoding-diff.txt</files>
  <read_first>
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Code Examples* → *Applying the three encoding
      settings (the D-21 round-trip)* (lines 1266-1329) — the six-step body: GET, record the two
      clobber-able values, `jq` merge, POST the whole object, read back and assert **both** the
      change and the non-change, then a full sorted diff. Do not rebuild it from scratch.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pattern 1* — why the POST is a full-object
      replace, the three properties whose live values differ from their constructor defaults, and the
      two traps: `EncoderAppPath` (absent from GET because null; setting it non-empty makes
      `EncodingConfigurationStore.Validate` throw and the POST 500), and `TranscodingTempPath`
      (changing it to a new non-empty value requires `Directory.Exists(newPath)` **as the Jellyfin
      process sees it** — which is why this task is ordered after task 1's bind).
    - `scripts/check-music-consumers.sh` lines **418-427** (`jf_api` and the `-H @<(printf …)`
      process substitution), **500-513** (the credential mode/owner refusal), **555-571** (address
      and key gating, and the WR-01 note that without the key `docker inspect` would still resolve an
      address and the first call would abort on `set -u`), **140-149** (the three wrong ways to
      address Jellyfin).
    - `scripts/freeze-music-apply.sh` lines 1-45 — the mutating-script header shape (`Where it runs`
      / `Usage` / `Workflow` / `⚠️ MUTATES` / `Idempotent`) if this is written as a script rather
      than run inline.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *The Two Discretion Values* — why
      `SegmentKeepSeconds` is **300** and `ThrottleDelaySeconds` stays **180**, including the silent
      clamps `max(keep, 20)` and `max(delay, 60)`.
    - `.planning/phases/02.1-…/02.1-VALIDATION.md` rows **13, 14, 15**.
  </read_first>
  <action>
    Run this on LXC 100. Jellyfin publishes no host port and is reachable only from there, and the
    D-40 key lives at `/mnt/fast/secrets/jellyfin-deercrest.env`, mode 0600 root.

    Resolve the address with
    `docker inspect jellyfin --format '{{(index .NetworkSettings.Networks "t3_proxy").IPAddress}}'`
    plus `:8096`. Never an IP literal (the address moved once already and docker IPAM can move it
    again), never the bare name `jellyfin` (LXC 100's `resolv.conf` carries `search deercrest.info`,
    so it resolves to Cloudflare's public edge and would return 200 from the public website with the
    container down), never `172.16.1.76` (unreachable under macvlan host isolation). Source the
    secrets file only after asserting `stat -c '%a %U'` equals `600 root`, and pass the key through
    `-H @<(printf 'Authorization: MediaBrowser Token="%s"\n' "$JELLYFIN_API_KEY")` so it never enters
    argv. Never echo it; never `set -x`.

    Work under `/mnt/fast/spike-021/`, not `/tmp` (tmpfs).

    **Step 1 — GET the current object** to `encoding.before.json`, immediately before the POST.
    `encoding.xml` was last written 2026-08-31 18:27 by `disable-jellyfin-hwaccel.sh`, so a cached
    copy is not safe. Note that the GET omits only null-valued properties (`WhenWritingNull`), which
    on this server is `EncoderAppPath`, `TranscodingTempPath`, `FallbackFontPath` and
    `EncoderPreset` — 43 keys returned of 47 declared. Feeding the GET body back deserialises those
    to null, which is what they already are.

    **Step 2 — record the two values a partial POST would clobber.** `HardwareAccelerationType`
    (expected `"none"`) and `EnableHardwareEncoding` (expected `false`). **These are the assertion.**

    **Step 3 — merge with `jq` against the GET response.** Never construct the body by hand and in
    particular **never set `EncoderAppPath`**: it is absent from the GET because it is null, and a
    body that helpfully copies `EncoderAppPathDisplay` into it makes `Validate` throw
    `InvalidOperationException("Unable to update encoder app path.")` and the POST returns 500.
    Set exactly five keys: `EnableSegmentDeletion` = `true`, `SegmentKeepSeconds` = `300`,
    `EnableThrottling` = `true`, `ThrottleDelaySeconds` = `180`, `TranscodingTempPath` =
    `"/cache/transcodes"`.

    The two chosen numbers, recorded so they are not re-litigated: `SegmentKeepSeconds` **300**, down
    from 720, because the cleaner deletes the whole prefix behind the client's download head and the
    real cost of a high value is **orphan residue** — the cleaner stops the instant the job exits, so
    a restart leaves `keep + lead` of segments behind for up to 48 h. At this estate's measured
    2.24 MB/s stream-copy rate that is ~2.0 GB per orphan at 720 s versus ~1.1 GB at 300 s. 300 is
    15× the silent clamp floor of 20, and 50 segments is a larger seek-back window than any client UI
    offers; seeking back further costs a transcode restart, not a failure.
    `ThrottleDelaySeconds` **180**, unchanged, because it sits 3× above the hard floor of 60 and
    ~10× above typical client prefetch, so the throttle engages only on genuine run-ahead. Values
    below 60 are silently clamped, so configuring 30 would be a lie in the config file.

    **Step 4 — POST the whole object** to `/System/Configuration/encoding` with
    `Content-Type: application/json` and `--data-binary @…`. Expect **204 No Content**; record the
    HTTP status. No container restart is required — `TranscodingSegmentCleaner` and
    `TranscodingThrottler` both call `_config.GetEncodingOptions()` inside their timer callbacks, so
    they pick the change up on the next tick. Do not restart.

    **Step 5 — read back and assert both directions** with a single `jq -e` compound expression: the
    five intended values, **and** `HardwareAccelerationType` still equal to the step-2 value, **and**
    `EnableHardwareEncoding` still equal to the step-2 value (VALIDATION.md rows 13 and 14). Asserting
    only the five you changed is the 01-06 path-set diff shape — it passes while something else was
    rewritten.

    **Step 6 — a full sorted diff:** `diff <(jq -S . encoding.before.json) <(jq -S . encoding.readback.json)`,
    captured verbatim into the artifact. It must show **exactly** the intended lines and nothing else
    (VALIDATION.md row 15). Note that `TranscodingTempPath` will appear as an *addition* rather than a
    modification, because it was null and therefore omitted from the before-GET. If any other key
    moved, stop and report — that is the full-object-replace hazard firing, and the recovery is to
    POST `encoding.before.json` back unmodified.

    Copy `encoding.before.json`, `encoding.after.json` and `encoding.readback.json` into the
    `.planning/` artifact directory alongside the diff so the evidence is in git, and screen them for
    credentials before committing.
  </action>
  <verify>
    <automated>jq -e '.EnableSegmentDeletion == true and .SegmentKeepSeconds == 300 and .EnableThrottling == true and .ThrottleDelaySeconds == 180 and .TranscodingTempPath == "/cache/transcodes" and .HardwareAccelerationType == "none" and .EnableHardwareEncoding == false' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/encoding.readback.json && test "$(grep -cE '^[<>]' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-07-encoding-diff.txt)" -le 12 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The POST returned HTTP **204**, recorded in the artifact.
    - The `jq -e` compound assertion over `encoding.readback.json` exits 0 for all seven conditions: the five intended values **and** `HardwareAccelerationType == "none"` **and** `EnableHardwareEncoding == false` (VALIDATION.md rows 13 and 14).
    - `EnableHardwareEncoding` in the readback is `false`, not `true`. Its constructor default is `true`, so a `true` here means a partial POST re-armed the amdgpu hazard that cost ~6 hours of downtime on 2026-08-31 — treat it as a stop, not a note.
    - The full `diff <(jq -S . before) <(jq -S . readback)` is captured verbatim and shows **only** lines corresponding to the five intended keys. Any other differing key is enumerated and explained, or the plan stops (VALIDATION.md row 15).
    - `encoding.before.json` is preserved under `/mnt/fast/spike-021/` **and** committed under `.planning/…/artifacts/`, because it is the rollback source.
    - No restart occurred: `docker inspect jellyfin --format '{{.State.StartedAt}}'` is unchanged from task 1's recorded value.
    - The API key never entered argv: the transcript shows `-H @<(printf` and contains no occurrence of the key's value. `grep -cEi '(token|api_key|password|secret)[[:space:]]*[=:]'` over every committed artifact returns 0.
    - `EncoderAppPath` is absent from `encoding.after.json`: `jq -e 'has("EncoderAppPath") | not' encoding.after.json` exits 0.
  </acceptance_criteria>
  <done>The five retention settings are live, the two amdgpu mitigation values are provably untouched, and a full sorted diff shows nothing else moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose file → running container | The recreate is where a file change becomes runtime; a mount that exists in the file but not in the container is the failure this task exists to catch |
| script → Jellyfin admin API | The D-40 key is **administrator-equivalent** by design — `CustomAuthenticationHandler.cs:53-57` maps any API key to `UserRoles.Administrator`. This plan widens its use from read to write |
| POST body → EncodingOptions | The endpoint is a full-object replace; every property absent from the body reverts to a C# constructor default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-34 | **Tampering** | `POST /System/Configuration/encoding` re-enabling hardware encoding | mitigate | Strict read-modify-write: GET, record `HardwareAccelerationType` and `EnableHardwareEncoding`, `jq`-merge only five keys, POST the whole object, then assert **both** values are unchanged. `EnableHardwareEncoding`'s ctor default is `true` and its live value is `false` — a partial POST re-arms a hazard with a measured ~6 h outage |
| T-021-35 | Repudiation | Asserting only the fields you meant to change | mitigate | A full `diff <(jq -S .) <(jq -S .)` of the whole object before and after, captured verbatim. This is 01-06's path-set-diff lesson applied: a field-set assertion passes while something else was rewritten |
| T-021-36 | Information Disclosure | `$JELLYFIN_API_KEY` in the process list or in a committed artifact | mitigate | `-H @<(printf …)` process substitution so the key never enters argv; no `echo`, no `set -x`; the secrets file is sourced only after `stat -c '%a %U'` equals `600 root`; every committed artifact is screened with `grep -cEi '(token\|api_key\|password\|secret)[[:space:]]*[=:]'` returning 0 |
| T-021-37 | Elevation of Privilege | An administrator-equivalent API key used for writes | accept | Recorded, not fixed: Jellyfin 10.11 has no scoped-key mechanism. The key already exists and is already used read-only by `check-music-consumers.sh`; this plan widens it to write, which is a real change in what a leak of that file costs. Accepted as a CHOICE in the same style as 02-09's `sec=sys` |
| T-021-38 | Denial of Service | Jellyfin refusing to start on a foreign `.jellyfin-*` marker | mitigate | Task 1 names the check and the recovery: `find … -name '.jellyfin-*'` and remove the foreign one. `CreateAndCheckMarker` scans recursively and throws |
| T-021-39 | Denial of Service | A 500 from `EncodingConfigurationStore.Validate` | mitigate | Never set `EncoderAppPath` — it is absent from the GET because it is null, and a non-empty value that differs from the stored one throws. Build the body from the GET response, never by hand and never from the XML. `TranscodingTempPath` requires `Directory.Exists` as Jellyfin sees it, which is why task 2 is ordered after task 1's bind |
| T-021-40 | **Denial of Service** | The recreate interrupting live playback | accept | Explicitly accepted by D-27 as a routine compose change, with rollback improvised. The rollback is nonetheless written down below, and the anonymous volume is deliberately still present as the fallback |
| T-021-SC | Tampering | npm/pip/cargo installs | mitigate | **No package install occurs in this plan.** No image is pulled — the recreate uses the already-resident `jellyfin/jellyfin:10.11.11`, confirmed by `docker inspect` to match `jellyfin.yaml:59` with no deploy drift. `jq`, `curl` and `docker` are already present on LXC 100 |
</threat_model>

<verification>
- VALIDATION.md rows 1, 2, 3, 7: the post-recreate mount shape, each asserted separately.
- VALIDATION.md row 6: the image cache file count and byte total unchanged across the recreate.
- VALIDATION.md row 10 (**fires**): the quota re-read **after** the recreate equals `53687091200`.
- VALIDATION.md rows 13, 14, 15: the five settings, the two preserved amdgpu values, and a full sorted diff showing nothing else moved.
- Assumption A4 recorded CONFIRMED or FALSIFIED from an executed write-through probe.
</verification>

<success_criteria>
- The running container holds zero `Type: volume` mounts, both binds resolve correctly, and `/data/transcode` is absent from the runtime.
- A file written into `/cache/transcodes` from inside the container appears on `/mnt/fast/transcode`.
- The ZFS quota reads `53687091200` **after** the recreate.
- The encoding readback satisfies all seven `jq -e` conditions, and the full sorted diff shows only the five intended keys.
- `encoding.before.json` is preserved as the rollback source, in git and on `/mnt/fast/`.
</success_criteria>

<rollback>
| If this fails | Revert by | Cost |
|---|---|---|
| The recreate — Jellyfin will not start | `git checkout HEAD~1 -- stacks/selfhosted/media/jellyfin.yaml` at `/mnt/fast/stacks`, then `docker compose -f stacks/selfhosted/media/compose.yaml up -d jellyfin`. **The anonymous volume still exists** — that is exactly why D-10 orders its deletion last | seconds; in-flight playback interrupted, accepted by D-27 |
| The marker-file guard refuses start | `find /mnt/fast/transcode /mnt/fast/appdata/media/jellyfin/cache -name '.jellyfin-*'`, remove the foreign one, restart | seconds |
| The nested bind mounted in the wrong order (A4 falsified) | Revert the compose file as above and report. Do **not** try to force ordering by reordering the compose lines — Docker sorts by destination segment count and ignores declaration order | seconds |
| The encoding POST 500s, or the readback assertion fails | POST `encoding.before.json` back, unmodified. It is kept at `/mnt/fast/spike-021/` — **not `/tmp`**, which is tmpfs on this host — and also committed under `.planning/` | seconds |
| `EnableHardwareEncoding` came back `true` | Stop. POST `encoding.before.json` back immediately; that re-arms nothing and restores the mitigation. Then investigate the merge before retrying | seconds |
</rollback>

<output>
Create `.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-07-SUMMARY.md` when done
</output>
