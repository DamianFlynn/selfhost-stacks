---
phase: 02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod
plan: 03
type: execute
wave: 2
depends_on: ["02.1-02"]
files_modified:
  - infra/jellyfin-transcode-dataset.tf
  - infra/variables.tf
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-terraform-plan.json
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-quota-df.txt
autonomous: true
requirements: [TRAN-07, TRAN-03]

must_haves:
  truths:
    - "fast/transcode's existence and its five tuning properties are declared in infra/ Terraform, so a later property edit re-runs instead of planning clean and doing nothing"
    - "The apply that lands them was proven, mechanically and before it ran, to contain exactly one change and zero destroys"
    - "quota=50G is live on fast/transcode and reads back as 53687091200 bytes from atlantis, with the dataset still mounted and still carrying its setgid bit"
    - "df inside LXC 100 either does or does not report the dataset's size as the quota, and which one is written down as a verdict rather than assumed"
  artifacts:
    - path: "infra/jellyfin-transcode-dataset.tf"
      provides: "the null_resource declaring the dataset and its properties"
      contains: "triggers"
    - path: "infra/variables.tf"
      provides: "var.jellyfin_transcode_quota"
      contains: "jellyfin_transcode_quota"
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-terraform-plan.json"
      provides: "the zero-destroys assertion output derived from the saved plan file that was actually applied"
      contains: "destroys"
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-quota-df.txt"
      provides: "the A3 empirical evidence plus the post-apply ownership and setgid readings"
      contains: "53687091200"
  key_links:
    - from: "infra/jellyfin-transcode-dataset.tf triggers"
      to: "the remote-exec zfs set and chown commands"
      via: "every value the provisioner interpolates also appears in triggers, so an edit to any of them re-runs it"
      pattern: "jellyfin_transcode_quota"
    - from: "infra/jellyfin-transcode-dataset.tf"
      to: "var.jellyfin_transcode_quota in infra/variables.tf"
      via: "variable reference, never an inlined 50G literal"
      pattern: "var\\.jellyfin_transcode_quota"
---

<objective>
Declare `fast/transcode` and its tuning in code, and set the bound this whole phase rests on.

The dataset already exists on atlantis, is empty, has 1.4 T available, is owned `apps:apps` with the
setgid bit, and is already bind-mounted into LXC 100 as `mp18` — declared at
`infra/lxc-selfhost.tf:248-251`. It has **never been declared**: `infra/host.tf:63` only `mkdir -p`s
the path. That `mp18` already exists is the single luckiest fact in the phase, because
`infra/lxc-selfhost.tf:322-341` carries `ignore_changes = [mount_point]`, which means adding a bind
mount by editing that file **plans clean and does nothing**. This phase needs no `mount_point` change
at all.

D-12 makes the ZFS quota the load-bearing bound: it lives in a dataset property, so it cannot be
flipped from a UI, survives a container recreate, survives an image upgrade, and caps the blast radius
at one dataset. D-13 sets it at **50 G** — a backstop, not a working limit. A total runaway is then
4% of the pool's 1.31 T free rather than an outage.

The residual cost is real and is this plan's whole discipline problem: declaring the dataset means
running `terraform apply` in `infra/`, where a bare apply was measured on 2026-08-31 to plan
`1 to destroy` on `proxmox_virtual_environment_container.selfhost` — **LXC 100 and its ~103
containers**. A visual plan review is not sufficient. The guard is a saved plan file plus a mechanical
`terraform show -json | jq` assertion of exactly one change and zero destroys, and the apply consumes
that same saved file — which is what makes "I read it" and "that is what ran" the same claim.

**Three defects the cross-AI review found in the reviewed version of this plan, all repaired here:**

- The property acceptance criterion asserted `recordsize 1048576` from a `zfs get -H` invocation that
  returns **`1M`**. The `-p` flag is not optional; without it the criterion fails for the wrong reason.
- The saved plan path was `/mnt/fast/spike-021/phase021.tfplan`, an **LXC 100** path — but Terraform
  runs on the workstation, where that path does not exist.
- The provisioner ran an unconditional `chown` on every trigger change against a directory whose mode
  is `drwxrwsr-x`, and `var.apps_uid` / `var.apps_gid` were interpolated into the provisioner while
  being absent from `triggers` — so an edit to either would not re-run it.

Purpose: the quota is the only bound in the architecture with no gating conditions on it — the only
one that still holds when segment deletion, throttling and the 48-hour cleanup task have all failed.
Output: `infra/jellyfin-transcode-dataset.tf`, `var.jellyfin_transcode_quota`, `quota=50G` live, and
the A3 `df` evidence the standing check's second instrument depends on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-CONTEXT.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-PATTERNS.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-RESEARCH.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline the Terraform tree, then declare the dataset and its properties</name>
  <files>infra/jellyfin-transcode-dataset.tf, infra/variables.tf</files>
  <read_first>
    - `infra/nfs-music-export.tf` lines **125-215** — **this is the analog to copy**, not
      `infra/lxc-mpe.tf`. Note in particular the `triggers` block with its `revision` bump field, the
      `connection` block's variable names (`var.proxmox_host`, `var.proxmox_ssh_user`,
      `var.proxmox_ssh_password`), and the WR-06 comment at 163-175 explaining why `set -e` is the
      **first** inline command.
    - `infra/lxc-mpe.tf` lines **48-70** — the weaker precedent D-07 originally cited, so the
      deviation is auditable. It has **no `triggers`** (after the first apply it is inert, so a later
      `quota=` edit plans clean and does nothing — which defeats the entire point of declaring
      properties) and **no `set -e`** (Terraform reports the *last* command's status, so a failed
      `zfs set` would be invisible).
    - `infra/lxc-selfhost.tf` lines **162-168** (every child ZFS dataset needs its own explicit
      `mount_point`), **248-251** (the `/mnt/fast/transcode` mount that already exists — **do not
      touch it**), and **322-341** (the `prevent_destroy` + `ignore_changes` lifecycle block and its
      stated trade-off).
    - `infra/host.tf` lines 50-80 — where `/mnt/fast/transcode` is `mkdir -p`'d but never declared,
      and where `null_resource.host_setup` is defined (the `depends_on` target).
    - `infra/variables.tf` — the existing declaration style for `var.apps_uid`, `var.apps_gid`,
      `var.proxmox_host` and `var.music_temp_export_path`, so the new variable matches.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *The resource shape* (lines 1052-1107) — the
      composed resource, already assembled from the above.
    - `.planning/phases/02.1-…/02.1-PATTERNS.md` § *NEW `infra/jellyfin-transcode-dataset.tf`* — the
      side-by-side of both precedents and why the newer one wins.
    - `.planning/phases/02.1-…/02.1-REVIEWS.md` § *Plan 03* — the three defects repaired here, in
      particular the `chown`/setgid concern and the `triggers` gap.
  </read_first>
  <action>
    Append the D-32 watch line for `plan=02.1-03` first, using the field set defined in 02.1-01's
    artifact header. Observation only.

    Then baseline the tree **before** adding anything: `cd infra && terraform plan -detailed-exitcode
    -input=false`. Exit **0** means no changes — proceed. Exit **2** means pre-existing drift; **STOP
    and report** rather than proceeding, because a pre-existing diff would be swept into this phase's
    apply. This is VALIDATION.md row 28; 02-09 already proved exit 0 here, so a 2 is new information
    and must be investigated, not absorbed. Never run Terraform in a git worktree:
    `infra/terraform.tfstate`, `infra/terraform.tfvars` and `.terraform/` are all gitignored and
    absent from a fresh worktree, so an apply there would run credential-less against empty state and
    attempt to recreate the LXC from scratch.

    Add `var.jellyfin_transcode_quota` to `infra/variables.tf` with type `string`, default `"50G"`,
    and a description naming D-13 and stating that it is a backstop rather than a working limit — with
    segment deletion on, steady state should sit orders of magnitude below it, so playback never meets
    the ceiling. Do not inline `50G` in the resource; the quota is threaded as a variable the same way
    `var.apps_uid`, `var.apps_gid` and `var.music_temp_export_path` already are.

    Create `infra/jellyfin-transcode-dataset.tf` — one file, one concern — containing
    `resource "null_resource" "jellyfin_transcode_dataset"` with
    `depends_on = [null_resource.host_setup]`.

    A file header comment stating: `fast/transcode` already exists on atlantis and is already
    bind-mounted into LXC 100 as `mp18` (`infra/lxc-selfhost.tf:248-251`); it has never been
    *declared*, because `host.tf:63` only `mkdir -p`s the path; and a `null_resource` adds **no
    attribute** to `proxmox_virtual_environment_container.selfhost`, which is precisely why this is
    safe — the destroy landmine is untouched.

    A `triggers` map. **Every value the provisioner interpolates must appear here**, or an edit to it
    plans clean and does nothing — which is the `lxc-mpe.tf` defect this file exists to avoid, and the
    review found the reviewed version reproducing it for two of them. The map carries:

    - `properties = join(",", ["quota=${var.jellyfin_transcode_quota}", "compression=off",
      "sync=disabled", "recordsize=1M", "atime=off"])`
    - `owner = "${var.apps_uid}:${var.apps_gid}"` — **added by the review**; these were interpolated
      into the provisioner but absent from `triggers`
    - `revision = "1"` for forcing a re-run

    Comment why the `triggers` block exists at all: without it the block is inert after the first
    apply, and note in the same comment that this **supersedes D-07's cited line reference**
    (`infra/lxc-mpe.tf:48-50`) while honouring D-07's intent — both RESEARCH.md and PATTERNS.md
    independently concluded `nfs-music-export.tf` is the correct source.

    A `connection` block matching `nfs-music-export.tf`'s exactly, and a `remote-exec` provisioner
    whose `inline` list begins with `"set -e"` — first, with the WR-06 comment explaining that
    Terraform concatenates `inline` into one script and reports the **last** command's exit status, so
    without this a failed `zfs set` is invisible and surfaces later as something unrelated.

    The inline commands, in order:

    1. A guarded create: `zfs list fast/transcode >/dev/null 2>&1 || zfs create -p fast/transcode`.
    2. `zfs set` for `quota=${var.jellyfin_transcode_quota}`, `compression=off`, `sync=disabled`,
       `recordsize=1M`, `atime=off` — each idempotent by construction, since setting a property to its
       current value is a no-op.
    3. **A conditional `chown`, not an unconditional one** — the review's finding. Guard it:
       `[ "$(stat -c %u:%g /mnt/fast/transcode)" = "${var.apps_uid}:${var.apps_gid}" ] || chown
       ${var.apps_uid}:${var.apps_gid} /mnt/fast/transcode`. The directory is `drwxrwsr-x` and the
       setgid bit is what gives new files the `apps` group; running `chown` on every trigger change is
       at best pointless churn and at worst a way to lose it.
    4. **Re-assert the mode immediately after**, unconditionally, so the setgid bit is a stated
       invariant rather than a hope: `chmod 2775 /mnt/fast/transcode`. `chmod` is idempotent and this
       removes the question entirely rather than reasoning about whether Linux's `chown` strips SGID
       from directories.
    5. `stat -c '%a %U:%G %n' /mnt/fast/transcode` so the applied ownership and mode are echoed into
       the Terraform output and land in the run log.
    6. `zfs get -Hp -o property,value quota,compression,sync,recordsize,atime,mounted fast/transcode`
       so the applied state — **including `mounted`** — is echoed too. Note the `-p`: without it
       `recordsize` prints `1M` rather than `1048576`, which is the review's finding 9 in its
       upstream form.

    Property rationale to record in the file (D-05): `compression=off` because segments are already
    compressed H.264/HEVC and lz4 burns CPU on a host running 103 containers; `sync=disabled` because
    the data is disposable — and a warning that this must not be copied to any dataset holding
    non-disposable data; `recordsize=1M` for large sequential segment writes, which is correct **only**
    because the dataset is empty today, since `recordsize` applies to newly written blocks only. Also
    record the D-15/CONS-04 choice of `quota` over `refquota`: `refquota` does not count descendants,
    so a future child dataset or snapshot escapes the bound entirely, and it returns `EDQUOT` ("Disk
    quota exceeded") where `quota` returns **`ENOSPC`** ("No space left on device") — the errno the
    incident's 764 zero-byte files already signature, and the string every log grep in this estate is
    built on. Do **not** set `refquota`.

    Do not edit `infra/lxc-selfhost.tf`. Do not add or remove a `mount_point`.
  </action>
  <verify>
    <automated>cd infra && terraform fmt -check -diff jellyfin-transcode-dataset.tf variables.tf && terraform validate && grep -q 'var.jellyfin_transcode_quota' jellyfin-transcode-dataset.tf && grep -q 'triggers' jellyfin-transcode-dataset.tf && grep -q 'var.apps_uid' jellyfin-transcode-dataset.tf && grep -q 'chmod 2775' jellyfin-transcode-dataset.tf && grep -q 'zfs get -Hp' jellyfin-transcode-dataset.tf && test "$(grep -c '"50G"' jellyfin-transcode-dataset.tf)" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The pre-change `cd infra && terraform plan -detailed-exitcode -input=false` exited **0**, recorded in the summary (VALIDATION.md row 28). If it exited 2, the plan stopped here and reported.
    - `terraform validate` exits 0 and `terraform fmt -check` reports no diff on both changed files.
    - `infra/variables.tf` declares `jellyfin_transcode_quota` with default `"50G"`; `grep -c 'jellyfin_transcode_quota' infra/variables.tf` returns ≥ 1.
    - `grep -c '"50G"' infra/jellyfin-transcode-dataset.tf` returns **0** — the quota is not inlined.
    - The `triggers` block names **every** value the provisioner interpolates: it contains a `join(",",` expression covering all five properties **and** an `owner` key built from `var.apps_uid` and `var.apps_gid`. `grep -c 'var.apps_uid' infra/jellyfin-transcode-dataset.tf` returns ≥ **2** — once in `triggers`, once in `inline`. This is the review's finding; the reviewed version had it in `inline` only, so an id change would have planned clean and done nothing.
    - The first element of the `inline` list is the literal `"set -e"` — verify by reading the file, not by grep alone.
    - The `chown` is **conditional** — guarded by a `stat -c %u:%g` comparison — and is immediately followed by an unconditional `chmod 2775`. `grep -c 'chmod 2775' infra/jellyfin-transcode-dataset.tf` returns ≥ 1.
    - The final read-back inline command uses `zfs get -Hp` (with `-p`) and includes `mounted` in its property list. `grep -c 'zfs get -H ' infra/jellyfin-transcode-dataset.tf` returns **0** — the non-parsable form appears nowhere.
    - `grep -c 'refquota' infra/jellyfin-transcode-dataset.tf` returns **0** in executable HCL; a comment explaining the rejection is expected and permitted.
    - `git diff --name-only` lists exactly `infra/jellyfin-transcode-dataset.tf` and `infra/variables.tf` — `infra/lxc-selfhost.tf` is untouched.
    - The file header comment names `infra/lxc-selfhost.tf:248-251` (the existing `mp18` mount that must not be touched) and states that a `null_resource` adds no attribute to the guarded LXC resource.
  </acceptance_criteria>
  <done>The declaration exists, validates, formats clean, threads the quota through a variable, carries `triggers` covering every interpolated value including the uid/gid pair, starts its `inline` list with `set -e`, guards the `chown` and re-asserts the setgid mode, and reads properties back with `-Hp`. The pre-change plan was clean.</done>
</task>

<task type="auto">
  <name>Task 2: Apply from a saved plan proven to contain zero destroys, then verify the properties, the mount and the df instrument</name>
  <files>.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-terraform-plan.json, .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-quota-df.txt</files>
  <read_first>
    - `infra/jellyfin-transcode-dataset.tf` — as written in task 1, so the expected resource address
      in the assertion matches the resource name actually used.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *The safe invocation* (lines 1004-1050) — the
      five-step sequence and its two further constraints (never in a git worktree; a `null_resource`
      destroy removes nothing from the host, which 02-07 learned the hard way when a temporary export
      stayed live while `terraform plan` reported clean).
    - `infra/lxc-selfhost.tf` lines 322-341 — what `prevent_destroy` covers and, critically, what it
      does **not**: every other resource in `infra/` is unprotected, including
      `null_resource.patch_lxc_config`, `start_lxc` and `provision_lxc`, whose provisioners run
      against the live LXC config.
    - `.planning/phases/02.1-…/02.1-VALIDATION.md` rows 9, 12, 28, 29, 30, 31 and **36**.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *`quota` vs `refquota`* — specifically the closing
      paragraph on `df` inside LXC 100 reflecting the quota via `statvfs`, flagged as assumption
      **A3** and explicitly requiring empirical proof rather than assumption.
    - `.planning/phases/02.1-…/02.1-REVIEWS.md` § *Plan 03* — the saved-plan path defect and the
      `recordsize` invocation defect.
  </read_first>
  <action>
    Record `df -B1 --output=size /mnt/fast/transcode` from LXC 100 **before** the apply, into
    `.planning/phases/02.1-…/artifacts/02.1-03-quota-df.txt`. It should read approximately the pool's
    free space (~1.4 T), not 50 G. This is half of assumption A3's empirical proof and it is
    unobtainable once the quota is set.

    Save the plan rather than applying blind. **The plan file must live on the workstation**, because
    that is where Terraform runs — the reviewed version specified
    `-out=/mnt/fast/spike-021/phase021.tfplan`, which is an LXC 100 path and does not exist on the
    workstation at all. Create a workstation-local temporary file
    (`PLANFILE="$(mktemp -t phase021-XXXXXX)"`), record its absolute path in the transcript, and use
    `cd infra && terraform plan -out="$PLANFILE" -input=false`. The constraint that matters is that
    the **same file** is read and then applied; where it lives is only a correctness question about
    which machine it is on.

    **Do not commit the plan file itself.** A binary Terraform plan can carry variable values,
    including `var.proxmox_ssh_password`, and this repository is public. Only the derived JSON
    assertion object is committed — see the next paragraph — and it is screened before it goes in.

    Assert the saved plan **mechanically**. Run `terraform show -json "$PLANFILE"` piped to a `jq`
    filter that selects `.resource_changes[]` whose `.change.actions != ["no-op"]` and emits an object
    with `count` (the length), `destroys` (the count of entries whose `.change.actions` contains
    `"delete"`) and `addresses` (the list of addresses). Capture **only that derived object** to
    `.planning/phases/02.1-…/artifacts/02.1-03-terraform-plan.json` — never the full `show -json`
    output, which contains the variable values. The expected value is exactly `count == 1`,
    `destroys == 0`, and `addresses == ["null_resource.jellyfin_transcode_dataset"]`.

    **If any of those three does not hold, STOP.** Do not apply, do not "read the plan and decide" — a
    visual plan review is explicitly not what this task asks for. A bare apply in this directory was
    measured on 2026-08-31 to plan `1 to destroy` on
    `proxmox_virtual_environment_container.selfhost`, i.e. LXC 100 and its ~103 containers, and
    `prevent_destroy` covers only that one resource. Report the assertion output and halt.

    Only then apply the **saved plan file**: `terraform apply "$PLANFILE"`. Never a fresh apply.
    `terraform apply` without a plan file re-plans, and a re-plan can differ from the plan that was
    read — applying the saved file is what makes "I read it" and "that is what ran" the same claim.
    Delete the plan file afterwards and record that it was deleted.

    Re-assert cleanliness: `terraform plan -detailed-exitcode -input=false` must exit **0** after the
    apply (VALIDATION.md row 30). A 2 here means the resource is not idempotent and must be fixed
    before proceeding.

    Verify from the authoritative source, not from Terraform's output. Append all of the following to
    the quota-df artifact:

    - `ssh root@172.16.1.158 'zfs get -Hp -o property,value quota,compression,sync,recordsize,atime,mounted fast/transcode'`
      (row 31 and row 36). **The `-p` is mandatory.** Without it the same command returns
      `recordsize 1M`, and an acceptance criterion asserting `1048576` fails for a reason that has
      nothing to do with the change — which is exactly what the review found in the reviewed version.
    - `ssh root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode'` asserted equal to
      `53687091200` (row 9).
    - `ssh root@172.16.1.158 'stat -c "%a %U:%G" /mnt/fast/transcode'` — assert `2775` and
      `apps:apps`. The setgid bit is what makes new files inherit the `apps` group, and the
      provisioner's conditional `chown` plus unconditional `chmod 2775` exist to keep it; proving it
      after the apply is what turns that from an intention into a fact.

    Then record `df -B1 --output=size /mnt/fast/transcode` from LXC 100 **after** the apply and compare
    against the before reading. This settles assumption A3 empirically: if the after value is
    ≈ `53687091200` then ZFS's `statvfs` derives `f_blocks` from `referenced + available` with
    `available` capped by the directory quota, and the standing check has a genuine second instrument
    it can use without reaching atlantis (VALIDATION.md row 12). If it is **not** ≈ 50 G, record that
    A3 is **FALSIFIED** and say so explicitly — the standing check then keeps that line as
    REPORTED-only forever and the quota assertion must always reach atlantis. Do not quietly adjust
    the expectation to whatever was observed.

    Finally re-run the standing check in `--baseline` mode and record it. The quota and mounted
    assertions should now both read green while the mount-shape and encoding assertions are still red,
    because the compose edit and the recreate have not happened yet.
  </action>
  <verify>
    <automated>jq -e '.count == 1 and .destroys == 0 and (.addresses | index("null_resource.jellyfin_transcode_dataset") != null)' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-03-terraform-plan.json && ( cd infra && terraform plan -detailed-exitcode -input=false >/dev/null; test $? -eq 0 ) && test "$(ssh -n -o BatchMode=yes root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode')" = "53687091200" && test "$(ssh -n -o BatchMode=yes root@172.16.1.158 'zfs get -Hp -o value mounted fast/transcode')" = "yes" && ssh -n -o BatchMode=yes root@172.16.1.158 'zfs get -Hp -o property,value recordsize fast/transcode' | grep -qx 'recordsize	1048576' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The saved-plan assertion artifact exists and `jq -e '.count == 1 and .destroys == 0'` over it exits 0, with `addresses` containing exactly `null_resource.jellyfin_transcode_dataset` (VALIDATION.md row 29). This is a **fires** row: the assertion was executed against the saved plan file that was subsequently applied, not against a re-plan.
    - The saved plan file lived on the **workstation** — its absolute path is recorded in the transcript, is not under `/mnt/fast/`, and the transcript records that it was deleted after the apply. The committed JSON is the derived `{count, destroys, addresses}` object only, never the full `terraform show -json` output, and `grep -cEi '(token|api_key|password|secret)' ` over it returns 0.
    - The apply consumed the saved plan file — the summary records the exact `terraform apply <file>` command line, and no bare `terraform apply` appears anywhere in the transcript.
    - Post-apply `cd infra && terraform plan -detailed-exitcode -input=false` exits **0** (row 30).
    - `ssh root@172.16.1.158 'zfs get -Hp -o value quota fast/transcode'` returns exactly `53687091200` (row 9).
    - `ssh root@172.16.1.158 'zfs get -Hp -o property,value quota,compression,sync,recordsize,atime,mounted fast/transcode'` returns `compression off`, `sync disabled`, **`recordsize 1048576`**, `atime off` and `mounted yes` (rows 31 and 36). The `-p` is what makes `1048576` the correct expectation; `zfs get -H` without it returns `1M` and the assertion would fail for the wrong reason.
    - `ssh root@172.16.1.158 'stat -c "%a %U:%G" /mnt/fast/transcode'` returns `2775 apps:apps` — the setgid bit survived the provisioner.
    - The quota-df artifact contains a **before** and an **after** reading of `df -B1 --output=size /mnt/fast/transcode` taken from LXC 100, with the before reading in the terabyte range and the after reading explicitly compared against `53687091200`. The artifact states in words whether assumption **A3** is CONFIRMED or FALSIFIED (row 12).
    - `git status --porcelain infra/` shows no unexpected modification to `infra/lxc-selfhost.tf`, `infra/host.tf` or `infra/lxc-mpe.tf`.
    - `ssh root@172.16.1.158 'zfs list -r -t all fast/transcode'` still returns the dataset alone — no children, no snapshots — confirming the `quota`/`refquota` equivalence argument's premise still holds.
    - The post-apply `--baseline` run of `check-jellyfin-transcode.sh` is recorded, showing the quota and mounted assertions green.
  </acceptance_criteria>
  <done>`quota=50G` plus the four tuning properties are live on `fast/transcode`, applied from a workstation-local saved plan mechanically proven to contain one change and zero destroys, with a clean re-plan afterwards, `mounted=yes`, `2775 apps:apps` intact, and the A3 `df` question answered from measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation Terraform → atlantis | `remote-exec` over ssh with `var.proxmox_ssh_password`, executing `zfs`, `chown` and `chmod` as root on the Proxmox host |
| Terraform state → live infrastructure | `infra/` manages the LXC that runs ~103 containers; `prevent_destroy` protects exactly one resource in that directory and nothing else |
| saved plan file → public repo | A binary Terraform plan can carry variable values including the SSH password; the repo is public and one prior exposure is still recoverable via `git log -S` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-14 | **Denial of Service** | `terraform apply` in `infra/` | mitigate | Saved-plan discipline with a **mechanical** `terraform show -json \| jq` assertion of `count == 1` and `destroys == 0` executed **before** the apply, and the apply consuming that same file. A bare apply here was measured planning `1 to destroy` on LXC 100 and its ~103 containers. A visual plan review is explicitly insufficient and is not what the task asks for |
| T-021-15 | **Information Disclosure** | The saved plan file committed to a public repo | mitigate | Added by the review's path finding. The plan file is workstation-local, is deleted after the apply, and is **never committed**. Only the derived `{count, destroys, addresses}` JSON goes into git, and it is screened with `grep -cEi '(token\|api_key\|password\|secret)'` returning 0 |
| T-021-16 | Denial of Service | Terraform run inside a git worktree | mitigate | `infra/terraform.tfstate`, `infra/terraform.tfvars` and `.terraform/` are gitignored and absent from a worktree; an apply there runs credential-less against empty state and tries to recreate the LXC. `parallelization` is already `false` in `.planning/config.json`. The task states the constraint explicitly |
| T-021-17 | Tampering | Silent provisioner failure | mitigate | `"set -e"` is the **first** element of the `inline` list (WR-06). Terraform reports the provisioner's status as the *last* command's exit status, so without it a failed `zfs set quota=` is invisible and surfaces later as something unrelated |
| T-021-18 | Tampering | Losing the setgid bit on `/mnt/fast/transcode` | mitigate | Added by the review. The `chown` is guarded by a `stat -c %u:%g` comparison so it is a no-op on re-runs, and an unconditional `chmod 2775` follows it. The post-apply assertion reads `2775 apps:apps` back, which turns the intention into a fact rather than a reasoning exercise about whether Linux `chown` strips SGID from directories |
| T-021-19 | Tampering | A property edit that plans clean and does nothing | mitigate | Added by the review. Every value the provisioner interpolates — the five properties **and** the uid/gid pair — appears in `triggers`. The reviewed version had the uid/gid in `inline` only, reproducing the exact `lxc-mpe.tf` defect the `triggers` block exists to avoid |
| T-021-20 | **Denial of Service** (the control itself) | ZFS `quota` on `fast/transcode` | mitigate | This is the phase's primary control: `quota=50G` bounds a resource-exhaustion path that took `/` to zero bytes. `quota` is chosen over `refquota` so a future child dataset or snapshot cannot escape the bound, and so the errno is `ENOSPC` rather than `EDQUOT` |
| T-021-21 | Information Disclosure | SSH password in committed HCL | mitigate | The `connection` block references `var.proxmox_ssh_password` only; the value lives in gitignored `infra/terraform.tfvars`. Screen the diff with `git diff --cached \| grep -cEi '(token\|api_key\|password\|secret)[[:space:]]*='` — a match on the variable *reference* is expected, a match on a literal value is a stop |
| T-021-22 | Repudiation | `null_resource` destroy semantics | accept | Removing this resource from state removes **nothing** from the host (02-07's finding: a temporary export stayed live while `terraform plan` reported clean). Accepted and recorded in the rollback table rather than mitigated, because the correct un-declare is a separate reconciler resource, not a deletion |
| T-021-SC | Tampering | npm/pip/cargo installs | mitigate | **No package install occurs in this plan.** `terraform` (>= 1.6.0), the `bpg/proxmox ~> 0.95` and `hashicorp/null ~> 3.2` providers and OpenZFS 2.4.3-pve1 are all already present and pinned in `infra/providers.tf` |
</threat_model>

<verification>
- VALIDATION.md row 28: pre-change `terraform plan -detailed-exitcode` exit 0.
- VALIDATION.md row 29 (**fires**): the saved plan asserted `count == 1`, `destroys == 0` before the apply ran, with the derived JSON recorded and the plan file itself never committed.
- VALIDATION.md row 30: post-apply `terraform plan -detailed-exitcode` exit 0.
- VALIDATION.md row 31: the five properties read back from atlantis with `-Hp`, `recordsize` as `1048576`.
- VALIDATION.md row 9: `zfs get -Hp -o value quota fast/transcode` == `53687091200`.
- VALIDATION.md row 36: `mounted` == `yes`, and `2775 apps:apps` intact after the provisioner.
- VALIDATION.md row 12 (A3): `df -B1 --output=size /mnt/fast/transcode` recorded before and after, with a CONFIRMED/FALSIFIED verdict written down.
- VALIDATION.md row 37: a `plan=02.1-03` D-32 watch line appended.
</verification>

<success_criteria>
- `infra/jellyfin-transcode-dataset.tf` declares the dataset with `triggers` covering every interpolated value, `set -e` first, a guarded `chown` followed by `chmod 2775`, a `-Hp` read-back, and the quota threaded through `var.jellyfin_transcode_quota`.
- The apply was made from a **workstation-local** saved plan whose zero-destroys assertion is recorded as a derived JSON object, the plan file was deleted and never committed, and `infra/lxc-selfhost.tf` was not touched.
- `fast/transcode` reads back `quota=53687091200`, `compression=off`, `sync=disabled`, `recordsize=1048576`, `atime=off`, `mounted=yes`, and `stat` reports `2775 apps:apps`.
- Assumption A3 has a written verdict backed by a before/after `df` pair.
</success_criteria>

<rollback>
| If this fails | Revert by | Cost |
|---|---|---|
| The zero-destroys assertion does not hold | Nothing has been applied. Delete the saved plan file, revert the `.tf` files, and report — the assertion firing is the guard working, not a problem to route around | seconds |
| The apply errors mid-provisioner | `set -e` makes the failure visible. The `zfs set`, guarded `chown` and `chmod` are individually idempotent; re-run `terraform plan -out` and repeat the assertion. Nothing is destroyed by a failed `remote-exec` | seconds |
| The setgid bit or ownership is wrong afterwards | `ssh root@172.16.1.158 'chown 568:568 /mnt/fast/transcode && chmod 2775 /mnt/fast/transcode'`. The dataset is empty at this point, so no file inherits a wrong group | seconds |
| `mounted` reads `no` | Stop before proceeding to plan 02.1-04. An unmounted dataset means the quota does not bound the path the bind will reach, and the whole cutover chain would install a control that does not apply. `zfs mount fast/transcode` on atlantis, then re-assert | seconds |
| The quota breaks legitimate playback | `ssh root@172.16.1.158 'zfs set quota=none fast/transcode'` — takes effect immediately, no restart, no container involvement | seconds |
| The declaration itself needs reverting | `terraform destroy -target=null_resource.jellyfin_transcode_dataset` removes the resource from **state** but **removes nothing from the host** (02-07). Reverting the properties is a `zfs set` on atlantis. State and host diverge — say so in the summary rather than assuming a destroy cleaned up | seconds, with divergence |
</rollback>

<output>
Create `.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-03-SUMMARY.md` when done
</output>
