================================================================================
02.1 D-32 PASSIVE `/` WATCH  --  OBSERVATION ONLY
================================================================================

WHAT THIS IS
  D-32 (operator decision, 2026-09-02). The operator chose a PASSIVE guard over
  actively clearing the anonymous volume's `transcodes/`. This file records a
  cheap re-observation of LXC 100's root-filesystem headroom and the anonymous
  volume's transcode footprint, taken at the head of each plan in phase 02.1, so
  that if `/` starts filling mid-phase there is an EARLY SIGNAL rather than a
  surprise.

WHAT THIS IS NOT
  This watch ASSERTS NOTHING. It is not a gate, not a check, and not a control.
  It does not pass or fail. It is a record of observations, and it exists to be
  read by a human comparing consecutive lines.

  IT NEVER WRITES TO OR DELETES LIVE TRANSCODE DATA.
  The watch is `du`, `df`, `find` and `docker inspect` and nothing else. No
  `rm`, no `mv`, no `truncate`, no redirect into
  /var/lib/docker/volumes/$OLD_VOL/_data/ is permitted by any plan appending
  here.

  The existing cache is NOT removed by this guard. It is removed by the normal
  cutover path -- the copy (02.1-04), the recreate (02.1-05), and the
  `docker volume rm` in 02.1-06.

THE VOLUME UNDER OBSERVATION

  The next line is deliberately at COLUMN 0, breaking this block's indentation.
  Every plan from 02.1-02 onward guards on it with `grep -E '^OLD_VOL=[0-9a-f]{64}$'`
  before appending a sample, and that anchored pattern cannot match an indented
  line. De-indented by 02.1-02 for exactly that reason -- the same class of bug
  02.1-01 fixed in the FORMAT block below, where a correctly-written artifact
  tripped its own mechanical check. Do not re-indent it.

OLD_VOL=d98b2ff94482ce115d1869142ad6be8d8425a5d7f776188b2a1a3ad6adcccfa4

  Captured 2026-09-02 by plan 02.1-01 from
    docker inspect jellyfin --format '{{range .Mounts}}{{if eq .Type "volume"}}{{.Name}}{{"\n"}}{{end}}{{end}}'
  which returned EXACTLY ONE non-empty line (asserted; VOLMOUNT_COUNT=1).

  Mirrored to a durable host path: /mnt/fast/spike-021/old-vol.txt
  (on /mnt/fast, NEVER /tmp -- /tmp on LXC 100 is tmpfs against ~4.7 GiB
  available RAM, and 1.1 GB written there once took the whole box down.)

  Captured HERE, in the first plan, rather than in the copy plan, because
  `docker inspect jellyfin` is the only reliable source for this id and the
  recreate in 02.1-05 removes the container object that carries it. It also
  means the standing check's old-volume section (built in 02.1-02) is live from
  its first run instead of printing "not yet captured" for three plans.

FORMAT
  One line per sample, appended at the HEAD of each plan, oldest first.
  Every size is a BYTE INTEGER. Measuring with `df` in GiB-rounding block mode
  (the `-B` flag with a `G` argument) is PROHIBITED: it rounds UP, which is up
  to 1 GiB of optimism against a 20 GiB floor (03-01's finding, commit b217ada).
  The flag is spelled out in words rather than written literally so that a
  mechanical grep for it over this artifact returns zero -- the check is "no
  field was measured that way", and a prohibition notice must not trip it.

  An unreadable `df` or `du` is recorded as the literal UNKNOWN, never as 0.
  "Could not look" and "it is empty" are different answers, and 03-01 already
  paid for conflating them.

  Once 02.1-06 deletes the volume, the oldvol_* fields become the literal GONE
  rather than UNKNOWN, so a later reader can tell the planned transition apart
  from an instrument failure at a glance.

  Fields:
    <iso8601-utc> plan=<id>
      root_avail_bytes=        `df -B1 --output=avail /`
      root_size_bytes=         `df -B1 --output=size /`
      oldvol_transcodes_bytes= `du -sb .../_data/transcodes`   <-- the growing part
      oldvol_total_bytes=      `du -sb .../_data`
      oldvol_transcodes_files= `find .../transcodes -type f | wc -l`

================================================================================
SAMPLES
================================================================================

2026-09-02T20:38:32Z plan=02.1-01 root_avail_bytes=21668859904 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13884071113 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- the phase-start reading is NOT what the
plan set assumed, and the margin is 185 MiB.
--------------------------------------------------------------------------------

  root_avail_bytes = 21668859904 = 20.18 GiB
  D-17 floor       = 21474836480 = 20.00 GiB
  MARGIN           =   194023424 =  0.18 GiB = 185 MiB

  The phase-start gate PASSES -- 20.18 GiB is above the floor, so 02.1-01 did
  not stop -- but it passes by 185 MiB, not by the "~33-36 GiB free" the plan
  set was written against. STATE.md records `/` at 36 GiB after 03-01's reap
  (34 GiB after the four spike pulls). It is now 20.18 GiB.

  Verified with a SECOND INDEPENDENT INSTRUMENT before being written down, per
  the 01-09 standing rule. `stat -f -c %a /` returned bavail=5290180 blocks at
  bsize=4096 = 21668577280 bytes, matching plain `df -B1 /` to the byte. The
  276 KiB delta against the --output=avail reading is seconds of live drift on
  a running filesystem, not disagreement.

  Note also `stat -f` bfree=6971996 (28.56 GiB) vs bavail=5290180 (20.18 GiB):
  8.38 GiB is root-reserved. The floor is checked against AVAIL, the
  conservative figure, which is correct -- do not be tempted by bfree.

  WHERE IT WENT: oldvol_transcodes_bytes = 12.55 GiB across 1,324 files. The
  Jellyfin container has been up since 2026-08-31T18:27:41Z (the amdgpu
  incident recovery), so this refilled in roughly two days -- about 6 GiB/day,
  and it accounts for very nearly all of the ~14 GiB `/` has lost since 03-01.

  THIS IS THE INCIDENT REPEATING, CAUGHT EARLY. It is exactly what D-32 was
  ruled to detect, and it is already firing on the first sample. It is not a
  reason to stop the phase -- it is the reason the phase exists, and D-31
  already put the cutover on the critical path so that `/` is structurally
  protected at the end of wave 6. The cutover chain is what reclaims this
  12.55 GiB.

  IT IS A REASON TO NOT LET THE PHASE IDLE. At ~6 GiB/day against 185 MiB of
  margin, `/` crosses the D-17 floor within about a day and reaches zero within
  roughly three. Any plan in this phase that stalls -- particularly at a
  blocking human gate -- is spending a budget measured in hours. If a later
  sample shows root_avail_bytes below 21474836480, that is the operator
  decision point the 02.1-01 rollback table describes: the reap in 02.1-09 may
  need to move earlier, and that is an operator call, not a planner one.

2026-09-02T20:54:23Z plan=02.1-02 root_avail_bytes=21674303488 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13884071113 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- the volume is FLAT over 16 minutes, and that
is worth more than the margin figure.
--------------------------------------------------------------------------------

  Delta against plan=02.1-01, 16 minutes earlier:

    root_avail_bytes         21668859904 -> 21674303488   +5443584  (+5.2 MiB)
    oldvol_transcodes_bytes  13473768858 -> 13473768858          0
    oldvol_total_bytes       13884071113 -> 13884071113          0
    oldvol_transcodes_files         1324 -> 1324                 0

  MARGIN against the D-17 floor of 21474836480:
    21674303488 - 21474836480 = 199467008 = 0.19 GiB = 190 MiB

  THE CACHE DID NOT GROW AT ALL in this window -- byte-identical on all three
  fields. The ~6 GiB/day rate 02.1-01 computed is an AVERAGE over the ~2 days
  since the container came up on 2026-08-31T18:27:41Z; it is NOT a steady drip.
  Transcode cache grows only while something is transcoding, so the real shape
  is bursts during playback and flat between them. `/` even recovered 5.2 MiB
  here, from something unrelated.

  DO NOT READ THIS AS REASSURANCE. A flat 16 minutes says nobody was watching
  television, not that the retention problem is gone. The next evening of
  playback is the same burst that produced 12.55 GiB in two days, and 190 MiB
  does not survive one of them. What the flatness DOES buy is that the budget is
  not being spent while the phase is merely running -- it is spent when the TV
  is on, which is a different and less predictable clock than "hours from now".

  02.1-02's OWN STOP CONDITION IS TRIPPED. That plan says to stop and report if
  the measured margin is under ~5 GiB. It is 190 MiB. 02.1-02 was completed
  anyway, deliberately and on a narrow basis: every action in it is READ-ONLY
  with respect to the estate -- an ssh'd `git pull --ff-only` into
  /mnt/fast/stacks (which is on /mnt/fast, NOT on /), a `--baseline` run that
  asserts nothing, and artifacts written under .planning/ on the workstation.
  Finishing it cannot consume `/`, and NOT finishing it would have left the
  phase with no standing check on the host while the budget ran down -- the
  precise failure 02.1-01's closing note warns about.

  THE OPERATOR DECISION IS STILL OWED, AND IT IS OWED BEFORE 02.1-03. That plan
  is the first mutation (Terraform, the dataset and its quota). The question the
  stop condition exists to ask is an ORDERING one: should 02.1-09's image reap
  move ahead of 02.1-03 to widen the margin first? D-31 put the reap at the end
  of the phase. That was decided when the plan set assumed ~13 GiB of headroom.
  It is 190 MiB. Nobody has re-ruled on it with this number in hand.

2026-09-02T21:26:06Z plan=02.1-03 root_avail_bytes=21651746816 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13884071113 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- the ordering decision 02.1-02 said was OWED
has been RULED, and the cache is still flat at 32 minutes.
--------------------------------------------------------------------------------

  Delta against plan=02.1-02, ~32 minutes earlier:

    root_avail_bytes         21674303488 -> 21651746816  -22556672  (-21.5 MiB)
    oldvol_transcodes_bytes  13473768858 -> 13473768858          0
    oldvol_total_bytes       13884071113 -> 13884071113          0
    oldvol_transcodes_files         1324 -> 1324                 0

  MARGIN against the D-17 floor of 21474836480:
    21651746816 - 21474836480 = 176910336 = 0.16 GiB = 168.7 MiB

  THE CACHE IS STILL BYTE-IDENTICAL, now across ~48 minutes and three samples.
  The 21.5 MiB `/` lost in this window did NOT come from the transcode cache --
  all three oldvol fields are unchanged. It is ordinary churn from the other 78+
  containers writing to `/` (docker logs, container layers), which is the same
  class of movement as the +5.2 MiB gain 02.1-02 recorded. Do not attribute it
  to the retention problem; the retention problem is not currently running.

  THE ORDERING DECISION IS RULED: 02.1-03 PROCEEDS, THE REAP STAYS IN 02.1-09.
  02.1-02 recorded that the ~5 GiB stop condition was tripped and that the
  question -- "should 02.1-09's image reap move ahead of 02.1-03?" -- was owed to
  the operator before the first mutation. It has now been answered: NO, proceed
  in the planned order.

  The basis is narrow and is stated so it can be checked rather than trusted.
  02.1-03's mutation is a `zfs create`-guarded declaration plus five `zfs set`
  property writes on `fast/transcode`, and a `chown`/`chmod` on its mountpoint.
  Every one of those lands on the `fast` POOL, which has 1442767437824 bytes
  (1.31 TiB) available, and NONE of them writes a byte to `/`. Measured, not
  assumed: `stat -c %d` reports / = 64519, /mnt/fast = 64519, and
  /mnt/fast/transcode = 47 -- the dataset is a genuinely distinct filesystem, so
  the phase's central premise holds and this plan's writes cannot touch `/`.

  A NOTE ON /mnt/fast, because it is counter-intuitive and will mislead someone:
  /mnt/fast is NOT itself a mount point. It is a plain directory on the ext4
  root, and only its CHILDREN are ZFS datasets. `df /mnt/fast` therefore reports
  `/` and looks alarming. Always measure the actual target path
  (/mnt/fast/transcode), never its parent.

  WHAT WOULD OVERTURN THIS RULING: a sample whose root_avail_bytes is below
  21474836480, or an oldvol_transcodes_bytes that has resumed growing. Neither
  is true here. The ruling covers 02.1-03 ONLY -- it is not a general licence
  for the rest of the phase, and 02.1-04 onward (the copy, the recreate) DO
  touch `/` and must be re-ruled on their own numbers.

  ADDENDUM, same plan, ~10 minutes later: A SECOND AND UNRELATED STOP CONDITION
  FIRED, and 02.1-03 halted on it rather than on the margin. The Terraform
  baseline `terraform plan -detailed-exitcode -input=false` in infra/ exited 2,
  not 0, so the plan's own precondition (VALIDATION.md row 28) is not met. The
  drift is output-only -- the `verify_commands` heredoc, committed in 76b3783 by
  a phase-02 docs plan and never applied -- and a saved-plan probe measured
  `.resource_changes` filtered to non-no-op as count=0, destroys=0, addresses=[].
  Zero infrastructure changes. But the plan says STOP on a 2, so this artifact's
  ruling above ("02.1-03 PROCEEDS") is now conditional on an operator answering
  the Terraform question. The `/` ruling itself is unaffected and stands.

  CLOSURE, same plan, ~8 minutes later: THE OPERATOR RULED OPTION B -- clear the
  drift first as its own separate, guarded apply, THEN execute 02.1-03 against a
  genuinely clean exit-0 baseline. Chosen deliberately over the cheaper option of
  proceeding with a documented exception, because row 28 is a VALIDATION row this
  phase gets verified against and only B satisfies it LITERALLY rather than by
  exception. Executed as two applies, each from its own saved, separately-
  asserted plan file: the first measured count=0 / destroys=0 / addresses=[] and
  consumed the stale output alone (Terraform's own words: "without changing any
  real infrastructure"), after which `terraform plan -detailed-exitcode` exited
  0; the second measured count=1 / destroys=0 /
  addresses=["null_resource.jellyfin_transcode_dataset"]. Both plan files lived
  on the WORKSTATION under $TMPDIR and were deleted after use; neither was
  committed. 02.1-03 then completed, and the post-apply `/` reading of
  21651214336 bytes (20.16 GiB) is within ordinary churn of the sample above --
  confirming this plan's writes all landed on the fast pool and none on `/`,
  which is the basis the `/` ruling was granted on.

  STILL TRUE, AND STILL THE THING TO WATCH: the `/` ruling covered 02.1-03 ONLY.
  02.1-04 onward (the copy, the recreate) DO touch `/` and must be re-ruled on
  their own numbers. The margin is 0.16 GiB and the cache has now been flat
  across four samples spanning roughly an hour.

2026-09-02T21:49:07Z plan=02.1-04 root_avail_bytes=21643157504 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13884071113 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- the `/` ruling for 02.1-04 was GRANTED with a
NUMERIC SELF-HALT BUDGET, and the copy turned out to be `/`-NEUTRAL by measurement.
--------------------------------------------------------------------------------

  Delta against plan=02.1-03, ~23 minutes earlier:

    root_avail_bytes         21651746816 -> 21643157504   -8589312  (-8.2 MiB)
    oldvol_transcodes_bytes  13473768858 -> 13473768858          0
    oldvol_total_bytes       13884071113 -> 13884071113          0
    oldvol_transcodes_files         1324 -> 1324                 0

  MARGIN against the D-17 floor of 21474836480:
    21643157504 - 21474836480 = 168321024 = 0.16 GiB = 160.5 MiB

  FIFTH CONSECUTIVE BYTE-IDENTICAL CACHE READING, now spanning 70 minutes
  (20:38:32Z -> 21:49:07Z) across five samples. The 8.2 MiB `/` lost in this
  window is again ordinary churn from the other 78+ containers -- all three
  oldvol fields are unchanged, so none of it is the retention problem. The
  margin's slow erosion (185 -> 190 -> 168.7 -> 160.5 MiB) is that churn, not
  transcoding.

  THE `/` RULING FOR 02.1-04 IS GRANTED, WITH A BUDGET RATHER THAN A BLANKET.
  02.1-03 recorded that its ruling covered itself only and that 02.1-04 onward
  had to be re-ruled. It has now been re-ruled BY THE EXECUTE-PHASE ORCHESTRATOR
  (NOT A HUMAN DECISION), and the ruling is narrower and more explicit than
  02.1-03's:

  ATTRIBUTION, STATED PRECISELY, BECAUSE LATER PHASES WILL ACT ON THIS RECORD.
  An earlier draft of this block said the `/` budget had been "re-ruled by the
  operator". That was WRONG and is corrected here. In this phase to date:

    - THE OPERATOR (THE HUMAN) HAS RULED EXACTLY ONE THING: the Terraform drift
      question in 02.1-03, selecting Option B -- clear the output-only drift in
      its own guarded no-op apply first, then run the plan against a literally
      clean exit-0 baseline. That was a human decision, taken at a decision
      checkpoint, and it is recorded in the 02.1-03 CLOSURE note above.
    - THE `/` SELF-HALT BUDGET BELOW WAS RULED BY THE ORCHESTRATOR AGENT. No
      human was asked and no human answered. It carries an agent's authority,
      not an operator's, and it should be weighed as such -- if a later plan
      wants to lean on it for a materially riskier step than this one, escalate
      to the operator rather than treating it as settled policy.

    - The D-17 floor of 21474836480 is a deliberately conservative POLICY
      tripwire, not a physical limit. Actually-free is 20.16 GiB. The failure in
      this estate's incident record is ACTUAL exhaustion of `/` -- which killed
      ssh on both atlantis and LXC 100 while ping still answered -- and there is
      ~20 GiB of real headroom before anything of that shape.
    - Proceed within a hard numeric self-halt budget, measured BEFORE each
      `/`-touching step: STOP if any single step would consume more than 2 GiB of
      `/`; STOP if `/` actually-free would drop below 10 GiB; STOP if the
      transcode cache stops being flat and starts growing mid-run.
    - This phase is the thing that RESTORES the margin, so proceeding is the
      `/`-relieving path.

  WHAT 02.1-04 ACTUALLY SPENT: ZERO bytes of `/`. Measured, not assumed:
  `stat -c %d` reports / = 64519, /var/lib/docker/volumes = 64519 (the SOURCE is
  on `/`), and /mnt/fast/appdata/media = 63 -- a genuinely distinct ZFS
  filesystem with 1.31 TiB available. The copy therefore READS from `/` and
  WRITES to another filesystem; no step staged through a temp directory on `/`;
  and no image was pulled (the `image:` tag is unchanged at 10.11.11), which
  would have been the one genuine multi-GB `/` consumer available to this wave.

  A NUMBER THE PLAN SET DID NOT HAVE, AND THE ONE THAT CHANGED THE COPY: the
  volume's `_data` is 13884071113 bytes today, of which `transcodes` alone is
  13473768858 across 1,324 files. The plan was written against a `transcodes` of
  472 K and one file. The 392 MiB the plan repeatedly names is `_data` MINUS
  `transcodes` = 410302255 bytes across 2,664 files, and that is what was copied.
  `transcodes/` was deliberately EXCLUDED -- it is scheduled for deletion in
  02.1-06, it would be shadowed by the nested /cache/transcodes bind and become
  unreachable dead weight, and copying it would have blown the plan's own
  headroom precondition (21474836480 + 2 x 13884071113 = 49242978706 against
  21643157504 available). See 02.1-04-SUMMARY.md, deviation 1.

  WHAT WOULD OVERTURN THIS: the budget conditions above -- a step costing more
  than 2 GiB of `/`, `/` actually-free under 10 GiB, or a resumed rise in
  oldvol_transcodes_bytes. None fired. THE RULING COVERS 02.1-04 ONLY. 02.1-05 is
  the recreate and is a genuinely different shape -- it stops and restarts the
  container -- so it gets its own numbers, as this one did.

  ADDENDUM, ~10 minutes later: THIS SAMPLE AND THE BLOCK ABOVE WERE WRITTEN BY AN
  EXECUTOR INSTANCE THAT WAS THEN KILLED MID-PLAN by a transient API server
  error -- a harness failure, not a plan defect and not quota. Recorded here
  because the block above is written in the past tense about a copy that, at the
  moment those words were composed, had only just finished and had NOT been
  committed. A continuation agent picked the plan up and re-derived every
  assertion from live state rather than trusting the narrative:

    root_avail_bytes  21643157504 (21:49, above) -> 21657853952 (22:00)  +14.0 MiB

  `/` went UP across the copy. That is the empirical confirmation of the
  `/`-neutrality claim above, and it is worth more than the `stat -c %d`
  reasoning that predicted it. All three oldvol_* fields are STILL byte-identical
  at 22:00 -- a SIXTH consecutive identical reading, now spanning 82 minutes.

  A TRAP THE FAILURE EXPOSED, WORTH CARRYING FORWARD: the orchestrator's
  post-failure damage check read `/mnt/fast/transcode` (0 entries) and concluded
  the copy had never started. It had in fact COMPLETED. `/mnt/fast/transcode`
  being empty is this plan's SUCCESS condition, not evidence of inaction -- the
  copy's destination is `/mnt/fast/appdata/media/jellyfin/cache`. When checking
  for damage after an interrupted plan, read the path the plan WRITES to, not the
  path it asserts stays empty.

2026-09-02T22:16:55Z plan=02.1-05 root_avail_bytes=21662257152 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13885711901 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- THIS IS THE CUTOVER SAMPLE. The line above is
the LAST reading taken while Jellyfin was still writing its cache onto `/`.
--------------------------------------------------------------------------------

  Delta against plan=02.1-04, ~28 minutes earlier:

    root_avail_bytes         21643157504 -> 21662257152  +19099648  (+18.2 MiB)
    oldvol_transcodes_bytes  13473768858 -> 13473768858          0
    oldvol_total_bytes       13884071113 -> 13885711901   +1640788  (+1.56 MiB)
    oldvol_transcodes_files         1324 -> 1324                 0

  MARGIN against the D-17 floor of 21474836480:
    21662257152 - 21474836480 = 187420672 = 0.17 GiB = 178.7 MiB

  VERIFIED WITH A SECOND INSTRUMENT before being written down, per the 01-09
  standing rule: `stat -f -c '%a %S' /` returned bavail=5288637 at bsize=4096 =
  21662257152 bytes, matching `df -B1 --output=avail /` TO THE BYTE.

  SEVENTH CONSECUTIVE BYTE-IDENTICAL TRANSCODE READING, now spanning 98 minutes
  (20:38:32Z -> 22:16:55Z). Nobody has watched television all evening, which is
  the only reason a phase with 178 MiB of margin has been comfortable.

  THE +1.56 MiB ON oldvol_total IS NOT THE RETENTION PROBLEM AND IS FULLY
  ACCOUNTED FOR. It is 10 files under images/resized-images/, written by Jellyfin
  between 02.1-04's 21:54Z copy and now -- ordinary live-cache churn. They were
  copied across DETERMINISTICALLY by this plan's pre-cutover reconciliation pass
  rather than absorbed by the row-6 tolerance, and the +1640788 figure reappears
  identically on the destination's scope and images totals afterwards. See
  02.1-05-mounts.txt sections 2 and 5.

  THE `/` RULING FOR 02.1-05 WAS GRANTED BY THE EXECUTE-PHASE ORCHESTRATOR (AN
  AGENT -- NOT A HUMAN DECISION), on the same numeric self-halt terms 02.1-04 got
  and with one addition specific to this plan's shape:

    - STOP if any single step would consume more than 2 GiB of `/`.
    - STOP if `/` actually-free would drop below 10 GiB.
    - STOP if the transcode cache starts growing mid-run (someone began playback).
    - NEW, AND SPECIFIC TO A RECREATE: a container recreate can PULL AN IMAGE into
      /var/lib/docker on `/`, which is the one genuine multi-GB `/` consumer
      available to this wave. Check residency BEFORE recreating and prefer the
      image already on disk.

  THE ATTRIBUTION NOTE FROM 02.1-04 STILL STANDS AND IS NOT WEAKENED BY REPETITION:
  the operator has ruled exactly ONE thing in this phase -- 02.1-03's Terraform
  Option B. Everything about the `/` budget is an agent's ruling and carries an
  agent's authority.

  NOTHING FIRED, AND THE RECREATE WAS `/`-POSITIVE:

    image residency asserted BEFORE the recreate: already present, digest
    d57d4a0c…, 1561075198 bytes. NO PULL OCCURRED -- the digest was identical
    after, and compose's default pull policy is `missing`.

    root_avail_bytes  21662257152 (22:16:55Z, this sample)
                   -> 21699317760 (22:21:24Z, post-recreate)   +35.3 MiB
                   -> 21708996608 (22:21:52Z, standing check)  +44.6 MiB

  `/` WENT UP ACROSS THE CUTOVER -- the old container's writable layer released
  and not fully replaced. Margin against the D-17 floor is now 223.1 MiB, the
  widest it has been since the phase began.

  WHAT CHANGED AT 2026-09-02T22:19:52Z, AND WHY THIS FILE'S FIELDS ARE ABOUT TO
  MEAN SOMETHING DIFFERENT. The container was recreated onto declared binds. From
  that instant every new cache write lands on fast/appdata/media and every new
  transcode write lands on the 50 G quota-bounded fast/transcode. Proven, not
  assumed: zero `Type: volume` mounts, both binds matched exactly under grep -qx,
  and a file written into /cache/transcodes from INSIDE the container appeared on
  /mnt/fast/transcode and NOT under the cache bind (assumption A4 CONFIRMED).
  Jellyfin then wrote its own `.jellyfin-transcode` marker into the dataset at
  22:20Z, which is the server itself agreeing.

  SO oldvol_transcodes_bytes IS NOW A FROZEN STOCK, NOT A GROWING ONE. If it ever
  moves again from here, that is not the old problem continuing -- it is
  jellyfin.yaml having been reverted, and the standing check's section 2 is the
  alarm. The 12.55 GiB itself is still on `/` and is reclaimed by 02.1-06's
  `docker volume rm`, which is deliberately still ahead of us: the volume is the
  rollback, and D-10 keeps it until the thing it protects is proven. It is proven
  now, so 02.1-06 may proceed.

  THE STANDING CHECK WENT 5 FAILURES -> 1. The one remaining red is section 3,
  "the anonymous volume STILL EXISTS", and it is SUPPOSED to be red at this point
  in the phase. A green section 3 here would mean the rollback had been destroyed
  before the cutover was proven.

  WHAT WOULD OVERTURN THE RULING FOR THE NEXT PLAN: unchanged from 02.1-04 --
  a step costing more than 2 GiB of `/`, `/` actually-free under 10 GiB, or a
  resumed rise in oldvol_transcodes_bytes. THE RULING COVERS 02.1-05 ONLY. 02.1-06
  is the `docker volume rm`, a different shape again (it is the plan that RELEASES
  12.55 GiB rather than spending any), and it gets its own numbers.

2026-09-02T22:38:35Z plan=02.1-06 root_avail_bytes=21694996480 root_size_bytes=134682742784 oldvol_transcodes_bytes=13473768858 oldvol_total_bytes=13885711901 oldvol_transcodes_files=1324

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- a REAL viewing session happened between this
sample and the next, it produced ZERO transcode bytes, and that is a NON-
OBSERVATION rather than a pass.
--------------------------------------------------------------------------------

  Delta against plan=02.1-05, ~22 minutes earlier:

    root_avail_bytes         21662257152 -> 21694996480  +32739328  (+31.2 MiB)
    oldvol_transcodes_bytes  13473768858 -> 13473768858          0
    oldvol_total_bytes       13885711901 -> 13885711901          0
    oldvol_transcodes_files         1324 -> 1324                 0

  MARGIN against the D-17 floor of 21474836480:
    21694996480 - 21474836480 = 220160000 = 0.21 GiB = 209.9 MiB

  VERIFIED WITH A SECOND INSTRUMENT before being written down, per the 01-09
  standing rule: `stat -f -c '%a %S' /` returned bavail=5296630 at bsize=4096 =
  21694996480 bytes, matching `df -B1 --output=avail /` TO THE BYTE.

  EIGHTH CONSECUTIVE BYTE-IDENTICAL TRANSCODE READING, now spanning exactly two
  hours (20:38:32Z -> 22:38:35Z). The margin is the WIDEST it has been in the
  phase -- 209.9 MiB against 185 MiB at the phase head.

  THE OPERATOR WATCHED A FULL EPISODE IN THE HOUR BEFORE THIS SAMPLE, AND IT
  PRODUCED NOTHING. Measured by the orchestrator at 22:41:48Z, three minutes
  after this line:

    /mnt/fast/transcode         used=0 bytes, 1 file (the .jellyfin-transcode
                                marker Jellyfin itself wrote at 22:20Z)
    old volume transcodes/      13473768858 -- byte-identical
    NEW files in the old volume since the 22:19:52Z cutover: 0
    active ffmpeg processes:    0

  THE EPISODE DIRECT-PLAYED. The client played the source codec natively, so
  Jellyfin never invoked ffmpeg and no segments were produced ANYWHERE.

  WHAT THAT IS, AND WHAT IT IS NOT -- and this is the whole reason it is written
  down at this length. It is NOT evidence that the relocation works. An empty
  /mnt/fast/transcode after a direct-play session is the ABSENCE OF A TEST, not a
  passing one, and reading it as a pass would discharge a CONS-04 "fires" row on a
  non-observation -- precisely the "transcript that only looks like evidence"
  failure this plan's objective names. 02.1-04's ADDENDUM already recorded the
  mirror-image version of this trap (an empty /mnt/fast/transcode read as "the
  copy never started" when it had in fact completed). Same directory, opposite
  wrong conclusion, twice in one phase.

  WHAT IT GENUINELY IS, recorded as exactly that and no more: a full real playback
  session ran end-to-end through the new bind shape, Jellyfin stayed healthy, and
  ZERO bytes were written to `/`. That is real evidence the cutover did not break
  playback and that direct-play traffic does not touch `/`. It is also why the
  "cache is flat between sessions" claim carried from earlier waves is now BETTER
  supported than before, not weaker.

  CONSEQUENCE FOR THIS PLAN: 02.1-06 does not and never did rely on organic
  transcode activity -- its proof client FORCES a transcode by sending a
  PlaybackInfo device profile with an EMPTY DirectPlayProfiles list, which is what
  makes the session above impossible to repeat accidentally. The operator has now
  stopped for the night, so any growth in /mnt/fast/transcode from here is THIS
  PLAN'S doing, which makes it clean, attributable evidence.

  THE `/` RULING FOR 02.1-06 WAS GRANTED BY THE EXECUTE-PHASE ORCHESTRATOR (AN
  AGENT -- NOT A HUMAN DECISION), on the same numeric self-halt terms as 02.1-04
  and 02.1-05: STOP if any single step would consume more than 2 GiB of `/`; STOP
  if `/` actually-free would drop below 10 GiB; STOP if the transcode cache
  resumes growing outside this plan's own driven playback. The attribution note
  from 02.1-04 still stands: the operator has ruled exactly ONE thing in this
  phase, 02.1-03's Terraform Option B.

  THIS PLAN IS `/`-POSITIVE BY CONSTRUCTION. It spends nothing and releases
  12.55 GiB. If a step in it is about to consume `/` rather than free it,
  something is wrong and it stops.

2026-09-02T23:53:03Z plan=02.1-06 root_avail_bytes=35575803904 root_size_bytes=134682742784 oldvol_transcodes_bytes=GONE oldvol_total_bytes=GONE oldvol_transcodes_files=GONE

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- THIS IS THE TRANSITION LINE. The volume this
file has watched since 20:38:32Z NO LONGER EXISTS, and the `oldvol_*` fields are
GONE from here on, never UNKNOWN.
--------------------------------------------------------------------------------

  WHY `GONE` AND NOT `UNKNOWN`. The FORMAT block above reserves UNKNOWN for an
  instrument that could not look, because 03-01 already paid for conflating
  "could not look" with "it is empty". `docker volume rm` succeeded at
  2026-09-02T23:52:53Z and returned the id; the fields are unmeasurable because
  the thing they measured was deliberately destroyed, not because `du` failed.
  Every later sample carries GONE. A future UNKNOWN in these fields would mean
  something quite different and should be investigated.

  Delta against plan=02.1-05's post-recreate readings:

    root_avail_bytes         21694996480 -> 35575803904  +13880807424  (+12.93 GiB)
    oldvol_transcodes_bytes  13473768858 -> GONE
    oldvol_total_bytes       13885711901 -> GONE
    oldvol_transcodes_files         1324 -> GONE

  MEASURED ACROSS THE DELETION ITSELF, which is the honest number because the
  22:38 -> 23:52 window also contains two driven transcode sessions of this
  plan's own making:

    df -B1 --output=avail /  immediately before  : 21674889216
    df -B1 --output=avail /  ten seconds after   : 35575803904
    subtraction                                  : +13900914688 = 12.95 GiB

  VERIFIED WITH A SECOND INSTRUMENT before being written down, per the 01-09
  standing rule: `stat -f -c '%a %S' /` returned bavail=8685499 at bsize=4096 =
  35575803904 bytes, matching `df -B1 --output=avail /` TO THE BYTE.

  MARGIN against the D-17 floor of 21474836480:
    35575803904 - 21474836480 = 14100967424 = 13.13 GiB

  THE MARGIN WENT FROM 185 MiB AT THE PHASE HEAD TO 13.13 GiB. That is a factor
  of about seventy-three, and it is the whole point of the phase. The reading is
  now BETTER than the ~33-36 GiB the plan set was originally written against and
  that STATE.md recorded after 03-01's reap -- so the number that made every plan
  from 02.1-01 onward uncomfortable is no longer the constraint.

  WHAT THE 12.95 GiB IS NOT: it is not 02.1-09's image reap, which has not run.
  `docker system df` still reports 62.44 GB reclaimable across 95 images and that
  is a separate, later, separately-attributed number. Under D-31 the reap now
  runs after this deletion, which is what keeps the two figures from being
  confused with one another.

  ON THE PLAN'S OWN EXPECTED FIGURE OF ~392 MiB: it is stale and known to be.
  392 MiB is `_data` MINUS `transcodes/`, correct only for a plan set written
  when `transcodes/` held 472 K in one file. 02.1-01's very first D-32 sample
  measured 13473768858 bytes across 1,324 files, and 02.1-04 recorded the
  correction in its deviation 1. The acceptance number was wrong; the reclaim is
  right.

  ALSO TRUE, AND THE THING TO WATCH NEXT: the 02.1-05 recreate MINTED TWO NEW
  ANONYMOUS VOLUMES, `c479d815…a0cf09` and `efbcf4d6…d8310e`, both created at
  22:19:12Z, both dangling, both 0 bytes and 0 entries. Nothing writes to them --
  jellyfin holds zero `Type: volume` mounts, asserted -- so this is not the old
  problem re-arming. They were NOT removed: D-11 scopes 02.1-06 to an inventory
  and nobody has ruled on deleting them. But the failure this phase exists to fix
  began as exactly one anonymous volume that nobody was watching, so they are
  named here rather than left in a listing.

  THE STANDING CHECK IS GREEN FOR THE FIRST TIME: FAILURES 0, exit 0 in default
  mode. Section 3 -- "the anonymous volume STILL EXISTS" -- has been the single
  deliberate red since 02.1-02 built the script, and it was supposed to be. It
  has now cleared for the right reason.

2026-09-03T00:44:07Z plan=02.1-09 root_avail_bytes=35555835904 root_size_bytes=134682742784 oldvol_transcodes_bytes=GONE oldvol_total_bytes=GONE oldvol_transcodes_files=GONE

--------------------------------------------------------------------------------
READ THIS BEFORE THE NEXT SAMPLE -- the URGENCY THIS PLAN WAS WRITTEN UNDER NO
LONGER EXISTS, and that CHANGES THE RISK CALCULUS OF THE APPROVAL GATE.
--------------------------------------------------------------------------------

  Delta against plan=02.1-06's post-deletion sample, ~51 minutes earlier:

    root_avail_bytes         35575803904 -> 35555835904   -19968000  (-19.0 MiB)
    oldvol_transcodes_bytes  GONE -> GONE
    oldvol_total_bytes       GONE -> GONE
    oldvol_transcodes_files  GONE -> GONE

  MARGIN against the D-17 floor of 21474836480:
    35555835904 - 21474836480 = 14080999424 = 13.11 GiB

  VERIFIED WITH A SECOND INSTRUMENT before being written down, per the 01-09
  standing rule: `stat -f -c '%a %S' /` returned bavail=8680624 at bsize=4096 =
  35555835904 bytes, matching `df -B1 --output=avail /` TO THE BYTE.

  THE 19.0 MiB LOST IN THIS WINDOW IS ORDINARY CHURN from the other 100+
  containers writing to `/` -- the same class of movement as the +5.2 MiB and
  -21.5 MiB readings recorded between 02.1-02 and 02.1-04. It is NOT the
  retention problem: the old volume is GONE, jellyfin holds zero `Type: volume`
  mounts, and every transcode write now lands on the quota-bounded
  fast/transcode dataset (devid=47, parent=64519 -- re-read this sample).

  THE MOST IMPORTANT THING ON THIS LINE IS WHAT IT MEANS FOR 02.1-09's GATE.
  This plan carries the phase's ONLY BLOCKING HUMAN APPROVAL, and when the plan
  set was written that gate sat against a margin of 185 MiB and a stated budget
  "measured in hours" (see the 02.1-01 block at the head of this file). D-31
  moved the reap to wave 8 precisely so the gate would stop costing anything.
  It has worked: the margin is 13.11 GiB, a factor of about seventy-three on the
  phase-head reading, and 02.1-06 -- not this plan -- is what bought it.

  CONSEQUENCE, STATED SO A LATER READER DOES NOT HAVE TO INFER IT: there is NO
  SPACE PRESSURE justifying a marginal deletion here. The correct posture at
  this gate is CONSERVATIVE -- exclude anything doubtful and say why -- and the
  gate may take as long as the operator wants. If a future reading of this file
  is used to argue that a reap was urgent, check the margin on the line, not the
  prose in the plan.

  THE STANDING CHECK IS GREEN AT THIS SAMPLE: `check-jellyfin-transcode.sh`
  FAILURES total 0, exit 0, `/` headroom 33.11 GiB, volume mounts 0, quota
  53687091200, transcode zfs=yes devid=47 parent=64519.

  NOTHING IN THIS PLAN HAS BEEN DELETED AT THE TIME THIS LINE WAS WRITTEN. The
  inventory is read-only and the prune has not run.

2026-09-03T07:08:41Z plan=02.1-10 root_avail_bytes=37022195712 root_size_bytes=134682742784 oldvol_transcodes_bytes=GONE oldvol_total_bytes=GONE oldvol_transcodes_files=GONE

--------------------------------------------------------------------------------
THIS IS THE LAST LINE. THE WATCH IS CLOSED -- 02.1-10 is the final plan of the
phase and the last writer in the totally-ordered append chain.
--------------------------------------------------------------------------------

  Delta against plan=02.1-09's PRE-REAP sample, ~6h24m earlier:

    root_avail_bytes         35555835904 -> 37022195712  +1466359808  (+1.37 GiB)
    oldvol_transcodes_bytes  GONE -> GONE
    oldvol_total_bytes       GONE -> GONE
    oldvol_transcodes_files  GONE -> GONE

  The +1.37 GiB is 02.1-09's reap (+1.45 GiB, measured in its own six-second
  window at 37039017984) MINUS 16822272 bytes (16.0 MiB) of ordinary churn from
  the other 100+ containers in the six hours since. Both figures are recorded
  rather than one being chosen, for the same reason 02.1-09 recorded both of its
  own: they are different windows, not disagreeing instruments.

  MARGIN against the D-17 floor of 21474836480:
    37022195712 - 21474836480 = 15547359232 = 14.48 GiB

  VERIFIED WITH A SECOND INSTRUMENT before being written down, per the 01-09
  standing rule: `stat -f -c '%a %S' /` returned bavail=9038622 at bsize=4096 =
  37022195712 bytes, matching `df -B1 --output=avail /` TO THE BYTE.

  THE PHASE-HEAD-TO-CLOSE ARITHMETIC, which is the only number this file was
  opened to produce:

    2026-09-02T20:38:32Z  plan=02.1-01  root_avail_bytes=21668859904  margin  185 MiB
    2026-09-03T07:08:41Z  plan=02.1-10  root_avail_bytes=37022195712  margin 14.48 GiB

  A factor of about eighty. 12.95 GiB of it is 02.1-06's `docker volume rm` --
  the phase's actual subject -- and 1.45 GiB is 02.1-09's image reap, which is a
  different and separately-attributed number. Do not merge them; 02.1-06 and
  02.1-09 each went to some trouble to keep them apart.

  WHAT THE WATCH ACTUALLY BOUGHT, recorded plainly because a passive observation
  series is easy to dismiss afterwards as having done nothing. It fired on its
  FIRST sample: 02.1-01 measured a margin of 185 MiB against a plan set written
  for ~33-36 GiB, and every subsequent plan was ruled on that number rather than
  on the assumption. Two orderings (the 02.1-03 Terraform question, the 02.1-09
  reap position) and three consecutive `/`-budget grants were decided against
  these lines. It also caught the thing it was built for -- 12.55 GiB of transcode
  cache accumulating in ~2 days -- on the first reading, before `/` reached zero.

  AND WHAT IT DID NOT BUY, which belongs here too: it ASSERTED NOTHING all phase.
  Every line is an observation a human had to read. The standing coverage that
  replaces it -- `check-jellyfin-transcode.sh` section 1, folded into
  `scripts/quick-health-check.sh` by THIS plan -- is what turns the `/` headroom
  reading into something that fails closed. That fold-in is the watch's successor
  and the reason this file stops here rather than continuing into later phases.
  It is still MANUAL (D-22): it fires when someone runs it. See the third
  EXIT-CODE BEHAVIOUR CHANGED notice in quick-health-check.sh for that limit,
  stated where the person about to trust a green tick will be standing.
