================================================================================
02.1-10  NEGATIVE CONTROLS, PROPAGATION AND ANCHOR PROOFS, AND THE KNOWN LIMITS
================================================================================

WHAT THIS IS
  The executed evidence for VALIDATION.md rows 21, 22, 23 and 24 -- all four of
  which are `fires` rows -- plus a fourth control (the usage-error exit code)
  and the restore-and-reverify step for every test that touched anything.

  Rows 21-24 are `fires` rows for one reason: reading the fail-closed branches
  in check-jellyfin-transcode.sh and agreeing they look right discharges
  NOTHING. This estate's worst outage was six weeks of two containers being down
  while the health check reported clean, and the shape of that failure was a
  check that could not see them rather than a check that saw them and lied. A
  branch that has never been executed is a branch that is not known to work.

WHAT WAS *NOT* MUTATED, STATED UP FRONT BECAUSE IT IS THE DESIGN
  Controls 1, 2 and 3 are PURE ENVIRONMENT OVERRIDES. They set ZFS_HOST,
  JELLYFIN_CONTAINER and JELLYFIN_SECRETS for the duration of one process and
  change nothing else. Control 4 passes a bad flag. Across all four:

    - no file was created, edited, moved or deleted
    - no mode or owner was changed, temporarily or otherwise
    - no container was created, stopped, started or removed
    - no ZFS property was read-modified or written
    - no package was installed and no image was pulled

  Asserted rather than claimed, after the four controls ran:

    jellyfin:                                  running healthy
    git -C /mnt/fast/stacks status --porcelain scripts/   -> EMPTY
    sha256 check-jellyfin-transcode.sh (host)  d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
    sha256 check-jellyfin-transcode.sh (wkstn) d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
    docker ps -a | grep -c '^no-such-container-021$'      -> 0
    stat -c '%a %U' /mnt/fast/secrets/jellyfin-deercrest.env -> 600 root
    test -e /mnt/fast/spike-021/no-such-credential.env    -> ABSENT (as required)

  THE THREE ENV OVERRIDES ARE WHY THIS PAGE EXISTS AT ALL. Plan 02.1-02 added
  ZFS_HOST, JELLYFIN_CONTAINER and JELLYFIN_SECRETS in the ${VAR:-default} form
  specifically so these controls could be run without mutating a real file. The
  cross-AI review's Plan-02 finding is the reason the third one is there: the
  reviewed version hardcoded the credential path, so control 3 either could not
  run or required chmod'ing the real file -- and `600 root` on that file is
  itself an invariant that check-music-consumers.sh asserts. Staging a failure
  in one check by breaking a different check's invariant is not a control, it is
  a second defect.

  Overriding a path does NOT weaken the gate. The 600-root assertion runs
  against whatever path is in use, so control 3 is refused for the RIGHT reason.

WORDING THE REFUSAL ACCURATELY (control 3)
  The credential gate fires on the file being ABSENT, or on its mode/owner not
  being exactly `600 root`. It does NOT fire on the file being "unreadable":
  this script runs as root and root can read anything, so "unreadable" describes
  a condition that cannot occur here. The distinction is kept because a later
  reader who believes the gate catches unreadability will trust it to catch
  something it structurally cannot.

--------------------------------------------------------------------------------
SUMMARY OF THE SIX PROOFS
--------------------------------------------------------------------------------

  #  What                                    Row   Command shape                 Exit
  -  --------------------------------------  ----  ----------------------------  ----
  1  unroutable ZFS host                     21    ZFS_HOST=192.0.2.1               1
  2  non-existent container                  22    JELLYFIN_CONTAINER=no-such-...   1
  3  absent credential path                  --    JELLYFIN_SECRETS=/…/no-such…     1
  4  usage error                             --    --nonsense                       2
  5  the fold-in propagates the exit code    23    host script mv'd aside           1
  6  the fold-in's summary anchor            24    anchor renumbered on the host    1

  Plus, as the last entry of each half: the unmodified run exits 0.

  Controls 5 and 6 live in the second half of this file, after task 2's fold-in
  exists to be tested. They are the two that DO break something, and each one
  records a pre-test sha256, restores, asserts the hash matches, and re-runs
  green -- because a proof that leaves the thing it broke broken is a defect it
  installed rather than a control it discharged.

================================================================================
CONTROL 1 -- UNROUTABLE ZFS HOST  (VALIDATION.md row 21, a `fires` row)
================================================================================

COMMAND
  ssh -n -o BatchMode=yes root@172.16.1.159 \
    'ZFS_HOST=192.0.2.1 bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh'

WHY 192.0.2.1 AND NOT A RANDOM ADDRESS
  RFC 5737 TEST-NET-1. It is reserved for documentation and is guaranteed not to
  be allocated, so the ssh fails at connect rather than hanging against a live
  host that happens not to answer, and it cannot accidentally reach a real
  machine on this or any other estate.

EXPECTED
  exit non-zero; the output names the route as unreachable or says UNKNOWN; and
  NO green line is printed for the quota assertion or for the `mounted`
  assertion. A quota reading that could not be taken must be its own distinct
  red -- never a `!= 53687091200` mismatch, which a reader parses as DRIFT and
  goes looking for a configuration change that never happened.

OBSERVED -- exit 1, and all three limbs hold:
  - section 0: "zfs unreachable both locally and at root@192.0.2.1 ... UNKNOWN,
    not green", ZFS_ROUTE=unavailable
  - section 4: TWO separate reds, "quota unverifiable -- no zfs route ...
    UNKNOWN, not 50 G" and "mounted state unverifiable from atlantis". Neither
    is phrased as a mismatch.
  - summary: `transcode quota: UNKNOWN bytes` and `transcode mounted: zfs=UNKNOWN`
  - FAILURES total: 3

THE CONTAINER-SIDE INSTRUMENT STILL ANSWERED, AND THAT IS THE WHOLE REASON
THERE ARE TWO. Recorded explicitly because the plan asks either way:

  ✅ /mnt/fast/transcode device id 47 differs from /mnt/fast's 64519 --
     separately mounted (VALIDATION row 36, instrument 2)

  Instrument 2 is a container-side `stat -c %d` comparison that never reaches
  atlantis, so it kept working while instrument 1's route was severed. That is
  the designed behaviour: the mounted state degrades from "asserted twice" to
  "asserted once, and the reader is told which half is missing", rather than
  from "asserted" to "unknown". The summary line carries both halves for exactly
  this reason -- `zfs=UNKNOWN devid=47 parent=64519` is legible at a glance as
  one instrument down and one still reporting.

FULL TRANSCRIPT (ANSI stripped; exit code appended by the invocation)
--------------------------------------------------------------------------------
  🎬 Jellyfin Transcode Retention Audit
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    container:  jellyfin
    transcode:  /mnt/fast/transcode  (dataset fast/transcode)
    cache:      /mnt/fast/appdata/media/jellyfin/cache
    host:       selfhost
    date:       2026-09-03T07:09:24Z
    mode:       assert (exits non-zero on any failed check)
  
  🧰 0. Routes and preconditions (D-19)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ docker /usr/bin/docker
    ✅ curl /usr/bin/curl
    ✅ jq /usr/bin/jq
    ✅ stat /usr/bin/stat
    ❌ zfs unreachable both locally and at root@192.0.2.1 — section 4's authoritative quota and
           mounted readings cannot be made. UNKNOWN, not green.
    ZFS_ROUTE=unavailable
    ✅ Jellyfin resolved to 192.168.90.25:8096 via docker inspect (never a pinned literal, never the bare name)
    ✅ Jellyfin credential /mnt/fast/secrets/jellyfin-deercrest.env (600 root)
  
  💾 1. Root filesystem headroom (TRAN-05, D-16, D-17)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / avail=37021646848 bytes (34.48 GiB) of size=134682742784 bytes (125.43 GiB)
    ✅ / headroom 34.48 GiB ≥ floor 20.00 GiB — margin 14.48 GiB
  
  🐳 2. The mount invariant (TRAN-01, TRAN-02, D-02, D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ container 'jellyfin' exists (asserted before anything is enumerated)
    ✅ zero Type:volume mounts on jellyfin (the invariant, not the instance — D-18)
    ✅ /cache/transcodes is 'bind /mnt/fast/transcode' (VALIDATION row 1, TRAN-01)
    ✅ /cache is 'bind /mnt/fast/appdata/media/jellyfin/cache' (VALIDATION row 2, TRAN-01)
    ✅ /data/transcode appears in no mount destination (D-02, VALIDATION row 3)
  
  🗑  3. The old anonymous volume (secondary to section 2, per D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ the anonymous volume d98b2ff94482… is gone (VALIDATION row 8, D-10)
  
  📏 4. The transcode quota and its mount (TRAN-03, D-12, D-13)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ❌ quota unverifiable — no zfs route (see section 0). UNKNOWN, not 50 G
    ❌ mounted state unverifiable from atlantis — no zfs route (see section 0)
    ✅ /mnt/fast/transcode device id 47 differs from /mnt/fast's 64519 — separately mounted (VALIDATION row 36, instrument 2)
    findmnt /mnt/fast/transcode: zfs fast/transcode   (reported, not asserted)
    df size of /mnt/fast/transcode: 53687091200 bytes (50.00 GiB)   (REPORTED not asserted — A3, VALIDATION row 12)
  
  ⚙️  5. The encoding values, read back (TRAN-04, D-20, D-30) — REPORTED, not asserted
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    TranscodingTempPath = /cache/transcodes   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    EnableSegmentDeletion = true   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    SegmentKeepSeconds = 300   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    EnableThrottling = true   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    ThrottleDelaySeconds = 180   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    HardwareAccelerationType = none   (amdgpu mitigation, D-30 — REPORTED; drift here is the expensive regression)
    EnableHardwareEncoding = false   (amdgpu mitigation, D-30 — REPORTED; drift here is the expensive regression)
  
  📊 6. Summary
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / headroom:              37021646848 bytes (34.48 GiB)  (target ≥ 21474836480)
    volume mounts:           0  (target 0)
    transcode quota:         UNKNOWN bytes  (target 53687091200)
    transcode mounted:       zfs=UNKNOWN devid=47 parent=64519  (target yes, and devid ≠ parent)
    encoding values:         REPORTED not asserted  (see section 5)
    unreachable:             0  (could-not-look, counted apart from bound violations — VALIDATION row 38)
    toolchain missing:       1
    FAILURES total:          3
  
  ❌ 3 failed checks
  EXIT=1
--------------------------------------------------------------------------------

================================================================================
CONTROL 2 -- NON-EXISTENT CONTAINER  (VALIDATION.md row 22, a `fires` row)
================================================================================

COMMAND
  ssh -n -o BatchMode=yes root@172.16.1.159 \
    'JELLYFIN_CONTAINER=no-such-container-021 bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh'

WHY THIS CONTROL MATTERS MORE THAN IT LOOKS -- THE SUBTLE PART
  `docker inspect` on a missing container produces EMPTY OUTPUT. The
  zero-volume-mounts assertion in section 2 is literally `result == ""`. So an
  ABSENT CONTAINER WOULD SATISFY THE VERY TEST THAT IS SUPPOSED TO PROVE THE
  CONTAINER IS CLEAN. "Nothing is mounted" and "there is nothing to ask about"
  would render identically, and the phase's central invariant would report green
  on a stopped Jellyfin.

  That is the CR-02 distinction from disable-jellyfin-hwaccel.sh -- "NOTHING HELD"
  AND "COULD NOT LOOK" ARE DIFFERENT ANSWERS AND MUST NOT SHARE A VERDICT -- and
  it is why check-jellyfin-transcode.sh:338-349 proves the container EXISTS
  before it enumerates anything, with its own distinct message. This control is
  the proof that that guard is load-bearing rather than decorative.

EXPECTED
  exit non-zero, AND the output explicitly reports the container as ABSENT
  rather than passing section 2 on an empty enumeration. The plan's rollback
  table says that if it passes, that is a real defect in
  check-jellyfin-transcode.sh to fix here before the fold-in lands.

OBSERVED -- exit 1, and the guard held. NO defect to fix:
  - section 2 prints
      "container 'no-such-container-021' DOES NOT EXIST -- the mount assertions
       below are UNKNOWN.  This is NOT 'zero anonymous volumes'. An absent
       container yields an empty mount enumeration, which would otherwise pass
       the very test it makes meaningless."
    and the three bind assertions below it are NOT printed at all, green or red.
  - the summary line reads `volume mounts: UNKNOWN -- container absent or docker
    unavailable  (target 0)`. It does NOT read `0`. That branch
    (check-jellyfin-transcode.sh:592-596) is the one that keeps the summary
    honest for the fold-in, which reads that line and nothing else.
  - section 0 also reds separately: the t3_proxy address could not be resolved,
    so section 5 is UNREACHABLE and `unreachable: 1` is counted APART from the
    two bound violations (VALIDATION row 38).
  - FAILURES total: 3

  Note what stayed green and correctly so: `/` headroom, the quota and the
  mounted state. None of those depends on the container existing, and a control
  that turned the whole transcript red would have told us less, not more.

FULL TRANSCRIPT (ANSI stripped; exit code appended by the invocation)
--------------------------------------------------------------------------------
  🎬 Jellyfin Transcode Retention Audit
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    container:  no-such-container-021
    transcode:  /mnt/fast/transcode  (dataset fast/transcode)
    cache:      /mnt/fast/appdata/media/jellyfin/cache
    host:       selfhost
    date:       2026-09-03T07:09:39Z
    mode:       assert (exits non-zero on any failed check)
  
  🧰 0. Routes and preconditions (D-19)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ docker /usr/bin/docker
    ✅ curl /usr/bin/curl
    ✅ jq /usr/bin/jq
    ✅ stat /usr/bin/stat
    ✅ zfs delegated to root@172.16.1.158 (LXC 100 is unprivileged; the pool lives on the Proxmox host)
    ZFS_ROUTE=ssh:172.16.1.158
    ❌ could not resolve no-such-container-021's t3_proxy address from docker — section 5 cannot run
    ✅ Jellyfin credential /mnt/fast/secrets/jellyfin-deercrest.env (600 root)
  
  💾 1. Root filesystem headroom (TRAN-05, D-16, D-17)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / avail=37021511680 bytes (34.48 GiB) of size=134682742784 bytes (125.43 GiB)
    ✅ / headroom 34.48 GiB ≥ floor 20.00 GiB — margin 14.48 GiB
  
  🐳 2. The mount invariant (TRAN-01, TRAN-02, D-02, D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ❌ container 'no-such-container-021' DOES NOT EXIST — the mount assertions below are UNKNOWN.
           This is NOT 'zero anonymous volumes'. An absent container yields an empty mount
           enumeration, which would otherwise pass the very test it makes meaningless.
  
  🗑  3. The old anonymous volume (secondary to section 2, per D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ the anonymous volume d98b2ff94482… is gone (VALIDATION row 8, D-10)
  
  📏 4. The transcode quota and its mount (TRAN-03, D-12, D-13)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ quota fast/transcode = 53687091200 bytes (50 G) via ssh:172.16.1.158 (VALIDATION row 9)
    ✅ fast/transcode mounted=yes via ssh:172.16.1.158 (VALIDATION row 36, instrument 1)
    ✅ /mnt/fast/transcode device id 47 differs from /mnt/fast's 64519 — separately mounted (VALIDATION row 36, instrument 2)
    findmnt /mnt/fast/transcode: zfs fast/transcode   (reported, not asserted)
    df size of /mnt/fast/transcode: 53687091200 bytes (50.00 GiB)   (REPORTED not asserted — A3, VALIDATION row 12)
  
  ⚙️  5. The encoding values, read back (TRAN-04, D-20, D-30) — REPORTED, not asserted
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ❌ encoding values: UNREACHABLE — no Jellyfin route (see section 0). UNKNOWN, not green
  
  📊 6. Summary
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / headroom:              37021511680 bytes (34.48 GiB)  (target ≥ 21474836480)
    volume mounts:           UNKNOWN — container absent or docker unavailable  (target 0)
    transcode quota:         53687091200 bytes  (target 53687091200)
    transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid ≠ parent)
    encoding values:         REPORTED not asserted  (see section 5)
    unreachable:             1  (could-not-look, counted apart from bound violations — VALIDATION row 38)
    toolchain missing:       1
    FAILURES total:          3
  
  ❌ 3 failed checks
  EXIT=1
--------------------------------------------------------------------------------

================================================================================
CONTROL 3 -- ABSENT CREDENTIAL PATH
================================================================================

COMMAND
  ssh -n -o BatchMode=yes root@172.16.1.159 \
    'JELLYFIN_SECRETS=/mnt/fast/spike-021/no-such-credential.env bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh'

  The path was asserted ABSENT before and after:
    test -e /mnt/fast/spike-021/no-such-credential.env  ->  does not exist

THIS CONTROL IS EXECUTABLE AT ALL ONLY BECAUSE THE PATH IS ENV-OVERRIDABLE.
  Plan 02.1-02 made JELLYFIN_SECRETS an override in response to the cross-AI
  review's Plan-02 finding. The reviewed version hardcoded it. Without the
  override this control had exactly two forms, and both are wrong:

    (a) do not run it -- leaving a fail-closed branch on the credential route
        unexecuted, which is the thing rows 21-24 exist to refuse; or
    (b) chmod or chown the REAL /mnt/fast/secrets/jellyfin-deercrest.env to
        stage the condition -- which breaks an invariant a DIFFERENT check
        (check-music-consumers.sh) asserts, in order to test this one.

  Pointing the variable at a path that does not exist is the clean third option
  and it is why 02.1-02's change was worth making.

EXPECTED
  exit non-zero, and section 5 reports UNKNOWN rather than an empty settings
  report. An encoding section that printed no values and said nothing would be
  indistinguishable from an encoding section that read seven values and found
  them all correct -- the CONS-04 substitution in its purest form.

OBSERVED -- exit 1:
  - section 0: "Jellyfin credential /mnt/fast/spike-021/no-such-credential.env
    is ABSENT -- section 5 cannot run (D-40)"
  - section 5: "encoding values: UNREACHABLE -- no API key (see
    /mnt/fast/spike-021/no-such-credential.env, D-40). UNKNOWN, not green"
    -- and NO key/value lines are printed. The seven REPORTED lines that appear
    in every healthy run are absent, so there is no possibility of reading a
    stale or empty report as a clean one.
  - `unreachable: 1`, counted apart from bound violations
  - FAILURES total: 2

  Sections 1-4 stayed green, correctly: none of them needs the API key. Again,
  a control that reddened everything would be less informative.

THE REAL CREDENTIAL FILE WAS NOT TOUCHED, AND THIS IS ASSERTED TWICE OVER
  BEFORE (from control 1's own transcript, which ran first and read the real
  path because it overrode only ZFS_HOST):
    ✅ Jellyfin credential /mnt/fast/secrets/jellyfin-deercrest.env (600 root)
  AFTER (an independent stat, run once all four controls had completed):
    600 root root /mnt/fast/secrets/jellyfin-deercrest.env

  The "before" reading is the instrument's own output rather than a separate
  command, which is stronger than a bracketing pair of stats: it is the same
  assertion the estate relies on, executed in situ.

THE REFUSAL IS WORDED ACCURATELY, AND THE WORDING IS THE POINT
  The gate fires on ABSENT, or on a `600 root` MISMATCH. It does not fire on
  "unreadable". This script runs as root; root can read anything; "unreadable"
  is a condition that cannot occur here. check-jellyfin-transcode.sh:285-289
  carries the same correction in-band, made in response to the same review
  finding. Recorded here because a reader who believes the gate catches
  unreadability will trust it to catch something it structurally cannot.

FULL TRANSCRIPT (ANSI stripped; exit code appended by the invocation)
--------------------------------------------------------------------------------
  🎬 Jellyfin Transcode Retention Audit
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    container:  jellyfin
    transcode:  /mnt/fast/transcode  (dataset fast/transcode)
    cache:      /mnt/fast/appdata/media/jellyfin/cache
    host:       selfhost
    date:       2026-09-03T07:09:50Z
    mode:       assert (exits non-zero on any failed check)
  
  🧰 0. Routes and preconditions (D-19)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ docker /usr/bin/docker
    ✅ curl /usr/bin/curl
    ✅ jq /usr/bin/jq
    ✅ stat /usr/bin/stat
    ✅ zfs delegated to root@172.16.1.158 (LXC 100 is unprivileged; the pool lives on the Proxmox host)
    ZFS_ROUTE=ssh:172.16.1.158
    ✅ Jellyfin resolved to 192.168.90.25:8096 via docker inspect (never a pinned literal, never the bare name)
    ❌ Jellyfin credential /mnt/fast/spike-021/no-such-credential.env is ABSENT — section 5 cannot run (D-40)
  
  💾 1. Root filesystem headroom (TRAN-05, D-16, D-17)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / avail=37021261824 bytes (34.48 GiB) of size=134682742784 bytes (125.43 GiB)
    ✅ / headroom 34.48 GiB ≥ floor 20.00 GiB — margin 14.48 GiB
  
  🐳 2. The mount invariant (TRAN-01, TRAN-02, D-02, D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ container 'jellyfin' exists (asserted before anything is enumerated)
    ✅ zero Type:volume mounts on jellyfin (the invariant, not the instance — D-18)
    ✅ /cache/transcodes is 'bind /mnt/fast/transcode' (VALIDATION row 1, TRAN-01)
    ✅ /cache is 'bind /mnt/fast/appdata/media/jellyfin/cache' (VALIDATION row 2, TRAN-01)
    ✅ /data/transcode appears in no mount destination (D-02, VALIDATION row 3)
  
  🗑  3. The old anonymous volume (secondary to section 2, per D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ the anonymous volume d98b2ff94482… is gone (VALIDATION row 8, D-10)
  
  📏 4. The transcode quota and its mount (TRAN-03, D-12, D-13)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ quota fast/transcode = 53687091200 bytes (50 G) via ssh:172.16.1.158 (VALIDATION row 9)
    ✅ fast/transcode mounted=yes via ssh:172.16.1.158 (VALIDATION row 36, instrument 1)
    ✅ /mnt/fast/transcode device id 47 differs from /mnt/fast's 64519 — separately mounted (VALIDATION row 36, instrument 2)
    findmnt /mnt/fast/transcode: zfs fast/transcode   (reported, not asserted)
    df size of /mnt/fast/transcode: 53687091200 bytes (50.00 GiB)   (REPORTED not asserted — A3, VALIDATION row 12)
  
  ⚙️  5. The encoding values, read back (TRAN-04, D-20, D-30) — REPORTED, not asserted
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ❌ encoding values: UNREACHABLE — no API key (see /mnt/fast/spike-021/no-such-credential.env, D-40). UNKNOWN, not green
  
  📊 6. Summary
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / headroom:              37021261824 bytes (34.48 GiB)  (target ≥ 21474836480)
    volume mounts:           0  (target 0)
    transcode quota:         53687091200 bytes  (target 53687091200)
    transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid ≠ parent)
    encoding values:         REPORTED not asserted  (see section 5)
    unreachable:             1  (could-not-look, counted apart from bound violations — VALIDATION row 38)
    toolchain missing:       1
    FAILURES total:          2
  
  ❌ 2 failed checks
  EXIT=1
--------------------------------------------------------------------------------

================================================================================
CONTROL 4 -- USAGE ERROR IS EXIT 2, NOT EXIT 1
================================================================================

COMMAND
  ssh -n -o BatchMode=yes root@172.16.1.159 \
    'bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh --nonsense'

WHY THIS IS A CONTROL AND NOT A FORMALITY
  This is the estate's exit-code convention, established at
  check-music-freeze.sh:31-42: a failed ASSERTION is exit 1, a usage or
  ENVIRONMENT error is exit 2. It is what lets the fold-in in
  quick-health-check.sh distinguish "the check found problems with the estate"
  from "the check was invoked wrongly / cannot run here". Conflating the two
  produces the worst possible reading of a red: an operator investigating a
  Jellyfin fault that is really a typo, or -- far worse -- dismissing a genuine
  red as a harness problem.

  The same exit 2 also covers the Darwin platform guard: this script measures
  free space with `df -B1 --output=avail`, BSD df silently ignores --output, and
  the result would parse into a WRONG NUMBER rather than into an error. "This
  instrument cannot be used here" is not "the estate is broken".

OBSERVED -- exit exactly 2:
  unknown option: --nonsense
  usage: bash scripts/check-jellyfin-transcode.sh [--baseline]
  EXIT=2

FULL TRANSCRIPT (ANSI stripped; exit code appended by the invocation)
--------------------------------------------------------------------------------
  unknown option: --nonsense
  usage: bash scripts/check-jellyfin-transcode.sh [--baseline]
  EXIT=2
--------------------------------------------------------------------------------

================================================================================
THE UNMODIFIED RUN -- THE TRANSCRIPT ENDS ON A GREEN BASELINE
================================================================================

COMMAND
  ssh -n -o BatchMode=yes root@172.16.1.159 \
    'bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh'

WHY THIS IS RECORDED RATHER THAN ASSUMED
  Four consecutive induced failures is a transcript that ends on a red, and a
  reader who stops there cannot tell an induced red from a residual one. Ending
  on the unmodified run with no overrides proves the four controls left the
  estate exactly as they found it -- and it proves it with the same instrument
  that produced the reds, rather than with a separate inspection that could
  disagree for its own reasons.

OBSERVED -- exit 0, FAILURES total 0, unreachable 0, toolchain missing 0:
  / headroom:              37021057024 bytes (34.48 GiB)  (target >= 21474836480)
  volume mounts:           0  (target 0)
  transcode quota:         53687091200 bytes  (target 53687091200)
  transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid != parent)
  encoding values:         REPORTED not asserted  (see section 5)

  The D-30 amdgpu mitigation is intact in the same run:
  HardwareAccelerationType = none, EnableHardwareEncoding = false.

FULL TRANSCRIPT (ANSI stripped; exit code appended by the invocation)
--------------------------------------------------------------------------------
  🎬 Jellyfin Transcode Retention Audit
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    container:  jellyfin
    transcode:  /mnt/fast/transcode  (dataset fast/transcode)
    cache:      /mnt/fast/appdata/media/jellyfin/cache
    host:       selfhost
    date:       2026-09-03T07:10:01Z
    mode:       assert (exits non-zero on any failed check)
  
  🧰 0. Routes and preconditions (D-19)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ docker /usr/bin/docker
    ✅ curl /usr/bin/curl
    ✅ jq /usr/bin/jq
    ✅ stat /usr/bin/stat
    ✅ zfs delegated to root@172.16.1.158 (LXC 100 is unprivileged; the pool lives on the Proxmox host)
    ZFS_ROUTE=ssh:172.16.1.158
    ✅ Jellyfin resolved to 192.168.90.25:8096 via docker inspect (never a pinned literal, never the bare name)
    ✅ Jellyfin credential /mnt/fast/secrets/jellyfin-deercrest.env (600 root)
  
  💾 1. Root filesystem headroom (TRAN-05, D-16, D-17)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / avail=37021057024 bytes (34.48 GiB) of size=134682742784 bytes (125.43 GiB)
    ✅ / headroom 34.48 GiB ≥ floor 20.00 GiB — margin 14.48 GiB
  
  🐳 2. The mount invariant (TRAN-01, TRAN-02, D-02, D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ container 'jellyfin' exists (asserted before anything is enumerated)
    ✅ zero Type:volume mounts on jellyfin (the invariant, not the instance — D-18)
    ✅ /cache/transcodes is 'bind /mnt/fast/transcode' (VALIDATION row 1, TRAN-01)
    ✅ /cache is 'bind /mnt/fast/appdata/media/jellyfin/cache' (VALIDATION row 2, TRAN-01)
    ✅ /data/transcode appears in no mount destination (D-02, VALIDATION row 3)
  
  🗑  3. The old anonymous volume (secondary to section 2, per D-18)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ the anonymous volume d98b2ff94482… is gone (VALIDATION row 8, D-10)
  
  📏 4. The transcode quota and its mount (TRAN-03, D-12, D-13)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ✅ quota fast/transcode = 53687091200 bytes (50 G) via ssh:172.16.1.158 (VALIDATION row 9)
    ✅ fast/transcode mounted=yes via ssh:172.16.1.158 (VALIDATION row 36, instrument 1)
    ✅ /mnt/fast/transcode device id 47 differs from /mnt/fast's 64519 — separately mounted (VALIDATION row 36, instrument 2)
    findmnt /mnt/fast/transcode: zfs fast/transcode   (reported, not asserted)
    df size of /mnt/fast/transcode: 53687091200 bytes (50.00 GiB)   (REPORTED not asserted — A3, VALIDATION row 12)
  
  ⚙️  5. The encoding values, read back (TRAN-04, D-20, D-30) — REPORTED, not asserted
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    TranscodingTempPath = /cache/transcodes   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    EnableSegmentDeletion = true   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    SegmentKeepSeconds = 300   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    EnableThrottling = true   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    ThrottleDelaySeconds = 180   (REPORTED not asserted — a standing check cannot prove a setting FIRES; that is 02.1-08's job)
    HardwareAccelerationType = none   (amdgpu mitigation, D-30 — REPORTED; drift here is the expensive regression)
    EnableHardwareEncoding = false   (amdgpu mitigation, D-30 — REPORTED; drift here is the expensive regression)
  
  📊 6. Summary
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    / headroom:              37021057024 bytes (34.48 GiB)  (target ≥ 21474836480)
    volume mounts:           0  (target 0)
    transcode quota:         53687091200 bytes  (target 53687091200)
    transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid ≠ parent)
    encoding values:         REPORTED not asserted  (see section 5)
    unreachable:             0  (could-not-look, counted apart from bound violations — VALIDATION row 38)
    toolchain missing:       0
    FAILURES total:          0
  
  ✅ Jellyfin transcode retention intact
  EXIT=0
--------------------------------------------------------------------------------

================================================================================
CONTROL 5 -- THE FOLD-IN PROPAGATES THE EXIT CODE  (row 23, a `fires` row)
================================================================================

WHY THIS TEST HAS THE SHAPE IT HAS
  The cross-AI review's Plan-09 finding: the reviewed version of row 23 forced
  the failure by TEMPORARILY EDITING quick-health-check.sh -- because the
  fold-in's ssh command is hardcoded -- and, unlike the anchor-break test, it
  had NO STATED RESTORE-AND-REVERIFY STEP. A proof that leaves the thing it
  broke broken is a defect it installed, not a control it discharged. The
  revised row forces the failure from the HOST side instead, which needs no edit
  at all. `git diff scripts/quick-health-check.sh` shows no test-related change,
  and the file was never touched during either half of this control.

  The `.bak` was written to /mnt/fast/spike-021/, OUTSIDE the git working tree,
  rather than beside the script. A `.bak` inside scripts/ would make
  `git status --porcelain scripts/` non-empty for the duration -- one deleted
  tracked file plus one untracked file -- and the closing repo-clean assertion is
  the catch-all for a half-finished restore. Do not weaken the catch-all to run
  the test.

PRE-TEST HASH (recorded before anything moved)
  d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5

--------------------------------------------------------------------------------
5a -- THE SCRIPT MOVED ASIDE.  Exit propagates: 1.  BRANCH: not the one the plan
      predicted, and the reason is worth recording.
--------------------------------------------------------------------------------

  mv /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh \
     /mnt/fast/spike-021/check-jellyfin-transcode.sh.bak
  test -e <original path>  ->  GONE

  bash scripts/quick-health-check.sh  ->  EXIT=1

  Jellyfin transcode retention: ❌ BROKEN (check-jellyfin-transcode.sh exit 127)
    Failed assertions:
    Summary:

  ❌ Health check FAILED — see the music freeze harness, the consumers audit and/or the
     Jellyfin transcode retention audit above.

  ROW 23'S ACTUAL CLAIM HOLDS: the remote failure propagates and the shared
  health check exits 1.

  BUT THE PLAN'S STATED MECHANISM IS FACTUALLY WRONG, and it is recorded rather
  than quietly worked around. The plan says: *"The ssh returns non-zero with
  EMPTY OUTPUT, which drives the empty-output UNKNOWN branch."* It does not. The
  fold-in's redirection is `ssh host "bash /path/script 2>&1"` -- the `2>&1` is
  INSIDE the remote command string, so it captures the remote bash's own error:

    $ ssh -n root@172.16.1.159 "bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh 2>&1"
    bash: /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh: No such file or directory
    rc=127

  Output is therefore NON-empty and RC is 127, so the `else` (BROKEN) branch
  fires, not the `-z` branch. The empty-output branch's real triggers are an ssh
  that never runs the command -- whose stderr is the LOCAL ssh's, not captured
  by that redirection -- or a script that genuinely prints nothing.

  This is the same class of error as VALIDATION rows 11, 15 and 16, and it is
  handled the same way: the plan text is NOT edited and the outcome is NOT
  massaged to match it. What is done instead is 5b, below, which drives the
  branch the plan wanted driven, by a mechanism that actually drives it.

  Worth keeping: the BROKEN branch was itself exercised here for the first time,
  including its two empty sub-sections (no `❌` lines and no summary block to
  print, because the script never ran). It degrades correctly -- an unhelpful
  transcript, but an exit code that is right and a header that names the script
  and its exit status.

--------------------------------------------------------------------------------
5b -- A ZERO-BYTE SCRIPT.  This is what actually drives the empty-output UNKNOWN
      branch, and it exposes the branch's most important property.
--------------------------------------------------------------------------------

  : > /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh   (0 bytes)

  bash scripts/quick-health-check.sh  ->  EXIT=1

  Jellyfin transcode retention: ⚠️  UNKNOWN — 172.16.1.159 unreachable or the audit produced no output
    The transcode retention state is unknown, NOT green. Check the host, then re-run:
    ssh root@172.16.1.159 'cd /mnt/fast/stacks && git pull --ff-only && bash scripts/check-jellyfin-transcode.sh'

  THE PROPERTY THIS PROVES, AND IT IS THE WHOLE POINT OF THE BRANCH: a zero-byte
  script EXITS 0. So TRANSCODE_RC was **0** -- a clean success -- and the block
  still refused to print a tick, because `[ -z "$TRANSCODE_OUT" ]` is tested
  FIRST, before the return code is consulted. Absence of a failure signal is not
  evidence of health. Had the order been reversed, a silently-truncated or
  emptied check would have reported green on a successful exit, which is
  precisely the six-weeks-of-silence failure this estate has already paid for.

  A zero-byte file is also the cheapest honest simulation of the condition the
  branch names: "the audit produced no output".

--------------------------------------------------------------------------------
5 -- RESTORE, HASH-VERIFIED
--------------------------------------------------------------------------------

  rm the stub; mv the .bak back into place.

  POST-RESTORE sha256: d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
  PRE-TEST   sha256:   d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
  match: YES
  git -C /mnt/fast/stacks status --porcelain scripts/  ->  EMPTY
  .bak left behind:  NO

  RE-RUN GREEN:
    bash scripts/quick-health-check.sh  ->  EXIT=0
    Jellyfin transcode retention: ✅ Transcode retention intact
      / headroom:              37015347200 bytes (34.47 GiB)  (target ≥ 21474836480)
      volume mounts:           0  (target 0)
      transcode quota:         53687091200 bytes  (target 53687091200)
      transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid ≠ parent)
      unreachable:             0
      FAILURES total:          0

  Four states recorded, as required: pre-test hash, the failing runs with their
  exit codes and their branch output, the restored hash, and the green re-run.

================================================================================
CONTROL 6 -- THE FOLD-IN'S SUMMARY ANCHOR  (row 24, a `fires` row)
================================================================================

THE DEFECT BEING TESTED (WR-09), AND WHY IT HAS BEEN FOUND THREE TIMES NOW
  The fold-in reads its evidence by anchoring on a LITERAL STRING in a different
  file: `sed -n '/^📊 6\. Summary/,$p'`. If that heading is renumbered -- by
  inserting a section 3b, by splitting section 4, by any ordinary edit -- the sed
  yields nothing, the grep selects nothing, and the BRANCH THAT STILL PRINTS A
  TICK RUNS ANYWAY. A green tick with no supporting detail is what the reader
  sees, and there is nothing in the transcript to distinguish it from a healthy
  estate.

  It was found and fixed in the freeze fold-in (WR-09), then again in the
  consumers fold-in (02-09). This is the third, and it is the first one where
  the branch has been driven on purpose rather than reasoned about.

PRE-TEST HASH
  d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5

FIRST LIMB -- with the check passing, the summary lines print (not the UNKNOWN
branch). Already recorded above, at control 5's green re-run and again at the
close of this file.

SECOND LIMB -- the anchor renumbered on the host-side copy:

  line 589 before:  echo "📊 6. Summary"
  line 589 after:   echo "📊 7. Summary"
  sha256 while broken: 253561cfb75aed875459fb6c011e04ff830a11e238d40a3e701a671af94eedc0

  THE UNDERLYING CHECK IS STILL COMPLETELY HEALTHY -- asserted, because it is
  what makes this control meaningful:

    ssh ... 'bash /mnt/fast/stacks/scripts/check-jellyfin-transcode.sh'
    standalone EXIT=0

  So the fold-in is looking at a passing check, on the branch that prints a tick,
  with one string changed. That is the exact condition of the defect.

  bash scripts/quick-health-check.sh  ->  EXIT=1

  Jellyfin transcode retention: ⚠️  UNKNOWN — the audit exited 0 but its '📊 6. Summary' block was not found.
    The section heading this fold-in anchors on has changed, so nothing here was
    actually read. State is UNKNOWN, not green. See check-jellyfin-transcode.sh's summary.

  ❌ Health check FAILED — see the music freeze harness, the consumers audit and/or the
     Jellyfin transcode retention audit above.

  UNKNOWN, not a tick. Exit 1, not 0. The branch is reachable and it fires.

RESTORE, HASH-VERIFIED
  POST-RESTORE sha256: d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
  PRE-TEST   sha256:   d33356c551113fd972c515fc288ef8f00a1180bb8f17119165c81c21971444b5
  match: YES
  git -C /mnt/fast/stacks status --porcelain scripts/  ->  EMPTY

  RE-RUN GREEN:
    bash scripts/quick-health-check.sh  ->  EXIT=0
    Jellyfin transcode retention: ✅ Transcode retention intact
      / headroom:              37014966272 bytes (34.47 GiB)  (target ≥ 21474836480)
      volume mounts:           0  (target 0)
      transcode quota:         53687091200 bytes  (target 53687091200)
      transcode mounted:       zfs=yes devid=47 parent=64519  (target yes, and devid ≠ parent)
      unreachable:             0
      FAILURES total:          0

  Three states plus both hashes, as required.

================================================================================
CLOSING REPO-LEVEL ASSERTION -- NEITHER TEST LEFT ANYTHING BEHIND
================================================================================

  workstation:  git status --porcelain scripts/                      ->  EMPTY
  LXC 100:      git -C /mnt/fast/stacks status --porcelain scripts/  ->  EMPTY
  the two pre-test .sha files under /mnt/fast/spike-021/ were removed after use
  and were never committed.

================================================================================
THREE OF THIS PLAN'S OWN ACCEPTANCE GREPS ARE NON-DISCRIMINATING -- RECORDED AS
PLANNING DEFECTS, NOT MADE TO PASS
================================================================================

  This phase has a standing rule, established by 02.1-06 and re-applied by
  02.1-09: an assertion that fails on a correct outcome is a planning defect, and
  the correct response is to record it, not to edit the estate until the
  assertion passes. Three of task 2's greps are in that class. All three were
  ALREADY failing against the file BEFORE this plan touched it -- measured
  against `git show HEAD:scripts/quick-health-check.sh` at the parent commit:

    grep -c 'bash -s'                        raw 1   (wanted 0)
    grep -c 'x1b'                            raw 2   (wanted 0)
    grep -c 'EXIT-CODE BEHAVIOUR CHANGED'    raw 1   (wanted 2, "the two existing notices")

  WHY THE FIRST TWO FAIL. The prohibitions are themselves written down in the
  file. `quick-health-check.sh:177` explains that the shape to be kept away from
  is `bash -s` plus a heredoc; the ANSI-strip comments explain that `\x1b` is a
  GNU sed extension and macOS needs `$'\033'` instead. A literal grep matches the
  NOTICE EXPLAINING THE PROHIBITION, so a correctly-written file fails its own
  check. This is the identical shape as 02.1-06's ENOSPC-prose defect, the D-32
  watch's block-mode flag spelled in words, and check-jellyfin-transcode.sh's own
  FLOOR comment -- three separate places in this phase already carry the
  workaround of naming a string rather than writing it.

  THE DISCRIMINATING SUBSTITUTE, which proves strictly more than the raw grep
  and is the same device VALIDATION row 34 uses for the prune prohibition
  ("comment-stripped grep over the script -- note 03-01's false positive from
  echoed prose"):

    sed 's/#.*//' scripts/quick-health-check.sh | grep -c 'bash -s'   ->  0
    sed 's/#.*//' scripts/quick-health-check.sh | grep -c 'x1b'       ->  0

  Zero occurrences in EXECUTABLE lines; the raw counts are 1 and 3, every one of
  them prose. The control is discriminating rather than vacuous: the raw and
  stripped counts differ, so the strip is doing work.

  THE THIRD ONE WAS FIXED RATHER THAN RECORDED, and the distinction matters. The
  first two fail because the FILE IS CORRECT. The third failed because the
  file's own naming was INCONSISTENT: notice 2, written by plan 02-09, opened
  with "A SECOND FATAL BLOCK WAS ADDED" and never carried the shared phrase, so
  the file held two such notices and a grep for the convention found one. Making
  the wording consistent is a real improvement to the file, not a fabrication to
  satisfy a check -- six words were added to notice 2 and nothing was removed,
  with an in-band note saying what changed and why. The count is now 4: three
  notice headers plus one occurrence inside that explanatory note. Recorded
  explicitly, because "prose that mentions the string satisfies a grep for the
  string" is the exact trap 02.1-06 documented in the other direction.

  FOURTH, AND SEPARATE: the plan's acceptance criterion names the date
  `2026-09-02` in the third notice. Execution crossed midnight UTC, so the notice
  records `2026-09-03`, which is when the block was actually added, with the
  discrepancy explained in-band. Both dates appear in the file. A notice whose
  entire purpose is to say when something changed should not say the wrong day --
  and plan 02.1-06 already lost a run to this exact UTC rollover.

================================================================================
THE THREE KNOWN LIMITS -- WHERE EACH ONE LANDED, AND WHY IT LANDED THERE
================================================================================

  Both cross-AI reviewers independently made the same point: an operational
  limit recorded where nobody will read it has not been recorded. The three
  limits therefore went to THREE DIFFERENT DESTINATIONS, chosen by WHO NEEDS
  EACH ONE rather than by where it was convenient to write them.

--------------------------------------------------------------------------------
LIMIT 1 -- THE CHECK IS MANUAL (D-22)
DESTINATION: in-band in scripts/quick-health-check.sh's third notice.  ADDED.
--------------------------------------------------------------------------------

  What is recorded: the script only fires when somebody types it -- no cron, no
  timer, no notification path, nothing that will speak at 3am. Scheduling and
  alerting are DELIBERATELY DEFERRED, and the reason is named rather than left
  as a shrug: PHASE 2 CLOSED WITH A MOUNT-FAILURE NOTIFICATION THAT WAS NEVER
  PROVEN TO DELIVER. An unproven notification path is worse than a known-manual
  check because it FEELS covered.

  It also states the thing a reader is most likely to get wrong about this
  phase: the fold-in closes the "we had no way to see it" half of the incident.
  It does NOT close the "nobody looked for 36 hours" half.

  WHY HERE: the person about to trust a green tick from this script is exactly
  the person who needs to know it only ran because they typed it. A limit on an
  instrument belongs on the instrument.

--------------------------------------------------------------------------------
LIMIT 2 -- THE LIVE TV LATENT GAP (A8)
DESTINATION: stacks/selfhosted/media/jellyfin.yaml.  VERIFIED, NOT DUPLICATED.
--------------------------------------------------------------------------------

  Plan 02.1-04 already landed it. This task VERIFIED it rather than writing it
  again, and it is complete and precise as it stands. Checked clause by clause,
  in the comment block above the two cache binds:

    - names LiveStream.TempFilePath as the writer                      PRESENT
    - states /LiveTv/TunerHosts returns [] today, so it is LATENT      PRESENT
    - gives the MECHANISM: those writes are bounded by NEITHER segment
      deletion NOR throttling, because both levers gate on RunTimeTicks
      and a live stream does not have a runtime                        PRESENT
    - states the consequence: if a tuner host is ever added, the ZFS
      quota is the ONLY bound left standing                            PRESENT
    - names the ~48 h DeleteTranscodeFileTask window beneath it        PRESENT
    - tells the reader to re-read the block before adding a tuner      PRESENT

  Nothing was added and nothing was moved. `grep -c 'LiveStream.TempFilePath'`
  is 1 in jellyfin.yaml and 0 in PROJECT.md -- the mechanism detail is NOT
  duplicated into a document where nobody will be standing when they need it.

  WHY THERE: the person who enables Live TV will be editing the stack file. They
  will not be reading a Music Library Consolidation planning document, which is
  what PROJECT.md is.

  ONE THING WAS CORRECTED IN THE SAME BLOCK, and it is a different limit: the
  VOLUME INVENTORY POINTER (D-11) two paragraphs below. It read "the other five
  local Docker volumes ... are inventoried READ-ONLY in 02.1-06-SUMMARY.md".
  Both halves were wrong. Measured after the deletion there are SEVEN plain
  local volumes -- five referenced, two dangling -- and the table with names,
  sizes and owning containers, plus the 36-versus-6 reconciliation, is in
  artifacts/02.1-06-transcode-proof.txt § 7, not in the SUMMARY. A pointer that
  points at the wrong file is worse than no pointer, so it now names both
  documents and the section. The two findings a reader should not have to open
  a file to learn were promoted inline: `docker system df`'s `0B reclaimable` is
  not reassurance (five of seven are REFERENCED and therefore invisible to prune
  however large they grow -- the exact state Jellyfin's volume was in at
  12.55 GiB), and a compose recreate on this daemon MINTS anonymous volumes as a
  side effect, which is how the 02.1-05 recreate left two behind.

--------------------------------------------------------------------------------
LIMIT 3 -- THE WIDENED API-KEY BLAST RADIUS
DESTINATION: PROJECT.md's Key Decisions table.  ADDED.
--------------------------------------------------------------------------------

  What is recorded: the D-40 key at /mnt/fast/secrets/jellyfin-deercrest.env is
  ADMINISTRATOR-EQUIVALENT BY JELLYFIN'S DESIGN, not by misconfiguration --
  Jellyfin.Api/Auth/CustomAuthenticationHandler.cs:53-57 maps any API key to
  UserRoles.Administrator unconditionally, and Jellyfin 10.11 offers no
  scoped-key mechanism. The key already existed and was already read by
  check-music-consumers.sh; THIS PHASE WIDENED ITS USE TO WRITE, against
  /System/Configuration/encoding (plan 02.1-05). A leak of that file now costs
  write access to server configuration rather than read access to metadata.

  Recorded as an accepted residual risk stated as a CHOICE, in the same style as
  02-09's `sec=sys` acceptance -- not as an oversight and not silently. There is
  NOTHING TO IMPLEMENT: a scoped key does not exist in this Jellyfin. What
  changes is the documented cost of a leak, and the compensating controls are
  named and are real (600 root asserted on every sourcing with a refusal to
  source otherwise; the key passed to curl by process substitution so it never
  enters argv; screened out of this PUBLIC repo with a positive control on every
  plan that touches it; no port reachable from outside LXC 100).

  WHY THERE: this one is genuinely ESTATE-WIDE. It is not about the transcode
  path or about this script -- it is about a credential two different checks
  already use and a future security review will ask about. That is what
  PROJECT.md's Key Decisions table is for.

  AND WHY ONLY THIS ONE. PROJECT.md is a Music Library Consolidation document
  already around 240 lines, and both reviewers flagged the reviewed plan for
  pushing all three limits into it. It gained exactly three short entries: this
  risk, a one-line pointer to jellyfin.yaml for the retention controls and the
  Live TV gap, and a one-line pointer to quick-health-check.sh's third notice
  for the manual-only limit. `git diff --stat .planning/PROJECT.md` shows
  3 insertions, 0 deletions -- an addition, not a transplanted section.

--------------------------------------------------------------------------------
WHAT WAS DELIBERATELY NOT IMPLEMENTED
--------------------------------------------------------------------------------

  No cron entry. No systemd unit. No notification code. No new or scoped API
  key. No container recreate -- these are comment and documentation edits with
  no runtime effect (the binds and the settings landed in plans 02.1-04 and
  02.1-05), and recreating a container the house watches television on, for a
  comment edit, would interrupt playback to buy nothing.

  The first two are deferred by D-22. The third does not exist in Jellyfin 10.11.

================================================================================
WHAT THIS PHASE DOES *NOT* COVER -- THE HONEST LIST
================================================================================

  The plan's objective is three things: turn a one-time proof into standing
  coverage, prove the coverage fails closed, AND RECORD WHAT IS NOT COVERED.
  The first two are above. This is the third, and it is deliberately longer than
  the three "known limits" section, because the limits are the ones somebody
  chose and these are the ones that were found.

  Nothing below is fixed here. Each is named with enough detail to act on.

--- A. DEFECTIVE ASSERTIONS IN THIS PHASE'S OWN CONTRACT ------------------------

  These FAIL ON CORRECT OUTCOMES. They were left failing rather than edited,
  because 02.1-10 verifies against the plan text and editing it would have
  destroyed the evidence. They are planning defects, NOT execution failures.

  A1. VALIDATION row 15 -- UNSATISFIABLE AS WRITTEN. It asserts the changed-key
      set EQUALS five names including ThrottleDelaySeconds, but that value was
      already 180, so setting it to 180 changed nothing. 02.1-05 substituted a
      subset check plus a value assertion, which proves strictly MORE: a key-set
      comparison is silent on values and would pass SegmentKeepSeconds changed
      to 999.

  A2. VALIDATION row 11 -- ERRNO PAIR INVERTED. It requires the ENOSPC phrase
      present and the quota-exceeded phrase absent. Measured: 0 and 2, on all
      ten attempts. An OpenZFS dataset `quota` refusal surfaces as EDQUOT;
      ENOSPC is a genuinely full POOL. Left failing by 02.1-06.

  A3. VALIDATION row 16 -- CEILING OMITS THE FORWARD BUFFER. It counts only the
      SegmentKeepSeconds retention window and ignores the ThrottleDelaySeconds
      region ahead of the download head that the configuration deliberately
      maintains, so a working cleaner cannot satisfy it. Left failing by
      02.1-06.

  A4. 02.1-06's own task-3 <verify> block -- cannot pass, for A2's reason.
      Deliberately not made to pass; inserting the string would have fabricated
      a firing that did not happen.

  A5. VALIDATION row 33 -- NEAR-VACUOUS, and 02.1-09 recorded it as such: `/`
      already cleared the 20 GiB floor by 13.11 GiB before the reap, so the
      assertion would have passed had the prune deleted nothing. Row 34 was
      re-asserted with a discriminating control instead (comment-stripped 0,
      raw 4).

  A6. THREE OF THIS PLAN'S OWN TASK-2 GREPS -- see the section above. Two are
      non-discriminating (they match the prohibition notices that explain why
      not to use the thing) and now carry a comment-stripped substitute; the
      third named a date that execution crossed midnight past.

  THE PATTERN, WHICH IS THE USEFUL PART: five of these six are the SAME SHAPE --
  a mechanical check whose text is satisfied or defeated by PROSE ABOUT the
  thing rather than by the thing. This phase now carries the workaround in four
  separate places (the D-32 watch's block-mode flag, check-jellyfin-transcode's
  FLOOR comment, 02.1-06's ENOSPC artifact, and the comment-stripped greps
  here). If a future phase writes a mechanical assertion over a file that also
  DOCUMENTS the thing being asserted, strip comments first or name the string
  rather than writing it.

--- B. FACTUAL ERRORS IN THE PLAN TEXT, FOUND AND NOT EDITED -------------------

  B1. The research's errno mapping is wrong (A2 above), AND THE PLAN'S INFERENCE
      FROM IT IS ALSO WRONG. The plan said an EDQUOT form would mean `refquota`
      is in force and 02.1-03 picked the wrong property. Measured at the moment
      of refusal: refquota = 0/default, quota = 53687091200/local, no ancestor
      carries either. **02.1-03 chose the RIGHT property.**

  B2. The claim that `docker image ls -q` dedupes by ID is INVERTED. Measured:
      `-q` tracks TAGS (89 raw == 89 tags; `sort -u` gives 86), so it
      OVER-reports when a tag is removed.

  B3. 02.1-08: the `image:` literal is at jellyfin.yaml:76, not :59, and
      renovate.json5 had 38 packageRules, not 40. Every INDEX the plan relies on
      is nonetheless correct.

  B4. 02.1-07: the plan's two named disposable images (alpine:3,
      curlimages/curl:latest) were not resident at all -- 03-01's reap had
      already taken them.

  B5. This plan (02.1-10): the row-23 mechanism. Documented in full in the
      control 5 section above.

--- C. REAL, OPEN, AND WORTH DOING -------------------------------------------

  C1. THE ESTATE'S ENOSPC-KEYED LOG GREPS ARE BLIND TO A TRANSCODE-QUOTA
      REFUSAL. Worse than the plan described: BOTH `quota` and `refquota` return
      EDQUOT, so no choice of ZFS property produces a match. Switching property
      would not fix it. Adding the quota-exceeded string to those greps would.

  C2. renovate.json5 IS UNVALIDATED. `renovate-config-validator` is not
      installed and installing it was out of scope. 02.1-08 proved the file
      PARSES and the new rule's SHAPE is right -- syntax and shape, not semantic
      validity. In THIS repo a single bad key has silently jammed the entire
      Renovate run while everything looked idle.

  C3. check-renovate.sh HAS THREE LATENT `set -e` ABORTS at lines 115, 138 and
      157, and LINE 157 IS INVERTED -- it aborts when there are ZERO pending
      Renovate PRs, i.e. exactly when the estate is healthy, which also makes
      its own "No pending Renovate PRs" branch unreachable. Hidden today only
      because 27 branches are open. Remedy is `|| true` on each -- three tokens.

  C4. THE JELLYFIN RULE ADDED BY 02.1-08 HAS NEVER BEEN OBSERVED REFUSING TO
      AUTOMERGE ANYTHING. There is no renovate/jellyfin-* branch among the 27
      open ones, so that half is discharged BY CONSTRUCTION rather than by a
      firing observation. Under CONS-04 that distinction is the whole point. The
      observation becomes available when upstream next ships a Jellyfin minor,
      and it cannot be manufactured.

  C5. `do_inventory` in the image-headroom script ENUMERATES WITHOUT `-a`,
      hiding nine untagged images. Three are unreferenced (~1.02 GiB) and six
      are load-bearing. Fix is to enumerate with `-a` and re-run the gate.

  C6. TWO DANGLING 0-BYTE ANONYMOUS VOLUMES remain from the 02.1-05 recreate
      (c479d815…, efbcf4d6…). Inventory-only under D-11; nobody has ruled on
      deleting them. Now named in jellyfin.yaml as well as in the artifacts.

  C7. NINE DANGLING IMAGES remain after 02.1-09's reap. In scope by design --
      the operator ruled on four rows and scope was not expanded past them.

--- D. INSTRUMENT TRAPS THIS PHASE PAID FOR, CARRY THEM FORWARD ---------------

  D1. `docker system df` RECLAIMABLE HAS NOW MISLED IN BOTH DIRECTIONS. It ROSE
      0.57 GB during 03-01's real 18 GiB reclaim and FELL only 0.21 GB during
      02.1-09's 1.45 GiB one. It is context, never a measure and never a target.

  D2. `docker run --rm <img>` IS A PULL WHEN THE IMAGE IS ABSENT. 02.1-09
      tripped this on a freshly reaped host and had to revert. The generic
      utility images are exactly the ones most likely to be gone.

  D3. A LOG-READING INSTRUMENT MUST ASSERT LOG-LEVEL LIVENESS AT GREP TIME.
      02.1-06 nearly committed a confident false negative -- with a plausible
      mechanism already drafted -- because `set +e` inside an ERR trap handler is
      GLOBAL, not function-local, so the script ran on and grepped a log that had
      been silently reset to a lower level. "Zero matches" and "the control did
      not fire" were the same output.

      DOES THE STANDING COVERAGE ADDED BY THIS PLAN INHERIT THAT HAZARD?
      Asserted, not assumed: NO. Neither check-jellyfin-transcode.sh nor the
      fold-in reads a log file. The check's evidence is `df`, `docker inspect`,
      `stat -c %d`, `zfs get` over ssh and one HTTP GET -- every one of them a
      direct query whose failure is distinguishable from an empty result, which
      is why sections 0-5 all have explicit UNKNOWN paths. The fold-in reads the
      check's own stdout and treats BOTH "nothing came back" and "the anchor is
      missing" as UNKNOWN rather than green, and both branches were driven on
      purpose above. The hazard is recorded here anyway, because the NEXT thing
      folded in may well read a log, and this is the third time this estate has
      paid for the same shape.

--- E. WHAT REAL PLAYBACK DID AND DID NOT PROVE -------------------------------

  The operator watched a full episode at roughly 22:20-22:40Z on 2026-09-02,
  AFTER the 22:19:52Z cutover. It DIRECT-PLAYED: zero transcode segments, zero
  ffmpeg, zero bytes written to `/`.

  WHAT THAT IS: real evidence that the cutover does not break real playback.
  WHAT IT IS NOT: any transcode evidence at all. An empty /mnt/fast/transcode
  after a direct-play session is the ABSENCE OF A TEST, not a passing one. The
  firing proofs come from 02.1-06's two DRIVEN sessions, which force a transcode
  by sending a PlaybackInfo device profile with an empty DirectPlayProfiles list
  precisely so the session above cannot be repeated by accident.

  Recorded because the same directory produced the mirror-image wrong conclusion
  in 02.1-04 (an empty /mnt/fast/transcode read as "the copy never started" when
  it had in fact completed). Same path, opposite wrong reading, twice in one
  phase.
