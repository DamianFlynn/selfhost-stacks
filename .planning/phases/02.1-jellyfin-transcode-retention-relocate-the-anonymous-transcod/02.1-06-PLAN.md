---
phase: 02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod
plan: 06
type: execute
wave: 2
depends_on: ["02.1-03", "02.1-05"]
files_modified:
  - stacks/selfhosted/media/jellyfin.yaml
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-06-cache-census.txt
autonomous: true
requirements: [TRAN-01]

must_haves:
  truths:
    - "The anonymous volume's full id is recorded in a durable file on /mnt/fast before anything can make it unobtainable"
    - "The 392 MB of image and xmltv cache exists at /mnt/fast/appdata/media/jellyfin/cache with the same file count and byte total it had inside the anonymous volume"
    - "jellyfin.yaml declares /cache and /cache/transcodes as binds, no longer declares /dev/shm:/data/transcode, and explains all of it in the file where the next reader will look"
    - "The stale comment block claiming hardware acceleration is VAAPI no longer contradicts the live HardwareAccelerationType: none"
    - "/mnt/fast/stacks on LXC 100 carries the edited compose file, at the same SHA as origin/main"
  artifacts:
    - path: "stacks/selfhosted/media/jellyfin.yaml"
      provides: "the two bind mounts, the deleted /dev/shm mount, and the D-21 comment block"
      contains: "/mnt/fast/transcode"
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-06-cache-census.txt"
      provides: "pre-copy and post-copy file count and byte total, the rsync probe result, and the captured volume id"
      contains: "OLD_VOL"
  key_links:
    - from: "stacks/selfhosted/media/jellyfin.yaml volumes:"
      to: "/mnt/fast/transcode (the fast/transcode ZFS dataset, quota 50G)"
      via: "bind mount to /cache/transcodes, nested on top of the /cache bind"
      pattern: "/mnt/fast/transcode:/cache/transcodes"
    - from: "stacks/selfhosted/media/jellyfin.yaml volumes:"
      to: "/mnt/fast/appdata/media/jellyfin/cache (a plain dir inside the already-mounted fast/appdata/media dataset)"
      via: "bind mount to /cache"
      pattern: "/mnt/fast/appdata/media/jellyfin/cache:/cache"
---

<objective>
Move the bytes and change the declaration — everything up to, but not including, the recreate.

The `jellyfin/jellyfin` image declares `VOLUME /cache`. It is unbound, so Docker put it on `/` as an
anonymous volume, `d98b2ff94482…`. STATE.md names *"the volume being anonymous rather than a declared
bind mount"* as the root cause of `/` reaching zero bytes. That volume holds 392 MiB today: `images`
370 M (the artwork cache), `xmltv` 16 M (the TV guide), shader caches ~4 M, and `transcodes` 472 K —
but it held **19 GB** on 2026-09-01, from a single unbounded stream-copy HLS remux.

D-08 binds **all** of `/cache`, not only the transcodes path, because binding only `transcodes` would
leave the named root cause standing and would leave the 370 MB image cache — the other, slower grower
— on `/`. D-09 points `/cache` at `/mnt/fast/appdata/media/jellyfin/cache`, a **plain directory**
inside the already-mounted `fast/appdata/media` dataset; that is decisive rather than aesthetic,
because `infra/lxc-selfhost.tf:163-167` requires every child ZFS dataset to have its own explicit
`mount_point`, so a new dataset would mean editing the LXC resource that carries `prevent_destroy`
and `ignore_changes = [mount_point]`. D-01/D-03 then nest `/cache/transcodes` on top from
`/mnt/fast/transcode`, which plan 03 has already given `quota=50G`.

D-10 copies the 392 MB first — preserving the artwork cache (no burst of re-fetching on a server the
house watches) and the xmltv cache (no blank TV guide on first load) — and deletes the anonymous
volume only **after** Jellyfin is proven running on the new path. That deletion is plan 08's task,
two plans away, and the ordering is not negotiable.

D-02 deletes `/dev/shm:/data/transcode` and its comment together. `/dev/shm` holds **0 bytes** and
always has, because `TranscodingTempPath` was never set to `/data/transcode`. Leaving it is a decoy:
it reads as "already solved", which is plausibly part of why 19 GB accumulated unnoticed.

Purpose: this is the structural fix. Everything else in the phase bounds or detects; this is the part
that moves the write target off `/`.
Output: the cache copied and censused, `jellyfin.yaml` edited and documented, the volume id captured,
and the host checkout carrying it all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-CONTEXT.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-PATTERNS.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-RESEARCH.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the anonymous volume's id, census the cache, probe the copy, then copy it</name>
  <files>.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-06-cache-census.txt</files>
  <read_first>
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pitfall 5* — the anonymous volume's name is
      unobtainable after the recreate, because `docker inspect jellyfin` is the only reliable source
      and the recreate removes the container object that references it. The capture command and the
      "must be exactly 1 before, 0 after" assertion are given there.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Copy safety — `rsync -a` and the NFSv4-ACL
      question* (lines 918-955) — the measured ACL properties of `fast`, `fast/appdata/media` and
      `fast/transcode`, why the `tank` failure mode probably does not apply here, and the three-step
      recommendation: probe, fall back, assert by count not by rsync's summary.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pitfall 3* — `CreateAndCheckMarker` scans
      `.jellyfin-*` **recursively** under the transcode path and throws on any foreign marker. The
      copy targets the **cache** directory, not the transcode dataset; do not conflate them.
    - `CLAUDE.md` § *Constraints* — the documented `rsync -a` failure on `tank`
      (`mkstemp … Operation not permitted`, exit 23 while printing success-looking stats) and the
      estate-safe fallback form `rsync -rlt --no-p --no-o --no-g`.
    - `.planning/phases/02.1-…/02.1-VALIDATION.md` row **6** — the copy-integrity assertion.
  </read_first>
  <action>
    Capture the anonymous volume's full id **first**, before anything else. Run
    `docker inspect jellyfin --format '{{range .Mounts}}{{if eq .Type "volume"}}{{.Name}}{{"\n"}}{{end}}{{end}}'`
    on LXC 100, assert the result is **exactly one** non-empty line, and write that id to
    `/mnt/fast/spike-021/old-vol.txt` — a durable path on `/mnt/fast`, never `/tmp`, which is tmpfs on
    this host and once took the whole 28 GB box down with 1.1 GB of EPG XML. `docker inspect` on the
    container is the only reliable source and the recreate in plan 07 removes the object that carries
    it; after the recreate the volume becomes dangling, but so could others, and D-10 says delete
    **by name**. Mirror the id into the census artifact so it also lives in git.

    Take the pre-copy census from inside the volume's `_data` directory at
    `/var/lib/docker/volumes/<id>/_data`. Record, separately for the whole tree and for the `images`
    subtree: the file count and the total byte size, using a single `find … -printf` form so the
    identical command can be run on the destination afterwards. Also record `du -sb` on both for a
    second instrument. Record the directory listing one level deep so the `xmltv`, shader-cache and
    `transcodes` entries are named.

    Probe the copy before doing it (assumption A5). `fast/appdata/media` is `acltype=nfsv4` but
    `aclmode=passthrough`, not `restricted` — and it is `restricted` that produces the `EPERM`
    breaking `rsync -a` on `tank`. So it probably works here. Do not rely on "probably": the estate
    has been burned by exactly this shape. `rsync -a` **one file** from the volume into a scratch
    directory under `/mnt/fast/appdata/media/jellyfin/`, then check **both** the exit status **and**
    that the file arrived with the right size. If it fails, fall back to the documented estate-safe
    form `rsync -rlt --no-p --no-o --no-g`, or plain `cp -R`. Record which form was used and why.

    Create `/mnt/fast/appdata/media/jellyfin/cache` and copy the volume's `_data` contents into it.
    Ownership is explicitly irrelevant here by D-04's reasoning: Jellyfin runs as `uid=0(root)` (the
    `user:` line in `jellyfin.yaml:64` is commented out and `PUID`/`PGID` are an LSIO convention the
    official image ignores), it has `CAP_DAC_OVERRIDE` inside the userns, and every file here is
    regenerable with a single reader. Do not "fix" ownership; Phase 1's `568:568` reasoning does not
    apply to a scratch cache.

    Assert the copy by **file count and byte total**, running the identical `find … -printf` on the
    destination and comparing against the recorded source figures — not by rsync's summary.
    `rsync`'s stats can look like success while it exits 23; that is written down in CLAUDE.md and
    03-01 hit the same lesson. The `images` subtree count and byte total must match exactly
    (VALIDATION.md row 6).

    Do **not** copy anything into `/mnt/fast/transcode`. That dataset is empty and must stay empty
    until Jellyfin writes to it: `recordsize=1M` applies to newly written blocks only, and a stray
    `.jellyfin-transcode` marker under the wrong path is a Jellyfin start-refusal hazard.

    Do not delete the anonymous volume. Do not restart or recreate the container.
  </action>
  <verify>
    <automated>ssh -n -o BatchMode=yes root@172.16.1.159 'test -s /mnt/fast/spike-021/old-vol.txt && OLD=$(cat /mnt/fast/spike-021/old-vol.txt) && SRC=$(find /var/lib/docker/volumes/$OLD/_data/images -type f -printf "%s\n" | wc -l) && DST=$(find /mnt/fast/appdata/media/jellyfin/cache/images -type f -printf "%s\n" | wc -l) && test "$SRC" = "$DST" && test "$SRC" -gt 0' && grep -q 'OLD_VOL' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-06-cache-census.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/mnt/fast/spike-021/old-vol.txt` exists on LXC 100 and contains exactly one line: a 64-character volume id beginning `d98b2ff9`. The census artifact records that the pre-recreate `Type: volume` mount count was exactly **1**.
    - The census artifact records, for the source `_data` tree and its `images` subtree: file count, total bytes from `find … -printf`, and `du -sb` — all taken **before** the copy.
    - The `rsync -a` probe result is recorded with both its exit status and the arrived file's size, and the artifact states which copy form was used for the real copy and why (assumption A5 CONFIRMED or FALSIFIED).
    - Post-copy, the identical `find … -printf` on `/mnt/fast/appdata/media/jellyfin/cache/images` returns the **same file count and the same byte total** as the source (VALIDATION.md row 6). Both figures appear in the artifact side by side.
    - The whole-tree file count and byte total also match, or every difference is enumerated by path and explained.
    - `/mnt/fast/transcode` is still empty: `ssh root@172.16.1.159 'find /mnt/fast/transcode -mindepth 1 | wc -l'` returns **0**.
    - The anonymous volume still exists: `docker volume ls -q | grep -qx "$(cat /mnt/fast/spike-021/old-vol.txt)"` exits **0**. Nothing was deleted in this task.
    - Nothing was written under `/tmp` on LXC 100.
  </acceptance_criteria>
  <done>The volume id is captured to a durable path, the 392 MB is copied to `/mnt/fast/appdata/media/jellyfin/cache`, and the copy is proven by matching file counts and byte totals rather than by rsync's summary.</done>
</task>

<task type="auto">
  <name>Task 2: Edit jellyfin.yaml — add the two binds, delete the /dev/shm mount, and document all of it</name>
  <files>stacks/selfhosted/media/jellyfin.yaml</files>
  <read_first>
    - `stacks/selfhosted/media/jellyfin.yaml` — the whole file. In particular lines **34-55** (the
      pre-existing comment block asserting "Hardware acceleration: VAAPI", which contradicts the live
      `HardwareAccelerationType: none`), line **59** (`image: jellyfin/jellyfin:10.11.11`), line
      **64** (the commented-out `user:` line), lines **80-81** (`PUID`/`PGID`, present but inert on
      the official image), lines **90-143** (the volume block), **93-140** (the Phase 1 comment block
      D-21 explicitly names as the pattern to follow), and **142-143** (the dead `/dev/shm` mount and
      its comment, to be deleted together).
    - `.planning/phases/02.1-…/02.1-PATTERNS.md` § *`stacks/selfhosted/media/jellyfin.yaml`* — the
      93-140 block quoted verbatim and its **ten-part structure** enumerated. Follow that structure;
      it is the estate's worked example of "applied by API, explained in compose".
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pattern 2* — moby sorts container mounts by the
      **number of path segments** in the destination, not by declaration order and not by path
      length, so `/cache` (1) always mounts before `/cache/transcodes` (2). Declaration order here is
      cosmetic; say so in the comment so nobody "fixes" it.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Runtime State Inventory* — the rows on
      `PUID`/`PGID` being inert, on Live TV (`/LiveTv/TunerHosts` returns `[]`) being a latent gap,
      and on the `.bak` proving the 2026-08-31 VAAPI change.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Open Questions* item 3 — the `/config/cache`
      aliasing consequence of D-09, to be recorded as a known property.
  </read_first>
  <action>
    Delete lines 142-143 **together** — the `/dev/shm:/data/transcode` mount and the
    "Transcode to RAM for better performance" comment above it (D-02). `/dev/shm` is sized 15 G
    against 24 Gi total RAM with only ~4.7 Gi actually available, so a 19 GB cache there would be an
    OOM rather than a full disk, and the estate has a documented incident where 1.1 GB in tmpfs took
    the whole box down. It has held 0 bytes since it was added, because `TranscodingTempPath` was
    never set to `/data/transcode`. Deleting the code and leaving the comment would preserve the
    decoy; delete both.

    Add two bind mounts after the existing `/mnt/tank/media:/media:rw` line:
    `/mnt/fast/appdata/media/jellyfin/cache:/cache:rw` and
    `/mnt/fast/transcode:/cache/transcodes:rw`. Write them in that reader-expected order and state in
    the comment that the order is cosmetic — Docker sorts by destination segment count, so `/cache`
    (1 segment) always mounts before `/cache/transcodes` (2), which is exactly what `sortMounts`
    exists for. Note the correction a reader is likely to get wrong: the sort is on the **number of
    separators**, not on path length.

    Add a second comment block, immediately above the new mounts, in the **same ten-part shape** as
    lines 93-140:

    1. A `NOTE (2026-09-02):` sentence stating the invariant — every byte Jellyfin caches lands on a
       declared bind to `/mnt/fast`, never an anonymous Docker volume on `/`.
    2. What it looked like before and why: the image declares `VOLUME /cache`, it was unbound, Docker
       put it on `/` as anonymous volume `d98b2ff9…`, and on 2026-09-01 that volume held **19 GB**
       from one unbounded stream-copy HLS remux of an 18 GB / 17.93 Mbit/s Bluray — 5,131 files, 764
       of them zero-byte, the ENOSPC signature — while `/` went from 2.2 GiB free to zero.
    3. `Classification:` the term the assertion script uses, so the compose file and
       `scripts/check-jellyfin-transcode.sh` agree — the invariant asserted is **zero `Type: volume`
       mounts** (D-18), not the absence of one particular id.
    4. `Why:` where the control actually lives, with date, plan number and APPLIED/PERMANENT status.
       The hard bound is a ZFS property on `fast/transcode` (`quota=50G`, declared in
       `infra/jellyfin-transcode-dataset.tf`) — it cannot be flipped from a UI and survives a
       container recreate. The graceful layer is Jellyfin's own retention settings, applied by REST.
    5. The numbered list of the exact settings, `old -> new`, each tagged with its decision id:
       `TranscodingTempPath` absent -> `/cache/transcodes` (D-03); `EnableSegmentDeletion` false ->
       true (D-14); `SegmentKeepSeconds` 720 -> 300 (discretion, sized against the measured
       17.93 Mbit/s stream-copy bitrate); `EnableThrottling` false -> true (D-14);
       `ThrottleDelaySeconds` 180 -> 180, unchanged and deliberately so (3× the silent clamp floor of
       60).
    6. The one that matters more than it looks: `EnableSegmentDeletion` does more than delete. On a
       stream-copy HLS job — exactly this estate's case — it also adds `-readrate 10` to ffmpeg's
       input, capping the remux at 10× realtime. That is a stronger bound on "an abandoned stream
       writes an entire film in minutes" than throttling is, and it is delivered by the *deletion*
       switch.
    7. Deliberately **UNCHANGED**: `HardwareAccelerationType: none` and `EnableHardwareEncoding:
       false`. These are the amdgpu HMM mitigation applied **2026-08-31 18:27** by
       `scripts/disable-jellyfin-hwaccel.sh`, not drift. The encoding endpoint is a full-object
       replace and `EnableHardwareEncoding`'s constructor default is **`true`**, so this phase writes
       both back deliberately and asserts them after the write (D-30).
    8. Deliberately left alone rather than changed blind: `PUID` / `PGID` / `UMASK_SET` at lines
       80-81 are present but **inert** on the official `jellyfin/jellyfin` image — they are an LSIO
       convention. Jellyfin runs as `uid=0(root)`, so transcode files land `0:568` (host uid 100000,
       an unmapped id). That is expected, not the drift Phase 1 spent 2,674 entries correcting
       (D-04). Setting `user: 568:568` was rejected: it would change `/config` and `/media` access
       and the `video`/`render` group membership for VAAPI — an enormous blast radius for a scratch
       directory.
    9. `Why not /dev/shm:` sized 15 G against 24 Gi RAM with ~4.7 Gi available, so a large cache
       there is an OOM rather than a full disk; and the bind that was there had never been written
       to, which made it read as "already solved".
    10. `What breaks if someone reverts this:` Docker re-creates an anonymous volume for `/cache` on
        `/`, and a single abandoned stream-copy remux of a Bluray-class title refills it at
        2.24 MB/s — 19 GB in ~36 hours with no `ffmpeg` still running at the end, which is what
        happened.

    Record two known properties inside that block rather than leaving them to be discovered: the
    D-09 **aliasing** — `/mnt/fast/appdata/media/jellyfin/cache` is visible at both `/config/cache`
    and `/cache`, which is harmless in 10.11 because `CreateAndCheckMarker(ProgramDataPath, "data")`
    on `/config` is non-recursive, but a future change to Jellyfin's backup scope over `/config`
    would sweep in a live transcode directory. And the **Live TV latent gap** (A8):
    `/LiveTv/TunerHosts` returns `[]` today, so `LiveStream.TempFilePath` — which writes live-TV
    buffers into the transcode path bounded by **neither** segment deletion nor throttling, since
    both require `RunTimeTicks` — is latent rather than live. If a tuner host is ever added, the ZFS
    quota is the only bound left.

    Separately, correct the **stale** comment block at lines 34-55. It still asserts "Hardware
    acceleration: VAAPI", which contradicts the live `HardwareAccelerationType: none` set on
    2026-08-31. D-30 says the reason is written down; correcting this is in scope. Rewrite it to
    state the current reality, its date, the script that set it, and that re-enabling VAAPI is
    deliberately **out of scope** for this phase — orphaned VAAPI `ffmpeg` is a confirmed contributor
    to the 2026-08-31 oops and belongs to its own investigation. Do not delete the VAAPI device
    passthrough or the `video`/`render` group configuration; only the prose is wrong.

    Do not change the `image:` tag at line 59. Do not change the `/config` or `/media` mounts. Do not
    uncomment the `user:` line.
  </action>
  <verify>
    <automated>docker compose -f stacks/selfhosted/media/compose.yaml config >/dev/null && grep -q '/mnt/fast/appdata/media/jellyfin/cache:/cache' stacks/selfhosted/media/jellyfin.yaml && grep -q '/mnt/fast/transcode:/cache/transcodes' stacks/selfhosted/media/jellyfin.yaml && test "$(grep -c '/dev/shm' stacks/selfhosted/media/jellyfin.yaml)" -eq 0 && grep -q 'HardwareAccelerationType' stacks/selfhosted/media/jellyfin.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f stacks/selfhosted/media/compose.yaml config` exits 0 — the file still parses as part of the `media` project.
    - `grep -c '/dev/shm' stacks/selfhosted/media/jellyfin.yaml` returns **0** — both the mount and its comment are gone (D-02).
    - `grep -c '/data/transcode' stacks/selfhosted/media/jellyfin.yaml` returns **0**.
    - The volumes list contains exactly one line matching `/mnt/fast/appdata/media/jellyfin/cache:/cache` and exactly one matching `/mnt/fast/transcode:/cache/transcodes`.
    - The new comment block contains all ten parts. Verifiable anchors: it names `d98b2ff9`, the figure `19 GB`, `zero Type: volume` or `Type: volume`, `quota=50G`, all five setting names (`TranscodingTempPath`, `EnableSegmentDeletion`, `SegmentKeepSeconds`, `EnableThrottling`, `ThrottleDelaySeconds`), `-readrate 10`, `HardwareAccelerationType`, `EnableHardwareEncoding`, `2026-08-31`, `PUID`, `0:568`, and a "what breaks if someone reverts this" sentence containing a number.
    - The block records the D-09 aliasing (`/config/cache` and `/cache` name the same host directory) and the A8 Live TV latent gap (`LiveStream.TempFilePath`, bounded by neither lever).
    - The lines 34-55 block no longer asserts that hardware acceleration is VAAPI: `grep -c 'Hardware acceleration: VAAPI' stacks/selfhosted/media/jellyfin.yaml` returns **0**, and the replacement text names `none`, the date `2026-08-31` and `disable-jellyfin-hwaccel.sh`.
    - `git diff stacks/selfhosted/media/jellyfin.yaml` shows the `image:` line, the `/config` mount, the `/media` mount and the commented-out `user:` line all unchanged.
    - No credential appears in the diff: `git diff --cached stacks/selfhosted/media/jellyfin.yaml | grep -cEi '(token|api_key|password|secret)[[:space:]]*[=:]'` returns 0.
  </acceptance_criteria>
  <done>`jellyfin.yaml` declares both binds, no longer declares `/dev/shm:/data/transcode`, carries a ten-part comment block explaining the change where the next reader will look, and no longer contains a stale claim that hardware acceleration is VAAPI.</done>
</task>

<task type="auto">
  <name>Task 3: Push to origin and fast-forward the host checkout</name>
  <files>stacks/selfhosted/media/jellyfin.yaml</files>
  <read_first>
    - `.planning/phases/02.1-…/02.1-CONTEXT.md` § D-26 — push to `origin`, then `git pull --ff-only`
      at `/mnt/fast/stacks`; the path proven in Phase 2 after the `scp` workaround was retired, and
      an **explicit step** rather than an assumption.
    - `.planning/phases/02.1-…/02.1-02-SUMMARY.md` — plan 02 already brought the host from 4 commits
      behind to level with `origin/main`; this task keeps it there.
    - `stacks/selfhosted/media/jellyfin.yaml` — the edited file, so what lands on the host is known.
  </read_first>
  <action>
    Commit the compose edit and the census artifact, then `git push origin main`.

    Deliver to the host with a single inline command:
    `ssh -n root@172.16.1.159 'cd /mnt/fast/stacks && git pull --ff-only'`. Inline `ssh host 'cmd'`
    only — never `bash -s` with a heredoc, which `-n` silently discards (the 02-05 finding).

    Then assert, do not assume: the host's `git rev-parse HEAD` must equal the workstation's
    `git rev-parse origin/main`, and `git -C /mnt/fast/stacks show HEAD:stacks/selfhosted/media/jellyfin.yaml`
    must contain both new bind lines and no `/dev/shm`. This estate has a documented failure where
    merged Renovate PRs never touched the host while `docker ps` disagreed with git; a `git pull` that
    printed output is not evidence.

    Do **not** run `docker compose up` in this task. The recreate is plan 07's first act, and it is
    ordered after this delivery deliberately — the file must be on the host before the container is
    rebuilt from it.
  </action>
  <verify>
    <automated>test "$(git rev-parse origin/main)" = "$(ssh -n -o BatchMode=yes root@172.16.1.159 'cd /mnt/fast/stacks && git rev-parse HEAD')" && ssh -n -o BatchMode=yes root@172.16.1.159 "grep -q '/mnt/fast/transcode:/cache/transcodes' /mnt/fast/stacks/stacks/selfhosted/media/jellyfin.yaml && test \$(grep -c '/dev/shm' /mnt/fast/stacks/stacks/selfhosted/media/jellyfin.yaml) -eq 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse origin/main` on the workstation equals `git rev-parse HEAD` at `/mnt/fast/stacks` on LXC 100.
    - On the host, `stacks/selfhosted/media/jellyfin.yaml` contains both `/mnt/fast/appdata/media/jellyfin/cache:/cache` and `/mnt/fast/transcode:/cache/transcodes`, and `grep -c '/dev/shm'` on it returns **0**.
    - `ssh root@172.16.1.159 'cd /mnt/fast/stacks && git status --porcelain'` shows no modified tracked files — only the two known untracked files (`prometheus.yaml.bak`, `monitoring.app.yaml.disabled`), which do not conflict with a fast-forward.
    - The **running** container has not been recreated: `docker inspect jellyfin --format '{{.State.StartedAt}}'` is unchanged from before this task, and `docker inspect jellyfin --format '{{range .Mounts}}{{if eq .Type "volume"}}x{{end}}{{end}}'` still returns a non-empty string. The compose file changed; the container has not.
  </acceptance_criteria>
  <done>The host checkout is at `origin/main` and carries the edited `jellyfin.yaml`, with the running container deliberately untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous volume → new bind target | 392 MB of regenerable cache crosses filesystems; a partial copy that reports success is the documented `rsync` failure mode on this estate |
| compose file in git → running container | The file on the host is what the recreate builds from; a push that never reached the host is a documented estate failure |
| ZFS ACL semantics → copy tooling | `fast/appdata/media` is `acltype=nfsv4`; `aclmode` differs from the dataset where `rsync -a` is known to fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-28 | Repudiation | `rsync` exiting 23 while printing success-looking stats | mitigate | A two-file probe first (checking exit status **and** arrived size), then the real copy, then assertion by `find … -printf` **file count and byte total** on both sides — never by rsync's summary. The estate-safe fallback `rsync -rlt --no-p --no-o --no-g` is pre-specified |
| T-021-29 | **Denial of Service** | Losing the anonymous volume's id before it can be deleted by name | mitigate | The id is captured to `/mnt/fast/spike-021/old-vol.txt` **before** anything can make it unobtainable, and asserted to be exactly one. `docker volume prune` is rejected outright — after the recreate it would also be free to remove anything else that became dangling |
| T-021-30 | Denial of Service | Writing the census or the id to `/tmp` on LXC 100 | mitigate | `/tmp` there is tmpfs against ~4.7 GiB available RAM. All artifacts land on `/mnt/fast/` or under `.planning/` |
| T-021-31 | Denial of Service | Jellyfin refusing to start on a foreign `.jellyfin-*` marker | mitigate | `CreateAndCheckMarker` scans recursively under the transcode path and throws on any other marker. The copy targets the **cache** directory only; `/mnt/fast/transcode` is asserted still empty at the end of task 1. The rollback table carries the recovery command |
| T-021-32 | Tampering | A compose change that never reached the host | mitigate | Task 3 asserts SHA equality **and** greps the delivered file's content on the host, rather than trusting `git pull`'s output |
| T-021-33 | Information Disclosure | `jellyfin.yaml` and the census committed to a public repo | mitigate | Neither carries a credential — the Jellyfin API key lives at `/mnt/fast/secrets/jellyfin-deercrest.env`, mode 0600 root, and is referenced by variable name only. Screen the diff: `grep -cEi '(token\|api_key\|password\|secret)[[:space:]]*[=:]'` returns 0 |
| T-021-SC | Tampering | npm/pip/cargo installs | mitigate | **No package install occurs in this plan.** No image is pulled — the `image:` tag at `jellyfin.yaml:59` is unchanged at `10.11.11`, which `docker inspect` already confirms is what is running (no deploy drift) |
</threat_model>

<verification>
- VALIDATION.md row 6: `images` file count and byte total match between source and destination, from the identical `find … -printf` on both.
- `/mnt/fast/spike-021/old-vol.txt` holds exactly one volume id; the pre-recreate `Type: volume` count was exactly 1.
- `docker compose -f stacks/selfhosted/media/compose.yaml config` exits 0.
- Host SHA equals `origin/main` and the delivered file carries both binds and no `/dev/shm`.
- The running container is unchanged — the recreate belongs to plan 07.
</verification>

<success_criteria>
- The anonymous volume's id is captured durably and the volume still exists.
- 392 MB of cache is at `/mnt/fast/appdata/media/jellyfin/cache` with matching file counts and byte totals.
- `jellyfin.yaml` declares both binds, has no `/dev/shm` mount, carries the ten-part comment block, and no longer claims hardware acceleration is VAAPI.
- `/mnt/fast/transcode` is still empty.
- The host checkout is at `origin/main` with the edited file present.
</success_criteria>

<rollback>
| If this fails | Revert by | Cost |
|---|---|---|
| The copy is incomplete or the counts do not match | Nothing is live yet — the container still uses the anonymous volume. Delete `/mnt/fast/appdata/media/jellyfin/cache` and re-copy with the fallback form. The source is untouched | minutes |
| The compose edit is wrong | `git checkout HEAD~1 -- stacks/selfhosted/media/jellyfin.yaml` on the workstation and at `/mnt/fast/stacks`. The container has not been recreated, so nothing is running from it | seconds |
| The host `git pull --ff-only` refuses | It refuses rather than merging, by design. Inspect `git status` at `/mnt/fast/stacks`; the two known untracked files do not conflict. Do not force and do not stash — report | seconds |
| A stray `.jellyfin-*` marker lands under the wrong path | `find /mnt/fast/transcode /mnt/fast/appdata/media/jellyfin/cache -name '.jellyfin-*'` and remove the foreign one | seconds |
</rollback>

<output>
Create `.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-06-SUMMARY.md` when done
</output>
