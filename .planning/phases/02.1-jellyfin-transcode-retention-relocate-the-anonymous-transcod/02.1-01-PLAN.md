---
phase: 02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-D32-watch.txt
autonomous: true
requirements: [TRAN-01, TRAN-02, TRAN-03, TRAN-04, TRAN-05, TRAN-06, TRAN-07, TRAN-08, TRAN-09]

must_haves:
  truths:
    - "TRAN-01 through TRAN-09 exist in REQUIREMENTS.md before any other plan in this phase cites them, and REQUIREMENTS.md is still internally consistent afterwards — its traceability table, its coverage totals and its two-way arithmetic all agree"
    - "ROADMAP.md Phase 02.1 states a real Goal, the nine TRAN requirement IDs and nine Success Criteria instead of the [Urgent work - to be planned] / TBD placeholders"
    - "The anonymous volume's full id is recorded in a durable file on /mnt/fast before anything in this phase can make it unobtainable"
    - "A phase-start observation of / headroom and the anonymous volume's transcode footprint is on disk, so a mid-phase refill has an early signal instead of being a surprise"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRAN-01..TRAN-09 definitions in their own subtotalled section, with the traceability table and both arithmetic checks updated"
      contains: "TRAN-09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 02.1 Goal / Requirements / Success Criteria / Plans, placeholders removed, wave headers matching the D-31 order"
      contains: "TRAN-01"
    - path: ".planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-D32-watch.txt"
      provides: "the D-32 passive watch artifact, opened with the phase-start sample and the captured volume id"
      contains: "OLD_VOL"
  key_links:
    - from: ".planning/REQUIREMENTS.md TRAN block"
      to: "every other plan's frontmatter requirements field in this phase"
      via: "requirement IDs defined before they are cited (the D-31 ordering constraint)"
      pattern: "TRAN-0[1-9]"
    - from: "artifacts/02.1-D32-watch.txt"
      to: "/mnt/fast/spike-021/old-vol.txt on LXC 100"
      via: "the same volume id mirrored into git, so the durable host file has a committed twin"
      pattern: "OLD_VOL"
---

<objective>
Land this phase's requirement IDs before anything cites them, and open the passive `/` watch that
runs for the phase's whole duration.

This plan is the first half of a **split** made under **D-31**. The reviewed plan set put the
documentation work, the Renovate policy work and the `check-renovate.sh` repair in one plan and ran
it first. D-31 moves the cutover to the front — but every plan's frontmatter cites TRAN-01…09, and
those IDs are *landed by* that documentation work. Running it late would mean nine plans referencing
requirement IDs that exist nowhere authoritative.

The resolution D-31 records: **split it.** The REQUIREMENTS.md / ROADMAP.md TRAN-block landing is
small, zero-risk and documentation-only, so it runs first, here. The Renovate rule and the
`check-renovate.sh` repair — the actual policy work, and the part carrying real execution risk
(lines 26-173 of that script have **never executed** under `set -euo pipefail`) — move to the tail
as plan **02.1-08**.

Two things ride along, both cheap and both load-bearing for the plans behind them:

**The anonymous volume's id, captured now.** `docker inspect jellyfin` is the only reliable source
for it, and the recreate in plan 02.1-05 removes the container object that carries it. Capturing it
here rather than in the copy plan means the standing check's old-volume section (built in 02.1-02)
is live from its first run instead of printing "not yet captured" for three plans.

**The D-32 watch, opened.** The operator chose a passive guard over actively clearing the anonymous
volume's `transcodes/`. This plan takes the phase-start observation — `/` headroom in bytes and a
`du -sb` of the volume's transcode footprint — and every subsequent plan appends one timestamped
line. **Observation only: nothing is written into or deleted from live transcode data.** The
existing cache is removed by the normal cutover path in 02.1-06, not by this watch.

Purpose: make the requirement IDs authoritative before they are cited, and give the phase an early
signal if `/` starts filling while the phase is running.
Output: REQUIREMENTS.md and ROADMAP.md carrying TRAN-01..09, a durable volume-id file, and an open
watch artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-CONTEXT.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-RESEARCH.md
@.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define TRAN-01..TRAN-09 and keep REQUIREMENTS.md internally consistent</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — **the whole file**, and specifically four regions that must all
      move together: the v1 category headings and their `- [ ] **ID**: description` bullet form
      (lines 11-147); the `## Traceability` table (line 191 onward, one row per requirement, 39 rows);
      the `**Coverage:**` block and the `**Arithmetic, both ways:**` block at lines ~240-250, which
      state `By category: SAFE 5 + WRIT 4 + CONS 4 + TAGR 6 + CONF 6 + INBX 3 + QUAL 4 + IMPT 3 +
      INGS 4 = 39` and `By phase: 10 + 3 + 3 + 3 + 3 + 6 + 6 + 4 + 1 = 39`; and the `**By phase:**`
      table at line ~259.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Phase Requirements* (lines 169-189) — the nine
      TRAN rows with their descriptions and the decisions each discharges.
    - `.planning/phases/02.1-…/02.1-REVIEWS.md` § *Plan 01* — the MEDIUM finding this task exists to
      close: the file carries a Traceability table (39 rows) and a two-way arithmetic check, and
      adding nine requirements without touching those leaves the document self-contradictory on
      exactly the internal-consistency claim it makes about itself.
    - `.planning/phases/02.1-…/02.1-CONTEXT.md` § D-31 — why this task is split out and runs first.
  </read_first>
  <action>
    Add a new requirement section to `.planning/REQUIREMENTS.md` headed
    `### Transcode retention — the cache cannot fill the Docker host's root filesystem (phase
    insertion 02.1)`, and define TRAN-01 through TRAN-09 as unchecked `- [ ] **TRAN-0N**:` bullets
    using the descriptions from RESEARCH.md § *Phase Requirements* verbatim in substance. Keep each
    bullet's decision mapping in a parenthesised note so the D-numbers stay traceable: TRAN-01 (D-01,
    D-02, D-03, D-08, D-09, D-10), TRAN-02 (D-08, D-10, D-18), TRAN-03 (D-12, D-13, D-15), TRAN-04
    (D-14, D-15, D-21), TRAN-05 (D-16, D-17, D-18, D-19, D-20, D-22), TRAN-06 (D-23, D-24, D-25),
    TRAN-07 (D-05, D-06, D-07), TRAN-08 (D-28), TRAN-09 (D-29, D-30).

    TRAN-09's note must record that it originated as a research finding (Pitfall 1) rather than a
    CONTEXT decision, and was ruled in scope by the operator on 2026-09-02 as D-29.

    **Then keep the document self-consistent — this is the whole point of the task.** REQUIREMENTS.md
    makes an explicit claim about its own internal consistency ("Both totals agree, and every
    requirement appears in exactly one phase row above"). Adding nine IDs without touching the
    machinery that backs that claim makes the file contradict itself. Four things move together:

    1. **The `## Traceability` table** gains nine rows — `| TRAN-01 | Phase 02.1 | Pending |` through
       `| TRAN-09 | Phase 02.1 | Pending |` — placed between the CONS-03 and CONS-04 rows so the
       decimal-phase ordering matches the roadmap's.
    2. **The `Coverage:` block** keeps `v1 requirements: 39 total` as the *milestone* denominator so
       the figure Phase 1-9 progress is measured against stays meaningful, and gains a separate line
       for the insertion. Write it as: `v1 requirements: 39 total` / `Mapped to phases: 39 ✓` /
       `Unmapped: 0` / `Phase insertions (not part of the v1 denominator): TRAN-01…09 = 9` /
       `Total tracked: 48`.
    3. **The two-way arithmetic** gains a third line rather than being rewritten. Keep
       `By category: … = 39` and `By phase: … = 39` exactly as they are — they are the v1 statement
       and they are still true — and append:
       `Phase insertion 02.1: TRAN 9 — tracked separately, so both v1 totals above are unchanged and
       still agree. Total tracked = 39 + 9 = 48.`
    4. **The `By phase:` table** gains a `| 02.1 — Jellyfin Transcode Retention *(inserted)* |
       TRAN-01…09 | 9 |` row between the Phase 2 and Phase 3 rows, with a footnote under the table
       stating the row is an insertion and is excluded from the v1 total of 39.

    Append a dated revision note in the same style as the existing `> **Revision 2026-08-17:**` block,
    recording that the growth is a **phase insertion**, not a scope change to the milestone, and that
    the v1=39 denominator is deliberately preserved so milestone progress remains comparable.

    Do not renumber, reword or re-map any existing requirement. Do not change any existing
    Traceability row.
  </action>
  <verify>
    <automated>test "$(grep -cE '^- \[ \] \*\*TRAN-0[1-9]\*\*' .planning/REQUIREMENTS.md)" -eq 9 && test "$(grep -cE '^\| TRAN-0[1-9] \|' .planning/REQUIREMENTS.md)" -eq 9 && grep -q 'Total tracked' .planning/REQUIREMENTS.md && grep -qF 'INGS 4 = **39**' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*TRAN-0[1-9]\*\*' .planning/REQUIREMENTS.md` returns exactly **9**.
    - `grep -cE '^\| TRAN-0[1-9] \|' .planning/REQUIREMENTS.md` returns exactly **9** — every TRAN id
      has a Traceability row, so the file's own "every requirement appears in exactly one phase row"
      claim is still true.
    - The `By category: … = **39**` and `By phase: … = **39**` lines are **byte-identical** to their
      pre-change form. Verify with `git diff .planning/REQUIREMENTS.md` showing them unchanged; the
      v1 denominator is deliberately preserved rather than incremented to 48.
    - A third arithmetic line exists naming `39 + 9 = 48` and stating the insertion is tracked
      separately: `grep -q 'Total tracked' .planning/REQUIREMENTS.md` succeeds.
    - The `By phase:` table contains a `02.1` row with count `9`, and a footnote under the table
      states it is excluded from the v1 total.
    - A dated revision note exists recording that this is a phase insertion, not a milestone scope
      change.
    - No pre-existing requirement bullet changed: `git diff .planning/REQUIREMENTS.md` shows
      additions only outside the Coverage and arithmetic blocks that were deliberately extended.
  </acceptance_criteria>
  <done>TRAN-01..TRAN-09 are defined with their D-number mappings, every one has a Traceability row, and REQUIREMENTS.md's coverage totals, both arithmetic checks and by-phase table all agree with each other while preserving the v1=39 milestone denominator.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the ROADMAP Phase 02.1 placeholders and rewrite its plan list to the D-31 wave order</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines **208-240** — the Phase 02.1 block carrying the placeholders
      (`**Goal:** [Urgent work - to be planned]` at line 210 and `**Requirements**: TBD` at line 211,
      which is the **only** occurrence of that string anywhere in the file) and the wave-grouped plan
      list, which still reflects the pre-D-31 order.
    - `.planning/ROADMAP.md` lines **178-206** (Phase 02's block) — the exact heading/field ordering a
      phase entry uses: `**Goal**`, `**Depends on**`, `**Requirements**`, `**Success Criteria** (what
      must be TRUE)`, numbered criteria, `**Plans**`, `**Research**`.
    - `.planning/ROADMAP.md` line **50** (the phase-list entry) and line **564** (the § *Progress* row
      `| 02.1. Jellyfin Transcode Retention *(inserted)* | 0/9 | Not started | - |`) — the plan count
      changes from 9 to **10** because of D-31's split, so both must move.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Proposed ROADMAP Wording* (lines 1596-1667) — the
      drafted Goal, Requirements line and eight Success Criteria.
    - `.planning/phases/02.1-…/02.1-CONTEXT.md` § D-31 — the new wave order and the two ordering
      constraints it resolves.
  </read_first>
  <action>
    Replace the Phase 02.1 block's placeholder lines with: the Goal paragraph, `**Depends on**`,
    `**Requirements**: TRAN-01, TRAN-02, TRAN-03, TRAN-04, TRAN-05, TRAN-06, TRAN-07, TRAN-08,
    TRAN-09`, a `**Success Criteria** (what must be TRUE):` block carrying nine numbered criteria, a
    `**Plans**: 10 plans` line, and a `**Research**:` line.

    Take the wording from RESEARCH.md § *Proposed ROADMAP Wording* — with two corrections the research
    draft does not contain, because it was written before D-29/D-30 were ruled: the `**Requirements**`
    line must include **TRAN-09** (the draft stops at TRAN-08), and a ninth Success Criterion must be
    added stating that `scripts/disable-jellyfin-hwaccel.sh enable` can no longer silently revert
    `TranscodingTempPath`, `EnableSegmentDeletion` or `EnableThrottling` — proven both positively (a
    `check` → `disable` → `enable` round trip against a throwaway `encoding.xml`) **and** negatively
    (a fault-injected run that genuinely fails).

    Then rewrite the `Plans:` list and its wave headers to the **D-31 order**, which is not the order
    currently written there. Ten plans in eight waves:

    - **Wave 1** *(no dependencies)* — `02.1-01` (land TRAN-01…09, capture the volume id, open the
      D-32 watch) and `02.1-02` (write `scripts/check-jellyfin-transcode.sh`, record the `--baseline`
      before-state, fast-forward the host checkout).
    - **Wave 2** *(blocked on 02.1-02)* — `02.1-03` (declare `fast/transcode` in Terraform, set
      `quota=50G`, applied from a saved plan mechanically asserted to have zero destroys).
    - **Wave 3** *(blocked on 02.1-03)* — `02.1-04` (copy the 392 MB cache, edit `jellyfin.yaml`,
      deliver to the host).
    - **Wave 4** *(blocked on 02.1-04)* — `02.1-05` (recreate the container, prove the mount shape,
      apply the five encoding settings by read-modify-write).
    - **Wave 5** *(blocked on 02.1-05)* — `02.1-06` (prove the bounds fire with a driven HLS client,
      delete the anonymous volume, prove ENOSPC via a temporary quota shrink). **`/` is structurally
      protected from the end of this wave.**
    - **Wave 6** *(blocked on 02.1-06)* — `02.1-07` (harden `disable-jellyfin-hwaccel.sh`) and
      `02.1-08` (the `jellyfin/jellyfin` Renovate rule and the `check-renovate.sh` repair).
    - **Wave 7** *(blocked on 02.1-07)* — `02.1-09` (reclaim unreferenced Docker images through the
      existing approval gate — **blocking human gate**). It depends on 02.1-07 so the hardening
      plan's sandboxed proof still has its utility image resident when it runs.
    - **Wave 8** *(blocked on 02.1-06 and 02.1-09)* — `02.1-10` (execute the fail-closed negative
      controls, fold into `quick-health-check.sh`, record the known limits).

    Add a short note under the wave headers recording **why** the order is what it is: D-31 puts the
    cutover on the critical path so `/` is protected at the end of wave 5 rather than behind a
    blocking human gate, and D-28 and D-29 are unchanged in scope — only their position moved.

    Update line **50**'s phase-list entry and the § *Progress* row from `0/9` to `0/10`.

    Do not touch any other phase's block, and do not renumber phases.
  </action>
  <verify>
    <automated>! grep -q 'Urgent work - to be planned' .planning/ROADMAP.md && ! grep -qE '^\*\*Requirements\*\*: TBD' .planning/ROADMAP.md && grep -q 'TRAN-09' .planning/ROADMAP.md && test "$(grep -cE '^- \[ \] 02\.1-[0-9]{2}-PLAN\.md' .planning/ROADMAP.md)" -eq 10 && grep -q '0/10' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Urgent work - to be planned' .planning/ROADMAP.md` fails (exit 1).
    - `grep -qE '^\*\*Requirements\*\*: TBD' .planning/ROADMAP.md` fails (exit 1). **This replaces the
      reviewed plan's vacuous criterion**, which grepped for `TBD (run /gsd-plan-phase 02.1` — a
      string that appears nowhere in the file and therefore failed today regardless of any change.
      The actual placeholder is `**Requirements**: TBD` on line 211, and it is the sole occurrence.
    - The Phase 02.1 `**Requirements**` line names all nine IDs including `TRAN-09`.
    - The Success Criteria block contains at least 9 numbered items, and one of them names
      `disable-jellyfin-hwaccel.sh` and requires **both** a positive and a negative proof.
    - `grep -cE '^- \[ \] 02\.1-[0-9]{2}-PLAN\.md' .planning/ROADMAP.md` returns exactly **10**, and
      the ten filenames match the ten files actually on disk — verify by listing the phase directory,
      not from memory.
    - Each plan bullet's stated wave matches the `wave:` value in that plan's own frontmatter. Check
      all ten: a mismatch here is precisely how a re-order half-lands and reads as complete.
    - `**Plans**: 10 plans` appears, line 50's phase-list entry is updated, and the § *Progress* row
      reads `0/10`.
    - A note under the wave headers records D-31's reason for the order and states that D-28 and
      D-29 kept their scope.
    - `git diff .planning/ROADMAP.md` shows changes confined to the Phase 02.1 region, line 50 and the
      Progress row. No other `### Phase ` heading changed.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 02.1 carries a real Goal, all nine requirement IDs, nine Success Criteria, and a ten-plan list whose wave grouping matches both the D-31 order and the plans' own frontmatter, with no placeholder remaining.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the anonymous volume id and open the D-32 passive watch</name>
  <files>.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-D32-watch.txt</files>
  <read_first>
    - `.planning/phases/02.1-…/02.1-CONTEXT.md` § **D-32** — observation only; the watch never writes
      to or deletes live transcode data, and the 19 GB-class cache is removed by the normal cutover
      path in 02.1-06, not by this guard.
    - `.planning/phases/02.1-…/02.1-RESEARCH.md` § *Pitfall 5* — the anonymous volume's name becomes
      unobtainable after the recreate, because `docker inspect jellyfin` is the only reliable source
      and the recreate removes the container object that references it. The capture command and the
      "must be exactly 1 before, 0 after" assertion are given there.
    - `.planning/phases/02.1-…/02.1-VALIDATION.md` rows **19** and **37** — the headroom reading in
      bytes, and the watch's format and its observation-only status.
    - `scripts/spike03-image-headroom.sh` lines 163-168 — `avail_gb()`, why `df -B1` and never
      `df -BG` (it rounds **up**, up to 1 GiB of optimism on a 20 GiB floor; 03-01's finding, commit
      `b217ada`), and why an unreadable `df` yields an empty string treated as UNKNOWN rather than 0.
    - `CLAUDE.md` § *Storage and Permissions*, plus the `lxc100-tmp-is-tmpfs` estate note — `/tmp` on
      LXC 100 is tmpfs against ~4.7 GiB available RAM, and 1.1 GB written there once took the whole
      box down. Artifacts land on `/mnt/fast/` or under `.planning/`.
  </read_first>
  <action>
    On LXC 100, capture the anonymous volume's full id **before anything in this phase can make it
    unobtainable**, using
    `docker inspect jellyfin --format '{{range .Mounts}}{{if eq .Type "volume"}}{{.Name}}{{"\n"}}{{end}}{{end}}'`.
    Assert the result is **exactly one** non-empty line. Write that id to
    `/mnt/fast/spike-021/old-vol.txt` — a durable path on `/mnt/fast`, never `/tmp`. Create
    `/mnt/fast/spike-021/` if it does not exist.

    Capturing it here rather than in the copy plan is deliberate: the standing check built in 02.1-02
    has an old-volume section that reads this file, and without it that section prints "not yet
    captured" for three plans. It also means the id survives an interrupted later plan.

    Open `.planning/phases/02.1-…/artifacts/02.1-D32-watch.txt` with a header block stating: what the
    watch is (D-32); that it is **observation only** and asserts nothing; that it never writes to or
    deletes live transcode data; that the existing cache is removed by 02.1-06's normal cutover path;
    and the one-line-per-sample format. Record `OLD_VOL=<id>` in the header so the id lives in git as
    well as on the host.

    Take the phase-start sample and append it as the first data line. Every field measured on LXC 100
    and recorded in **bytes**, never `df -BG`:

    - an ISO-8601 UTC timestamp
    - `plan=02.1-01` — which plan took the sample
    - `root_avail_bytes=` from `df -B1 --output=avail / | tail -1 | tr -d ' '`
    - `root_size_bytes=` from `df -B1 --output=size / | tail -1 | tr -d ' '`
    - `oldvol_transcodes_bytes=` from `du -sb /var/lib/docker/volumes/$OLD_VOL/_data/transcodes` —
      the growing part, and the number this watch exists to observe
    - `oldvol_total_bytes=` from `du -sb` on the whole `_data` tree
    - `oldvol_transcodes_files=` from a `find … -type f | wc -l` on the same path

    An unreadable `df` or `du` yields an empty field recorded as `UNKNOWN`, never as `0` — "could not
    look" and "it is empty" are different answers, and 03-01 already paid for conflating them.

    State in the artifact header that subsequent plans append exactly one such line at their head, and
    that once 02.1-06 deletes the volume the `oldvol_*` fields become `GONE` rather than `UNKNOWN`, so
    the transition is legible to a reader scanning the file later.

    **Do not** delete, truncate, move or write into `/var/lib/docker/volumes/$OLD_VOL/_data/`. The
    watch is `du`, `df` and `docker inspect` and nothing else. Do not restart or recreate the
    container. Do not run `docker volume prune`.

    Finally, read the phase-start `root_avail_bytes` against D-17's 20 GiB floor (`21474836480`). If
    it is already below the floor, **stop and report** rather than proceeding: the phase's starting
    assumption is that `/` has ~33-36 GiB free, and a phase that begins under its own floor needs a
    different first move than the one planned.
  </action>
  <verify>
    <automated>ssh -n -o BatchMode=yes root@172.16.1.159 'test -s /mnt/fast/spike-021/old-vol.txt && test "$(wc -l < /mnt/fast/spike-021/old-vol.txt)" -eq 1 && docker volume ls -q | grep -qx "$(cat /mnt/fast/spike-021/old-vol.txt)"' && grep -q 'OLD_VOL' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-D32-watch.txt && grep -q 'plan=02.1-01' .planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/artifacts/02.1-D32-watch.txt && test "$(ssh -n -o BatchMode=yes root@172.16.1.159 "df -B1 --output=avail / | tail -1 | tr -d ' '")" -ge 21474836480 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/mnt/fast/spike-021/old-vol.txt` exists on LXC 100 and contains exactly one line: a
      64-character volume id beginning `d98b2ff9`. The pre-capture `Type: volume` mount count was
      asserted to be exactly **1** and recorded.
    - `docker volume ls -q | grep -qx "$(cat /mnt/fast/spike-021/old-vol.txt)"` exits **0** — the
      volume still exists. Nothing was deleted in this task.
    - The watch artifact exists, carries `OLD_VOL=` with the same 64-character id, and its header
      states in words that the watch is observation-only, writes nothing into live transcode data,
      and deletes nothing.
    - The first data line carries all six fields with `plan=02.1-01` and an ISO-8601 UTC timestamp,
      and every size field is a **byte integer** or the literal `UNKNOWN`. `grep -c 'df -BG'` over
      the artifact returns **0**.
    - `root_avail_bytes` is ≥ `21474836480` (D-17's floor). If it were not, the plan stopped here.
    - `oldvol_transcodes_bytes` is recorded as a number — this is the phase-start value the later
      samples are compared against, and the whole point of the guard.
    - Nothing under `/var/lib/docker/volumes/` was modified: the `du -sb` on the `_data` tree taken
      at the end of the task equals the one taken at the start, and no `rm`, `mv`, `truncate` or
      redirect into that path appears anywhere in the transcript.
    - Nothing was written to `/tmp` on LXC 100.
  </acceptance_criteria>
  <done>The volume id is captured to a durable host path and mirrored into git, the D-32 watch artifact is open with its phase-start sample, and the anonymous volume is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 | `ssh root@172.16.1.159` runs the capture and the watch sample; the remote exit status is the only evidence of what happened |
| the watch → live transcode data | The watch reads a directory that Jellyfin is actively writing to; a guard that mutates what it observes is not a guard |
| repo → public GitHub | This repository is public and has one prior credential exposure still recoverable via `git log -S` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-01 | **Denial of Service** | Losing the anonymous volume's id before it can be deleted by name | mitigate | The id is captured **first**, to `/mnt/fast/spike-021/old-vol.txt`, and asserted to be exactly one line. `docker inspect` on the container is the only reliable source and the recreate in 02.1-05 removes the object that carries it. `docker volume prune` is rejected outright — after the recreate it would also be free to remove anything else that became dangling |
| T-021-02 | **Tampering** | The D-32 watch mutating the data it observes | mitigate | D-32 is observation-only by ruling. The task uses `du`, `df`, `find` and `docker inspect` and nothing else, and the acceptance criteria assert the `_data` tree's `du -sb` is identical at the start and end of the task. No `rm`, `mv`, `truncate` or redirect into that path is permitted |
| T-021-03 | Repudiation | Recording `0` where the instrument could not look | mitigate | An unreadable `df` or `du` records the literal `UNKNOWN`, never `0`. 03-01 already paid for this conflation; `df -BG` is prohibited by name because it rounds **up** and reports 2.2 GiB free as `3G` |
| T-021-04 | Repudiation | A requirement ID cited before it is defined | mitigate | This is the D-31 ordering constraint, resolved structurally rather than by convention: task 1 lands TRAN-01…09 in the first task of the first plan of wave 1, before any plan that cites them can run |
| T-021-05 | Denial of Service | Writing artifacts to `/tmp` on LXC 100 | accept-with-control | `/tmp` there is tmpfs against 24 GiB total RAM with ~4.7 GiB available; 1.1 GB written to it once took the box down. All artifacts land under `.planning/` or `/mnt/fast/`, asserted |
| T-021-06 | Information Disclosure | Planning documents committed to a public repo | mitigate | This plan touches REQUIREMENTS.md, ROADMAP.md and a watch artifact recording free-space figures and a volume id — none carries a credential. Screen the diff before committing with `git diff --cached \| grep -cEi '(token\|api_key\|password\|secret)[[:space:]]*[=:]'` returning 0 (the T-01-38 gate from 01-07) |
| T-021-SC | Tampering | npm/pip/cargo installs | mitigate | **No package install occurs in this plan.** RESEARCH.md § *Package Legitimacy Audit* records that this phase installs nothing. `docker`, `df`, `du` and `find` are already present on LXC 100 |
</threat_model>

<verification>
- TRAN-01…09 are defined in REQUIREMENTS.md with nine matching Traceability rows, and both v1
  arithmetic lines are byte-identical to their pre-change form.
- No placeholder remains in ROADMAP.md's Phase 02.1 block, and its ten-plan list matches both the
  D-31 wave order and the plans' own `wave:` frontmatter values.
- VALIDATION.md row 19: `/` headroom ≥ 21474836480 bytes at phase start.
- VALIDATION.md row 37: the D-32 watch artifact exists with its phase-start sample and the volume id.
- `git diff --stat` shows exactly three files changed and no unrelated file touched.
</verification>

<success_criteria>
- REQUIREMENTS.md defines TRAN-01..TRAN-09 with their D-number mappings, nine Traceability rows, an updated by-phase table, a preserved v1=39 denominator and a third arithmetic line reconciling 39 + 9 = 48.
- ROADMAP.md Phase 02.1 carries a real Goal, all nine requirement IDs, nine Success Criteria, ten plan filenames in eight waves matching the D-31 order, and `0/10` in the Progress table.
- `/mnt/fast/spike-021/old-vol.txt` holds exactly one volume id, and the volume still exists.
- The D-32 watch artifact is open with its phase-start sample, recorded in bytes, and the anonymous volume's `_data` tree is provably unmodified by this plan.
</success_criteria>

<rollback>
| If this fails | Revert by | Cost |
|---|---|---|
| The REQUIREMENTS/ROADMAP edits are wrong | `git checkout HEAD~1 -- .planning/REQUIREMENTS.md .planning/ROADMAP.md`. Nothing here touches the live estate | seconds |
| The volume-id capture returns more or fewer than one line | Stop and report. More than one `Type: volume` mount means the assumption behind D-10's delete-by-name is wrong and the copy plan's premise needs re-checking; zero means the volume is already gone and 02.1-06's row 5 is unobtainable | — |
| `/` is already below the 20 GiB floor at phase start | Stop and report. Do not proceed into the cutover chain on a filesystem that is already under its own floor — the reap in 02.1-09 exists to widen headroom and may need to move earlier, which is an operator decision, not a planner one | — |
</rollback>

<output>
Create `.planning/phases/02.1-jellyfin-transcode-retention-relocate-the-anonymous-transcod/02.1-01-SUMMARY.md` when done
</output>
