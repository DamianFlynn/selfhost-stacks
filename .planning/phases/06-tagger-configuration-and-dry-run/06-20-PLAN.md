---
phase: 06-tagger-configuration-and-dry-run
plan: 20
type: execute
wave: 6
depends_on: ["06-14"]
gap_closure: true
files_modified:
  - scripts/phase06-incremental-control.sh
autonomous: true
requirements: [CONF-02]
closes_findings: [IN-02, IN-05, IN-10, IN-08-incremental-half]

must_haves:
  truths:
    - "`--arm` with a missing value exits 3 (a usage error) with a message, not 1 (a measured opposite outcome) in silence — the documented exit-code table and the script's behaviour agree"
    - "The re-offer classifier reads any path count, so a multi-album source folder classifies instead of falling to indeterminate — the `SRC_FOLDER` override no longer silently disables the control it is offered on"
    - "`remote_is_blind()` is either routed through by its callers or deleted: no named classification sits in the file unused where a reader will assume it is in force"
    - "The RED-versus-UNKNOWN precedence matches the oracle's and is stated in the EXIT CODES block, so the two instruments written in the same phase no longer disagree"
    - "Every changed branch is DRIVEN by `--self-test` or by a direct argument-parsing invocation, locally, with no ssh and no docker"
  artifacts:
    - path: "scripts/phase06-incremental-control.sh"
      provides: "the aligned, driveable incremental-import negative control"
      contains: "Skipped [0-9]"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-20-incremental-driven.txt"
      provides: "the driven usage-error, classifier and precedence transcripts"
  key_links:
    - from: "scripts/phase06-incremental-control.sh EXIT CODES block"
      to: "scripts/phase06-oracle.sh EXIT CODES block"
      via: "a single stated convention — a blind instrument outranks a measured red — implemented identically in both"
      pattern: "outranks"
---

<objective>
Close the four findings against `phase06-incremental-control.sh`: IN-05 (a usage error exiting with the code reserved for a measured failure, silently), IN-10 (a classifier hard-coded to a path count of 1, so the `SRC_FOLDER` override silently disables the control), IN-02 (a named blindness classifier defined and never called) and the incremental half of IN-08 (the two sibling instruments disagree on whether RED or UNKNOWN wins).

Purpose: this script is the phase's strongest piece of evidence — a genuine two-arm negative control whose overlays differ by exactly one key and which drives opposite observable outcomes, and `06-VERIFICATION.md` verified CONF-02 on it. That is exactly why its defects matter: an instrument this load-bearing must not have a mode in which it stops working quietly. IN-10 is the sharpest of the four — the control silently stops working for any `SRC_FOLDER` that is not single-album, which is precisely what offering the `SRC_FOLDER` override invites someone to do.

Output: one instrument, aligned with its sibling, with every changed branch driven offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@scripts/phase06-incremental-control.sh
</context>

<constraints>
**Do not run `phase06-incremental-control.sh --arm a` or `--arm b` against the live estate.** Both arms perform a real `beet import` into a throwaway root, and CONF-02's proof is already committed and verified. Every change here is proven by `bash -n`, `shellcheck`, `--self-test`, and direct argument-parsing invocations that exit before any remote call. If a change cannot be proven that way, record it as undriven rather than running an arm.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Fix the usage-error exit, widen the re-offer classifier, and resolve the dead classifier</name>
  <files>scripts/phase06-incremental-control.sh</files>
  <read_first>
    - `scripts/phase06-incremental-control.sh:915-935` — the argument dispatch, specifically `--arm) MODE="arm"; ARM="${2:-}"; shift 2 ;;` at `:924`. With only one argument left, `shift 2` returns non-zero and `set -e` (`:101`) terminates the script with status 1 and NO output.
    - `scripts/phase06-incremental-control.sh:95-105` — the `set -e` / `set -u` line and whatever the surrounding comment says about why.
    - `scripts/phase06-incremental-control.sh` — the header's EXIT CODES block (`grep -n "EXIT CODES" scripts/phase06-incremental-control.sh`), which documents 1 as *"the arm produced the OPPOSITE outcome"* and 3 as usage/blind. Read the whole block: the fix is to make behaviour match this table, not to change the table.
    - `scripts/phase06-incremental-control.sh:650-670` — the re-offer classifier, specifically `grep -q 'Skipped 1 paths\.'` at `:658`, and the `indeterminate` → BLIND fall-through it lands in when the count is not 1. Read the whole classifier so the three outcomes (re-offered / not re-offered / indeterminate) stay three outcomes.
    - `scripts/phase06-incremental-control.sh` — the `SRC_FOLDER` override declaration and its contract (`grep -n SRC_FOLDER scripts/phase06-incremental-control.sh`), which is what makes IN-10 reachable in practice.
    - `scripts/phase06-incremental-control.sh:330-345` — `remote_is_blind()` and its non-callers. Every caller instead tests `[ "$RE_RC" -ne 0 ]`, which is WIDER than the function (it also catches rc 1, 125, 126, 127), so behaviour is fail-closed today — but the named classification the function encodes is unused and a reader will assume it is in force.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:441-453` (IN-02), `:470-477` (IN-05), `:516-523` (IN-10).
  </read_first>
  <action>
**Edit 1 (IN-05) — a usage error must exit 3, loudly.** Restructure the `--arm` case so a missing value cannot be swallowed by `set -e`: shift once unconditionally, shift the second argument only if one exists, then require `ARM` to be non-empty and to be one of the two legal values. On failure print a usage message to stderr naming the flag and the legal values, and exit with the code the EXIT CODES block documents for a usage error. **Audit every other flag in the same dispatch for the same `shift 2` shape** and fix each one the same way — the review found this instance, but the defect is in the idiom, and a second occurrence would reproduce it.

**Edit 2 (IN-10) — the classifier must read any count.** Replace the hard-coded `Skipped 1 paths.` match with one that accepts any non-negative integer path count. Keep the classifier's three outcomes intact and keep the `indeterminate` → BLIND fall-through for genuinely unrecognised output — widening the number must not widen what counts as recognised output in any other respect. Add a one-line comment stating why the count is not pinned: `SRC_FOLDER` is an offered override, a source folder yielding two albums prints a different count, and pinning the count silently disabled the control for exactly the case the override invites.

**Edit 3 (IN-02) — resolve the dead classifier, do not leave it.** Either route the callers through `remote_is_blind()` AND add an explicit arm for a non-blind non-zero status — so the wider `-ne 0` behaviour that exists today is preserved rather than accidentally narrowed — or delete the function. **Deleting is acceptable and may be the better answer here**, because the current `-ne 0` test is strictly safer than the function and narrowing it would be a regression dressed as a cleanup. Whichever is chosen, record the choice and the reason in the summary, and make sure `grep -c '\bremote_is_blind\b'` afterwards returns either 0 or a number greater than 1 — never exactly 1, which is the signature of the finding.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/phase06-incremental-control.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -q "Skipped 1 paths" && { echo "IN-10: the hard-coded path count survives"; exit 1; }; printf "%s\n" "$s" | grep -qE "Skipped \[0-9\]\+ paths" || { echo "IN-10: no widened path-count pattern found"; exit 1; }; n=$(grep -c "\bremote_is_blind\b" "$f" || true); [ "$n" -ne 1 ] || { echo "IN-02: remote_is_blind is still defined exactly once and never called"; exit 1; }; bad=$(printf "%s\n" "$s" | grep -n "shift 2" || true); [ -z "$bad" ] || { echo "IN-05: a shift 2 survives in the dispatch:"; echo "$bad"; exit 1; }; out=$(bash "$f" --arm 2>&1); rc=$?; [ "$rc" -eq 3 ] || { echo "IN-05: --arm with no value exited $rc, expected 3"; exit 1; }; [ -n "$out" ] || { echo "IN-05: --arm with no value exited silently"; exit 1; }; out2=$(bash "$f" --arm zzz 2>&1); rc2=$?; [ "$rc2" -eq 3 ] || { echo "IN-05: --arm with an illegal value exited $rc2, expected 3"; exit 1; }; bash "$f" --self-test >/dev/null || { echo "--self-test regressed"; exit 1; }; echo "06-20 task 1 OK"'</automated>
  </verify>
  <done>
    `--arm` with a missing value and `--arm` with an illegal value both exit 3 with a message on stderr; no `shift 2` remains anywhere in the dispatch; the re-offer classifier accepts any integer path count with a comment saying why it is not pinned, and its three outcomes are unchanged; `remote_is_blind` is either used (with an explicit non-blind non-zero arm preserving today's wider behaviour) or deleted, and `grep -c` for it never returns exactly 1. `--self-test` still exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align the RED-vs-UNKNOWN precedence with the oracle, state it, and drive it</name>
  <files>scripts/phase06-incremental-control.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-20-incremental-driven.txt</files>
  <read_first>
    - `scripts/phase06-incremental-control.sh:710-730` — the exit-verdict logic, which checks `failed` before the blind/unknown condition.
    - `scripts/phase06-oracle.sh:2160-2175` — the sibling's verdict logic, which checks `UNKNOWNS` first.
    - `scripts/phase06-incremental-control.sh` — the header's EXIT CODES block, where the convention must be stated, and the `--self-test` harness (`grep -n "self.test\|SELFTEST" scripts/phase06-incremental-control.sh`) to see how cases are built and counted.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:498-504` — IN-08 verbatim: *"Both are defensible; having both in one phase means a reader cannot infer the convention."*
    - `README.md` § Health Checks — the rule that "could not look" is kept distinct from "nothing is wrong", which is the reason the oracle's order is the one being adopted.
    - `scripts/phase06-incremental-control.sh` as task 1 left it.
  </read_first>
  <action>
**Edit 1 — adopt the oracle's order.** The house convention, decided in plan 06-19 and recorded there: **a blind instrument outranks a measured red.** The reason is this estate's standing rule that "could not look" must never be folded into another verdict — reporting a red while an instrument was blind asserts a cause the run did not establish. Swap this script's verdict logic so the blind/unknown condition is evaluated BEFORE the measured-failure condition, matching `phase06-oracle.sh:2160-2175`.

**Do not change what either condition means or which exit code it produces** — only which one is consulted first when both are true. Note in a comment that the two conditions are not mutually exclusive, which is why the order is a decision rather than an accident.

**Edit 2 — state it in the EXIT CODES block.** Write the convention down: which wins when both hold, the one-sentence reason, and a pointer to `scripts/phase06-oracle.sh` as the sibling implementing the same rule. Both headers must now say the same thing, because a convention implemented in two places and written in neither is what IN-08 reports.

**Edit 3 — drive it.** Add a `--self-test` case that produces both a measured failure and a blind read in the same run and requires the blind verdict to win, plus a case with a measured failure and no blind read requiring the red verdict. Two cases, because one proves only that the branch exists. Update the harness's case count and banner in the same edit.

**Then record everything in `artifacts/06-20-incremental-driven.txt`:**

1. The full `--self-test` transcript and exit status, with the two precedence cases visible.
2. The four argument-parsing drives, each with its exit status and its stderr: `--arm` with no value, `--arm zzz`, `--arm a` **with a deliberately impossible host or container override so the run refuses before any import** (if no such refusal exists before the first remote call, do NOT run it — say so and record the branch as undriven), and a legal `--self-test` invocation for contrast.
3. A driven IN-10 control: feed the classifier a synthetic transcript containing `Skipped 2 paths.` and require it to classify rather than fall to indeterminate, paired with a `Skipped 1 paths.` transcript that still classifies the same way it always did. The pair is the point — widening a pattern without proving the old case still works is how a widening becomes a regression.
4. `bash -n` and `shellcheck -S warning` output.
5. The IN-02 decision (used or deleted) with its reason.
6. An explicit statement that **no arm was run against the live estate**, that CONF-02's committed proof stands unchanged, and which branches remain undriven and why.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/phase06-incremental-control.sh; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-20-incremental-driven.txt; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; awk "/EXIT CODES/,/^# =/" "$f" | grep -qiE "outranks|wins" || { echo "IN-08: the precedence convention is not stated"; exit 1; }; awk "/EXIT CODES/,/^# =/" "$f" | grep -q "phase06-oracle" || { echo "IN-08: the sibling is not named"; exit 1; }; bash "$f" --self-test >/dev/null || { echo "--self-test failed"; exit 1; }; test -s "$a" || { echo "artifact missing"; exit 1; }; for k in IN-02 IN-05 IN-08 IN-10; do grep -q "$k" "$a" || { echo "$k not recorded"; exit 1; }; done; grep -q "Skipped 2 paths" "$a" || { echo "the IN-10 widened-case drive is not recorded"; exit 1; }; grep -q "Skipped 1 paths" "$a" || { echo "the IN-10 old-case pair is not recorded"; exit 1; }; grep -qiE "no arm was run|did not run an arm|no --arm" "$a" || { echo "the no-arm-run statement is missing"; exit 1; }; grep -qiE "not driven|undriven" "$a" || { echo "the NOT-DRIVEN section is missing"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/phase06-incremental-control.sh" || true); [ -z "$d" ] || { echo "test debris:"; echo "$d"; exit 1; }; echo "06-20 task 2 OK"'</automated>
  </verify>
  <done>
    The verdict logic evaluates the blind condition before the measured-failure condition, matching `phase06-oracle.sh`, with a comment noting the two are not mutually exclusive; the EXIT CODES block states the convention, the reason and the sibling; `--self-test` grew two precedence cases (both-conditions and red-only) and its banner count matches. The artifact records the self-test transcript, the four argument-parsing drives with statuses and stderr, an IN-10 drive paired with its old single-path case, `bash -n`/`shellcheck` output, the IN-02 decision with its reason, an explicit no-arm-run statement, and a NOT-DRIVEN section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command-line arguments → `set -e` → process exit status | A dispatch that lets `set -e` terminate the script converts a usage error into a measured verdict, silently. |
| `SRC_FOLDER` override → the re-offer classifier | An offered override that silently disables the control it is offered on is worse than no override. |
| workstation → LXC 100 root ssh → `docker exec` → real `beet import` into a throwaway root | Both arms import. Nothing in this plan may reach that path. |
| this public repo → the world | No credential in the script or the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-110 | Repudiation | A usage error reported with the exit code reserved for "the arm produced the OPPOSITE outcome", and reported silently, so a mis-typed invocation reads as a measured result | mitigate | IN-05: the dispatch shifts safely, validates the value and exits 3 with a stderr message; driven twice (missing value, illegal value) with both statuses asserted by the verify command. Every other `shift 2` in the dispatch audited. |
| T-06-111 | Spoofing | A multi-album `SRC_FOLDER` silently turning the classifier indeterminate, so the negative control stops discriminating while still running | mitigate | IN-10: the path count is read as any integer; driven with a `Skipped 2 paths.` transcript AND paired with the original single-path case to prove the widening is not a regression. |
| T-06-112 | Repudiation | A named blindness classifier sitting unused, so a reader believes a narrower classification is in force than the `-ne 0` test actually applies | mitigate | IN-02: used with an explicit non-blind non-zero arm, or deleted; `grep -c` must never return exactly 1; the decision and its reason recorded. |
| T-06-113 | Repudiation | Two sibling instruments in one phase disagreeing on whether a blind instrument or a measured red wins, so no convention can be inferred | mitigate | IN-08: the oracle's order adopted, implemented here, stated in both EXIT CODES blocks with each naming the other; driven by two self-test cases. |
| T-06-114 | Denial of Service | Running an arm to "prove" the changes, performing a real import during a paper phase | mitigate | Plan-level constraint forbidding `--arm a`/`--arm b`; all drives are offline argument-parsing or `--self-test`; the verify command requires the no-arm-run statement in the artifact. |
| T-06-115 | Tampering | Narrowing the blindness test while "cleaning up" IN-02, so statuses 1, 125, 126 and 127 stop being caught | mitigate | The action requires an explicit non-blind non-zero arm if the function is routed through, and explicitly permits deletion as the safer answer. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S warning` clean on `scripts/phase06-incremental-control.sh`.
- `bash scripts/phase06-incremental-control.sh --arm` exits 3 with stderr output; `--arm zzz` likewise.
- No `shift 2` remains in the dispatch; no `Skipped 1 paths` literal remains.
- `grep -c '\bremote_is_blind\b'` returns 0 or >1, never 1.
- Both EXIT CODES blocks (this script and `phase06-oracle.sh`) state the same precedence convention and name each other.
- No arm was run against the live estate.
</verification>

<success_criteria>
- A usage error is loud and correctly coded.
- The negative control keeps working when its own override is used.
- No named classification sits unused.
- The two sibling instruments agree, and say so.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-20-SUMMARY.md` when done. Record the IN-02 decision and the NOT-DRIVEN list; plan 06-21 carries both into the disposition register.
</output>
