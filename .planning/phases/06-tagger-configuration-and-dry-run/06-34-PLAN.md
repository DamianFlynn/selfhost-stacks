---
phase: 06-tagger-configuration-and-dry-run
plan: 34
type: execute
wave: 15
depends_on: ["06-30", "06-31", "06-32", "06-33"]
gap_closure: true
gap_closure_round: 3
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md
  - .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP2.md
  - .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CONF-06]
closes_findings: []

must_haves:
  truths:
    - "Every one of the ten R3 findings carries an explicit disposition in one register, in ID order, each citing the plan, the commit and the artifact — so the set cannot be lost at the next context boundary"
    - "`06-REVIEW-GAP2.md` is WIRED: it points at the register, with nothing above the appended line edited"
    - "Round 3's residue is carried by name in `deferred-items.md` rather than dropped — including the two things this round deliberately refused to do"
    - "The review's Verified-and-clean section is recorded as off-limits, so round 4 does not re-litigate nine items already checked adversarially"
    - "ROADMAP and STATE describe round 3 accurately and say plainly that this is a record, not a re-close: no requirement checkbox moved, CONF-04's Jellyfin half is still OPEN under Phase 7 entry criterion E6, and the phase is not declared complete"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md"
      provides: "the ten-row round-3 disposition register with counts that reconcile"
      contains: "R3-10"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md"
      provides: "DEF-06-34-* entries carrying round 3's residue, including the refusals"
      contains: "DEF-06-34"
  key_links:
    - from: "06-REVIEW-GAP2.md"
      to: "06-DISPOSITIONS-GAP2.md"
      via: "an appended wiring block that edits nothing above it"
      pattern: "06-DISPOSITIONS-GAP2"
    - from: ".planning/ROADMAP.md Phase 6"
      to: "plans 06-30 … 06-34"
      via: "Wave 14 and Wave 15 blocks in the round-2 style"
      pattern: "ROUND 3"
---

<objective>
Round 3's record. Plans 06-30 through 06-33 closed the ten `R3-*` findings in
`06-REVIEW-GAP2.md`; this plan supplies the part a fix commit cannot — a statement of what happened
to **each** finding, wired where the next reader will find it.

The discipline is round 2's, one level in, and the reason is written in `06-DISPOSITIONS.md`: *a
finding recorded where the next phase does not read is a finding nobody owns.* Round 2 existed
because round 1 was declared closed before anyone read its own diff. Round 3 exists because round 2
was reviewed and found to have closed several classes partially while claiming completeness. **The
recursion stops by recording honestly, not by recording nothing** — and this round is deliberately
five plans against round 2's eight, because ten findings of which five are INFO and several are
comment corrections do not warrant eight plans.

**Three things this plan must get right, each because the alternative has already gone wrong here:**

1. **Do not use the SDK's state or roadmap verbs.** `gsd-sdk query state.*` and `roadmap.*` corrupted
   both files repeatedly in this session — truncating STATE.md sentences mid-line, and blanking the
   ROADMAP Phase 6 Notes cell on all three invocations. Edit both files **by hand**, and
   `git diff` each before committing.
2. **This is a record, not a re-close.** No `/gsd-verify` is run and no verification result is
   claimed. No requirement checkbox moves. **CONF-04 is not closed** — its Jellyfin half is still
   OPEN and Phase 7 entry criterion **E6** still owns the discharge. The phase is **not** declared
   complete; that is the verifier's call.
3. **Record the refusals as loudly as the fixes.** Round 3 deliberately did not do two things a
   reader might expect: plan 06-32 refused the `/tmp/p6-mf.*` sweep, and plan 06-33 refused both the
   third-conjunct gate and wiring `assert_beet_invocation_contract` live. A refusal that is not
   written down reads as an oversight, and the next round re-opens it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ten-row round-3 disposition register</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md</files>
  <read_first>
    - `06-DISPOSITIONS-GAP.md` in full — **this is the format to mirror**, not to invent a variant
      of. Note its structure: *Why this file exists* → *Source* table → *Counts* tables that
      reconcile more than one way → one row per finding in ID order → the lessons section at the
      end. Copy that shape.
    - `06-REVIEW-GAP2.md` in full: its frontmatter counts (0 critical, 5 warning, 5 info, 10 total),
      its `diff_base`, its four reviewed files, its Summary, all ten findings, **and its
      Verified-and-clean section** (nine items).
    - The four round-3 summaries: `06-30-SUMMARY.md`, `06-31-SUMMARY.md`, `06-32-SUMMARY.md`,
      `06-33-SUMMARY.md`. Take each finding's disposition, commit and artifact path from the summary
      of the plan that owned it, not from this plan.
    - The four artifacts, to cite by path:
      `artifacts/06-30-qhc-census-and-counts.txt`, `artifacts/06-31-oracle-pin-and-layer3.txt`,
      `artifacts/06-32-incremental-trap-fences.txt`,
      `artifacts/06-33-checker-case6-and-contract.txt`.
  </read_first>
  <action>
Create `06-DISPOSITIONS-GAP2.md`, mirroring `06-DISPOSITIONS-GAP.md`'s structure.

**(a) Header and provenance.** State that the source review read **round 2's own gap-closure
changes** — the fixes plans 06-22 through 06-29 wrote to close `06-REVIEW-GAP.md` — and found
**0 Critical, 5 Warning, 5 Info**. Record the review's `diff_base`, the four files, the ID namespace
`R3-01 … R3-10`, and the review's own measurement note: every grep was `/usr/bin/grep` by absolute
path, no script was executed against the estate, and the self-tests were not re-run. Record that
round 3 had **no cross-family adjudication** — round 2 had one, this round did not — so there are no
appended findings and the ten are the whole set. Say so explicitly; an absent adjudication that is
not mentioned reads as a lost one.

**(b) Counts that reconcile at least two ways**, as round 2's register does: the review's frontmatter
`0 + 5 + 5 = 10`, and the dispositions summing to 10.

**(c) One row per finding, `R3-01` … `R3-10`, in ID order.** Each row carries:
  - the finding's one-line subject and its file;
  - its **disposition** — `FIXED`, `FIXED (undriven)`, `ACCEPTED` or `CARRIED`;
  - **which kind of fix it was**: `CODE`, `CLAIM CORRECTION`, or `BOTH`. This column is the point of
    this round. The review's central charge is that round 2 closed classes partially and then wrote
    that it had closed them completely; a register that does not distinguish a widened fence from a
    narrowed sentence reproduces exactly that ambiguity one level up;
  - the owning plan, the commit, and the artifact path holding the driven transcript;
  - a **provenance clause** naming who confirmed the finding and how — three of the five Warnings
    (R3-01, R3-02, R3-05) were independently confirmed by the orchestrator, R3-05 by execution
    **twice**, and several findings are explicitly static reasoning that was never executed. Carry
    that distinction; do not flatten "grep-verified" and "reasoned about" into one word.

**(d) Record what was driven and what was not**, per finding. In particular: the layer-3 block
(R3-03, R3-04) is reachable only from a live `--run`, which this phase does not perform, so those
fixes are proven by an offline parse drive and static reading, not by a live run. The `SIGTERM`
behaviour behind R3-09 is static reasoning about POSIX `sh`, not executed against the container.
`quick-health-check.sh` was not executed at all. **Say all of this in the register**, because the
alternative — a register of ten FIXEDs with no distinction — is precisely the over-claim the review
is about.

**(e) Reproduce the review's Verified-and-clean section as a named, closed list.** Nine items were
checked adversarially and found correct: the GC-01 here-strings, case 7 as a genuine regression case,
`D04_N_ASSERT`'s correct empty-set behaviour, the `-1` UNKNOWN sentinels, `self_test_fences`'
non-destructiveness, GC-07's correction, GC-12(a)'s safe deletion, GC-14's pure-echo overrides, and
GC-16's regex widening. Record them under a heading that says plainly: **round 4 must not
re-litigate these**, and cite this register rather than re-deriving them.

**(f) A lessons section** in round 2's style, drawn from what round 3 actually found rather than
restated from round 2. At minimum:
  - that the round's dominant defect class was **a claim broader than its code**, and that the honest
    fix for several findings was to narrow the sentence rather than widen the code — with the
    corollary that a plan which silently widens scope to make a comment true is worse than one that
    corrects the comment;
  - that **two reviews in a row produced findings inside the previous review's own fixes**, and that
    the way out is not another round but a review whose findings are Info-only and whose fixes are
    claim corrections — which is most of what round 3 was;
  - that R3-07's own census sentence printed a code-only figure as a raw grep count, and R3-05's
    subject was a count invalidated by a later plan in the same round: **self-referential measurement
    in files that grep themselves is this phase's signature hazard**, and the mitigation adopted was
    to state recipes rather than numbers.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
D=.planning/phases/06-tagger-configuration-and-dry-run
R=$D/06-DISPOSITIONS-GAP2.md
G=/usr/bin/grep

test -s "$R"

# All ten findings appear, in their own right.
for i in 01 02 03 04 05 06 07 08 09 10; do
  test "$($G -cF "R3-$i" "$R")" -ge 1
done

# Every finding carries a disposition and a fix-kind. The disposition vocabulary must appear at
# least once per finding; the fix-kind vocabulary must be used, not collapsed to one value.
test "$($G -cF 'FIXED' "$R")" -ge 10
test "$($G -cF 'CLAIM CORRECTION' "$R")" -ge 3
test "$($G -cF 'CODE' "$R")" -ge 3

# All four owning plans and all four artifacts are cited by name.
for p in 06-30 06-31 06-32 06-33; do test "$($G -cF "$p" "$R")" -ge 1; done
for a in 06-30-qhc-census-and-counts 06-31-oracle-pin-and-layer3 06-32-incremental-trap-fences 06-33-checker-case6-and-contract; do
  test "$($G -cF "$a" "$R")" -ge 1
done

# The Verified-and-clean list is carried forward as closed.
test "$($G -ciF 'verified-and-clean' "$R")" -ge 1
test "$($G -cF 're-litigate' "$R")" -ge 1

# What was NOT driven is recorded, not flattened into ten FIXEDs. -E and not -F: an alternation
# is a regex, and `grep -F` would look for the backslash-pipe literally and silently return 0.
test "$($G -ciE 'not driven|not-driven|undriven' "$R")" -ge 1
test "$($G -ciF 'static' "$R")" -ge 1

# No adjudication is implied where none happened.
test "$($G -ciF 'adjudicat' "$R")" -ge 1
    </automated>
  </verify>
  <done>
`06-DISPOSITIONS-GAP2.md` exists in `06-DISPOSITIONS-GAP.md`'s shape, with a Source table, counts
that reconcile at least two ways, and one row per finding `R3-01`…`R3-10` in ID order. Each row
carries a disposition, a fix-kind (CODE / CLAIM CORRECTION / BOTH), the owning plan, the commit, the
artifact path and a provenance clause distinguishing grep-verified from statically reasoned. What
was not driven is recorded per finding, including the whole layer-3 block and the `SIGTERM`
behaviour. The absence of a cross-family adjudication is stated rather than left ambiguous. The
review's nine Verified-and-clean items are carried forward under a heading that closes them against
round 4. A lessons section records this round's own findings, not round 2's.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the review to the register and carry round 3's residue — including its two refusals</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP2.md, .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md</files>
  <read_first>
    - `06-REVIEW-GAP.md`'s appended wiring block — anchor:
      `/usr/bin/grep -n 'this file is now WIRED' .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md`.
      Read it. **That is the template**, including its opening disclaimer that nothing above the line
      was edited.
    - `deferred-items.md` — the entry format (`## DEF-06-NN-NN — title`, then `**Found during:**`),
      and the highest existing `DEF-` number, so round 3's entries continue the series without
      colliding. There are eleven entries after round 2.
    - The four round-3 summaries' **NOT-DRIVEN registers** — these are the source of the residue.
      Each plan was required to write one.
    - `06-REVIEW-GAP2.md`'s Verified-and-clean section and the planning note that three items are
      already tracked elsewhere and must **not** be re-opened: the ~23 remaining
      `printf … | grep -q` SIGPIPE-141 sites (already in the DEF register), CONF-04's Jellyfin half
      (Phase 7 entry criterion E6), and the three un-rotated secrets in `DEF-06-29-09`.
  </read_first>
  <action>
**(a) Append a wiring block to `06-REVIEW-GAP2.md`**, in the round-2 template's shape. It must:
  - **edit nothing above the appended line** — say so in the block's opening, as round 2's does. The
    review's finding text, its anchors and its frontmatter counts are the record of what was found
    on 2026-09-22 and stand as written, including where a later measurement refined them;
  - name the register by full path;
  - state the summary counts and how they reconcile;
  - name the plans that supplied the fixes (06-30, 06-31, 06-32, 06-33) and the plan that wrote the
    register (06-34);
  - name the `DEF-06-34-*` entries carrying the residue;
  - carry forward **one correction the planning found and the review did not**: R3-07's census
    sentence says `/usr/bin/grep -c` gives `assert_beet_invocation_contract` "exactly two hits", and
    it gives **four** — two code, two comments. The finding's conclusion is correct and unaffected;
    only the census is wrong. Record it the way round 2 recorded the GC-15/GC-17 label crossing: as
    a correction in band, not a silent fix and not a downgrade of the finding.

**(b) Add `DEF-06-34-*` entries to `deferred-items.md`** for round 3's residue. At minimum, one entry
each for:
  - **the two refusals**, which matter most because a refusal nobody wrote down reads as an
    oversight: (i) plan 06-32 refused the best-effort `/tmp/p6-mf.*` sweep the review floats under
    R3-09 — a prefix-gated `rm -rf` over names the run did not mint is a wider destructive reach than
    the finding justifies on a shared container `/tmp`; (ii) plan 06-33 refused the third-conjunct
    gate under R3-08, because it would be implied by the other two conjuncts and therefore vacuous,
    and refused to wire `assert_beet_invocation_contract` into the live path, recording the arm-
    polarity precondition at the site instead. Each entry states the refusal, its reason, and the
    condition under which it should be revisited;
  - **the raw exit-code-notice count in `quick-health-check.sh` is deliberately unpinned**, with the
    recipe to re-derive it and the reason writing it moves it — and the note that three earlier
    notices' delta sentences were deliberately left untouched as dated historical records, so a
    future reader does not "correct" three accurate statements into three wrong ones;
  - **the layer-3 block is undriven** (R3-03, R3-04) — reachable only from a live `--run`, proven
    offline, and therefore a candidate for the live pilot's evidence set;
  - **the `*"rm "*` token test in `self_test_fences` is weaker than a full destructive-program check**
    (R3-06), bounded by the fence texts being three short literals under version control, with the
    decision recorded that the cases were deliberately not widened;
  - **the `SIGKILL` residual** on both in-container traps (R3-09): widening to `INT TERM HUP` reduces
    the window and does not close it, and `SIGKILL` cannot be trapped.

**(c) Do not re-open what is already tracked.** The remaining `printf … | grep -q` sites, CONF-04's
Jellyfin half and `DEF-06-29-09`'s secrets each already have an owner. Reference them; do not create
duplicate entries.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
D=.planning/phases/06-tagger-configuration-and-dry-run
G=/usr/bin/grep

# (a) the review is wired, and nothing above the appended line changed. The second test is the
# strong one: every finding heading the review shipped with is still present and unedited.
test "$($G -cF '06-DISPOSITIONS-GAP2' $D/06-REVIEW-GAP2.md)" -ge 1
test "$($G -c '^### R3-' $D/06-REVIEW-GAP2.md)" -eq 10
test "$($G -cF 'findings:' $D/06-REVIEW-GAP2.md)" -eq 1
test "$($G -cF 'Verified-and-clean' $D/06-REVIEW-GAP2.md)" -ge 1
# the inherited census correction is carried in band
test "$($G -cF 'assert_beet_invocation_contract' $D/06-REVIEW-GAP2.md)" -ge 2

# (b) the residue is carried under its own ID series
test "$($G -c '^## DEF-06-34-' $D/deferred-items.md)" -ge 5
# the refusals are named as refusals, not as omissions
test "$($G -ciF 'refus' $D/deferred-items.md)" -ge 2
test "$($G -cF 'p6-mf' $D/deferred-items.md)" -ge 1
test "$($G -cF 'SIGKILL' $D/deferred-items.md)" -ge 1
test "$($G -cF 'R3-0' $D/deferred-items.md)" -ge 3

# (c) nothing already-owned was duplicated into a new entry
test "$($G -c '^## DEF-06-29-' $D/deferred-items.md)" -ge 1
    </automated>
  </verify>
  <done>
`06-REVIEW-GAP2.md` carries an appended wiring block naming the register by full path, stating the
counts and how they reconcile, naming the four fix plans and this one, naming the `DEF-06-34-*`
entries, and recording the inherited correction to R3-07's census without downgrading the finding.
All ten finding headings and the frontmatter above the appended line are unedited. `deferred-items.md`
carries at least five `DEF-06-34-*` entries, including both refusals stated as refusals with their
reasons and revisit conditions, the deliberately-unpinned raw count with its recipe, the undriven
layer-3 block, the bounded `rm ` token test and the `SIGKILL` residual. Nothing already owned
elsewhere was duplicated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tell ROADMAP and STATE the truth about round 3 — by hand, and without closing anything</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - **`.planning/ROADMAP.md` Phase 6.** Anchors, resolved with `/usr/bin/grep -n`:
      `### Phase 6: Tagger Configuration and Dry Run`, the plan-count line
      `**Plans**: 29 plans in 13 waves`, the round-2 wave headers
      `**Wave 13 — GAP CLOSURE — ROUND 2**`, the last plan checklist line
      `06-29-PLAN.md`, the `**Phase 6 disposition:**` paragraph and its
      `**Amended 2026-09-22 by plan 06-29` continuation, and the Phase 6 row in the milestone table
      (find it with `/usr/bin/grep -n '| 6. Tagger Configuration and Dry Run |'`).
      **Round 2 used Waves 10-13; round 3 continues at Wave 14 in the same style.**
    - **`.planning/STATE.md` § Current Position** — anchor `## Current Position`. Read the whole
      field, including the round-3 block at its end recording why re-verification was withheld.
    - `06-DISPOSITIONS-GAP2.md` as written by task 1, for the counts to quote.
  </read_first>
  <action>
⛔ **Edit both files by hand with Edit/Write. Do NOT run `gsd-sdk query state.*` or any `roadmap.*`
verb.** In this session those verbs truncated STATE.md sentences mid-line and blanked the ROADMAP
Phase 6 Notes cell on **all three** invocations. Run `git diff .planning/STATE.md` and
`git diff .planning/ROADMAP.md` before committing and confirm the only changes are the ones this
task asks for.

**(a) ROADMAP — the wave blocks.** **These already exist**: the planner wrote the Wave 14 and
Wave 15 blocks after the Wave 13 block, with every plan line **unchecked** (`- [ ]`). Your job is to
**tick each of the five** to `- [x]` as its plan lands, and to verify the block text still describes
what the plans actually did — if a plan's scope moved during execution, correct its line here rather
than leaving a description the summaries contradict. Do **not** re-add the blocks. For reference,
the shape the planner used is:

  `**Wave 14 — GAP CLOSURE — ROUND 3** *(added 2026-09-23; planned against `06-REVIEW-GAP2.md`, a
  code review of round 2's own gap-closure changes. These four plans own four different files and
  run in parallel.)*`

  with one `- [x]` line per plan 06-30 … 06-33, each naming what it closed in the round-2 format
  (`…; closes R3-01 and R3-05`), then:

  `**Wave 15 — GAP CLOSURE — ROUND 3** *(blocked on Wave 14)*`

  with the `06-34-PLAN.md` line.

**(b) ROADMAP — the plan-count line.** **Already done by the planner**: it reads
**34 plans in 15 waves** with a round-3 parenthetical. Verify it, do not rewrite it. If the round
ended with a different number of plans than five, correct both the count and the wave count here —
that is the one circumstance in which this line moves.

**(c) ROADMAP — amend the disposition paragraph**, appending after the round-2 amendment. It must
say, in this order and this plainly:
  - a third gap-closure round ran against `06-REVIEW-GAP2.md`; **zero Critical**; all ten findings
    carry a disposition in `06-DISPOSITIONS-GAP2.md`, with the counts;
  - the round's character: **the dominant defect class was a claim broader than its code**, and for
    several findings the honest fix was to narrow the sentence rather than widen the code — each
    finding's register row says which;
  - the two refusals, by name;
  - **"This is a record, not a re-close, and no re-verification was performed."** Plan 06-34 ran no
    `/gsd-verify` and claims no verification result. `REQUIREMENTS.md` is untouched,
    `- [ ] **CONF-04**` stands, its Jellyfin half is still OPEN and entry criterion **E6** still owns
    the discharge. No requirement checkbox moved and **the phase is not declared complete**;
  - the recommendation: `/gsd-verify 06` next.

**(d) ROADMAP — the Phase 6 row in the milestone table.** Append to the Notes cell, matching the
existing voice, keeping every existing sentence. Keep the plan-count cell consistent with (b).
**Do not blank or rewrite the cell** — append. This cell is the exact thing the SDK verb destroyed
three times, so re-read it after editing.

**(e) STATE.md § Current Position.** Update the round-3 block that currently says planning is in
progress: record that **ROUND 3 IS DONE — 2026-09-23**, that plans 06-30..06-33 closed all ten
findings and 06-34 dispositioned them with the counts, and that `06-REVIEW-GAP2.md` is WIRED. Then:
  - record the instrument baselines **as measured at HEAD after this round**, not carried forward
    from this plan's text: `phase06-oracle.sh --self-test` exit 0 and its **pinned** case count
    (the pin is now `ST_PLANNED_CASES`, so quote the pin and say it is gated),
    `check-beets-config.sh --self-test` exit 0 / 7 cases, `phase06-incremental-control.sh
    --self-test` exit 0, `bash -n` clean on all four, and `sh -n` clean on the two extracted
    in-container programs;
  - keep the standing warning in place and restate it for this round: **re-verification has not been
    performed**, round 3 claims no verification result, and "round 3 complete" must not be read as
    "phase complete";
  - keep **Still open, unchanged by round 3:** CONF-04's Jellyfin half under entry criterion **E6**;
  - add one line on whether a round 4 review is warranted. The honest answer, and the one to write,
    is that round 3 produced **zero Critical** and that most of its fixes were claim corrections, so
    the next step is `/gsd-verify 06` — **and** that a fourth review of round 3's own diff is the
    same discipline that caught both previous rounds, so it should be a deliberate decision rather
    than an omission. Do not decide for the operator; state both and name it as their call.

**(f) Do not touch anything else in either file.** In particular: no `Progress:` line, no
requirement checkbox, no phase status word, and nothing outside Phase 6.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
G=/usr/bin/grep
R=.planning/ROADMAP.md
S=.planning/STATE.md

# (a) both new wave blocks exist, in the round-2 style
test "$($G -cF 'Wave 14 — GAP CLOSURE — ROUND 3' $R)" -eq 1
test "$($G -cF 'Wave 15 — GAP CLOSURE — ROUND 3' $R)" -eq 1
for p in 06-30 06-31 06-32 06-33 06-34; do
  test "$($G -cF "$p-PLAN.md" $R)" -ge 1
done

# (b) the plan count moved and the round-2 blocks were not disturbed
test "$($G -cF '29 plans in 13 waves' $R)" -eq 0
test "$($G -cF '34 plans in 15 waves' $R)" -eq 1
test "$($G -cF 'Wave 13 — GAP CLOSURE — ROUND 2' $R)" -eq 1
test "$($G -cF '06-29-PLAN.md' $R)" -ge 1

# (c) the record-not-a-re-close language is present and nothing was closed
test "$($G -cF '06-DISPOSITIONS-GAP2' $R)" -ge 1
test "$($G -ciF 'record, not a re-close' $R)" -ge 2
test "$($G -cF 'E6' $R)" -ge 1

# (d) THE NOTES CELL SURVIVED. This is the cell the SDK verb blanked three times: the Phase 6 row
# must still be long, and must still carry its round-1 and round-2 sentences.
test "$($G -cF '| 6. Tagger Configuration and Dry Run |' $R)" -eq 1
test "$($G -F '| 6. Tagger Configuration and Dry Run |' $R | wc -c | tr -d ' ')" -ge 3000
test "$($G -cF 'Round 2 COMPLETE' $R)" -ge 1

# CONF-04 IS NOT CLOSED. The checkbox is untouched in REQUIREMENTS.md and unstaged there.
# -e is load-bearing: the pattern starts with `-`, and BSD grep parses a leading `-` as an option
# and exits 2 with a usage message, which under `set -e` would abort this block for the wrong reason.
test "$($G -cF -e '- [ ] **CONF-04**' .planning/REQUIREMENTS.md)" -eq 1
test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)"

# (e) STATE records the round as done without claiming a verification result
test "$($G -cF 'ROUND 3 IS DONE' $S)" -ge 1
test "$($G -cF 'ST_PLANNED_CASES' $S)" -ge 1
test "$($G -ciF 're-verification has not been performed' $S)" -ge 1
test "$($G -cF 'E6' $S)" -ge 1

# (f) the two lines the SDK verbs historically corrupt are untouched by this plan
test -z "$(git diff -U0 -- $S | $G '^[+-].*Progress: \[' || true)"

# The files still parse as the documents they are: STATE.md keeps its frontmatter and its heading.
test "$($G -c '^## Current Position' $S)" -eq 1
    </automated>
  </verify>
  <done>
ROADMAP carries Wave 14 and Wave 15 blocks in round 2's style with all five plans listed, a
plan-count line reading 34 plans in 15 waves, and a disposition amendment that states the counts, the
round's character, both refusals and — twice — that this is a record and not a re-close. The Phase 6
milestone Notes cell was appended to and not rewritten; it still carries its round-1 and round-2
sentences and is still long. STATE § Current Position records ROUND 3 IS DONE with the measured
instrument baselines including the new gated pin, keeps the standing re-verification warning, keeps
CONF-04's Jellyfin half open under E6, and states the round-4 question as the operator's call
without deciding it. `REQUIREMENTS.md` is untouched and unstaged, no `Progress:` line moved, and both
edits were made by hand with `git diff` read before committing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the review → the next context window | a finding recorded where the next phase does not read is a finding nobody owns; the register and the wiring block are the crossing |
| a disposition register → a reader who trusts it | ten undifferentiated `FIXED` rows would reproduce, one level up, the exact over-claim the review is about |
| the GSD SDK's state/roadmap verbs → STATE.md and ROADMAP.md | measured, repeatedly: truncated sentences and a blanked Notes cell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R3REC | Repudiation (a round closed without a per-finding record) | `06-DISPOSITIONS-GAP2.md` | mitigate | one row per finding in ID order, each with disposition, fix-kind, plan, commit, artifact and provenance |
| T-06-R3OVER | Repudiation (a register that reads as ten complete fixes when several were claim corrections) | the fix-kind column and the not-driven notes | mitigate | CODE / CLAIM CORRECTION / BOTH per row; undriven surfaces named per finding |
| T-06-R3REF | Repudiation (a deliberate refusal read as an oversight and re-opened next round) | the two refusals | mitigate | `DEF-06-34-*` entries stating each refusal, its reason and its revisit condition |
| T-06-R3SDK | Tampering (an automated verb corrupting the files it updates) | STATE.md, ROADMAP.md | mitigate | hand edits only; `git diff` on both before commit; the Notes cell length and the `Progress:` line both asserted |
| T-06-34-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- **No `gsd-sdk query state.*` and no `roadmap.*` verb is run.** Both files are edited by hand and
  `git diff` is read on each before committing.
- **No `/gsd-verify` is run and no verification result is claimed.**
- **No requirement checkbox moves.** `REQUIREMENTS.md` is untouched and must show clean in
  `git status`. CONF-04 stays OPEN on its Jellyfin half under Phase 7 entry criterion **E6**.
- **The phase is not declared complete.**
- `06-REVIEW-GAP2.md` is appended to only; all ten finding headings and its frontmatter are unedited.
- No estate contact, no `git push`, no host `git pull`, no `--run`, no `--arm`.
- This plan must not touch `scripts/setup-neocortex-memory.sh`,
  `stacks/selfhosted/neocortex-memory/` or `stacks/selfhosted/agentic-os/`, and touches no file
  under `scripts/`.
</verification>

<success_criteria>
- All ten `R3-*` findings carry an explicit disposition **and an explicit fix-kind** in one register
  whose counts reconcile.
- `06-REVIEW-GAP2.md` is WIRED, with nothing above the appended line edited, and carries the
  inherited correction to R3-07's census.
- Round 3's residue — including both refusals, the unpinned raw count, the undriven layer-3 block
  and the `SIGKILL` residual — is carried by name in `deferred-items.md`.
- The review's nine Verified-and-clean items are closed against round 4 by citation.
- ROADMAP and STATE describe round 3 accurately, by hand, and close nothing.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-34-SUMMARY.md` when done.
State in it, explicitly: that no verification was run, that no requirement checkbox moved, that
CONF-04 remains OPEN under E6, and that the phase is not declared complete.
</output>
