---
phase: 06-tagger-configuration-and-dry-run
plan: 17
type: execute
wave: 8
depends_on: ["06-16"]
gap_closure: true
files_modified:
  - scripts/check-music-consumers.sh
  - scripts/quick-health-check.sh
autonomous: true
requirements: [CONF-04]
closes_findings: [WR-03]

must_haves:
  truths:
    - "`check-music-consumers.sh` cannot print its green banner while any CONF-04 artist row sits at a recorded baseline rather than at target — the banner is gated BELOW the pending check, not above it"
    - "The pending state has a machine-readable signal: exit 3, documented as measured-but-not-at-target, distinct from 0 (green) and 1 (a measured failure) — so a post-Phase-7 regression back to baseline can no longer exit 0"
    - "CONF-04's three-state distinction survives: pending is still not red, is still not summed with the MA verdict, and the Jellyfin half is still OPEN — this changes how pending is reported to machines, not whether CONF-04 is closed"
    - "`quick-health-check.sh`'s fold-in understands 3 as its own condition: not green, not the generic BROKEN arm, and it names CONF-04 rather than reporting an unexplained non-zero exit"
    - "Both new branches are DRIVEN: exit 3 is produced by the live estate at its current baseline, and the green path is produced by a controlled forcing of the pending counters to zero which is then reverted"
  artifacts:
    - path: "scripts/check-music-consumers.sh"
      provides: "the documented third exit code and the gated green banner"
      contains: "exit 3"
    - path: "scripts/quick-health-check.sh"
      provides: "the fold-in arm that reports 3 as CONF-04 pending, not as BROKEN"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-17-conf04-exit3.txt"
      provides: "both driven transcripts and the fold-in output"
  key_links:
    - from: "scripts/check-music-consumers.sh exit path"
      to: "scripts/quick-health-check.sh fold-in at :1954-2007"
      via: "the propagated remote exit status, with a dedicated arm for 3"
      pattern: "CONSUMERS_RC.*-eq 3"
---

<objective>
Close WR-03, the one Warning `06-VERIFICATION.md` escalated into its Key Link table as PARTIAL: `check-music-consumers.sh` prints `✅ Both music consumers see the pinned albums through the NFS export` and exits **0** while CONF-04 is measurably open, because `warn()` prints and never touches `FAILURES`.

Purpose: the three-state design (pending ≠ red ≠ green) is right and is well argued in band. The defect is that the third state is invisible to the only machine-readable output the script has. `quick-health-check.sh` folds this script in and propagates its status, so a regression in which `PreferNonstandardArtistsTag` reverts to `false` AFTER Phase 7 discharges it would land the rows back on their baseline and exit 0 — a regression detector reporting the regression in yellow text nobody's tooling reads. The D-34 option read at `:1221`/`:1225` in the same file already does it right, with `jellyfin_fail`; this makes the artist rows consistent with it without collapsing pending into red.

Output: a documented third exit code, a green banner that can no longer print over a non-zero pending count, a fold-in that understands the new status, and both branches driven against the live estate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@scripts/check-music-consumers.sh
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: A documented third exit code, with the green banner gated below it</name>
  <files>scripts/check-music-consumers.sh</files>
  <read_first>
    - `scripts/check-music-consumers.sh:1296-1310` — `FAILURES`, `fail()`, `pass()`, `warn()` (`warn` prints and returns; it touches no counter) and `info()`, plus the scoped sub-counters `EXPORT_FAILURES` / `MA_FAILURES` / `JELLYFIN_FAILURES` and their `export_fail` / `ma_fail` / `jellyfin_fail` wrappers at `:1310-1316`. Note that `jellyfin_fail` and `ma_fail` DO increment `FAILURES`; the pending branches deliberately do not.
    - `scripts/check-music-consumers.sh` — the Jellyfin pending branch. Locate it with `grep -n JELLYFIN_ARTIST_PENDING scripts/check-music-consumers.sh` and read the whole `if/elif/else` ladder it sits in, including the `$ATARGET` / `$ABASE` comparison and the multi-line explanation about `PreferNonstandardArtistsTag` being a PROBE-TIME option that a Default-mode refresh does not re-run.
    - `scripts/check-music-consumers.sh` — the MA pending branch. Locate it with `grep -n MA_ARTIST_PENDING scripts/check-music-consumers.sh` and read its ladder, including the `$AMABASE` comparison and the CHARACTERISED 2026-09-21 paragraph ruling out the delimiter explanation.
    - `scripts/check-music-consumers.sh` — the exit path. Locate it with `grep -n 'Both music consumers see the pinned albums' scripts/check-music-consumers.sh` and read from the `--baseline` early exit through the `FAILURES -gt 0 → exit 1` block, the pending-count warning block, and the green banner. **The green banner currently prints AFTER the pending warning with no gate between them; that ordering is the defect.**
    - `scripts/check-music-consumers.sh` — the usage/header block at the top of the file (`sed -n '1,60p'`), specifically any documented exit-code table and the `--baseline` contract, and the line the header marks `KEEP THIS HEADING LITERAL AND NEVER RENUMBER IT SILENTLY` (`grep -n "KEEP THIS HEADING LITERAL" scripts/check-music-consumers.sh`) — `quick-health-check.sh` anchors on `📊 6. Summary` and renumbering it breaks a consumer in a different file, silently.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:203-237` — WR-03 verbatim, with its proposed fix.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md:48` and `:72` — Truth 4 (CONF-04 correctly recorded as OPEN, two verdicts never summed) and the PARTIAL Key Link row. **CONF-04 is NOT being closed by this plan** and nothing here may read as closing it.
    - `.planning/ROADMAP.md` entry criterion E6 (`grep -n "E6\." .planning/ROADMAP.md`) — where the Jellyfin half discharges and the two measurements Phase 7 must take.
  </read_first>
  <action>
Three edits to `scripts/check-music-consumers.sh`. Do not touch the pending branches' measurement logic, their thresholds, their baselines or their explanatory text — the verdicts stay exactly as measured.

**Edit 1 — gate the green banner.** In the exit path, insert a terminating branch BETWEEN the existing pending-count warning and the green banner:

- Keep the `--baseline` early exit first and unchanged (it always exits 0 by contract; say so in a comment if it does not already, because `--baseline` must remain able to record a before-state).
- Keep `FAILURES -gt 0 → exit 1` next and unchanged. A measured failure outranks a pending row; if both are present the run is red.
- Then, when `JELLYFIN_ARTIST_PENDING + MA_ARTIST_PENDING` is greater than zero: print the existing yellow `CONF-04 IS NOT CLOSED` block verbatim — the three lines naming the counts, the "Reported, not asserted" qualifier and the pointer to sections 4b/4c and `beets.md` — and then `exit 3`. The green banner must be BELOW this branch and therefore unreachable while the pending count is non-zero.
- The green banner keeps its exact current text so no consumer that greps for it breaks.

**Edit 2 — document exit 3 in the header, in the file's own register.** Add it to the exit-code documentation with a one-line meaning — *measured, but not at target; never green* — and a short paragraph stating three things explicitly, because the next reader will otherwise collapse the distinction this file exists to preserve: (a) 3 is NOT a failure, the rows were read successfully and hold the value they were recorded holding; (b) 3 is NOT green, and no tooling may treat it as success; (c) the Jellyfin and MA verdicts are still separate and are NEVER summed into one CONF-04 answer — the pending COUNT is summed only to decide whether any row is off target, which is a different question from whether CONF-04 is closed.

**Edit 3 — name the two counters at the point they are incremented.** Beside each of the two `warn` calls in the pending branches, add a one-line comment stating that this branch now determines the process exit status via the `exit 3` gate, so a future edit that changes the branch changes the exit code. That is the cross-file contract this file already uses comments to protect; make this one explicit rather than implicit.

**Do not renumber any `📊 N.` heading.** `quick-health-check.sh` anchors on `📊 6. Summary` and the file's own header forbids it.
  </action>
  <acceptance_criteria>
    - In `scripts/check-music-consumers.sh`, the line number of `CONF-04 IS NOT CLOSED` is LESS than that of the terminating `exit 3`, which is LESS than that of `Both music consumers see the pinned albums`
    - `exit 3` is present in the exit path; the green banner string is byte-unchanged
    - The header documents exit 3 as measured-but-not-at-target / never green, and contains all three clauses: not a failure, not green, and the two consumer verdicts are NEVER summed
    - `📊 6. Summary` is byte-present and unrenumbered
    - Each of the two pending branches carries a comment stating that it now determines the process exit status
    - `bash -n` and `shellcheck -S warning` exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/check-music-consumers.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; grep -q "exit 3" "$f" || { echo "exit 3 is absent"; exit 1; }; p=$(grep -n "CONF-04 IS NOT CLOSED" "$f" | tail -1 | cut -d: -f1); e=$(grep -n "^  exit 3\|^ *exit 3" "$f" | tail -1 | cut -d: -f1); g=$(grep -n "Both music consumers see the pinned albums" "$f" | tail -1 | cut -d: -f1); [ -n "$p" ] && [ -n "$e" ] && [ -n "$g" ] || { echo "could not locate the three exit-path landmarks"; exit 1; }; [ "$p" -lt "$e" ] && [ "$e" -lt "$g" ] || { echo "ordering wrong: pending($p) must precede exit 3($e) which must precede the green banner($g)"; exit 1; }; grep -q "📊 6. Summary" "$f" || { echo "the anchored summary heading was renumbered"; exit 1; }; grep -qiE "measured.*not at target|not at target.*never green" "$f" || { echo "exit 3 is not documented in the header"; exit 1; }; grep -qE "never (be )?summed|NEVER summed" "$f" || { echo "the never-summed clause is missing from the exit-3 documentation"; exit 1; }; echo "06-17 task 1 OK"'</automated>
  </verify>
  <done>
    The exit path runs `--baseline` → `FAILURES -gt 0 → exit 1` → pending-count warning → `exit 3` → green banner, in that source order, with the green banner unreachable while the pending count is non-zero. Exit 3 is documented in the header as measured-but-not-at-target, never green, with the not-a-failure / not-green / never-summed clauses all stated. The `📊 6. Summary` anchor and the green banner text are byte-unchanged. `bash -n` and `shellcheck -S warning` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teach the health entry point that 3 means CONF-04 pending, and drive both branches</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-17-conf04-exit3.txt</files>
  <read_first>
    - `scripts/quick-health-check.sh:1946-2007` — the music-consumers fold-in IN FULL: the `ssh -n` with the Linux-side `timeout`, `CONSUMERS_RC=$?` captured before any pipe, the ANSI strip via bash `$'…'` quoting (GNU `\x1b` is not available on macOS), the empty-output branch, the 124 branch, the `-eq 0` branch with its `📊 6. Summary` anchor guard, and the final `else` arm that prints `❌ BROKEN (check-music-consumers.sh exit $CONSUMERS_RC)`. **Exit 3 currently lands in that BROKEN arm** — non-green, which is right, but labelled as an instrument failure, which is wrong.
    - `scripts/quick-health-check.sh:39-64` — the SECOND exit-code notice, which is the one covering this fold-in, and the convention that a new fatal condition or a changed condition at an existing site gets its own notice carrying the shared phrase.
    - `scripts/quick-health-check.sh:145-165` — the notice form used for *"NO NEW BLOCK, A NEW FATAL CONDITION AT AN EXISTING SITE"*, which is the shape this change needs.
    - `scripts/quick-health-check.sh` as plan 06-16 left it — read the ELEVENTH notice it added, because this plan adds the TWELFTH and must continue the measured grep-count bookkeeping rather than restarting it.
    - `scripts/check-music-consumers.sh` as task 1 left it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-14-SUMMARY.md` — the recorded current baseline for the three Jellyfin rows (0/4, 1/2, 1/2) and the MA row, so the expected pending count in the driven run is known before the run rather than read off it.
    - `README.md` § Health Checks — the three rules.
  </read_first>
  <action>
**Edit — a dedicated fold-in arm for 3.** Between the `-eq 0` arm and the final `else` in `scripts/quick-health-check.sh`'s music-consumers fold-in, add `elif [ "$CONSUMERS_RC" -eq 3 ]`. It must:

- Print a NOT-GREEN marker, not a tick and not `❌ BROKEN`. Use the file's existing `⚠️` register, the same one the UNKNOWN branches use, and say in one line that CONF-04 is measured and open, not that the audit failed.
- Echo the audit's own `CONF-04 IS NOT CLOSED` lines from `$CONSUMERS_OUT` so the operator sees the counts without re-running the script.
- Still apply the `📊 6. Summary` anchor guard exactly as the `-eq 0` arm does, and report an absent anchor as UNKNOWN — a changed heading must not be able to hide behind the new arm.
- Set `EXIT_CODE=1`. **This is the load-bearing half.** The estate's entry point exits 0 when healthy and non-zero on any violation; a measured-and-open requirement is not healthy. State in the branch comment that CONF-04 pending deliberately makes the whole health check non-zero for as long as it is open, and that this is what makes a post-Phase-7 regression back to baseline detectable by tooling rather than only by a reader.
- Leave the final `else` arm intact for genuinely unexpected statuses, with its `❌ BROKEN` text unchanged.

**Add the TWELFTH `EXIT-CODE BEHAVIOUR CHANGED` notice**, in the *new fatal condition at an existing site* form at `:145-165`, naming: the music-consumers fold-in now exits this script 1 on a CONF-04 pending status (3) that previously landed in the BROKEN arm — same non-zero verdict, correct label — and stating that it will stay non-zero until Phase 7 discharges CONF-04's Jellyfin half (ROADMAP entry criterion E6). Carry the measured grep-count bookkeeping forward from the ELEVENTH notice: measure both counts before and after, state them, do not round.

**Then drive both branches and record everything in `artifacts/06-17-conf04-exit3.txt`.**

1. **Exit 3, from the live estate as it stands.** Push and pull so the host's `/mnt/fast/stacks` carries the changed script, then run `ssh root@172.16.1.159 'cd /mnt/fast/stacks && bash scripts/check-music-consumers.sh'` and record the full transcript plus the exit status. The expected result is exit 3 with the pending counts matching `06-14-SUMMARY.md`'s recorded baseline and NO green banner in the output. Assert the absence of the banner explicitly — that absence is the fix.
2. **Green, forced and reverted.** The pending count cannot be driven to zero by measurement without a real re-probe, which is Phase 7's job and must not happen here. Drive it instead by a controlled local perturbation: temporarily set both pending counters to zero immediately before the exit path, run, require exit 0 WITH the green banner, then restore the file and assert its sha256 matches the pre-test value. Record both transcripts and both hashes. Without this control the exit-3 gate is only proven to block, not proven to release.
3. **The fold-in.** Run `bash scripts/quick-health-check.sh` from the workstation and record the music-consumers block's output verbatim. Require the new `⚠️` CONF-04 line, require it NOT to read `❌ BROKEN`, and require `EXIT_CODE` to be non-zero. **Name the pre-existing `interpolated-host-path inventory MOVED: expected=12, found=13` red in the artifact** — the top-level exit code is non-discriminating because of it, so the artifact must say which reds are pre-existing, which belong to plan 06-16, and which are this plan's.
4. **State plainly what has and has not changed about CONF-04.** One short paragraph: the Jellyfin half is still OPEN, the MA half is still discharged, the two verdicts are still recorded separately and are not summed, `REQUIREMENTS.md`'s `- [ ] CONF-04` stays unchecked, and ROADMAP entry criterion E6 still owns the discharge. What changed is that the open state now has an exit code.

Do not edit `REQUIREMENTS.md`, `ROADMAP.md`'s phase-status row, or any CONF-04 verdict in this plan.
  </action>
  <acceptance_criteria>
    - `scripts/quick-health-check.sh` contains an `elif [ "$CONSUMERS_RC" -eq 3 ]` arm which prints a `⚠️` CONF-04 line, does NOT print `❌ BROKEN`, applies the `📊 6. Summary` anchor guard, and sets `EXIT_CODE=1`
    - The generic `❌ BROKEN (check-music-consumers.sh exit $CONSUMERS_RC)` arm still exists for unexpected statuses
    - `grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh` returns `12`
    - `artifacts/06-17-conf04-exit3.txt` contains a live remote transcript with exit status 3, the pending counts matching `06-14-SUMMARY.md`'s baseline, and the green banner string provably ABSENT from that transcript
    - It contains a forced-zero-pending transcript exiting 0 WITH the banner, plus equal pre/post sha256 of the restored script
    - It contains the `quick-health-check.sh` music-consumers block output showing the new line, and names the pre-existing `interpolated-host-path` red
    - It contains a paragraph stating CONF-04's Jellyfin half is still OPEN, the MA half is still discharged, the verdicts are not summed, and E6 still owns the discharge
    - `git diff --name-only` does not include `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/quick-health-check.sh; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-17-conf04-exit3.txt; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S error "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -qE "CONSUMERS_RC\" -eq 3" || { echo "no dedicated arm for exit 3"; exit 1; }; h=$(grep -c "^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED" "$f"); [ "$h" -eq 12 ] || { echo "expected 12 notice headers, found $h"; exit 1; }; printf "%s\n" "$s" | grep -q "BROKEN (check-music-consumers.sh exit" || { echo "the generic BROKEN arm was removed"; exit 1; }; test -s "$a" || { echo "artifact missing"; exit 1; }; grep -qiE "exit (status )?3|rc=3" "$a" || { echo "the exit-3 run is not recorded"; exit 1; }; grep -q "CONF-04 IS NOT CLOSED" "$a" || { echo "the pending block is not in the transcript"; exit 1; }; grep -qiE "sha256" "$a" || { echo "the forced-green control has no restore-and-assert hash"; exit 1; }; grep -qiE "interpolated-host-path" "$a" || { echo "the pre-existing red is not named"; exit 1; }; grep -qiE "still OPEN|stays OPEN|remains OPEN" "$a" || { echo "the CONF-04 status paragraph is missing"; exit 1; }; git diff --name-only | grep -q "REQUIREMENTS.md" && { echo "REQUIREMENTS.md must not change in this plan"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/check-music-consumers.sh|^ M scripts/quick-health-check.sh" || true); [ -z "$d" ] || { echo "test debris:"; echo "$d"; exit 1; }; echo "06-17 task 2 OK"'</automated>
  </verify>
  <done>
    `quick-health-check.sh` has a dedicated `CONSUMERS_RC -eq 3` arm that prints a CONF-04-naming `⚠️` line (never `❌ BROKEN`), applies the `📊 6. Summary` anchor guard, and sets `EXIT_CODE=1`; the generic BROKEN arm survives for unexpected statuses; the TWELFTH exit-code notice is present with measured counts. The artifact holds a live exit-3 transcript with the green banner provably absent, a forced-green control with before/after sha256 and a restored file, the fold-in output showing the new line, the pre-existing reds named, and a paragraph stating that CONF-04's Jellyfin half is still open and the two verdicts are still not summed. `REQUIREMENTS.md` is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `check-music-consumers.sh` exit status → `quick-health-check.sh` → any future scheduler or alerting | The exit code is the ONLY machine-readable output. Yellow text is invisible to everything downstream. |
| workstation → LXC 100 root ssh → Music Assistant and Jellyfin HTTP APIs | Credentials for both live at `/mnt/fast/secrets/` on LXC 100, which is why the audit is host-resident. |
| section headings (`📊 6. Summary`) → the fold-in's grep anchor | A cross-file contract enforced only by a comment. Renaming breaks a consumer in a different file, silently. |
| this public repo → the world | The audit reads two sets of credentials. No value from either may enter a transcript or an artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-80 | Repudiation | A green banner and exit 0 printed over a measurably open CONF-04, so a post-Phase-7 regression is undetectable by tooling | mitigate | The `exit 3` gate sits above the banner in source order; the verify command asserts that ordering by line number, and the live run asserts the banner is absent. |
| T-06-81 | Spoofing | Exit 3 read downstream as a success because it is "not a failure" | mitigate | Documented in the header as *measured, but not at target; never green*, and the fold-in sets `EXIT_CODE=1`, so the estate entry point is non-zero for as long as CONF-04 is open. |
| T-06-82 | Tampering | The three-state distinction collapsed — pending folded into red, or the two consumer verdicts summed into one CONF-04 answer | mitigate | The pending branches' measurement logic, thresholds and text are untouched; the header's never-summed clause is asserted present by the verify command; `REQUIREMENTS.md` and the ROADMAP status row are explicitly out of scope. |
| T-06-83 | Repudiation | The `📊 6. Summary` heading renumbered, silently breaking the fold-in's anchor | mitigate | Heading asserted byte-present by the verify command; the new arm applies the same anchor guard and reports an absent anchor as UNKNOWN. |
| T-06-84 | Tampering | The forced-green control left in the tracked file | mitigate | Pre/post sha256 recorded in the artifact and a `git status --porcelain` debris check in the verify command. |
| T-06-85 | Information Disclosure | An MA or Jellyfin credential reaching the committed transcript | mitigate | The audit already reports credential presence without value (`${VAR:+set}${VAR:-unset}`, confirmed sound in `06-REVIEW.md` § Notes). Transcripts are captured as-is with no added debug output; no `set -x`. |
| T-06-86 | Denial of Service | The audit hanging and wedging the health check | accept | Already bounded Linux-side at `timeout $REMOTE_TIMEOUT` with a dedicated 124 branch; unchanged by this plan. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `bash -n` and `shellcheck` clean on both scripts.
- Source order in `check-music-consumers.sh`: pending warning → `exit 3` → green banner.
- A live run on the current estate exits 3 with no green banner.
- A forced-zero pending count exits 0 with the banner, and the file is restored byte-identically.
- `quick-health-check.sh` reports the CONF-04 line and a non-zero `EXIT_CODE`, and `grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED'` returns 12.
</verification>

<success_criteria>
- CONF-04's open state has a machine-readable signal.
- Pending, red and green remain three distinct states.
- CONF-04 is not closed, not summed, and not marked complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-17-SUMMARY.md` when done. Record the measured pending counts at the time of the run; plan 06-21 cites them and ROADMAP entry criterion E6 is updated against them.
</output>
