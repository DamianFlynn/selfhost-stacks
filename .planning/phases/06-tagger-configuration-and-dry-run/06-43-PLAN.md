---
phase: 06-tagger-configuration-and-dry-run
plan: 43
type: execute
wave: 21
depends_on: ["06-42"]
gap_closure: true
requirements: [CONF-04]
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt
autonomous: false

must_haves:
  truths:
    - "The round's outcome is stated as exactly one of three named branches, from the measured numbers, with no averaging across rows and no verdict that both halves of CONF-04 are summed into"
    - "The forbidden refresh mode was not issued anywhere in the round, proved mechanically across every artifact the round produced"
    - "The operator has seen the measured outcome and chosen what it means for CONF-04 before any document is edited"
    - "The BRANCH value is recomputable by a script from its four recorded inputs — the three row verdicts, the SAFETY line, and the instrument's two Jellyfin counters — and a written value that disagrees with the recompute fails the task"
    - "BRANCH: A is unreachable without an independent corroborating instrument run that actually looked"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt"
      provides: "the branch verdict, the three per-row numbers that produced it, the round-wide forbidden-mode audit, and the operator's recorded answer"
      contains: "BRANCH:"
      min_lines: 40
  key_links:
    - from: "artifacts/06-43-conf04-verdict.txt BRANCH: line"
      to: "plans 06-44 and 06-45"
      via: "both downstream plans read this one line and write the matching text"
      pattern: "BRANCH: (A|B|PARTIAL)"
---

<objective>
State what the measurement means, in one machine-readable line, and get the operator's decision
before any record is edited.

Rounds 1 through 4 of this phase's gap closure repeatedly produced a claim broader than the code
beside it. This plan exists so round 5 does not: the branch is computed from the three measured
numbers in `06-42`, written once, and everything downstream reads it rather than re-deriving it.

Three branches, and all three are legitimate outcomes:

  **A — CONF-04's Jellyfin half CLOSED.** All three pinned rows read `AT-TARGET`: 4, 2 and 2
  distinct browseable `ArtistItems` entities with distinct Ids, zero `;` in any entity Name.

  **B — the mtime hypothesis is DISPROVEN.** All three rows read `AT-BASELINE` after a refresh that
  Jellyfin's own log confirms ran. The touch did not cause a re-probe, or the re-probe did not
  produce entities. Recorded as a reproducible negative and handed back to the operator with the
  two original options. **This is not a failure of the plan set.**

  **PARTIAL — some rows moved and some did not.** Most plausibly rows 2 and 3 reach target while
  row 1 (ARTPOP) does not: its whole album is part of the 30 library items that carry a populated
  `Artists` string list with ZERO linked artist entities, which is already owned as Phase 7 entry
  criterion E5 and may require artist-entity creation that a per-file refresh does not perform.
  CONF-04 stays OPEN on a PARTIAL, and the residue is named.

Purpose: one honest verdict, chosen by the operator, before the documents move.
Output: one committed artifact, `06-43-conf04-verdict.txt`.

⛔ THE HARD FENCE. On branch B or PARTIAL the correct response is the recorded negative and the
operator's choice. It is NOT another refresh, NOT a wider refresh mode, and NOT the aggressive
per-item "replace all metadata" mode, which is forbidden estate-wide and is not reachable from any
branch of this round.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-41-conf04-reprobe-drive.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt
@.planning/phases/06-tagger-configuration-and-dry-run/06-42-SUMMARY.md
@scripts/check-music-consumers.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute the branch from the measured numbers, and audit the whole round for the forbidden mode</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt`
      Sections C, E, G and K — the three per-row verdicts (and the delimited
      `--- ROW VERDICTS BEGIN/END ---` block that carries them in parseable form), the census
      delta, the post-refresh `zfs diff` and the single `SAFETY:` line.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt`
      — the deployed unmodified instrument's `INSTRUMENT RUN:`, `JF_AT_TARGET:` and `JF_PENDING:`
      lines. This is the INDEPENDENT corroboration, taken in wave 20 before this plan runs, and it
      is a binding input to the branch — not a footnote to it.
    - `scripts/check-music-consumers.sh` header § `EXIT 3 — THE PENDING STATE`, paragraph (c) —
      "THE JELLYFIN AND THE MA VERDICT ARE STILL SEPARATE AND ARE NEVER SUMMED INTO ONE CONF-04
      ANSWER … Do not publish 'N of M pending' as a CONF-04 completion figure, and never let a
      green MA half offset a pending Jellyfin one." That rule binds this artifact.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` § Gaps Summary and
      § Recommended path — the two options the operator was offered and declined, which branch B
      hands back.
    - `.planning/ROADMAP.md` Phase 7 entry criterion **E6** — note that its SECOND measurement
      (whether a second ≥4-artist track yields four artists in Music Assistant or three, which is
      the only thing that separates "MA caps at 3" from "`Twista` specifically failed to map") is
      NOT closed by this round and stays with Phase 7 on every branch.
  </read_first>
  <action>
    Write `06-43-conf04-verdict.txt`. No estate contact is needed for this task beyond re-reading
    committed artifacts; issue no API call and no ssh command that changes anything.

    SECTION L — the numbers. Copy 06-42's `--- ROW VERDICTS BEGIN ---` … `--- ROW VERDICTS END ---`
    block into this artifact **byte-for-byte**, markers included. Do not retype it, do not reorder
    it, do not reformat it: the verify block `diff`s the two blocks and fails on any difference, so
    a transcription slip between the measurement and the verdict cannot survive. Beneath the block,
    write the same three rows out in prose for a human reader, and record 06-42's `SAFETY:` value
    and the instrument's `INSTRUMENT RUN:`, `JF_AT_TARGET:` and `JF_PENDING:` values, each copied
    verbatim onto its own line.

    SECTION M — the branch, on a single line of the exact form `BRANCH: A`, `BRANCH: B` or
    `BRANCH: PARTIAL`. It is computed by the rule below and by no other, evaluated strictly in
    order, from exactly four inputs: the three `verdict=` values in the copied block, 06-42's
    `SAFETY:` value, and the instrument's `INSTRUMENT RUN:` / `JF_AT_TARGET:` / `JF_PENDING:`
    values. The verify block re-evaluates the same rule from the same lines and fails the task if
    the written value differs — so this is a computation with a check, not a judgement:

      1. `SAFETY:` is not `PASS`                                            →  `PARTIAL`
      2. `INSTRUMENT RUN:` is not `MEASURED`                                →  `PARTIAL`
         (reason: `UNCORROBORATED`)
      3. all three `verdict=AT-TARGET` **and** `JF_AT_TARGET` is `3` **and**
         `JF_PENDING` is `0`                                               →  `A`
      4. all three `verdict=AT-BASELINE` **and** `JF_AT_TARGET` is `0` **and**
         `JF_PENDING` is `3`                                               →  `B`
      5. anything else                                                      →  `PARTIAL`

    Exactly one `BRANCH:` line may appear in the file. On `PARTIAL`, name the reason on the
    following line.

    ⚠ TWO THINGS THE RULE DELIBERATELY DOES NOT USE, and a reader will reach for both.

      **The exit code is not an input.** `check-music-consumers.sh` exits **3 on branch A and on
      branch B alike**, because `MA_ARTIST_PENDING` stays `1` either way — MA returns three artists
      against row 1's four-value tag, which is a reported measured discrepancy owned by Phase 7
      E6's second measurement. An exit code that cannot distinguish A from B cannot decide between
      them, and wiring it in would be the first step toward summing the two halves.

      **The MA counters are not inputs.** `MA_AT_TARGET` and `MA_REPORTED` are recorded in SECTION
      L for the record and take no part in rule 3, 4 or 5. A surviving MA pending row does NOT
      disqualify branch A, and a green MA half may never offset a pending Jellyfin one. This is
      `check-music-consumers.sh`'s own point (c) — the two verdicts are separate and are never
      summed into one CONF-04 answer — applied at the one step where it would be easiest to break.

    ⚠ WHY RULE 2 EXISTS, stated plainly because it can block a close. `BRANCH: A` permanently
    records a requirement as met in a public repository. It requires two independent measurements
    agreeing: this round's own API reads (06-42 task 1) and the deployed instrument's own verdict
    (06-42 task 3). If the instrument could not look — most likely a host checkout that has not
    pulled — the honest state is "measured but uncorroborated", which is not A. The remedy is a
    two-minute operator action (push, `git pull --ff-only` on LXC 100, re-run 06-42 task 3), and it
    should be written into the artifact as such rather than left looking like a dead end.

    SECTION N — what the branch does and does not mean, written out so no downstream reader has to
    infer it:
      - On A: CONF-04's JELLYFIN half is closed by measurement. The MA half was already discharged
        on 2026-09-21. CONF-04 as a requirement closes. State explicitly that
        `check-music-consumers.sh` will still exit **3**, because MA row 1 remains a reported
        measured discrepancy (3 returned against a 4-value tag) and `MA_ARTIST_PENDING` is still 1
        — and that this is CORRECT and must not be tuned out. The two verdicts are separate and are
        never summed; the sum exists only to answer "is any row off target", which is a different
        question from "is CONF-04 closed".
      - On B or PARTIAL: CONF-04 stays OPEN, honestly, and the two original options go back to the
        operator: (a) an explicit override carrying the Jellyfin half to Phase 7 entry criterion
        E6, or (b) hold Phase 6 open until Phase 7's first write.
      - On every branch: E6's SECOND measurement — the ≥4-artist MA discrimination — is NOT closed
        by this round and stays with Phase 7. Say so in the artifact; it is the single easiest
        thing for a future reader to over-claim.

    SECTION O — the round-wide audit, delimited between `--- ROUND AUDIT BEGIN ---` and
    `--- ROUND AUDIT END ---`, so the counts inside it are not taken over the prose discussing
    them (`DEF-06-39-06`). Record, mechanically:
      - the occurrence count of the forbidden mode's literal token across every
        `artifacts/06-4*-*.txt` file this round produced — expected `0`;
      - the occurrence count of the case-insensitive phrase for the forbidden UI button across the
        same set — expected `0`;
      - the total number of write verbs (POST/PUT/DELETE) issued by the entire round, from
        06-41 SECTION B's endpoint audit — expected `1`;
      - the number of files under `/mnt/tank/media/Music` whose content changed — expected `0`;
      - the number whose mtime changed — expected `3`;
      - whether `tank/downloads@pre-phase5` is still present — expected PRESENT;
      - whether `tank/media/Music@pre-06-41-conf04-reprobe` still exists — expected PRESENT, and
        stated as still held rather than released. It is not destroyed by this round; its release
        is a separate operator decision once the outcome is accepted.
    Write the token being counted in a BRACKETED form in the recipe (so the recipe is not itself an
    occurrence of the thing it measures) and state in band that this is the mitigation
    `DEF-06-39-06` requires.
  </action>
  <acceptance_criteria>
    - The artifact contains the literal headings `SECTION L`, `SECTION M`, `SECTION N`,
      `SECTION O`.
    - The artifact contains exactly one line matching `^BRANCH: (A|B|PARTIAL)$`.
    - SECTION L's `--- ROW VERDICTS BEGIN ---` … `--- ROW VERDICTS END ---` block is byte-identical
      to 06-42's, proved by `diff`, and holds exactly three `ROW|` lines carrying all three pinned
      internal paths.
    - SECTION L records, each copied verbatim onto its own line, 06-42's `SAFETY:` value and the
      instrument's `INSTRUMENT RUN:`, `JF_AT_TARGET:`, `JF_PENDING:`, `MA_AT_TARGET:` and
      `MA_REPORTED:` values.
    - The written `BRANCH:` value equals the value independently recomputed by the verify block
      from the three `verdict=` fields, the `SAFETY:` value and the two Jellyfin counters, under
      the five-step rule in the action. A disagreement fails the task and is not reconciled in
      prose.
    - `BRANCH: A` is impossible unless `INSTRUMENT RUN: MEASURED`, `JF_AT_TARGET: 3` and
      `JF_PENDING: 0` — the recompute enforces it, so an uncorroborated close cannot be written.
    - Neither the instrument's `EXIT CODE` nor either MA counter appears as an input to the branch;
      a surviving `MA_REPORTED: 1` does not disqualify `BRANCH: A`.
    - SECTION N contains the sentence stating that the Jellyfin and MA verdicts are never summed,
      and — on branch A — explicitly states that `check-music-consumers.sh` still exits `3` because
      `MA_ARTIST_PENDING` remains `1`, and that this must not be tuned out.
    - SECTION N explicitly states, on every branch, that Phase 7 entry criterion E6's second
      measurement (the ≥4-artist MA discrimination) is NOT closed by this round.
    - The artifact contains exactly one `--- ROUND AUDIT BEGIN ---` / `--- ROUND AUDIT END ---`
      pair, and inside it the write-verb total is recorded as `1`, the changed-content count as
      `0`, the changed-mtime count as `3`, and both snapshots as PRESENT.
    - `grep -c FullRefresh` over the artifact returns `0`.
    - No file outside `.planning/` was modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt; AF=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt; RR=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt; test -f "$A" || exit 1; test -f "$AF" || { echo "06-42 after-state missing"; exit 1; }; test -f "$RR" || { echo "06-42 instrument re-run missing — the branch cannot be corroborated"; exit 1; }; for s in "SECTION L" "SECTION M" "SECTION N" "SECTION O"; do grep -qF "$s" "$A" || { echo "missing: $s"; exit 1; }; done; test "$(grep -cE "^BRANCH: (A|B|PARTIAL)$" "$A")" -eq 1 || { echo "need exactly one well-formed BRANCH: line"; exit 1; }; grep -qF "never summed" "$A" || exit 1; grep -qF "E6" "$A" || exit 1; test "$(grep -cF -- "--- ROUND AUDIT BEGIN ---" "$A")" -eq 1 || exit 1; test "$(grep -cF -- "--- ROUND AUDIT END ---" "$A")" -eq 1 || exit 1; test "$(grep -cF "FullRefresh" "$A")" -eq 0 || exit 1; grep -qF "tank/media/Music@pre-06-41-conf04-reprobe" "$A" || exit 1; grep -qF "tank/downloads@pre-phase5" "$A" || exit 1; for f in .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-4*.txt; do test "$(grep -cF "FullRefresh" "$f")" -eq 0 || { echo "forbidden token in $f"; exit 1; }; done; diff <(sed -n "/^--- ROW VERDICTS BEGIN ---$/,/^--- ROW VERDICTS END ---$/p" "$AF") <(sed -n "/^--- ROW VERDICTS BEGIN ---$/,/^--- ROW VERDICTS END ---$/p" "$A") >/dev/null || { echo "SECTION L row-verdicts block is not byte-identical to 06-42 — transcription drift"; exit 1; }; NR_=$(grep -cE "^ROW\|" "$A"); test "$NR_" -eq 3 || { echo "expected 3 ROW lines, found $NR_"; exit 1; }; S=$(grep -E "^SAFETY: " "$AF" | tail -1 | sed "s/^SAFETY: //" | cut -d" " -f1); I=$(grep -E "^INSTRUMENT RUN: " "$RR" | tail -1 | sed "s/^INSTRUMENT RUN: //" | cut -d" " -f1); JT=$(grep -oE "^JF_AT_TARGET: [0-9]+$" "$RR" | tail -1 | cut -d" " -f2); JP=$(grep -oE "^JF_PENDING: [0-9]+$" "$RR" | tail -1 | cut -d" " -f2); test -n "$S" -a -n "$I" || { echo "COULD NOT LOOK: SAFETY=$S INSTRUMENT RUN=$I unreadable"; exit 1; }; test -n "$JT" || JT=x; test -n "$JP" || JP=x; NT=$(grep -cE "^ROW\|.*verdict=AT-TARGET$" "$A"); NB=$(grep -cE "^ROW\|.*verdict=AT-BASELINE$" "$A"); if [ "$S" != "PASS" ]; then E=PARTIAL; elif [ "$I" != "MEASURED" ]; then E=PARTIAL; elif [ "$NT" -eq 3 ] && [ "$JT" = "3" ] && [ "$JP" = "0" ]; then E=A; elif [ "$NB" -eq 3 ] && [ "$JT" = "0" ] && [ "$JP" = "3" ]; then E=B; else E=PARTIAL; fi; W=$(grep -oE "^BRANCH: (A|B|PARTIAL)$" "$A" | tail -1 | cut -d" " -f2); test "$W" = "$E" || { echo "BRANCH mismatch: written=$W recomputed=$E (SAFETY=$S INSTRUMENT=$I verdicts AT-TARGET/AT-BASELINE=$NT/$NB JF_AT_TARGET=$JT JF_PENDING=$JP)"; exit 1; }; echo OK'</automated>
  </verify>
  <done>One machine-readable `BRANCH:` line, computed from the measured per-row verdicts and the safety gate by a stated rule, with the two never-summed verdicts kept apart, E6's surviving second measurement named, and a mechanical round-wide audit proving one write verb, zero content changes, three mtime changes and zero occurrences of the forbidden mode.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: The operator decides what the measurement means for CONF-04</name>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt`
      Sections L, M, N and O — the numbers, the branch, the consequences and the audit.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` § Recommended path
      — the two options verbatim, which branch B and PARTIAL hand back.
  </read_first>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt</files>
  <decision>
    Accept the measured branch and authorise plans 06-44 and 06-45 to write the matching record —
    or, on branch B / PARTIAL, choose which of the two original options Phase 6 closes under.
  </decision>
  <context>
    No document has moved yet. `REQUIREMENTS.md:152` still carries `- [ ] CONF-04`, ROADMAP.md's
    Phase 6 status row still reads `In Progress`, and `06-VERIFICATION.md` still scores 5/6. Every
    one of those is edited by 06-45, and this is the gate in front of it.

    Present, quoting the artifact rather than paraphrasing:
      1. The `BRANCH:` line and the three per-row numbers that produced it.
      2. The `SAFETY:` line from 06-42 and the round audit from SECTION O.
      3. On branch A: what 06-44 and 06-45 will write — `REQUIREMENTS.md:152` ticked, ROADMAP's
         Phase 6 criterion-4 text and status row updated, Phase 7 E6 amended to record that its
         FIRST measurement discharged in Phase 6 while its SECOND (the ≥4-artist MA
         discrimination) stays with Phase 7, and `06-VERIFICATION.md`'s CONF-04 gap recorded as
         closed with its evidence. State plainly that this plan set does NOT self-declare 6/6:
         re-scoring the verification report is `/gsd-verify 06`'s call and 06-45 recommends it
         rather than performing it.
      4. On branch B or PARTIAL: the two original options, unchanged from `06-VERIFICATION.md`'s
         own recommendation, plus what is now known that was not known before — the mtime
         hypothesis was driven and did not close the gap, and exactly which of the three rows
         moved.
      5. On every branch: the snapshot `tank/media/Music@pre-06-41-conf04-reprobe` is still held.
         Releasing it is a separate decision and is deliberately not bundled into this one.
  </context>
  <options>
    <option id="accept-a">
      <name>Branch A — accept the close; authorise the record</name>
      <pros>CONF-04 closes on measurement taken today; Phase 6 becomes re-verifiable; the instrument's Jellyfin rows go green on their own logic</pros>
      <cons>Only valid if all three rows read AT-TARGET and SAFETY reads PASS</cons>
    </option>
    <option id="negative-carry-e6">
      <name>Branch B / PARTIAL — record the negative, carry the Jellyfin half to Phase 7 E6 under an explicit override</name>
      <pros>Honest; makes the roadmap's existing argument auditable rather than implicit; unblocks Phase 6's verification with a signed override</pros>
      <cons>CONF-04 remains unmet at Phase 6 close</cons>
    </option>
    <option id="negative-hold-open">
      <name>Branch B / PARTIAL — record the negative and hold Phase 6 open until Phase 7's first write</name>
      <pros>No override needed; the phase closes only when the requirement is actually met</pros>
      <cons>Phase 6 stays In Progress indefinitely; `quick-health-check.sh` keeps exiting non-zero on the consumers block</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The operator was shown the verbatim `BRANCH:` line, the three per-row numbers, the `SAFETY:`
      line and the round audit.
    - The operator's selection is recorded verbatim in the plan summary and appended to
      `06-43-conf04-verdict.txt` under a `SECTION P — OPERATOR DECISION` heading, with a UTC
      timestamp.
    - No document outside `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/` has
      been modified at the moment this checkpoint resolves.
    - If the selection is `accept-a`, the artifact's `BRANCH:` line reads `BRANCH: A`. An
      `accept-a` over a `BRANCH: B` or `BRANCH: PARTIAL` is refused, and the refusal is recorded.
  </acceptance_criteria>
  <action>
    Present the five items listed in `<context>` above, quoting `06-43-conf04-verdict.txt` verbatim
    rather than paraphrasing it, and wait for an explicit selection. Then append the operator's
    answer to that artifact under a new `SECTION P — OPERATOR DECISION` heading with a UTC
    timestamp. Refuse an `accept-a` selection if the recorded `BRANCH:` line is not `BRANCH: A`,
    and record the refusal in the same section. Edit no other file: every document edit belongs to
    plans 06-44 and 06-45, which run after this gate.
  </action>
  <verify>
    <automated>bash -c 'set -u; A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt; grep -qF "SECTION P — OPERATOR DECISION" "$A" || { echo "operator decision not recorded"; exit 1; }; B=$(grep -oE "^BRANCH: (A|B|PARTIAL)$" "$A" | cut -d" " -f2); test -n "$B" || exit 1; if grep -qF "accept-a" "$A"; then test "$B" = "A" || { echo "accept-a recorded over BRANCH: $B"; exit 1; }; fi; test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md .planning/ROADMAP.md stacks/selfhosted/arrs/beets.md)" || { echo "a downstream document was edited before the gate resolved"; exit 1; }; echo OK'</automated>
  </verify>
  <done>The operator has seen the verbatim `BRANCH:` line, the three per-row numbers, the `SAFETY:` line and the round audit, and has selected one of the three options — recorded verbatim in the plan summary and appended to the verdict artifact as `SECTION P` with a UTC timestamp. No requirement record has moved yet.</done>
  <resume-signal>Select: `accept-a`, `negative-carry-e6`, or `negative-hold-open`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured artifacts → this plan's verdict | the one place where numbers become a claim; the classic site of this phase's recurring "claim broader than its code" defect |
| this plan → the public git repo | the artifact is committed to a PUBLIC repository |
| operator decision → downstream document edits | 06-44 and 06-45 act on the recorded branch; a mis-recorded branch propagates into REQUIREMENTS.md and ROADMAP.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-43-01 | Repudiation | a branch claimed rather than computed | mitigate | the `BRANCH:` value is defined by an explicit rule over 06-42's per-row verdicts and its single `SAFETY:` line, and the acceptance criteria refuse a `BRANCH: A` that is inconsistent with either |
| T-06-43-02 | Spoofing | the MA and Jellyfin verdicts summed into one CONF-04 answer | mitigate | SECTION N states the non-summing rule verbatim and, on branch A, states that the script still exits 3 with `MA_ARTIST_PENDING` at 1 and that this must not be tuned out |
| T-06-43-03 | Elevation of privilege | an executor over-claiming that E6 is fully discharged | mitigate | SECTION N is required on every branch to state that E6's second measurement — the ≥4-artist MA discrimination — stays with Phase 7 |
| T-06-43-04 | Tampering | the forbidden refresh mode reached on a negative branch | mitigate | branch B and PARTIAL both terminate in a recorded negative and an operator choice; the round audit counts the forbidden token across every artifact the round produced and requires 0 |
| T-06-43-05 | Repudiation | a self-referential count in the audit measuring its own recipe | mitigate | the audit is delimited, and the counted token is written in bracketed form in the recipe, per `DEF-06-39-06`'s stated mitigation |
| T-06-43-06 | Information disclosure | credentials in the committed verdict artifact | mitigate | this task issues no authenticated call; the artifact carries only counts, verdicts and paths, and is credential-screened before commit |
| T-06-43-07 | Spoofing | `BRANCH: A` written on a single unverified chain of artifacts | mitigate | the verify recomputes the branch from four independently-read inputs and `diff`s 06-42's row-verdict block byte-for-byte; `BRANCH: A` additionally requires `INSTRUMENT RUN: MEASURED` with `JF_AT_TARGET: 3` / `JF_PENDING: 0`, and 06-45 re-reads the same corroboration before ticking anything |
| T-06-43-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing — no package manager is invoked |
</threat_model>

<verification>
- Exactly one well-formed `BRANCH:` line exists AND equals the value the verify block independently
  recomputes from the three `verdict=` fields, 06-42's `SAFETY:` value and the instrument's two
  Jellyfin counters.
- SECTION L's row-verdict block is byte-identical to 06-42's.
- Neither the instrument's exit code nor either MA counter is an input to the branch.
- SECTION N carries the non-summing rule and the E6-second-measurement carve-out.
- The round audit shows 1 write verb, 0 content changes, 3 mtime changes, both snapshots present,
  and 0 occurrences of the forbidden mode's token across every round-5 artifact.
- `SECTION P` records the operator's verbatim answer with a UTC timestamp.
- No file outside the phase's `artifacts/` directory was modified by this plan.
</verification>

<success_criteria>
The round's outcome is one computed, machine-readable line that a script re-derives from four
independently-read inputs and refuses to accept on disagreement — unreachable at `A` without a
corroborating instrument run that actually looked; the two CONF-04 verdicts stay separate; the surviving Phase 7 measurement is named; and
the operator has chosen the consequence before any record moves.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-43-SUMMARY.md` when done.
</output>
