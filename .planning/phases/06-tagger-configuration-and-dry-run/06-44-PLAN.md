---
phase: 06-tagger-configuration-and-dry-run
plan: 44
type: execute
wave: 22
depends_on: ["06-43"]
gap_closure: true
requirements: [CONF-04]
files_modified:
  - scripts/check-music-consumers.sh
  - scripts/quick-health-check.sh
autonomous: true

must_haves:
  truths:
    - "check-music-consumers.sh reached its Jellyfin-half verdict on its own unmodified logic in 06-42 task 3, before this plan touched it — and no target, baseline, threshold, branch, counter or exit code is altered here to match that result"
    - "The instrument keeps its three honest states (PASS / PENDING / RED) and keeps the Jellyfin and MA verdicts separate and never summed into one CONF-04 answer"
    - "Every in-band sentence naming Phase 7 or entry criterion E6 as the owner of the Jellyfin half's discharge now states what is actually true after round 5"
    - "quick-health-check.sh still exits non-zero while any artist row is off target, and no success-producing override was added"
  artifacts:
    - path: "scripts/check-music-consumers.sh"
      provides: "the standing CONF-04 drift detector, with its in-band prose corrected and its logic provably untouched"
      contains: "ARTIST_PROOF_ROWS"
    - path: "scripts/quick-health-check.sh"
      provides: "the health entry point's exit-3 arm, naming both owners correctly and still setting EXIT_CODE=1"
      contains: "CONSUMERS_RC"
  key_links:
    - from: "scripts/check-music-consumers.sh section 4b"
      to: "scripts/quick-health-check.sh exit-3 arm"
      via: "the exit 3 pending gate, unchanged"
      pattern: "JELLYFIN_ARTIST_PENDING \\+ MA_ARTIST_PENDING"
---

<objective>
The instrument has already spoken. Now make its prose true.

`scripts/check-music-consumers.sh` is this estate's standing CONF-04 drift detector. Round 5 changed
the estate, not the instrument — so the instrument produced its verdict with its logic untouched, in
**06-42 task 3**, in wave 20, before the branch was computed and before the operator decided
anything. This plan runs afterwards and only corrects the sentences that named Phase 7 as the owner
of a discharge that either happened here or provably did not.

That ordering is deliberate and was corrected during planning: the instrument re-run used to be this
plan's first task, which put the round's one independent cross-check AFTER the operator's binding
decision. It now runs before it.

Purpose: a file that tells the truth about a verdict it earned on untouched code.
Output: two prose-corrected scripts. This plan writes no artifact and makes no estate contact.

⛔ WHAT MAY NOT CHANGE, stated first because it is the whole point: no `target` value, no `baseline`
value, no threshold, no branch condition, no counter, no exit code, and no ordering in the exit
ladder. `ARTIST_PROOF_ROWS` keeps both numeric columns on every row — the baselines are what make a
post-discharge REGRESSION detectable, and deleting them would trade a detector for a tidier table.
The PASS / PENDING / RED triad stays. `JELLYFIN_ARTIST_PENDING` and `MA_ARTIST_PENDING` stay
separate and are never summed into a CONF-04 completion figure.

⛔ THE HARD FENCE still binds: nothing added here may issue, suggest or make reachable the
aggressive per-item "replace all metadata" refresh mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-41-conf04-reprobe-drive.txt
@.planning/phases/06-tagger-configuration-and-dry-run/06-43-SUMMARY.md
@scripts/check-music-consumers.sh
@scripts/quick-health-check.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct `check-music-consumers.sh`'s in-band prose to what is now true — and change nothing else</name>
  <files>scripts/check-music-consumers.sh</files>
  <read_first>
    - `scripts/check-music-consumers.sh` in full, and specifically: the `EXIT 3 — THE PENDING
      STATE` header paragraph including its point (c) on the two never-summed verdicts; the
      `⚠ THE `target` COLUMN IS NOT TRUE TODAY` paragraph above `ARTIST_PROOF_ROWS`; the three row
      definitions; section 4b's PENDING branch and its `echo` block; the `artist rows PENDING (JF)`
      summary line; and the exit-3 banner block above `exit 3`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt`
      Sections M, N and P — the branch, its stated consequences, and the operator's answer. The
      prose written here must match SECTION N sentence for sentence in substance.
    - `deferred-items.md` § `DEF-06-39-06` — the self-referential-measurement mitigation: any
      assertion counting a token in a file that discusses that token must use a comment-stripped
      recipe or an inequality, never a raw equality.
  </read_first>
  <action>
    Edit comments and user-facing strings ONLY. The instrument's own verdict was already taken, on untouched code, by **06-42 task 3** — this plan never runs it and never needs to. Every one of the following sentences currently tells
    a reader that the Jellyfin half's discharge belongs to Phase 7, and after round 5 that is either
    false (branch A) or true-but-now-better-informed (branch B / PARTIAL). Correct each at its site,
    citing plan `06-41` and the artifacts by name:

      1. The `⚠ THE `target` COLUMN IS NOT TRUE TODAY` paragraph above `ARTIST_PROOF_ROWS`. On
         branch A: record that the target column became true on the measured date, by what
         mechanism (an mtime touch on exactly three files inside a ZFS snapshot fence, followed by
         the same targeted Default-mode `POST /Library/Media/Updated` this file already describes),
         and that the baseline column is DELIBERATELY RETAINED because it is now the regression
         detector rather than the current state. On branch B / PARTIAL: record that the mtime lever
         named in this very paragraph was driven on the measured date and what it did and did not
         move. Either way the paragraph must keep its existing statement that the aggressive
         refresh mode is forbidden and is not issued here or anywhere.
      2. Section 4b's PENDING-branch `echo` block. It currently reads "It discharges when Phase 7
         writes or imports the file". On branch A that branch now fires only on a REGRESSION, and
         the text must say so — a row back at its 2026-09-20 baseline after the discharge means
         something reverted (most likely `PreferNonstandardArtistsTag`, which section 4a asserts
         independently), not that the estate is waiting. On branch B / PARTIAL the text keeps its
         meaning and gains the measured fact that the touch route was tried.
      3. The `artist rows PENDING (JF)` summary line's parenthetical, currently "discharges on
         Phase 7's first write".
      4. The exit-3 banner block. Its last line currently reads "Discharges on ROADMAP entry
         criterion E6, not here." On branch A that is still TRUE OF THE SURVIVING MA ROW and FALSE
         OF THE JELLYFIN ROWS, so the banner must name which half each count belongs to and who
         owns it: the Jellyfin half discharged in Phase 6 (round 5), so a non-zero Jellyfin count is
         now a regression; the MA half's surviving row is owned by E6's SECOND measurement — whether
         a second ≥4-artist track yields four artists or three, which is the only thing that
         separates "MA caps the list at 3" from "`Twista` specifically failed to map" — and that
         stays with Phase 7 on every branch.
      5. The header's EXIT 3 paragraph where it says "4b is a probe-time option awaiting Phase 7's
         first write". Point (c) — the two never-summed verdicts — is RESTATED, not weakened.

    Do NOT: change any `target` or `baseline` number; delete either numeric column; add, remove or
    reorder a branch; touch `JELLYFIN_ARTIST_OK`, `JELLYFIN_ARTIST_PENDING`, `MA_ARTIST_OK` or
    `MA_ARTIST_PENDING`; change the exit ladder or its ordering; move the green banner; add a skip
    sentinel or any knob that can make a block greener. There is no success-producing override
    anywhere in this file and none is added.

    Do NOT touch section 4c, the MA half, or `MA_VERSION_PROVEN`. Do NOT touch
    `stacks/selfhosted/arrs/beets/config.yaml` or `06-EXPECTED-TREE.txt` — neither is in scope for
    this round.

    Keep the `📊 6. Summary` heading literal and un-renumbered: `quick-health-check.sh` anchors on
    it in two places.

    When writing the new prose, do not introduce the forbidden mode's literal token, and do not
    write a sentence that would make an existing absence-grep match prose rather than code.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/check-music-consumers.sh` exits 0, and `shellcheck -S warning` reports
      nothing new relative to the pre-edit run (record both).
    - The three `ARTIST_PROOF_ROWS` definitions are present verbatim, proving no threshold moved:
      the file contains the literal substrings `|4|0|3|ARTISTS|Jewels n`,
      `|2|1|2|ARTISTS|California Gurls` and `|2|1|2|ARTISTS|Just Give Me a Reason`.
    - The exit ladder is intact and in order: the line matching `BASELINE_MODE -eq 1` occurs before
      the line matching `FAILURES -gt 0`, which occurs before the line matching
      `JELLYFIN_ARTIST_PENDING + MA_ARTIST_PENDING`, which occurs before the line containing
      `Both music consumers see the pinned albums`.
    - `exit 3` is still present exactly once in the pending gate, and `exit 0`, `exit 1`, `exit 2`
      each still appear.
    - Every changed line in `git diff -U0 -- scripts/check-music-consumers.sh` is a comment line or
      an `echo`/`warn`/`info` line: the count of changed lines that are neither is `0`.
    - The four `JELLYFIN_D34_OPTIONS` rows and both `*_ARTIST_PENDING` counter names are unchanged.
    - The file still states the never-summed rule (case-insensitive match on `never (be )?summed`
      — it reads `ARE NEVER SUMMED INTO ONE CONF-04 ANSWER` today), and the sentence naming
      E6's second measurement as remaining with Phase 7 is present.
    - `grep -c FullRefresh scripts/check-music-consumers.sh` returns `0`.
    - The `📊 6. Summary` heading is present exactly once and is un-renumbered.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; F=scripts/check-music-consumers.sh; bash -n "$F" || exit 1; for s in "|4|0|3|ARTISTS|Jewels n" "|2|1|2|ARTISTS|California Gurls" "|2|1|2|ARTISTS|Just Give Me a Reason" "JELLYFIN_ARTIST_PENDING" "MA_ARTIST_PENDING"; do grep -qF "$s" "$F" || { echo "missing: $s"; exit 1; }; done; grep -qiE "never (be )?summed" "$F" || { echo "the never-summed rule was dropped"; exit 1; }; test "$(grep -cF "6. Summary" "$F")" -ge 1 || exit 1; test "$(grep -cF "FullRefresh" "$F")" -eq 0 || exit 1; B=$(grep -nE "^if \[\[ .BASELINE_MODE -eq 1" "$F" | tail -1 | cut -d: -f1); FA=$(grep -nE "^if \[\[ .FAILURES -gt 0" "$F" | tail -1 | cut -d: -f1); P=$(grep -nE "^if \[\[ .*JELLYFIN_ARTIST_PENDING" "$F" | tail -1 | cut -d: -f1); G=$(grep -nE "^echo -e .*Both music consumers see the pinned albums" "$F" | tail -1 | cut -d: -f1); test -n "$B" -a -n "$FA" -a -n "$P" -a -n "$G" || { echo "an exit-ladder gate line is missing: B=$B FA=$FA P=$P G=$G"; exit 1; }; test "$B" -lt "$FA" -a "$FA" -lt "$P" -a "$P" -lt "$G" || { echo "exit ladder order broken: $B $FA $P $G"; exit 1; }; N=$(git diff -U0 -- "$F" | grep -E "^[+-][^+-]" | grep -vcE "^[+-][[:space:]]*(#|echo |echo$|warn |info )" || true); test "${N:-0}" -eq 0 || { echo "$N non-comment, non-message lines changed"; exit 1; }; echo OK'</automated>
  </verify>
  <done>Every in-band sentence that named Phase 7 or E6 as the owner of the Jellyfin half now states the round-5 truth, the three-state design and the two never-summed verdicts are restated rather than weakened, and a mechanical diff check proves no line that is not a comment or a printed message was touched — so no threshold, branch, counter or exit code moved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct `quick-health-check.sh`'s exit-3 arm, and keep it exiting non-zero</name>
  <files>scripts/quick-health-check.sh</files>
  <read_first>
    - `scripts/quick-health-check.sh` — the `elif [ "$CONSUMERS_RC" -eq 3 ]` arm in full, including
      the paragraph beginning "⚠ EXIT_CODE=1 HERE IS THE LOAD-BEARING HALF", the anchor guard on
      `📊 6. Summary`, and the `CONSUMERS_OVERRIDDEN` notice.
    - `scripts/check-music-consumers.sh` as edited by task 1 — the two files' accounts of the exit-3
      contract must agree; a correction in one and not the other recreates `GC-04`'s stale-citation
      class.
    - `README.md` § Health Checks — fail closed, keep "could not look" distinct from "nothing is
      wrong", bound remote commands Linux-side, assert rather than report.
  </read_first>
  <action>
    Edit comments and printed strings ONLY, in the `-eq 3` arm and nowhere else.

    The arm currently states that the non-zero exit "clears when ROADMAP entry criterion E6
    discharges the Jellyfin half, and on nothing else." After round 5 that sentence needs to name
    two owners rather than one, matching the banner text task 2 wrote:
      - on branch A: the Jellyfin half discharged in Phase 6 (round 5, plans 06-41/06-42), so a
        Jellyfin pending count appearing here is now a REGRESSION and should be read as one; the
        block stays non-zero while the MA row remains reported, and that clears on E6's SECOND
        measurement in Phase 7;
      - on branch B / PARTIAL: the sentence stands, with the added fact that the mtime route was
        driven and did not discharge it, cited to the artifacts.

    ⚠ `EXIT_CODE=1` on this arm STAYS. It is load-bearing and deliberate: a requirement that is
    measured and open is not healthy, and the non-zero status is what makes a post-discharge
    regression detectable by tooling rather than by a human reading yellow text. Do not add an
    override, a threshold, a grace period or a skip — there is no success-producing knob anywhere in
    this file and none is added by this plan.

    Leave untouched: the anchor guard and its `📊 6. Summary` pattern, the `CONSUMERS_OVERRIDDEN`
    notice on every arm, the empty-output and `-eq 124` arms, the bounded `ssh -n`, the
    `CONSUMERS_RC` capture before any pipe, and every other block in the file.

    While here, do not "fix" `DEF-06-10-01` (the freeze fold-in's inner-ssh bound) — it is a named,
    owned deferral with its own blast radius and it is out of this round's scope.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/quick-health-check.sh` exits 0.
    - The `-eq 3` arm still sets `EXIT_CODE=1`: the file contains a line matching
      `CONSUMERS_RC" -eq 3` and, within that arm, `EXIT_CODE=1` appears at least twice (the
      anchor-guard sub-branch and the reporting sub-branch).
    - The anchor guard is intact: the file still contains the literal `📊 6. Summary` pattern used
      by both the `-eq 0` and `-eq 3` arms.
    - `CONSUMERS_OVERRIDDEN` still appears on every arm of the consumers block: the occurrence
      count of the guard `CONSUMERS_OVERRIDDEN" -eq 1` is **7**, measured at this plan's base
      commit on 2026-09-23 and asserted unchanged.
    - Every changed line in `git diff -U0 -- scripts/quick-health-check.sh` is a comment line or an
      `echo` line: the count of changed lines that are neither is `0`.
    - No new `if`, `elif`, `case` or `EXIT_CODE` assignment was added anywhere in the file.
    - `grep -c FullRefresh scripts/quick-health-check.sh` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; F=scripts/quick-health-check.sh; bash -n "$F" || exit 1; grep -qE "CONSUMERS_RC. -eq 3" "$F" || exit 1; grep -qF "6. Summary" "$F" || exit 1; test "$(grep -cE "CONSUMERS_OVERRIDDEN. -eq 1" "$F")" -eq 7 || { echo "the override notice count moved from its measured 7"; exit 1; }; grep -qF "CONSUMERS_OVERRIDDEN" "$F" || exit 1; test "$(grep -cF "FullRefresh" "$F")" -eq 0 || exit 1; N=$(git diff -U0 -- "$F" | grep -E "^[+-][^+-]" | grep -vcE "^[+-][[:space:]]*(#|echo |echo$)" || true); test "${N:-0}" -eq 0 || { echo "$N non-comment, non-echo lines changed"; exit 1; }; A=$(git diff -U0 -- "$F" | grep -cE "^\+[[:space:]]*(if|elif|case|EXIT_CODE=)" || true); test "${A:-0}" -eq 0 || { echo "control flow or EXIT_CODE added"; exit 1; }; echo OK'</automated>
  </verify>
  <done>The health entry point's exit-3 arm names both owners correctly, keeps `EXIT_CODE=1`, keeps its anchor guard and its override notice, and a mechanical diff check proves no control flow and no exit-code assignment was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git → the host's `/mnt/fast/stacks` checkout | the only delivery path for these scripts; the corroborating run in 06-42 task 3 fails closed on a stale checkout |
| this plan → two standing health-check scripts | these files gate the estate's only health entry point; a loosened assertion here is silent everywhere |
| this plan → the public git repo | both scripts and the transcript are committed to a PUBLIC repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-44-01 | Tampering | a threshold or baseline edited to meet the measured result | mitigate | the three row definitions are asserted present verbatim including both numeric columns, and a mechanical diff check requires every changed line to be a comment or a printed message |
| T-06-44-02 | Tampering | the exit ladder reordered or the green banner moved above the pending gate | mitigate | the four gate lines' relative order is asserted by line number, which is exactly the `WR-03` regression this gate was built to prevent |
| T-06-44-03 | Spoofing | a success-producing override added to make the health check exit 0 | mitigate | `EXIT_CODE=1` on the exit-3 arm is asserted to remain, and the diff is asserted to add no `if`/`elif`/`case`/`EXIT_CODE=` line |
| T-06-44-04 | Repudiation | prose edited to match a verdict the instrument never corroborated | mitigate | the corroborating run happened in 06-42 task 3 on untouched code, and 06-43's branch recompute refuses `BRANCH: A` unless `INSTRUMENT RUN: MEASURED` with `JF_AT_TARGET: 3` / `JF_PENDING: 0`; this plan only writes prose downstream of that gate |
| T-06-44-05 | Spoofing | the two verdicts collapsed into one CONF-04 figure by the new prose | mitigate | the phrase `never summed` is asserted present, and the new banner text is required to name which half each count belongs to |
| T-06-44-06 | Repudiation | the two scripts' accounts of the exit-3 contract drifting apart | mitigate | task 3 reads task 2's edit first and the acceptance criteria require the anchor and the contract wording to agree — the `GC-04` stale-citation class |
| T-06-44-07 | Information disclosure | a credential entering a public file through the new prose | mitigate | this plan edits comments and printed strings only and copies no command output; credential-screen both scripts before commit — the repo is public and has one prior exposure still recoverable via `git log -S` |
| T-06-44-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing — no package manager is invoked |
</threat_model>

<verification>
- `bash -n` is clean on both scripts.
- All three `ARTIST_PROOF_ROWS` definitions survive verbatim, both numeric columns intact.
- The exit ladder's four gates are in their original order.
- The changed-line audit on both scripts reports `0` lines that are neither comments nor printed
  messages, and `0` added control-flow or `EXIT_CODE` lines.
- `never summed` is present; the E6-second-measurement sentence is present; `FullRefresh` count is
  `0` in both scripts.
</verification>

<success_criteria>
Every sentence in the instrument and in the health entry point that named the wrong owner for
CONF-04's Jellyfin half now states the round-5 truth — written downstream of a verdict the
instrument earned on untouched code in wave 20, and carrying a mechanical proof that not one line of
logic, threshold, counter or exit code moved to get there.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-44-SUMMARY.md` when done.
</output>
