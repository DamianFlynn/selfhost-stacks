---
phase: 06-tagger-configuration-and-dry-run
plan: 35
type: execute
wave: 16
depends_on: ["06-34"]   # round 3's last plan: these four fix the code plans 06-30..06-33 wrote and 06-34 dispositioned
gap_closure: true
gap_closure_round: 4
files_modified:
  - scripts/quick-health-check.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt
autonomous: true
requirements: [CONF-03, CONF-06]
closes_findings: [R4-01, R4-07, R4-08]

must_haves:
  truths:
    - "The hand-escaped `\\\"` wrapper the file's own ⛔ paragraph forbids by name appears nowhere in the file — including at the four `_drift_pair` call sites that sit eleven lines BELOW the prohibition itself"
    - "R3-01's appended sentence no longer says the forbidden-shape class was closed by plan 06-30; it records that four instances survived, where they were, and which plan closed them"
    - "No sentence in the census block states a count of rendered knobs as a measured fact — the block states the per-plan lists and the recipe, and nothing that a later edit moves without touching the sentence"
    - "`DASH_RESOLVE_IP` reaches its remote command string only through its rendered `_Q` form, and it is named in the census block's per-plan list"
    - "The census recipe's second grep matches every remote call site in the file, including the two that use `bounded_ssh … ssh $SSH_OPTS` with no `-n` — and the recipe line is still not an occurrence of what it measures"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "four `_drift_pair` paths and the dashboard resolve address rendered once each, a census block with no self-moving count, and a widened recipe"
      contains: "R4-01"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt"
      provides: "the before/after remote command strings for each newly rendered value, captured by echo and never sent, plus the widened recipe's output showing the two previously-missed call sites"
  key_links:
    - from: "DRIFT_APPDATA_ROOT (operator knob)"
      to: "the remote `_drift_pair` invocations inside DRIFT_CMD"
      via: "four per-pair `printf '%q'` renderings, interpolated unquoted into a body that already quotes `\"$3\"`"
      pattern: "DRIFT_AUDIO_Q"
    - from: "DASH_RESOLVE_IP (operator knob)"
      to: "the remote `curl --resolve` command string"
      via: "a `printf '%q'` rendering beside the knob definition"
      pattern: "DASH_RESOLVE_IP_Q"
---

<objective>
Close the three round-4 findings that land in `scripts/quick-health-check.sh`: **WR-01**, **IN-01**
and **IN-02** of `06-REVIEW-GAP3.md`.

⚠ **FINDING IDs IN THIS ROUND ARE ALIASED, AND THIS IS NOT OPTIONAL.** Round 4's report reuses
round 1's `WR-*` / `IN-*` namespace. Round 1's IDs are already cited **in band in all four
scripts** — `quick-health-check.sh` alone carries seven `WR-01` mentions, five `WR-05` and one
`WR-02`, none of them round 4's. Writing a bare `WR-01` into this file would be ambiguous to every
future reader and would make any grep for it non-discriminating. **In band, and in every verify
block, this round's findings are `R4-01 … R4-10`**, continuing the phase's per-round convention
(`CR`/`WR`/`IN` = round 1, `GC-*` = round 2, `R3-*` = round 3). Plan 06-39 carries the mapping
table. This plan owns:

| Round-4 report ID | In-band ID | Subject |
|---|---|---|
| WR-01 | **R4-01** | the ⛔-forbidden `\"$KNOB\"` shape is live four times, eleven lines below its own prohibition, while R3-01's new text says the class is closed |
| IN-01 | **R4-07** | `DASH_RESOLVE_IP` still reaches a remote command string raw, and the census does not mention it |
| IN-02 | **R4-08** | the census recipe's second grep misses the two `bounded_ssh` remote call sites |

**R4-01 is the round's headline, and it is the same charge round 3 levelled at round 2, now
levelled at round 3.** Plan 06-30 removed the hand-escaped wrapper from the `EXTCONF_PATH` site
three hundred lines *below* the prohibition, and wrote, in the same hunk, that the paragraph *"was
simply never applied to the EXTCONF_PATH site"* — implying the instance was singular and the class
now closed. It is not: **four identical instances sit eight to eleven lines below the prohibition
paragraph itself**, inside the very `DRIFT_CMD` string the prohibition is embedded in, interpolating
the overridable `DRIFT_APPDATA_ROOT`.

**Do not overstate it, and do not dramatise it.** The review bounds the finding explicitly and the
bound must survive into whatever you write: a non-default `DRIFT_APPDATA_ROOT` already forces
`EXIT_CODE=1` at the override guard, a broken remote command lands in the could-not-look arm, and
there is no privilege crossing — the operator who can set the variable already has root on LXC 100.
**The defect is the claim, not the exploit.**

**Say which fix you are making, per finding, and say it in band:**
- **R4-01 — BOTH.** Four call sites get rendered values (code); R3-01's closure sentence and the
  census block's *"all nine"* count are corrected (claim).
- **R4-07 — BOTH.** One new rendering (code); the census list gains the knob (claim).
- **R4-08 — CLAIM CORRECTION ONLY.** The recipe is a comment. Widening it changes no behaviour.

Output: a file in which the shape its own ⛔ paragraph forbids does not occur; a census block that
states lists and recipes and no count; and a recipe whose haystack is the whole haystack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP3.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the four `_drift_pair` paths and the dashboard resolve address, and correct the two sentences that say the class is closed</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt</files>
  <read_first>
    - **`scripts/quick-health-check.sh` — the file you are modifying.** Read the whole GC-17 / R3-01
      census block and the `DRIFT_CMD` construction that follows it, as one continuous read.
    - **Do not trust any line number in this plan or in `06-REVIEW-GAP3.md`.** Every citation below
      is an anchor string. Resolve each with `/usr/bin/grep -n '<anchor>' scripts/quick-health-check.sh`
      before editing. This file and `.planning/ROADMAP.md` have both moved under their own commits
      inside this phase, and `DEF-06-29-02` / `DEF-06-29-06` exist because citations went stale here
      twice already.
    - The four live instances. Anchor: `/usr/bin/grep -n '^_drift_pair ' scripts/quick-health-check.sh`
      — four hits. Read all four, and read `_drift_pair() {` above them: **the body already quotes
      `\"\$3\"`**, which is why the call sites need no quotes at all once the value is rendered.
    - The knob definition and its override guard: `DRIFT_APPDATA_ROOT="${DRIFT_APPDATA_ROOT:-` and
      `if [ "$DRIFT_APPDATA_ROOT" != "/mnt/fast/appdata" ]; then`. The guard forces `EXIT_CODE=1`;
      that is the bound the review states and your comment must keep.
    - The existing rendering shape, which is the one to copy — nine today:
      `/usr/bin/grep -n '_Q=\$(printf' scripts/quick-health-check.sh`. Note where each sits:
      `DRIFT_REPO_ROOT_Q` is rendered **immediately above** `DRIFT_CMD=`, inside the block. That is
      the precedent for where the four new per-pair renderings go.
    - The prohibition paragraph and R3-01's appended claim. Anchors:
      `DO NOT "fix" a future site of this shape by hand-escaping quotes inside the command string`,
      `THIS PARAGRAPH WAS RIGHT WHEN IT WAS WRITTEN AND IS UNCHANGED`, and the clause to correct,
      `was simply never applied`.
    - The census block's own count sentence. Anchor: `THE BOUND, and it has not changed: all nine are ADDITIVE knobs`.
      **That "nine" is a self-moving number of exactly the class this phase has now mis-shipped three
      rounds running** (GC-10, R3-05, R4-05). It is in scope here because this edit moves it.
    - The `DASH_RESOLVE_IP` knob and its one remote site. Anchors: `DASH_RESOLVE_IP="${DASH_RESOLVE_IP:-`,
      the override guard `if [ "$DASH_RESOLVE_IP" != "127.0.0.1" ]; then`, the remote line
      `--resolve traefik.deercrest.info:443:$DASH_RESOLVE_IP https://`, and **two non-remote uses that
      must stay RAW** — the prose bullet `PUBLIC REACHABILITY` and the operator echo
      `listening on 443 at $DASH_RESOLVE_IP`.
    - `06-REVIEW-GAP3.md` §§ WR-01 and IN-01 in full, **including WR-01's "Bound, so the finding is
      not overstated" paragraph** and its parenthetical warning that `%q` renders a whole word, so
      the suffix must be rendered *with* the value rather than appended to the rendered form.
  </read_first>
  <action>
**Disposition, stated up front and repeated in band: R4-01 is a CODE WIDENING AND A CLAIM
CORRECTION. R4-07 is the same, in miniature.** Neither half of either closes its finding alone.

**(a) Render one value per drift pair, not one for the root.** Immediately above `DRIFT_CMD=`,
beside the existing `DRIFT_REPO_ROOT_Q` rendering, add four renderings — suggested names
`DRIFT_AUDIO_Q`, `DRIFT_SABBEETS_Q`, `DRIFT_SURVIVOR_Q`, `DRIFT_FLASK_Q` — each rendering the
**whole concatenated path**, e.g. `printf '%q' "$DRIFT_APPDATA_ROOT/arrs/sabnzbd/config/scripts/audio.bash"`.

⚠ **Do not render `DRIFT_APPDATA_ROOT` alone and append the suffix to the rendered form.** `%q`
renders a complete word; a suffix bolted onto the rendered value is outside the escaping and
reintroduces the split on the first space. The review states this explicitly and it is the one way
this fix ships broken while looking right.

**(b) Interpolate the rendered values unquoted at the four call sites.** Delete the `\"` … `\"`
wrapper entirely; do not replace it with anything. The body's `sha256sum \"\$3\"` supplies the
quoting on the remote side, so the call line needs only `$DRIFT_AUDIO_Q`. Change nothing else on
those four lines — the first two positional arguments are literals under version control.

⛔ **Do not reproduce the retired construction anywhere in the file, including in a comment
describing what you removed.** The verification forbids the literal file-wide and that is
deliberate: a grep for a token's absence cannot distinguish a claim from its retraction. This is
`06-DISPOSITIONS-GAP2.md` § Lessons 4, and round 2 shipped that defect twice. Describe the shape in
prose — *"a hand-escaped `\"` wrapper around an overridable path, inside the remote command
string"* — and reconstruct the literal only in the artifact.

**(c) Render `DASH_RESOLVE_IP` (R4-07).** `DASH_RESOLVE_IP_Q=$(printf '%q' "$DASH_RESOLVE_IP")`
immediately below the knob definition, in the shape the nine existing renderings use. Swap the
rendered form into the `curl --resolve` remote string **only**. The override guard's comparison and
the operator echo keep the raw value: rendering them would print backslashes at a human.

**(d) Correct R3-01's closure sentence.** The clause `was simply never applied` must go. Replace it
with a record that states, plainly and without inflation:
  - the prohibition paragraph itself was right and is unchanged — **again**;
  - it was not applied to the `EXTCONF_PATH` site (06-30 closed that, R3-01), **and it was also not
    applied to the four `_drift_pair` sites eleven lines below the paragraph itself**, which is
    where R4-01 found them;
  - therefore **the round-3 sentence claiming the class was closed was false when it was written**,
    and this is the second consecutive round in which a documented-forbidden shape survived inside
    the file that documents it;
  - the bound, carried over intact: these are additive operator knobs, every default contains no
    space, a non-default `DRIFT_APPDATA_ROOT` already forces `EXIT_CODE=1`, a broken remote command
    lands in the could-not-look arm, and there is **no privilege crossing** — the operator who sets
    the knob already has root on LXC 100. The consequence is a false claim, not an exploit;
  - name **R4-01**.

**(e) Withdraw the "all nine" count.** Replace that number with a statement in the form the census
block already uses for the total: say that the rendered set's size is **deliberately not stated
here**, give the recipe, and say in one clause why — a number written into a file that greps itself
is moved by the next edit without touching the sentence that states it. This is the *third*
consecutive round in which that exact class shipped (GC-10 → R3-05 → R4-05, the last of them in a
sibling file in the very round that corrected it here). Say so in one clause; it is the reason the
block is a recipe.

**(f) Extend the per-plan list (R4-07's claim half).** The list of what each plan owned gains a
06-35 line naming `DRIFT_APPDATA_ROOT` (four call sites, one knob) and `DASH_RESOLVE_IP`. Keep the
existing 06-26 and 06-30 lines exactly as they stand — they are accurate records of what those
plans owned, and rewriting them would turn two true historical statements into two claims about a
file state neither describes.

**(g) Record the drive.** Into `artifacts/06-35-qhc-drift-quoting-and-census.txt`, for each of the
five newly rendered values: set the knob to a value containing a space, and additionally one
containing a `"`, then **echo the remote command string that would be sent, before and after the
change, and do not run it.** Show the space surviving as a single word after. This is a capture, not
an execution: `quick-health-check.sh` contacts LXC 100 (172.16.1.159) and atlantis (172.16.1.158),
and **this plan runs it not once**.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides the exit code
F=scripts/quick-health-check.sh
G=/usr/bin/grep   # absolute: the operator's zsh aliases grep -> ugrep, and BSD grep is not GNU grep

# --- (a)(b) the forbidden shape is GONE file-wide, including from any comment describing it ---
# This is the whole construction, not a bare variable name: `\"$DRIFT_APPDATA_ROOT/` is not a
# substring of any rendered replacement, because the replacement names a different variable
# entirely. A test on `DRIFT_APPDATA_ROOT` alone would match the knob definition, the override
# guard and the prose, and would be unsatisfiable.
test "$($G -cF '\"$DRIFT_APPDATA_ROOT/' $F)" -eq 0

# The four call sites still exist and still number four -- this plan changes their arguments, not
# their count, so pinning 4 here does not pin a number this plan moves.
test "$($G -c '^_drift_pair ' $F)" -eq 4
# and each now carries a rendered word. Named individually: that is the assertion that matters.
for v in DRIFT_AUDIO_Q DRIFT_SABBEETS_Q DRIFT_SURVIVOR_Q DRIFT_FLASK_Q; do
  test "$($G -cF "$v=\$(printf '%q'" $F)" -eq 1
  test "$($G -cF "\$$v" $F)" -ge 2      # the rendering line plus at least its call site
done
# the rendered value must carry the WHOLE path, not the root with a suffix bolted on outside the
# escaping. Anchor on the longest suffix: if it appears inside the printf argument, the render is
# whole-word. If someone appended it to the rendered form instead, this returns 0.
test "$($G -cF "printf '%q' \"\$DRIFT_APPDATA_ROOT/arrs/sabnzbd/config/scripts/audio.bash\"" $F)" -eq 1

# --- (c) R4-07: the dashboard resolve address is rendered, and only at the remote site ---
# Absence pattern includes the FOLLOWING token, because `$DASH_RESOLVE_IP` IS a substring of
# `$DASH_RESOLVE_IP_Q`. Testing the bare name would fail the correct change -- the exact defect
# that cost round 2 a plan.
test "$($G -cF '$DASH_RESOLVE_IP https://traefik' $F)" -eq 0
test "$($G -cF '$DASH_RESOLVE_IP_Q https://traefik' $F)" -eq 1
test "$($G -cF "DASH_RESOLVE_IP_Q=\$(printf '%q'" $F)" -eq 1
# the non-remote uses still read the raw knob
test "$($G -cF '"$DASH_RESOLVE_IP" != ' $F)" -ge 1
test "$($G -cF 'listening on 443 at $DASH_RESOLVE_IP' $F)" -eq 1

# --- (d) the false closure sentence is withdrawn and the prohibition survives ---
test "$($G -cF 'was simply never applied' $F)" -eq 0
test "$($G -cF 'by hand-escaping quotes inside the command string' $F)" -ge 1
test "$($G -cF 'THIS PARAGRAPH WAS RIGHT WHEN IT WAS WRITTEN AND IS UNCHANGED' $F)" -ge 1
test "$($G -cF 'R4-01' $F)" -ge 1
# R3-01's own record is still cited -- this is a correction to a claim, not an erasure of history
test "$($G -cF 'R3-01' $F)" -ge 1

# --- (e) the self-moving count is withdrawn ---
test "$($G -cF 'all nine are ADDITIVE knobs' $F)" -eq 0
# and the bound itself survives in some form: the two load-bearing clauses are still present
test "$($G -ciF 'ADDITIVE knob' $F)" -ge 1
test "$($G -ciF 'static reasoning' $F)" -ge 1

# --- (f) the per-plan list gained this plan's line without disturbing the earlier two ---
test "$($G -cF '06-26' $F)" -ge 1
test "$($G -cF '06-30' $F)" -ge 1
test "$($G -cF '06-35' $F)" -ge 1

# --- the total rendering count only ever GREW. -ge, never -eq: this plan adds five and task 2
# adds none, but an -eq here is a constant that the next plan in this file moves silently.
test "$($G -cF '_Q=$(printf' $F)" -ge 14

bash -n $F

# --- (g) the drive is captured, not executed ---
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt
test -s "$ART"
test "$($G -cF 'R4-01' "$ART")" -ge 1
test "$($G -cF 'DRIFT_APPDATA_ROOT' "$ART")" -ge 1
test "$($G -cF 'DASH_RESOLVE_IP' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- `/usr/bin/grep -cF '\"$DRIFT_APPDATA_ROOT/' scripts/quick-health-check.sh` returns **0**, and the
  literal appears in no comment either.
- Four new `_Q` renderings exist by name, each rendering a **whole** concatenated path, sited beside
  the existing `DRIFT_REPO_ROOT_Q` rendering; the four `_drift_pair` call lines interpolate them
  unquoted and still number exactly four.
- `DASH_RESOLVE_IP_Q` exists and is the only form interpolated into the `curl --resolve` remote
  string; the override comparison and the operator echo still read the raw knob.
- The clause `was simply never applied` is gone; the prohibition paragraph and the
  `THIS PARAGRAPH WAS RIGHT…` sentence survive; `R4-01` and `R3-01` are both named.
- The literal `all nine are ADDITIVE knobs` is gone and no replacement states a count of rendered
  knobs as a fact; the additive-knob and static-reasoning bounds survive.
- The per-plan list carries 06-26, 06-30 and 06-35 lines, with the first two byte-unchanged.
- `bash -n scripts/quick-health-check.sh` exits 0.
- The artifact holds the before/after remote command string for all five rendered values, produced
  by `echo`; `quick-health-check.sh` was executed **zero** times.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Widen the census recipe's second grep so its haystack is the whole haystack</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt</files>
  <read_first>
    - **`scripts/quick-health-check.sh`** — the recipe block you are modifying. Anchor:
      `/usr/bin/grep -n "ssh -n .SSH_OPTS'" scripts/quick-health-check.sh` resolves the recipe line
      itself. Read the whole recipe paragraph, including the sentence that introduces the criterion
      (*"for each knob that reaches a remote command string, check that only its `_Q` form is
      interpolated"*) — the criterion is right; only the haystack is short.
    - The two call sites the recipe misses. Anchor:
      `/usr/bin/grep -n 'bounded_ssh "\$PROBE_TIMEOUT" ssh \$SSH_OPTS' scripts/quick-health-check.sh`
      — two hits, both `root@172.16.1.159`, neither carrying `-n`. Read both: **neither interpolates
      a knob today**, which is why this is Info and not a Warning. Your comment must say so.
    - `06-REVIEW-GAP3.md` § IN-02 in full, including its proposed widened pattern.
    - **The self-non-matching property of the existing recipe line, which must survive your edit.**
      The recipe is a comment inside the file it greps. The current line escapes the `$` so that the
      line is not itself a hit. Verify the same holds for whatever you write, by running it.
  </read_first>
  <action>
**Disposition: R4-08 is a CLAIM CORRECTION ONLY.** The recipe lives in a comment. Nothing
executable changes, no call site moves, and no `_Q` is added by this task. Widening any code here to
make the old recipe complete would be the exact inversion this round exists to stop.

**(a) Widen the second grep** to an `-E` pattern that matches both forms, e.g.
`/usr/bin/grep -nE 'ssh (-n )?\$SSH_OPTS'`, keeping the recipe's existing column alignment so the
two greps still read as a pair.

**(b) State what the widening bought, in one clause**: two `bounded_ssh` call sites that carry no
`-n` were outside the old haystack, so a recipe offered as the durable replacement for a wrong
number under-counted by two. Name **R4-08**. Add the bound the review states: **neither of those two
sites interpolates a knob today**, so nothing was wrong — the recipe was.

**(c) Prove the recipe is still not an occurrence of what it measures.** Run both greps as written
and confirm the recipe's own lines are absent from the output. Record the run in the artifact. This
is the same device `EXIT-CODE BEHAVIOUR CHANGE[D]` uses elsewhere in this file, and the reason to
check rather than assume is that the widened pattern contains more literal text than the one it
replaces.

**(d) Do not touch the first grep** (the knob-definition pattern). The review does not fault it, and
re-deriving it is scope creep.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
F=scripts/quick-health-check.sh
G=/usr/bin/grep

# (a) the widened pattern is present in band. The old narrow recipe line is a whole construction
# that is NOT a substring of the widened one -- the widened form carries an alternation where the
# old carried a literal -- so this pair of tests is discriminating.
test "$($G -cF "ssh (-n )?\\\$SSH_OPTS" $F)" -ge 1

# (b) the finding is named
test "$($G -cF 'R4-08' $F)" -ge 1

# (c) THE RECIPE STILL DOES NOT COUNT ITSELF, and the haystack genuinely widened.
# The VACUITY GUARD COMES FIRST and is not optional: an empty hit set would satisfy the
# no-comment-lines test trivially. That is CR-01's and GC-03's shape, and it printed a green tick
# over nothing twice in this phase already.
test "$($G -cE 'ssh (-n )?\$SSH_OPTS' $F)" -ge 3
test "$($G -nE 'ssh (-n )?\$SSH_OPTS' $F | $G -c '^[0-9]*:[[:space:]]*#')" -eq 0
test "$($G -E 'ssh (-n )?\$SSH_OPTS' $F | $G -cF 'bounded_ssh')" -eq 2

bash -n $F

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-35-qhc-drift-quoting-and-census.txt
test "$($G -cF 'R4-08' "$ART")" -ge 1
test "$($G -cF 'bounded_ssh' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- The census block's second recipe grep is an `-E` pattern matching both `ssh -n $SSH_OPTS` and
  `ssh $SSH_OPTS`; the first recipe grep is byte-unchanged.
- Running the widened recipe over the file returns at least three hits, **zero** of which are
  comment lines — the recipe is still not an occurrence of what it measures — and returns **both**
  `bounded_ssh` call sites.
- `R4-08` is named in band, together with the bound that neither newly-covered site interpolates a
  knob today.
- No `_Q` rendering, no call site and no executable line changed in this task; `bash -n` exits 0.
- The artifact records the widened recipe's output, showing the two previously-missed sites and the
  absence of the recipe's own lines.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (`DRIFT_APPDATA_ROOT`) → the remote `DRIFT_CMD` string parsed by `root@172.16.1.159`'s bash | four hand-escaped `\"` wrappers; a value bearing `"` terminates the quoting, a value bearing `$` or a backtick is expanded or executed **by the remote shell as root** |
| operator env (`DASH_RESOLVE_IP`) → the remote `curl --resolve` command string | word-splits on a space; fails loudly, no green tick reachable |
| the file's own prose → the next reader | a prohibition paragraph plus a sentence saying the class it forbids is closed, with four live instances eleven lines below both |
| the file's own prose → the file's own census | a stated count of rendered knobs that this very edit moves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R401 | Tampering (remote command re-parsing as root) | the four `_drift_pair` call sites in `quick-health-check.sh` | mitigate | one `printf '%q'` rendering per **whole** path, interpolated unquoted into a body that already quotes `"$3"`; drives captured by `echo`, never sent |
| T-06-R401b | Repudiation (a comment asserting a class closed while four instances live below it) | R3-01's appended closure sentence | mitigate | the clause withdrawn; the four sites named; the second consecutive survival recorded in band |
| T-06-R401c | Repudiation (a census stating a count its own edit moves) | the `all nine` sentence | mitigate | the number withdrawn and replaced by the existing recipe, with the reason stated in one clause |
| T-06-R407 | Tampering (remote command word-splitting) | `DASH_RESOLVE_IP` at the `curl --resolve` site | mitigate | rendered once beside the knob definition; non-remote uses stay raw |
| T-06-R408 | Repudiation (a durable recipe that under-counts its own haystack) | the census recipe's second grep | mitigate | widened to `ssh (-n )?\$SSH_OPTS`, re-checked to be non-self-matching, bound stated |
| T-06-35-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- **`scripts/quick-health-check.sh` is NOT executed by this plan, not once.** It contacts LXC 100
  (172.16.1.159) and the Proxmox host (172.16.1.158). Every drive here is a captured command string
  produced by `echo`, or a `grep` over the file.
- `bash -n scripts/quick-health-check.sh` parses clean after every task.
- **How these verify blocks are driven, which is not optional.** Before the fix, extract both
  `<automated>` blocks to `$TMPDIR`, run `bash -n` on each, then run each **against the unfixed
  tree**. Each must fail at its **first** fix-related assertion — task 1 at the
  `\"$DRIFT_APPDATA_ROOT/` absence test, task 2 at the widened-pattern presence test — and neither
  may reach its end. A block that passes pre-fix is vacuous and must be rewritten before any edit is
  made. Record both pre-fix transcripts in the artifact. This is round 2's practice (all 19 blocks
  extracted, parsed and driven pre-fix) and it is why `DEF-06-21-08` and `DEF-06-29-07` exist.
- No `--run`, no `--arm`, no `git push`, no host `git pull`, no estate contact of any kind.
- `git diff --name-only -- scripts/` lists only `scripts/quick-health-check.sh`.
- This plan must not touch `scripts/setup-neocortex-memory.sh`,
  `stacks/selfhosted/neocortex-memory/` or `stacks/selfhosted/agentic-os/` — other sessions commit
  to `main` in this same checkout.
- **No requirement checkbox moves. CONF-04 is not closed and is not claimed. The phase is not
  declared complete** — that is the verifier's call, and `/gsd-verify 06` runs after this round.
- `06-VERIFICATION.md` is **stale and out of scope**. Do not read it as a gap source, do not
  "correct" it, do not touch it.
</verification>

<success_criteria>
- R4-01 closed as a code widening **and** a claim correction, with both halves visible: four whole
  paths rendered, and the sentence claiming the class was closed replaced by one that records where
  the four survivors were.
- The construction the file's own ⛔ paragraph forbids appears nowhere in the file, including in
  prose describing its removal.
- R4-07 closed the same way, in miniature.
- R4-08 closed as a claim correction only — no executable line changed — and the widened recipe is
  demonstrably still not an occurrence of what it measures.
- No number stating a count of rendered knobs survives anywhere in the census block.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-35-SUMMARY.md` when done.
Include a **NOT-DRIVEN register** naming, at minimum, every branch reachable only from a live
`quick-health-check.sh` run against the estate — which is all of them, since this plan executes the
script zero times — and state per finding whether the fix was CODE, CLAIM CORRECTION or BOTH.
</output>
