================================================================================
06-42 — THE DEPLOYED, UNMODIFIED INSTRUMENT, RE-RUN (the independent corroboration)
================================================================================
Taken : 2026-09-24T13:47:01Z  (UTC)
Host  : selfhost (LXC 100, 172.16.1.159), run from the deployed path
        /mnt/fast/stacks/scripts/check-music-consumers.sh — no override, no copy step, and
        none may be invented. The script reaches this host by git and by nothing else.
Bounds: the remote driver carries `set -o pipefail`; the script's exit status was read into a
        variable BEFORE anything was piped, and the ssh exit status was read into a variable at
        the workstation before anything was piped there. `timeout N cmd | wc -l` silently exits
        0 (DEF-06-10-01) and that shape is not used. The run was bounded at 900 s Linux-side
        and completed well inside it. Measured ssh rc: 0. stderr: empty.

WHY THIS RUN LIVES IN THE MEASUREMENT PLAN AND NOT IN 06-44. It was originally 06-44's first
task, which runs AFTER the operator's binding decision in 06-43. That ordering let the operator
commit to a branch before the one automated cross-check that could catch a wrong one had run.
It is relocated here so BOTH measurements exist before anything is decided. Nothing else about
it changed, and it still runs against code nobody in this round has touched — 06-44's prose
edits come later, by construction.

--------------------------------------------------------------------------------
THE FIXED-FORMAT LINES (06-43's branch recompute and 06-45's tick gate parse these)
--------------------------------------------------------------------------------
INSTRUMENT RUN: MEASURED
HOST CHECKOUT: MATCH ee82fb20a4383ee5f274ce177f6d0a03de0797c4
EXIT CODE: 3
JF_AT_TARGET: 0
JF_PENDING: 3
MA_AT_TARGET: 2
MA_REPORTED: 1

--------------------------------------------------------------------------------
WHY `MEASURED` IS EARNED HERE, ASSERTION BY ASSERTION
--------------------------------------------------------------------------------
`MEASURED` may be written ONLY when the host checkout matched, the run completed within its
bound, and the `📊 6. Summary` block was found and parsed. Anything else — a STALE checkout, a
`timeout` kill, an empty transcript, a summary whose heading could not be anchored — is
COULD NOT LOOK with the reason named. 06-43 treats a non-`MEASURED` value as disqualifying for
BRANCH: A, and that is deliberate: an uncorroborated close is not a close.

  (1) HOST CHECKOUT MATCHED, and it is a real comparison rather than an assumed one:
        local  HEAD : ee82fb20a4383ee5f274ce177f6d0a03de0797c4
        host   HEAD : ee82fb20a4383ee5f274ce177f6d0a03de0797c4   (`git rev-parse HEAD` in
                      /mnt/fast/stacks on LXC 100)
      ⚠ THE HOST WAS TWO COMMITS BEHIND WHEN THIS TASK STARTED, AND THAT IS RECORDED RATHER
        THAN SMOOTHED OVER. This plan's own tasks 1 and 2 committed the after-state artifact,
        which moved local HEAD past the host's e7e5406. The remedy the plan itself names was
        applied — push, `git pull --ff-only` on the host, re-run — and it took under a minute.
        Both commits touched only `.planning/` artifacts; neither touched `scripts/`. The
        checkout is now exactly equal, so this is a MATCH on the real test and not on a
        relaxed one. A stale checkout reported as corroboration is exactly the class this
        phase spent four rounds removing, which is why the equality is asserted rather than
        argued away as harmless.

  (2) THE DEPLOYED FILE IS THE FILE UNDER TEST, proved by content and not only by commit id:
        sha256 of /mnt/fast/stacks/scripts/check-music-consumers.sh on LXC 100
          : 1ed695cf483029c2e7c3fd815fc97bc1711a8a3e499556910955332a7a6ee819
        sha256 of scripts/check-music-consumers.sh at the workstation
          : 1ed695cf483029c2e7c3fd815fc97bc1711a8a3e499556910955332a7a6ee819
        -> byte-identical.
      ⚠ A STALE CLAIM CORRECTED. 06-41 SECTION B states the host's copy at /mnt/fast/stacks is
        pre-Phase-6 (c67d497) and carries NO `ARTIST_PROOF_ROWS` at all. MEASURED HERE, THAT IS
        NO LONGER TRUE: the host copy is byte-identical to the repo copy and does carry the
        table. 06-41's statement was correct for its own run and is superseded by this
        measurement, not contradicted retroactively. It is recorded so a later reader does not
        re-derive the old conclusion from the older artifact.
        `git status --porcelain -- scripts/` on the host: 0 lines — the deployed tree is clean,
        so the sha is the committed content and not a local edit that happens to hash the same.

  (3) NO INSTRUMENT SCRIPT WAS EDITED BY THIS ROUND. At the workstation, at the time of this
      run, `git diff --name-only -- scripts/check-music-consumers.sh scripts/quick-health-check.sh`
      returned nothing, and `git status --short scripts/` returned nothing. The last commit
      touching check-music-consumers.sh is dc0fd47 (plan 06-17, WR-03). The corroboration is
      therefore earned on untouched code. 06-44 owns every file change to the instrument and
      runs later precisely so this measurement is taken first.

  (4) THE RUN COMPLETED IN BOUND and the summary was found and parsed: the script returned an
      exit status of its own (3, not 124), the transcript is 217 lines, and the
      `📊 6. Summary` heading was anchored and its four `artist rows` counters read off it.

  (5) THE TWO JELLYFIN COUNTERS ACCOUNT FOR EVERY PINNED ROW: 0 at target + 3 pending = 3,
      the number of rows in ARTIST_PROOF_ROWS. The MA counters (2 at target, 1 reported) are
      recorded beside them and are NOT added to them, here or anywhere.

--------------------------------------------------------------------------------
INTERPRETATION, IN BAND, ADJUSTING NOTHING
--------------------------------------------------------------------------------
  The status of 3 means MEASURED, BUT NOT AT TARGET on at least one row. `FAILURES total: 0`
  in the summary confirms it is the pending gate that fired and not the failure gate — under
  `check-music-consumers.sh`'s own EXIT-CODE CONVENTION, a measured failure outranks a pending
  row and would have produced 1 instead. Nothing is broken and nothing needs debugging.

  ⚠ A 3 WOULD HAVE BEEN EXPECTED EVEN ON A FULLY SUCCESSFUL RE-PROBE, because MA row 1 remains
    a reported measured discrepancy that plan 06-13 owns. On a successful re-probe the counters
    would read 3 at target (JF) / 0 pending (JF) / 1 reported (MA) — AND THE PROCESS WOULD
    STILL HAVE ENDED ON 3. The status is therefore recorded and is deliberately NOT an input to
    06-43's branch: it cannot distinguish A from B, because the MA row holds it at 3 either way.
    It is not a defect, it is not a failed corroboration, and no line of any script may be
    changed to make it end on 0. This task edited no script.

  WHAT THE COUNTERS ACTUALLY SAY, AND WHAT THEY CORROBORATE. 0 of 3 Jellyfin rows are at
  target; 3 of 3 are at their recorded 2026-09-20 baseline. That is the SAME result this
  plan's SECTION C measured through a completely different route — a hand-built GET against
  the API with exact-Path selection, versus the deployed script's own § 4b reading
  `ArtistItems`, distinct Ids and the `;` screen. TWO INSTRUMENTS, ONE ANSWER: the re-probe did
  not move the rows.

  An exit status of 1 would have meant a measured failure outranked the pending rows and would
  NOT have been written as MEASURED corroboration of anything. An exit status of 0 would have
  meant both pending counters were zero, contradicting the MA half's recorded discrepancy, and
  would have been recorded as a finding rather than a win. Neither occurred.

  MA answered on this run (2.11.0b2, route http://172.16.1.31:8095, 70 albums on the pinned
  local provider), so section 4c is a measurement and not the `ma_fail` UNKNOWN-not-green path.

--------------------------------------------------------------------------------
THE FULL TRANSCRIPT, ANSI COLOUR STRIPPED, VERBATIM
--------------------------------------------------------------------------------
The bytes below are the script's own stdout+stderr with SGR escapes removed and nothing else
altered. They were not retyped: retyping a transcript is how a "verbatim" record stops being
one. The `📊 6. Summary` block and its four `artist rows` lines are inside this transcript, at
their own position in the run, rather than quoted a second time — one copy that is certainly
the real one beats two copies that a reader has to reconcile.

Credential-screened before commit (this repository is PUBLIC): the only matches for
credential-shaped strings are the script's own `600 root` MODE assertions on the two secret
FILE PATHS, which carry no values; the long opaque strings are Jellyfin item Ids and the Music
Assistant provider instance id, both of which are already committed in this repository.

--- CONSUMERS TRANSCRIPT BEGIN ---
🎧 Music Consumers Audit (export + Music Assistant + Jellyfin)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  library:      /mnt/tank/media/Music
  export host:  172.16.1.158 (atlantis)
  NFS client:   172.16.1.31 (HA NUC, LAN source address - NOT the tailnet address, D-13)
  host:         selfhost
  date:         2026-09-24T13:47:01Z
  MA proven at: 2.11.0b2   (D-21 amended / D-56 — auto_update is ON, so this
                          recorded version IS the drift detector)
  mode:         assert (exits non-zero on any failed check)

🧰 0. Toolchain preconditions
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ curl /usr/bin/curl — curl 8.14.1 (x86_64-pc-linux-gnu) libcurl/8.14.1 OpenSSL/3.5.1 zlib/1.3.1 brotli/1.1.0 zstd/1.5.7 libidn2/2.3.8 libpsl/0.21.2 libssh2/1.11.1 nghttp2/1.64.0 nghttp3/1.8.0 librtmp/2.3 OpenLDAP/2.6.10
  ✅ jq /usr/bin/jq — jq-1.7
  ✅ ssh /usr/bin/ssh — OpenSSH_10.0p2 Debian-7, OpenSSL 3.5.1 1 Jul 2025
  ✅ sha256sum /usr/bin/sha256sum — sha256sum (GNU coreutils) 9.7
  ✅ docker /usr/bin/docker — Docker version 29.2.1, build a5c7197

  ✅ MA credential /mnt/fast/secrets/ma-deercrest.env (600 root)
  ✅ Jellyfin credential /mnt/fast/secrets/jellyfin-deercrest.env (600 root)

  ✅ export queries delegated to root@172.16.1.158 (exportfs does not exist on LXC 100)
  ✅ Music Assistant reachable at http://172.16.1.31:8095, auth/login OK (fresh JWT, not cached)
  ✅ Jellyfin resolved to 192.168.90.17:8096 via docker inspect (never a pinned literal, never the bare name)
  ⚠️  HA_SSH_KEY not set/readable on this host — section 5's 'ha mounts info' enrichment is SKIPPED.
      This is by design, not a gap: the NUC SSH key lives on the workstation and copying it
      here would be credential sprawl for an enrichment check. The MA-side assertions in
      sections 3 and 5 always run and are stronger evidence — an emergency bind leaves
      /media/music empty, so MA's provider-scoped album count and the three exact-match
      assertions all fail. Do not 'fix' this by adding the key.

  routes: export=ssh:172.16.1.158  ma=http://172.16.1.31:8095  jellyfin=docker-inspect:192.168.90.17:8096  ha=skipped
  MA version: live=2.11.0b2  proven-at=2.11.0b2

📤 1. The NFS export on atlantis (CONS-01, D-12/D-13, T-02-06/T-02-07)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  export line: /mnt/tank/media/Music 172.16.1.31(sync,wdelay,hide,no_subtree_check,mountpoint,anonuid=568,anongid=568,sec=sys,ro,secure,root_squash,all_squash)
  ✅ CONS-01: client is exactly 172.16.1.31
  ✅ CONS-01: client field is neither '*' nor a CIDR
  ✅ CONS-01: option present — ro
  ✅ CONS-01: option present — all_squash
  ✅ CONS-01: option present — anonuid=568
  ✅ CONS-01: option present — anongid=568
  ✅ CONS-01: option present — mountpoint
  ✅ CONS-01: option present — no_subtree_check
  ✅ CONS-01: option present — sec=sys
  ✅ CONS-01: forbidden option absent — no_root_squash
  ✅ CONS-01: forbidden option absent — crossmnt
  ✅ CONS-01: forbidden option absent — rw
  ✅ CONS-01: forbidden option absent — insecure
  ✅ CONS-01: forbidden option absent — no_all_squash
  ✅ CONS-01: forbidden option absent — fsid=
  ✅ CONS-01: nfs-server is active on atlantis
  ✅ CONS-01: nfs-server is not active on LXC 100 (state: inactive)

🎛  2. Music Assistant reachability and provider identity (CONS-02, D-30)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  music providers configured in MA:
      builtin--bbsqELSo	builtin	true	(null)
      spotify--g2SQC45P	spotify	true	Spotify Damian
      radiobrowser	radiobrowser	true	(null)
      ambient_sounds	ambient_sounds	true	Ambient Sounds
      filesystem_local--XJaJWNUS	filesystem_local	true	(null)
  ✅ CONS-02: provider filesystem_local--XJaJWNUS is enabled
  ✅ CONS-02: provider type is 'music'
  ✅ CONS-02: provider status is 'loaded'
  ✅ CONS-02: provider last_error is empty
  note: a Spotify music provider is ENABLED (spotify--g2SQC45P).
      Every album assertion below is filtered on the LOCAL provider instance id for
      exactly this reason. An unfiltered query can match Spotify and report a pass
      about an NFS export it never touched (D-30, T-02-16).

💿 3. The three proof albums in Music Assistant (CONS-02, D-06/D-07/D-09/D-10)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [single-artist] Lifelines — Chris Norman
      path:       /mnt/tank/media/Music/Chris Norman/Lifelines (2026)
      tracks:     15   scope: library
      audio_md5:  39da9daf74c3a4e2dcc0f35a7612d122   (QUAL-01 join key, one representative track)
  ✅ source directory exists on disk
  ✅ CONS-02: exact match in MA, provider-attributed to filesystem_local--XJaJWNUS

  [multi-disc] The Ultimate Hits — Garth Brooks
      path:       /mnt/tank/media/Music/Garth Brooks/The Ultimate Hits (2007)
      tracks:     36   scope: library
      audio_md5:  48556eceed2f2cc16709269ee93aa18f   (QUAL-01 join key, one representative track)
  ✅ source directory exists on disk
  ✅ CONS-02: exact match in MA, provider-attributed to filesystem_local--XJaJWNUS

  [various-artists] Mastermix Essential Hits - Pop 4 - 2005-2009 — Various Artists
      path:       /mnt/tank/downloads/complete/nzb/unsorted/VA-Mastermix.Essential.Hits.Pop.4.2005-2009-2025
      tracks:     50   scope: temp-export
      audio_md5:  097b998f01a67d2f738d3cf415c9d7f2   (QUAL-01 join key, one representative track)
  ✅ source directory exists on disk
  ⚠️  [various-artists] scope=temp-export and MA_TEMP_PROVIDER_INSTANCE is unset — REPORTED, not
         asserted. This album is served by the SECOND, TEMPORARY export, which is
         torn down by 'terraform apply' with music_temp_export_enabled=false (D-15).
         It was asserted for real in plan 02-07 while that control was standing:
         exact match on album AND albumartist, filtered to the temporary provider.
         Set MA_TEMP_PROVIDER_INSTANCE to re-assert it when the control is next up.

🎬 4. The three proof albums in Jellyfin (CONS-04 groundwork, D-42)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Asserting the SAME pinned albums against the second consumer is what gives Phase 7's
  CONS-04 one script that answers the whole question, and gives this phase's blast-radius
  check a real assertion rather than 'Jellyfin looks fine'.

  ✅ [single-artist] CONS-04: 'Lifelines' — 'Chris Norman' present in Jellyfin's Music library (exact match)
  ✅ [multi-disc] CONS-04: 'The Ultimate Hits' — 'Garth Brooks' present in Jellyfin's Music library (exact match)
  ⚠️  [various-artists] 'Mastermix Essential Hits - Pop 4 - 2005-2009' is scope=temp-export — out of Jellyfin's reach by design (it mounts
      /mnt/tank/media only, never /mnt/tank/downloads). REPORTED, not asserted.

🎛  4a. The D-34 library options on Jellyfin's Music library (CONF-04, plan 06-03)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ CONF-04: PreferNonstandardArtistsTag is true — the D-34 change itself — a false here means it silently reverted
  ✅ CONF-04: UseCustomTagDelimiters is false — D-34 REJECTED this one; a true here widens splitting to slash, pipe and backslash across 1,244 files
  ✅ CONF-04: SaveLocalMetadata is false — the Phase 1 freeze, re-asserted because this is now the one place in the repo that reads this endpoint
  ✅ CONF-04: EnableRealtimeMonitor is false — the Phase 1 freeze, same reason
  CustomTagDelimiters: ["/","|",";","\"]
  DelimiterWhitelist:  []

🧑‍🎤 4b. D-22 artist entities in Jellyfin (CONF-04, plan 06-03)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ArtistItems is the BROWSEABLE ENTITY list — each entry has an Id a user can click.
  Artists is the flat string list and is NOT what is asserted: 30 of the 1,244 library items
  carry a populated Artists list with ZERO linked entities, so asserting on Artists would
  report green on a row nobody can navigate to.

  ⚠️  CONF-04 (D-22): [ARTISTS] 0 entities, target 4 — PENDING A RE-PROBE, not green.
      '/media/Music/Lady Gaga/ARTPOP (2013)/CD 01-05 Lady Gaga - Jewels n’ Drugs.flac'
      This is the 2026-09-20 baseline, unchanged. PreferNonstandardArtistsTag is a
      PROBE-TIME option and a Default-mode refresh does not re-run the prober on a file
      whose mtime has not changed (measured twice, plan 06-03). It discharges when
      Phase 7 writes or imports the file — NOT by rescanning, and NOT by the aggressive
      refresh mode, which rewrites .nfo into the library and is forbidden here.
      why this row: widest N; exercises a dollar sign and dotted initials in artist names
  ⚠️  CONF-04 (D-22): [ARTISTS] 1 entities, target 2 — PENDING A RE-PROBE, not green.
      '/media/Music/Katy Perry/Teenage Dream (2010)/CD 01-03 Katy Perry - California Gurls.flac'
      This is the 2026-09-20 baseline, unchanged. PreferNonstandardArtistsTag is a
      PROBE-TIME option and a Default-mode refresh does not re-run the prober on a file
      whose mtime has not changed (measured twice, plan 06-03). It discharges when
      Phase 7 writes or imports the file — NOT by rescanning, and NOT by the aggressive
      refresh mode, which rewrites .nfo into the library and is forbidden here.
      why this row: different album and artist folder — isolates a per-album re-probe from a real fix
  ⚠️  CONF-04 (D-22): [ARTISTS] 1 entities, target 2 — PENDING A RE-PROBE, not green.
      '/media/Music/P!nk/The Truth About Love (2012)/CD 01-04 P!nk - Just Give Me a Reason.flac'
      This is the 2026-09-20 baseline, unchanged. PreferNonstandardArtistsTag is a
      PROBE-TIME option and a Default-mode refresh does not re-run the prober on a file
      whose mtime has not changed (measured twice, plan 06-03). It discharges when
      Phase 7 writes or imports the file — NOT by rescanning, and NOT by the aggressive
      refresh mode, which rewrites .nfo into the library and is forbidden here.
      why this row: albumartist is SECOND in the tag — the order is the tell in Jellyfin

🎧 4c. D-22 artist entities in Music Assistant (CONF-04, plan 06-13)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ⚠️  CONF-04 (D-22): MA reports 3 artists for 'Jewels n' Drugs', the tag holds 4 —
      REPORTED at its 2026-09-20 baseline, NOT green. names: Twista | Lady Gaga | Too $hort
      This is a MEASURED DISCREPANCY, not a 'not yet': MA read this file fresh through
      the export and still produced fewer artists than the ARTISTS tag carries.
      CHARACTERISED 2026-09-21 (plan 06-13). Do NOT blame the delimiter — both easy
      explanations are ruled out by measurement:
        - the ';' splitter WORKS on this build (rows 2 and 3 each split a two-name
          ARTISTS tag whose ARTIST tag holds only one of the names);
        - mb_id_count==1 CANNOT fire here — this file carries FOUR musicbrainz
          artist ids, and the short-circuit would have returned 1 artist, not 3.
      The loss is at MA's artist-ENTITY stage: 'Twista' exists nowhere in MA's
      library artists. Cap-at-3 vs Twista-specifically is undecidable on one sample;
      Phase 7's first >=4-artist track is the measurement that settles it.
      why this row: widest N; exercises a dollar sign and dotted initials in artist names
  ✅ CONF-04 (D-22): [ARTISTS] MA reports 2 distinct artists for 'California Gurls' — Katy Perry | Snoop Dogg
  ✅ CONF-04 (D-22): [ARTISTS] MA reports 2 distinct artists for 'Just Give Me a Reason' — P!nk | Nate Ruess

🔌 5. Mount liveness (CONS-03)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  The documented stale state is precisely 'entries exist, playback of any of them fails'.
  An MA entry can be a survivor from before the mount broke, so presence alone proves
  nothing — provenance and count both have to be scoped to the local provider.

  albums attributed to filesystem_local--XJaJWNUS: 70
  ✅ CONS-03: at least 3 albums are attributed to the local provider
  ⚠️  CONS-03: 'ha mounts info' enrichment skipped (HA_ROUTE=skipped). The MA-side assertion
      above is the load-bearing one and it ran. See the header for why the key is not here.

📊 6. Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MA version (live):           2.11.0b2   (assertions proven at 2.11.0b2)
  export route:                ssh:172.16.1.158
  ma route:                    http://172.16.1.31:8095
  jellyfin route:              docker-inspect:192.168.90.17:8096
  jellyfin vfolder route:      docker-inspect:192.168.90.17:8096+/Library/VirtualFolders   (its own endpoint, its own route)
  ha route:                    skipped   (skipped is expected on LXC 100 — see header)
  pinned provider instance:    filesystem_local--XJaJWNUS
  temp provider instance:      <unset — scope=temp-export rows reported, not asserted (D-15)>
  proof albums pinned:         3   (target 3: single-artist, multi-disc, various-artists)
  albums matched in MA:        2   (target 2, exact name+albumartist, provider-attributed)
  MA out-of-scope:             1   (scope=temp-export with no temp provider pinned — reported, not asserted)
  albums matched in Jellyfin:  2   (target 2, exact name+albumartist)
  jellyfin out-of-scope:       1   (scope!=library — reported, not asserted)
  D-34 options read:           PreferNonstandardArtistsTag=true  UseCustomTagDelimiters=false  SaveLocalMetadata=false  EnableRealtimeMonitor=false
  artist rows pinned:          3   (D-22, all in the ARTISTS tag position)
  artist rows at target (JF):  0
  artist rows PENDING (JF):    3   (at the 2026-09-20 baseline — NOT green; discharges on Phase 7's first write)
  artist rows at target (MA):  2
  artist rows REPORTED (MA):   1   (measured discrepancy against the tag — NOT green; plan 06-13 owns it)
  MA albums, local provider:   70   (target >= 3)
  toolchain missing:           0
  export assertions failed:    0
  MA assertions failed:        0
  Jellyfin assertions failed:  0
  FAILURES total:              0

⚠️  CONF-04 IS NOT CLOSED: 3 Jellyfin and 1 MA artist
   row(s) are at a recorded baseline, not at target. Reported, not asserted — see 4b/4c
   and stacks/selfhosted/arrs/beets.md § 'Phase 6 — tagger configuration and dry run'.
   Exiting 3: measured, but not at target. NOT a failure (FAILURES is 0) and NOT
   green. Discharges on ROADMAP entry criterion E6, not here.
--- CONSUMERS TRANSCRIPT END ---

================================================================================
END OF 06-42 INSTRUMENT RE-RUN
================================================================================
