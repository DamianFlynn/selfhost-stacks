---
phase: 06-tagger-configuration-and-dry-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stacks/selfhosted/arrs/beets/config.yaml
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-05]
must_haves:
  truths:
    - "The vendored beets config is valid under BOTH rc6's eyconf/JSON schema and beets 2.13.1 (D-03), so one file serves two containers"
    - "`plugins:` is a YAML list, not the canonical space-separated string, so rc6 cannot reject it and kill the watchdog behind a live-looking page (D-10)"
    - "Every key whose absence would hand rc6's schema default control is set explicitly — `directory`, `statefile`, `import.duplicate_action`, `match.medium_rec_thresh` — because rc6's `duplicate_action` default is `remove`, a silent delete path"
    - "Imports copy, never move (CONF-01), and `incremental` is paired with `incremental_skip_later` (CONF-02)"
    - "Path formats put `$albumartist` at the top level exactly, `comp:` is OVERRIDDEN not deleted (D-15), DJ content routes to a `DJ/` sibling on an EXACT query (D-13), multi-disc is flat and disc-prefixed (D-14), singletons have their own first-position rule (D-19b), `%aunique{}` is kept (D-16)"
    - "Match disambiguation is configured as written: `GB` not `UK`, `original_year`, `musicbrainz.extra_tags` (CONF-05)"
    - "The D-18 protected DJ-field register is named in the file, per container format, so the dry-run reporter has an inspectable list rather than an inference"
    - "The file carries NO credential of any kind — the repo is public"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets/config.yaml"
      provides: "the single vendored beets configuration for both containers"
      contains: "per_disc_numbering"
  key_links:
    - from: "stacks/selfhosted/arrs/beets/config.yaml"
      to: "beets Item.destination()"
      via: "the paths: stanza evaluated by beet move -p"
      pattern: "albumtype:=dj"
---

<objective>
Write the whole of Phase 6's tagger configuration into the one vendored file, `stacks/selfhosted/arrs/beets/config.yaml`. Its own header currently reads "EVERYTHING ELSE ABOUT THIS TAGGER'S CONFIGURATION BELONGS TO PHASE 6" — this plan is that phase, and the header must be rewritten rather than silently outgrown.

Purpose: CONF-01, CONF-02, CONF-03 and CONF-05 are all properties of this file. It is also the file that must be correct **before** beets-flask ever starts (plan 06-03), because rc6 writes its own destructive example config into `$BEETSDIR/config.yaml` when that file is absent — a config that arms `embedart.auto`, `lastgenre.auto force`, `scrub` and `asciify_paths`, i.e. exactly the three SAFE-01 switches Phase 1 turned off.

Output: one rewritten vendored config, offline-validated, credential-re-screened, committed. Nothing is deployed to the host by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-PATTERNS.md
@stacks/selfhosted/arrs/beets/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the vendored config with the full Phase 6 stanza</name>
  <files>stacks/selfhosted/arrs/beets/config.yaml</files>
  <read_first>
    - stacks/selfhosted/arrs/beets/config.yaml — ALL 97 lines. Its 47-line header is the house style every addition must match: a "Consumed by" block, the vendored/authoritative split, a credential screen, and "every key carries WHY". Lines 36-39 are the scope statement this task rewrites.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "P2 — Path formats: the actual stanza" (lines 259-392) — the recommended stanza with its first-match-wins citation, the `per_disc_numbering` source quote, and the `02-05` rendering note.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "D-30's two arms measure two different objects" (lines 528-562) — the rc6 schema-default table naming `import.duplicate_action → remove`, `match.medium_rec_thresh → 0.10`, `directory → /music/imported`.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "P1 — The DJ protected-field map" (lines 197-256) — the per-format field table for the D-18 register, including that `genre` is NOT a beets 2.x field (it is `genres`) and `EnergyLevel` is neither a beets nor a mediafile field.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-PATTERNS.md § "stacks/selfhosted/arrs/beets/config.yaml (vendored config, batch)" (lines 630-685) — the four header moves and the "an absent key is the on switch" argument.
  </read_first>
  <action>
Rewrite `stacks/selfhosted/arrs/beets/config.yaml` in place, preserving the existing header's four devices (Consumed-by block, vendored/authoritative split, per-key WHY comments, credential screen) and the three existing SAFE-01 `auto: no` blocks verbatim.

**Header changes.**
- The "Consumed by" block now names TWO containers: `beets-flask` (`metasauce/beets-flask:v2.0.0-rc6`, beets 2.12.0, `./flask.yaml`) as the ACTIVE runtime, and `beets` (`lscr.io/linuxserver/beets:2.13.1-ls349`, `./beets.yaml`) as the DORMANT agent-driven CLI arm. Per D-03 both mount this one file; per D-04 only flask's 2.12.0 ever opens the real `library.db`.
- Replace the lines 36-39 scope statement ("EVERYTHING ELSE … BELONGS TO PHASE 6"). Do not delete it silently — restate it as a dated retraction in this repo's house style (see `beets.md:70-77`, "Stated as retractions, not quiet replacements"): keep the superseded sentence visible, then state that Phase 6 is that phase and this commit is where the rest landed.
- Add a dated note that this file must exist at `$BEETSDIR/config.yaml` BEFORE beets-flask first starts, because rc6's `write_examples_as_user_defaults()` copies its own example in when the file is absent, and that example enables `embedart.auto: yes`, `lastgenre.auto: yes` with `force: yes`, `scrub` and `asciify_paths: yes`.
- Re-run the credential screen paragraph and re-date it. This repository is PUBLIC.

**D-10 — `plugins:` becomes a YAML list.** Replace line 60's `plugins: musicbrainz` with the flow-sequence form `plugins: [musicbrainz]`. Keep the existing TAGR-05 comment block above it and add one sentence: rc6 types this key `list[str]` and REJECTS the string form with `…is not of type 'array', 'null'`, and a rejected config kills the inbox watchdog while the server keeps serving a page (Phase 3 friction 9). `musicbrainz` stays named explicitly because since beets 2.4.0 it is a plugin and omitting it silently disables all autotagging.

**Explicit keys that rc6 would otherwise default.** Each needs its own WHY comment naming rc6's default as the thing being refused:
- `directory: /media/Music` — the real library root as the flask container sees it (`/mnt/tank/media:/media:ro`). rc6's schema default is `/music/imported`, a path that does not exist in the container. The `:ro` mount, not this key, is what prevents writes (D-05).
- `statefile: /config/state.pickle` — named explicitly because `beet -l` does NOT redirect it; the state lives in a pickle, not in `library.db`, and D-29 layer 3 asserts this exact file's sha256.
- `library: /config/library.db` — keep the existing line and its comment.
- Under `import:` — `duplicate_action: ask`. rc6's schema default is `remove`, which deletes duplicates with no prompt. State that plainly.
- Under `match:` — `medium_rec_thresh: 0.25` (beets' own default; rc6 injects `0.10`).

**CONF-01 — `import.copy: yes`, `import.move: no`.** With the WHY: there is no `beet undo`, and a bulk run over 97 GB with `move` set consumes its own source. Note that rc6 additionally TYPE-enforces these as `Literal[True]`/`Literal[False]`, so a `move: yes` config is rejected at validation rather than overridden — which is the friction-9 kill path, not a safety net.

**CONF-02 — `import.incremental: yes` AND `import.incremental_skip_later: yes`.** WHY: the first without the second is the sharpest trap in the set — `ImportTask.finalize()` calls `save_history()` on a SKIPPED task unless `incremental_skip_later` is set, so one quiet pass permanently marks every hard album complete and the backlog becomes invisible rather than imported. Cite `beets/importer/tasks.py@v2.12.0:310-322`. Note that `set_config` forces `resume: False` whenever `incremental` is truthy, so the two are mutually exclusive by construction.

**CONF-03 / D-13 / D-14 / D-15 / D-16 / D-19b — the `paths:` stanza.** Write the keys in exactly this order, because `Item.destination()` takes the FIRST non-`default` key whose query matches and confuse iterates user-config keys before default-only ones (`beets/library/models.py@v2.12.0:1182-1215`, `beets/util/pathformats.py@v2.12.0`):
1. `singleton:` → `Singles/$artist/$title%sunique{}` — D-19b. Written out and placed FIRST because an un-overridden `singleton:` inherits the lowest precedence of any non-default rule.
2. `'albumtype:=dj disctotal:2..'` → `DJ/$albumartist/$album%aunique{}/$disc-$track $title` — D-13 + D-14.
3. `'albumtype:=dj'` → `DJ/$albumartist/$album%aunique{}/$track $title` — D-13.
4. `'disctotal:2..'` → `$albumartist/$album%aunique{}/$disc-$track $title` — D-14, placed before `comp:` on purpose so a multi-disc compilation still gets the disc prefix.
5. `comp:` → `$albumartist/$album%aunique{}/$track $title` — D-15. Identical to `default` BY DESIGN: that is what "the comp rule resolves to `$albumartist`" means, written as an override rather than a deletion. Comment that defining `paths:` does NOT remove inherited default keys (the `get_path_formats` docstring says so verbatim), so `Compilations/` survives unless `comp:` is explicitly overridden.
6. `default:` → `$albumartist/$album%aunique{}/$track $title`.

Two query-syntax traps must be written into the comments: `albumtype:dj` with a bare colon is a SUBSTRING match that would also capture `djmix` and `adjacent` — hence `albumtype:=dj` (exact `MatchQuery`); and a path-format key must NEVER contain `/`, because `PathQuery.is_path_query()` rewrites any bare term containing a slash into a path query.

**Supporting keys that the D-14 and CONF-03 results depend on, each stated rather than inherited:**
- `per_disc_numbering: yes` — default is `no`. With `no`, `$track` is the ABSOLUTE index, so `$disc-$track` renders `02-17` for disc 2 track 5: plausible-looking and wrong. Cite `beets/autotag/hooks.py@v2.12.0:415-455`.
- `asciify_paths: no` — default is `no`, but rc6's example config sets `yes`, which would mangle `P!nk` and every non-ASCII album name in the tree CONF-03 is about.
- `va_name: "Various Artists"` — CONF-03's exact string, case included. `$albumartist` for a VA release resolves to this value verbatim, which is what makes `Various Artists/` appear and `Compilations/` not.
- `max_filename_length: 0`.
- An `aunique:` block with `keys: albumartist album`, the default `disambiguators: albumtype year label catalognum albumdisambig releasegroupdisambig`, and `bracket: '[]'` — stated because D-16 keeps `%aunique{}` and the dry run must report every firing. Comment the landmine: when NO disambiguator separates an ambiguous set, `%aunique{}` returns the numeric database id, which is not reproducible across libraries.

**The `02-05` rendering decision, recorded in the file.** `disc` and `track` are both `types.PaddedInt(2)`, so `$disc-$track` renders `02-05`, not the literal `2-05` D-14's text writes. Accept `02-05` and say so: it sorts correctly, and `%right{$disc,1}` breaks at disc 10. Note that the existing 1,244-file library uses a THIRD convention (`CD 02-01 Artist - Title.ext`), that Phase 6 does not reconcile the two, and that `06-EXPECTED-TREE.txt` is written to `02-05`.

**D-19a, stated honestly.** Add a comment block: beets' query language has NO field-to-field comparison, so there is no path-template expression for "album equals albumartist". The guard is therefore a BLOCKING class assertion in `scripts/phase06-oracle.sh` (plan 06-08) — zero produced album directories whose basename equals their parent artist directory's basename, case-folded — and the existing `Def Leppard/Def Leppard (2015)/` defect is scheduled for repair in Phase 7. Name both halves so nobody reads the missing template trick as an oversight.

**CONF-05 — match disambiguation.** Under `match:` add a `preferred:` block with `countries: ['GB', 'US']` and `original_year: yes`. WHY: MusicBrainz stores `GB`, not `UK`; `UK` alone silently matches nothing, and entries are REGEXES, not literals. Add a `musicbrainz:` block with `extra_tags: [year, catalognum, country, media, label]`.

**CONF-04 write side, amended by D-34.** Add a comment block (no key — there is none to add): beets CANNOT be configured to emit `;` in `ARTIST`. `item.artist` is `"".join()` over MusicBrainz's own `joinphrase` values (`beetsplug/musicbrainz.py@v2.12.0:341-367`) and no delimiter/separator/join key exists in `config_default.yaml` at 2.12.0 or 2.13.1. What beets DOES write is the per-artist list into `TXXX:ARTISTS` (MP3) / `ARTISTS` (Vorbis). Music Assistant prefers that tag and splits it on `;`; Jellyfin reads it only with `PreferNonstandardArtistsTag` enabled, which D-34 enables in plan 06-12. Record that this supersedes D-23's "configure `;` on the write side".

**D-18 — the named protected DJ-field register.** Add a comment-only register (these are fields to PRESERVE, not keys to set) listing, per D-18 name: `bpm` → beets `bpm`, ID3 `TBPM`, Vorbis `BPM`; `initialkey` → beets `initial_key`, ID3 `TKEY`, Vorbis `INITIALKEY`; `EnergyLevel` → NO beets field and NO mediafile field, ID3 `TXXX:EnergyLevel` only; `genre` → beets `genres` (NOT `genre`, which does not exist in beets 2.x), ID3 `TCON`, Vorbis `GENRE`; `comment` → beets `comments`, ID3 `COMM`, Vorbis `DESCRIPTION` (written before `COMMENT`). State the mechanism and its limit: `bpm`/`initial_key`/`genres`/`comments` are real beets fields, read on import and written back by `Item.write()`; MusicBrainz supplies none of them, so with `scrub.auto: no` they are preserved — a MECHANISM, not a guarantee, which is why D-18 wants a named list and a report. State that `EnergyLevel` must be asserted by a RAW ID3 frame-set comparison, never by a beets query, because a beets query for it returns empty and reads as clean. D-19 applies: Phase 6 preserves these fields and generates none of them.

**`import.write`** — set explicitly to `yes` with a comment that this is the Phase 7 behaviour, that Phase 6's every invocation overrides it to `no` through a `-c` overlay, and that the D-18 register above is what a `write: yes` import is required not to damage.

Do NOT add a Discogs token, a `discogs` plugin entry, or any `env_file`. Phase 4 D-24 dismissed that credential.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('stacks/selfhosted/arrs/beets/config.yaml')); assert d['plugins']==['musicbrainz'], d['plugins']; assert d['import']['copy'] is True and d['import']['move'] is False; assert d['import']['incremental'] is True and d['import']['incremental_skip_later'] is True; assert d['import']['duplicate_action']=='ask'; assert d['match']['medium_rec_thresh']==0.25; assert 'GB' in d['match']['preferred']['countries'] and 'UK' not in d['match']['preferred']['countries']; assert d['match']['preferred']['original_year'] is True; assert d['musicbrainz']['extra_tags']; assert d['per_disc_numbering'] is True; assert d['asciify_paths'] is False; assert d['va_name']=='Various Artists'; assert d['directory']=='/media/Music'; assert d['statefile']=='/config/state.pickle'; assert d['library']=='/config/library.db'; ks=list(d['paths'].keys()); assert ks[0]=='singleton', ks; assert 'albumtype:=dj disctotal:2..'==ks[1], ks; assert 'albumtype:=dj'==ks[2], ks; assert 'disctotal:2..'==ks[3], ks; assert ks[4]=='comp' and ks[5]=='default', ks; assert d['paths']['comp']==d['paths']['default']; assert d['paths']['default'].startswith('\$albumartist/'); assert d['scrub']['auto'] is False and d['lastgenre']['auto'] is False and d['embedart']['auto'] is False; print('config OK')"</automated>
  </verify>
  <done>
    `stacks/selfhosted/arrs/beets/config.yaml` parses as YAML and every assertion in the verify command passes. `plugins` is a list. All four rc6-landmine keys are set explicitly. The `paths:` keys are in the stated order with `albumtype:=dj` using the EXACT-match `=` prefix. The D-18 register, the D-19a honesty note, the C-2/D-34 write-side note and the `02-05` rendering decision are all present as comments. The file contains no credential.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the config offline — query parsing, rc6-schema shape, and the credential screen</name>
  <files>stacks/selfhosted/arrs/beets/config.yaml</files>
  <read_first>
    - stacks/selfhosted/arrs/beets/config.yaml — the file as task 1 left it.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "Query syntax available in a path key" (lines 292-307) — the `shlex.split()` behaviour and the YAML-parse measurement this task re-runs.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "rc6's own config validation — friction 9, at source" (lines 571-581) — that extra non-schema keys ARE allowed at every nesting level, which is what makes `paths:`, `per_disc_numbering` and `match.preferred` safe under rc6.
  </read_first>
  <action>
Run three offline proofs on the workstation and record their output verbatim in the plan summary. No container and no host access is required; nothing is deployed.

**Proof 1 — the path keys parse as the beets queries intended.** For each of the four query-form keys (`albumtype:=dj disctotal:2..`, `albumtype:=dj`, `disctotal:2..`, and the two aliases `comp`/`singleton`), run `shlex.split()` on the key and assert the term count: 2 for the compound key, 1 for each simple key. Assert that `albumtype:=dj` splits to a single term whose prefix is `=` (exact `MatchQuery`), NOT a bare-colon substring query. Assert that NO path-format key contains a `/` character anywhere.

**Proof 2 — the `%aunique{}` / `%sunique{}` templates are the only template functions used, and every one exists.** Extract every `%name{` token from the `paths:` values and assert the set is a subset of the beets 2.12.0/2.13.1 template-function set: `lower upper capitalize title left right if asciify time aunique sunique first ifdef`. This catches a typo'd `%unique{}` that YAML would happily accept and beets would render as a literal.

**Proof 3 — the credential screen, re-run and dated.** Screen the whole file for: any key whose name matches `token|secret|password|passwd|api_?key|user_token|auth`; any URL carrying embedded credentials (`://[^/]*:[^/]*@`); any e-mail address; and any high-entropy run of 20+ base64/hex characters. Each check must be run and its count recorded — including the zeroes. Run a POSITIVE CONTROL first: append a known secret-shaped string to a scratch COPY of the file (never the real file), confirm the screen matches it, delete the copy. A screen that has never matched anything is not a screen.

Update the file's credential-screen paragraph with today's date and the confirmation that the positive control fired.

Do NOT attempt to validate against rc6's eyconf schema here — no container exists yet. Plan 06-06 asserts the SERVER-COMMITTED config, which is the only place that validation is observable.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re, shlex, yaml
src = open('stacks/selfhosted/arrs/beets/config.yaml').read()
d = yaml.safe_load(src)
keys = list(d['paths'].keys())
assert shlex.split('albumtype:=dj disctotal:2..') == ['albumtype:=dj','disctotal:2..']
for k in keys:
    assert '/' not in k, f'path-format key contains a slash: {k}'
assert 'albumtype:=dj' in keys
FUNCS = {'lower','upper','capitalize','title','left','right','if','asciify','time','aunique','sunique','first','ifdef'}
used = set(re.findall(r'%([a-z]+)\{', ' '.join(d['paths'].values())))
assert used <= FUNCS, f'unknown template functions: {used - FUNCS}'
assert 'aunique' in used and 'sunique' in used
bad = re.findall(r'(?i)^\s*[a-z_]*(token|secret|password|passwd|api_?key|user_token)\s*:', src, re.M)
assert not bad, bad
assert not re.search(r'://[^/\s]*:[^/\s]*@', src)
assert not re.search(r'[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}', src)
print('offline proofs OK; template funcs used:', sorted(used))
PY</automated>
  </verify>
  <done>
    All three proofs run and their output — counts included, zeroes included — is quoted in the plan summary. The positive control for proof 3 fired against a scratch copy and that copy no longer exists (`git status --porcelain` shows only `config.yaml`). The credential-screen paragraph in the file carries today's date.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → public internet | This repository is PUBLIC and already carries one credential exposure recoverable via `git log -S`. Anything committed here is published. |
| vendored config → beets/rc6 runtime | A config key set here changes what two containers do to a 34 GB library and a 97 GB backlog, with no `beet undo`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `stacks/selfhosted/arrs/beets/config.yaml` in a public repo | mitigate | Task 2's credential screen, run with a positive control against a scratch copy and re-dated in the file. No Discogs token, no `env_file`, no placeholder. |
| T-06-02 | Tampering | `import.duplicate_action` left unset → rc6 injects `remove` | mitigate | Set `duplicate_action: ask` explicitly with the rc6 default named in its WHY comment. Asserted by task 1's verify. |
| T-06-03 | Tampering | `$BEETSDIR/config.yaml` absent at first container start → rc6 installs its own example arming `embedart.auto`, `lastgenre.auto force`, `scrub`, `asciify_paths` | mitigate | Header note here; the pre-start assertion itself is plan 06-03 task 3. |
| T-06-04 | Denial of Service | `plugins:` in string form rejected by rc6's schema → watchdog dies while the page still serves | mitigate | D-10's YAML-list form, asserted by task 1's verify; the firing half is D-09's two-part liveness proof in plans 06-03 and 06-05. |
| T-06-05 | Tampering | A path-format key containing `/` silently becomes a `PathQuery` | mitigate | Task 2 proof 1 asserts no key contains a slash. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. The only install-shaped action in the phase is rc6's `uv pip install -r /config/requirements.txt`, audited in 06-RESEARCH.md § Package Legitimacy Audit (beets, mutagen → 2 OK, 0 SUS, 0 SLOP) and pinned in plan 06-03. |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('stacks/selfhosted/arrs/beets/config.yaml'))"` exits 0.
- Both task verify commands exit 0.
- `git diff --stat` shows exactly one file changed.
- `grep -c 'plugins: \[musicbrainz\]' stacks/selfhosted/arrs/beets/config.yaml` returns 1.
- The comment-stripped grep for a bare-colon DJ query returns 0: `grep -v '^\s*#' stacks/selfhosted/arrs/beets/config.yaml | grep -c "albumtype:dj'" ` → 0 (the exact-match form `albumtype:=dj` does not satisfy it).
</verification>

<success_criteria>
- CONF-01's `copy`/`move` pair, CONF-02's `incremental` pair, CONF-03's `paths:` stanza and CONF-05's `preferred`/`extra_tags` keys all exist in the vendored file with a stated WHY.
- All four rc6 schema-default landmines (`directory`, `statefile`, `import.duplicate_action`, `match.medium_rec_thresh`) are set explicitly.
- The file is credential-free and the screen was proven capable of matching.
- Nothing was deployed to `/mnt/fast/appdata` by this plan. The appdata copy is still the pre-Phase-6 file; plan 06-03 deploys it.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-01-SUMMARY.md` when done.
</output>

