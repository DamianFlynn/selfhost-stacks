---
phase: 06-tagger-configuration-and-dry-run
plan: 45
type: execute
wave: 23
depends_on: ["06-44"]
gap_closure: true
requirements: [CONF-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
  - .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
  - stacks/selfhosted/arrs/beets.md
autonomous: true

must_haves:
  truths:
    - "Every record of CONF-04's state in the project — REQUIREMENTS.md, ROADMAP.md's Phase 6 criterion and status row, ROADMAP.md's Phase 7 E6, 06-VERIFICATION.md and beets.md — states the same thing, and that thing is what was measured"
    - "Phase 7 entry criterion E6's SECOND measurement, the ≥4-artist Music Assistant discrimination, is explicitly retained with Phase 7 on every branch"
    - "The round's residue is carried by name as DEF-06-45-* rather than absorbed"
    - "No verification verdict is self-declared: re-scoring 06-VERIFICATION.md is left to /gsd-verify 06 and recommended in band"
    - "REQUIREMENTS.md's CONF-04 box cannot be ticked unless the deployed instrument independently corroborated the close — the tick gate reads 06-42's instrument counters, not just 06-43's branch line"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-04's checkbox and its traceability row, matching the measured state"
      contains: "CONF-04"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 criterion 4, the Phase 6 status row, and Phase 7 entry criterion E6"
      contains: "E6"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md"
      provides: "the CONF-04 gap's disposition and the round-5 record"
      contains: "gaps:"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md"
      provides: "DEF-06-45-* carry-forward entries for the round's residue"
      contains: "DEF-06-45-"
  key_links:
    - from: ".planning/REQUIREMENTS.md CONF-04"
      to: "scripts/check-music-consumers.sh sections 4b and 4c"
      via: "the same two never-summed verdicts, stated identically in both"
      pattern: "never summed"
---

<objective>
Make every record of CONF-04 say the same thing, and make that thing the measurement.

Six documents currently carry a CONF-04 state: `REQUIREMENTS.md`'s checkbox and traceability row,
`ROADMAP.md`'s Phase 6 criterion 4, `ROADMAP.md`'s Phase 6 status row, `ROADMAP.md`'s Phase 7 entry
criterion E6, `06-VERIFICATION.md`, and `beets.md`'s Phase 6 section. Round 5 changed the fact they
all describe. This plan brings them into agreement with each other and with the instrument.

Purpose: one truth value for CONF-04, in every place a reader could look.
Output: five edited documents, no code.

⚠ THIS PLAN DOES NOT DECLARE THE PHASE VERIFIED. Rounds 1 through 4 each ended with "this is a
record, not a re-close, and no re-verification was performed," and round 5 holds to that. The
CONF-04 checkbox moves because the operator explicitly decided to close it here on a measurement —
not because a plan says so. Re-scoring `06-VERIFICATION.md` from 5/6 is `/gsd-verify 06`'s call and
this plan recommends it in band rather than performing it.

⚠ BRANCH-AWARE. Read `06-43`'s `BRANCH:` line and `SECTION P` (the operator's recorded answer)
FIRST. Every edit below has a branch-A form and a branch-B/PARTIAL form. Write the one that
matches; do not write the optimistic one and soften it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt
@.planning/phases/06-tagger-configuration-and-dry-run/06-44-SUMMARY.md
@scripts/check-music-consumers.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: `REQUIREMENTS.md`'s CONF-04, and ROADMAP's Phase 6 criterion 4 and status row</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` around line 152 — the `- [ ] **CONF-04**` checkbox and its
      parenthetical, plus every other occurrence of `CONF-04` in the file (there is at least one
      traceability row the checkbox's note points at; find them all by grep rather than editing
      only the one this plan names).
    - `.planning/ROADMAP.md` § `### Phase 6: Tagger Configuration and Dry Run` success criterion 4,
      including its 2026-09-20 D-34 amendment, and the `| 6. Tagger Configuration and Dry Run |`
      row in the `## Progress` table.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt`
      Sections L, M, N and P — the numbers, the branch, its stated consequences and the operator's
      answer.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt`
      — `INSTRUMENT RUN:`, `JF_AT_TARGET:` and `JF_PENDING:`. **This plan does not take `BRANCH: A`
      on trust.** It re-reads the corroboration itself, because this is the task that permanently
      records a requirement as met in a public repository, and a single upstream line should not be
      the only thing standing between a transcription slip and that record.
    - `scripts/check-music-consumers.sh` as edited by 06-44 — the wording used there is the
      canonical phrasing; these documents must not contradict it.
  </read_first>
  <action>
    ⛔ THE TICK GATE, EVALUATED FIRST, BEFORE ANY EDIT. Read three lines and stop if they do not
    agree:

      1. `06-43`'s single `BRANCH:` line;
      2. `06-42-consumers-rerun.txt`'s `INSTRUMENT RUN:` value;
      3. that same file's `JF_AT_TARGET:` and `JF_PENDING:` values.

    **`REQUIREMENTS.md`'s CONF-04 box may be ticked only when all four hold: `BRANCH: A`,
    `INSTRUMENT RUN: MEASURED`, `JF_AT_TARGET: 3`, `JF_PENDING: 0`.** If `BRANCH: A` is written but
    the instrument did not corroborate it — a `COULD NOT LOOK`, or counters that disagree — STOP
    the plan, edit nothing, and report the contradiction. Do not tick and annotate; do not tick and
    defer; do not reconcile it in prose. The verify block enforces this independently of whatever
    the action text is read to mean.

    This gate is deliberately redundant with 06-43's branch recompute, which already refuses to
    write `BRANCH: A` without corroboration. Two gates, because the failure being guarded against
    is not the estate misbehaving — it is a mistake propagating through a chain of artifacts into a
    permanent public record, and the last link in that chain is the one worth double-checking.

    ⚠ The gate reads the JELLYFIN counters only. `MA_AT_TARGET` and `MA_REPORTED` take no part in
    it: on branch A the MA half still reports one measured discrepancy, `MA_ARTIST_PENDING` stays
    `1`, and the standing check still exits `3`. That is correct and is not a reason to withhold
    the tick. The two verdicts are never summed.

    Cite line numbers by anchor, not by number: this phase has already been bitten by stale
    `file:line` citations (`GC-04`), and this plan edits files that other documents point into.
    Find every site by grepping for `CONF-04`.

    **`REQUIREMENTS.md`.**
    On branch A: change `- [ ] **CONF-04**` to `- [x] **CONF-04**` and rewrite its parenthetical
    from "OPEN at Phase 6 close — MA half discharged, Jellyfin half awaiting Phase 7's first write"
    to the measured fact: both halves demonstrated, the Jellyfin half by a live re-probe driven in
    Phase 6 round 5 (mtime touch on three files inside a ZFS snapshot fence plus the targeted
    Default-mode refresh; plans 06-41 and 06-42), reaching N distinct browseable `ArtistItems`
    entities with distinct Ids and zero `;` in any entity name. **Keep the existing sentence that
    the two halves are never summed** — it is the single most load-bearing sentence in that
    parenthetical and it stays true after closure. Add that Music Assistant's 4-value row still
    returns three, which is REPORTED and is owned by Phase 7 E6's second measurement, so the
    standing check still exits 3 and that is correct.
    On branch B / PARTIAL: leave the checkbox unticked and extend the parenthetical with the
    measured negative — the mtime route was driven on the recorded date and what it did and did not
    move — plus which of the two options the operator chose in `06-43` SECTION P.

    Update every other `CONF-04` site in `REQUIREMENTS.md` to match. A checkbox that says one thing
    and a traceability row that says another is the exact shape `06-VERIFICATION.md` was written to
    catch.

    **`ROADMAP.md` Phase 6 success criterion 4.** Follow this project's established convention: the
    criterion TEXT is deliberately not rewritten; the correction lands as a dated in-band amendment
    beneath it, the way the D-33 and D-34 amendments already do. Add a `2026-09-23` amendment
    recording: the operator's decision to drive the re-probe inside Phase 6 rather than carry it to
    E6; the mechanism; the three per-row results with their targets and baselines; the branch; and
    — on every branch — that the criterion's own sentence "It discharges on Phase 7's first write"
    is superseded by that decision, naming it as superseded rather than deleting it.

    **`ROADMAP.md` Phase 6 status row** in the `## Progress` table. Append a `Round 5` sentence in
    the same voice as the four rounds before it, and — on branch A only — change the `Status` cell
    from `In Progress` to the wording the project uses for a phase whose requirements are all met
    but whose verification has not been re-run. Do NOT write `Complete` off this plan's own
    authority: rounds 1-4 each refused exactly that, and the phase's completion is the verifier's
    call. State in the cell that `/gsd-verify 06` has not been run since round 2 and is the
    recommended next step. Also correct the cell's existing sentence that
    `quick-health-check.sh` "exits non-zero on the consumers block until E6 discharges CONF-04" —
    on branch A the Jellyfin half discharged here and the surviving non-zero is the MA row.

    Do not touch Phase 6's plan list, wave headings, or any of the four gap-closure round records.
    They are history and they are accurate.
  </action>
  <acceptance_criteria>
    - On branch A **with corroboration** (`INSTRUMENT RUN: MEASURED`, `JF_AT_TARGET: 3`,
      `JF_PENDING: 0`): the unticked `- [ ] **CONF-04**` form returns `0` and the ticked
      `- [x] **CONF-04**` form returns `1`. On branch B, on PARTIAL, **or on a `BRANCH: A` the
      instrument did not corroborate**: the unticked form still returns `1`, the ticked form
      returns `0`, and the plan halted without editing anything.
    - The MA counters were not consulted by the gate: a `MA_REPORTED: 1` alongside a corroborated
      branch A does not withhold the tick.
    - `REQUIREMENTS.md` still contains the phrase `never summed` in CONF-04's parenthetical.
    - Every occurrence of `CONF-04` in `REQUIREMENTS.md` states the same truth value; a grep for
      `awaiting Phase 7` in that file returns `0` on branch A.
    - `ROADMAP.md`'s Phase 6 success criterion 4 retains its original criterion sentence and gains a
      dated amendment containing the literal strings `06-41`, `06-42` and `superseded`.
    - `ROADMAP.md`'s Phase 6 criterion-4 amendment names the three per-row results as numbers.
    - `ROADMAP.md`'s Phase 6 `## Progress` row contains `Round 5` and contains `/gsd-verify 06`.
    - `ROADMAP.md` contains no cell reading `Complete` for Phase 6 written by this plan.
    - The Phase 6 plan list, its wave headings and the four existing round records are byte-identical
      to before: `git diff -U0 -- .planning/ROADMAP.md` contains no changed line matching
      `06-[0-3][0-9]-PLAN.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; R=.planning/REQUIREMENTS.md; M=.planning/ROADMAP.md; V=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt; RR=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-consumers-rerun.txt; test -f "$V" || { echo "no verdict artifact"; exit 1; }; test -f "$RR" || { echo "no instrument re-run artifact — the tick gate cannot be evaluated"; exit 1; }; B=$(grep -oE "^BRANCH: (A|B|PARTIAL)$" "$V" | tail -1 | cut -d" " -f2); test -n "$B" || { echo "no branch recorded"; exit 1; }; I=$(grep -E "^INSTRUMENT RUN: " "$RR" | tail -1 | sed "s/^INSTRUMENT RUN: //" | cut -d" " -f1); JT=$(grep -oE "^JF_AT_TARGET: [0-9]+$" "$RR" | tail -1 | cut -d" " -f2); JP=$(grep -oE "^JF_PENDING: [0-9]+$" "$RR" | tail -1 | cut -d" " -f2); test -n "$I" -a -n "$JT" -a -n "$JP" || { echo "COULD NOT LOOK: corroboration lines unreadable (INSTRUMENT=$I JF=$JT/$JP)"; exit 1; }; CORROB=no; if [ "$I" = "MEASURED" ] && [ "$JT" = "3" ] && [ "$JP" = "0" ]; then CORROB=yes; fi; if [ "$B" = "A" ] && [ "$CORROB" = "no" ]; then echo "TICK GATE: BRANCH: A is written but the deployed instrument did not corroborate it (INSTRUMENT RUN=$I JF_AT_TARGET=$JT JF_PENDING=$JP). CONF-04 must NOT be ticked."; test "$(grep -c "^- \[x\] \*\*CONF-04\*\*" "$R")" -eq 0 || { echo "but it WAS ticked — halt and revert"; exit 1; }; exit 1; fi; if [ "$B" = "A" ] && [ "$CORROB" = "yes" ]; then test "$(grep -c "^- \[x\] \*\*CONF-04\*\*" "$R")" -eq 1 || { echo "CONF-04 not ticked on a corroborated branch A"; exit 1; }; test "$(grep -c "awaiting Phase 7" "$R")" -eq 0 || { echo "stale awaiting-Phase-7 wording survives"; exit 1; }; else test "$(grep -c "^- \[ \] \*\*CONF-04\*\*" "$R")" -eq 1 || { echo "CONF-04 must stay unticked on branch $B"; exit 1; }; fi; grep -qF "never summed" "$R" || exit 1; grep -qF "06-41" "$M" || exit 1; grep -qF "superseded" "$M" || exit 1; grep -qF "Round 5" "$M" || exit 1; grep -qF "/gsd-verify 06" "$M" || exit 1; test "$(git diff -U0 -- "$M" | grep -E "^[+-][^+-]" | grep -cE "06-[0-3][0-9]-PLAN.md" || true)" -eq 0 || { echo "an existing plan-list line was modified"; exit 1; }; echo OK'</automated>
  </verify>
  <done>CONF-04's checkbox, its traceability rows, Phase 6's criterion-4 amendment and Phase 6's status row all state the measured branch, the never-summed rule survives the closure, and no phase-completion verdict was self-declared.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP's Phase 7 entry criterion E6, and the round's residue as `DEF-06-45-*`</name>
  <files>.planning/ROADMAP.md, .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § `**Entry criteria inherited from Phase 6**`, criterion **E6** in
      full, and criteria **E5** and **E12** (E5 already owns the 30 items carrying a populated
      `Artists` list with zero `ArtistItems`, which is the most likely cause of a PARTIAL on row 1;
      E12 is the catch-all for gap-closure's undriven live-estate residue).
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — the established
      entry format: a `## DEF-06-NN-NN — <one-line title>` heading, then **Found during**, the
      finding, **Why it is deferred, not fixed here**, and a condition under which it should be
      revisited.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt`
      SECTION N — the stated consequences, including the sentence retaining E6's second
      measurement with Phase 7.
  </read_first>
  <action>
    **`ROADMAP.md` entry criterion E6.** E6 currently carries TWO measurements in one criterion.
    Split them explicitly rather than editing the criterion as a single unit, because exactly one
    of them moved:

      - **E6's first measurement** — "whether Jellyfin's re-probe produces 4/2/2 entities". On
        branch A: mark it DISCHARGED IN PHASE 6, dated, by plans 06-41/06-42, citing the artifacts,
        and state the mechanism (mtime touch inside a snapshot fence + the targeted Default-mode
        refresh) so Phase 7 does not re-derive it. On branch B / PARTIAL: mark it STILL OPEN and
        record that the mtime route was driven and did not discharge it, so Phase 7 does not spend
        its first hour re-trying a route already measured.
      - **E6's second measurement** — "whether a second ≥4-artist track yields four artists in
        Music Assistant or three — the only measurement that separates 'MA caps the list at 3' from
        '`Twista` specifically failed to map'". This is **NOT closed by round 5 on any branch** and
        REMAINS with Phase 7. Say so in words that cannot be skimmed past; it is the single easiest
        thing for a future reader to assume went with the first.

    Keep E6's existing explanatory text about why the rows read 0/4, 1/2, 1/2 — it is the historical
    record of why the criterion existed — and add the outcome beneath it rather than overwriting it.
    Do not renumber E1-E12 and do not touch E5 or E12's text.

    **`deferred-items.md`.** Append new entries in the file's established format, numbered
    `DEF-06-45-01` onward, continuing from the existing 36. At minimum:

      - the ZFS snapshot `tank/media/Music@pre-06-41-conf04-reprobe` is **still held and not
        released**. Its release is a separate operator decision, deliberately not bundled into the
        round's closure, and nothing schedules its destruction. Name the condition under which it
        should be released (the outcome accepted and Phase 7's pilot fence planned) and note that
        it costs negligible space on a pool with 9 T free.
      - on branch B / PARTIAL: the measured negative — the mtime lever was driven and what it did
        not move — written so the next attempt does not repeat it, and attached to E6.
      - on a PARTIAL where row 1 (ARTPOP) alone failed to move: the connection to **E5** — that
        album is part of the 30 items carrying a populated `Artists` string list with zero linked
        `ArtistItems`, so "the prober re-ran" and "browseable artist entities exist" may be two
        different facts, and a per-file refresh may not create the entities. Record it as a
        hypothesis with the evidence, not as a conclusion.
      - anything this round could not look at, named as a could-not-look rather than as a clean
        result — for example a STALE host checkout in 06-44 task 1, if that is what happened.

    Do not restate `DEF-06-21-08`, `DEF-06-29-07` or `DEF-06-39-06`; cross-reference them if the
    round hit the same classes.
  </action>
  <acceptance_criteria>
    - `ROADMAP.md`'s E6 text distinguishes its two measurements explicitly and contains the literal
      strings `first measurement` and `second measurement`.
    - E6 states that the second measurement remains with Phase 7, and contains the literal
      `Twista` (the name that makes the discrimination concrete).
    - On branch A, E6's first measurement is marked discharged and cites `06-41` and `06-42`. On
      branch B / PARTIAL, it is marked still open and records the driven negative.
    - Entry criteria E1 through E12 all still exist and are not renumbered: `grep -cE '^  E[0-9]+\.'`
      on `ROADMAP.md` returns the same count as before the edit, and that before-count is recorded
      in the plan summary rather than assumed.
    - `deferred-items.md` contains at least one heading matching `^## DEF-06-45-01 — `, and every
      new entry carries a **Found during** line and a revisit condition.
    - `deferred-items.md` contains a `DEF-06-45-*` entry naming
      `tank/media/Music@pre-06-41-conf04-reprobe` as still held and not released.
    - The unique `DEF-*` count in `deferred-items.md` is strictly greater than `36`.
    - No existing `DEF-06-*` entry was modified: `git diff -U0 -- .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md`
      contains zero lines beginning with `-` other than the file-header marker.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; M=.planning/ROADMAP.md; D=.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md; grep -qF "first measurement" "$M" || exit 1; grep -qF "second measurement" "$M" || exit 1; grep -qF "Twista" "$M" || exit 1; grep -qE "^## DEF-06-45-01 — " "$D" || { echo "no DEF-06-45-01 entry"; exit 1; }; grep -qF "tank/media/Music@pre-06-41-conf04-reprobe" "$D" || exit 1; N=$(grep -oE "DEF-[0-9]{2}-[0-9]{2}-[0-9]{2}" "$D" | sort -u | wc -l | tr -d " "); test "$N" -gt 36 || { echo "unique DEF count did not grow past 36 (got $N)"; exit 1; }; R=$(git diff -U0 -- "$D" | grep -cE "^-[^-]" || true); test "${R:-0}" -eq 0 || { echo "$R existing deferred-items lines were removed or rewritten"; exit 1; }; echo OK'</automated>
  </verify>
  <done>E6 now carries two separately-dispositioned measurements with the Music Assistant discrimination explicitly retained by Phase 7, the entry-criteria numbering is intact, and the round's residue — the held snapshot above all — is carried by name as `DEF-06-45-*` with nothing existing overwritten.</done>
</task>

<task type="auto">
  <name>Task 3: `06-VERIFICATION.md`'s gap record, and the durable account in `beets.md`</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md, stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` in full — its
      frontmatter (`status`, `score`, `re_verification`, `gaps`, `deferred`), truth #4's row in the
      Observable Truths table, the Requirements Coverage row for CONF-04, the Anti-Patterns row
      citing `REQUIREMENTS.md:152`, and the § Gaps Summary / § Recommended path text naming the two
      options the operator declined.
    - `stacks/selfhosted/arrs/beets.md` § `## Phase 6 — tagger configuration and dry run
      (2026-09-20)`, and specifically `### The finding that matters more than the change` (the
      probe-time finding this round acted on) and `### CONF-04 — the verdict, as TWO verdicts that
      must never be summed`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-43-conf04-verdict.txt`
      in full.
  </read_first>
  <action>
    **`06-VERIFICATION.md`.** Record the round-5 outcome against the gap that report raised, and do
    not manufacture a verdict:

      - In the frontmatter `gaps:` entry for CONF-04, add a dated `round_5:` sub-record naming the
        branch, the mechanism, the three per-row results, and the artifacts. On branch A, mark the
        gap's `status` as closed-by-measurement with its evidence; on branch B / PARTIAL, leave it
        open and record what was driven.
      - Leave the headline `status:` and `score:` fields as they are, and add an explicit in-band
        note — both in the frontmatter as a `superseded_note:` and in the body — stating that
        re-scoring this report is `/gsd-verify 06`'s call, that this plan performs no verification
        and claims no verification result, and that `/gsd-verify 06` is the recommended next step.
        This is the same discipline rounds 1-4 held to, and it matters more here, not less: this is
        the first round that moves a requirement checkbox.
      - Add a short body section, `## Round 5 — CONF-04's Jellyfin half, driven` , that states the
        operator declined both of the report's own recommended paths and chose a third — drive the
        re-probe now — and what that produced.
      - Update the § Recommended path paragraph so it no longer offers the two options as the only
        outcomes; leave the options themselves visible as the record of what was on the table.

    **`beets.md`.** This is the estate's durable account and the page a future operator reads at
    3 a.m. Add a dated subsection under the existing Phase 6 section, after `### The finding that
    matters more than the change`, titled so it reads as the sequel to that finding. Record:
      - the finding that is now closed or refined: that section says the evidence "arrives when a
        file is written or newly imported — i.e. Phase 7", and names `(b) the file's mtime
        changing` as a mechanism. Round 5 drove exactly (b), without writing content. Say whether
        it worked.
      - the mechanism in enough detail to be repeated: three files, `touch` from atlantis as real
        root, a ZFS snapshot taken in the same remote step, the same targeted Default-mode
        `POST /Library/Media/Updated` at file scope, a ≥120 s wait, and the `zfs diff` +
        `.nfo` hash-manifest controls proving nothing was written into the library;
      - the numbers, per row, with targets and baselines;
      - that the aggressive refresh mode was not issued and remains forbidden;
      - that the six `P!nk/TRUSTFALL (2023)` DO-NOT-RESCAN rows were excluded and verified
        unchanged;
      - that the snapshot is still held;
      - and the two-verdicts rule, restated: the Jellyfin and MA halves are never summed, and the
        standing check still exits 3 while the MA row is reported.

    Do not rewrite the existing Phase 6 sections. They are the dated record of what was true on
    2026-09-20 and 2026-09-21; corrections land as dated additions beside them, which is the
    convention this page already uses throughout.

    Do not write the forbidden mode's literal token into either file in a way that would break an
    existing absence-grep over `artifacts/` — these two files are outside that directory, but keep
    the descriptive naming convention anyway for consistency with the round's artifacts.
  </action>
  <acceptance_criteria>
    - `06-VERIFICATION.md`'s frontmatter parses as YAML and contains a `round_5` key under the
      CONF-04 gap entry and a `superseded_note` key.
    - `06-VERIFICATION.md` contains the literal strings `06-41`, `06-42`, `/gsd-verify 06`, and a
      sentence stating that this plan performed no verification and claims no verification result.
    - `06-VERIFICATION.md` contains a body heading matching `^## Round 5`.
    - `06-VERIFICATION.md`'s headline `status:` and `score:` values are unchanged from before this
      plan — the before-values are recorded in the plan summary and compared, not assumed.
    - `beets.md` gains exactly one new dated subsection under its Phase 6 section, and that
      subsection contains: the three pinned track titles, the literal
      `tank/media/Music@pre-06-41-conf04-reprobe`, the literal `TRUSTFALL`, the literal `zfs diff`,
      and a restatement of the never-summed rule (case-insensitive `never (be )?summed`; the page reads
      `must never be summed` today).
    - `beets.md`'s existing Phase 6 subsections are unmodified: `git diff -U0 -- stacks/selfhosted/arrs/beets.md`
      contains zero lines beginning with `-` other than a trailing-context artifact, i.e. the diff
      is purely additive.
    - No file outside this plan's `files_modified` list was changed.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; V=.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md; BM=stacks/selfhosted/arrs/beets.md; python3 -c "import sys,yaml;t=open(sys.argv[1]).read().split(chr(45)*3)[1];yaml.safe_load(t)" "$V" || { echo "frontmatter no longer parses"; exit 1; }; for s in "round_5" "superseded_note" "06-41" "06-42" "/gsd-verify 06"; do grep -qF "$s" "$V" || { echo "missing in VERIFICATION: $s"; exit 1; }; done; grep -qE "^## Round 5" "$V" || exit 1; for s in "tank/media/Music@pre-06-41-conf04-reprobe" "TRUSTFALL" "zfs diff" "Jewels n" "California Gurls" "Just Give Me a Reason"; do grep -qF "$s" "$BM" || { echo "missing in beets.md: $s"; exit 1; }; done; grep -qiE "never (be )?summed" "$BM" || exit 1; R=$(git diff -U0 -- "$BM" | grep -cE "^-[^-]" || true); test "${R:-0}" -eq 0 || { echo "$R existing beets.md lines were removed or rewritten; the edit must be additive"; exit 1; }; echo OK'</automated>
  </verify>
  <done>`06-VERIFICATION.md` carries a dated round-5 record against the gap it raised while explicitly refusing to re-score itself, and `beets.md` carries a repeatable durable account of the mechanism, the numbers, the controls and the two never-summed verdicts — both purely additive to the dated records already there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured artifacts → the project's requirement records | the point at which a measurement becomes a tick in `REQUIREMENTS.md`; the highest-consequence edit in the round |
| this plan → `06-VERIFICATION.md` | a plan editing a verifier's report; the self-certification hazard rounds 1-4 each refused |
| this plan → the public git repo | all five documents are committed to a PUBLIC repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-45-01 | Repudiation | a requirement ticked on a branch that did not earn it | mitigate | the verify reads BOTH `06-43`'s `BRANCH:` line AND `06-42`'s independent instrument counters, asserts the checkbox state against the conjunction in both directions, and additionally fails if a `BRANCH: A` without corroboration was nonetheless ticked |
| T-06-45-02 | Elevation of privilege | a plan self-declaring the phase verified | mitigate | `status:` and `score:` in `06-VERIFICATION.md` are asserted unchanged; a `superseded_note` states that re-scoring is `/gsd-verify 06`'s call; no `Complete` cell is written for Phase 6 by this plan |
| T-06-45-03 | Tampering | historical records rewritten to match the new outcome | mitigate | the `beets.md` and `deferred-items.md` edits are asserted PURELY ADDITIVE (zero removed lines), and ROADMAP's existing plan list and four round records are asserted untouched |
| T-06-45-04 | Spoofing | E6 read as fully discharged when only its first measurement moved | mitigate | E6 is required to name both measurements explicitly and to retain the `Twista` discrimination with Phase 7; the verify asserts both literals |
| T-06-45-05 | Repudiation | two documents left disagreeing about CONF-04 | mitigate | every `CONF-04` occurrence in `REQUIREMENTS.md` is updated together, and a stale-wording grep (`awaiting Phase 7`) must return 0 on branch A |
| T-06-45-06 | Tampering | the held snapshot destroyed in passing as "cleanup" | mitigate | the snapshot is carried as a named `DEF-06-45-*` entry stating it is still held and that its release is a separate operator decision; no plan in this round destroys it |
| T-06-45-07 | Information disclosure | a credential entering a public document | mitigate | this plan edits prose only and copies no command output containing headers; credential-screen all five files before commit |
| T-06-45-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing — no package manager is invoked |
</threat_model>

<verification>
- `REQUIREMENTS.md`'s CONF-04 checkbox matches the CONJUNCTION of `06-43`'s `BRANCH:` line and
  `06-42`'s instrument corroboration (`INSTRUMENT RUN: MEASURED`, `JF_AT_TARGET: 3`,
  `JF_PENDING: 0`); an uncorroborated `BRANCH: A` halts the plan with the box untouched. No stale
  `awaiting Phase 7` wording survives on a corroborated branch A.
- ROADMAP Phase 6 criterion 4 carries a dated amendment naming `06-41`, `06-42` and `superseded`;
  the status row carries `Round 5` and `/gsd-verify 06`; the plan list is untouched.
- E6 distinguishes its two measurements, names `Twista`, and retains the second with Phase 7;
  E1-E12 are not renumbered.
- `deferred-items.md` grows past 36 unique `DEF-*` entries, starts at `DEF-06-45-01`, names the
  held snapshot, and removes nothing.
- `06-VERIFICATION.md` parses, carries `round_5` and `superseded_note`, keeps its `status`/`score`,
  and recommends `/gsd-verify 06`.
- `beets.md`'s edit is purely additive and carries the mechanism, the numbers, the controls, the
  excluded DO-NOT-RESCAN set and the never-summed rule.
</verification>

<success_criteria>
Six records of CONF-04 — checkbox, traceability row, Phase 6 criterion, Phase 6 status row, Phase 7
E6, verification report and the durable estate page — all state the same measured truth; Phase 7
keeps the measurement that is genuinely still its own; the round's residue and the held snapshot are
carried by name; and no verification verdict was self-declared.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-45-SUMMARY.md` when done.
</output>
