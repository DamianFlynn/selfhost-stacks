---
phase: 06-tagger-configuration-and-dry-run
plan: 37
type: execute
wave: 16
depends_on: ["06-34"]   # round 3's last plan: these four fix the code plans 06-30..06-33 wrote and 06-34 dispositioned
gap_closure: true
gap_closure_round: 4
files_modified:
  - scripts/phase06-incremental-control.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt
autonomous: true
requirements: [CONF-02, CONF-06]
closes_findings: [R4-03, R4-04]

must_haves:
  truths:
    - "A signal delivered to either in-container program terminates it: the cleanup runs, the handler re-raises, and the program dies from the signal it was sent rather than continuing and exiting 0"
    - "A killed run can no longer produce a normal-looking transcript — neither `MANIFEST OK` nor a clean `PROBE-RC` line can be printed after a delivered INT, TERM, HUP or PIPE"
    - "`PIPE` is trapped, because a `docker exec` client killed by `timeout` closes the exec's streams and the in-container shell takes SIGPIPE on its next write — the signal the widened list did not cover"
    - "The R3-09 paragraph no longer presents `timeout` delivering SIGTERM to the in-container shell as an established routine mechanism; it names the forwarding question, says which part is unconfirmed, and says what is believed to arrive instead"
    - "R3-10's two-stage prefix-plus-character-class predicate is preserved exactly — round 4 drove it and found it correct, and this plan must not disturb it"
  artifacts:
    - path: "scripts/phase06-incremental-control.sh"
      provides: "terminal signal handlers on both in-container programs, PIPE covered, and a mechanism paragraph that distinguishes what was proven from what was assumed"
      contains: "R4-03"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt"
      provides: "the dash drive both directions — pre-fix a TERM is swallowed and the program exits 0, post-fix the program dies with the signal's status — plus the SIGPIPE pair and `sh -n` over both extracted heredoc bodies"
  key_links:
    - from: "a delivered INT/TERM/HUP/PIPE"
      to: "the in-container program's exit status"
      via: "a handler that cleans, un-traps and re-raises"
      pattern: "kill -TERM"
---

<objective>
Close the two round-4 findings that land in `scripts/phase06-incremental-control.sh`: **WR-03** and
**WR-04** of `06-REVIEW-GAP3.md`.

⚠ **FINDING IDs IN THIS ROUND ARE ALIASED, AND THIS IS NOT OPTIONAL.** Round 4's report reuses
round 1's `WR-*` / `IN-*` namespace, and round 1's IDs are already cited **in band in this file**
(`CR-01`, `IN-02`, `IN-05`, `IN-08` ×2, `IN-10` ×3). Writing a bare `WR-03` here would be ambiguous
to every future reader and would make any grep for it non-discriminating. **In band, and in every
verify block, this round's findings are `R4-01 … R4-10`**, continuing the phase's per-round
convention (`CR`/`WR`/`IN` = round 1, `GC-*` = round 2, `R3-*` = round 3). Plan 06-39 carries the
mapping table. This plan owns:

| Round-4 report ID | In-band ID | Subject |
|---|---|---|
| WR-03 | **R4-03** | the new `INT TERM HUP` handlers do not exit, so a delivered signal is swallowed: the scratch path is deleted and the program runs on |
| WR-04 | **R4-04** | R3-09's stated mechanism — `timeout` delivering SIGTERM to the in-container shell — is not established, and the signal that most likely *does* arrive is not in the list |

**R4-03 is the sharper of the two, and the review executed it.** POSIX runs a trapped signal's
action and then **resumes** the shell. Both handlers are a bare `case` with no `exit` and no
re-raise, so round 3's widening converted "terminate" into *"delete the scratch directory, keep
going, and exit 0"*. Before that diff a SIGTERM killed the program; now it does not. The review
drove this under `dash` — the same interpreter family these programs run under in the container —
and the transcript is in the finding.

Most downstream paths degrade closed (`sort`, `find` and `sort -z` all fail once `MANDIR` is gone
and route to `MANIFEST BLIND … exit 2`), **but not all**: a signal after the last manifest write
lets the program print `MANIFEST OK` and exit 0, and in `write_prog_taghistory` a signal between the
program write and its execution removes the program file, makes the interpreter invocation fail, and
the shell still reaches the end and exits 0 — **a normal-looking transcript over an interrupted
run.** That is the defect. The in-band claim beside it describes only a SIGKILL window-of-leakage
residual, which is a different and much smaller thing.

**R4-04 is the claim underneath it.** The comment presents a bound expiry as a *routine* outcome
that the widened list now covers. But `timeout` sends SIGTERM to the **`docker exec` client on LXC
100**, not to the `sh -s` inside the container, and `docker exec` has no signal-forwarding path to
the exec'd process. If that holds here, the added `TERM` never fires for this program at all. What
*does* happen is that the exec's stdout stream closes and the in-container shell takes SIGPIPE on
its next write — and `PIPE` was not in the list.

**Do not overstate either.** The review is explicit that its own R4-04 reasoning is **static** and
was deliberately not executed against the container, and that 06-32's honesty about this was right;
the finding is that the reasoning **omitted the forwarding question**, not that it was unverified.
Whatever you write must carry that distinction forward rather than replacing one confident
mechanism story with another.

**Say which fix you are making, per finding, and say it in band:**
- **R4-03 — CODE.** Terminal handlers, driven under `dash` in both directions.
- **R4-04 — BOTH.** `PIPE` joins the list (code, and it is the review's option (a)); the paragraph
  stops naming an unconfirmed mechanism as routine (claim). The PIPE arm's mechanism is **still
  unconfirmed against the container** and must be recorded as such — it is a candidate
  `FIXED (undriven)` row, exactly as R3-09 was.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP3.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/phase06-incremental-control.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make both in-container handlers terminal, add PIPE, and drive it under dash in both directions</name>
  <files>scripts/phase06-incremental-control.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt</files>
  <read_first>
    - **`scripts/phase06-incremental-control.sh` — the file you are modifying.** Read
      `write_prog_manifest()` in full, from its `cat > "$1" <<'REOF'` to its `REOF`, and then
      `write_prog_taghistory()` in full the same way. Anchors:
      `/usr/bin/grep -n 'write_prog_manifest() {\|write_prog_taghistory() {' scripts/phase06-incremental-control.sh`.
    - The two trap statements. Anchors: `trap 'case "${MANDIR:-}" in` and
      `trap 'case "${THPROG:-}" in`. **Read the predicate character by character.** Round 4 drove it
      and recorded it in its *checked and found clean* section: the two-stage
      `prefix` → `remainder must be non-empty and drawn from [A-Za-z0-9._-]` shape refuses `""`, the
      bare prefix, and `/tmp/p6-mf.a/../../../home`, while accepting a real `mktemp` name. **It is
      correct and must survive this change unaltered** — you are changing *when the handler runs and
      what happens after it*, not *what it is willing to remove*.
    - The note explaining `""` rather than `''` for the empty pattern: **the trap body is a
      single-quoted string, so an inner single quote would terminate it.** That constraint still
      applies to anything you write inside a trap body. If you move the predicate into a shell
      function inside the program, the constraint relaxes — say which you did and why.
    - The downstream writes that make the defect reachable. In `write_prog_manifest`: the final
      `echo "MANIFEST OK"`, and the three `MANIFEST BLIND … exit 2` arms above it. In
      `write_prog_taghistory`: the `printf … > "$THPROG"`, the interpreter invocation, and the
      `PROBE-RC` / `STATEFILE-SHA` lines after it. These are the paths the review names as **not**
      degrading closed.
    - The transport, so you know what delivers what. Anchor:
      `timeout $REMOTE_TIMEOUT docker exec -i -u beetle $CONTAINER sh -s`.
    - `06-REVIEW-GAP3.md` §§ WR-03 and WR-04 in full, including WR-03's executed `dash` transcript
      and its suggested handler shape.
    - **`dash` must be available to drive this.** Check with `command -v dash`. If it is absent,
      **do not silently fall back to `bash`** — `bash` is not the interpreter family these programs
      run under, and a drive under the wrong shell is a could-not-look, not a pass. Use
      `/bin/sh` only if you can show it is dash-family, and record which interpreter every drive
      used, by name and version, in the artifact.
  </read_first>
  <action>
**Disposition: R4-03 is CODE. The `PIPE` half of R4-04 is CODE; its paragraph is task 2's.**

**(a) Give each program a named cleanup function and four terminal handlers**, in the shape the
review supplies:
  - one function holding the **existing R3-10 predicate, unchanged**, for that program's variable;
  - `trap '<fn>' EXIT` — unchanged in effect from today;
  - one handler each for `INT`, `TERM`, `HUP` and `PIPE` that runs the cleanup, **removes its own
    trap**, and **re-raises the same signal at itself**, so the program dies from the signal it was
    sent and the caller's exit status says so.

Keep `EXIT` separate from the four, as the review does. Note in band that the cleanup can therefore
run twice on a signalled exit and that this is **harmless and intended**: the predicate refuses a
path it has already removed only in the sense that `rm -rf`/`rm -f` on an absent path succeeds, so
the second call is a no-op. Say it, so nobody "optimises" the EXIT trap away and re-opens the
normal-termination case.

**(b) Add `PIPE` for the reason R4-04 gives, and say what it is and is not.** A `docker exec` client
killed by `timeout` closes the exec's streams; the in-container shell then takes SIGPIPE on its next
write, and both programs write to stdout. A shell killed by an **untrapped** SIGPIPE runs no trap at
all, which is how the leak survives even if the `TERM` path never fires. State plainly that this is
**the same class of static reasoning** as R3-09's and is **not confirmed against the container**.

**(c) Do not touch the predicate.** Round 4 drove it and found it correct. Re-deriving, tightening
or "simplifying" it is out of scope, and a change there would be the one place this plan could turn
a correct fence into a broken one. If the predicate moves into the function body verbatim, prove the
move is verbatim by diffing the extracted text.

**(d) Drive it, both directions, under `dash`, with no estate contact.** Build the drive as a
standalone script in a `mktemp -d` that reproduces the program's trap shape over a `mktemp -d`
scratch — the review's own method. At minimum:
  1. **pre-fix shape, SIGTERM**: the program continues past the signal and exits **0**; the scratch
     directory is gone. This is the defect, reproduced.
  2. **post-fix shape, SIGTERM**: the program does **not** continue; the wait status reports death
     by SIGTERM (128+15); the scratch directory is gone.
  3. the same pair for **INT** and **HUP**.
  4. **SIGPIPE pair**: with the reader closed, the pre-fix shape leaves the scratch directory
     behind (no trap fires); the post-fix shape removes it and dies from SIGPIPE.
  5. a **positive control**: an ordinary, unsignalled run still reaches its end, prints its terminal
     line, exits 0, and leaves nothing behind. Without this a "fix" that kills the program early
     looks identical to a fix that works.

**(e) Parse both real heredoc bodies as POSIX `sh`.** `bash -n` on the outer file parses heredocs as
**data** and cannot see a syntax error inside either program — that is why round 3 extracted both
bodies and ran `sh -n` on each, and it is the gate this change most needs, since it edits program
text `bash -n` will not read. Extract both, assert each is non-empty **before** parsing it (an empty
extraction passes `sh -n` trivially and would be a vacuous gate of exactly the CR-01 shape), and
record the line counts.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides the exit code
F=scripts/phase06-incremental-control.sh
G=/usr/bin/grep

# --- (a) the four handlers are terminal at BOTH programs. Count the re-raises: two programs x
# four signals = 8. This number is a property of the change, not of the file's prose, so pinning
# it does not pin something a later sentence moves.
test "$($G -cF 'kill -INT' $F)" -eq 2
test "$($G -cF 'kill -TERM' $F)" -eq 2
test "$($G -cF 'kill -HUP' $F)" -eq 2
test "$($G -cF 'kill -PIPE' $F)" -eq 2
# each re-raise is preceded by its own un-trap, so the re-raise is not caught by the same handler
test "$($G -cF 'trap - INT' $F)" -eq 2
test "$($G -cF 'trap - TERM' $F)" -eq 2
test "$($G -cF 'trap - HUP' $F)" -eq 2
test "$($G -cF 'trap - PIPE' $F)" -eq 2

# --- the OLD non-terminal shape is gone. Whole construction: a trap whose signal list is the four
# names with no handler body of its own. It is NOT a substring of the replacement, which splits the
# list across separate trap statements.
test "$($G -cF "esac' EXIT INT TERM HUP" $F)" -eq 0

# --- EXIT is still trapped separately at both programs
test "$($G -c "^[[:space:]]*trap .* EXIT\$" $F)" -eq 2

# --- (c) R3-10's predicate SURVIVED. Round 4 drove it and found it correct; a change here is the
# one way this plan could turn a correct fence into a broken one.
test "$($G -cF '""|*[!A-Za-z0-9._-]*) : ;;' $F)" -eq 2
test "$($G -cF '/tmp/p6-mf.' $F)" -ge 1
test "$($G -cF '/tmp/p6-taghist.' $F)" -ge 1
test "$($G -cF 'rm -rf "$MANDIR"' $F)" -eq 1
test "$($G -cF 'rm -f "$THPROG"' $F)" -eq 1

test "$($G -cF 'R4-03' $F)" -ge 1
test "$($G -cF 'R4-04' $F)" -ge 1

bash -n $F
rc=0; bash $F --self-test >/dev/null 2>&1 || rc=$?
test "$rc" -eq 0

# --- (e) the gate bash -n cannot give: both heredoc bodies parse as POSIX sh.
# The NON-EMPTY assertion comes first and is not optional -- `sh -n` over an empty file exits 0,
# so an extraction that silently matched nothing would print a green tick over no parse at all.
# That is CR-01's shape and this phase has shipped it twice.
W="$(mktemp -d)"
/usr/bin/awk '/^write_prog_manifest\(\) \{/{f=1;next} f&&/^REOF$/{exit} f&&!/<<.REOF.$/{print}' $F > "$W/manifest.sh"
/usr/bin/awk '/^write_prog_taghistory\(\) \{/{f=1;next} f&&/^REOF$/{exit} f&&!/<<.REOF.$/{print}' $F > "$W/taghist.sh"
test "$(/usr/bin/grep -c '' "$W/manifest.sh")" -ge 30
test "$(/usr/bin/grep -c '' "$W/taghist.sh")"  -ge 10
test "$(/usr/bin/grep -cF 'kill -TERM' "$W/manifest.sh")" -eq 1
test "$(/usr/bin/grep -cF 'kill -TERM' "$W/taghist.sh")"  -eq 1
/bin/sh -n "$W/manifest.sh"
/bin/sh -n "$W/taghist.sh"

# --- (d) the drive is recorded, with the interpreter named and both directions present
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt
test -s "$ART"
test "$($G -cF 'R4-03' "$ART")" -ge 1
test "$($G -ciF 'dash' "$ART")" -ge 1
test "$($G -cF '143' "$ART")" -ge 1     # 128+15: death by SIGTERM, the post-fix status
test "$($G -ciF 'positive control' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- Both in-container programs carry a named cleanup function holding R3-10's predicate **unchanged**,
  a separate `EXIT` trap, and four handlers (`INT`, `TERM`, `HUP`, `PIPE`) that clean, un-trap and
  re-raise — eight re-raises and eight un-traps across the file.
- The literal `esac' EXIT INT TERM HUP` appears **0** times; `rm -rf "$MANDIR"` and
  `rm -f "$THPROG"` each appear exactly once; the empty-remainder pattern
  `""|*[!A-Za-z0-9._-]*) : ;;` appears exactly twice.
- A `dash` drive in the artifact shows, for SIGTERM, SIGINT and SIGHUP: pre-fix the program
  continues and exits 0, post-fix it dies with status 128+signal. A SIGPIPE pair shows the scratch
  surviving pre-fix and removed post-fix. A positive control shows an unsignalled run completing
  normally and leaving nothing behind. The interpreter is named with its version.
- Both heredoc bodies extract to non-empty files (asserted before parsing), each contains exactly
  one `kill -TERM`, and each passes `/bin/sh -n`.
- `bash -n` exits 0 and `--self-test` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Stop the R3-09 paragraph naming an unconfirmed mechanism as routine, and state the two residuals it hides</name>
  <files>scripts/phase06-incremental-control.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt</files>
  <read_first>
    - **`scripts/phase06-incremental-control.sh`** — the R3-09 paragraph in full. Anchors, all whole
      constructions: `R3-09: THE SIGNAL LIST IS NOT JUST THE EXIT PSEUDO-SIGNAL`,
      `so a bound expiry is a ROUTINE outcome for a manifest over a large tree`,
      `THE RESIDUAL, stated rather than claimed away: SIGKILL cannot be trapped`, and the
      cross-reference in the second program, `R3-09, same reasoning as the manifest trap above`.
    - The `NOT DONE, deliberately` paragraph recording round 3's refusal of the `/tmp/p6-mf.*`
      sweep. **That refusal stands and its reasoning is unchanged** — R4-04 does not reopen it, and
      `DEF-06-34-01` owns it. Do not delete or weaken it.
    - `DEF-06-34-06` in `deferred-items.md` — the SIGKILL residual entry. Your corrected paragraph
      must stay consistent with it: `SIGKILL` is still untrappable and still a residual.
    - `06-REVIEW-GAP3.md` § WR-04 in full, and in particular its two options (a) and (b) and its
      closing instruction: *"Do not leave a comment that names one mechanism as routine when the
      transport probably does not deliver it."*
  </read_first>
  <action>
**Disposition: R4-04 is a CLAIM CORRECTION here (its code half was task 1's `PIPE` addition).** The
honest fix is to narrow the sentence, not to widen anything further. This round's whole subject is
claims outrunning their code; replacing one confident mechanism story with another would reproduce
the defect in a new shape.

**(a) Rewrite the mechanism sentence.** The paragraph must no longer present *"a bound expiry is a
ROUTINE outcome … and on the narrow list that expiry left behind exactly the minted leftover"* as an
established account. Replace it with a statement that distinguishes three things by name:
  1. **what is established**: the transport is `timeout $REMOTE_TIMEOUT docker exec … sh -s`, which
     is grep-confirmable in this file, and an `EXIT` trap does not run on an uncaught signal, which
     is POSIX;
  2. **what is not established**: that `timeout` delivers SIGTERM to the *in-container* shell.
     `timeout` signals the `docker exec` **client on LXC 100**, and `docker exec` is **not known to
     forward signals** to the exec'd process. **Say that the SIGTERM path is unconfirmed**, and say
     it was not tested, and say why it was not tested (this phase makes no estate contact);
  3. **what is believed to arrive instead**: the exec's stdout stream closes when the client dies,
     and the in-container shell takes `EPIPE`/`SIGPIPE` on its next write. Both programs write to
     stdout. This is why `PIPE` is now in the list — and this reasoning is **also static**, of the
     same grade as (2), not an upgrade over it.

**(b) Correct the residual paragraph, which currently frames the problem as a window of leakage.**
Round 3's `THE RESIDUAL` sentence says widening the list *shrinks the window*. Under R4-03 that
framing was wrong in a second way: on the non-terminal handlers the signal was **absorbed**, not
narrowed, and the program continued. Record, in this order:
  - what R4-03 changed and why the old framing under-described it — name the two reachable
    normal-looking-transcript paths (`MANIFEST OK` after the last write; the taghistory program
    file removed between write and execution);
  - that `SIGKILL` remains untrappable and is still a genuine residual, consistent with
    `DEF-06-34-06`;
  - that **neither trap has ever been observed firing inside the container**, and the condition that
    would drive it: a live `--arm a` / `--arm b` pair under a deliberately short `REMOTE_TIMEOUT`,
    checking the container's `/tmp` afterwards. Point at Phase 7 entry criterion **E12**, which
    already owns this class; **do not invent a new criterion.**

**(c) Update the cross-reference in the second program** so it points at the corrected paragraph
rather than at the superseded account. One clause, not a duplicate of the whole paragraph — the
file's own convention is *stated once there*.

**(d) Name R4-03 and R4-04, and state the fix kind for each**, so the next reviewer does not have to
infer whether a fence moved or a sentence did.

**(e) Keep round 3's refusal intact.** The `NOT DONE, deliberately` paragraph about the
`/tmp/p6-mf.*` sweep stays as written. R4-04 does not reopen it, and a refusal that quietly
disappears reads as an oversight — which is the whole reason `DEF-06-34-01` exists.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
F=scripts/phase06-incremental-control.sh
G=/usr/bin/grep

# (a) the unconfirmed-mechanism-as-routine sentence is withdrawn. Whole construction; a corrected
# paragraph that says the opposite cannot contain it.
test "$($G -cF 'so a bound expiry is a ROUTINE outcome for a manifest over a large tree' $F)" -eq 0
# and the forwarding question is now named in band
test "$($G -ciF 'forward' $F)" -ge 1
test "$($G -ciF 'unconfirmed' $F)" -ge 1
test "$($G -ciF 'SIGPIPE' $F)" -ge 1

# (b) the residual paragraph still names SIGKILL, consistently with DEF-06-34-06, and now names
# the absorbed-signal case too
test "$($G -cF 'SIGKILL' $F)" -ge 1
test "$($G -cF 'MANIFEST OK' $F)" -ge 2      # the emitting line, plus the paragraph naming it
test "$($G -cF 'E12' $F)" -ge 1

# (c)(d) both findings named, with the fix kind stated
test "$($G -cF 'R4-03' $F)" -ge 1
test "$($G -cF 'R4-04' $F)" -ge 1
test "$($G -cF 'CLAIM CORRECTION' $F)" -ge 1

# (e) round 3's refusal is intact and is still cited to its deferred entry
test "$($G -cF 'NOT DONE, deliberately' $F)" -ge 1
test "$($G -cF '06-34' $F)" -ge 1

# THIS TASK CHANGES NO EXECUTABLE LINE. Proven mechanically rather than asserted, the way round 3
# proved its three comment-only fixes: the diff for this task must be comments only.
test "$(git diff -U0 -- $F | $G -c '^[+-][^+-]' || true)" -ge 1
test "$(git diff -U0 -- $F | $G '^[+-][^+-]' | $G -cvE '^[+-][[:space:]]*#')" -eq 0

bash -n $F
rc=0; bash $F --self-test >/dev/null 2>&1 || rc=$?
test "$rc" -eq 0

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-37-incremental-terminal-traps.txt
test "$($G -cF 'R4-04' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- The literal `so a bound expiry is a ROUTINE outcome for a manifest over a large tree` is gone.
- The paragraph names, separately: what is established (the transport, and POSIX's `EXIT`-trap
  rule), what is **unconfirmed** (that `timeout` delivers SIGTERM to the in-container shell, given
  `docker exec` is not known to forward signals, untested because this phase makes no estate
  contact), and what is **believed** to arrive instead (stream close → `SIGPIPE`), with the last
  graded as the same kind of static reasoning as the second.
- The residual paragraph names the two normal-looking-transcript paths R4-03 closed, keeps
  `SIGKILL` as a genuine residual consistent with `DEF-06-34-06`, records that neither trap has been
  observed firing in the container, and attaches the driving condition to existing criterion **E12**
  without inventing a new one.
- The second program's cross-reference points at the corrected paragraph.
- `R4-03`, `R4-04` and `CLAIM CORRECTION` all appear in band; round 3's `NOT DONE, deliberately`
  refusal is byte-intact.
- **This task's diff is comments only** — `git diff -U0` shows at least one changed line and zero
  changed lines that are not comments.
- `bash -n` exits 0 and `--self-test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a delivered signal → the in-container program's control flow | POSIX resumes the shell after a trap action; round 3's handlers neither exit nor re-raise, so `terminate` became `clean up and carry on` |
| an interrupted run → the transcript the caller parses | `MANIFEST OK` and a clean `PROBE-RC` are both reachable after a delivered signal, so a killed run reads as a completed one |
| the `docker exec` client on LXC 100 → the `sh -s` inside the container | `timeout` signals the client; the daemon tears down the streams; no signal is known to be forwarded |
| the in-band mechanism story → the next reader | a comment naming one mechanism as routine when the transport probably does not deliver it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R403 | Spoofing (an interrupted run presenting as a completed one) | both in-container programs' signal handlers | mitigate | handlers clean, un-trap and re-raise so the program dies from the signal it was sent; driven under `dash` in both directions with a positive control |
| T-06-R404 | Repudiation (an in-band mechanism claim the transport does not support) | the R3-09 paragraph and its cross-reference | mitigate | the routine-SIGTERM sentence withdrawn; established / unconfirmed / believed separated by name; `PIPE` added and its own reasoning graded static |
| T-06-R403b | Tampering (a correct fence broken while fixing the handler around it) | R3-10's two-stage predicate | mitigate | predicate preserved verbatim and asserted by literal; round 4 already drove it and found it correct |
| T-06-37-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- **No estate contact.** No `--run`, no `--arm`, no ssh, no docker. `--self-test` is workstation-only
  and is the only invocation of this script. Every signal drive is a standalone script in a
  `mktemp -d`, never the real program against the real container.
- `bash -n scripts/phase06-incremental-control.sh` parses clean after every task, **and** both
  extracted heredoc bodies pass `/bin/sh -n` — the gate `bash -n` cannot give, since it parses
  heredocs as data. The extraction is asserted non-empty before it is parsed.
- **How these verify blocks are driven.** Before any edit, extract both `<automated>` blocks,
  `bash -n` each, and run each against the unfixed tree: task 1 must fail at the `kill -INT` count,
  task 2 at the routine-SIGTERM absence test. Neither may reach its end. A block that passes pre-fix
  is vacuous and must be rewritten first; record both pre-fix transcripts in the artifact.
- If `dash` is not installed, task 1's drive is a **could-not-look**, not a pass. Record it as such
  and say so in the summary rather than substituting `bash`.
- No `git push`, no host `git pull`. `git diff --name-only -- scripts/` lists only
  `scripts/phase06-incremental-control.sh`.
- This plan must not touch `scripts/setup-neocortex-memory.sh`,
  `stacks/selfhosted/neocortex-memory/` or `stacks/selfhosted/agentic-os/`.
- **No requirement checkbox moves. CONF-04 is not closed and is not claimed. The phase is not
  declared complete.**
- `06-VERIFICATION.md` is **stale and out of scope**. Do not read it as a gap source and do not
  touch it.
</verification>

<success_criteria>
- R4-03 closed as code and **driven under `dash` in both directions**: a delivered signal kills the
  program and the caller's status says so, and an unsignalled run is unaffected.
- R4-04 closed as both: `PIPE` is trapped, and the paragraph separates what is established from
  what is unconfirmed from what is believed — with the new reasoning graded no higher than the old.
- R3-10's predicate is byte-unchanged in effect and asserted so.
- Task 2's diff contains no executable line, proven mechanically.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-37-SUMMARY.md` when done.
Include a **NOT-DRIVEN register** stating explicitly that the container-side signal behaviour —
both the SIGTERM path and the new SIGPIPE path — remains **unobserved inside `beets-flask`**, that
the local `dash` drive proves the handler shape and not the delivery, and that the driving condition
attaches to existing Phase 7 entry criterion **E12**. State per finding whether the fix was CODE,
CLAIM CORRECTION or BOTH, and name the interpreter and version every drive ran under.
</output>
