---
phase: 06-tagger-configuration-and-dry-run
plan: 12
type: execute
wave: 4
depends_on: ["06-05", "06-07", "06-09"]
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-country-preference.txt
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-preview-crosscheck.txt
autonomous: true
requirements: [CONF-05, CONF-06]
must_haves:
  truths:
    - "Match disambiguation is DEMONSTRATED, not merely set: a *Now!* volume prefers the UK release over the US one under a real candidate lookup, with the chosen candidate's `country` read back (CONF-05)"
    - "`preferred.countries` is proven AS WRITTEN — `GB`, the code MusicBrainz actually stores; `UK` alone silently matches nothing, and entries are regexes"
    - "The embedded per-file `LOCATION=…/release/NNNNNN` Discogs id is NOT used: it would prove a different mechanism and leave the configured one untested (D-28). It is recorded as a Phase 9 shortcut, not acted on"
    - "`--pretend` cannot demonstrate any of this — it never calls `lookup_candidates`, so the proof is a real candidate lookup on a throwaway library (D-33)"
    - "D-30's second arm runs: flask `preview` on a subset, cross-checked against the `docker exec` oracle, and a disagreement between them is itself a finding worth having before Phase 7"
    - "OQ-4 is resolved by inspection: if rc6's `preview` does not show computed destination paths, the agreement basis is redefined on the chosen MBID plus the `albumartist`/`album`/`track` triple, and that redefinition is recorded rather than assumed"
    - "Never accept a match without a track-count check — MusicBrainz entries for this content are frequently 5-10 track stubs against 15-20 track releases, and a confident wrong match is the worst outcome"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-country-preference.txt"
      provides: "the driven CONF-05 proof: candidate list, chosen candidate, its country, and the negative control"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-preview-crosscheck.txt"
      provides: "D-30 arm 2, the OQ-4 resolution, and the agreement or disagreement between the two arms"
  key_links:
    - from: "match.preferred.countries"
      to: "the chosen MusicBrainz candidate's country field"
      via: "beet import -t on a throwaway library with autotag on"
      pattern: "GB"
---

<objective>
Demonstrate the two things that cannot be read off a config file: that `preferred.countries` actually changes which release is chosen, and that the flask inbox path agrees with the `docker exec` oracle.

Purpose: criterion 5 asks for disambiguation "demonstrated, not merely set". `--pretend` cannot do it — `lookup_candidates` is never in that pipeline, so there is no candidate to inspect. And D-30 exists because every Phase 6 criterion is phrased as a CLI operation while flask is the runtime, and its inboxes have no `--pretend`. If the two arms disagree, that disagreement is the finding, and it is far cheaper to have it now than in Phase 7.

Output: two transcripts. Nothing under `/mnt/tank/media` is written and the sampled source folders are proven unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-07-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-11-SUMMARY.md
@scripts/phase06-oracle.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONF-05 driven — a Now! volume prefers the UK release, with a negative control</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-country-preference.txt</files>
  <read_first>
    - .planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md — the S4 *Now!* row drawn for this proof, and its file count and `album` value.
    - stacks/selfhosted/arrs/beets/config.yaml — the `match.preferred` block and `musicbrainz.extra_tags`, exactly as committed.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "F-02: what `beet import --pretend` actually does" (lines 397-424) — why this proof cannot be `--pretend`.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "The throwaway library, and the state file `-l` does NOT cover" (lines 438-463) — the overlay that must set `library`, `statefile` AND `directory`.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md § D-28 — the embedded Discogs id is a Phase 9 shortcut, recorded and not acted on.
    - CLAUDE.md § Constraints — "never accept a match without a track-count check", and the measured fact that MusicBrainz entries for this content are frequently 5-10 track stubs against 15-20 track releases.
    - .planning/phases/03-tagger-spike/03-DECISION.md § Handoff — the one instrument Phase 3 proved unreliable, so it is not reached for here.
  </read_first>
  <action>
Drive a real candidate lookup on the sampled *Now!* volume, on a throwaway library, and read back which release beets chose and why.

**Overlay and fence.** Same discipline as every other throwaway invocation: a `-c` overlay setting `library: /tmp/p6c/lib.db`, `statefile: /tmp/p6c/state.pickle`, `directory: /tmp/p6c/tree`, with `copy: no`, `move: no`, `write: no`. Refuse to run against a non-empty `/tmp/p6c`. Take a sha256 manifest over the source folder before and after and assert it identical — this arm has `autotag` ON and is therefore the closest thing in Phase 6 to a real import, which is exactly why the fence matters most here.

**The instrument is `beet import -t` (timid) with autotag on**, run inside the flask container as `beetle` on the absolute `/venv/bin/beet`. Capture the full candidate list beets offers, not just the chosen one. For each candidate record: the release MBID, its `country`, its `year`/`original_year`, its `label`, its `catalognum`, its `media`, and its **track count**.

**The assertions.**
- The chosen candidate's `country` is `GB`. Record the full candidate list so it is visible that a US candidate was available and was not chosen — a preference that had nothing to prefer against proves nothing.
- `preferred.original_year` is exercised: record whether the chosen candidate's `original_year` differs from its `year`, and whether that influenced the ordering.
- `musicbrainz.extra_tags` is exercised: record which of `year`, `catalognum`, `country`, `media`, `label` beets actually used in the query, from the request beets made.
- **Track count.** Assert the chosen candidate's track count equals the folder's file count. Never accept a match without it: for this content a confident wrong match is more likely than no match, and it is the worst outcome available.

**The negative control, because a preference observed once is not a preference proven.** Re-run the identical lookup on a second overlay whose ONLY difference is `match.preferred.countries` — set it to `['US','GB']`, or empty it — and show the chosen candidate changes, or show it does not and record that the preference had no effect on this release. Two overlays, one key apart, opposite or identical outcomes both recorded. If the outcome is identical, the honest reading is that this release does not discriminate the setting, and a second *Now!* volume from the S4 stratum's next eligible row must be tried before CONF-05 is called demonstrated.

**Also record, and do not act on, the D-28 shortcut.** This collection carries a per-file `LOCATION=https://www.discogs.com/…/release/NNNNNN` tag. Using it would prove a different mechanism and leave the configured one untested, and the survivor is deliberately MusicBrainz-only with the Discogs credential dismissed in Phase 4 D-24. Record it in the artifact as a **Phase 9** shortcut with a pointer, so the option is not rediscovered as a novelty.

Clean up `/tmp/p6c` and assert it is gone. Assert the real `/config/library.db` and `/config/state.pickle` sha256 are unchanged.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-country-preference.txt; test -s "$f" || exit 1; grep -qE "\bGB\b" "$f" || { echo "no GB candidate recorded"; exit 1; }; grep -qE "\bUS\b" "$f" || { echo "no US candidate recorded — a preference with nothing to prefer against proves nothing"; exit 1; }; grep -qiE "track ?count|tracktotal" "$f" || { echo "the track-count check is missing"; exit 1; }; grep -qiE "negative control|second overlay|one key" "$f" || { echo "the negative control is not recorded"; exit 1; }; grep -qi "original_year" "$f" || exit 1; grep -qi "extra_tags" "$f" || exit 1; grep -qi "D-28" "$f" || grep -qi "discogs" "$f" || { echo "the D-28 shortcut is not recorded as deferred"; exit 1; }; if grep -qE "import[[:space:]]+--pretend" "$f"; then echo "--pretend used for a candidate proof; it never calls lookup_candidates"; exit 1; fi; grep -qi "state.pickle" "$f" || { echo "D-29 layer 3 not asserted for this arm"; exit 1; }; echo "CONF-05 driven proof recorded"'</automated>
  </verify>
  <done>
    The artifact holds the full candidate list with MBID, country, year/original_year, label, catalognum, media and track count for each; the chosen candidate is `GB` with a `US` alternative visible; the track count equals the folder's file count; `original_year` and `extra_tags` are each shown to have been exercised; the negative control is recorded with its outcome (and, if identical, a second S4 row was tried); the D-28 Discogs shortcut is recorded as deferred to Phase 9 and not used; `/tmp/p6c` is gone and both real beets state files are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-30 arm 2 — the flask preview cross-check, and the OQ-4 resolution</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-preview-crosscheck.txt</files>
  <read_first>
    - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-11-oracle-run.txt — arm 1's destination list, which arm 2 is compared against.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § OQ-4 (lines 1042-1044) — nobody has recorded whether rc6's `preview` shows computed destination paths; this must be resolved by inspection before the comparison is written.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "D-30's two arms measure two different objects" (lines 528-569) — why the two arms are not expected to be identical by construction.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "P5c" (lines 610-618) — `debounce_before_autotag: 30`; budget ≥ 35 s.
    - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-06-inbox-liveness.txt — the throwaway-folder discipline this task repeats: copy, transit, remove, with a source manifest either side.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md § D-30 and § D-35 — D-30 requires a subset to go through `02-review`; D-35's parenthetical says D-09's folder is "the only thing that ever transits 02-review this phase". Read both before writing the amendment note this task must record.
  </read_first>
  <action>
Run D-30's second arm: a subset of the sample through flask's `02-review` inbox on the `preview` policy, cross-checked against arm 1.

**Resolve OQ-4 FIRST, by inspection, before the comparison is written.** Put one folder through and look at what the preview actually renders. Two possible outcomes, and the plan covers both:
- **If preview shows computed destination paths:** the agreement basis is the paths themselves, compared against arm 1's destination list for the same folder.
- **If it does not:** redefine the agreement basis on the **chosen MBID plus the `albumartist`/`album`/`track` triple** per item, and record that redefinition explicitly in the artifact header with what preview did show. Do not stretch a weaker signal into a path claim.
Either way, write down what preview displays, because no record of it exists anywhere in this project and the next phase will want it.

**The transit discipline is the same as D-09 part 2's**, and the amendment must be recorded. Copy — never move — a subset of already-duplicated sample material into a uniquely named folder under `02-review`, take a sha256 manifest of the source before and after and assert it identical, wait past the 30 s debounce (≥ 35 s, bound ≥ 180 s), read the preview, then REMOVE the folder and assert `02-review` is back to zero entries.

⚠ **Record the in-band amendment.** D-35's parenthetical states that D-09's throwaway folder is the only thing that transits `02-review` this phase. D-30 requires this cross-check, so this is a **second, deliberate, named transit** — the last one — and it is recorded as an amendment rather than left to read as a contradiction between two locked decisions. Name the folder, state that it was copied and removed, and state that `02-review` is empty at the end.

**The comparison, reported honestly.** Compare arm 2's result against arm 1's for the same folder on the resolved basis. Three possible verdicts, all legitimate outcomes to record:
- **Agree** — say so, and say on which basis.
- **Disagree** — this is the finding D-30 exists to surface. Record both values side by side and the likely mechanism: arm 1 reads the confuse view through `beet`, while flask's server injects rc6's whole schema dataclass at confuse's highest priority in `commit_to_beets()`, so the two are genuinely different objects and a difference is explicable rather than mysterious. Cross-reference plan 06-07's inter-arm disagreement table.
- **Could not compare** — preview unreadable, folder never picked up, or the basis unresolvable. That is `UNKNOWN, not green`, never an implied agreement.

Do **not** change the vendored config to make the arms agree inside this plan. A disagreement is information; a config edited until two instruments agree is a lost measurement.

**Record the `bootleg` gate as still unexercised.** `03-asis` is registered (D-06) but nothing stages into it this phase, so the `bootleg` album-populated gate — never route a folder to `bootleg` without first asserting `album` is populated and distinct across the intended groups — remains a **Phase 7 routing-time** concern. State it, because the same button produced Phase 3's best and worst results purely on that condition, with no UI signal.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-preview-crosscheck.txt; test -s "$f" || exit 1; grep -qi "OQ-4" "$f" || { echo "the OQ-4 resolution is not recorded"; exit 1; }; grep -qiE "agree|disagree|could not compare" "$f" || { echo "no comparison verdict"; exit 1; }; grep -qi "AMEND" "$f" || { echo "the D-35 second-transit amendment is not recorded"; exit 1; }; grep -qi "bootleg" "$f" || { echo "the bootleg gate is not carried to Phase 7"; exit 1; }; grep -qiE "debounce|35 s|>= ?35" "$f" || { echo "the debounce wait is not evidenced"; exit 1; }; OUT=$(ssh -n -o BatchMode=yes -o ConnectTimeout=5 root@172.16.1.159 "set -o pipefail; timeout 60 find /mnt/tank/downloads/complete/nzb/_inbox/02-review -mindepth 1 | wc -l"); RC=$?; if [ "$RC" -eq 124 ]; then echo "UNKNOWN — bound exceeded; NOT an empty 02-review"; exit 1; fi; [ "$RC" -eq 0 ] || { echo "UNKNOWN — ssh exit $RC"; exit 1; }; N=$(printf "%s" "$OUT" | tr -d "[:space:]"); echo "$N" | grep -qE "^[0-9]+$" || { echo "UNKNOWN — non-numeric \"$N\""; exit 1; }; [ "$N" -eq 0 ] || { echo "02-review still holds $N entries"; exit 1; }; echo "D-30 arm 2 recorded; 02-review empty"'</automated>
  </verify>
  <done>
    The artifact records what rc6's `preview` actually displays, the resolved OQ-4 agreement basis with its reason, the comparison verdict against arm 1 (agree / disagree with both values / could-not-compare as `UNKNOWN, not green`), the in-band amendment naming this as the second and last `02-review` transit, and the `bootleg` gate carried forward to Phase 7. `02-review` is empty; the source manifest is identical either side; no config was edited to make the arms agree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beets → MusicBrainz WS/2 over the network | This is the only arm in Phase 6 that makes outbound metadata queries. It reads; it does not publish anything about the library beyond the query terms. |
| `02-review` inbox → live watchdog | Anything placed here is eligible to be acted on by a running importer. |
| CLI confuse view vs server-committed config | The two arms measure different objects; treating agreement as expected would hide a real difference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-60 | Tampering | An `autotag: yes` arm writing tags back to real source files under the `rw` downloads mount | mitigate | `write: no`, `copy: no`, `move: no` in the overlay; a sha256 manifest over the source folder identical before and after, asserted for both arms. |
| T-06-61 | Spoofing | A confident wrong MusicBrainz match accepted because the count "looked plausible" | mitigate | The chosen candidate's track count must equal the folder's file count; the full candidate list is recorded so an unavailable alternative cannot masquerade as a preference. |
| T-06-62 | Spoofing | A country preference "observed" on a release that cannot discriminate it | mitigate | A one-key negative control overlay; if the outcome is identical, a second S4 row must be tried before CONF-05 is called demonstrated. |
| T-06-63 | Repudiation | Arm 1 and arm 2 quietly reconciled by editing the config until they agree | mitigate | The plan forbids it explicitly; a disagreement is recorded side by side with its mechanism and cross-referenced to plan 06-07's table. |
| T-06-64 | Tampering | The cross-check folder left in `02-review`, or built from unique material | mitigate | Copied from already-duplicated sample material, removed at the end, `02-review` asserted empty by the verify command. |
| T-06-65 | Information Disclosure | MusicBrainz queries or transcripts leaking a credential into a public repo | mitigate | No credential is used: the survivor is MusicBrainz-only and the Discogs token was dismissed in Phase 4 D-24 and is explicitly NOT loaded here (D-28). Re-screen the artifacts before committing. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nothing is installed; `beet` and `python` are already inside the pinned rc6 image. |
</threat_model>

<verification>
- Both task verify commands exit 0.
- `02-review` reports zero entries from a bounded remote command.
- `scripts/check-music-freeze.sh` exits 0 after both arms.
- The real `library.db` and `state.pickle` sha256 are unchanged.
</verification>

<success_criteria>
- CONF-05 is discharged by demonstration, not by a read-back: a `GB` candidate chosen over an available `US` one, with the track count checked and a one-key negative control run.
- The D-28 Discogs shortcut is recorded and not used.
- D-30's second arm ran, OQ-4 is resolved and recorded, and the comparison verdict is one of agree / disagree-with-values / `UNKNOWN, not green`.
- The second `02-review` transit is recorded as an in-band amendment to D-35's parenthetical.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-12-SUMMARY.md` when done.
</output>
