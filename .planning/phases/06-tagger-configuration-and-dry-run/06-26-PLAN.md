---
phase: 06-tagger-configuration-and-dry-run
plan: 26
type: execute
wave: 2
depends_on: ["06-23"]
gap_closure: true
gap_closure_round: 2
files_modified:
  - scripts/quick-health-check.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]
closes_findings: [GC-17, GC-14, GC-09]

must_haves:
  truths:
    - "Every overridable path this file interpolates into a remote command string is quoted, so an override containing a space drives the branch it exists to drive instead of failing with a shell error"
    - "The three sites the review names are fixed AND the fourth site of the same shape found while planning is fixed with them — a class closed in one file, not three of four"
    - "The `CONSUMERS_SCRIPT` override is visible on every arm that can report it, not only on the arm that can report green — an operator is never told the estate is off target when what was measured is an arbitrary file"
    - "The failing-block tail names every block that can carry a ❌ or a ⚠️, including the D-03 mount block and the D-04 scan, so the reader is not sent to the green ones"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "quoted knob interpolation, override-visible diagnosis on every consumers arm, and a complete failing-block tail"
      contains: "CONSUMERS_OVERRIDDEN"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt"
      provides: "the driven space-bearing-override runs, before and after, and the tail's before/after text"
  key_links:
    - from: "D03_REPO_ROOT / D04_REPO_ROOT / DRIFT_REPO_ROOT / CONSUMERS_SCRIPT"
      to: "the remote command strings sent over ssh"
      via: "a printf '%q'-rendered copy of the value, not the raw value"
      pattern: "printf .%q."
---

<objective>
Close **GC-17**, **GC-14** and **GC-09** — the three round-2 findings in
`scripts/quick-health-check.sh` that are not part of the D-04 verdict ladder plan 06-23 owns.

GC-17: three overridable paths reach remote command strings unquoted —
`cd $D03_REPO_ROOT`, `D04_CMD="cd $D04_REPO_ROOT …` and `bash $CONSUMERS_SCRIPT`. An override
containing a space word-splits on the remote shell, so the branch the override exists to drive
fails with a shell error instead of being driven. This is `phase06-oracle.sh`'s WR-08 defect
reproduced in the sibling file WR-08's plan did not own. Severity WARNING, and the bound is real
and must be stated rather than dramatised: all three are additive knobs that cannot produce a green
tick, and every default value contains no spaces.

**Two citation corrections, both grep-verified during planning, that this plan inherits rather than
rediscovers.** (1) The cross-family reviewer cited `:1515`, `:1720`, `:2193`; two of those are an
`echo` and an assignment — its line numbers drifted because it numbered the diff, not the file. The
corrected sites are the three named above. (2) The adjudication's summary line says "GC-15 was
separately confirmed … the three sites are real, at `quick-health-check.sh:1515`, `:1728`, `:2196`"
— **that sentence conflates GC-15 with GC-17**. GC-15 is two `dex_cmd sha256sum` sites in
`phase06-oracle.sh` and belongs to plan 06-27; the three `quick-health-check.sh` sites are GC-17's
and belong here. Both findings are real; only the label was crossed.

**A fourth site of the same shape was found while planning and is closed here with the other
three:** `DRIFT_CMD="set -o pipefail; cd $DRIFT_REPO_ROOT …`. It is not in the review. Fixing three
of four instances of a class inside one file, in the round whose whole subject is siblings that
drifted apart, would be GC-08 committed deliberately.

GC-14: `CONSUMERS_OVERRIDDEN` is consulted only inside the `CONSUMERS_RC -eq 0` arm. On the new
exit-3 arm an overridden run prints `⚠️ CONF-04 MEASURED AND OPEN — artist rows at baseline, not at
target` with no hint that the audit which answered was not the deployed one. The additive contract
still holds — that arm sets `EXIT_CODE=1` unconditionally — so this is a **diagnosis** defect, not
a verdict defect: the operator is told the estate is off target when what was actually measured is
an arbitrary file. Exactly the shape of WR-10 and WR-03, which this same wave fixed elsewhere.

GC-09: the failing-block tail enumerates the blocks that can carry a ❌/⚠️, and the file restates
**four times** in the comment above it that the tail must be updated *"in the SAME COMMIT"* as any
site that can newly reach it, because *"a tail that lists every block except the failing one sends
the reader to the green ones."* Plan 06-16 gave the D-04 block two new fatal conditions and made
the D-03 CLI render's `exit 3` reachable; plan 06-17 added the consumers exit-3 arm; plan 06-23
adds a third D-04 fatal condition. The tail was not touched, and **the D-03 and D-04 blocks are not
named in it at all** — verified during planning: the tail's text contains no `D-04`.

Output: four quoted interpolations, an override-visible consumers diagnosis on every arm, and a
tail that names every block that can fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-23-SUMMARY.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quote every overridable path that reaches a remote command string — all four of them</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt</files>
  <read_first>
    - The four sites, located by anchor rather than by line number:
      `grep -n 'cd \$DRIFT_REPO_ROOT' scripts/quick-health-check.sh`,
      `grep -n 'cd \$D03_REPO_ROOT' scripts/quick-health-check.sh`,
      `grep -n 'cd \$D04_REPO_ROOT' scripts/quick-health-check.sh`,
      `grep -n 'bash \$CONSUMERS_SCRIPT' scripts/quick-health-check.sh`.
      Read the whole remote command string each one sits in, and the `RC=$?` line directly beneath
      the `ssh` that sends it — that "no pipe before capturing the status" discipline must survive.
    - Where each knob is defaulted and where its additive override contract is enforced:
      `grep -n 'DRIFT_REPO_ROOT=\|D03_REPO_ROOT=\|D04_REPO_ROOT=\|CONSUMERS_SCRIPT=' scripts/quick-health-check.sh`
      and the `*_OVERRIDDEN=1` blocks near each. The contract — any non-default value forces
      `EXIT_CODE=1` — is what bounds this finding and **must not be weakened**.
    - The file's own statement of the WR-08 rule as it applies here. `grep -n 'remote command string' scripts/quick-health-check.sh`
      and the comment block it lands in.
    - `scripts/phase06-oracle.sh` for the precedent: `grep -n 'remote_sh_c()' scripts/phase06-oracle.sh`
      and its comment, plus GC-11 in `06-REVIEW-GAP.md`, which records that `printf '%q'` renders
      for **bash** and therefore carries a dependency on the remote login shell. That dependency
      applies here too and must be written down rather than inherited silently.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md` — GC-17 in the
      cross-family adjudication section, **including the orchestrator correction about its line
      numbers**, and the separate note that GC-15 is a different finding in a different file.
  </read_first>
  <action>
**(a) Render each knob once, quoted, and interpolate the rendered form.** Beside each knob's
defaulting / override-contract block, build a quoted copy —
`D03_REPO_ROOT_Q="$(printf '%q' "$D03_REPO_ROOT")"` and the same for the other three — and
interpolate the `_Q` form into the remote command string. Do **not** hand-escape quotes inside the
command string: a `\"` wrapper survives a space but not a quote or a `$`, and this file has already
been bitten once by a half-measure that looked like a fix. Do not change the shape of the `ssh`
call, the `RC=$?` line beneath it, or anything about the command's semantics.

**(b) Do not touch `phase06-oracle.sh`.** GC-15 is two `dex_cmd sha256sum` sites in that file and
belongs to plan 06-27, which runs in this same wave. The adjudication's summary line crosses the
two labels; the corrected reading is in this plan's objective. If you find yourself opening the
oracle, stop — you are following the wrong citation.

**(c) Record the dependency, once.** `printf '%q'` renders for bash. For a single-line path with a
space the rendering is a backslash escape that any POSIX shell accepts, but for a value containing
a newline it produces bash ANSI-C `$'…'` quoting, which requires root's login shell on
`172.16.1.159` to be bash. It is; and a non-bash shell fails **loudly** with a syntax error and a
non-zero status, not quietly with a mangled command. One sentence beside the first `_Q`
assignment, cross-referenced from the other three. Name GC-17 and GC-11.

**(d) Drive it.** For each of the four knobs, run `quick-health-check.sh` with the knob set to a
value containing a **space** — a real path created in the scratch directory, so the branch has
something to find — and record, before and after the fix:
  - **before:** the remote shell reports a word-splitting error (or a wrong-path error naming only
    the first word), and the block reports a could-not-look for the wrong reason;
  - **after:** the override warning prints, the block drives its intended branch, and `EXIT_CODE`
    is 1 because of the **override contract** and not because of a shell error.
If LXC 100 is unreachable, drive the **construction** instead: capture the exact command string
each site would send (echo it rather than run it) before and after, and show the space surviving as
one word. Record which route was taken. Either way the additive contract must be re-confirmed: an
overridden run still cannot be green.

**Do not `git push` and do not ask the host to `git pull`.** If a host-side checkout is needed for
the drive, create a **scratch checkout** under a temporary path on LXC 100 and point the knob at
it. Two round-1 plans instructed a push-and-pull and two executors correctly refused; do not
reintroduce it.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
# Baselines measured against the pre-fix file while this plan was written: each of these is 1 today.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'cd $DRIFT_REPO_ROOT')" -eq 0
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'cd $D03_REPO_ROOT')" -eq 0
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'cd $D04_REPO_ROOT')" -eq 0
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'bash $CONSUMERS_SCRIPT')" -eq 0
# The four quoted renderings exist.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF "printf '%q'")" -ge 4
# The additive override contract is intact at all four knobs.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -c 'OVERRIDDEN=1')" -ge 4
# The oracle was NOT touched by this plan.
git diff --name-only -- scripts/ > /tmp/06-26-changed.txt
test "$(grep -cF 'scripts/phase06-oracle.sh' /tmp/06-26-changed.txt || true)" -eq 0
# The artifact records the drive and the citation correction.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt
test -s "$ART"
test "$(grep -ci 'GC-17' "$ART")" -ge 1
bash -n scripts/quick-health-check.sh
    </automated>
  </verify>
  <done>
None of the four overridable paths is interpolated raw into a remote command string; each is
rendered with `printf '%q'` once and the rendered form is what crosses the boundary. The bash
dependency is stated once and cross-referenced. The drive shows a space-bearing override driving
its intended branch rather than producing a shell error, and shows that such a run still cannot be
green. `scripts/phase06-oracle.sh` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the CONSUMERS_SCRIPT override visible on every arm that can report it</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt</files>
  <read_first>
    - `grep -n 'CONSUMERS_OVERRIDDEN' scripts/quick-health-check.sh` — all occurrences. Read the
      whole `CONSUMERS_RC` ladder from the `-eq 0` arm through the `-eq 3` arm to the generic
      `else`, including the `📊 6. Summary` anchor guard that appears in two of them.
    - The `-eq 0` arm's override block, whose wording is the model to follow —
      `grep -n 'CONSUMERS_SCRIPT override in effect' scripts/quick-health-check.sh`. Note what it
      says and why: *"Exit 0 from an overridden audit is evidence about THAT file, not about the
      estate."*
    - `grep -n 'CONF-04 MEASURED AND OPEN' scripts/quick-health-check.sh` — the exit-3 arm's
      diagnosis line, which is the one that currently lies by omission.
    - `06-DISPOSITIONS.md`'s WR-03 row and its bolded instruction to **read the fix correctly** —
      no `warn()` call site was promoted into `FAILURES`, and pending must stay distinct from a
      measured red. **Nothing in this task may collapse that distinction or alter any verdict.**
    - `deferred-items.md` `DEF-06-21-07`, which names the exit-3 arm's heading-anchor guard as
      undriven and gives the exact condition that would drive it. It needs no estate contact —
      `CONSUMERS_SCRIPT` exists precisely so it is driveable from a scratch copy.
  </read_first>
  <action>
**(a) Echo the override on the exit-3 arm and on the generic `else`.** Append a line naming the file
that actually answered — the same sentence the `-eq 0` arm uses, adapted — so a reader of a
non-green transcript can tell "the estate is off target" from "an arbitrary file was measured".
This is a **diagnosis** change only: do not touch `EXIT_CODE`, do not touch the pending counts, do
not touch the `📊 6. Summary` anchor guard, and do not alter one word of the CONF-04 verdict text.

**(b) State in band why the arms differ.** One line: the `-eq 0` arm needs the override notice to
stop a green tick; the `-eq 3` and `else` arms need it to stop a **wrong diagnosis**, which is the
same defect WR-10 and WR-03 fixed elsewhere in this file. Name GC-14.

**(c) Drive both new lines, and drive `DEF-06-21-07` while you are here.** All of it runs from a
scratch copy with no estate contact:
  - point `CONSUMERS_SCRIPT` at a scratch copy of `check-music-consumers.sh` that exits **3** with
    a well-formed `📊 6. Summary` block: the arm must print the CONF-04 diagnosis **and** the
    override line, and `EXIT_CODE` must be 1;
  - point it at a scratch copy that exits some other non-zero status: the generic `else` must print
    the override line;
  - **`DEF-06-21-07`:** point it at a scratch copy whose `📊 6. Summary` heading is renumbered. The
    exit-3 arm must report **UNKNOWN** rather than printing counts scraped from the wrong region.
    Record the result and whether it passes. If it passes, say so and note that `DEF-06-21-07` is
    now driven; if it does **not**, that is a new finding — record it, do not fix it here, and hand
    it to plan 06-29 for disposition.
  Append all of it to `artifacts/06-26-qhc-knobs-and-tail.txt`.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
# Baseline: CONSUMERS_OVERRIDDEN appeared 3 times in executable lines before this task.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'CONSUMERS_OVERRIDDEN')" -ge 5
# The CONF-04 verdict text is untouched — exactly one occurrence, as before.
test "$(grep -cF 'CONF-04 MEASURED AND OPEN' scripts/quick-health-check.sh)" -eq 1
# The anchor guard still exists in both arms that carry it.
test "$(grep -cF '📊 6\. Summary' scripts/quick-health-check.sh)" -ge 2
# The three drives are recorded, including DEF-06-21-07.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt
test "$(grep -ci 'GC-14' "$ART")" -ge 1
test "$(grep -cF 'DEF-06-21-07' "$ART")" -ge 1
bash -n scripts/quick-health-check.sh
    </automated>
  </verify>
  <done>
The exit-3 arm and the generic `else` both name the file that answered when `CONSUMERS_SCRIPT` is
overridden. No verdict, count, exit code or CONF-04 sentence changed. All three drives are recorded
from scratch copies with no estate contact, and `DEF-06-21-07`'s condition is driven with its
result stated either way.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make the failing-block tail name every block that can fail</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt</files>
  <read_first>
    - The tail and the five-paragraph comment stack above it. Extract both with
      `sed -n '/Health check FAILED/,/^fi$/p' scripts/quick-health-check.sh` (the start pattern is
      unique — verified — and the `^fi$` end delimiter exists), and read the comment paragraphs
      immediately above it, each of which restates the same-commit discipline.
    - The blocks the tail does **not** currently name. Enumerate them by finding every site that can
      set `EXIT_CODE=1`: `grep -n 'EXIT_CODE=1' scripts/quick-health-check.sh`, then attribute each
      to its block by reading upward to the nearest block heading. Do **not** work from the review's
      list alone — it names the D-03 and D-04 blocks, but the enumeration is what makes the tail
      correct, and a tail correct about two blocks and wrong about a third is the same defect.
    - `grep -n 'CONF-04 pending\|until E6\|entry criterion E6' scripts/quick-health-check.sh` — the
      twelfth notice's statement that a ⚠️ on the consumers block is **expected** until Phase 7
      entry criterion E6 discharges CONF-04, and is not to be tuned out.
    - Plan 06-23's summary and its new exit-code notice, if it has landed — this plan depends on
      06-23, so the D-04 block may have gained a third fatal condition that the tail must cover.
  </read_first>
  <action>
**(a) Rebuild the tail's list from the enumeration, not from the review.** Name every block that can
carry a ❌ or a ⚠️, including the D-03 vendored-config mount block and the D-04 throwaway-`-l` scan,
and including anything else the `EXIT_CODE=1` enumeration turns up that is currently missing. Keep
the existing wording and the existing image-drift caveat sentence; this is an addition, not a
rewrite.

**(b) Name the expected non-green state.** Add a clause stating that the consumers block carries a
⚠️ by design until ROADMAP entry criterion **E6** discharges CONF-04's Jellyfin half, so a reader
who finds the run non-zero on that block knows it is the recorded state rather than a new fault.
Do not phrase it as a reason to ignore the block, and do not state or imply that CONF-04 is closed
— it is not, and E6 owns it.

**(c) Extend the comment stack rather than adding a numbered notice.** The five paragraphs above the
tail already record every previous extension with its date, its plan and the same-commit reason;
this is the sixth of exactly that kind. A new `EXIT-CODE BEHAVIOUR CHANGED` notice is **not**
appropriate here: no new fatal condition is created by this task, and plan 06-23 adds the notice
for the condition that is. Say in the new paragraph that the tail had fallen behind three plans
(06-16, 06-17, 06-23) despite four in-band restatements of the discipline, and that this is why the
discipline keeps being restated. Name GC-09.

**(d) Prove it mechanically, not by eye.** Record in the artifact the `EXIT_CODE=1` site list with
each site's owning block, and a check that every block name in that list appears in the tail text.
Record the tail before and after.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
# Extract the tail with the bounded range verified during planning (unique start, existing end).
sed -n '/Health check FAILED/,/^fi$/p' scripts/quick-health-check.sh > /tmp/06-26-tail.txt
test -s /tmp/06-26-tail.txt
# Baseline: the tail contained zero mentions of D-04 and zero of D-03 before this task.
test "$(grep -c 'D-04' /tmp/06-26-tail.txt)" -ge 1
test "$(grep -c 'D-03' /tmp/06-26-tail.txt)" -ge 1
# The expected-non-green state is named, and CONF-04 is not claimed closed.
test "$(grep -c 'E6' /tmp/06-26-tail.txt)" -ge 1
test "$(grep -ci 'CONF-04 is closed' /tmp/06-26-tail.txt)" -eq 0
# The image-drift caveat survives.
test "$(grep -c 'image-drift block CANNOT fail' /tmp/06-26-tail.txt)" -eq 1
# No new numbered notice was added by THIS task (06-23 owns the only new one this round).
test "$(grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh)" -eq 13
# The mechanical proof is recorded.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-26-qhc-knobs-and-tail.txt
test "$(grep -ci 'GC-09' "$ART")" -ge 1
test "$(grep -cF 'EXIT_CODE=1' "$ART")" -ge 1
bash -n scripts/quick-health-check.sh
    </automated>
  </verify>
  <done>
The failing-block tail names every block that can set `EXIT_CODE=1`, derived from an enumeration
recorded in the artifact rather than from the review's two examples, and names the consumers
block's expected ⚠️ with its E6 clearing condition without claiming CONF-04 is closed. The comment
stack above it carries a sixth paragraph recording that the tail fell behind three plans. No new
numbered exit-code notice was added by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (four `*_REPO_ROOT` / `CONSUMERS_SCRIPT` knobs) → remote command strings over ssh | a value with a space, a quote or a `$` is re-parsed by the remote shell |
| `check-music-consumers.sh` on LXC 100 → this file's verdict | an overridden path means the answer describes a file, not the estate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-GC17 | Tampering (remote command re-parsing) | four unquoted interpolations | mitigate | each knob rendered once with `printf '%q'`; the rendered form is what crosses the boundary; the bash dependency stated in band |
| T-06-GC14 | Repudiation (a diagnosis that misattributes the measurement) | the consumers exit-3 and `else` arms | mitigate | the override is echoed on every arm; verdicts untouched |
| T-06-GC09 | Repudiation (a tail sending the reader to the green blocks) | the failing-block tail | mitigate | list rebuilt from an `EXIT_CODE=1` enumeration and checked mechanically |
| T-06-26-DEPLOY | Elevation of privilege | deploying unmerged Phase 6 to satisfy a drive | mitigate | no push, no host pull; a host-side scratch checkout is the only permitted live route |
| T-06-26-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- `bash -n scripts/quick-health-check.sh` parses clean.
- `git diff --name-only -- scripts/` lists only `scripts/quick-health-check.sh`.
- The exit-code notice header count is 13 — the one 06-23 added, and no more.
- No `git push`; the host was not asked to pull; no `--auto` or `--chain` invocation anywhere.
</verification>

<success_criteria>
- GC-17 closed at **four** sites, including the one found while planning; the additive override
  contract is intact at all four; a space-bearing override drives its branch rather than erroring.
- GC-14 closed: the override is named on the exit-3 arm and the generic `else`; no verdict changed.
- GC-09 closed: the tail names every block that can set `EXIT_CODE=1`, proven by enumeration, and
  names the consumers block's expected ⚠️ with E6 as its clearing condition.
- `DEF-06-21-07` is driven, with its result recorded either way.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-26-SUMMARY.md` when done.
Include a NOT-DRIVEN register. If the `DEF-06-21-07` drive fails, record it as a NEW finding for
plan 06-29 to disposition rather than fixing it here.
</output>
