---
phase: 06-tagger-configuration-and-dry-run
plan: 10
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - scripts/check-music-freeze.sh
  - scripts/quick-health-check.sh
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
must_haves:
  truths:
    - "The tagger census expects exactly TWO definitions and asserts them BY NAME with their class, each on its own line — a bare count cannot read as a pass and a third unnamed definition must still fail (D-11)"
    - "The census pattern is NOT narrowed to make the count fit; it already matches `beets-flask` on purpose, so a new definition goes red rather than arriving unnoticed"
    - "The pattern and `beets.md:1255`'s verbatim quote of it change in the SAME commit, because the two must not drift"
    - "The summary label tokens `quick-health-check.sh` greps for are unchanged, because renaming one breaks a consumer in a different file and breaks it SILENTLY"
    - "The vendored-file drift block covers the new flask config, and the D-03 half — that BOTH containers mount the ONE vendored beets config — is asserted from `docker inspect .Mounts`, which is the right instrument for a mount and a hash is not"
    - "Every CLI-arm invocation is asserted to pass a throwaway `-l` AND a `-c` overlay naming `library`, `statefile` and `directory`, because `-l` alone does not redirect `statefile:` (D-04)"
    - "Every new override can only make a block redder; the drift block's hard-coded comparison count moves from 3 to 4 deliberately, with its label set and its green line updated in the same edit"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "the revised two-definition, named-and-classed tagger census"
      contains: "flask.yaml"
    - path: "scripts/quick-health-check.sh"
      provides: "the widened vendored-file drift block and the D-03/D-04 assertions"
  key_links:
    - from: "scripts/check-music-freeze.sh"
      to: "stacks/selfhosted/arrs/beets.md:1255"
      via: "the census grep pattern quoted verbatim in both files"
      pattern: "beets-flask|beets|wrtag|soulbeet|picard"
---

<objective>
Bring the standing health harness back into agreement with reality, in the same commit that the reality changed.

Purpose: plan 06-04 added a second tagger definition, so `check-music-freeze.sh`'s `tagger definitions: 1` expectation is now red — correctly, by design, since `beets-flask` was put into that pattern on purpose in Phase 4 so a Phase 5/6 definition would go red rather than arrive unnoticed. D-11 revises the expectation to two, **named and classed**. D-03 adds a second consumer behind one vendored file, which is a `docker inspect .Mounts` question and not a fourth hash. D-04's throwaway-`-l` discipline becomes an assertion rather than a habit.

Output: three files changed together, with both cross-file contracts honoured in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-PATTERNS.md
@scripts/check-music-freeze.sh
@scripts/quick-health-check.sh
@stacks/selfhosted/arrs/beets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11 — the two-definition census, named and classed, with beets.md kept in step</name>
  <files>scripts/check-music-freeze.sh, stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - scripts/check-music-freeze.sh lines 755-802 — the tagger census in full: the three constraints stated in the file at 760-785 (the pattern already matches `beets-flask` ON PURPOSE and must not be narrowed; `beets-flask` is listed before `beets` deliberately; the pattern is quoted verbatim in `beets.md:1255` and the two must not drift), and the `GIT_LS`/`DEF_FILES`/`TAGGER_DEFS` block at 786-802 that this task revises.
    - scripts/check-music-freeze.sh lines 1035-1052 — the CROSS-FILE CONTRACT comment naming the six label tokens `quick-health-check.sh` greps for (`tagger definitions`, `beets databases`, `tagger databases`, `retired paths present`, `rw on Music`, `tagger-capable`), and the "name the exception on its own line" summary shape at 1049-1052 that D-11 asks for.
    - scripts/check-music-freeze.sh lines 740-748 and 1042-1043 — the UNKNOWN sentinels and the reason: "0" on a census that could not look is the exact false green this file exists to refuse.
    - scripts/check-music-freeze.sh lines 72-89 — the override contract and why `SURVIVOR_DB`/`APPDATA_ROOT` are plain constants.
    - stacks/selfhosted/arrs/beets.md line 1255 — the verbatim quote of the census pattern, and line 1327 — where D-11's revision was pre-flagged.
    - stacks/selfhosted/arrs/beets.md lines 1252-1272 — the criteria table with its evidence column (evidence is a RUNNABLE command, quoted verbatim so it cannot drift from the script) and the dated block-quote convention for a superseded disposition.
  </read_first>
  <action>
Revise the tagger census in `scripts/check-music-freeze.sh` and update its verbatim quote in `stacks/selfhosted/arrs/beets.md` — **in the same commit**, because the two must not drift.

**Do not narrow the pattern.** It already matches `beets-flask`, deliberately, and `beets-flask` is listed before `beets` in the alternation for a reason. Narrowing it to make the count fit would defeat the mechanism that just worked.

**Expect exactly two, and assert them by name and class.** The two are:
- `stacks/selfhosted/arrs/beets/flask.yaml` — class: **active front end**, `metasauce/beets-flask:v2.0.0-rc6`, beets 2.12.0.
- `stacks/selfhosted/arrs/beets/beets.yaml` — class: **dormant agent-driven CLI arm**, `lscr.io/linuxserver/beets:2.13.1-ls349`.

Print each on its own line with its class, in the "name the exception on its own line, never fold it into a count" shape the file already uses at 1049-1052 for the `rw on Music` rows. The pass condition is: exactly two files matched, and the matched set equals that named pair. A count of two whose members are different files is a **red**, not a pass — that is the whole point of naming them. A third unnamed definition must still fail.

**Keep the label token `tagger definitions` unchanged.** `quick-health-check.sh:1035-1040` greps for it and five siblings; renaming one breaks a consumer in a different file and breaks it silently. Add the class lines beneath the labelled summary line rather than restructuring it.

**Keep the UNKNOWN sentinel.** `TAGGER_DEFS="UNKNOWN"` before anything runs; a `git ls-files` that exits non-zero or lists nothing is `UNKNOWN`, explicitly "NOT 'two definitions'".

**Update `stacks/selfhosted/arrs/beets.md` in the same commit**, in three places:
1. Line 1255's verbatim pattern quote — re-quote it byte-identically to whatever the script now holds.
2. The criteria table's evidence cell for that row — the evidence must stay a runnable command that produces the new named-and-classed output.
3. Line 1327's `### Still open` pre-flag of D-11 — discharge it with a dated note, keeping the pre-flag legible rather than deleting it, per this file's convention for a superseded disposition.
Add the Phase 6 census result into the `## Phase 6 — tagger configuration and dry run (2026-09-20)` section plan 06-03 opened.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/check-music-freeze.sh; bash -n "$f" || exit 1; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s" "$s" | grep -q "flask.yaml" || { echo "flask.yaml not named in the census"; exit 1; }; printf "%s" "$s" | grep -q "beets/beets.yaml" || exit 1; printf "%s" "$s" | grep -q "beets-flask|beets|wrtag|soulbeet|picard" || { echo "the census pattern was narrowed or altered"; exit 1; }; printf "%s" "$s" | grep -q "tagger definitions" || { echo "the cross-file label token was renamed"; exit 1; }; printf "%s" "$s" | grep -q "TAGGER_DEFS=\"UNKNOWN\"" || { echo "the UNKNOWN sentinel was dropped"; exit 1; }; if printf "%s" "$s" | grep -qE "expected=1|== \"1\""; then echo "the old expected=1 expectation survives"; exit 1; fi; grep -q "beets-flask|beets|wrtag|soulbeet|picard" stacks/selfhosted/arrs/beets.md || { echo "beets.md:1255 quote did not move with the pattern"; exit 1; }; for tok in "tagger definitions" "beets databases" "tagger databases" "retired paths present" "rw on Music" "tagger-capable"; do printf "%s" "$s" | grep -q "$tok" || { echo "cross-file label token lost: $tok"; exit 1; }; done; bash "$f" --baseline >/dev/null || { echo "--baseline run failed"; exit 1; }; echo "D-11 census revision OK"'</automated>
  </verify>
  <done>
    `scripts/check-music-freeze.sh` expects exactly two definitions, names both files with their class on their own lines, keeps the pattern unnarrowed and all six cross-file label tokens intact, and retains the `UNKNOWN` sentinel. `beets.md` quotes the same pattern byte-identically, its evidence cell holds a runnable command producing the new output, and line 1327's pre-flag is discharged with a dated note rather than deleted. A live run of `check-music-freeze.sh` exits 0 with `tagger definitions: expected=2` green — capture that output in the summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-03 — widen the vendored-file drift block and assert both containers mount the one config</name>
  <files>scripts/quick-health-check.sh</files>
  <read_first>
    - scripts/quick-health-check.sh lines 1046-1159 — the vendored-file drift block IN FULL: the three-pair scope comment, the `_drift_pair` remote function, the hard-coded `DRIFT_LINES -ne 3` check at 1122-1126 ("A short answer is 'could not look', NOT 'the files that did report are fine'"), the closed label `case` at 1133-1143, and the green line at 1155 that names all three files.
    - scripts/quick-health-check.sh lines 572-575 — the `DRIFT_EXPECT_*` variables, and 563-575 for the override contract ("every one of them can only make the block REDDER; there is deliberately no success-producing override").
    - scripts/quick-health-check.sh lines 1161-1260 — the `extended.conf` block that follows, and its own override contract at 577-589 — the template for any new knob. Read 1183's argument against reflexively adding a fourth `_drift_pair` before choosing.
    - scripts/quick-health-check.sh lines 306-310 and 494-497 and 840-884 — the `grep -q`-as-last-stage counter-example, the greppable `timeout … | … pipefail` invariant, and the correct remote-bounding form.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-PATTERNS.md lines 688-752 — the three load-bearing facts about the block's scope that must not be broken, and the ⚠ that D-03's real shape is a `docker inspect .Mounts` assertion rather than a hash.
  </read_first>
  <action>
Two edits to `scripts/quick-health-check.sh`, kept separate because they are different claims measured by different instruments.

**Edit 1 — a FOURTH drift pair, for the new vendored flask config.** `stacks/selfhosted/arrs/beets/flask-config.yaml` is a genuinely new vendored file that a container reads at runtime from appdata, so it earns a pair. This is not the case `:1183` argues against — that argument is about using a hash to answer a mount question, which is edit 2's job. Add:
`_drift_pair flask-config.yaml stacks/selfhosted/arrs/beets/flask-config.yaml "$DRIFT_APPDATA_ROOT/arrs/beets/config/beets-flask/config.yaml"`
and update all four load-bearing pieces in the same edit, or the block breaks silently:
1. The three-pair scope comment at the top of the block becomes a four-pair comment, listing the new repo→appdata mapping alongside the existing three.
2. The hard-coded `DRIFT_LINES -ne 3` becomes `-ne 4`, keeping its message verbatim — a short answer is *could not look*, not "the files that did report are fine".
3. The label `case` gains an arm for `flask-config.yaml`. The label set is CLOSED: an unrecognised label is an UNKNOWN, not a skip.
4. A `DRIFT_EXPECT_FLASK_CONFIG` variable beside the existing ones at `:572-575`, ADDITIVE in the same sense — when set, the host hash must equal the repo hash AND the override, so setting it can only turn a green file red.
5. The green line at `:1155` names all **four** files.

**Edit 2 — the D-03 assertion, which is a mount question.** D-03's claim is that ONE vendored beets config is mounted into BOTH containers. The repo→appdata hash comparison is unchanged by that; what is new is the mount. Add a block that asserts, from `docker inspect .Mounts`:
- `beets-flask` has a mount whose `Destination` is `/config/config.yaml`, whose `Source` is `/mnt/fast/appdata/arrs/beets/config/config.yaml`, and whose `RW` is `false`.
- The dormant `beets` definition — rendered with `docker compose -f stacks/selfhosted/arrs/beets/beets.yaml config`, since the container is not running — declares the identical source path for the same destination, also `:ro`.
- Both declare `/mnt/tank/media` at `:ro`. That is D-05 asserted from the runtime rather than read from the file, which is the WR-02 lesson.
Fail closed on every branch: an empty `docker inspect` result is `UNKNOWN`, not "no mounts" — `check-music-freeze.sh`'s row-22 control already proved that an empty `docker inspect` must not satisfy a zero-test.

**Edit 3 — D-04's throwaway-`-l` assertion**, landing between the drift block and the `extended.conf` block, as the pattern map places it. Assert, over `git ls-files` output, that no executable line anywhere in `scripts/`, `stacks/` or the vendored configs invokes `beet` against the real library: every `beet` invocation intended for the CLI arm must carry BOTH a `-l` pointing outside `/config/library.db` AND a `-c` overlay. State the reason in the block: `-l` alone does **not** redirect `statefile:`, so a throwaway import on a throwaway `-l` still writes the shared `state.pickle`. Strip comments before counting — this repo has been bitten four times by a grep satisfied by prose *about* the thing rather than by the thing — and prove the strip is doing work by recording both the raw and the stripped counts.

**After editing, run the file's own greppable invariant** and record its output: `grep -n 'timeout $REMOTE_TIMEOUT.*|' scripts/quick-health-check.sh` must return only lines whose command string also contains `pipefail`, or whose final stage is a `grep -q`. The new remote calls must satisfy it.

Update the `EXIT-CODE BEHAVIOUR CHANGED` notice convention: if the new blocks add conditions that can exit the script 1, they must be named in a notice carrying the shared phrase, as the existing three do.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/quick-health-check.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S error "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s" "$s" | grep -q "flask-config.yaml" || { echo "the fourth drift pair is missing"; exit 1; }; printf "%s" "$s" | grep -q "DRIFT_LINES.*-ne 4" || { echo "the hard-coded comparison count was not moved to 4"; exit 1; }; printf "%s" "$s" | grep -q "DRIFT_EXPECT_FLASK" || { echo "the additive expectation variable is missing"; exit 1; }; printf "%s" "$s" | grep -q "\.Mounts" || { echo "the D-03 mount assertion is missing"; exit 1; }; printf "%s" "$s" | grep -q "statefile" || { echo "D-04 assertion does not mention statefile — -l alone does not redirect it"; exit 1; }; printf "%s" "$s" | grep -qE "DRIFT_LINES.*-ne 3" && { echo "the old count of 3 survives somewhere"; exit 1; }; grep -c "vendored files match (4)" "$f" >/dev/null || { echo "the green line does not name four files"; exit 1; }; bad=$(grep -n "timeout \$REMOTE_TIMEOUT.*|" "$f" | grep -v "pipefail" | grep -v "grep -q" || true); [ -z "$bad" ] || { echo "remote pipelines missing pipefail:"; echo "$bad"; exit 1; }; echo "D-03/D-04 harness edits OK"'</automated>
  </verify>
  <done>
    The drift block covers four pairs with the scope comment, the hard-coded count, the closed label `case`, the `DRIFT_EXPECT_FLASK_CONFIG` variable and the green line all moved together. The D-03 mount assertion reads `.Mounts` for both containers and fails closed on an empty inspect. The D-04 assertion names `statefile` and records both the raw and comment-stripped counts so the strip is proven to be doing work. The file's own greppable `timeout … | … pipefail` invariant returns clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the full harness and record what it says now</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-10-harness-run.txt</files>
  <read_first>
    - scripts/check-music-freeze.sh and scripts/quick-health-check.sh — as tasks 1 and 2 left them.
    - .planning/STATE.md § Phase 5 close — the standing known red: `quick-health-check.sh` exits 1 on the pre-existing `interpolated-host-path inventory MOVED: expected=12, found=13` gate, the only red line in that run, which makes the script's exit code non-discriminating. Read the block's own verdict line, not the script's exit code.
    - README § Health Checks — the three rules: fail closed with "could not look" distinct from "nothing is wrong"; bound remote commands Linux-side; assert rather than report.
  </read_first>
  <action>
Run `scripts/check-music-freeze.sh` and `scripts/quick-health-check.sh` from the workstation against the live estate and capture both transcripts in full to `artifacts/06-10-harness-run.txt`.

**Read each block's own verdict line, not the top-level exit code.** `quick-health-check.sh` is known to exit 1 on the pre-existing `interpolated-host-path inventory MOVED: expected=12, found=13` gate — a Phase 5 open item, not this phase's. Its exit code is therefore non-discriminating and the artifact must say so explicitly, naming which reds are pre-existing and which (if any) are new.

**Prove the new assertions are discriminating, not vacuous.** For each of the three new blocks, drive its red branch once with an additive override or a controlled perturbation that cannot manufacture success, record the red, restore, and re-run green:
- The drift block: set `DRIFT_EXPECT_FLASK_CONFIG` to a wrong-but-plausible hash; expect exactly one red naming `flask-config.yaml` and the other three pairs still green. Unset and re-run.
- The D-03 mount assertion: point the assertion's expected source at a path that is not mounted; expect a red that names the mount, not a silent pass. Restore.
- The tagger census: this one cannot be driven by an override (`SURVIVOR_DB`/`APPDATA_ROOT` are deliberately plain constants), so drive it by temporarily renaming nothing and instead asserting the census output names both files and their classes — and record that this half is discharged **by construction, not by a firing observation**, in the same register `beets.md` uses for the Renovate rule that has never been observed refusing anything.

Record a pre-test sha256 of every script touched, restore afterwards, assert the hash matches, and confirm `git status --porcelain` is clean of test debris. That is the standing discipline in this repo: every test that breaks something records a hash, restores, asserts, and re-runs green.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-10-harness-run.txt; test -s "$f" || exit 1; grep -q "tagger definitions" "$f" || exit 1; grep -qE "expected=2" "$f" || { echo "the two-definition census result is not recorded"; exit 1; }; grep -q "vendored files match (4)" "$f" || grep -q "flask-config.yaml" "$f" || { echo "the four-pair drift result is not recorded"; exit 1; }; grep -qi "interpolated-host-path" "$f" || { echo "the pre-existing red is not named, so the exit code reads as discriminating when it is not"; exit 1; }; grep -qiE "restored|sha256 identical|hash matches" "$f" || { echo "the perturbation controls were not restored-and-asserted"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/|^ M stacks/" || true); [ -z "$d" ] || { echo "test debris in the working tree:"; echo "$d"; exit 1; }; echo "harness run recorded"'</automated>
  </verify>
  <done>
    The artifact holds both transcripts in full, names which reds are pre-existing (the `interpolated-host-path` gate) and which are new, and records three perturbation controls — two driven to red and back to green with pre/post sha256, and the census half explicitly recorded as discharged by construction rather than by a firing observation. `git status --porcelain` carries no test debris.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo copy → appdata copy → container runtime | The appdata copy is authoritative at runtime. Drift between them is invisible to git and to `docker ps`. |
| `check-music-freeze.sh` summary labels → `quick-health-check.sh` grep selectors | A cross-file contract enforced only by a comment. Renaming a label breaks a consumer in a different file, silently. |
| workstation (macOS) → LXC 100 | macOS has no GNU `timeout`; `timeout N cmd \| wc -l` exits 0 silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-48 | Tampering | The census pattern narrowed to make the count fit, disarming the mechanism that just caught a new definition | mitigate | The verify command asserts the pattern alternation is byte-identical and that `flask.yaml` and `beets/beets.yaml` are both named in executable lines. |
| T-06-49 | Repudiation | A renamed summary label silently breaking the `quick-health-check.sh` fold-in | mitigate | All six contract tokens asserted present by the verify command; both files changed in one commit. |
| T-06-50 | Tampering | A fourth drift pair added without moving the hard-coded count, the label `case`, the expectation variable and the green line, so the block reports three of four and reads green | mitigate | The verify command asserts the count is 4, the old 3 is gone, the label arm and the `DRIFT_EXPECT_FLASK_CONFIG` variable exist, and the green line names four. |
| T-06-51 | Spoofing | An empty `docker inspect` satisfying the D-03 mount assertion as "no bad mounts" | mitigate | Fail-closed branch: an empty inspect result is `UNKNOWN`, never a pass — the row-22 control already proved this shape on this estate. |
| T-06-52 | Tampering | A `beet` invocation added later that opens the real `library.db`, migrating a 2.12.0 schema under flask's feet | mitigate | The D-04 assertion over comment-stripped executable lines in `scripts/`, `stacks/` and the vendored configs, requiring both `-l` and a `-c` overlay naming `statefile`. |
| T-06-53 | Spoofing | A grep satisfied by prose ABOUT the prohibition rather than by the prohibition | mitigate | Comment-stripped counting with BOTH the raw and stripped counts recorded, so the strip is demonstrably doing work — the device this repo has needed four separate times. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. `shellcheck` is used only if already present. |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S error` pass on both scripts.
- `scripts/check-music-freeze.sh` exits 0 with `tagger definitions: expected=2` green and both files named with their class.
- `grep -n 'timeout $REMOTE_TIMEOUT.*|' scripts/quick-health-check.sh` returns only `pipefail`-carrying or `grep -q`-terminated lines.
- `git diff --stat` shows exactly the three source files plus one artifact.
</verification>

<success_criteria>
- D-11 is discharged: two definitions, named and classed, pattern unnarrowed, `beets.md` in step in the same commit.
- D-03's mount half is asserted from `docker inspect .Mounts` for both containers, and the drift block covers all four vendored files with every load-bearing piece moved together.
- D-04's throwaway-`-l` + `-c` discipline is an assertion over comment-stripped executable lines, with the strip proven to be doing work.
- The harness run names its pre-existing reds so its exit code is not misread as discriminating.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-10-SUMMARY.md` when done.
</output>
