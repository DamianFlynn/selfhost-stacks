---
phase: 06-tagger-configuration-and-dry-run
plan: 19
type: execute
wave: 7
depends_on: ["06-18"]
gap_closure: true
files_modified:
  - scripts/phase06-oracle.sh
autonomous: true
requirements: [CONF-03, CONF-06]
closes_findings: [WR-01, WR-02, WR-06, IN-09, IN-12, IN-08-oracle-half]

must_haves:
  truths:
    - "No assertion in the oracle can print a green tick over a could-not-look: an unreadable fields TSV, an empty fields TSV and an unreadable container scratch directory all route to UNKNOWN, never to `ok`"
    - "`manifest_compare` refuses an EMPTY manifest with the same in-band reason its sibling script already gives — two empty listings can no longer compare equal and satisfy a layer of the wrote-nothing proof by having measured nothing"
    - "`assert_dj_count` refuses a sample in which the DJ stratum was never exercised, the way `assert_no_compilations` already refuses an unexercised compilation stratum — a zero-versus-zero comparison is VACUOUS, not a pass"
    - "The RED-versus-UNKNOWN precedence convention is stated in the file's EXIT CODES block, so a reader can infer it rather than having to compare two sibling scripts"
    - "Every changed branch is DRIVEN by `--self-test`, locally, with no ssh and no docker — and the cases that cannot be driven that way are named as undriven with their reason"
  artifacts:
    - path: "scripts/phase06-oracle.sh"
      provides: "the fail-closed assertion set with vacuity guards on every stratum"
      contains: "VACUOUS"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-19-oracle-vacuity-driven.txt"
      provides: "the self-test transcript with each new refusal driven"
  key_links:
    - from: "scripts/phase06-oracle.sh manifest_compare"
      to: "scripts/phase06-incremental-control.sh:186"
      via: "the empty-manifest guard copied verbatim from the sibling written in the same phase"
      pattern: "is EMPTY"
---

<objective>
Close the oracle's own instances of this phase's dominant defect class — assertions that cannot fail — now that plan 06-16 has closed the flagship instance in the health entry point.

Purpose: `06-REVIEW.md` found four of them in `phase06-oracle.sh`, and what makes them provable rather than stylistic is that the sibling script written in the same phase already guards against three of them and says why in band. `manifest_compare` has no empty-file guard while `phase06-incremental-control.sh:186` does, and states the reason verbatim: *"an empty listing compares equal to any other empty listing, which would pass vacuously."* `assert_dj_count` has no vacuity guard while its immediate neighbour `assert_no_compilations` refuses an unexercised stratum outright. `report_multi_artist` returns 0 on an unreadable input, so `run_assert` prints `✓ CONF-04 write side: the fields TSV is missing or unreadable, so the write side could not be shown` — a green tick whose own text says the instrument did not look, which is exactly the shape `CLAUDE.md` § Health Checks exists to outlaw. And an empty `fields.tsv` yields `awk … NF != 6` of zero, printing `✓ field view read: 0 items, six columns each`.

Each of these is masked today by an unrelated downstream control, and the review says so honestly. Masking is not guarding: `--baseline` exits before the positive control that masks WR-02, and the other masks are incidental rather than designed. Leaving them while fixing CR-01 would be inconsistent.

Output: one instrument, made unable to tick over a blind read, proved by its own offline self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-18-SUMMARY.md
@scripts/phase06-oracle.sh
</context>

<constraints>
**Do not run `phase06-oracle.sh --run` or `--baseline` against the live estate.** The phase's CONF-03/CONF-06 proof is committed (`artifacts/06-11-oracle-run.txt`, `06-11-wrote-nothing.txt`) and re-driving it would import. Every change here is proven offline by `--self-test`, `bash -n` and `shellcheck`. This is also why the changes are confined to refusal branches: a refusal that fires more often can only make a future run redder, never falsely greener, so the committed artifact's conclusions are unaffected.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Three fail-closed corrections — the empty manifest, the unreadable fields TSV, and the empty field view</name>
  <files>scripts/phase06-oracle.sh</files>
  <read_first>
    - `scripts/phase06-oracle.sh:288-328` — `manifest_compare` in full: the `-e`, `-f` and `-r` validations, the `DIFF_WHY` convention, the return-code vocabulary it uses, and the `diff` itself. Note what is NOT validated.
    - `scripts/phase06-incremental-control.sh:180-195` — **the required shape**, verbatim: `if [ ! -s "$f" ]; then DIFF_WHY="manifest '$f' is EMPTY - an empty listing compares equal to any other empty listing, which would pass vacuously"; return 2; fi`. Copy the guard and its in-band reason rather than paraphrasing; the two scripts should read the same on the same question.
    - `scripts/phase06-oracle.sh:1102-1125` — `capture_manifests`, which builds each manifest with `: > file` followed by appends, and `remote_manifest_meta` (`grep -n remote_manifest_meta scripts/phase06-oracle.sh`), whose `find <dir> -type f -printf …` returns rc 0 and NO OUTPUT on an existing but empty directory. This is why the empty case is reachable with a clean `RSH_RC`.
    - `scripts/phase06-oracle.sh:1905-1915` — the `--baseline` early exit, which is the reason the downstream positive control does not mask WR-02 in that mode.
    - `scripts/phase06-oracle.sh:985-1000` — `report_multi_artist`, specifically the `if [ ! -r "$fields" ]` branch that sets `ASSERT_WHY` and returns **0**.
    - `scripts/phase06-oracle.sh:1015-1035` — `run_assert`, and how it maps a return value to `ok` / `bad` / `unknown`: rc 0 → `ok "$label: $ASSERT_WHY"`, and the `*)` arm → `unknown`, which increments `UNKNOWNS` and forces exit 3.
    - `scripts/phase06-oracle.sh:2100-2115` and `:2140-2150` — the field-view read (`awk -F'\t' 'NF != 6 {n++} END {print n + 0}'` over `fields.tsv`, which yields 0 on an empty file and prints a tick) and the `report_multi_artist` call site. Note that `:2103-2113` already reports the unreadable condition as `unknown`, so the run verdict is right today and only the tick is wrong — the fix must not double-report.
    - `scripts/phase06-oracle.sh:1840-1855` — the dirty-destination precheck, including `:1843`'s single in-container pipeline (`[ ! -e $SCRATCH ] && echo absent || ls -A $SCRATCH | head -n 1`) as plan 06-18 left it (quoted, but still a pipeline), and the file's own house rule at `:185-186`: *"THE CONTAINER'S /bin/sh IS dash AND HAS NO `pipefail`. So no command sent INTO the container carries a pipeline at all."* Confirmed behaviour: `/bin/sh -c 'ls -A /nonexistent | head -n 1; echo rc=$?'` returns rc 0.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:145-201` (WR-01, WR-02), `:284-311` (WR-06), `:536-542` (IN-12) — all four findings with their proposed fixes.
  </read_first>
  <action>
**Edit 1 (WR-02) — the empty-manifest guard.** Add a `-s` test to `manifest_compare` alongside its existing `-e`/`-f`/`-r` validations, returning the same code those return and setting `DIFF_WHY` to the sibling's exact sentence: that the manifest is EMPTY, and that an empty listing compares equal to any other empty listing, which would pass vacuously. Place it so it fires before the `diff`. Add a one-line comment naming `scripts/phase06-incremental-control.sh` as the source, so the two stay recognisably the same guard.

**Edit 2 (WR-06) — stop ticking over an unreadable input.** In `report_multi_artist`, change the `[ ! -r "$fields" ]` branch to return the code `run_assert` maps to `unknown` rather than the code it maps to `ok`. Keep `ASSERT_WHY` text as it is — it is already correct and already says the instrument could not look; the defect is purely that it was routed to the green arm. **Then check the call site at `:2140-2150` against `:2103-2113`**: if the same condition is now reported twice (once by the earlier block, once by `run_assert`), keep both but make the second message say it is a restatement, or suppress the second — decide, and say which in the summary. A doubled UNKNOWN is noise; a missing one is the defect returning.

If the intent turns out to be that a *report* may never block the run, do not preserve that intent by returning 0 on an unreadable file. Keep `return 0` only for the case where the file is READABLE and empty-by-measurement, and make that message say so explicitly.

**Edit 3 (IN-12) — an empty field view is not a pass.** At the field-view read around `:2106-2112`, add a `-s` test on `fields.tsv` before the `awk` column count, and route an empty file to `unknown` rather than letting `NF != 6` yield zero and print `✓ field view read: 0 items, six columns each`. State in the message that zero items means the view was not produced, not that every item had six columns.

**Edit 4 (WR-01) — split the one in-container pipeline.** `:1843` is the single line in the file that breaks the file's own dash/`pipefail` house rule, and it breaks it in the precheck whose job is to refuse. If `$SCRATCH` exists but is unreadable by the container's service account, `ls` fails, `head` exits 0, the pipeline exits 0, `RSH_OUT` is empty, neither the `RSH_RC` guard nor the `[ -n "$RSH_OUT" ]` guard fires, and the script prints that the scratch directory is absent or empty — a could-not-look reported as the green answer.

Restructure it so there is no pipeline in the remote command and the status of the LOOK is read explicitly, the way every other remote read in this file is structured. Use a construction whose own exit status distinguishes the three outcomes the precheck actually needs — absent, present-and-empty, present-and-non-empty — from a fourth, present-but-unreadable, which must route to a refusal and not to "absent or empty". A `find` with `-mindepth 1` whose rc is read, or a two-call split with the listing written to a file and read back in a second bounded call, both satisfy this; pick one and say in a comment why it cannot silently exit 0 on an unreadable directory.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/phase06-oracle.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -q "is EMPTY" || { echo "WR-02: the empty-manifest guard is missing"; exit 1; }; printf "%s\n" "$s" | grep -q "pass vacuously" || { echo "WR-02: the sibling reason text was not carried over"; exit 1; }; awk "/report_multi_artist\(\)/,/^}/" "$f" | grep -q "return 0" && { awk "/report_multi_artist\(\)/,/^}/" "$f" | grep -A2 "! -r" | grep -q "return 0" && { echo "WR-06: the unreadable branch still returns 0"; exit 1; }; }; printf "%s\n" "$s" | grep -qE "\-s \"\\\$OUT/fields.tsv\"|\-s \"\\\$fields\"" || { echo "IN-12: no -s guard on the field view"; exit 1; }; bad=$(printf "%s\n" "$s" | grep -nE "dex_cmd sh -c .*ls -A .*\| *head" || true); [ -z "$bad" ] || { echo "WR-01: the in-container pipeline survives:"; echo "$bad"; exit 1; }; bash "$f" --self-test >/dev/null || { echo "--self-test regressed"; exit 1; }; echo "06-19 task 1 OK"'</automated>
  </verify>
  <done>
    `manifest_compare` refuses an empty manifest with the sibling's verbatim reason and a comment naming its source; `report_multi_artist`'s unreadable branch routes to `unknown` and the double-report question at `:2103-2113` is decided and recorded; the field-view read refuses an empty `fields.tsv` with a message saying zero items means the view was not produced; `:1843` carries no pipeline into the container and distinguishes present-but-unreadable from absent-or-empty, with a comment saying why it cannot silently exit 0. `--self-test` still exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: The DJ-stratum vacuity guard, the stated RED-vs-UNKNOWN convention, and self-test cases for every new refusal</name>
  <files>scripts/phase06-oracle.sh</files>
  <read_first>
    - `scripts/phase06-oracle.sh:730-780` — `assert_no_compilations` (`:732-755`) and `assert_dj_count` (`:761-779`), read side by side. The first refuses to pass when the compilation stratum was never exercised, with the reason "VACUOUS" in band; the second has no equivalent and lets a sample whose S5 rows sum to zero compare `0 -ne 0` and pass.
    - `scripts/phase06-oracle.sh:2120-2135` — the cross-check that only fires when the sample and the fixture DISAGREE, which is why both being zero is silent today.
    - `scripts/phase06-oracle.sh:2160-2175` — the exit-verdict logic: `UNKNOWNS` is checked FIRST, so a blind instrument beats a measured red.
    - `scripts/phase06-incremental-control.sh:715-725` — the sibling's exit-verdict logic, which checks `failed` first, so a measured red beats a blind instrument. **The two scripts written in the same phase disagree** (IN-08). Both are defensible; having both means a reader cannot infer the convention.
    - `scripts/phase06-oracle.sh` — the header's EXIT CODES block (`grep -n "EXIT CODES" scripts/phase06-oracle.sh`), which is where the convention must be stated.
    - `scripts/phase06-oracle.sh` — the `--self-test` harness as plan 06-18 left it (including the apostrophe case it added), so the new cases follow its existing shape and its case-count banner is updated once rather than twice.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` § DEF-06-12-01 — path rule 2 (`albumtype:=dj disctotal:2..`) is UNEXERCISED and UNOWNED, and seven `dj-mixes` folders carry the shape. **The IN-09 guard this task adds is what would have made that deferral visible as a refusal rather than as a silence.** Say so in the guard's message.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:506-514` (IN-09) and `:498-504` (IN-08).
  </read_first>
  <action>
**Edit 1 (IN-09) — the DJ-stratum vacuity guard.** Give `assert_dj_count` the guard its neighbour already has: when the wanted count is zero, refuse rather than compare. The message must use the same word its neighbour uses — VACUOUS — and must say that the sample holds no S5 files so the `albumtype` path rule was never exercised. Return the code that routes to a refusal in this file's own vocabulary, matching what `assert_no_compilations` returns for the equivalent condition; do not invent a new code.

Add one sentence to the guard's message naming `DEF-06-12-01`: path rule 2 is the only `paths:` rule no Phase 6 instrument has evaluated, and this guard is what turns that from a silence into a refusal the next time the oracle runs over a sample that misses the stratum.

**Edit 2 (IN-08) — state the convention.** The oracle checks `UNKNOWNS` before `failed`; the incremental control checks `failed` first. **Adopt the oracle's order as the house convention — a blind instrument outranks a measured red — because this estate's standing rule is that "could not look" must be kept distinct from and never folded into any other verdict, and reporting a red while an instrument was blind asserts a cause the run did not establish.** Do not change the oracle's logic, which already implements it. Instead, state the convention explicitly in the header's EXIT CODES block: which of the two wins when both are present, the one-sentence reason, and a pointer to `scripts/phase06-incremental-control.sh` as the sibling that follows the same convention (plan 06-20 aligns it). A convention that is implemented but not written down is exactly what IN-08 reports.

**Edit 3 — self-test cases for every branch this plan changed.** Add cases to `--self-test` that drive, locally and with no ssh or docker:

- `manifest_compare` over an EMPTY manifest — require the refusal and require `DIFF_WHY` to contain the vacuity reason. Pair it with a non-empty manifest that compares identical, so the guard is proven to discriminate rather than to refuse everything.
- `report_multi_artist` over a path that does not exist — require the UNKNOWN routing and require that no `✓` is printed for that label.
- The field-view read over an empty `fields.tsv` — require the UNKNOWN routing, and pair it with a well-formed six-column file that passes.
- `assert_dj_count` with a wanted count of zero — require the VACUOUS refusal; pair it with a non-zero matching count that passes, and a non-zero mismatching count that fails, so all three arms are exercised.

Update the harness's case count and banner text in one edit. **Every new case needs its positive pair**: a guard that refuses everything is as useless as one that refuses nothing, and this plan exists precisely because guards were not proven to discriminate.

**WR-01's restructured precheck (task 1, edit 4) cannot be driven offline** — it needs a container with an unreadable directory. Do not fabricate a self-test case for it. Record it as undriven with that reason in task 3's artifact.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/phase06-oracle.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -q "VACUOUS" || { echo "IN-09: the vacuity word is absent"; exit 1; }; n=$(printf "%s\n" "$s" | grep -c "VACUOUS"); [ "$n" -ge 2 ] || { echo "IN-09: only $n VACUOUS refusal(s) - assert_dj_count did not get one"; exit 1; }; grep -q "DEF-06-12-01" "$f" || { echo "IN-09: the guard does not name the deferral it makes visible"; exit 1; }; awk "/EXIT CODES/,/^# =/" "$f" | grep -qiE "UNKNOWN.*(wins|outranks|first)|blind.*outranks" || { echo "IN-08: the precedence convention is not stated in the EXIT CODES block"; exit 1; }; awk "/EXIT CODES/,/^# =/" "$f" | grep -q "phase06-incremental-control" || { echo "IN-08: the sibling is not named"; exit 1; }; out=$(bash "$f" --self-test 2>&1) || { echo "$out" | tail -30; exit 1; }; echo "$out" | grep -qi "vacuous" || { echo "the vacuity refusal is not exercised by --self-test"; exit 1; }; echo "$out" | grep -qiE "empty" || { echo "the empty-input refusals are not exercised by --self-test"; exit 1; }; echo "06-19 task 2 OK"'</automated>
  </verify>
  <done>
    `assert_dj_count` refuses a zero wanted-count as VACUOUS using its neighbour's vocabulary and names `DEF-06-12-01`; the EXIT CODES block states that a blind instrument outranks a measured red, with the reason and a pointer to the sibling; `--self-test` grew a discriminating pair of cases for each of the four changed branches (empty manifest, unreadable fields path, empty field view, zero DJ count) and its case count and banner match what it runs; `--self-test` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the driven refusals and what remains undriven</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-19-oracle-vacuity-driven.txt</files>
  <read_first>
    - `scripts/phase06-oracle.sh` as tasks 1 and 2 left it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-18-oracle-fence-driven.txt` — plan 06-18's artifact, for its NOT-DRIVEN section format and so this artifact continues rather than duplicates it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-11-wrote-nothing.txt` — the committed three-layer proof, referenced here as unchanged.
    - `README.md` § Health Checks — the three rules, particularly "fail closed with 'could not look' kept distinct from 'nothing is wrong'", which is the rule all four findings in this plan violate.
  </read_first>
  <action>
Record in `artifacts/06-19-oracle-vacuity-driven.txt`:

1. The full `bash scripts/phase06-oracle.sh --self-test` transcript and exit status, with the new cases visible and their positive pairs visible beside them. For each of the four changed branches, quote the refusal line AND the passing line, so the artifact shows the guard discriminating rather than only firing.
2. `bash -n` and `shellcheck -S warning` output, with the known SC2329 false positives noted as known rather than re-reported.
3. **A before/after statement for each finding**, one line each, in the form *what it used to print / what it prints now*. For WR-06 quote the exact old string (`✓ CONF-04 write side: the fields TSV is missing or unreadable, so the write side could not be shown`) and the new one, because a green tick whose text says the instrument did not look is the single clearest example of this phase's dominant defect class and is worth preserving verbatim in the record.
4. **The NOT-DRIVEN section.** WR-01's restructured precheck requires a container with a present-but-unreadable scratch directory and cannot be driven offline; record it as reviewed-by-reading plus `bash -n`/`shellcheck`, name the condition that would drive it, and state that it is a refusal path so it can only make a future run redder. Add anything else from tasks 1 and 2 that was not driven.
5. **The IN-08 decision, recorded as a decision**: the house convention is that a blind instrument outranks a measured red; the oracle already implemented it and now states it; `phase06-incremental-control.sh` is aligned by plan 06-20. Name both files so the convention is discoverable from either.
6. An explicit statement that no oracle `--run` or `--baseline` was performed, that every change in this plan is to a refusal branch and can therefore only make a future run redder, and that `artifacts/06-11-oracle-run.txt` and `06-11-wrote-nothing.txt` stand unchanged as the phase's CONF-03/CONF-06 evidence.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-19-oracle-vacuity-driven.txt; test -s "$a" || { echo "artifact missing"; exit 1; }; for k in WR-01 WR-02 WR-06 IN-09 IN-12 IN-08; do grep -q "$k" "$a" || { echo "$k is not recorded"; exit 1; }; done; grep -q "the write side could not be shown" "$a" || { echo "the WR-06 verbatim old string is not preserved"; exit 1; }; grep -qiE "not driven|undriven" "$a" || { echo "the NOT-DRIVEN section is missing"; exit 1; }; grep -qiE "no oracle .*--run|no --run|did not run the oracle" "$a" || { echo "the no-oracle-run statement is missing"; exit 1; }; grep -qiE "redder|only make .* red" "$a" || { echo "the refusal-branches-can-only-redden argument is missing"; exit 1; }; grep -q "phase06-incremental-control" "$a" || { echo "the IN-08 decision does not name both files"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/phase06-oracle.sh" || true); [ -z "$d" ] || { echo "test debris:"; echo "$d"; exit 1; }; echo "06-19 task 3 OK"'</automated>
  </verify>
  <done>
    The artifact holds the full self-test transcript with each new refusal and its positive pair quoted, `bash -n` and `shellcheck` output with SC2329 noted as known, a one-line before/after for each of the six findings including WR-06's verbatim old green tick, a NOT-DRIVEN section naming WR-01's precheck with the condition that would drive it, the IN-08 convention recorded as a decision naming both scripts, and an explicit no-run statement with the argument that refusal-only changes can only redden a future run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container filesystem state → the oracle's prechecks | An unreadable or partially-populated directory is data the instrument does not control, and it is the input the precheck must refuse on. |
| remote command exit status → the LXC-side verdict | The container's `/bin/sh` is dash with no `pipefail`; any pipeline sent in loses the status of every stage but the last. |
| measurement artifacts (`fields.tsv`, manifests) → the assertion functions | An empty or truncated artifact is indistinguishable from a clean measurement unless the size is checked. |
| this public repo → the world | No credential in the script or the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-100 | Spoofing | Two empty manifests comparing equal, satisfying layer 2 of the wrote-nothing proof by having measured nothing | mitigate | WR-02: `-s` guard in `manifest_compare` with the sibling's verbatim vacuity reason; driven by a self-test case with a discriminating positive pair. |
| T-06-101 | Repudiation | A green tick printed over a could-not-look (`report_multi_artist` returning 0 on an unreadable input; an empty `fields.tsv` reported as "0 items, six columns each") | mitigate | WR-06 and IN-12: both branches routed to `unknown`, driven by self-test cases; the old strings preserved verbatim in the artifact so the defect class stays recognisable. |
| T-06-102 | Spoofing | A sample that never exercised the DJ stratum passing the `albumtype` path-rule assertion by comparing zero with zero | mitigate | IN-09: VACUOUS refusal matching `assert_no_compilations`, naming `DEF-06-12-01`, driven with all three arms exercised. |
| T-06-103 | Spoofing | A present-but-unreadable container scratch directory reported as "absent or empty" because a dash pipeline exits 0 on the failing stage | mitigate | WR-01: the pipeline removed from the remote command and the look's status read explicitly; recorded as NOT DRIVEN with the condition that would drive it. |
| T-06-104 | Tampering | A guard added that refuses everything, making the instrument unusable and inviting its removal | mitigate | Every new self-test case carries a positive pair; the verify command requires both refusal and pass lines in the artifact. |
| T-06-105 | Repudiation | The RED-vs-UNKNOWN precedence differing between two sibling instruments, so a reader infers the wrong convention | mitigate | IN-08: the oracle's order adopted as the house convention and stated in its EXIT CODES block with the sibling named; plan 06-20 aligns the sibling. |
| T-06-106 | Denial of Service | Re-running the oracle to "prove" the changes, performing an import during a paper phase | mitigate | Plan-level constraint forbidding `--run`/`--baseline`; all changes are refusal branches, proven offline; the verify command requires the no-run statement. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S warning` clean on `scripts/phase06-oracle.sh`.
- `--self-test` exits 0 and exercises each of the four changed branches with a discriminating positive pair.
- `grep -c VACUOUS` over the script returns at least 2.
- The EXIT CODES block states the RED-vs-UNKNOWN convention and names the sibling.
- No oracle `--run` or `--baseline` was executed.
</verification>

<success_criteria>
- The oracle can no longer print a green tick over a blind read.
- Every vacuity guard has a proven positive pair.
- The precedence convention is written down, not just implemented.
- What could not be driven is named as such.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-19-SUMMARY.md` when done. Record the double-report decision from task 1 edit 2 and the NOT-DRIVEN list; plan 06-21 carries both into the disposition register.
</output>
