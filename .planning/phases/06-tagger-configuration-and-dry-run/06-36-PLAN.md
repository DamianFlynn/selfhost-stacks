---
phase: 06-tagger-configuration-and-dry-run
plan: 36
type: execute
wave: 16
depends_on: ["06-34"]   # round 3's last plan: these four fix the code plans 06-30..06-33 wrote and 06-34 dispositioned
gap_closure: true
gap_closure_round: 4
files_modified:
  - scripts/phase06-oracle.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt
autonomous: true
requirements: [CONF-03, CONF-06]
closes_findings: [R4-02, R4-06, R4-09, R4-10]

must_haves:
  truths:
    - "`--self-test` exits 0 on a machine without python3 and on a machine running as root, and the announced case count is adjusted at the same site that prints each deliberate skip's warn — a deliberate skip is no longer a hard failure"
    - "The announcement gate still fires, and still exits 1, when a section is dropped from the dispatcher or a case is deleted from a section — the skip-awareness did not disable the guard it replaced"
    - "The gate's message no longer asserts `A SECTION DID NOT RUN` as the only possible cause, and the maintenance instruction beside the pin names the reference environment the base figure is measured in"
    - "The layer-3 sha256 line parser — the guard in front of the one assertion whose job is to prove `-l` kept the real library.db closed — is driven by cases in the pinned self-test set, positively and negatively, and a case asserts the driven program text is the same text the four live consumers use"
    - "The four layer-3 consumers take the requested path through the environment rather than through `awk -v`, so a path containing a backslash is compared as written rather than after awk escape processing"
    - "The two `exit 3` arms R3-04 added state in band that step 12's cleanup does not run on them, that this is the pre-existing behaviour of the two `exit 3` arms above them, and what is left behind"
  artifacts:
    - path: "scripts/phase06-oracle.sh"
      provides: "a skip-aware announced-case gate, a driven layer-3 parser, environment-passed paths, and an in-band statement of the could-not-look cleanup gap"
      contains: "R4-02"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt"
      provides: "the four ablation transcripts (python3-absent, root ×2, dropped section, deleted case) both before and after the fix, plus the parser probes including the backslash case"
  key_links:
    - from: "the three environment-conditional skip arms"
      to: "ST_PLANNED_CASES"
      via: "a decrement at the same site that prints the skip's warn"
      pattern: "ST_PLANNED_CASES=\\$\\(\\(ST_PLANNED_CASES -"
    - from: "self_test_vacuity"
      to: "the four live layer-3 awk consumers"
      via: "a case asserting the driven program text is byte-identical to the shipped one"
      pattern: "ENVIRON\\[\"p\"\\]"
---

<objective>
Close the four round-4 findings that land in `scripts/phase06-oracle.sh`: **WR-02**, **WR-06**,
**IN-03** and **IN-04** of `06-REVIEW-GAP3.md`.

⚠ **FINDING IDs IN THIS ROUND ARE ALIASED, AND THIS IS NOT OPTIONAL.** Round 4's report reuses
round 1's `WR-*` / `IN-*` namespace, and round 1's IDs are already cited **in band in this very
file** — `phase06-oracle.sh` carries four `WR-01`, two `WR-02`, six `WR-06`, five `WR-07`, six
`WR-08`, three `IN-09`, four `IN-12` and more. **`WR-02` and `WR-06` both already mean something
else here**, thirty lines from where you are about to write. Writing a bare `WR-02` into this file
would be ambiguous to every future reader and would make any grep for it non-discriminating. **In
band, and in every verify block, this round's findings are `R4-01 … R4-10`**, continuing the
phase's per-round convention (`CR`/`WR`/`IN` = round 1, `GC-*` = round 2, `R3-*` = round 3). Plan
06-39 carries the mapping table. This plan owns:

| Round-4 report ID | In-band ID | Subject |
|---|---|---|
| WR-02 | **R4-02** | `ST_PLANNED_CASES=134` is not invariant over the self-test's own documented environment-conditional skips |
| WR-06 | **R4-06** | the new sha256 line parser guards the layer-3 `-l` assertion and has no case in the set R3-02 pinned in the same round |
| IN-03 | **R4-09** | `awk -v p="$REAL_LIB_DB"` applies awk escape processing to an overridable knob |
| IN-04 | **R4-10** | R3-04's two new `exit 3` arms leave the container scratch and the LXC stamp behind, and the gap is nowhere stated in band |

**R4-02 is round 3's centrepiece fix turned against itself, and it deserves to be stated plainly:
the fix made the instrument less trustworthy than it found it.** `ST_RUN` is incremented once per
`st_case`, and three case groups are environment-conditional *by design*, each printing a `warn`
whose own words are that the skip is "reported, never silently counted as a pass". Root skips one
case in `self_test_core` and four in `self_test_vacuity`; an absent `python3` skips two in
`self_test_classes`. So `134` is the count **only on a non-root machine that has python3**. The
review drove it: with `python3` absent the self-test exits 1 announcing **"A SECTION DID NOT RUN"**
when every section ran. As root the figure is 129, same false message.

This is a **false red with a wrong stated cause**, not a false green — which is why it is a Warning
and not a Blocker, and why you must not inflate it. But note the asymmetry the review names: the
gate fires most readily on the machines least likely to be the operator's, so its first real use is
likely to be a misdiagnosis, and the in-band remediation instruction ("Re-measure only after
confirming every section still runs") walks that operator into re-pinning 132, which then fails on
every box that *has* python3. **Do not replace one magic constant with another.** The announcement
must be adjusted by the same arm that decides to skip.

**Say which fix you are making, per finding, and say it in band:**
- **R4-02 — BOTH.** The arithmetic changes (code) and the gate's message plus the maintenance
  instruction stop asserting one cause and an unconditional re-measure (claim).
- **R4-06 — CODE.** New cases in the pinned set, driving the shipped parser.
- **R4-09 — CODE.** Four consumers take the path through `ENVIRON` instead of `awk -v`.
- **R4-10 — CLAIM CORRECTION.** The review's own words are that this is a **pre-existing class**,
  shared with the two `exit 3` arms two lines above, and that leaving state behind on a
  could-not-look is arguably right for a forensic instrument. The defect is that the gap is
  **unstated**. State it; do not add a trap. `phase06-oracle.sh` has no `trap` anywhere, by design,
  and inventing one in a hardening round is the scope creep this round exists to refuse.

**TASK ORDER IS LOAD-BEARING.** Task 3 re-measures the announcement and must run **last**, after
task 2 has finished adding cases — round 3's plan 06-31 ordered its pin task last for exactly this
reason and said so. Do not reorder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP3.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/phase06-oracle.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Take the layer-3 paths through the environment, and state the could-not-look cleanup gap in band</name>
  <files>scripts/phase06-oracle.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt</files>
  <read_first>
    - **`scripts/phase06-oracle.sh` — the file you are modifying.** Read the whole layer-3 BEFORE
      block and the whole layer-3 AFTER block as two continuous reads.
    - **Do not trust any line number in this plan or in `06-REVIEW-GAP3.md`.** Resolve every anchor
      with `/usr/bin/grep -n` first. `DEF-06-29-02` and `DEF-06-29-06` exist because citations in
      this phase went stale twice.
    - The four consumers. Anchor: `/usr/bin/grep -n 'sub(/\^\[0-9a-f\]' scripts/phase06-oracle.sh`
      — **four hits**, two in the BEFORE block reading `$OUT/layer3.before` and two in the AFTER
      block reading `$OUT/layer3.after`. Read all four. They are byte-identical apart from the
      variable being assigned, the `-v p=` argument and the input file.
    - The two knobs: `REAL_LIB_DB="${REAL_LIB_DB:-` and `REAL_STATE_PICKLE="${REAL_STATE_PICKLE:-`.
      Read the override contract stated above them.
    - R3-03's comment beside the BEFORE consumers — the paragraph that explains why the parser
      strips a leading `<hex><SP><SP>` and compares the **remainder** for exact equality, and that
      records the GNU backslash-escaped-output case as a **deliberate refusal**. Your change must
      not disturb that refusal: `ENVIRON` changes how the *requested* path arrives, not how the
      *printed* line is read.
    - The four `exit 3` arms in the AFTER block. Anchors: `capture_manifests after`,
      `rsh_classify "the layer-3 hashes after the run"`, and R3-04's two new guards
      `no sha256 line for $REAL_LIB_DB after the run` and
      `no sha256 line for $REAL_STATE_PICKLE after the run`.
    - Step 12 and what it would have removed. Anchors: `--- Step 12: cleanup` and the operator
      instruction line containing `docker exec -u $CONTAINER_USER $CONTAINER rm -rf --`. Read what
      that instruction says, because your in-band note should point at it rather than restate it.
    - `06-REVIEW-GAP3.md` §§ IN-03 and IN-04 in full. Note IN-04's explicit grading: *"This is a
      **pre-existing class**, not a new one"*, and that no `trap` exists anywhere in this file.
  </read_first>
  <action>
**Disposition: R4-09 is CODE. R4-10 is a CLAIM CORRECTION and nothing else.**

**(a) R4-09 — pass the requested path through the environment at all four consumers.** Replace
`awk -v p="$REAL_LIB_DB"` with a preceding environment assignment and an `ENVIRON["p"]` lookup, so
the value reaches awk without `-v`'s escape processing. Keep the existing `LC_ALL=C` prefix, keep
the program otherwise byte-identical, and make the **same** change at all four sites — the value of
the R3-03 fix was that one parser serves all four, and a partial conversion re-creates exactly the
split R3-03 removed.

**Say what this does and does not buy, in one clause in band.** It fails closed today: a mangled
comparison yields an empty hash, which routes to `unknown` and `exit 3`, never to a green tick or a
false RED. The review grades it a residual for that reason. What it buys is that a knob containing a
backslash is compared **as written**. Name **R4-09**.

**(b) R4-10 — state the cleanup gap where the arms are.** Add an in-band note at R3-04's two new
`exit 3` guards recording, accurately and without inflation:
  - step 12's `rm -rf` of the container scratch and `rm -f` of the LXC stamp do **not** run when
    these guards fire, so a could-not-look leaves both behind;
  - this is the **pre-existing behaviour of the two `exit 3` arms immediately above** — the
    `capture_manifests after` arm and the `rsh_classify` arm — and R3-04 increased the count of such
    exits rather than introducing the class;
  - leaving state behind on a could-not-look is **arguably the right call for a forensic
    instrument**: the scratch is the evidence of what the run saw, and a cleanup that runs on an
    unexplained exit destroys it;
  - what is left behind, named: the container scratch at `$SCRATCH` and the stamp at
    `$STAMP_REMOTE`, both removable with the operator instruction already printed elsewhere in this
    file — **cite that instruction's location by anchor phrase, do not duplicate the command**;
  - name **R4-10**.

⛔ **Do not add a `trap`.** This file has none anywhere, deliberately. A cleanup trap here would run
on every `exit 3`, including the forensic ones, and would be a behaviour change to the live path in
a round whose subject is claims outrunning code. If you think one is warranted, that is a deferred
item for plan 06-39 to record, not a change for this plan to make.

**(c) Record the drive.** Into `artifacts/06-36-oracle-pin-parser-and-cleanup.txt`, probe the
converted parser **as a detached copy** against synthetic `sha256sum` output — at minimum: a path
containing a **space** (must return its digest), a path containing a **backslash** (must return its
digest under `ENVIRON`, and must be shown NOT to under the old `-v` form — that is the both-
directions drive for R4-09), a GNU backslash-escaped output line (must return empty — the
deliberate refusal), and a line naming a different path (must return empty, so the emptiness guards
still fire). No estate contact: this is `awk` over files you write in a `mktemp -d`.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides the exit code
F=scripts/phase06-oracle.sh
G=/usr/bin/grep

# --- (a) R4-09: no consumer takes the path through awk -v any more ---
# Whole constructions. `awk -v p="$REAL_LIB_DB"` is not a substring of the ENVIRON replacement,
# which names no `-v` at all.
test "$($G -cF 'awk -v p="$REAL_LIB_DB"' $F)" -eq 0
test "$($G -cF 'awk -v p="$REAL_STATE_PICKLE"' $F)" -eq 0

# All FOUR live consumers converted, and counted by a pattern the self-test's own copy cannot
# satisfy: only the live sites read $OUT/layer3.*. Counting ENVIRON alone would rise when task 2
# adds its driven copy, which is the self-moving-count defect this phase has shipped three times.
test "$($G -F 'ENVIRON["p"]' $F | $G -cF '"$OUT/layer3.')" -eq 4
# and the requested path is exported on those same lines
test "$($G -F 'ENVIRON["p"]' $F | $G -cF 'p="$REAL_LIB_DB"')" -ge 2
test "$($G -F 'ENVIRON["p"]' $F | $G -cF 'p="$REAL_STATE_PICKLE"')" -ge 2

# R3-03's parser shape survives unchanged: still strip-a-prefix-then-compare-the-remainder, not a
# suffix test. A suffix test can match a line it was not asked about -- R3-03 refused it by name.
test "$($G -cF 'sub(/^[0-9a-f]+  /, "", rest)' $F)" -ge 4
test "$($G -cF 'R4-09' $F)" -ge 1

# --- (b) R4-10: the cleanup gap is stated at the arms ---
test "$($G -cF 'R4-10' $F)" -ge 1
# and the two guards R3-04 added still exist, unchanged in behaviour
test "$($G -cF 'no sha256 line for $REAL_LIB_DB after the run' $F)" -eq 1
test "$($G -cF 'no sha256 line for $REAL_STATE_PICKLE after the run' $F)" -eq 1
# NO TRAP WAS ADDED. This file has none and must still have none.
test "$($G -c '^[[:space:]]*trap ' $F)" -eq 0

bash -n $F

# The self-test still runs and still passes in this environment. Task 3 owns the pin; this is the
# outer gate that task 1 did not break it.
OUT="$(mktemp -d)" bash $F --self-test >/dev/null

# --- (c) the parser probes are recorded, including the backslash case that IS R4-09 ---
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt
test -s "$ART"
test "$($G -cF 'R4-09' "$ART")" -ge 1
test "$($G -cF 'R4-10' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- `/usr/bin/grep -cF 'awk -v p="$REAL_LIB_DB"'` and the `REAL_STATE_PICKLE` equivalent both return
  **0**; exactly **4** lines carry both `ENVIRON["p"]` and `"$OUT/layer3.`.
- The R3-03 parser shape (strip leading `<hex><SP><SP>`, compare the remainder for exact equality)
  is unchanged at all four sites, and its deliberate refusal of GNU-escaped output still holds.
- An in-band note at R3-04's two `exit 3` guards names the uncleaned scratch and stamp, records the
  class as pre-existing and shared with the two arms above, states the forensic rationale, and
  points at the existing operator instruction by anchor rather than duplicating its command.
- `/usr/bin/grep -c '^[[:space:]]*trap '` returns **0** — no trap was added.
- `bash -n` exits 0 and `--self-test` exits 0 in this environment.
- The artifact records four parser probes as detached copies, including the backslash case shown
  passing under `ENVIRON` and failing under the old `-v` form.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Give the layer-3 parser cases in the pinned set, including one that proves the driven text is the shipped text</name>
  <files>scripts/phase06-oracle.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt</files>
  <read_first>
    - **`scripts/phase06-oracle.sh`** — `self_test_vacuity()` in full, from its opening
      `local d="$OUT/st-vacuity"` to its end. This is where the new group goes: it is the section
      whose stated subject is guards that can or cannot fire, which is exactly what the parser is.
    - `st_case()` in full — note it takes **expectation, observed, description** and increments
      `ST_RUN` once per call. Every case you add moves the announced total, which is task 3's
      business, not yours: **do not touch `ST_PLANNED_CASES` in this task.**
    - The four converted consumers as task 1 left them (anchor
      `/usr/bin/grep -n 'ENVIRON\["p"\]' scripts/phase06-oracle.sh`), because the case you write
      must drive **that** text.
    - The existing byte-identity idiom in this file: `self_test_fences` compares the two stamp fence
      copies byte for byte. Anchor: `/usr/bin/grep -n 'self_test_fences() {' scripts/phase06-oracle.sh`.
      That is the precedent for the identity case below — it is not a new invention.
    - `DEF-06-29-03` in `deferred-items.md` — **the oracle's fence copies are protected by a check
      that FORBIDS the DRY single-source refactor.** Read it before you are tempted to hoist the awk
      program into one shared variable used by both the live sites and the self-test. Do not do that
      here either: keep the live copies where they are and prove identity instead.
    - **GC-01, which is still live estate-wide (`DEF-06-29-01`):** `… | grep -q` under `pipefail`
      returns 141 at >=64 KiB and reports a present substring as absent. Use `grep -c` with a
      counted comparison, never `grep -q`, in anything you add.
    - `06-REVIEW-GAP3.md` § WR-06 in full, including its suggested fixture line and its list of the
      negatives to add.
  </read_first>
  <action>
**Disposition: R4-06 is CODE.** New cases, no claim to correct — R3-03's account of the parser is
accurate; it simply had no case behind it.

**(a) Add a case group to `self_test_vacuity`** over a fixture file written into that function's own
`$d` scratch. Drive the parser text **as the live sites now carry it** (the `ENVIRON` form). At
minimum these cases, each a separate `st_case`:
  1. **positive** — a line whose path contains a **space** is matched exactly and its digest
     returned. This is the case R3-03 exists for and the one the review names.
  2. **positive** — a line whose path contains a **backslash** is matched exactly. This is the case
     that makes R4-09 a driven change rather than an asserted one.
  3. **negative** — a **GNU backslash-escaped** output line (`\<hash>  \<path>`) returns empty. This
     is R3-03's *deliberate* refusal; the case pins it as intended behaviour so a future reader does
     not "fix" it.
  4. **negative** — a **suffix-only near-miss**: a line whose path *ends with* the requested path
     but is not equal to it returns empty. This is the property that makes the parser an exact
     comparison rather than a suffix test, which R3-03 refused by name.
  5. **negative** — a **truncated** line (digest with no path, or a path with no digest) returns
     empty, so the emptiness guards at both blocks still fire.

**Each negative must be shown to be a real refusal and not an accident of an empty fixture.** Write
the positives and negatives into the **same** fixture file where possible, so a case returning empty
is returning empty over a non-empty input. A guard that refuses everything is exactly as useless as
one that refuses nothing — `self_test_vacuity`'s own opening words.

**(b) Add the identity case — this is the case that keeps the group honest.** Assert that the awk
program text your cases drive is the text the **live consumers** carry. Shape:
  - hold the program text once in a shell variable inside the self-test;
  - count the lines of this script that contain **both** that program text **and** the live-only
    marker `"$OUT/layer3.`, and `st_case 4 "$count"`;
  - resolve this script's own path fail-closed. If the path is not readable, the case must go
    **red or be reported as could-not-look — never silently pass**. That distinction is this
    phase's founding lesson (CR-01, GC-03, GC-05) and a vacuous identity case would be its fourth
    instance.

Counting on the live-only marker rather than on the program text alone is deliberate: the
self-test's own copy is in the same file, so a bare count of the program text would be
self-referential and would move when this task adds its copy — the exact defect GC-10, R3-05 and
R4-05 each are.

**(c) Drive the identity case to FAIL.** On a scratch copy from `mktemp -d`, mutate **one** live
consumer (change the regex, the two-space separator, or the `print $1` field index) and confirm the
self-test exits 1 at the identity case. A future edit to any of those three is exactly what the
review says currently goes green. Record the transcript.

**(d) Do not touch `ST_PLANNED_CASES`.** Task 3 re-measures it after this task has stopped moving
the count. Note in your summary how many cases you added, but do not encode that number anywhere in
the file.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
F=scripts/phase06-oracle.sh
G=/usr/bin/grep

# The group exists inside self_test_vacuity and names the finding.
test "$($G -cF 'R4-06' $F)" -ge 1

# The driven copy of the parser exists IN ADDITION to the four live ones. Counted the same
# discriminating way task 1 used: live sites carry the layer3 marker, the driven copy does not.
test "$($G -cF 'ENVIRON["p"]' $F)" -ge 5
test "$($G -F 'ENVIRON["p"]' $F | $G -cF '"$OUT/layer3.')" -eq 4

# grep -q is forbidden in anything this task added (GC-01 is still live estate-wide).
# Whole construction, and scoped to the pipeline shape that actually carries the defect.
test "$($G -cF '| grep -q' $F)" -eq 0
test "$($G -cF '| /usr/bin/grep -q' $F)" -eq 0

bash -n $F

# --- THE DRIVE. Both directions, both estate-free. ---
# 1. the real file still passes, with the new cases in it and the OLD pin still in place.
#    Task 3 has not run yet, so the announced total is stale by exactly the number of cases added:
#    this run is therefore EXPECTED TO EXIT 1 AT THE ANNOUNCEMENT GATE and nowhere else. Assert
#    that precisely -- a different failure means the new cases are broken.
ST_OUT="$(mktemp -d)"
rc=0; OUT="$ST_OUT" bash $F --self-test > "$ST_OUT/run.txt" 2>&1 || rc=$?
test "$rc" -eq 1
test "$($G -cF 'were announced' "$ST_OUT/run.txt")" -ge 1
test "$($G -cF 'SELF-TEST REGRESSION' "$ST_OUT/run.txt")" -eq 0

# 2. the identity case can FAIL: mutate one live consumer on a scratch copy.
MUT="$(mktemp -d)"
cp $F "$MUT/oracle.sh"
# change the field index on the FIRST live consumer only, leaving the other three and the driven
# copy alone. A plain `sed` on the first match, not a global substitution.
/usr/bin/sed '0,/print \$1 }/s//print $2 }/' "$MUT/oracle.sh" > "$MUT/oracle.mut.sh"
! cmp -s "$MUT/oracle.sh" "$MUT/oracle.mut.sh"
rc=0; OUT="$(mktemp -d)" bash "$MUT/oracle.mut.sh" --self-test > "$MUT/mut.txt" 2>&1 || rc=$?
test "$rc" -ne 0
test "$($G -cF 'SELF-TEST REGRESSION' "$MUT/mut.txt")" -ge 1

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt
test "$($G -cF 'R4-06' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- `self_test_vacuity` carries at least five new `st_case` assertions over a shared non-empty
  fixture: space-bearing path matched, backslash-bearing path matched, GNU-escaped line refused,
  suffix-only near-miss refused, truncated line refused.
- An identity case asserts that exactly **4** lines of this script carry both the driven program
  text and the live-only `"$OUT/layer3.` marker, and it resolves the script path fail-closed —
  an unreadable path goes red or reports could-not-look, never a silent pass.
- Mutating one live consumer's field index on a scratch copy makes `--self-test` print
  `SELF-TEST REGRESSION` and exit non-zero; the transcript is in the artifact.
- Running the real file's `--self-test` at the end of this task exits **1 at the announcement gate
  only** — `were announced` present, `SELF-TEST REGRESSION` absent — because task 3 has not
  re-measured yet. Any other failure means the new cases are broken.
- `| grep -q` appears nowhere in the file. `ST_PLANNED_CASES` is byte-unchanged by this task.
- `bash -n` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Make the announced case count adjust where the skip is decided, and stop the gate asserting one cause — run this task LAST</name>
  <files>scripts/phase06-oracle.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt</files>
  <read_first>
    - **`scripts/phase06-oracle.sh`** — the whole `if [ "$MODE" = "self-test" ]; then` block, from
      the R3-02 paragraph down to `exit 0`. Read the pin, the dispatcher, the announcement gate and
      the GC-12(b) gate below it as one continuous read; the ordering of the two gates is
      deliberate and must not change.
    - **The three environment-conditional skip arms, each read in full including the cases in its
      `else` branch** — you need the number of `st_case` increments each `else` performs, and you
      must **measure** that number rather than count invocations by eye. Anchors:
      `SKIPPED as root: an UNREADABLE manifest (mode 000)` (in `self_test_core`),
      `SKIPPED: no python3 on this workstation` (in `self_test_classes`),
      `SKIPPED as root: the PRESENT-BUT-UNREADABLE case` (in `self_test_vacuity`).
      Note that `st_mc`, `st_assert` and `st_grep_why` each ultimately call `st_case`, so
      "how many lines look like cases" is **not** the answer. Measure by ablation.
    - `st_case()` — confirm `ST_RUN` and `ST_PLANNED_CASES` are plain globals and that no function
      declares either `local`. A `local` would make a decrement inside a function invisible outside
      it, and the gate would then fire anyway with a *different* wrong cause.
    - The sentences to correct, both anchored as whole constructions:
      `Re-measure only after confirming every section still runs.` and
      `it MUST BE RE-MEASURED whenever a section or a case is added`.
    - The gate's message: `were announced - A SECTION DID NOT RUN`.
    - The sibling's guard, for the idiom: `/usr/bin/grep -n 'ST_PLANNED_CASES' scripts/check-beets-config.sh`.
      **Do not change the sibling.** Plan 06-38 owns that file this round and the two plans run in
      parallel.
    - `06-REVIEW-GAP3.md` § WR-02 in full, including its table of the three skip sites, its driven
      transcript, and its closing instruction that *"whatever form is chosen, the gate's message
      must stop asserting 'A SECTION DID NOT RUN' as the only possible cause"*.
  </read_first>
  <action>
**Disposition: R4-02 is BOTH — a code change to the arithmetic and a correction to two claims.**

**(a) Make each deliberate skip adjust the announcement at the site that prints its warn.** At each
of the three skip arms, immediately beside the `warn` that reports the skip, decrement
`ST_PLANNED_CASES` by the number of cases that arm's `else` branch would have run.

**Derive each number by ablation, not by eye.** For each arm in turn, on a scratch copy from
`mktemp -d`, force the arm to take its skip branch, run `--self-test`, and read the resulting
`ST_RUN` off the gate's own output; the difference from the unforced run is that arm's number.
Record all three measurements in the artifact. Counting `st_case`-looking lines is wrong here
because `st_mc`, `st_assert` and `st_grep_why` all funnel into `st_case`.

**Why this shape and not a per-section sentinel:** a *deliberate* skip adjusts the announcement at
the same site that decides to skip, while a **dropped section adjusts nothing** — so the gate still
fires for the case it was built for. Say that in band, in one sentence, because it is the property
that makes the change safe and the next reader will otherwise think the guard was weakened.

**(b) Re-measure the base constant, in the reference environment, and name that environment.** The
base is the count with **no** skip taken: non-root, python3 present. Read it off a green run after
task 2's cases are in. Do not copy any number from this plan, from the review, or from
`06-DISPOSITIONS-GAP2.md` — all three record 134, and task 2 moved it.

**(c) Correct the gate's message.** It must no longer assert `A SECTION DID NOT RUN` as the sole
cause. Name at least: a section dropped from the dispatcher, an early `return` inside a section, and
a case added or removed without the announcement moving — and state that a **deliberate**
environment skip is no longer among the causes, because those now self-adjust. Keep the message's
existing virtue: it still withholds the banner rather than printing an unbacked universal claim.
Name **R4-02**.

**(d) Correct the maintenance instruction.** The sentence `Re-measure only after confirming every
section still runs.` and the clause `it MUST BE RE-MEASURED whenever a section or a case is added`
must both go, because together they walk an operator on a python3-less box into re-pinning a figure
that then fails on every box that has python3 — the review's "self-destabilising across
environments". The replacement must say: the base counts the **unconditional** cases; each
environment-conditional skip adjusts the announcement at its own site; and a re-measure is valid
**only in the reference environment**, which must be named explicitly (non-root, `python3`
present). State the failure mode being prevented in one clause so nobody simplifies it back.

**(e) Record all five drives** in `artifacts/06-36-oracle-pin-parser-and-cleanup.txt`, each with its
exit code and the gate line, **both before and after this task's change** where the before-state is
meaningful:
  1. real file, this workstation — exit **0**;
  2. `python3` absent, simulated by a one-token ablation of the `command -v python3` test on a
     scratch copy (the review used a stripped `PATH`; the ablation is more robust and is the idiom
     round 3's plan 06-31 already used) — exit **0** after, exit **1** with the false
     "A SECTION DID NOT RUN" message before;
  3. root, simulated by replacing the `[ "$(id -u)" = "0" ]` test with a true test on a scratch
     copy, **each arm singly and then both together** — exit **0** after, exit **1** before;
  4. **the non-vacuity control, and it is the most important one:** drop the `self_test_fences`
     call from the dispatcher on a scratch copy — must still exit **1** and fire the announcement
     gate. If this passes green, the skip-awareness disabled the guard and the task has failed;
  5. delete one `st_case` line from an **unconditional** section on a scratch copy — must still
     exit **1**.

Every drive is estate-free: `--self-test` contacts nothing. Set `OUT` to a fresh `mktemp -d` on each
run so the drives cannot read each other's scratch.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
F=scripts/phase06-oracle.sh
G=/usr/bin/grep

# (a) each of the three skip arms adjusts the announcement. -ge 3, not -eq 3: an -eq here is a
# constant that a fourth conditional skip would move silently, which is this phase's signature
# defect wearing a different hat.
test "$($G -cF 'ST_PLANNED_CASES=$((ST_PLANNED_CASES -' $F)" -ge 3
# and the base is still declared exactly once
test "$($G -cE '^[[:space:]]*ST_PLANNED_CASES=[0-9]+$' $F)" -eq 1

# (c)(d) the two sentences that made the instrument self-destabilising are withdrawn. Both are
# whole constructions and neither can be a substring of a replacement that says the opposite.
test "$($G -cF 'Re-measure only after confirming every section still runs.' $F)" -eq 0
test "$($G -cF 'it MUST BE RE-MEASURED whenever a section or a case is added' $F)" -eq 0
test "$($G -cF 'were announced - A SECTION DID NOT RUN' $F)" -eq 0
test "$($G -cF 'R4-02' $F)" -ge 1
# the replacement names the reference environment rather than leaving it implicit
test "$($G -cF 'python3' $F)" -ge 2

# the two gates are still SEPARATE ARMS in the same order -- GC-12(b) exists because two failures
# once shared one banner, and this task must not merge them.
test "$($G -cF 'if [ "$ST_FAIL" -ne 0 ] || [ "$REDS" -ne 0 ]; then' $F)" -eq 1

bash -n $F

# --- THE DRIVES. All estate-free; --self-test contacts nothing. ---
# 1. green here
rc=0; OUT="$(mktemp -d)" bash $F --self-test >/dev/null 2>&1 || rc=$?
test "$rc" -eq 0

# 2. python3 absent -> STILL GREEN. This is the finding.
W="$(mktemp -d)"
/usr/bin/sed 's/command -v python3 >/command -v python3-absent-p6 >/' $F > "$W/nopy.sh"
! cmp -s $F "$W/nopy.sh"
rc=0; OUT="$(mktemp -d)" bash "$W/nopy.sh" --self-test > "$W/nopy.txt" 2>&1 || rc=$?
test "$rc" -eq 0
test "$($G -ciF 'SKIPPED: no python3' "$W/nopy.txt")" -ge 1

# 3. root -> STILL GREEN, both arms forced together
/usr/bin/sed 's/\[ "\$(id -u)" = "0" \]/[ 0 = 0 ]/g' $F > "$W/root.sh"
! cmp -s $F "$W/root.sh"
rc=0; OUT="$(mktemp -d)" bash "$W/root.sh" --self-test > "$W/root.txt" 2>&1 || rc=$?
test "$rc" -eq 0
test "$($G -ciF 'SKIPPED as root' "$W/root.txt")" -ge 2

# 4. THE NON-VACUITY CONTROL: a dropped section must STILL fail. Without this the three tests
# above are satisfied by a gate that no longer gates anything.
/usr/bin/sed '/^  self_test_fences$/d' $F > "$W/drop.sh"
! cmp -s $F "$W/drop.sh"
rc=0; OUT="$(mktemp -d)" bash "$W/drop.sh" --self-test > "$W/drop.txt" 2>&1 || rc=$?
test "$rc" -eq 1
test "$($G -cF 'were announced' "$W/drop.txt")" -ge 1

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-36-oracle-pin-parser-and-cleanup.txt
test "$($G -cF 'R4-02' "$ART")" -ge 1
test "$($G -ciF 'non-vacuity' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- At least three sites carry `ST_PLANNED_CASES=$((ST_PLANNED_CASES - …))`, each immediately beside
  the `warn` that reports its skip; exactly one line declares the base constant.
- `--self-test` exits **0** on the real file, **0** with `python3` ablated away, and **0** with both
  root arms forced — each printing its skip's `warn`.
- `--self-test` exits **1** and fires the announcement gate when `self_test_fences` is dropped from
  the dispatcher, and when one `st_case` is deleted from an unconditional section.
- The literals `Re-measure only after confirming every section still runs.`,
  `it MUST BE RE-MEASURED whenever a section or a case is added` and
  `were announced - A SECTION DID NOT RUN` are all gone; the replacement names at least three
  possible causes, says deliberate skips are no longer among them, and names the reference
  environment (non-root, python3 present) for re-measuring the base.
- The announcement gate and the `ST_FAIL`/`REDS` gate are still two separate arms in the same order.
- All five drives are recorded in the artifact with exit codes and gate lines, before and after.
- `bash -n` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the workstation's environment (root-ness, `python3` presence) → the self-test's verdict | round 3 made the verdict environment-dependent without noticing; the instrument that certifies the `rm -rf` receiving-side fences now reds on environments it was never measured in |
| operator env (`REAL_LIB_DB`, `REAL_STATE_PICKLE`) → `awk -v` | `-v` expands escape sequences in the value before the comparison |
| an unexercised guard → the one assertion proving `-l` kept the real `library.db` closed | the layer-3 parser has no case; a future edit to its regex, separator or field index goes green |
| an `exit 3` on a could-not-look → the container scratch and the LXC stamp | both survive the exit; the gap is not stated in band |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R402 | Denial of Service (a false red with a wrong stated cause, trained away by the operator) | `ST_PLANNED_CASES` and its gate | mitigate | the announcement adjusts at each skip site; the base is re-measured in a named reference environment; the message names three causes; a dropped-section control proves the gate still fires |
| T-06-R406 | Tampering (an undriven guard silently stops guarding) | the layer-3 sha256 line parser | mitigate | five behavioural cases plus an identity case pinning the driven text to the four live consumers, the identity case driven to FAIL by mutating a live site |
| T-06-R409 | Tampering (awk escape processing mangling an overridable path) | the four layer-3 consumers | mitigate | the path passed through `ENVIRON`; fails closed either way, and the backslash case is driven both directions |
| T-06-R410 | Repudiation (an unstated cleanup gap on could-not-look exits) | R3-04's two `exit 3` arms | mitigate | stated in band, graded as pre-existing, with the forensic rationale and what is left behind; **no trap added** |
| T-06-36-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- **No estate contact.** `--self-test` runs entirely on the workstation over `mktemp -d` fixtures:
  no `--run`, no `--arm`, no ssh, no docker. Every ablation is a scratch copy under `mktemp -d`;
  **`scripts/phase06-oracle.sh` itself is never mutated in place**, and `git status --porcelain --
  scripts/phase06-oracle.sh` must show only this plan's intended edit at the end of each task.
- Every self-test invocation sets `OUT` to a fresh `mktemp -d`, so no drive reads another's scratch
  and none writes to the default `${TMPDIR:-/tmp}/phase06-oracle`.
- `bash -n scripts/phase06-oracle.sh` parses clean after every task.
- **How these verify blocks are driven.** Before any edit, extract all three `<automated>` blocks,
  `bash -n` each, and run each against the unfixed tree: task 1 must fail at the `awk -v p=` absence
  test, task 2 at the `ENVIRON["p"] >= 5` test, task 3 at the `ST_PLANNED_CASES=$((…` presence test.
  None may reach its end. A block that passes pre-fix is vacuous and must be rewritten first.
  Record the three pre-fix transcripts in the artifact.
- Task 3 runs **last**. Task 2 deliberately leaves the real file's `--self-test` exiting 1 at the
  announcement gate; that is expected and is asserted rather than tolerated.
- No `git push`, no host `git pull`. `git diff --name-only -- scripts/` lists only
  `scripts/phase06-oracle.sh`.
- This plan must not touch `scripts/check-beets-config.sh` — plan 06-38 owns it this round and runs
  in parallel — nor `scripts/setup-neocortex-memory.sh`, `stacks/selfhosted/neocortex-memory/` or
  `stacks/selfhosted/agentic-os/`.
- **No requirement checkbox moves. CONF-04 is not closed and is not claimed. The phase is not
  declared complete.**
- `06-VERIFICATION.md` is **stale and out of scope**. Do not read it as a gap source and do not
  touch it.
</verification>

<success_criteria>
- R4-02 closed as code **and** claim: a deliberate skip is no longer a hard failure, a dropped
  section still is, and neither the gate's message nor the maintenance instruction asserts a single
  cause or an unconditional re-measure.
- R4-06 closed as code: the parser is driven positively and negatively inside the pinned set, and
  the driven text is pinned to the shipped text by a case that has been made to fail.
- R4-09 closed as code, driven in both directions on the backslash case.
- R4-10 closed as a claim correction, with **no trap added** and the class correctly graded as
  pre-existing.
- No number in this file states a count that this file's own text moves.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-36-SUMMARY.md` when done.
Include a **NOT-DRIVEN register** naming, at minimum, the layer-3 block itself — reachable only from
a live `--run`, which this phase does not perform, so the new cases prove the parser and not the
block — and state per finding whether the fix was CODE, CLAIM CORRECTION or BOTH. Record the three
measured skip deltas and the re-measured base explicitly, and say which environment the base was
measured in.
</output>
