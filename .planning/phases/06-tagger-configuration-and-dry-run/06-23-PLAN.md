---
phase: 06-tagger-configuration-and-dry-run
plan: 23
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_closure_round: 2
files_modified:
  - scripts/quick-health-check.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
closes_findings: [GC-03, GC-16]

must_haves:
  truths:
    - "The D-04 block cannot print a green tick over an empty ASSERTED set — CR-01 reproduced one nesting level in is closed, and the closure is driven, not read"
    - "The green line's own parenthesis can no longer read `0 of N`: the green condition itself requires a non-zero asserted count, so the tick and the number it quotes cannot disagree"
    - "The D-04 invocation pattern sees a BEET-variable invocation after a shell separator and after a `docker` prefix, exactly as it already sees the literal `beet` in those positions — the fourth branch no longer has a blind spot the other three do not"
    - "The widening is proven ADDITIVE on today's tree: raw / comment-stripped / invocation-shaped / executable / asserted / exempt / documentation are unchanged at 199 / 98 / 10 / 8 / 3 / 5 / 2, so no pin moves and no count is inflated to make a guard fire"
    - "The new fatal condition is recorded in the file's own exit-code notice convention, in the SAME COMMIT as the site that can reach it"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "the missing asserted-set vacuity guard, the strengthened green condition, the widened fourth regex branch, and the thirteenth exit-code notice"
      contains: "D04_N_ASSERT"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt"
      provides: "the driven reproduction of the green-over-nothing tick, its refusal after the fix, the widening's positive and negative controls, and the before/after count table"
  key_links:
    - from: "D04_INVOKE_ASSERT (the set the block actually iterates)"
      to: "the guard that refuses a vacuous assertion"
      via: "a D04_N_ASSERT -eq 0 arm in the same ladder as the D04_N_EXE guard"
      pattern: "D04_N_ASSERT. -eq 0"
---

<objective>
Close **GC-03**, this round's centrepiece, and **GC-16**, the blind spot in the same detector.

GC-03: plan 06-16 added condition **K**, a guard refusing a zero **executable** count. But the set
the block actually asserts over is `D04_INVOKE_ASSERT` = executable **minus exempt**, and nothing
guards *that* being zero. Delete or reshape the three `check-beets-config.sh` invocations and
`D04_N_EXE` becomes 5, `D04_N_EXEMPT` stays 5, `D04_N_ASSERT` becomes **0**. Guard K does not fire
(5 ≠ 0), the exempt pin does not fire (5 = 5), the doc pin does not fire, and
`while … <<< "$D04_INVOKE_ASSERT"` over an empty string runs its body **zero** times, so `D04_BAD`
stays 0 and the block prints
`✅ no ASSERTED beet invocation opens the real library (0 of 5 invocation-shaped lines outside *.md)`.
A green tick whose own parenthesis says it asserted over nothing. **That is CR-01 — the Critical
this phase's whole gap closure existed to close — reproduced one nesting level in.** Until it is
fixed, the phase's headline claim that CR-01 is closed is not true.

GC-16: the fourth branch of `D04_INV_RE` (the `BEET`-variable branch added by 06-16) anchors only
on content start or immediately inside an opening quote. The three *literal* `beet` branches in the
same regex **also** anchor on `[[:space:]](&&|;)[[:space:]]*` and on `docker[[:space:]]…`. So
`&& $BEET_BIN config` and `docker exec -u beetle … $BEET_BIN config` are invocation-shaped and
**invisible to the scan** — measured while planning: of four synthetic lines, the regex matched the
content-start `$BEET_BIN` form and the literal `&& beet` form, and missed both variable forms after
a separator and after `docker`. GC-03's own text names this as the silent route by which the
asserted set can drop to zero, which is why the two findings ship together.

**Read the correction before you write anything.** The cross-family reviewer graded GC-16 BLOCKER
and justified it by claiming the block comment promises separator anchoring. **That justification is
false** — the comment says "content start, or immediately inside an opening quote", which is exactly
what the code does. There is no documentation mismatch to fix. The substantive gap is real and is
graded WARNING: a latent blind spot with **no current instance in the tree**.

Purpose: a detector whose asserted set can silently empty is the same defect as a detector that
sees nothing, one level in; and a detector with a branch-asymmetric blind spot is how that set
empties without any counter moving off its pin. Both are in the one block, and fixing one without
the other leaves the route open.

Output: a guard that refuses a vacuous assertion, a green condition that cannot contradict its own
parenthesis, a fourth regex branch symmetric with its three siblings, a before/after count table
proving the widening moved nothing, and the thirteenth exit-code notice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refuse a vacuous ASSERTED set, and make the green condition unable to contradict its own count</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt</files>
  <read_first>
    - The D-04 verdict ladder. Locate it with
      `grep -n 'the invocation pattern matched NO executable line' scripts/quick-health-check.sh`
      and read from the `if [ "$D04_RAW" -eq 0 ]` arm down to the closing `fi` of the block —
      four guards (raw zero, comment-strip no-op, executable zero, then the `else`), the
      `while IFS= read -r d04_line … done <<< "$D04_INVOKE_ASSERT"` loop, the exempt pin, the doc
      pin and the green condition.
    - The counters. `grep -n 'D04_N_EXE=\|D04_N_EXEMPT=\|D04_N_ASSERT=\|D04_INVOKE_ASSERT=' scripts/quick-health-check.sh`.
      Note that all five counts are produced by `printf … | grep -c '^HEAD:'`, which returns **1**
      for a single blank line and **0** for genuinely empty input — read the pipeline carefully
      before assuming `D04_N_ASSERT` is 0 in the empty case, and if it is not, say so and guard on
      the condition that IS true.
    - The green condition and the line it prints. Locate with
      `grep -n 'no ASSERTED beet invocation opens the real library' scripts/quick-health-check.sh`
      and read the `if` immediately above it.
    - Condition K's wording, which the new arm must match in vocabulary. Locate with
      `grep -n 'A VACUOUS D-04 SCAN' scripts/quick-health-check.sh` and read conditions K and L in
      the eleventh notice in full.
    - The notice convention. `grep -n 'THIS IS THE .* SUCH NOTICE' scripts/quick-health-check.sh`
      and `grep -n '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh`. Read the
      eleventh and twelfth notices, including their self-referential `headers N -> N+1,
      raw M -> M+1` count lines — that convention is the one GC-10 shows being skipped elsewhere,
      so follow it here. **Measure the current ordinal; do not trust a number written in this
      plan.** At the time of writing there were 12 headers and the last named notice was the
      TWELFTH.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md` — GC-03 in full,
      including its suggested arm text and its belt-and-braces suggestion for the green condition.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md` — the CR-01 row and
      its "residue, carried not closed" paragraph. The residue stays carried; this plan closes the
      *nested* instance, it does not discharge ROADMAP entry criterion E10.
  </read_first>
  <action>
**(a) Add the missing arm.** Insert a fourth guard in the same ladder, immediately after the
`D04_N_EXE -eq 0` arm and before the `else`, refusing a zero **asserted** count. Its message must
say, in condition K's own vocabulary: every invocation-shaped executable line is EXEMPT (`N` of
`M`), nothing was asserted over, and **this is NOT "no bare beet invocations"**. Set `EXIT_CODE=1`.
Keep the `⚠️  UNKNOWN` prefix the three sibling guards use — this is a could-not-look, not a
measured failure, and the file's convention is that the two stay distinct.

**(b) Strengthen the green condition.** Add a requirement that the asserted count be greater than
zero to the conjunction guarding the ✅ line. This is belt and braces over (a) and it is worth the
line: the tick's own text quotes `$D04_N_ASSERT of $D04_N_EXE`, so with (b) in place the tick and
the number it quotes cannot disagree, whatever future ladder edit is made above it.

**(c) Record it as a new fatal condition, in the same commit.** Add the next numbered
`EXIT-CODE BEHAVIOUR CHANGED` notice at the top of the file, following the eleventh and twelfth
exactly: state that no block is added, name the new condition with the next free letter after L,
state what it refuses and why in two or three sentences, and include the measured
self-referential counts (`headers N -> N+1`, `raw M -> M+1`) taken **before and after** the edit
rather than assumed. State plainly that this is CR-01 reproduced one nesting level in and cite
`06-REVIEW-GAP.md` GC-03. Do **not** claim it discharges ROADMAP entry criterion E10.

**(d) Drive it, both ways, without touching the estate.** The block's inputs are all local after
the ssh returns, so extract the ladder's logic into a scratch harness rather than running the whole
health check: build a `D04_INVOKE_*` set from a synthetic `git grep`-shaped fixture and run the
ladder over it.
  - **Drive RED:** a fixture in which every invocation-shaped executable line matches
    `D04_EXEMPT_RE` — executable 5, exempt 5, asserted 0. Record that the **pre-fix** ladder prints
    the ✅ line reading `0 of 5`, and that the **post-fix** ladder prints the UNKNOWN refusal and
    sets `EXIT_CODE=1`. The pre-fix half is the whole point: a fix with no recorded reproduction of
    the defect is a fix nobody can check.
  - **Drive the PAIR:** the real tree's own set — executable 8, exempt 5, asserted 3 — must still
    reach the green condition after the fix. A guard that refuses everything invites its own
    removal, and this phase's convention (06-19) is that every new guard ships with a passing
    partner in the same transcript.
  - Also record what the counters actually are on an empty `D04_INVOKE_ASSERT`, measured rather
    than reasoned, since `grep -c` over a here-string of the empty string is the exact subtlety
    the `read_first` note flags.
Write all of it to `artifacts/06-23-d04-assert-vacuity.txt`.

**Do not run `git push`, and do not ask the host to `git pull`.** The host is at a pre-Phase-6
commit and deploying unmerged Phase 6 to the live estate to make a check line up is out of bounds.
The block correctly reports UNKNOWN against the live estate until the operator deploys; that is a
recorded deliberate non-finding (`06-DISPOSITIONS.md`, deliberate non-findings §2), not something
to clear here. If a live cross-check is wanted, use a **scratch checkout on the host** and point
`D04_REPO_ROOT` at it — noting that the override's additive contract means such a run can never be
green, which is correct and expected.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
# Baselines below were measured against the pre-fix file while this plan was written.
# 1. The vacuous-asserted-set guard exists. Today: 0.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'D04_N_ASSERT" -eq 0')" -ge 1
# 2. The green condition requires a non-zero asserted count. Today: 0.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'D04_N_ASSERT" -gt 0')" -ge 1
# 3. A new exit-code notice landed. Today: 12 headers.
test "$(grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh)" -eq 13
# 4. The refusal is an UNKNOWN, not a measured red — it uses the siblings' prefix.
test "$(grep -v '^[[:space:]]*#' scripts/quick-health-check.sh | grep -cF 'Nothing was asserted over')" -ge 1
# 5. The driven artifact exists, is non-trivial, and records BOTH directions.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt
test -s "$ART"
test "$(wc -c < "$ART")" -gt 2000
test "$(grep -ci 'GC-03' "$ART")" -ge 1
test "$(grep -cF '0 of 5' "$ART")" -ge 1
# 6. Parse clean.
bash -n scripts/quick-health-check.sh
    </automated>
  </verify>
  <done>
The D-04 ladder carries a fourth guard refusing a zero asserted count, and the green condition
requires the asserted count to be non-zero. `artifacts/06-23-d04-assert-vacuity.txt` shows the
pre-fix ladder printing `✅ … (0 of 5 …)` over an all-exempt fixture and the post-fix ladder
refusing it with `EXIT_CODE=1`, alongside the passing partner over the real 8/5/3 set. A new
numbered exit-code notice names the condition with measured before/after self-referential counts.
No `git push` was issued and the host was not asked to pull.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give the BEET-variable regex branch the same anchors its three literal siblings already have</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt</files>
  <read_first>
    - `grep -n 'D04_INV_RE=' scripts/quick-health-check.sh` — the whole regex on one line. Read the
      four alternatives separately before editing: (1) content start + optional `sudo` + literal
      `beet`; (2) `[[:space:]](&&|;)[[:space:]]*` + literal `beet`; (3) `docker[[:space:]][^`]*[[:space:]]`
      + literal `beet`; (4) `(content start + optional sudo | ")` + `$`-expanded `BEET[A-Z_]*` +
      a flag or a lowercase subcommand word.
    - The comment immediately above it, four lines beginning `# Invocation shape, on the content
      only:` and the `# FOURTH BRANCH added 2026-09-22 (plan 06-16)` paragraph. **The comment is
      accurate as written** — it says "content start, or immediately inside an opening quote", which
      is what the code does. Do not "fix" a documentation mismatch; there is none. Widening the code
      means the comment must be **updated to match the new behaviour**, which is a different edit.
    - `grep -n 'D04_EXEMPT_RE=' scripts/quick-health-check.sh` and the exempt/doc pins
      (`grep -n 'D04_EXEMPT_BASELINE\|D04_DOC_BASELINE' scripts/quick-health-check.sh`). A widening
      that moves either pin is a widening that changes the verdict, and must be reported rather than
      absorbed.
    - `grep -n 'the 26-vs-8 measurement' scripts/quick-health-check.sh` — the block comment's
      account of why BOTH halves of branch 4 are required (a command-position anchor *and* a
      following flag/subcommand). Preserve that property: widening the anchor set must not drop the
      trailing requirement, or the count goes back to 26.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md` — GC-16 and, in the
      cross-family adjudication section, the **orchestrator correction** recording that the
      reviewer's BLOCKER justification was false.
  </read_first>
  <action>
**(a) Widen the fourth branch's anchor set to match branches 1-3.** Add the shell-separator anchor
(`[[:space:]](&&|;)[[:space:]]*`) and the `docker` prefix anchor to branch 4's leading alternation,
so a `BEET`-variable expansion in command position is seen in the same three places a literal
`beet` already is. Keep branch 4's **trailing** requirement (a flag or a lowercase subcommand word)
exactly as it is — that half is what keeps the executable count at 8 rather than 26, and dropping
it while widening the anchor is how this edit turns into a regression.

**(b) Update the comment to describe what the code now does.** Rewrite the "content start, or
immediately inside an opening quote" clause to name all the anchors, and add one sentence saying
why the asymmetry mattered: the three literal branches already anchored on separators and `docker`,
so a `$BEET_BIN` invocation after `&&` or inside a `docker exec` line was invocation-shaped and
invisible — and GC-03 names exactly that as the silent route by which the asserted set can drop to
zero without any counter leaving its pin. Cite GC-16. Record in one clause that there was **no
current instance in the tree**, so this closes a latent blind spot rather than an active miss.

**(c) Prove the widening additive, by measurement.** Reproduce the block's whole pipeline locally
against this repository's `HEAD` — `git grep -n -I -w -E -e 'beet' -e 'BEET[A-Z_]*' HEAD --
scripts stacks`, then the comment strip, then `D04_INV_RE`, then the `.md` split, then
`D04_EXEMPT_RE` — with the **old** regex and with the **new** one, and record both count vectors
side by side. Measured while planning, the old vector is:

| count | value |
|---|---|
| raw | 199 |
| comment-stripped | 98 |
| invocation-shaped | 10 |
| executable | 8 |
| asserted | 3 |
| exempt | 5 |
| documentation | 2 |

The new vector must be **identical**. If any figure moves, stop and report it rather than raising a
pin to match — a pin raised to absorb a widening is the exemption-grows-unseen failure the exempt
baseline exists to prevent. (Note this table is also the evidence plan 06-28 needs for GC-10; put
it in the artifact in a form that can be quoted.)

**(d) Drive the widening, positively and negatively.** Over a synthetic fixture of `git grep`-shaped
lines, record: `&& $BEET_BIN config -d` and `docker exec -u beetle c $BEET_BIN config -d` are
**missed** by the old regex and **matched** by the new one; a content-start `$BEET_BIN config -d`
and a literal `&& beet config -d` are matched by both (the unchanged control); and a back-ticked or
in-string mention with no following flag or subcommand is matched by **neither** (the narrowing that
makes the stripped count meaningful is intact). Append to
`artifacts/06-23-d04-assert-vacuity.txt`.

No `git push`, no host `git pull`, no `--auto` anything.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt
# 1. The regex line still exists and is still a single assignment.
test "$(grep -c 'D04_INV_RE=' scripts/quick-health-check.sh)" -eq 1
# 2. The counts are unchanged — reproduce the block's pipeline with the file's OWN current regex.
INV_RE="$(sed -n "s/^[[:space:]]*D04_INV_RE='\(.*\)'$/\1/p" scripts/quick-health-check.sh)"
EX_RE="$(sed -n "s/^[[:space:]]*D04_EXEMPT_RE='\(.*\)'$/\1/p" scripts/quick-health-check.sh)"
test -n "$INV_RE"
test -n "$EX_RE"
git grep -n -I -w -E -e 'beet' -e 'BEET[A-Z_]*' HEAD -- scripts stacks > /tmp/06-23-hits.txt
grep '^HEAD:' /tmp/06-23-hits.txt > /tmp/06-23-raw.txt
grep -vE '^HEAD:[^:]*:[0-9]*:[[:space:]]*(#|//)' /tmp/06-23-raw.txt > /tmp/06-23-kept.txt
grep -E "$INV_RE" /tmp/06-23-kept.txt > /tmp/06-23-inv.txt || true
grep '\.md:' /tmp/06-23-inv.txt > /tmp/06-23-doc.txt || true
grep -v '\.md:' /tmp/06-23-inv.txt > /tmp/06-23-exe.txt || true
grep -E "$EX_RE" /tmp/06-23-exe.txt > /tmp/06-23-exempt.txt || true
grep -vE "$EX_RE" /tmp/06-23-exe.txt > /tmp/06-23-assert.txt || true
test "$(grep -c '^HEAD:' /tmp/06-23-raw.txt)" -eq 199
test "$(grep -c '^HEAD:' /tmp/06-23-kept.txt)" -eq 98
test "$(grep -c '^HEAD:' /tmp/06-23-inv.txt)" -eq 10
test "$(grep -c '^HEAD:' /tmp/06-23-exe.txt)" -eq 8
test "$(grep -c '^HEAD:' /tmp/06-23-exempt.txt)" -eq 5
test "$(grep -c '^HEAD:' /tmp/06-23-assert.txt)" -eq 3
test "$(grep -c '^HEAD:' /tmp/06-23-doc.txt)" -eq 2
# 3. The widened regex now SEES the two shapes it used to miss.
printf '%s\n' 'HEAD:scripts/x.sh:10:    cd /tmp && $BEET_BIN config -d' > /tmp/06-23-synth.txt
printf '%s\n' 'HEAD:scripts/x.sh:11:    docker exec -u beetle c $BEET_BIN config -d' >> /tmp/06-23-synth.txt
test "$(grep -cE "$INV_RE" /tmp/06-23-synth.txt)" -eq 2
# 4. The unchanged controls still match.
printf '%s\n' 'HEAD:scripts/x.sh:12:$BEET_BIN config -d' > /tmp/06-23-ctrl.txt
printf '%s\n' 'HEAD:scripts/x.sh:13:    cd /tmp && beet config -d' >> /tmp/06-23-ctrl.txt
test "$(grep -cE "$INV_RE" /tmp/06-23-ctrl.txt)" -eq 2
# 5. The artifact records the widening drive and the count table.
test "$(grep -ci 'GC-16' "$ART")" -ge 1
test "$(grep -cF '199' "$ART")" -ge 1
# 6. Parse clean.
bash -n scripts/quick-health-check.sh
    </automated>
  </verify>
  <done>
`D04_INV_RE`'s fourth branch anchors on content start, opening quote, shell separator and `docker`,
and still requires a following flag or lowercase subcommand. The block's whole pipeline reproduced
against `HEAD` yields 199 / 98 / 10 / 8 / 3 / 5 / 2 — identical to the pre-widening vector, so no
pin moved. The two previously-invisible shapes are matched, the two controls are unchanged, and a
mention with no following flag is still matched by neither. The comment describes the anchors the
code actually uses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository tree (LXC 100 checkout) → D-04 scan | untrusted text; the scan's verdict gates the claim that no `beet` invocation opens the real library |
| operator env → `D04_REPO_ROOT` / `D04_EXEMPT_BASELINE` / `D04_DOC_BASELINE` | additive knobs; each already forces `EXIT_CODE=1` on any non-default value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-GC03 | Repudiation (a green tick over an empty asserted set) | D-04 verdict ladder | mitigate | a fourth guard refusing a zero asserted count, plus the same requirement inside the green condition; both driven from a synthetic all-exempt fixture |
| T-06-GC16 | Spoofing (a real invocation hidden from the detector by position) | `D04_INV_RE` branch 4 | mitigate | anchor set widened to match branches 1-3; additivity proven by an unchanged seven-figure count vector |
| T-06-23-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
| T-06-23-DEPLOY | Elevation of privilege | deploying unmerged Phase 6 to the live estate to satisfy a check | mitigate | no `git push`, no host `git pull`; a host-side scratch checkout under `D04_REPO_ROOT` is the only permitted live route, and its additive contract keeps such a run non-green |
</threat_model>

<verification>
- `bash -n scripts/quick-health-check.sh` parses clean.
- The D-04 block's pipeline, reproduced locally against `HEAD` with the file's own current regexes,
  yields 199 / 98 / 10 / 8 / 3 / 5 / 2.
- The artifact contains a pre-fix transcript showing `✅ … (0 of 5 …)` and a post-fix transcript
  showing the refusal.
- `git diff --name-only -- scripts/` lists only `scripts/quick-health-check.sh`.
- No `git push`; the host was not asked to pull.
</verification>

<success_criteria>
- GC-03 closed: an all-exempt set is refused as UNKNOWN with `EXIT_CODE=1`, driven; the real 8/5/3
  set still reaches green, driven; the green condition itself requires a non-zero asserted count.
- GC-16 closed: branch 4 anchors on separators and `docker` as well as content start and opening
  quote, with the trailing flag/subcommand requirement intact, and the widening is proven additive
  by an unchanged count vector.
- The new fatal condition is recorded in a new numbered exit-code notice, with measured
  self-referential counts, in the same commit as the site that can reach it.
- Nothing in this plan claims to discharge ROADMAP entry criterion E10 or to close CONF-04.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-23-SUMMARY.md` when done.
Include a NOT-DRIVEN register for anything changed but not observed firing.
</output>
