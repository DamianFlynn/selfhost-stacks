---
phase: 06-tagger-configuration-and-dry-run
plan: 30
type: execute
wave: 14
depends_on: ["06-29"]   # round 2's last plan: these four fix code plans 06-22..06-28 wrote and 06-29 dispositioned
gap_closure: true
gap_closure_round: 3
files_modified:
  - scripts/quick-health-check.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt
autonomous: true
requirements: [CONF-03, CONF-06]
closes_findings: [R3-01, R3-05]

must_haves:
  truths:
    - "GC-17's census sentence states a number that grep agrees with, or states what it actually means — it no longer asserts as a measured fact a count that grep contradicts"
    - "The one surviving site that used the construction GC-17's own prohibition forbids — a hand-escaped `\\\"` wrapper around a path variable inside a remote `sh -c` — no longer uses it, and the path reaches the remote shell as a positional parameter"
    - "Every remaining overridable knob that crosses into a remote command string is rendered once with `printf '%q'` and only the rendered form is interpolated, so an override with a space degrades to a could-not-look rather than to a union of two directory scans"
    - "The thirteenth exit-code notice no longer asserts a constant delta that a later plan in the same round invalidated; it states a recipe the reader runs instead, written so the recipe does not count itself"
    - "The notice headers still number thirteen: this plan adds no fourteenth notice and moves no header count"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "a complete GC-17 census, the EXTCONF path passed positionally, and an exit-code notice whose own measurement is not stale"
      contains: "R3-01"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt"
      provides: "the before/after remote command strings captured (not executed) for each newly quoted knob, and the before/after grep measurements for the notice counts"
  key_links:
    - from: "EXTCONF_PATH (operator knob)"
      to: "docker exec sabnzbd sh -c"
      via: "a positional parameter, not an interpolated hand-escaped wrapper"
      pattern: "EXTCONF_PATH_Q"
---

<objective>
Close the two round-3 findings in `scripts/quick-health-check.sh`: **R3-01** and **R3-05**.

Both are the same defect in different clothing, and it is the defect this whole round exists to
stop: **round 2 closed a class partially and then wrote in band that it had closed it completely.**

**R3-01.** Plan 06-26's GC-17 comment says, as a measured fact, *"There are FOUR such sites in this
file and they were all raw"*. Grep says five more overridable knobs are still interpolated raw into
a string a remote shell parses: `D03_CLI_COMPOSE`, `D03_CLI_PROFILE`, `D03_FLASK_CONTAINER`,
`MUSIC_UNDERSCORE_ROOT` (two sites) and `EXTCONF_PATH`. Four of those five fail closed on a space —
`find` exits non-zero, `pipefail` fires, the existing could-not-look arm catches it — so they
degrade a knob from *drives the branch* to *cannot be driven*, which this file's own words call
"an undriveable branch is an unproven branch". The fifth is sharper. `EXTCONF_PATH` is wrapped in
hand-escaped double quotes inside a single-quoted remote `sh -c`, which is **verbatim the
construction the same comment block forbids sixty lines earlier**, and the block it feeds reads the
`requireBeetsMatch` guard — the one value standing between `audio.bash` and `rm -rf "$1"/*`.

The review is explicit that this is a WARNING and not a Critical: `EXTCONF_PATH` is an operator-set
environment knob with a safe default, not attacker-controlled input, and the injection consequence
is static reasoning that was **not executed**. Do not dramatise it and do not inflate it in the
comment you write.

**R3-05.** The thirteenth exit-code notice records, as a measurement taken across its own edit,
`headers 12 -> 13`, `raw 15 -> 16`, and "the raw count therefore still runs THREE ahead". Measured
now: headers **13**, raw **17**, delta **four**. The extra raw match was added later in the same
round by plan 06-26's GC-09 tail repair — the line that says a new notice was deliberately *not*
added, which in saying so added a raw match. The eighth notice institutionalised these two counts
as the file's own drift detector for exactly this problem, so a wrong stated baseline sends the
fourteenth notice's author to a false starting number.

**Say which fix you are making, per finding.** R3-01 is *both*: widen the code for five knobs
**and** correct the claim. R3-05 is a **claim correction only** — no behaviour changes, and the
honest form is to stop asserting a constant that has now drifted three times.

Output: five knobs rendered through `printf '%q'` and one of them passed positionally instead; a
census sentence that grep agrees with; and a notice that tells the reader what to run rather than
what the answer was in September.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish GC-17's census — quote the five surviving knobs, pass the EXTCONF path positionally, and correct the sentence that counted wrong</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt</files>
  <read_first>
    - **Do not trust any line number in this plan or in the review.** Every citation below is an
      anchor string. Resolve each with `/usr/bin/grep -n '<anchor>' scripts/quick-health-check.sh`
      before you edit. `.planning/ROADMAP.md` and this file have both moved under their own commits
      inside this phase, and round 2's citation-repair plan found nine stale references where the
      review named four.
    - The GC-17 comment block in full. Anchors:
      `EVERY OVERRIDABLE PATH THAT CROSSES INTO A REMOTE COMMAND STRING IS RENDERED ONCE WITH`,
      `There are FOUR such sites in this`, and the prohibition
      `DO NOT "fix" a future site of this shape by hand-escaping quotes inside the command string`.
      Read the whole block, including the bound it states ("all four are ADDITIVE knobs that cannot
      produce a green tick"), because your corrected sentence must keep that bound.
    - The existing `_Q` renderings, which are the shape to copy:
      `/usr/bin/grep -n '_Q=\$(printf' scripts/quick-health-check.sh` — four today
      (`DRIFT_REPO_ROOT_Q`, `D03_REPO_ROOT_Q`, `D04_REPO_ROOT_Q`, `CONSUMERS_SCRIPT_Q`).
    - The five knob definitions:
      `/usr/bin/grep -n 'D03_FLASK_CONTAINER=\|D03_CLI_COMPOSE=\|D03_CLI_PROFILE=\|EXTCONF_PATH=\|MUSIC_UNDERSCORE_ROOT=' scripts/quick-health-check.sh`.
      Each is a `${VAR:-default}` knob. **Render the `_Q` immediately after the definition**, as the
      four existing ones do — not at the use site, so a second future use cannot pick up the raw form.
    - The five remote sites. Anchors, all fixed strings:
      `docker inspect $D03_FLASK_CONTAINER --format`,
      `docker compose --profile $D03_CLI_PROFILE -f $D03_CLI_COMPOSE config`,
      `find $MUSIC_UNDERSCORE_ROOT -type d -name` (**two** hits — one under `set -o pipefail; … | wc -l`,
      one that lists the offending paths), and
      `cat \"$EXTCONF_PATH\"`.
    - The **non-remote** uses of the same variables, which must stay RAW: the override-detection
      `if [ "$D03_CLI_COMPOSE" != …` / `[ "$EXTCONF_PATH" != …` / `[ "$MUSIC_UNDERSCORE_ROOT" != …`
      guards, and every `echo` that prints a knob's value to the operator. Quoting those would print
      backslashes at the human. `/usr/bin/grep -n 'EXTCONF_PATH' scripts/quick-health-check.sh`
      returns roughly eight hits; exactly **one** of them is a remote command string.
    - The positional shape to copy, in the sibling file:
      `/usr/bin/grep -n 'remote_sh_c() {' scripts/phase06-oracle.sh` and its comment. The pattern is
      `sh -c '<program referring to "$1">' sh <argument>` — the program text carries no interpolation
      at all and the value arrives as `$1` inside the remote shell.
    - `06-REVIEW-GAP2.md` § R3-01 in full, including its table and its explicit refusal to grade the
      finding Critical.
  </read_first>
  <action>
**Disposition, stated up front and repeated in the comment you write: R3-01 is a CODE WIDENING AND A
CLAIM CORRECTION.** Five knobs get the treatment GC-17 described; the sentence that miscounted gets
replaced by one grep agrees with. Neither half alone closes this finding.

**(a) Render five `_Q` forms**, each immediately below its knob definition, in the existing shape
`VAR_Q=$(printf '%q' "$VAR")`: `D03_FLASK_CONTAINER_Q`, `D03_CLI_COMPOSE_Q`, `D03_CLI_PROFILE_Q`,
`MUSIC_UNDERSCORE_ROOT_Q`, `EXTCONF_PATH_Q`.

**(b) Swap the rendered form into the four straightforward remote sites** — the `docker inspect`
format call, the `docker compose --profile … -f … config` render, and **both** `find … -type d
-name '_*'` sites. Change nothing else on those lines. Leave every override-detection comparison and
every operator-facing `echo` on the raw variable.

**(c) The EXTCONF site is the one that must change shape, not just gain a `_Q`.** Today the path is
wrapped in hand-escaped double quotes inside a single-quoted remote `sh -c`. Replace it with the
positional form the oracle's `remote_sh_c` already uses, so the path is never interpolated into text
a shell parses:

  - the remote program text becomes a `sh -c` program that reads its path from `$1` — escaped so the
    *local* shell building the ssh string does not expand it;
  - the value is appended as `sh` followed by `$EXTCONF_PATH_Q`.

Verify by eye that the resulting string, read as the remote shell will read it, still reads the same
file when `EXTCONF_PATH` holds its default. **The consumer is unchanged** — this block parses
`extended.conf` for `requireBeetsMatch` and `conversion()`, and the parse is out of scope here.

⛔ **Do not reproduce the retired construction verbatim anywhere in the file**, including in a
comment describing what you removed. The verification below forbids that literal file-wide, which is
deliberate: a claim and its retraction are indistinguishable to grep, and round 2 shipped that exact
defect twice. If you want to describe what was removed, write *"a hand-escaped `\"` wrapper around
the path variable, inside a single-quoted remote `sh -c`"* — describe the shape, do not paste it.

**(d) Correct the census sentence.** `There are FOUR such sites in this` must go. Replace it with
either the re-measured total, or — better, and what the review suggests — the honest restatement:
*the four sites plan 06-26 owned*, plus a named list of the five plan 06-30 owned, plus the recipe a
future reader runs to re-derive the census rather than trusting a number (`/usr/bin/grep -nE
'^[A-Z0-9_]+="\$\{[A-Z0-9_]+:-'` cross-referenced against `/usr/bin/grep -n 'ssh -n \$SSH_OPTS'`).
Name **R3-01** in band. **Keep the existing prohibition paragraph exactly as it stands** — it was
right, it was simply not applied to the site three hundred lines below it — and add one clause
recording that a documented-forbidden instance survived in this same file for a round, which is why
the census is now a recipe and not a number.

Keep the bound the existing block states and do not inflate it: these are additive operator knobs
that cannot produce a green tick, the defaults are safe, and the injection consequence for
`EXTCONF_PATH` is static reasoning that was never executed.

**(e) Record the drive.** Into
`artifacts/06-30-qhc-census-and-counts.txt`, for each of the five knobs: set the knob to a value
containing a space (and for `EXTCONF_PATH` additionally one containing a `"`), **echo the remote
command string that would be sent, before and after the change, and do not run it.** Show the space
surviving as a single word after. This is a capture, not an execution: `quick-health-check.sh`
contacts LXC 100 and the Proxmox host, and **this plan runs it not once**.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
F=scripts/quick-health-check.sh
G=/usr/bin/grep   # absolute: the operator's zsh aliases grep -> ugrep, and a script's grep is BSD grep

# --- (a) five new renderings, on top of the four that already existed -> 9 ---
# Unanchored on purpose: one of the four pre-existing renderings is indented inside an `if`, so a
# `^`-anchored pattern measures 3 and not 4 and would make this threshold self-contradicting -- the
# exact defect (a threshold that disagrees with its own comment) that cost round 2 a plan.
test "$($G -cF '_Q=$(printf' $F)" -ge 9
# and each of the five is present by name, which is the assertion that actually matters
for v in D03_FLASK_CONTAINER_Q D03_CLI_COMPOSE_Q D03_CLI_PROFILE_Q MUSIC_UNDERSCORE_ROOT_Q EXTCONF_PATH_Q; do
  test "$($G -cF "$v=\$(printf '%q'" $F)" -eq 1
done

# --- (b) the four straightforward remote sites carry the rendered form and not the raw one ---
# Each absence pattern is a whole construction that is NOT a substring of its own replacement,
# because "_Q" intervenes before the next token. A bare variable-name test would substring-match
# the fix and fail the correct change; that exact defect cost round 2 a plan.
test "$($G -cF 'docker inspect $D03_FLASK_CONTAINER --format' $F)" -eq 0
test "$($G -cF 'docker inspect $D03_FLASK_CONTAINER_Q --format' $F)" -eq 1
test "$($G -cF 'docker compose --profile $D03_CLI_PROFILE -f $D03_CLI_COMPOSE config' $F)" -eq 0
test "$($G -cF 'docker compose --profile $D03_CLI_PROFILE_Q -f $D03_CLI_COMPOSE_Q config' $F)" -eq 1
test "$($G -cF 'find $MUSIC_UNDERSCORE_ROOT -type d -name' $F)" -eq 0
test "$($G -cF 'find $MUSIC_UNDERSCORE_ROOT_Q -type d -name' $F)" -eq 2

# --- (c) the forbidden construction is gone file-wide, including from any comment ---
test "$($G -cF 'cat \"$EXTCONF_PATH\"' $F)" -eq 0
test "$($G -cF 'EXTCONF_PATH_Q' $F)" -ge 2

# --- the raw knob still drives its NON-remote uses: override detection and operator echoes ---
test "$($G -cF '"$EXTCONF_PATH" != ' $F)" -ge 1
test "$($G -cF '"$MUSIC_UNDERSCORE_ROOT" != ' $F)" -ge 1

# --- (d) the census sentence is corrected and the prohibition survives ---
test "$($G -cF 'There are FOUR such sites in this' $F)" -eq 0
test "$($G -cF 'by hand-escaping quotes inside the command string' $F)" -ge 1
test "$($G -cF 'R3-01' $F)" -ge 1

# --- nothing structural moved: the notice headers are Task 2's business, not Task 1's ---
test "$($G -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' $F)" -eq 13

bash -n $F

# --- (e) the drive is captured, not executed ---
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt
test -s "$ART"
test "$($G -cF 'R3-01' "$ART")" -ge 1
test "$($G -cF 'EXTCONF_PATH' "$ART")" -ge 1
    </automated>
  </verify>
  <done>
Nine `_Q` renderings exist, five of them new and each sited immediately below its knob definition.
The four straightforward remote sites interpolate only the rendered form; every override-detection
comparison and operator echo still reads the raw value. The EXTCONF path reaches the remote shell as
a positional parameter and the hand-escaped wrapper appears nowhere in the file, not even quoted in
a comment. The census sentence no longer asserts "FOUR", names R3-01, keeps the prohibition
paragraph intact, and gives the reader a recipe rather than a number. The artifact holds the
before/after remote command string for each knob, captured by echo — `quick-health-check.sh` was
not run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retract the thirteenth notice's stale delta and replace it with a recipe that does not count itself</name>
  <files>scripts/quick-health-check.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt</files>
  <read_first>
    - The thirteenth notice in full. Anchor the header with
      `/usr/bin/grep -n 'THIS IS THE THIRTEENTH SUCH NOTICE' scripts/quick-health-check.sh`, then
      read from that header down to the end of the notice. The three lines to change are anchored by
      the fixed strings `headers   12 -> 13`, `raw       15 -> 16`, and
      `The raw count therefore still runs THREE ahead of the header count, unchanged by this edit.`
    - **Which instances are yours.** That last sentence appears **four** times in this file today
      (`/usr/bin/grep -cF` it). Only the one inside the thirteenth notice is in scope. The other
      three are earlier notices recording a measurement that was true when each was written; they are
      dated historical records and **you must not re-measure or rewrite them** — doing so would turn
      three accurate historical statements into three claims about a file state none of them
      describes.
    - The line that broke the invariant:
      `/usr/bin/grep -n 'NOT ADDED HERE, deliberately' scripts/quick-health-check.sh` — plan 06-26's
      GC-09 tail repair. Read it. It is correct and stays; it is merely the raw match nobody counted.
    - The eighth notice, which institutionalised the two greps as the file's own drift detector.
      Find it by reading upward from the thirteenth for the notice that describes the historic case
      where the raw grep returned 1 while the file held two notices.
    - `06-REVIEW-GAP2.md` § R3-05 in full — **including its closing paragraph**, which separately
      re-verified the notice's *other* measured claim (that the next free condition letter is `P`,
      because `M`, `N` and `O` are out of alphabetical order) and found it **correct**. That claim
      stays untouched. Re-litigating it is scope creep.
  </read_first>
  <action>
**Disposition: R3-05 is a CLAIM CORRECTION ONLY.** No behaviour changes, no code moves, no new
notice. Widening anything here to make the old number true would be the exact inversion this round
was called to stop.

**(a) Retract the constant-delta assertion in the thirteenth notice only.** Replace the three lines
carrying `headers   12 -> 13`, `raw       15 -> 16` and the "THREE ahead … unchanged by this edit"
sentence with:

  1. a one-sentence record that these counts were measured across plan 06-23's edit and were
     **invalidated later in the same round** by plan 06-26's tail repair — the line that says a new
     notice was deliberately not added, which in saying so added one raw match — so the stated delta
     of three became four without anyone touching the sentence that stated it;
  2. the statement that **the header count is the durable one and the raw count is not pinned
     anywhere**, for the same reason `06-DISPOSITIONS-GAP.md` records for the D-04 raw counts:
     writing a number into a file that greps itself moves that number;
  3. the two recipes to run, written **so that they do not count themselves**. Render the shared
     phrase in any in-band recipe with the final letter bracketed — `EXIT-CODE BEHAVIOUR CHANGE[D]`
     — which is a valid grep pattern matching the real occurrences while the text you just wrote is
     not itself an occurrence. Say in one clause why it is written that way, because the next reader
     will otherwise "fix" it.

Name **R3-05**. Do **not** add a fourteenth notice: this edit changes no exit code, no block count
and no condition, and the notice series exists for behaviour changes, not for corrections to its own
arithmetic. Record that decision in the same paragraph so the omission is not read as an oversight.

**(b) Leave the other three instances of the sentence alone**, and add one clause to the thirteenth
notice saying so explicitly: each earlier notice states the delta as of its own edit, each was
accurate then, and none has been re-measured — so a reader must not read those three as claims about
the file's present state.

**(c) Do not touch the condition-letter paragraph.** The review re-verified it independently and
found it correct (`A`–`L` ascending, then `O`, `M`, `N`, `P`), and it carries the warning against
counting forward from the last notice's highest letter, which is still the live hazard.

**(d) Record the measurement.** Append to `artifacts/06-30-qhc-census-and-counts.txt` the before and
after of both greps, run with `/usr/bin/grep` by absolute path, labelled with which is the header
count and which the raw. State plainly that the raw figure recorded there is a **dated observation
and not a pin**, and that the artifact is not a place the file reads from.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
F=scripts/quick-health-check.sh
G=/usr/bin/grep

# The header count is the durable one and this plan must not move it. It was 13 before this task
# and it is 13 after: no fourteenth notice, and the recipe form written in band cannot match here
# because a bracketed final letter is not the literal the header carries.
test "$($G -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' $F)" -eq 13

# The stale pair is gone from the thirteenth notice. These two literals exist nowhere else in the
# file, so an equality test is safe -- but note they are forbidden file-wide on purpose: a comment
# that quotes the retired numbers in order to disown them is indistinguishable, to grep, from a
# comment that still asserts them.
test "$($G -cF 'headers   12 -> 13' $F)" -eq 0
test "$($G -cF 'raw       15 -> 16' $F)" -eq 0

# The constant-delta sentence: four instances today, one of which is the thirteenth notice's.
# -le 3 rather than -eq 3 deliberately -- it proves the in-scope instance was retracted without
# pinning a number that a defensible wider reading of the brief would move.
test "$($G -cF 'The raw count therefore still runs THREE ahead of the header count, unchanged by this edit.' $F)" -le 3

# The replacement is a recipe written so it does not count itself.
test "$($G -cF 'EXIT-CODE BEHAVIOUR CHANGE[D]' $F)" -ge 1
test "$($G -cF 'R3-05' $F)" -ge 1

# The re-verified, correct claim is untouched: the condition letter paragraph still warns about P.
test "$($G -cF 'THE CONDITION LETTER IS P, NOT M' $F)" -eq 1

# Plan 06-26's tail repair -- correct, and the thing that broke the invariant -- still stands.
test "$($G -cF 'NOT ADDED HERE, deliberately' $F)" -ge 1

bash -n $F

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-30-qhc-census-and-counts.txt
test "$($G -cF 'R3-05' "$ART")" -ge 1
    </automated>
  </verify>
  <done>
The thirteenth notice no longer asserts `headers 12 -> 13`, `raw 15 -> 16` or a constant delta of
three. In their place it records that plan 06-26 invalidated the measurement later in the same
round, states that the header count is durable while the raw count is deliberately unpinned, and
gives both recipes with the shared phrase written so the recipe is not itself a match — with one
clause saying why, so nobody unbrackets it. No fourteenth notice was added and that omission is
recorded as a decision. The three earlier instances of the sentence are untouched and are explicitly
labelled as dated. The condition-letter paragraph is byte-unchanged. The artifact records both
greps before and after, and labels the raw figure a dated observation rather than a pin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (`EXTCONF_PATH`) → remote `docker exec sabnzbd sh -c` | the sharp one: the block it feeds reads the `requireBeetsMatch` guard standing between `audio.bash` and `rm -rf "$1"/*` |
| operator env (`D03_CLI_COMPOSE`, `D03_CLI_PROFILE`, `D03_FLASK_CONTAINER`, `MUSIC_UNDERSCORE_ROOT`) → remote command strings | fail-closed today; the failure mode is an undriveable knob, plus a union-of-two-scans case where both split words name real directories |
| the file's own prose → the file's own drift detector | a notice asserting a count that a later plan moved; the detector's baseline is its own text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R301 | Tampering (remote command re-parsing) | the five surviving raw interpolations in `quick-health-check.sh` | mitigate | `printf '%q'` at four sites; the EXTCONF path passed as `sh -c`'s positional parameter so it is never parsed as command text; drives captured by echo, never executed |
| T-06-R301b | Repudiation (a comment asserting a class is closed while an instance survives) | the GC-17 census sentence | mitigate | the number replaced by a recipe plus the named list of sites each plan owned |
| T-06-R305 | Repudiation (a drift detector carrying a false baseline) | the thirteenth exit-code notice | mitigate | the constant delta retracted; the recipe written so it does not count itself; the raw count explicitly unpinned |
| T-06-30-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- `bash -n scripts/quick-health-check.sh` parses clean after every task.
- **`scripts/quick-health-check.sh` is NOT executed by this plan, not once.** It contacts LXC 100
  (172.16.1.159) and the Proxmox host (172.16.1.158). Every drive in this plan is a captured command
  string, produced by `echo`.
- No `--run`, no `--arm`, no `git push`, no host `git pull`.
- `git diff --name-only -- scripts/` lists only `scripts/quick-health-check.sh`.
- This plan must not touch `scripts/setup-neocortex-memory.sh`,
  `stacks/selfhosted/neocortex-memory/` or `stacks/selfhosted/agentic-os/` — other sessions commit
  to `main` in this same checkout.
- No requirement checkbox moves. CONF-04 is not closed. The phase is not declared complete.
</verification>

<success_criteria>
- R3-01 closed, as a code widening **and** a claim correction, with both halves visible: five knobs
  rendered through `printf '%q'`, the EXTCONF path passed positionally, and a census sentence that
  grep agrees with.
- The construction GC-17's own prohibition forbids appears nowhere in the file, including in prose
  describing its removal.
- R3-05 closed as a claim correction only: no behaviour change, no fourteenth notice, no header-count
  movement, and a recipe that does not count itself.
- The three earlier delta sentences and the re-verified condition-letter paragraph are untouched.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-30-SUMMARY.md` when done.
Include a NOT-DRIVEN register naming, at minimum, every branch reachable only from a live
`quick-health-check.sh` run against the estate — which is all of them, since this plan executes the
script zero times.
</output>
