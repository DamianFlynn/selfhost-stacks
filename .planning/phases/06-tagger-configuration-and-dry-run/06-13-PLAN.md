---
phase: 06-tagger-configuration-and-dry-run
plan: 13
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - scripts/check-music-consumers.sh
  - stacks/selfhosted/arrs/beets.md
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt
autonomous: false
requirements: [CONF-04]
must_haves:
  truths:
    - "The Music Assistant arm is planned and written in full, but NOT executed without operator confirmation that MA is back online — the task stops and asks (D-36)"
    - "If MA never returns, CONF-04 stays OPEN. It must never read as passed on an unreachable consumer"
    - "MA unreachable is reported as *could not look*, distinctly from *nothing is wrong*, using the literal house string `UNKNOWN, not green` and `ma_fail` — never `warn` and never the out-of-scope branch"
    - "Jellyfin was proven FIRST and Music Assistant LAST (D-25), because MA never purges stale entries and a rolled-back write would leave a phantom in its database forever"
    - "'Parsed correctly' means N distinct browseable artist entities in MA's OWN API, checked independently of Jellyfin (D-22)"
    - "DJ content IS visible in Music Assistant — no narrowing of MA's provider path (D-17), because the operator DJs from this material and cannot build crates from untagged tracks"
    - "Research assumption A2 — that `music/tracks/library_items` is the command carrying `artists[]` — is RESOLVED against the live server's own `commands.json` before being relied on, not assumed"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt"
      provides: "the MA artist-entity read-back, or the recorded could-not-look with CONF-04 left OPEN"
  key_links:
    - from: "scripts/check-music-consumers.sh"
      to: "Music Assistant /api"
      via: "ma_api, JWT via -H @<(printf ...), never argv"
      pattern: "UNKNOWN, not green"
---

<objective>
Close criterion 4's second half — or record, precisely and without ambiguity, that it could not be closed.

Purpose: MA (`8095`) and Home Assistant (`8123`) on `172.16.1.31` were both measured closed twice on 2026-09-20 because the host is down for maintenance — the operator's own statement, not an inference from a probe. Deferring this half to Phase 7 was rejected: Phase 7 is the phase that first writes at scale, and discovering an MA parsing failure there is precisely the late discovery Phase 6 exists to prevent. So the check is written in full, gated two ways, and CONF-04 stays OPEN rather than passing if MA never returns.

Output: the MA half of the D-22 check exercised against the live server, or an explicit could-not-look record. Either way the result is written into `beets.md` and the standing assertion stays in `check-music-consumers.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-PATTERNS.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-03-SUMMARY.md
@scripts/check-music-consumers.sh
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator gate — is Music Assistant back online?</name>
  <decision>Whether to execute the Music Assistant half of criterion 4 now, or leave CONF-04 OPEN and close the phase without it.</decision>
  <context>
D-36 makes this a stop-and-ask, not a retry loop. MA on `172.16.1.31:8095` and Home Assistant on `:8123` were both probed closed twice on 2026-09-20; the host is down for maintenance by the operator's own statement. A check that runs blind against a down service produces a red that means "could not look", and this project's standing rule is that "could not look" must never be folded into a verdict.

Two further facts bear on the choice. MA **never purges stale entries** — that is Phase 2's founding premise — so anything MA indexes it keeps; and this phase's primary route (D-20, answered offline) means both consumers already hold the candidate tracks, so on this route the phantom risk is nil. Nothing here writes to the library.

Before answering, note what the automated probe will report either way: a reachability assertion runs first, and an unreachable MA produces `ma_fail "… UNKNOWN, not green"`, never a pass and never a `warn`.
  </context>
  <options>
    <option id="run-now">
      <name>MA is back online — run the check</name>
      <pros>CONF-04 closes in Phase 6, which is where the cheap discovery belongs. The parsing behaviour is confirmed against 2.11.0b0 rather than read from 2.10.4 source.</pros>
      <cons>None material on the D-20 primary route: both consumers already hold the tracks and nothing is written.</cons>
    </option>
    <option id="leave-open">
      <name>MA is still down — leave CONF-04 OPEN</name>
      <pros>Honest. The phase closes with a named open item rather than a verdict resting on an unreachable consumer.</pros>
      <cons>Phase 7 inherits the risk it was supposed to be spared. The open item must be carried explicitly into the Phase 7 entry criteria, not just noted.</cons>
    </option>
    <option id="retry-later">
      <name>Hold this plan and retry after the maintenance window</name>
      <pros>Keeps CONF-04 closable in Phase 6 without running blind.</pros>
      <cons>Blocks phase closure on an external schedule.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md § D-36 and § D-25 — the gate and the ordering, and why deferring to Phase 7 was rejected.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "Music Assistant" (lines 711-744) — the measured outage, `MA_URL`, the 2.10.4 source read, and the `mb_id_count == 1` short-circuit.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-VALIDATION.md § Manual-Only Verifications — the row that makes this task `autonomous: false`.
  </read_first>
  <acceptance_criteria>
    - A reachability probe of `172.16.1.31:8095` is run and its result recorded BEFORE the question is asked, so the operator answers against a current measurement rather than a three-day-old one.
    - The operator's answer is recorded verbatim in the plan summary, with the option id.
    - If the answer is `leave-open` or `retry-later`, task 2 does not run and task 3 records CONF-04 as OPEN with the reason and the date.
  </acceptance_criteria>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt</files>
  <action>
Before asking the question, take a CURRENT reachability measurement and put it in front of the operator — the last one is days old and the maintenance window may have closed.

Probe, from LXC 100 and bounded Linux-side (`set -o pipefail` inside the remote command string, ssh RC read on the very next line with no local pipe in front of it, RC 124 branched out first as *could not look*): TCP `172.16.1.31:8095` (Music Assistant) and TCP `172.16.1.31:8123` (Home Assistant), plus ICMP to `172.16.1.31` so "host down" and "service down" stay distinguishable — at research time the host answered ICMP while both ports were closed, and those are different situations. Record all three results with a UTC timestamp into the header of `artifacts/06-13-ma-artist-entities.txt`.

Then present the three options with that measurement attached, and STOP. Do not proceed to task 2 on a probe result alone: D-36 makes this an operator decision, not a retry loop, because the host being reachable is not the same as the operator being ready for it to be exercised.

Record the operator's answer verbatim, with its option id, in the same artifact.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt; test -s "$f" || { echo "gate artifact missing"; exit 1; }; grep -q "8095" "$f" || { echo "the MA reachability probe is not recorded"; exit 1; }; grep -q "8123" "$f" || { echo "the HA probe is not recorded"; exit 1; }; grep -qiE "icmp|ping" "$f" || { echo "host-down and service-down are not distinguished"; exit 1; }; grep -qE "run-now|leave-open|retry-later" "$f" || { echo "the operator answer and its option id are not recorded"; exit 1; }; echo "D-36 gate recorded"'</automated>
  </verify>
  <done>
    A current three-part reachability measurement (MA 8095, HA 8123, ICMP) carrying a UTC timestamp is recorded in the artifact header, the operator was shown it, and their answer is recorded verbatim with its option id. Execution stopped at this task until that answer was given.
  </done>
  <resume-signal>Select: run-now, leave-open, or retry-later</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve A2, then read back N distinct artist entities from MA</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt, scripts/check-music-consumers.sh</files>
  <read_first>
    - scripts/check-music-consumers.sh lines 371-395 — `ma_login`, and the CR-01 note recording the corrected argv defect: a comment asserting a control that is not implemented is worse than no comment, because it stops the next reader looking.
    - scripts/check-music-consumers.sh lines 397-412 — `ma_api`, and why the bearer JWT reaches curl through `-H @file` with a process substitution: argv carries only the path, the token never touches disk, and `printf` is a bash builtin so it has no argv of its own.
    - scripts/check-music-consumers.sh lines 1021-1056 — `ma_fail "… UNKNOWN, not green"`. This is the branch D-36 requires; lines 973-982's `warn` out-of-scope branch is the WRONG precedent and must not be reused.
    - scripts/check-music-consumers.sh — the MA gate body plan 06-03 task 3 added, which this task completes.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § "Music Assistant" (lines 711-744) — `TAG_SPLITTER = ";"`, the `artists` property's preference order, the `mb_id_count == 1` short-circuit, that responses are **not** `.result`-wrapped, and that `music/albums/count` **silently ignores** its `provider` argument.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § Assumptions Log A1 and A2 (lines 1006-1007) — A1 (2.11.0b0 matches the 2.10.4 read) and A2 (the command name), and that `GET /api-docs/commands.json` is this script's own stated authority.
    - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-before.txt — the pinned `ARTISTS` proof rows and their expected N, so MA is checked on the same subjects Jellyfin was.
  </read_first>
  <action>
Run only if task 1 answered `run-now`.

**Resolve A2 before relying on it.** Fetch `GET /api-docs/commands.json` from the live MA server — the script's own stated authority — and find the command whose objects carry an `artists[]` list per track. Research inferred `music/tracks/library_items` from the album-side command's shape and explicitly did **not** confirm it. Record the resolved command name and whether A2 was CONFIRMED or FALSIFIED. If falsified, record the real command and correct the comment in `check-music-consumers.sh` in the same commit — a stale comment naming a command that does not exist is the defect class CR-01 already caught in this file once.

While there, re-confirm A1 against the running 2.11.0b0 rather than the 2.10.4 source: check that the server still prefers the `ARTISTS` tag and splits on `;`. If the behaviour has moved, that is a finding and CONF-04's basis changes.

**Authenticate the way this script already does.** MA 2.11 beta has no token UI: username + password produces a 90-day JWT and you log in per run. The password must never enter argv — pass it through the environment into `jq` as `$ENV.MA_PASSWORD`, exactly as `ma_login` does — and the JWT must reach curl only through `-H @<(printf …)`. `/proc/<pid>/cmdline` is world-readable on LXC 100. Credentials come from `/mnt/fast/secrets/ma-deercrest.env`, gated on mode `600 root` and REFUSED rather than sourced if it is not, and sourced without `set -a`.

**The D-22 read-back.** For each pinned `ARTISTS` proof row from plan 06-03 — the 4-artist Lady Gaga track, the 2-artist Katy Perry track, and the P!nk row — query MA for that track and assert `artists | length == N` **and** that the entity identifiers are distinct. Not one artist literally named `Lady Gaga;T.I.;Too $hort;Twista`, and not a count that merely matches: Phase 3 measured 4 tracks written onto entirely different songs with counts looking plausible, which is why identity, not arity alone, is the assertion.

**Record the `mb_id_count == 1` caveat with a measurement, not a shrug.** A single MusicBrainz artist id suppresses splitting entirely in MA, so a track beets tags with one `mb_artistid` and a `;`-joined name reads as one artist. Record whether each proof row carries one or several `musicbrainz_artistids`, because that determines whether the row is a valid test of the splitter at all.

**D-17 — DJ content is visible, and the provider path is not narrowed.** Assert MA's filesystem provider still covers the whole library path with no exclusion of the DJ tree. Record the provider instance id explicitly (`filesystem_local--XJaJWNUS` at last measurement) and use `music/…/library_items` with that explicit provider — `music/albums/count` silently ignores its `provider` argument and will report green off a completely different source. The operator DJs from this material with Traktor, Algoriddim djay and Rekordbox and cannot build crates from untagged tracks; hiding the DJ collection from MA is not an option on the table.

**Do not write anything to the library.** MA never purges stale entries, so nothing this task does may cause MA to index a file that will later be removed. On the D-20 primary route both consumers already hold the candidate tracks, so no write is needed — assert that no file under `/mnt/tank/media` changed by comparing a `LC_ALL=C find`-based manifest over the three proof albums before and after.

Write the full transcript, the A1/A2 verdicts, the per-row results and the provider assertion to `artifacts/06-13-ma-artist-entities.txt`.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt; test -s "$f" || { echo "MA artifact missing"; exit 1; }; if grep -qi "leave-open\|retry-later" "$f"; then grep -qi "CONF-04.*OPEN" "$f" || { echo "gate declined but CONF-04 is not recorded OPEN"; exit 1; }; grep -qi "UNKNOWN, not green" "$f" || { echo "the could-not-look record does not use the house string"; exit 1; }; echo "MA arm deferred; CONF-04 recorded OPEN"; exit 0; fi; grep -qiE "A2.*(CONFIRMED|FALSIFIED)" "$f" || { echo "A2 not resolved against commands.json"; exit 1; }; grep -qiE "A1.*(CONFIRMED|FALSIFIED)" "$f" || { echo "A1 not re-confirmed against 2.11.0b0"; exit 1; }; grep -qi "artists" "$f" || exit 1; grep -qiE "musicbrainz_artistid|mb_id_count" "$f" || { echo "the mb_id_count short-circuit caveat is not measured per row"; exit 1; }; grep -qi "filesystem_local" "$f" || { echo "the explicit provider instance is not recorded — albums/count silently ignores its provider arg"; exit 1; }; grep -qiE "unchanged|identical" "$f" || { echo "the library-untouched manifest verdict is missing"; exit 1; }; bash -n scripts/check-music-consumers.sh || exit 1; s=$(grep -v "^[[:space:]]*#" scripts/check-music-consumers.sh); printf "%s" "$s" | grep -q "ma_fail" || exit 1; printf "%s" "$s" | grep -q "UNKNOWN, not green" || exit 1; echo "MA arm executed and recorded"'</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api-docs/commands.json` was fetched from the live server and A2 carries an explicit CONFIRMED or FALSIFIED verdict; if FALSIFIED, the real command name is recorded and the script's comment is corrected in the same commit.
    - A1 carries a verdict against the running 2.11.0b0, not against the 2.10.4 source read.
    - Each pinned proof row reports `artists | length` with distinct entity identifiers, and its `musicbrainz_artistids` count, so a `mb_id_count == 1` short-circuit cannot be mistaken for a parsing failure.
    - The MA provider instance id is named explicitly and `music/albums/count` is not used as evidence for anything.
    - A `LC_ALL=C find`-based manifest over the three proof albums is identical before and after.
    - The MA password never appears in argv and the JWT reaches curl only via `-H @<(printf …)`.
  </acceptance_criteria>
  <done>
    Either the MA read-back ran and every acceptance criterion above is evidenced in the artifact, or task 1 declined the gate and this task did not run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the CONF-04 verdict — closed, or OPEN with its reason</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md § "Phase 6 — tagger configuration and dry run (2026-09-20)" — the section plan 06-03 opened and plan 06-10 added to.
    - stacks/selfhosted/arrs/beets.md lines 1252-1272 — the criteria table with its evidence column and the dated block-quote convention for a superseded disposition.
    - stacks/selfhosted/arrs/beets.md lines 942 and 1074 — the `### Still open` standing-action list.
    - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt — the result to record.
    - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-after.txt — the Jellyfin half, so the row records both consumers side by side.
  </read_first>
  <action>
Write criterion 4's verdict into `stacks/selfhosted/arrs/beets.md`, in the Phase 6 section, with both consumers on the same row and the evidence a runnable command in this file's existing style.

**If the MA arm ran and passed:** record CONF-04 as discharged, naming both consumers, both entity counts per proof row, the resolved MA command name, the A1/A2 verdicts, and the D-34 Jellyfin library option with its before and after values. State plainly what was proven: Jellyfin and MA read the **same field** (`ARTISTS`) by construction rather than by coincidence, and the `;` in the requirement text is a property of the existing library's twelve files, not of anything the pipeline will produce.

**If the MA arm did not run:** record CONF-04 as **OPEN**, with the date, the reason (MA and HA on `172.16.1.31` down for maintenance; probed closed on the stated dates), and the exact remaining step. Put it in the `### Still open` list **and** in the criteria table's row — a limit recorded where nobody reads it has not been recorded. State explicitly that the Jellyfin half passed and the MA half is *could not look*, and that the two must not be summed into a verdict. Carry it into Phase 7's entry criteria as a named blocker rather than a note.

Either way, record the two standing risks this criterion leaves behind:
- Every `%aunique{}` firing from plan 06-11's list is a **known MA risk to verify in Phase 7**: MA's `missing_album_artist_action: folder_name` fires only when the album folder and the album tag agree, and silently falls back to `Various Artists` when they do not — while `config/providers/get` still reads back `folder_name`.
- The D-34 library option is live-service state that **git does not capture**, held in place only by the assertion in `check-music-consumers.sh`. Name that dependency.

Do not edit any earlier phase's section. Phase 6 adds its own.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=stacks/selfhosted/arrs/beets.md; grep -q "Phase 6 — tagger configuration and dry run" "$f" || exit 1; grep -q "CONF-04" "$f" || { echo "the criterion 4 verdict is not recorded"; exit 1; }; grep -qi "Music Assistant" "$f" || exit 1; grep -q "PreferNonstandardArtistsTag" "$f" || exit 1; grep -qi "aunique" "$f" || { echo "the %aunique{} MA risk is not carried to Phase 7"; exit 1; }; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-13-ma-artist-entities.txt; if grep -qi "CONF-04.*OPEN" "$a"; then grep -qiE "CONF-04.*OPEN|OPEN.*CONF-04" "$f" || { echo "MA deferred but beets.md does not record CONF-04 OPEN"; exit 1; }; grep -qi "Still open" "$f" || exit 1; fi; echo "CONF-04 verdict recorded"'</automated>
  </verify>
  <done>
    `beets.md`'s Phase 6 section carries criterion 4's verdict with both consumers on one row, the D-34 option with before/after values and its git-does-not-capture dependency, and the `%aunique{}` firing list as a named Phase 7 MA risk. If the MA arm did not run, CONF-04 appears as OPEN in both the criteria table and the `### Still open` list, with the reason, the date and the exact remaining step, and it is carried into Phase 7's entry criteria as a blocker. No earlier phase section was edited.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Music Assistant API | Username + password produce a 90-day JWT; MA 2.11 beta has no token UI, so credentials are handled on every run. |
| script argv → LXC 100 process table | `/proc/<pid>/cmdline` is world-readable on LXC 100 (no hidepid). This exact defect existed in `ma_login` until 2026-09-01. |
| MA index → permanence | MA **never purges stale entries**. Anything it indexes, it keeps — a ZFS rollback does not reach it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-66 | Information Disclosure | MA password or JWT in argv on a host with a world-readable process table | mitigate | Password passed to `jq` via `$ENV.MA_PASSWORD`; JWT via `-H @<(printf …)`; credential file gated on `600 root` and REFUSED rather than sourced; no `set -a`. |
| T-06-67 | Spoofing | An unreachable MA reported as `warn` or as out-of-scope, letting CONF-04 read as passed | mitigate | `ma_fail` with the literal house string `UNKNOWN, not green`; the verify command asserts CONF-04 is recorded OPEN whenever the gate is declined. |
| T-06-68 | Tampering | A phantom track permanently indexed in MA by a write that is later rolled back | mitigate | Nothing is written: the D-20 primary route means both consumers already hold the candidates, and a `LC_ALL=C find` manifest over the three proof albums is asserted identical before and after. D-21's fenced write does not fire. |
| T-06-69 | Spoofing | `music/albums/count` reporting green off Spotify with no mount, because it silently ignores its `provider` argument | mitigate | The explicit provider instance id is named and `library_items` is used; `albums/count` is excluded as evidence by name. |
| T-06-70 | Spoofing | A `mb_id_count == 1` short-circuit read as a parsing failure, or a passing row that never exercised the splitter | mitigate | Each proof row's `musicbrainz_artistids` count is recorded, so a row that cannot test the splitter is visible as such. |
| T-06-71 | Repudiation | Research assumption A2 relied on without confirmation, leaving a standing check that queries a command that may not exist | mitigate | Resolved against the live server's own `commands.json` with a CONFIRMED/FALSIFIED verdict; a falsification corrects the script's comment in the same commit. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nothing is installed; `curl`, `jq` and `ssh` are existing toolchain preconditions gated by the script's section 0. |
</threat_model>

<verification>
- Task 1's gate is answered by the operator and the answer is recorded with its option id.
- Task 2's verify exits 0 on either branch — executed with all criteria evidenced, or deferred with CONF-04 recorded OPEN in the house string.
- `bash -n scripts/check-music-consumers.sh` passes and the script still carries `ma_fail` and `UNKNOWN, not green`.
- `beets.md` carries the criterion 4 verdict.

</verification>

<success_criteria>
- The MA arm either ran against a live server with A1 and A2 both resolved, or did not run and CONF-04 is explicitly OPEN — never a verdict resting on an unreachable consumer.
- D-22's "N distinct entities per consumer, checked separately" holds for MA on the same proof rows Jellyfin used.
- D-17 holds: MA's provider path is not narrowed and DJ content stays visible.
- Nothing under `/mnt/tank/media` was written, so MA holds no phantom.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-13-SUMMARY.md` when done.
</output>
