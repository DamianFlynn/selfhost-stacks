---
phase: 06-tagger-configuration-and-dry-run
plan: 14
type: execute
wave: 5
depends_on: ["06-06", "06-08", "06-10", "06-11", "06-12", "06-13"]
files_modified:
  - stacks/selfhosted/arrs/beets.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, CONF-06]
must_haves:
  truths:
    - "All five ROADMAP criteria are RE-MEASURED from live state at close, not carried forward from plan summaries"
    - "Criterion 3's instrument is recorded as `beet move -p`, not `--pretend`, with the D-33 addendum cited — the ROADMAP criterion text is NOT rewritten"
    - "Criterion 4 is recorded honestly: Jellyfin proven, and Music Assistant either proven or explicitly OPEN. The two are never summed into a verdict"
    - "Criterion 5's 'run confirmed to have written nothing' is discharged by the three-layer D-29 proof, not by a file count — Phase 1 measured a path-set comparison passing while content changed underneath"
    - "Every carried-forward item is registered where the phase that owns it will read it: the OQ-2 DJ-routing mechanism and the `bootleg` album-populated gate into Phase 7; the folder-count lag and the Discogs-id shortcut into Phase 9; the `Def Leppard` repair into Phase 7 (D-19a)"
    - "`tank/downloads@pre-phase5` is NOT released — Phase 6 wrote nothing, so it produced no evidence that Phase 5's changes were correct (D-32)"
    - "CONTEXT.md's 'six criteria' versus the ROADMAP's five is recorded as cosmetic; no phantom sixth criterion is invented (C-7)"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "the Phase 6 closure: criteria table with runnable evidence, the still-open list, and the carried-forward register"
      contains: "Phase 6"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-01..CONF-06 traceability moved off Pending with their evidence"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "stacks/selfhosted/arrs/beets.md § Phase 6"
      via: "the plans list and the criteria annotations"
      pattern: "Phase 6"
---

<objective>
Close the phase on re-measured evidence, and hand the next phases exactly the items they need, in the files they will actually read.

Purpose: this project's failure mode is not a wrong measurement, it is a correct measurement recorded where nobody looks, or a criterion ticked from a plan summary rather than from live state. Phase 5 closed by re-measuring all four criteria; Phase 6 does the same for five. And the three things Phase 7 must inherit — the DJ-routing mechanism gap, the `bootleg` album-populated gate, and the `Def Leppard` repair — are unowned unless they are written into its entry criteria here.

Output: `beets.md`'s Phase 6 closure, the CONF traceability rows, and the ROADMAP's plans list and criteria annotations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md
@stacks/selfhosted/arrs/beets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure all five criteria from live state and write the Phase 6 closure into beets.md</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 858-887 — the Phase 6 entry: the goal, the requirement list, and the FIVE numbered success criteria, quoted exactly as written so the closure answers the criteria rather than a paraphrase.
    - stacks/selfhosted/arrs/beets.md lines 1252-1272 — the Phase 4 criteria table: an evidence column holding a RUNNABLE command quoted verbatim, a plan-number column, and the dated block-quote convention for a superseded disposition with the original left standing.
    - stacks/selfhosted/arrs/beets.md § "Phase 6 — tagger configuration and dry run (2026-09-20)" — the section plans 06-03, 06-10 and 06-13 have been adding to.
    - All twelve prior Phase 6 summaries and every artifact under `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/` — for the evidence pointers. Do not copy their verdicts; re-measure.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-RESEARCH.md § Contradictions C-7 (line 160) — CONTEXT.md says "six criteria" three times and the ROADMAP lists five. Cosmetic; record it and do not invent a sixth.
  </read_first>
  <action>
Re-measure each of the five ROADMAP criteria **from live state**, then write the closure.

**Re-measurement, not carry-forward.** For each criterion, run the assertion again now and quote the fresh output. A criterion ticked from a plan summary written days earlier is a claim about the past.
1. **Imports copy rather than move** — re-run `scripts/check-beets-config.sh` and quote the `import.copy` / `import.move` lines from the **server-committed** arm, naming which route answered.
2. **`incremental: yes` with `incremental_skip_later: yes`** — quote both from the same arm, AND cite plan 06-08's two-arm negative control, because this criterion's whole point is that a read-back is not proof it applies. Name the two opposite outcomes.
3. **A run on a bucket-A sample prints top-level folders equal to `ALBUMARTIST` exactly, case included; `Various Artists/` appears and `Compilations/` does not** — cite plan 06-11's zero-diff and its class assertions. ⚠ Record the instrument correction in band: the criterion says `--pretend`, and `--pretend` **cannot** produce the intended tree; the instrument was `beet move -p` per the D-33 addendum at `.planning/REQUIREMENTS.md`. **Do not rewrite the ROADMAP criterion text** — add the correction as a dated note beside it, the way this project has handled every other instrument correction.
4. **A multi-artist track shows its artists parsed correctly in BOTH Jellyfin and Music Assistant** — record Jellyfin's per-row entity counts and MA's, or MA's `UNKNOWN, not green` if plan 06-13's gate was declined. **Never sum the two halves into one verdict.** Record the D-34 amendment: the write side is the `ARTISTS` tag, not `;` in `ARTIST`, and the `;` in the criterion text describes the existing twelve library files.
5. **Match disambiguation demonstrated, with the run confirmed to have written nothing** — cite plan 06-12's candidate list with the chosen `GB` release and its track-count check and negative control, and plan 06-11's three-layer D-29 proof. ⚠ Record in band that the criterion's "source and library file counts unchanged" is **weaker than what was done**: a count would pass while content changed underneath, which Phase 1 measured happening, so the proof is a checksum manifest plus `library.db` and `state.pickle` hashes, not a count.

**Write the closure into `beets.md`'s Phase 6 section**, in the established shape:
- A criteria table: criterion, verdict, **evidence as a runnable command quoted verbatim**, and the plan number that produced it. Quote the commands so they cannot drift from the scripts.
- The instrument corrections for criteria 3 and 5, and the D-34 amendment for criterion 4, each as a dated note with the original text left standing.
- A `### Still open` list holding, at minimum: CONF-04's MA half if it did not run; the Phase 5 items Phase 6 did not touch (the `interpolated-host-path` gate making `quick-health-check.sh` exit 1, the Immich API key on a systemd command line, `dropbox/` inside nine `rw` binds); and the live-service D-34 Jellyfin option that git does not capture.
- The C-7 note: CONTEXT.md refers three times to "six criteria" and the ROADMAP lists five. Cosmetic, recorded so the numbering is not read as a missing criterion.
- **D-32, stated as a standing instruction:** `tank/downloads@pre-phase5` is **NOT** released at Phase 6 sign-off. Phase 6 wrote nothing, so it produced no evidence that Phase 5's changes were correct; only Phase 7's real import exercises them. It is the only undo for 4,750 renames, 751 tag writes and 26,005 chowns, and it is not a clean undo.
- **D-08, discharged as a read rather than a measurement:** rc6's schema exposes **no** pagination, page-size or inbox-item-limit knob at all; the only levers on the documented "laggy past some hundred folders" risk are `gui.inbox.ignore` and batch cadence. Phase 3's friction 5 stays `NOT EXERCISED`. Carried to Phase 9.

Do not edit any earlier phase's section. Phase 6 adds its own.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=stacks/selfhosted/arrs/beets.md; grep -q "Phase 6 — tagger configuration and dry run" "$f" || exit 1; for c in CONF-01 CONF-02 CONF-03 CONF-04 CONF-05 CONF-06; do grep -q "$c" "$f" || { echo "$c not in the closure"; exit 1; }; done; grep -q "beet move -p" "$f" || { echo "criterion 3 instrument correction missing"; exit 1; }; grep -qi "pre-phase5" "$f" || { echo "D-32 standing instruction missing"; exit 1; }; grep -qi "Still open" "$f" || exit 1; grep -qi "interpolated-host-path" "$f" || { echo "the Phase 5 open items are not carried"; exit 1; }; grep -qiE "six criteria|C-7" "$f" || { echo "the C-7 cosmetic note is missing"; exit 1; }; grep -qi "NOT EXERCISED" "$f" || { echo "friction 5 disposition missing"; exit 1; }; n=$(grep -cE "^\|.*scripts/(check-beets-config|phase06-oracle|phase06-incremental-control|check-music-freeze|check-music-consumers)\.sh" "$f"); [ "$n" -ge 4 ] || { echo "the criteria table carries only $n runnable-command evidence cells, want >=4"; exit 1; }; echo "Phase 6 closure OK"'</automated>
  </verify>
  <done>
    `beets.md` carries a Phase 6 criteria table with a verdict, a verbatim runnable command and a plan number per criterion, all five re-measured from live state with fresh output quoted. The criterion 3 and 5 instrument corrections and the criterion 4 D-34 amendment are dated notes with the originals left standing. The `### Still open` list, the C-7 note, the D-32 standing instruction and D-08's read-only answer are all present. No earlier phase section was edited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the CONF traceability rows off Pending and update the ROADMAP</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 280-285 — the CONF-01…CONF-06 traceability rows, and the D-33/D-34 addenda plan 06-02 attached to two of them.
    - .planning/REQUIREMENTS.md line 276 (TAGR-05) and the INBX-01/02/03 rows — the house style for a completed row: the plan numbers, what was re-asserted at close, the ⚠ caveats stated rather than hidden, and the addenda left standing.
    - .planning/ROADMAP.md lines 858-887 — the Phase 6 entry with `**Plans**: TBD` to replace and the five criteria to annotate.
    - .planning/ROADMAP.md lines 889-929 — the Phase 7 entry, whose depends-on and entry criteria must inherit Phase 6's carried-forward items.
    - stacks/selfhosted/arrs/beets.md § Phase 6 — the closure task 1 wrote, which these rows point at.
  </read_first>
  <action>
**`.planning/REQUIREMENTS.md` — the six CONF rows.** Update each from `Pending` to its real disposition, in the house style: the plan numbers that discharged it, what was re-asserted from live state at close, and any ⚠ caveat stated rather than hidden. Specifically:
- CONF-01, CONF-02, CONF-03, CONF-05, CONF-06 → Complete (Phase 6), each naming its evidence and its re-measurement at close. CONF-02 must name plan 06-08's two-arm negative control, not just the read-back. CONF-06 keeps the D-33 addendum standing and records that the instrument was `beet move -p`.
- CONF-04 → Complete **only if** plan 06-13's MA arm ran and passed. Otherwise **OPEN**, with: the Jellyfin half discharged and named, the MA half recorded as *could not look* with its date and reason, and the exact remaining step. Do not write "Complete (Jellyfin)" — a half-discharged requirement with a confident-sounding cell is the shape this project exists to avoid. The D-34 addendum stays standing either way.
Also record, in CONF-03's or CONF-06's row, the ⚠ that D-14's literal `2-05` renders as `02-05` (both fields are `PaddedInt(2)`), that Phase 6 chose `02-05` and wrote the expected tree to it, and that the existing library uses a third convention (`CD 02-01 Artist - Title`) which Phase 6 deliberately did not reconcile.

**`.planning/ROADMAP.md` — the Phase 6 entry.** Replace `**Plans**: TBD` with the wave structure and a one-line objective per plan, in the shape Phase 5's entry uses (`**Wave N** *(blocked on Wave N-1 completion)*` with a checkbox line per plan). Mark each plan `[x]` with any in-band amendment recorded in italics beside it — Phase 5's entry is the model, and its amendments (the `Vol 036` pilot substitution, the narrowed chown scope) are exactly the shape to follow. Annotate criteria 3 and 5 with their dated instrument corrections and criterion 4 with the D-34 amendment; **do not rewrite the criterion text**.

**`.planning/ROADMAP.md` — hand off to Phase 7 and Phase 9.** Add to Phase 7's entry, as named entry criteria rather than notes:
- **The DJ-routing mechanism gap (OQ-2 / C-6).** `--set albumtype=dj` is a `beet import` CLI flag backed by `import.set_fields`; rc6's `InboxFolderSchema` carries only `path`, `name`, `auto_threshold` and `autotag`, so there is no per-inbox field-setting mechanism. Phase 6 proved the path rule; the mechanism for a real flask-driven DJ import is unowned. Name the three candidate routes: a global `import.set_fields` (wrong — hits everything), a post-import `beet modify albumtype=dj` plus `beet move`, or a hook plugin.
- **The `bootleg` album-populated gate.** Never route a folder to `bootleg` without first asserting `album` is populated and distinct across the intended groups. The same button produced Phase 3's best and worst results purely on that condition, with no UI signal. `03-asis` is registered but nothing staged into it in Phase 6, so this is untested.
- **The `rw` grant is Phase 7's FIRST act** (D-05), done deliberately with the snapshot fence already in place — and `tank/downloads@pre-phase5` is not released until Phase 7's pilot passes (D-32).
- **Repair `Def Leppard/Def Leppard (2015)/`** (D-19a), plus any album directory plan 06-11 reported in that shape.
- **CONF-04's MA half**, if it is OPEN.
- **DUPE-01 / DUPE-02 still need a roadmap decision before Phase 7.** Phase 1 measured 828 duplicate groups / 19.4% duplication; Phase 3 re-confirmed `dj-mixes` is a byte-for-byte duplicate subset of `unsorted`, which makes Phase 7's diff join **last-wins** — a silent behaviour rather than a chosen one. It is not Phase 6's, but it is the nearest unowned blocker and it belongs in Phase 7's entry criteria.
- **Every `%aunique{}` firing from plan 06-11's list is a known MA risk to verify** (D-16).

Add to Phase 9's entry: beets-flask's folder-count lag past ~100 folders with batch cadence as the mitigation (D-08, with rc6's no-pagination-knob finding attached), and the per-file `LOCATION=…/release/NNNNNN` Discogs-id disambiguation shortcut (D-28).

Do not mark Phase 6 complete in the ROADMAP if CONF-04 is OPEN. Record it as closed-with-one-open-requirement, named.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; r=.planning/REQUIREMENTS.md; m=.planning/ROADMAP.md; for c in CONF-01 CONF-02 CONF-03 CONF-05 CONF-06; do grep -qE "\| $c \|.*(Complete|OPEN)" "$r" || { echo "$c still Pending or unrecorded"; exit 1; }; done; grep -qE "\| CONF-04 \|.*(Complete|OPEN)" "$r" || { echo "CONF-04 not dispositioned"; exit 1; }; grep -q "02-05" "$r" || { echo "the 02-05 rendering caveat is not recorded"; exit 1; }; grep -q "beet move -p" "$r" || exit 1; grep -qi "negative control" "$r" || { echo "CONF-02 does not name the negative control"; exit 1; }; grep -q "Plans\*\*: TBD" "$m" && { echo "ROADMAP Phase 6 still says TBD"; exit 1; }; grep -q "06-01-PLAN.md" "$m" || { echo "the plan list is not in the ROADMAP"; exit 1; }; grep -qi "set_fields" "$m" || { echo "the OQ-2 DJ-routing gap is not handed to Phase 7"; exit 1; }; grep -qi "bootleg" "$m" || { echo "the bootleg gate is not handed to Phase 7"; exit 1; }; grep -qi "DUPE-01" "$m" || { echo "the DUPE blocker is not in Phase 7 entry criteria"; exit 1; }; grep -qi "pre-phase5" "$m" || { echo "D-32 is not in Phase 7 entry criteria"; exit 1; }; echo "traceability + roadmap OK"'</automated>
  </verify>
  <done>
    All six CONF rows carry a real disposition with plan numbers, re-measurement evidence and ⚠ caveats; CONF-04 is either Complete with both consumers named or OPEN with its remaining step, never "Complete (Jellyfin)". The ROADMAP's Phase 6 entry lists every plan by wave with its objective and any in-band amendment, annotates criteria 3, 4 and 5 with dated corrections without rewriting them, and Phase 7's entry criteria now name the DJ-routing mechanism gap with its three candidate routes, the `bootleg` gate, the `rw`-grant-first rule, the `Def Leppard` repair, the DUPE blocker, the `%aunique{}` MA risk list and CONF-04 if open. Phase 9 carries the lag risk and the Discogs-id shortcut.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan summary → closure verdict | A criterion ticked from a summary is a claim about the past. Re-measurement is the boundary control. |
| Phase 6 → Phase 7 | An item recorded only in a Phase 6 artifact is an item Phase 7 will not read. Entry criteria in the ROADMAP is where it must land. |
| git repo → public internet | Public repo; the closure quotes commands, counts and option names, never credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-72 | Repudiation | Criterion 4 recorded as "Complete (Jellyfin)", summing a proven half with an unreachable half into a confident-sounding verdict | mitigate | The plan forbids that cell by name; CONF-04 is Complete only if both halves ran, otherwise OPEN with the remaining step, and the verify command asserts one of the two. |
| T-06-73 | Repudiation | Criteria 3 and 5's instrument corrections applied by rewriting the ROADMAP text, erasing the history of the wrong instrument | mitigate | Dated notes beside the criteria, originals left standing, citing the D-33 addendum — the same precedent TAGR-05 set. |
| T-06-74 | Tampering | `tank/downloads@pre-phase5` released on Phase 6's sign-off, destroying the only undo for 4,750 renames, 751 tag writes and 26,005 chowns immediately before the phase that first writes at scale | mitigate | D-32 written as a standing instruction in `beets.md` and as a Phase 7 entry criterion in the ROADMAP; the verify command asserts both. |
| T-06-75 | Repudiation | The OQ-2 DJ-routing mechanism, the `bootleg` gate and the DUPE blocker left in artifacts nobody reads | mitigate | All three placed in Phase 7's ROADMAP entry criteria, with the candidate routes named so the next phase does not restart from zero; the verify command greps for each. |
| T-06-76 | Information Disclosure | The closure quoting a credential from a transcript | mitigate | The closure quotes commands, counts, option names and hashes. Re-screen `beets.md`'s Phase 6 section for credential-shaped strings before committing; the repo is public and already carries one exposure recoverable via `git log -S`. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and runs only assertion scripts already in the repo. |
</threat_model>

<verification>
- Both task verify commands exit 0.
- `scripts/quick-health-check.sh` is run once at close and its per-block verdicts are recorded; read the block verdicts, not the exit code, because the pre-existing `interpolated-host-path` gate makes the exit code non-discriminating.
- `git diff --stat` shows exactly `stacks/selfhosted/arrs/beets.md`, `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
- A credential screen over the Phase 6 section of `beets.md` returns clean, with a positive control run first.
</verification>

<success_criteria>
- All five ROADMAP criteria are re-measured from live state and recorded with runnable evidence and a plan number.
- CONF-01..CONF-06 carry real dispositions; CONF-04 is never half-ticked.
- The instrument corrections and the D-34 amendment are dated notes, not rewrites.
- Phase 7 inherits the DJ-routing gap, the `bootleg` gate, the `rw`-grant-first rule, the `Def Leppard` repair, the DUPE blocker and the `%aunique{}` risk list as named entry criteria; Phase 9 inherits the lag risk and the Discogs shortcut.
- `tank/downloads@pre-phase5` is explicitly NOT released.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-14-SUMMARY.md` when done.
</output>
