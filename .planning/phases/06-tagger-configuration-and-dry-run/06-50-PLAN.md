---
phase: 06-tagger-configuration-and-dry-run
plan: 50
type: execute
wave: 3
depends_on: ["06-46", "06-47", "06-48", "06-49"]
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md
  - .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CONF-06]
gap_closure: true
round: 6

must_haves:
  truths:
    - "All five `06-REVIEW.md` findings are dispositioned by name, each with one of the register's four closed words, and the totals reconcile three ways"
    - "The ID mapping table exists BEFORE any citation is followed: round 6's in-band IDs are `R6-*`, a fresh namespace, and the table says which `R6-*` means which `WR-*`/`IN-*`"
    - "WR-02's tension is resolved ONE way, with the choice and its reasoning stated — the round does not both add in-band narrative and claim to have reduced it"
    - "`06-REVIEW.md` is wired to the register by append, and is not overwritten"
    - "Nothing in round 6 ticks CONF-04, touches Jellyfin refresh or mtime, or converts a carry into a close"
    - "NO record this plan writes states an outcome that has not happened yet: R6-08 and R6-09 are PENDING in all three records, and the ROADMAP plan-count numerator is the MEASURED count of plans executed at write time, not the count of plans authored"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md"
      provides: "Round 6's disposition register: the R6-* mapping table, all five findings plus the four non-review items, the WR-02 position, and the round's refusals"
      min_lines: 120
      contains: "THE ID MAPPING TABLE"
    - path: ".planning/ROADMAP.md"
      provides: "Round 6's plan list, the disposition paragraph amendment, and a status row whose plan count is measured and whose status word still reads In Progress"
  key_links:
    - from: ".planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md"
      to: "06-DISPOSITIONS-GAP4.md"
      via: "an appended wiring block naming the register and the namespace"
      pattern: "06-DISPOSITIONS-GAP4"
    - from: "06-DISPOSITIONS-GAP4.md mapping table"
      to: "the in-band R6-* citations in scripts/check-music-freeze.sh"
      via: "the table's In-band ID column"
      pattern: "R6-0"
---

<objective>
Record round 6. Produce `06-DISPOSITIONS-GAP4.md` — the register that says what happened to **each**
finding so the set cannot be lost at the next context boundary — wire `06-REVIEW.md` to it, update
`ROADMAP.md` and `STATE.md`, and state round 6's position on WR-02 once and coherently.

Purpose: this phase's headline lesson, recorded in `06-DISPOSITIONS.md` and re-learned in three
subsequent rounds, is that *a finding recorded where the next phase does not read is a finding
nobody owns*. Round 4 additionally learned that a review reusing a prior round's ID namespace makes
every grep for a finding non-discriminating **in exactly the files that carry the citation**, and
paid for it four times over. `06-REVIEW.md` reuses `WR-*`/`IN-*` — round 1's namespace, already
cited in band across all six scripts — so round 6 uses **`R6-*`** and this register carries the
mapping table.

Output: one register, one wiring block, two planning-document updates.

**⚠ THE BINDING RULE OF THIS PLAN, and the one the cross-AI review found broken in three places:
do not publish an outcome before it happens.** This plan runs at **wave 3**. Plan `06-51` runs at
wave 4 and plan `06-52` at wave 5; both are `autonomous: false` and both have legitimate halt/hold
branches. Therefore **every** record this plan writes — the register, `ROADMAP.md` **and**
`STATE.md` — states R6-08 and R6-09 as **PENDING** and names `06-52` task 3 as the plan that closes
them. `.planning/STATE.md` is the record the earlier fix missed: the register and the ROADMAP got
the two-step treatment and STATE did not, so STATE was left claiming a host sync and a snapshot
decision that may never happen. It gets the same treatment here.

**D-R6-M4 — the operator's decision on what the `Plans Complete` column means. Locked; do not
re-litigate.** `ROADMAP.md`'s phase-status table header reads `| Phase | Plans Complete | Status |
Completed |`, and the Phase 4 precedent row reads `16/16 | In Progress` with the note "All 16 plans
executed". **The column means EXECUTED, not authored.** So this plan does **not** write `52/52`.
It writes the **measured count of plans actually executed at the moment of the edit**, over a
denominator of 52, and `06-52` at wave 5 closes it to `52/52` as the round's last act. This is the
same shape as the STATE.md fix above — do not publish an outcome before it happens — and it is a
decision, not an argument: state it in one line in the register and move on.

**L3 — the `**Plans**: 45 plans in 23 waves` header is NOT this plan's to update.** It is owned by
`06-52` at wave 5, for the same reason the count is: consistent with D-R6-M4, the count moves when
the plans have run. Say so in the register in one clause so the header is not left unowned — being
unowned is what made it stale in the first place.

**Scope fence.** This plan changes **no verdict** and performs **no verification**: it does not run
`/gsd-verify` and claims no verification result. It does **not** touch `.planning/REQUIREMENTS.md`,
does **not** tick CONF-04, does **not** run `requirements mark-complete`, and does **not** change
`ROADMAP.md`'s Phase 6 status word away from `In Progress`. It does **not** touch the
`**Plans**: … plans in … waves` header (06-52 owns it). It edits no script, no artifact written
by an earlier plan, and no prior `DEF-` entry.

**The `HEAD --` rule, stated once and binding on every byte-identity criterion in this plan.** A
bare `git diff --exit-code <path>` compares the working tree against the **index**, not a commit.
With an edit *staged* it exits **0** — driven: `git diff --exit-code protected.txt` exits 0 while
`git diff --exit-code HEAD -- protected.txt` exits 1. This plan stages and commits while it runs, so
the blind state is the likely one. **Every byte-identity guarantee here is written
`git diff --exit-code HEAD -- <path>`.** Capture the full base-commit hash with `git rev-parse HEAD`
before the first write and use it for every "unchanged since the plan started" comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP3.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: write 06-DISPOSITIONS-GAP4.md — the mapping table, the register, and the WR-02 position</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP3.md` — **in full, and
      its header first.** It is the shape this file must match: *Why this file exists*, the *Source*
      table, **THE ID MAPPING TABLE — read this before following any citation** with its measured
      collision counts and its re-derivable recipe, the *Counts* section reconciling three ways, the
      closed four-word disposition vocabulary (FIXED / FIXED (undriven) / ACCEPTED / CARRIED), the
      closed fix-kind vocabulary (CODE / CLAIM CORRECTION / BOTH), the per-finding register table,
      *What was driven and what was not*, *Verified-and-clean*, *Corrections to the source material*,
      *What this register does NOT do*, and *Lessons*. Match this shape; do not invent a new one and
      do not invent a fifth disposition word.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` — all five findings in
      full, and its frontmatter counts (0 critical / 3 warning / 2 info / 5 total).
    - The four round-6 summaries written before this plan: `06-46-SUMMARY.md`, `06-47-SUMMARY.md`,
      `06-48-SUMMARY.md`, `06-49-SUMMARY.md`. The dispositions and fix kinds recorded here are
      **the summaries'**, re-read from them, not this plan's expectations. ⚠ If `06-47` shipped a
      `06-47-PARTIAL.md` instead of a `-SUMMARY.md`, it hit its abort branch: read the PARTIAL,
      record R6-05's disposition from what it actually says, and do not treat the plan as complete.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt`
      — in particular any divergence it recorded as owned by **this plan** (`ROADMAP.md`) or as
      owned jointly by this plan and `06-51` (`deferred-items.md`). A divergence recorded with this
      plan's name on it is this plan's to carry into *Corrections to the source material*.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — `DEF-06-39-01` (the
      namespace rule), `DEF-06-39-02` (the refusals, including `ST_PLANNED_CASES=7` excluded by
      name), `DEF-06-39-06` (the self-referential-count class), and the new `DEF-06-48-01` /
      `DEF-06-48-02`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` — its status, score
      and two secondary findings, so *Corrections to the source material* is accurate rather than
      invented.
  </read_first>
  <action>
Write `06-DISPOSITIONS-GAP4.md`, matching `06-DISPOSITIONS-GAP3.md`'s structure section for section.

**THE ID MAPPING TABLE comes before anything a reader might follow a citation from.** Round 6's
in-band namespace is `R6-*`. The rows, five for the review plus four for the round's other items so
one table covers the round:

  `WR-01` → `R6-01` — unused `xs` / `xdst` bindings in the five-field tagger-inventory read.
  `WR-02` → `R6-02` — the extreme comment-to-code ratio across all six scripts.
  `WR-03` → `R6-03` — pinned counts with no automated cross-check.
  `IN-01` → `R6-04` — `CLAUDE.md` claims no conventions exist while the scripts enforce several.
  `IN-02` → `R6-05` — `beets.md` is a 2,470-line running log with no pointer to current state.
  (no review ID) → `R6-06` — the `DEF-06-45-04` non-detecting recipe, corrected and driven.
  (no review ID) → `R6-07` — the bracketing convention's scope, decided (`DEF-06-48-02`).
  (no review ID) → `R6-08` — the host `git pull --ff-only` sync (plan 06-51).
  (no review ID) → `R6-09` — the snapshot-release decision (plan 06-52).

⚠ **`R6-08` and `R6-09` HAVE NOT EXECUTED when this register is written, and the register must say
so rather than describing them as if they had.** Plan 06-51 runs at wave 4 and plan 06-52 at wave 5;
this register is written at wave 3. Both are `autonomous: false` and can legitimately end at `halt`.
Write both rows' disposition cell as the literal word `PENDING`, and beside each name (i) its owning
plan, (ii) the `DEF-` entry that will carry its outcome (`DEF-06-51-01`, `DEF-06-52-01`) and
(iii) the `ROUND CLOSE` section below, which plan 06-52 task 3 fills as the round's last act. The
`DEF-` entries are the **authoritative fallback**: if the round never reaches its close, a reader
following these rows still lands on the truth rather than on a silence.

Then create the **`ROUND CLOSE`** section itself, near the end of the register, as an explicit
placeholder — a heading plus one sentence saying it is not yet written, naming plan 06-52 task 3 as
its owner and the two `DEF-` entries as its sources. A placeholder that names its owner is a
commitment; an absent section is an omission nobody notices.

**Write the PENDING prose so it STAYS TRUE after 06-52 fills the rows.** Any sentence in this
register that describes R6-08/R6-09's state must be phrased as a **dated statement about the moment
of writing**, not as a standing present-tense claim — for example "*were recorded as `PENDING` when
this register was written at wave 3; `06-52` task 3 fills them*", never "*are recorded as
`PENDING`*". The bare present tense goes false the instant 06-52 does its job, and it sits in prose
**outside** the `^\|` table-row anchor that re-checks the rows, so nothing catches it. Same rule
anywhere else the word appears in prose.

**The ROADMAP plan-count numerator — D-R6-M4, decided by the operator. State it once, in one line,
and do not argue it.** The `Plans Complete` column means **executed**, not authored: the table's own
header says `Plans Complete` and the Phase 4 precedent row reads `16/16 | In Progress` noted "All 16
plans executed". So the numerator this round writes into the Phase 6 status row is the **measured
count of plans executed at the time of the edit**, and `06-52` at wave 5 closes it to `52/52`.
Record that as a decision with its owner (`D-R6-M4`, operator, 2026-09-24), **not** as a defended
position — the earlier draft of this register argued for the *authored* reading and the operator
overruled it. Do not reproduce that argument; a re-litigated decision costs a round.
Record in the same clause that the `**Plans**: … plans in … waves` header near the Phase 6 section
is **owned by `06-52`** for the same reason, so it is not left unowned again.

State the aliasing clause the way round 4 did, **with measured counts rather than asserted ones**:
re-measure the pre-existing in-band citations of round 1's `WR-*` / `IN-*` IDs across the six
scripts at this plan's base commit, publish both the per-file figures and the re-derivable recipe,
and label the figures as a dated measurement (they move with every comment that names an ID — this
file's own subject matter). Then state the rule `DEF-06-39-01` established, and that round 6
honoured it **up front** rather than aliasing afterwards.

**The WR-02 / `R6-02` position — state it once, plainly, and do not straddle.**

  **`WR-02` is ACCEPTED, with rationale. It is NOT fixed this round, and the round binds itself so
  that the cost does not grow.**

Give the reasoning as reasoning:
  - WR-02's proposed fix is a bulk relocation of round-by-round narrative out of
    `quick-health-check.sh` (3,148 lines), `phase06-oracle.sh` (3,274) and four siblings. That is
    the largest possible diff across the six most heavily reviewed files in this repository,
    proposed at the end of a five-round recursion in which **every round's own diff became the next
    round's findings**, and in which round 3's centrepiece fix was round 4's largest finding.
  - The review itself calls the narrative "a genuine asset for provenance (every fail-closed branch
    has a stated reason and a driven test)". A bulk strip risks losing the reason a fail-closed
    branch exists, on instruments that run against a host carrying ~103 containers. The cost of the
    ratio is real; the cost of getting the strip wrong is higher and lands on the estate.
  - The durable half of WR-02's fix is therefore taken as a **forward rule**, written into
    `CONVENTIONS.md` by plan 06-49: from here on a comment explains *why a branch exists*, and
    *which round changed what* goes to the phase directory, cited by one short ID. The existing
    narrative is not stripped.
  - Round 6 **binds itself to that rule**: this round wrote **no** new in-band historical narrative.
    The only in-band text added to any script is one short `R6-01` / `R6-03` citation per changed
    site, plus the emitted remediation lines — which are *failure output*, not commentary, appearing
    where the maintainer is standing, which is the opposite of the WR-02 problem.
  - **Measure this claim rather than asserting it.** Publish the round's total script-diff line
    count and the comment-line share of it, with the recipe that produced both. A round that claims
    restraint without a number is the over-claim this register exists to prevent.

Record the **Counts** section reconciling three ways, exactly as `-GAP3` does: the review's
frontmatter (0 + 3 + 2 = 5); the dispositions by word; the fix kinds. Use only the four disposition
words and the three fix-kind words; invent no fifth. **Add one explicit sentence stating that the
reconciliation covers the five REVIEW findings only (`R6-01` … `R6-05`), that `R6-06`…`R6-09` are
round items outside it, and that when `06-52` task 3 fills `R6-08`/`R6-09` it must EXTEND this
section with their two dispositions rather than silently leaving the arithmetic describing a subset
that no longer matches the table.** Name `06-52` task 3 as the owner of that extension. A
reconciliation that is never re-run after the table grows is a check that stops checking.

Record **the round's refusals as loudly as its fixes**, each with a reason and a revisit condition:
  - `ST_PLANNED_CASES=7` in `check-beets-config.sh` was **not touched** — round 4 excluded it by
    name (`DEF-06-39-02`; `-GAP3` § *Verified-and-clean* item 7): a gated pin over an
    **unconditionally executed** set, where "fixing" it breaks a working guard.
  - **No `ARCHITECTURE.md` was created** — IN-01's architecture half is **ACCEPTED**: this estate's
    architecture is already documented in `NETWORK.md`, `MEDIA.md`, `DEPLOYMENT.md` and
    `TAILSCALE.md`, all four linked from `CLAUDE.md` § Documentation, so a stub would add a fifth,
    emptier front door to a mapped system.
  - **`artifacts/06-43-conf04-verdict.txt` was not edited** — a dated record; `DEF-06-45-04`'s own
    instruction, honoured, and proven against 06-48's captured base commit by sha256 rather than by
    an index-anchored `git diff`.
  - **The bracketing convention was not widened to `ROADMAP.md`** — decided with reasoning in
    `DEF-06-48-02`; no `ROADMAP.md` token edit was made.
  - **No CONF-04 work of any kind** — no re-probe, no mtime touch, no refresh POST, no checkbox
    move. The mtime lever is a measured negative (`DEF-06-45-02`) and must not be retried; the
    aggressive per-item refresh mode remains forbidden and unreached; CONF-04's Jellyfin half
    belongs to Phase 7 entry criterion **E6**, first measurement, and E6's second measurement stays
    with Phase 7 on every branch.

Include a **What this register does NOT do** section stating plainly: no verdict changes; no
verification is claimed or performed; `REQUIREMENTS.md` is untouched and the unticked CONF-04 box
stands; the ROADMAP status row still reads `In Progress`; the ROADMAP plan-count numerator is the
executed count at wave 3, not 52, per `D-R6-M4`; the `**Plans**: … waves` header is untouched and
belongs to `06-52`; prior rounds' carried items stay carried with no `DEF-` entry renumbered,
reworded or removed; **and that R6-08 and R6-09 were recorded as `PENDING` at the time of writing
because they had not executed, with `ROUND CLOSE` and the two `DEF-` entries named as where their
outcomes land.**

Include a **Lessons** section with round 6's own lessons, not earlier rounds' restated. At minimum:
(1) the review that closes a recursion is the one whose largest finding is *the cost of the record
itself*, and the honest response is a forward rule plus self-restraint, not a bulk rewrite;
(2) a detector can publish a true number and still be vacuous — `DEF-06-45-04` survived a full
review pass because the number beside it was right; (3) a convention with an undecided scope drifts
in whichever direction the next writer pushes it, so scope is part of the convention; (4) **the fix
for "do not publish an outcome before it happens" has to be applied to every record, not to the two
you were looking at** — round 6's own cross-AI review found `STATE.md` still claiming two
unexecuted plans' outcomes after the register and the ROADMAP had both been corrected.

Every measurement in this file is taken with `/usr/bin/grep` by absolute path; say so in the footer,
as `-GAP3` does.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && R=.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md && test -s "$R" && test "$(/usr/bin/grep -cF 'THE ID MAPPING TABLE' "$R")" -ge 1 && test "$(/usr/bin/grep -coE 'R6-0[1-9]' "$R")" -ge 9 && test "$(/usr/bin/grep -cE '^\|.*R6-0[89].*PEN[D]ING' "$R")" -eq 2 && test "$(/usr/bin/grep -cF 'D-R6-M4' "$R")" -ge 1 && test "$(/usr/bin/grep -cE 'Full[R]efresh' "$R")" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `06-DISPOSITIONS-GAP4.md` exists, is at least **120** lines, and carries every structural
      section `06-DISPOSITIONS-GAP3.md` has, named in the action's `read_first`.
    - The mapping table appears **before** any `R6-` citation is used in prose: the line number of
      the mapping-table heading is lower than the line number of the first register row.
    - The mapping table carries **nine** rows, `R6-01` … `R6-09`, and each of `WR-01`, `WR-02`,
      `WR-03`, `IN-01`, `IN-02` appears in its Review-ID column exactly once.
    - The `R6-08` and `R6-09` rows read `PENDING` and each names its owning plan, its `DEF-` entry
      and the `ROUND CLOSE` section. Measure with the token written **bracketed**, so the criterion
      is not an occurrence of what it counts:
      `/usr/bin/grep -cE '^\|.*R6-0[89].*PEN[D]ING'` returns **2**. The `^\|` anchor is
      load-bearing: it restricts the count to **table rows**, so prose elsewhere in the register
      that discusses the word cannot inflate it. Drive the recipe against a two-row control
      containing the real word and observe **2**, and against a prose line containing it and observe
      **0**, before trusting the number — a recipe that matches nothing passes a `-eq 0` check for
      the wrong reason, which is the `DEF-06-45-04` class.
      ⚠ Note that the companion `-cE 'R6-0[1-9]'` count in this task's `<automated>` block is
      **correct as written** and covers all nine IDs; it is **not** a typo for `R6-0[89]`, which is
      a different, narrower check scoped to the two PENDING rows. Do not "fix" either into the
      other — a cross-AI reviewer proposed exactly that and it was refuted by driving both.
    - **No sentence in the register states R6-08's or R6-09's PENDING status in the bare present
      tense.** Every such statement is dated to the moment of writing and names `06-52` task 3 as
      the filler, so it stays true after the rows are closed. Assert by reading every prose
      occurrence of the word (found with the bracketed `PEN[D]ING` needle, **without** the `^\|`
      anchor) and confirming each carries a temporal qualifier.
    - A `ROUND CLOSE` section exists as a named placeholder, states that it is not yet written, and
      names plan 06-52 task 3 as its owner and `DEF-06-51-01` / `DEF-06-52-01` as its sources.
    - The register states `D-R6-M4` in one line as a **decision with an owner** (operator,
      2026-09-24): the `Plans Complete` column means executed, the numerator written at wave 3 is
      the measured executed count, and `06-52` closes it to `52/52`. **The register contains no
      argument defending the "authored" reading** — that position was overruled and reproducing it
      invites a round 7.
    - The register states in one clause that the `**Plans**: … plans in … waves` header is owned by
      `06-52`, not by this plan.
    - Every one of the **five review findings** (`R6-01` … `R6-05`) has exactly one disposition
      drawn from the closed set `FIXED` / `FIXED (undriven)` / `ACCEPTED` / `CARRIED`. No fifth word
      appears as a disposition for any of them. `PENDING` is **not** a disposition word and may
      appear **only** on the `R6-08` / `R6-09` rows, which are not review findings and are not
      counted in the reconciliation below.
    - **The counts reconcile three ways** and the register says so: review frontmatter
      `0 + 3 + 2 = 5`; dispositions summing to 5; fix kinds summing to 5. If any two disagree the
      register states the disagreement rather than smoothing it.
    - The Counts section carries an explicit sentence stating that the reconciliation covers
      `R6-01`…`R6-05` only, that `R6-06`…`R6-09` sit outside it, and that **`06-52` task 3 must
      extend the section with `R6-08`/`R6-09`'s dispositions** when it fills those rows.
    - The WR-02 position is stated as **ACCEPTED** in one unambiguous sentence, with reasoning, and
      the self-binding claim is **measured**: the register publishes the round's script-diff line
      count and comment-line share together with the recipe that produced them.
    - The refusals section names all five refusals listed in the action, each with a reason and a
      revisit condition.
    - The register states in its own words that no verification was performed and no verdict changed.
    - `/usr/bin/grep -c '^\- \[x\] \*\*CONF-04' .planning/REQUIREMENTS.md` returns **0** and
      `/usr/bin/grep -c '^\- \[ \] \*\*CONF-04' .planning/REQUIREMENTS.md` returns **1**. ⚠ The
      first is a zero-expecting count — **drive it**: run the identical recipe against a scratch
      control line reading `- [x] **CONF-04** …` and observe **1**, so the **0** is evidence the box
      is unticked rather than evidence the pattern is broken.
    - `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` returns **0**. The `HEAD --` is
      load-bearing — see the objective's `HEAD --` rule.
    - Bracketing convention honoured, measured **after** writing: `/usr/bin/grep -cE 'Full[R]efresh'`
      returns **0** and `/usr/bin/grep -ciE 'replace all [m]etadata'` returns **0** against the
      register. Drive the `-cE` recipe against a control string containing the real token first and
      observe **1**.
    - No credential, no API key, no NUL byte — `file` reports the register as text.
  </acceptance_criteria>
  <done>Round 6's register exists, its namespace is fresh and mapped, every finding has one closed-vocabulary disposition, the totals reconcile and name their future extension, D-R6-M4 is recorded as a decision, and nothing pending is written as if it had happened.</done>
</task>

<task type="auto">
  <name>Task 2: wire 06-REVIEW.md to the register, and update ROADMAP.md and STATE.md</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP3.md` — its **appended
      wiring block** at the end of the file, which is the exact shape to reproduce: a pointer to the
      register, a statement of the namespace, and the warning that the review's IDs alias earlier
      in-band citations.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` — the whole file, and in
      particular its ending, so the wiring block is appended without disturbing a byte of the
      original. **This file is round 1's record and is cited by ten other files: append only, never
      overwrite, never edit above the append point.**
    - `.planning/ROADMAP.md` — the Phase 6 plan list (the `- [ ] 06-NN-PLAN.md — …` block), the
      Phase 6 **disposition paragraph** (beginning `**Phase 6 disposition:**`), and the Phase 6
      **status row** in the phase-status table. Read the table's **header row** too
      (`| Phase | Plans Complete | Status | Completed |`) so the column meanings are read rather than
      assumed, and read the **Phase 4 precedent row** (`| 4. … | 16/16 | In Progress| All 16 plans
      executed …`) which is the established reading D-R6-M4 codifies. Locate all of these by their
      text, not by line numbers any other document quotes — citations into this file go stale
      because the planner shifts it in its own commit.
    - `.planning/STATE.md` — the `## Current Position` section, its `Plan:` line and its
      round-by-round blocks, so the round-6 block matches the established idiom and is appended
      rather than replacing anything.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` — its status
      (`gaps_found`) and score (5/6), with which the ROADMAP status row must stay consistent.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt`
      — written by plan 06-47; if it named a `ROADMAP.md` divergence as owned by this plan, this is
      where it gets corrected. If it named a `deferred-items.md` divergence, this plan **records**
      it in the register's *Corrections to the source material* and `06-51` corrects the file — this
      plan does not hold the pen on `deferred-items.md`.
  </read_first>
  <action>
Three documents, each edited additively.

**(a) `06-REVIEW.md` — append a wiring block at the very end**, after all existing content. It
states: that round 6's fixes are recorded in `06-DISPOSITIONS-GAP4.md`; that this review's
`WR-*` / `IN-*` IDs **alias round 1's namespace, already cited in band across all six scripts**, so
the in-band and register IDs for this round are `R6-01 … R6-05`, plus `R6-06 … R6-09` for the
round's non-review items; that the mapping table sits in the register immediately after its Source
table; and one line giving this round's disposition totals. **Do not edit a byte above the append
point** — prove it by asserting the file's content before the append is byte-identical to its
**base-commit** form.

**(b) `.planning/ROADMAP.md`** — three additive edits:
  1. **The seven round-6 plan lines were already added at plan time** (`06-46` … `06-52`, in the
     existing `- [ ] 06-NN-PLAN.md — <one-line objective>` form, with `06-51` and `06-52` marked
     **`autonomous: false`** as the list already does for `06-41`). **Verify, do not re-append.**
     Confirm all seven lines are present exactly once each and that each description still matches
     what the plan actually did; correct a description only where execution diverged from it. A
     duplicate plan line is a defect — assert each of the seven appears exactly once.
  2. Amend the **Phase 6 disposition paragraph** with a round-6 block in the same idiom as its
     existing round-2/3/4/5 amendments: what round 6 was (the full `06-REVIEW.md` round, plus the
     verification residue and the two operator actions **still pending at this point**); what it
     closed; that WR-02 is ACCEPTED with its reasoning named; that the bracketing convention's scope
     was decided and **not** widened to ROADMAP prose; and that **no CONF-04 work was performed and
     no checkbox moved**.
     ⚠ **Write every round-6 finding reference in the paired form `R6-NN (WR-NN)` / `R6-NN (IN-NN)`
     — never a bare `WR-NN`.** `ROADMAP.md` already carries **6** occurrences of `WR-02` with at
     least two different referents (`R4-02 (WR-02)` from round 4, and round 1's
     `closes WR-01/WR-02/WR-06/…`). A bare `WR-02` added here would be the third referent in one
     file and would make a grep for it non-discriminating — which is exactly the `DEF-06-39-01`
     namespace trap the `R6-*` namespace was minted to prevent. Re-measure the `WR-02` occurrence
     count in `ROADMAP.md` before and after this edit and record both figures in the plan summary.
  3. Update the **Phase 6 status row**, under **D-R6-M4**:
     - **The plan-count numerator is MEASURED, not assumed.** The column is `Plans Complete` and
       means **executed** (the Phase 4 precedent row reads `16/16 | In Progress` noted "All 16 plans
       executed"). Count the plans that have actually executed at the moment of this edit — the
       reliable measure is the number of `06-NN-SUMMARY.md` files present in the phase directory
       (a `06-NN-PARTIAL.md` is **not** a completed plan and does not count). Write that measured
       numerator over the denominator **52**. Record the measured figure and the counting command in
       the plan summary. At wave 3 with 06-46 … 06-49 done the expected value is **49** — but
       **measure it, do not write 49 because this plan said so**, and if it reads otherwise record
       the measured value and use that.
     - **Do NOT write `52/52`.** Seven plans existing is not seven plans executed, and `06-51` and
       `06-52` are `autonomous: false` with legitimate halt branches. `06-52` task 3 closes the
       count to `52/52` at wave 5 as the round's last act.
     - **The status word stays `In Progress`.** Do not write `Complete` in the **status cell** in
       any casing: `06-VERIFICATION.md` scores this phase `gaps_found`, 5/6, and
       `REQUIREMENTS.md`'s CONF-04 box is unticked — a `Complete` status beside those two is the
       false close this phase has spent five rounds refusing. Put that reasoning in the narrative
       cell in one clause so the next reader does not "tidy" it.
     - The narrative cell gains a round-6 clause. **Do not gut or rewrite the existing narrative
       cell** — it is a 33,000-character record and appending is the only safe operation on it.
  4. **Do NOT touch the `**Plans**: 45 plans in 23 waves` header.** It is `06-52`'s at wave 5, for
     the same D-R6-M4 reason the count is. Leave it exactly as found.
  If plan 06-47's consistency artifact named a `ROADMAP.md` divergence as owned by this plan,
  correct it here and record in this plan's summary which passage changed and why.

**(c) `.planning/STATE.md`** — append a round-6 block to `## Current Position` in the established
idiom, and update the `Plan:` line to `<measured executed count> of 52` using the **same measured
figure** written into the ROADMAP status row, so the two records cannot disagree.

The block states: that round 6 **planned** plans 06-46 … 06-52 and that plans 06-46 … 06-50 have
executed at the time of writing; the five review findings and their dispositions; the WR-02 ACCEPTED
position in one sentence; the recipe correction and the scope decision; and — in the same emphatic
form the earlier blocks use — that **re-verification has NOT been performed by this round**, that
`REQUIREMENTS.md` was not touched, that the unticked CONF-04 box stands, and that `/gsd-verify 06`
is the next step and the verifier's call, not this plan's.

⛔ **The two operator actions are recorded as PENDING, with their outcomes explicitly NOT stated.**
`R6-08` (the host `git pull --ff-only`, plan `06-51`, wave 4) and `R6-09` (the snapshot-release
decision, plan `06-52`, wave 5) have **not run** when this block is written. Both plans are
`autonomous: false` and both have legitimate halt/hold branches. Write both as `PENDING`, name their
owning plan, name `DEF-06-51-01` / `DEF-06-52-01` as where their outcomes will land, and name
**`06-52` task 3 as the plan that folds the real outcomes back into this block**. Do **not** write
"round 6 executed plans 06-46 … 06-52", do **not** describe a sync that has not happened, and do
**not** describe a snapshot decision that has not been taken. `STATE.md` is what the next session
reads as position; a false position here is read as fact by everything downstream. This is the
record the earlier two-step fix missed — the register and the ROADMAP got it and STATE did not.

⚠ **Edit `STATE.md` BY HAND. Do not use any `state.*` SDK verb.** Four recorded instances in this
project have had a `state.*` write orphan a multi-line field and leave a dangling fragment, most
recently during plan 06-40. After editing, run `git diff .planning/STATE.md` and read the **body**
of the diff, not just `--stat`, confirming no existing line was truncated and no continuation was
orphaned. The same caution applies to `roadmap.*` verbs — prefer a hand edit and diff the body.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(/usr/bin/grep -cF '06-DISPOSITIONS-GAP4' .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md)" -ge 1 && test "$(/usr/bin/grep -cF '06-52-PLAN.md' .planning/ROADMAP.md)" -ge 1 && test "$(/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| [0-9]+/52 \| In Progress' .planning/ROADMAP.md)" -eq 1 && test "$(/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| 52/52 \|' .planning/ROADMAP.md)" -eq 0 && test "$(/usr/bin/grep -E '^\| 6\. Tagger Configuration and Dry Run \|' .planning/ROADMAP.md | /usr/bin/awk -F'|' '{gsub(/^[ \t]+|[ \t]+$/,"",$4); print tolower($4)}')" = "in progress" && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md && git diff --exit-code HEAD -- scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `06-REVIEW.md` gains a wiring block containing the literal strings `06-DISPOSITIONS-GAP4`,
      `R6-01` and `alias`. The file's content **above** the append point is byte-identical to
      `git show <BASE COMMIT>:…/06-REVIEW.md`: prove with `head -n <base_line_count>` piped through
      `cmp -s` against the base-commit content. Anchor to the captured base commit, not to a moving
      `HEAD`.
    - `.planning/ROADMAP.md` contains one `- [ ] 06-NN-PLAN.md` line for each of `06-46` … `06-52`
      (seven lines), and the `06-51` and `06-52` lines each contain the literal `autonomous: false`.
    - **The Phase 6 status row's plan count is the measured executed count over 52, and is NOT
      `52/52`.** Assert both halves:
      `/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| [0-9]+/52 \| In Progress'`
      returns **1**, and
      `/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| 52/52 \|'` returns **0**.
      The plan summary records the measured numerator and the command that counted it.
      ⚠ The `52/52`-absent check is zero-expecting — **drive it**: run the same recipe against a
      scratch control row containing `| 6. Tagger Configuration and Dry Run | 52/52 | In Progress|`
      and observe **1** first.
    - **The status CELL — not the whole row — is case-insensitively `in progress`.** Extract field 4
      of the row and compare:
      `/usr/bin/grep -E '^\| 6\. Tagger Configuration and Dry Run \|' .planning/ROADMAP.md |
       /usr/bin/awk -F'|' '{gsub(/^[ \t]+|[ \t]+$/,"",$4); print tolower($4)}'`
      must equal exactly `in progress`.
      ⚠ **Scope and casing are both load-bearing, and the previous form of this criterion had
      neither.** A case-SENSITIVE search for `Complete` across the whole row cannot fail: the row is
      one 33,323-character line containing **0** `Complete` but **10** case-insensitive matches
      (4 `COMPLETE`, 6 `complete`) in its narrative cell, all of them legitimate history. So a
      row-wide case-insensitive check would fail on correct content, and the row-wide
      case-sensitive check passes vacuously and could never catch a status word written in caps.
      Scoping to field 4 and lower-casing it is the form that can actually fail — and it does not
      require rewriting the narrative cell, which this plan is forbidden to gut.
      **Drive it:** run the same field-4 extraction against a scratch control row whose status cell
      reads `COMPLETE` and observe it print `complete`, proving the comparison can fail.
    - The Phase 6 disposition paragraph gains a round-6 block containing the literal strings
      `round 6`, `ACCEPTED`, and a clause stating no checkbox moved. **Every round-6 finding
      reference in the added text uses the paired form** — `/usr/bin/grep -cF 'R6-02 (WR-02)'`
      over the added lines returns **≥ 1**, and the added lines contain **no** bare `WR-0` token
      that is not immediately preceded by `R6-NN (`. The `WR-02` occurrence count for the whole
      file is recorded before and after the edit in the plan summary.
    - `.planning/ROADMAP.md`'s `**Plans**: … plans in … waves` header is **byte-unchanged** by this
      plan — it belongs to `06-52`. Assert the literal `45 plans in 23 waves` is still present
      exactly once.
    - `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` returns **0**, and
      `git diff --exit-code HEAD -- scripts/` returns **0** — no script is touched by this plan.
      The `HEAD --` is load-bearing on both.
    - `.planning/STATE.md`'s `Plan:` line reads `<measured executed count> of 52`, and that numerator
      is **identical** to the one written into the ROADMAP status row. Assert by extracting both and
      comparing.
    - **`.planning/STATE.md` states R6-08 and R6-09 as PENDING and states no outcome for either.**
      Assert positively: the round-6 block contains `R6-08`, `R6-09`, the bracketed `PEN[D]ING`
      token's real form, `DEF-06-51-01`, `DEF-06-52-01` and the literal `06-52`. Assert negatively:
      the block does **not** contain the phrase `executed plans 06-46 … 06-52` or any equivalent
      claim that all seven ran, and does not state a `proceed`/`halt` answer, a commit the host
      moved to, a sha256 verdict, or a `hold`/`release`/`defer` answer. Read the added block and
      confirm each by eye as well as by grep — this is the finding four of six reviewers raised and
      a grep alone will not catch a paraphrase.
    - `git diff .planning/STATE.md` shows **zero deletions of pre-existing content lines** other than
      the single `Plan:` line being replaced. Read the diff **body**; a `--stat`-only check does not
      satisfy this criterion.
    - No `state.*` or `roadmap.*` SDK verb was invoked. The summary states this explicitly.
    - Measured after writing, `/usr/bin/grep -cE 'Full[R]efresh'` over the lines this task ADDED to
      `ROADMAP.md` and `STATE.md` returns **0** — round 6 does not add a third occurrence while
      deciding the convention's scope. Drive the recipe against a control string containing the real
      token first and observe **1**.
  </acceptance_criteria>
  <done>The review is wired to the register by append; ROADMAP carries round 6's plans, a paired-form disposition block, a MEASURED executed plan count and a status cell still reading In Progress; STATE carries the round-6 block with both operator actions PENDING and 06-52 named as their closer; no requirement state moved and no script changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → every future agent and reader | `ROADMAP.md`, `STATE.md` and the register are what the next phase, the next round and `/gsd-verify` read. A wrong statement here is what a phase gets closed on. |
| `06-REVIEW.md` → ten citing files | Round 1's record is cited from ten places. Overwriting or editing above the append point silently invalidates all of them. |
| SDK `state.*` / `roadmap.*` verbs → the files they write | Recorded in this project to corrupt multi-line fields while `--stat` looks correct. |
| wave 3 record → waves 4 and 5 outcomes | Two `autonomous: false` plans with halt branches run after this one. Anything written here about their outcomes is a claim about the future. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06R6-20 | Tampering | `06-REVIEW.md`'s integrity | mitigate | Append-only, with a `cmp -s` byte-identity assertion over the pre-append content against the captured **base commit**. |
| T-06R6-21 | Tampering | `STATE.md` / `ROADMAP.md` via SDK writes | mitigate | Hand edits only; `state.*` and `roadmap.*` verbs explicitly forbidden; the diff **body** must be read, and zero deletions of pre-existing content is an acceptance criterion. |
| T-06R6-22 | Repudiation | converting a carry into a close | mitigate | `git diff --exit-code HEAD --` on `REQUIREMENTS.md`; the unticked-box grep is an acceptance criterion **with a driven control**; the status **cell** is pinned case-insensitively to `in progress` by a field-4 extraction that is itself driven against a `COMPLETE` control. |
| T-06R6-23 | Repudiation | an unresolvable citation from a colliding namespace | mitigate | The `R6-*` namespace is fresh and the mapping table is required to precede the first register row, with the collision counts re-measured rather than asserted. Every round-6 reference added to `ROADMAP.md` must use the paired `R6-NN (WR-NN)` form, because `WR-02` already has two referents in that file. |
| T-06R6-24 | Tampering | scripts changed under cover of a record-keeping plan | mitigate | `git diff --exit-code HEAD -- scripts/` is an acceptance criterion; no script is in `files_modified`. |
| T-06R6-43 | Repudiation | a record published ahead of the event it describes | mitigate | **All three** records — register, `ROADMAP.md` and `STATE.md` — write R6-08/R6-09 as `PENDING` with `06-52` named as closer; the ROADMAP numerator is the measured executed count per `D-R6-M4`, never `52/52`; and the register's PENDING prose is required to be dated so it does not go false when 06-52 fills the rows. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install of any kind. Row retained so the absence is stated. |
</threat_model>

<verification>
- Register exists with the mapping table ahead of the first citation; five findings, five
  dispositions, totals reconciling three ways and naming `06-52` as the extension's owner.
- `D-R6-M4` recorded as a decision; no defence of the authored reading anywhere in the register.
- R6-08/R6-09 PENDING in the register, in ROADMAP's narrative and in `STATE.md`, each naming
  `06-52` task 3 as closer.
- `06-REVIEW.md` append-only and byte-identical above the append point, anchored to the base commit.
- ROADMAP status row `<measured>/52 | In Progress`, never `52/52`; status cell case-insensitively
  `in progress` by a driven field-4 extraction; seven plan lines; disposition paragraph amended in
  paired `R6-NN (WR-NN)` form; `**Plans**: 45 plans in 23 waves` header untouched.
- `REQUIREMENTS.md` and `scripts/` byte-unchanged under `HEAD --`.
- Every zero-expecting recipe driven against a control first.
- No estate contact: zero `ssh`, zero `docker`.
</verification>

<success_criteria>
- Every `06-REVIEW.md` finding is dispositioned by name in one closed-vocabulary word.
- WR-02 is resolved one way — ACCEPTED, with reasoning and a **measured** self-binding claim.
- Round 6's record cannot be lost at a context boundary, and no citation aliases an earlier round.
- Nothing in the round converts a documented carry into a met requirement.
- No record written by this plan claims an outcome that has not occurred.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-50-SUMMARY.md` when done.
</output>
