---
phase: 06-tagger-configuration-and-dry-run
plan: 47
type: execute
wave: 1
depends_on: []
files_modified:
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [CONF-04, CONF-06]
gap_closure: true
round: 6

must_haves:
  truths:
    - "A reader opening `beets.md` at line 1 is told, within the first screen, that the file is a running log and where the authoritative current state lives"
    - "The pointer cites its target by HEADING TEXT, not by line number, so it cannot go stale the next time anything is inserted above it"
    - "The three records of Phase 6's disposition — `ROADMAP.md`, `beets.md`'s Phase 6 closure section, and `deferred-items.md` — state the same thing: CLOSED WITH ONE OPEN REQUIREMENT, CONF-04's Jellyfin half, carried to Phase 7 E6 under `negative-carry-e6`, checkbox deliberately unticked"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "IN-02/R6-05 navigational pointer at the head of the file, and the go-forward rule that every future closure section updates it"
      contains: "Current state — read this first"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt"
      provides: "The three-way disposition consistency audit, quoted verbatim from each of the three records"
  key_links:
    - from: "stacks/selfhosted/arrs/beets.md head"
      to: "the Phase 6 closure section"
      via: "a heading-text pointer, never a line number"
      pattern: "Phase 6 closed 2026-09-21"
---

<objective>
Close `06-REVIEW.md`'s **IN-02** (in-band ID **R6-05**) and settle the procedural closeout
`06-VERIFICATION.md` asks for, inside the one file that owns the durable operational record.

Purpose: `beets.md` is 2,470 lines of phase-by-phase running log. The authoritative current state
is the Phase 6 closure section near the end, and nothing at the top of the file points there — a
reader must know to scroll past five phases of history, including superseded claims deliberately
kept as retractions. Separately, `06-VERIFICATION.md`'s only remaining recommendation is
procedural: Phase 6 should be communicated as closed with one explicitly carried requirement, which
is already what `ROADMAP.md`'s Phase 6 disposition paragraph says and what the unticked checkbox
says. This plan makes `beets.md` say it in the same words and proves all three records agree.

Output: one pointer block at the head of `beets.md`, any minimal correction the consistency audit
turns up, and the audit transcript.

**Scope fence.** This plan does **not** restructure, split or summarise `beets.md`. IN-02's own fix
is "add a one-line pointer near the top"; the running-log form is deliberate and the superseded
claims are kept as retractions on purpose. This plan does **not** tick CONF-04, does **not** change
any status word to "Complete", and does **not** touch `.planning/REQUIREMENTS.md` or
`.planning/ROADMAP.md` — `06-50` owns `ROADMAP.md` and nothing in round 6 touches `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: R6-05 — a heading-text pointer at the head of beets.md, plus the rule that keeps it current</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - `stacks/selfhosted/arrs/beets.md:1-40` — the existing head: the title, the `beets/beets.yaml`
      companion line, the deleted-definitions paragraph, the "Nothing here is applied by
      `docker compose`" paragraph, the "State as of 2026-09-04" line, and the two existing
      blockquote amendments. You are inserting into an established blockquote idiom; match it.
    - `stacks/selfhosted/arrs/beets.md` around line 2051 — the exact heading text of the Phase 6
      closure section, character for character, including em-dashes. The pointer must reproduce it
      exactly or the link is decorative.
    - The two sub-headings under it — the five-criteria verdict table and the still-open list — for
      their exact heading text.
    - `stacks/selfhosted/arrs/beets.md` around line 1818 — the existing `negative-carry-e6`
      sentence, so the pointer's disposition wording matches the wording already in the file rather
      than inventing a second phrasing of the same fact.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` § IN-02.
  </read_first>
  <action>
Insert one blockquote block into `stacks/selfhosted/arrs/beets.md` immediately after the
"State as of 2026-09-04, after the Phase 1 safety harness and the Phase 3 tagger spike (last two
sections)." paragraph and before the existing `> **Amended 2026-09-11 (plan 04-05).**` blockquote,
so it is the FIRST blockquote a reader meets.

Open it with the bolded lead-in `Current state — read this first.` and state, concretely:

  1. That this file is a **running log appended phase by phase**; the newest section is the live
     picture and claims above it may be superseded in place or kept as retractions.
  2. Where the authoritative current state lives — the Phase 6 closure section, reproduced as its
     exact heading text (`Phase 6 closed 2026-09-21 — four criteria TRUE, one OPEN on a named half,
     all five RE-MEASURED at close`), with the five-criteria verdict table named as the verdict
     table and the still-open list named as the open list, each by its own exact heading text.
  3. **Cited by heading text, never by line number** — say so in one clause with the reason: line
     citations in this repository go stale the moment anything is inserted above them, and this file
     grows by append. (`06-REVIEW.md` § IN-02 suggests a line number; this is a deliberate,
     stated improvement on it, not an oversight.)
  4. Phase 6's disposition, in the same words the other records use: **CLOSED WITH ONE OPEN
     REQUIREMENT — CONF-04, on its Jellyfin half only**, carried to Phase 7 entry criterion **E6**
     under the operator's recorded `negative-carry-e6` override (2026-09-24), with
     `.planning/REQUIREMENTS.md`'s CONF-04 checkbox **deliberately still unticked**, because an
     override is a recorded carry and not a close. State that the Music Assistant half **is**
     discharged and that the two verdicts are **never summed**.
  5. The go-forward rule, as a rule and not a hope: **every future phase-closure section appended to
     this file must update this pointer in the same commit**, and a closure section whose pointer was
     not updated is the defect, not the pointer.

Use the existing `> ` blockquote idiom and the file's existing emphasis style. Do not add a table of
contents, do not renumber or reflow anything, and do not edit any other section in this task.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(/usr/bin/grep -c 'Current state — read this first' stacks/selfhosted/arrs/beets.md)" -eq 1 && test "$(/usr/bin/head -n 45 stacks/selfhosted/arrs/beets.md | /usr/bin/grep -cF 'Phase 6 closed 2026-09-21')" -ge 1 && test "$(/usr/bin/head -n 45 stacks/selfhosted/arrs/beets.md | /usr/bin/grep -cE 'beets\.md:[0-9]+|line ~?[0-9]{3,}')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'Current state — read this first' stacks/selfhosted/arrs/beets.md` returns
      exactly **1**.
    - The pointer appears within the **first 45 lines** of the file and **before** the existing
      `> **Amended 2026-09-11 (plan 04-05).**` blockquote. Assert by comparing line numbers from
      `/usr/bin/grep -n`.
    - The heading text the pointer reproduces matches the real heading **byte for byte**. Prove it
      mechanically: extract the quoted heading text from the pointer and `/usr/bin/grep -cF` it
      against the file; the count must be **≥ 2** (the pointer plus the real heading), and
      `/usr/bin/grep -cF` for the same string restricted to lines beginning with `#` must be **1**.
    - The first 45 lines contain **no** `beets.md:<number>` citation and **no** `line NNN` citation.
      `/usr/bin/head -n 45 … | /usr/bin/grep -cE 'beets\.md:[0-9]+|line ~?[0-9]{3,}'` returns **0**.
    - The pointer contains the literal strings `CONF-04`, `E6`, `negative-carry-e6`, `unticked` and
      `never summed`.
    - The pointer contains **no** occurrence of the words "Complete" or "passed" applied to Phase 6,
      and does **not** claim CONF-04 is met.
    - `git diff --stat stacks/selfhosted/arrs/beets.md` shows **0 deletions** for this task — the
      pointer is purely additive.
    - The forbidden-refresh-mode bracketing convention is honoured: measured with the extended-regex
      form (never `-F`), `/usr/bin/grep -cE 'Full[R]efresh'` over the lines this task ADDED returns
      **0**. Drive the recipe against a control string containing the real token first and observe
      it return **1**, so a non-detecting recipe cannot pass — this is `DEF-06-45-04`'s exact defect.
  </acceptance_criteria>
  <done>A reader opening `beets.md` is pointed, within the first screen, at the authoritative current state by heading text, and told the disposition and the go-forward rule.</done>
</task>

<task type="auto">
  <name>Task 2: the three-way disposition consistency audit, and any minimal correction it forces</name>
  <files>stacks/selfhosted/arrs/beets.md, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt</files>
  <read_first>
    - `.planning/ROADMAP.md` — the Phase 6 **disposition paragraph** (the block beginning
      `**Phase 6 disposition:** **CLOSED WITH ONE OPEN REQUIREMENT — CONF-04**`) and the Phase 6
      **status row** in the phase-status table (`| 6. Tagger Configuration and Dry Run | 45/45 | In
      Progress| …`). Locate both by their text, not by the line numbers quoted here.
    - `stacks/selfhosted/arrs/beets.md` — the Phase 6 closure section heading, its five-criteria
      table row 4, its still-open list's first bullet, and the `negative-carry-e6` sentence in the
      round-5 sequel section.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — `DEF-06-45-01`
      through `DEF-06-45-05`, in particular `DEF-06-45-02`'s statement of what the carry means.
    - `.planning/REQUIREMENTS.md` — the CONF-04 entry, READ ONLY. It is not in this plan's
      `files_modified` and must not be edited.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` § *Recommended path*
      and § *Gaps Summary* — the exact procedural recommendation being honoured.
  </read_first>
  <action>
Audit the three records against one another and write the result to
`artifacts/06-47-disposition-consistency.txt`.

Quote **verbatim**, each under its own labelled heading, with its source file named and its
locating heading text (not a line number) given:
  - `ROADMAP.md`'s Phase 6 disposition sentence and its status-row status word;
  - `beets.md`'s Phase 6 closure heading, criterion-4 verdict cell and still-open first bullet;
  - `deferred-items.md`'s statement of the carry;
  - `REQUIREMENTS.md`'s CONF-04 checkbox state, read with an explicit unticked/ticked count.

Then assert, as named checks with an explicit verdict word each, that all four agree on the five
facts that matter:
  (i)   Phase 6's disposition is **closed with one open requirement**, not "complete" and not
        "passed";
  (ii)  the open requirement is **CONF-04**, on its **Jellyfin half only**;
  (iii) the Music Assistant half is **discharged**, and the two verdicts are **never summed**;
  (iv)  the carry is an **operator override** named `negative-carry-e6`, owned by Phase 7 entry
        criterion **E6**, and an override is a **carry, not a close**;
  (v)   `REQUIREMENTS.md`'s CONF-04 checkbox is **unticked** and stays unticked.

**If any record disagrees**, correct it — but ONLY inside `stacks/selfhosted/arrs/beets.md`, the one
file this plan owns. If the disagreement is in `ROADMAP.md`, do **not** edit it: record the exact
disagreement in the artifact under a heading naming `06-50` as its owner, and 06-50 will carry the
correction. If the disagreement is in `REQUIREMENTS.md`, do **not** edit it at all — record it and
stop; a `REQUIREMENTS.md` edit in round 6 is out of scope by operator decision.

**The status word `In Progress` on the ROADMAP row is CORRECT and must not be changed to
`Complete`.** `06-VERIFICATION.md` scores this phase `gaps_found`, 5/6; a status row reading
`Complete` beside an unticked required checkbox is precisely the false close this phase has spent
five rounds refusing. Record that reasoning in the artifact explicitly, so a later reader does not
read the consistency audit as a mandate to flip it.

Every measurement in the artifact is taken with `/usr/bin/grep` **by absolute path** — the
workstation's bare `grep` is a shell function that has silently matched nothing in this phase before
(recorded in plan 06-41). State that in the artifact's header.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -s .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt && test "$(/usr/bin/file -b .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-47-disposition-consistency.txt | /usr/bin/grep -ci text)" -eq 1 && git diff --exit-code .planning/REQUIREMENTS.md && git diff --exit-code .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `artifacts/06-47-disposition-consistency.txt` exists, is non-empty, and `file` reports it as
      text (no NUL bytes — a NUL-bearing artifact shipped once in this phase, plan 06-41).
    - The artifact quotes **four** sources verbatim, each under its own labelled heading with its
      source file and a **heading-text** locator, not a line number.
    - The artifact records **five** named checks (i)–(v), each with an explicit verdict word from a
      closed set: `AGREE`, `DISAGREE`, or `COULD NOT LOOK`. No check is left unverdicted.
    - The artifact states explicitly that the ROADMAP status word `In Progress` is correct and must
      not be flipped to `Complete`, with the `gaps_found` 5/6 score as the reason.
    - `git diff --exit-code .planning/REQUIREMENTS.md` returns **0** — untouched.
    - `git diff --exit-code .planning/ROADMAP.md` returns **0** — untouched by this plan.
    - If any `beets.md` correction was made, `git diff stacks/selfhosted/arrs/beets.md` shows it and
      the artifact quotes the before and after text of each corrected passage.
    - The artifact contains no credential, no API key, no hostname/IP beyond what the quoted sources
      already contain.
    - Bracketing convention: measured with `/usr/bin/grep -cE 'Full[R]efresh'` (extended regex,
      **never** `-F`), the artifact returns **0**. The recipe is first driven against a control
      string containing the real token and observed to return **1**; both drives are recorded in the
      artifact so a non-detecting recipe cannot pass unnoticed.
  </acceptance_criteria>
  <done>All four records provably state the same disposition; any divergence is either corrected in `beets.md` or named with its owning plan; `REQUIREMENTS.md` and `ROADMAP.md` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public git repository → the world | This repo is public. Anything written into `beets.md` or an artifact is published. |
| documentation → future operator action | `beets.md` is the durable operational record; a wrong disposition statement here is what a future reader closes a phase on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06R6-06 | Information disclosure | new text in a public repo | mitigate | The pointer and the audit quote only existing committed prose; acceptance criteria forbid credentials, keys and new host identifiers, and require `file` to report text (no NUL). |
| T-06R6-07 | Tampering | the requirement record | mitigate | `git diff --exit-code` on `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` is an acceptance criterion; neither is in `files_modified`; the plan states the CONF-04 checkbox stays unticked. |
| T-06R6-08 | Repudiation | a false "phase complete" reading | mitigate | The pointer is required to contain `unticked` and `never summed` and is forbidden from claiming CONF-04 is met; the audit explicitly records why `In Progress` must not become `Complete`. |
| T-06R6-09 | Tampering | a non-detecting audit recipe | mitigate | Every published grep recipe must be extended-regex and driven against a control string containing the real token, observed to return 1 — the `DEF-06-45-04` defect, made unrepeatable by acceptance criterion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install of any kind in this plan. Row retained so the absence is stated. |
</threat_model>

<verification>
- `beets.md` head carries exactly one `Current state — read this first` pointer, additive only.
- The heading text it cites is proven byte-identical to the real heading by a `-cF` count of ≥ 2
  with exactly 1 hit on a `#`-prefixed line.
- Four records audited, five named checks, every one verdicted.
- `REQUIREMENTS.md` and `ROADMAP.md` byte-unchanged.
- No estate contact: zero `ssh`, zero `docker`.
</verification>

<success_criteria>
- IN-02 is closed by a pointer that cannot go stale, plus a rule that keeps it current.
- Phase 6's disposition reads identically in `ROADMAP.md`, `beets.md`, `deferred-items.md` and
  `REQUIREMENTS.md`, or the divergence is named with its owning plan.
- Nothing in this plan ticks a checkbox, flips a status word, or drives CONF-04.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-47-SUMMARY.md` when done.
</output>
