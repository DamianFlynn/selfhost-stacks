---
phase: 06-tagger-configuration-and-dry-run
plan: 46
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-music-freeze.sh
autonomous: true
requirements: [CONF-01, CONF-02, CONF-06]
gap_closure: true
round: 6

must_haves:
  truths:
    - "`shellcheck -S warning` is clean on ALL SIX reviewed instrument scripts, where it was clean on five before"
    - "The five-field `IFS='|' read` at the tagger-capable inventory names every column it binds, so a future column insertion is visible rather than silent"
    - "When either pinned count in `check-music-freeze.sh` fires, the failure output itself names the literal edit a maintainer must make, and names the one thing they must NOT do (silence it with the env override)"
    - "The failure count the script reports is unchanged by this plan — the remediation text is emitted beside the existing `fail`, never as a second `fail`"
    - "Every zero-expecting count this plan publishes is driven against a control that makes it return non-zero, so no `0` is accepted from an instrument that cannot detect"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "R6-01 unused-binding fix and R6-03(a) self-describing failure output"
      contains: "_xs"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-46-freeze-fixes.txt"
      provides: "Driven transcript: shellcheck before/after on all six scripts, both pinned-count fail arms driven from an extracted harness, and the driven control for every zero-expecting recipe"
  key_links:
    - from: "scripts/check-music-freeze.sh DECLARED_INTERP_EXPECTED fail arm"
      to: "the maintainer reading the failure"
      via: "an emitted remediation line naming the literal constant and its new value"
      pattern: "remediation"
    - from: "scripts/check-music-freeze.sh TAGGER_DEF_EXPECTED fail arm"
      to: "the D-11 named-pair rule"
      via: "an emitted remediation line requiring a THIRD definition be added BY NAME with its class"
      pattern: "remediation"
---

<objective>
Close `06-REVIEW.md`'s **WR-01** (in-band ID **R6-01**) and the `check-music-freeze.sh` half of
**WR-03** (in-band ID **R6-03**), in the one file that carries both.

Purpose: `shellcheck -S warning` is currently clean on five of six reviewed scripts, and WR-01 is
the sole finding across all six. This round must end cleaner than it started, not merely
differently. WR-03 is a maintenance trap rather than a correctness bug — the pinned counts fail
loud, confirmed by reading both branches — so the fix is not new assertion logic, it is making the
existing failure explain itself to the contributor who tripped it for a reason unrelated to their
change.

Output: two mechanical edits to `scripts/check-music-freeze.sh` and one driven transcript.

**Why this plan cites CONF-01, CONF-02 and CONF-06, stated here so the citation is defensible from
the plan text alone.** The two sites this plan touches back two different evidence chains:
  - `TAGGER_DEF_EXPECTED` is the **D-11 named-pair census** — exactly one ACTIVE front end and one
    DORMANT CLI arm, asserted by name and class. Under **D-03** (one vendored config and one
    `library.db` mounted into both containers), that census is what makes the phrase *"effective
    config"* unambiguous in **CONF-01** (imports copy, never move) and **CONF-02** (`incremental`
    with `incremental_skip_later`): it fixes **which** tagger's config those criteria are read from.
    A silent third definition would make both criteria answer about the wrong object.
  - `DECLARED_INTERP_EXPECTED` gates the interpolated-host-path inventory, which is the part of the
    §2 mount-posture evidence that keeps the `:ro` reading honest — **D-05**'s read-only invariant
    is **D-29 layer 1**, the *structural* half of **CONF-06**'s "the dry run wrote nothing". A
    volume line the parser cannot resolve makes §2 `UNKNOWN`, not clean, which is exactly the
    fail-closed distinction CONF-06's evidence depends on.

**CONF-03 is deliberately NOT cited.** `check-music-freeze.sh` asserts nothing about path formats —
that is `phase06-oracle.sh`'s expected-tree diff and class assertions. ⚠ Do not be misled by this
script's own in-band comments at the `TAGGER_DEF_*` constants and at the `git ls-files stacks`
census, which say `beets.md`'s *"criterion-1 row"* quotes their pattern verbatim: that is **Phase
4's** criterion 1 (*one tagger definition*), **not** Phase 6's criterion 1 (CONF-01, copy-not-move).
The two are different criteria in different phases and the comment does not establish a CONF-01 link
by itself.

**Scope fence — read this before touching anything.** This plan adds **no new assertion logic**,
changes **no** pass/fail predicate, and moves **no** pinned constant. `DECLARED_INTERP_EXPECTED`
stays at its current default and `TAGGER_DEF_EXPECTED` stays at `2`. The `check-beets-config.sh`
`ST_PLANNED_CASES=7` third of WR-03 is **NOT** touched by this plan and is dispositioned ACCEPTED
in `06-50` — round 4 excluded it by name as a deliberate refusal (`DEF-06-39-02`, and
`06-DISPOSITIONS-GAP3.md` § *Verified-and-clean* item 7: it is a gated pin over an
**unconditionally executed** set, and "fixing" it would break a working guard).

**The `HEAD --` rule, stated once and binding on every byte-identity criterion in this plan.** A
bare `git diff --exit-code <path>` compares the working tree against the **index**, not against a
commit. With an edit *staged*, it exits **0** — driven in a scratch repo: `git diff --exit-code
protected.txt` exits 0 while `git diff --exit-code HEAD -- protected.txt` exits 1. This plan stages
and commits while it runs, so the blind state is the likely one. **Every byte-identity guarantee
here is written `git diff --exit-code HEAD -- <path>`; the `HEAD --` is load-bearing and is not to
be dropped.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: R6-01 — name the two unused columns in the tagger-capable inventory read</name>
  <files>scripts/check-music-freeze.sh</files>
  <read_first>
    - `scripts/check-music-freeze.sh` — the whole `while IFS='|' read -r xn xs xsrc xdst xflag` loop
      at **:1044** and its enclosing `while IFS='|' read -r tn ts` loop, so you can see which of the
      five fields the body actually consumes and where the row shape is produced upstream. Read the
      producer of those `|`-separated rows too, so the column ORDER is read rather than assumed.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` § WR-01 — the exact finding
      and the exact one-line fix it proposes.
  </read_first>
  <action>
At `scripts/check-music-freeze.sh:1044`, change the five-field destructuring read so the two
columns the loop body does not consume are bound to underscore-prefixed names rather than to plain
names: `xs` becomes `_xs` and `xdst` becomes `_xdst`. `xn`, `xsrc` and `xflag` are unchanged. The
resulting binding list is, in order: `xn _xs xsrc _xdst xflag`.

Take the **named** form, not the review's alternative `read -r xn _ xsrc _ xflag`. Two bare
underscores discard the column NAMES, and the whole reason WR-01 is a finding rather than a nit is
that a five-field read with anonymous positions is where a future column insertion hides. The
underscore prefix is the shellcheck-recognised "deliberately unused" convention AND keeps the
column identity legible.

Add **at most one** short in-band comment line immediately above the changed line, citing `R6-01`
and stating in one clause that the underscore prefix marks a column that is parsed but not
consumed, so the field COUNT stays five. Do not write any round narrative, any history, or any
cross-reference to prior rounds at this site — round 6's position on `WR-02` (see `06-50`) is that
in-band historical annotation is the cost, and this round does not add to it. One line, one clause.

Before editing, confirm by reading that neither `$xs` nor `$xdst` is referenced anywhere in the
loop body on a non-comment line; if either IS referenced, STOP and report — that would make WR-01
a wrong finding and this plan's premise false.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n scripts/check-music-freeze.sh && shellcheck -S warning scripts/check-music-freeze.sh && test "$(/usr/bin/grep -cE "IFS='\|' read -r xn _xs xsrc _xdst xflag" scripts/check-music-freeze.sh)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -S warning scripts/check-music-freeze.sh` exits **0** and prints **zero** lines of
      output. (It currently exits non-zero with two SC2034 findings — capture the before-state first.)
    - `shellcheck -S warning` exits 0 with zero output on **all six** reviewed scripts:
      `check-beets-config.sh`, `check-music-consumers.sh`, `check-music-freeze.sh`,
      `phase06-incremental-control.sh`, `phase06-oracle.sh`, `quick-health-check.sh`.
    - `bash -n scripts/check-music-freeze.sh` exits 0.
    - `/usr/bin/grep -cE "IFS='\|' read -r xn _xs xsrc _xdst xflag" scripts/check-music-freeze.sh`
      returns exactly **1**.
    - No **non-comment** line of `scripts/check-music-freeze.sh` references `$xs` or `$xdst`.
      Measure with the comment-stripped recipe, never a raw count:
      `/usr/bin/grep -vE '^[[:space:]]*#' scripts/check-music-freeze.sh | /usr/bin/grep -cE '\$\{?(xs|xdst)\b'`
      returns **0**.
      ⚠ **This is a zero-expecting count, so DRIVE it before trusting it.** Run the identical `-cE`
      recipe against a one-line scratch control containing `echo "$xs $xdst"` and observe it return
      **≥ 1**. Only then is the **0** against the real script evidence that the references are gone,
      rather than evidence that the pattern matches nothing. A `-eq 0` that passes because the
      instrument cannot detect is the `DEF-06-45-04` class, and it is the defect this round exists
      to stop repeating. Capture both drives verbatim into `artifacts/06-46-freeze-fixes.txt`.
    - The field COUNT of that read is still five: the changed line binds exactly five names.
    - `git diff --stat` for this task shows `scripts/check-music-freeze.sh` only, with at most
      **2** insertions and **1** deletion (the changed read line, plus at most one comment line).
  </acceptance_criteria>
  <done>`shellcheck -S warning` is clean on all six reviewed scripts; the five-field read names every column; nothing else in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: R6-03(a) — make both pinned-count failures print the literal edit they require</name>
  <files>scripts/check-music-freeze.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-46-freeze-fixes.txt</files>
  <read_first>
    - `scripts/check-music-freeze.sh:489-520` — the `DECLARED_INTERP_EXPECTED` block: its header
      comment at `:495` (which already documents the override as existing so the failure branch can
      be driven), the `${VAR:-12}` default at `:508`, the `-eq` gate at `:511`, the `info` arm at
      `:512` and the `fail` arm at `:514` with its row dump at `:515`.
    - `scripts/check-music-freeze.sh:820-844` — the `TAGGER_DEF_EXPECTED` block: the member-and-count
      comparison at `:830`, the pass arm naming both definitions at `:831-836`, and the fail arm at
      `:838-842`. Note `:838`'s text already says "exactly this named pair".
    - `scripts/check-music-freeze.sh:201-204` — the `fail`/`pass`/`warn`/`info` helpers. `fail`
      increments `FAILURES`; `echo` does not. This matters: the remediation line must NOT be a
      second `fail`.
    - `scripts/check-music-freeze.sh:145-156` — the `TAGGER_DEF_*` constants and the comment stating
      the pair is asserted BY NAME AND BY CLASS, never by count alone.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` § WR-03 — fix option (a).
    - `.planning/phases/05-inbox-structure-and-the-junk-gate/deferred-items.md:10-40` — the
      pre-existing Phase 5 entry recording this exact trap firing in the field, so the remediation
      wording matches the remedy already written down rather than inventing a second one.
  </read_first>
  <action>
Two edits, both **emitted output** in an existing failure branch — not comments, not new
predicates, not new `fail` calls.

**(a) The interpolated-host-path arm.** Immediately after the existing row dump
(`printf '%s\n' "$INTERP_ROWS" | sed 's/^/         /'`, currently `:515`) and still inside the
`else` arm, add a plain `echo` at the same `         ` (9-space) continuation indent the dump uses,
emitting a remediation sentence that contains, concretely:
  1. the literal constant name `DECLARED_INTERP_EXPECTED` and its assignment site in this file,
     cited as `scripts/check-music-freeze.sh` plus the *constant name*, **never a line number** —
     line citations in this repo go stale on arrival;
  2. the two values, interpolated live: the currently pinned value and the freshly measured
     `$INTERP_COUNT`, phrased so the reader sees the literal target state (pin moves from the first
     to the second);
  3. the required precondition in one clause — every new line above must be READ BY HAND first,
     because these lines are invisible to §2's driving grep and §2 is `UNKNOWN`, not clean, until
     they are;
  4. the requirement that the pin move **in the same commit** as the change that moved the count;
  5. an explicit prohibition: do **not** export the `DECLARED_INTERP_EXPECTED` environment override
     to silence this — the override exists so the check can be made REDDER (to drive the failure
     branch), and using it to make a red check green inverts the one rule the `${VAR:-default}`
     contract rests on. Write the sentence so it names the override's **only** sanctioned use, and
     keep it consistent with the script's own header at this constant, which already states and
     accepts that the knob can move a red to a green *by declaration* and resolves nothing. Do not
     contradict that header: the prohibition is a **policy** on top of a documented capability, and
     saying so is what makes it honest. (`06-49` carries the same distinction into `CONVENTIONS.md`
     — see R6-04 convention 4.)

**(b) The tagger-definition arm.** Immediately after the existing `found:` dump
(`printf '%s\n' "$DEF_FILES" | sed 's/^/           /'`, currently `:842`) and still inside the
`else` arm, before the closing `fi`, add a plain `echo` at the same 9-space continuation indent used
by `:839-841`, emitting a remediation sentence containing, concretely:
  1. the fact that the remedy is **not** to bump `TAGGER_DEF_EXPECTED`;
  2. that a third definition must be added the same way the existing two are — a
     `TAGGER_DEF_<NAME>` path constant **and** a `TAGGER_DEF_<NAME>_CLASS` one-line class string,
     added to the expected SET built at the `DEF_EXPECTED` sort, with `TAGGER_DEF_EXPECTED` moved in
     the same commit;
  3. the reason in one clause, taken from D-11 and the constant's own header: a bare count cannot
     read as a pass, so an unnamed third definition must keep failing.

Use `echo` (not `printf` with a format built from data) so no value can be interpreted as a format
string. Interpolate values only with `"$VAR"` inside the double-quoted argument. Both lines are
*failure output*, so they cost nothing on a green run.

Add **at most one** short `R6-03` citation comment per edited arm, one clause each. No round
narrative, no history. See task 1's scope fence — the same rule binds here.

**Then drive both arms.** These branches are reachable only when the counts disagree, and
`check-music-freeze.sh` as a whole contacts the estate, so drive them from an **extracted scratch
harness** rather than a live run (this phase's established synthetic-fixture pattern):
  - Copy the two blocks' text out of the committed file into a scratch script under the session
    scratch directory, prefixed by stub `fail`/`pass`/`info`/`echo`-compatible helpers and stub
    values for `INTERP_ROWS`, `INTERP_COUNT`, `DEF_FILES`, `DEF_EXPECTED`, `TAGGER_DEFS`.
  - Drive each block **both ways**: matching counts (green arm, remediation line ABSENT) and
    mismatched counts (red arm, remediation line PRESENT with the stub values correctly
    interpolated).
  - Record all four outcomes, plus the `shellcheck` before/after matrix from task 1 and task 1's
    driven control for the `$xs`/`$xdst` recipe, into `artifacts/06-46-freeze-fixes.txt`.

The harness proves the arm's TEXT and its interpolation; it does not prove the live block. Say so
in the artifact in one clause — an undriven claim dressed as a driven one is the defect class this
phase exists to remove.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n scripts/check-music-freeze.sh && shellcheck -S warning scripts/check-music-freeze.sh && test "$(/usr/bin/grep -vE '^[[:space:]]*#' scripts/check-music-freeze.sh | /usr/bin/grep -c 'remediation')" -eq 2 && test "$(/usr/bin/grep -vE '^[[:space:]]*#' scripts/check-music-freeze.sh | /usr/bin/grep -cE '^[[:space:]]*fail ')" -eq 24 && git diff --exit-code HEAD -- scripts/check-beets-config.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-music-freeze.sh` exits 0 and `shellcheck -S warning` exits 0 with zero
      output.
    - Exactly **two** non-comment lines in `scripts/check-music-freeze.sh` emit a remediation
      sentence — one per fail arm. Measured comment-stripped, never raw:
      `/usr/bin/grep -vE '^[[:space:]]*#' scripts/check-music-freeze.sh | /usr/bin/grep -c 'remediation'` returns **2**.
      The word `remediation` must appear as a **literal substring of the emitted `echo` argument**
      on each arm — the comment-stripping filter removes `#` lines, so an `R6-03` comment carrying
      the word does not count toward this figure.
    - The comment-stripped count of `fail ` call sites is **24**, unchanged. That figure was measured
      at this plan's base commit with
      `/usr/bin/grep -vE '^[[:space:]]*#' scripts/check-music-freeze.sh | /usr/bin/grep -cE '^[[:space:]]*fail '`
      and is stated here so a drift is visible; re-measure it at the base commit before trusting it,
      and if it reads otherwise, record the measured value and use that. A remediation line that
      incremented `FAILURES` would double-count one violation.
    - The interpolated-host-path remediation text contains the literal string
      `DECLARED_INTERP_EXPECTED`, contains no hard-coded line number, and interpolates both
      `$DECLARED_INTERP_EXPECTED` and `$INTERP_COUNT`.
    - The tagger remediation text contains the literal strings `TAGGER_DEF_EXPECTED` and
      `_CLASS`, and contains a prohibition on bumping the count alone.
    - `DECLARED_INTERP_EXPECTED`'s default is **unchanged** at its base-commit value and
      `TAGGER_DEF_EXPECTED` is **unchanged** at `2`. Assert both by grep against the base commit
      (`git show <base>:scripts/check-music-freeze.sh`), not against the working tree.
    - `scripts/check-beets-config.sh` is **byte-identical** to its base-commit state:
      `git diff --exit-code HEAD -- scripts/check-beets-config.sh` returns **0**. The `HEAD --` is
      load-bearing — see the objective's `HEAD --` rule; a bare `git diff --exit-code <path>` is
      blind to a staged edit and this plan stages. WR-03's `ST_PLANNED_CASES` third is not touched
      here.
    - `artifacts/06-46-freeze-fixes.txt` exists and records **four** driven outcomes: interp-green
      (no remediation line), interp-red (remediation line present, both stub values interpolated
      into it), tagger-green (no remediation line), tagger-red (remediation line present) — each
      with the harness's actual captured output pasted, not narrated.
    - The artifact records task 1's **driven control** for the `$xs`/`$xdst` zero-expecting recipe:
      the control line, the recipe's non-zero count against it, and its **0** against the real
      script — both captured verbatim.
    - The artifact states in one clause that the harness proves the arm text and interpolation, and
      does **not** prove the live block (which needs estate contact this plan makes none of).
    - The artifact records the `shellcheck -S warning` before/after matrix across all six scripts:
      before = 5 clean / 1 with two SC2034 findings; after = 6 clean.
    - `check-beets-config.sh --self-test` exits **0** and reports **7** cases (6 of them red), and
      `phase06-oracle.sh --self-test` exits **0** at **140** cases — unchanged, because this plan
      touches neither. Both figures are the named-reference-environment values
      (`06-DISPOSITIONS-GAP3.md`); if this machine is root or lacks `python3` the oracle figure will
      legitimately differ and the artifact must say which environment it ran in.
    - The artifact contains no credential, no API key and no NUL byte (`file` reports `text`).
  </acceptance_criteria>
  <done>Both pinned-count failures name the literal edit they require and the one thing not to do; both arms driven in both directions from an extracted harness and recorded; no predicate, no constant and no `fail` count changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → deployed instrument on LXC 100 | `scripts/check-music-freeze.sh` is executed on a host running ~103 containers, folded in by `quick-health-check.sh`. A change that weakens a gate ships to a live estate. |
| script → operator-supplied env override | `DECLARED_INTERP_EXPECTED` is a `${VAR:-default}` knob an operator can set at run time. |
| script → emitted output | Values measured from the tree (`$INTERP_COUNT`, `$DEF_FILES`) are interpolated into text printed to a terminal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06R6-01 | Tampering | `check-music-freeze.sh` pass/fail predicates | mitigate | This plan changes no predicate and no pinned constant; acceptance criteria assert `DECLARED_INTERP_EXPECTED` and `TAGGER_DEF_EXPECTED` byte-unchanged against the **base commit** (not the index), and assert the comment-stripped `fail ` call-site count is identical. |
| T-06R6-02 | Tampering | the `${VAR:-default}` override contract | mitigate | The new remediation text explicitly prohibits using the env override to silence a red, and does so without contradicting the constant's own header, which already documents that the knob can declare a red green and resolve nothing. The prohibition is stated as policy over a documented capability. |
| T-06R6-03 | Information disclosure | emitted remediation text | mitigate | Only `$DECLARED_INTERP_EXPECTED`, `$INTERP_COUNT` and the already-printed `$DEF_FILES` reach the new lines; all are counts or repo-relative paths already printed by the surrounding block. No credential, no host, no key. |
| T-06R6-04 | Tampering | format-string injection through measured data | mitigate | The remediation lines use `echo` with a double-quoted argument, never `printf` with a data-derived format string. |
| T-06R6-05 | Denial of service | a syntax error shipping to the health entry point | mitigate | `bash -n` and `shellcheck -S warning` are acceptance criteria on every edit, and the round must end with six clean scripts where it started with five. |
| T-06R6-39 | Repudiation | a zero-expecting count accepted from a non-detecting recipe | mitigate | Every `-eq 0` criterion in this plan must be driven against a control that makes the same recipe return non-zero, with both drives captured in the artifact. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan performs **no** package-manager install of any kind. The row is retained so the absence is stated rather than assumed; `RESEARCH.md`'s Package Legitimacy Gate is not triggered. |
</threat_model>

<verification>
- `shellcheck -S warning` clean on all six reviewed scripts (was five of six).
- `bash -n` clean on `scripts/check-music-freeze.sh`.
- `check-beets-config.sh --self-test` exit 0 / 7 cases and `phase06-oracle.sh --self-test` exit 0 /
  140 cases, both unchanged (neither file is touched).
- `git diff --exit-code HEAD -- scripts/check-beets-config.sh` returns 0.
- `git diff` touches exactly one script and one new artifact.
- Every zero-expecting recipe published by this plan is driven against a control first.
- No estate contact of any kind: zero `ssh`, zero `docker`, zero `--run`.
</verification>

<success_criteria>
- WR-01 is closed with `shellcheck -S warning` clean across all six scripts.
- WR-03's two `check-music-freeze.sh` sites emit a self-describing remediation; its
  `ST_PLANNED_CASES=7` third is untouched and dispositioned ACCEPTED in `06-50`.
- No new assertion logic, no moved constant, no changed failure count.
- Both fail arms driven in both directions and the transcript committed.
- No `0` is published from a recipe that was not first shown capable of returning non-zero.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-46-SUMMARY.md` when done.
</output>
