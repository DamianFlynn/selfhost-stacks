---
phase: 06-tagger-configuration-and-dry-run
plan: 21
type: execute
wave: 9
depends_on: ["06-15", "06-16", "06-17", "06-18", "06-19", "06-20"]
gap_closure: true
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md
  - .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
  - .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
  - .planning/ROADMAP.md
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, CONF-06]
closes_findings: [WR-09, all-remaining-dispositions]

must_haves:
  truths:
    - "Every one of the 24 findings in `06-REVIEW.md` — CR-01, WR-01 through WR-10, IN-01 through IN-13 — carries an explicit disposition: fixed (naming the plan and the commit), accepted (with a stated reason), or carried forward (as a named entry that another document references)"
    - "`06-REVIEW.md` is WIRED: it is reachable from a document the next phase actually reads, so the finding set cannot be lost the next time nobody re-reads that file"
    - "CR-01's residue — the named D-04 exemption register and its pinned baseline — is carried into ROADMAP's Phase 7 entry-criteria list, so Phase 7 revisits it rather than inheriting it silently"
    - "WR-09 is disposed honestly: the `/downloads:rw` + `01-auto` autotag path is named as NOT covered by the three controls `config.yaml` claims, the un-released `tank/downloads@pre-phase5` snapshot is named as what actually bounds it, and flipping `import.write` is scheduled beside the Phase 7 `rw` grant"
    - "No CONF-04 verdict, no requirement checkbox and no phase-status wording is changed by this plan — the disposition register records what was done, it does not re-close anything"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md"
      provides: "the 24-row disposition register with a status per finding"
      contains: "CR-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 entry criteria E10 and E11 carrying CR-01's residue and WR-09"
      contains: "E10."
  key_links:
    - from: ".planning/ROADMAP.md Phase 7 entry criteria"
      to: ".planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md"
      via: "a named entry criterion citing the review and the disposition register by path"
      pattern: "06-DISPOSITIONS"
---

<objective>
Wire the code review into the project's memory, and give every finding a disposition.

Purpose: `06-VERIFICATION.md`'s Required-Artifacts table marks `06-REVIEW.md` **NOT WIRED** — *"The Critical finding (CR-01) and all 10 Warnings remain unaddressed; no fix commit, no override, no carry-forward reference."* Plans 06-15 through 06-20 supply the fixes. This plan supplies the part a fix commit does not: a register stating what happened to each of the 24 findings, and a reference to that register from a document the next phase actually opens. The failure mode being closed is the one the gap statement names directly — a Critical and ten Warnings sitting in a file nothing else points at.

This plan also disposes WR-09, which is the one substantive finding no earlier plan touches, because its fix would change `config.yaml` and therefore change the sha256 of the very object every CONF-01 / CONF-02 / CONF-05 proof in this phase was taken against. That is a real dependency conflict, not a difficulty judgement, and it is why WR-09 is documented and carried rather than coded.

Output: a disposition register, four carry-forward entries, two new Phase 7 entry criteria, and a runbook that names the uncovered write path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-15-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-16-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-17-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-18-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-19-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-20-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 24-row disposition register, built from what the plans actually did</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md, .planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md` IN FULL — all 24 findings with their IDs, file:line references and proposed fixes, plus the `findings:` frontmatter counts (critical 1, warning 10, info 13, total 24) and the § *Notes on things checked and found sound*.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md:54-72` — the Required Artifacts table's `06-REVIEW.md` **NOT WIRED** row and the Key Link table's **PARTIAL** row, which state exactly what "wired" means here.
    - All six summaries: `06-15-SUMMARY.md` through `06-20-SUMMARY.md`. **Read what each plan RECORDS, not what its plan file intended.** Several plans allow a choice (`expect_ne` called or deleted; `remote_is_blind` used or deleted; the WR-06 double-report kept or suppressed) and several record NOT-DRIVEN items. The register must reflect the recorded outcome, and an item a plan recorded as undriven is `FIXED (undriven)`, not `FIXED`.
    - `git log --oneline` since the review's `diff_base` — to cite the actual commit for each fixed finding rather than the plan number alone.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — the existing `DEF-06-*` format and its level of detail (found-during / what / why deferred / why it is or is not urgent). New entries must match it.
  </read_first>
  <action>
**Create `06-DISPOSITIONS.md`** with one row per finding, all 24, in review order (CR-01, WR-01…WR-10, IN-01…IN-13). Every row carries: the finding ID, a one-clause restatement of the defect, the disposition, and the evidence.

The disposition vocabulary is closed — use exactly these four and no others:

- **FIXED** — a commit changed the behaviour AND the changed branch was driven. Cite the plan number, the commit hash and the artifact file that holds the driven transcript.
- **FIXED (undriven)** — a commit changed the behaviour but the branch could not be driven in this phase. Cite the plan, the commit, the artifact, AND the named condition that would drive it. This is the register's honest middle and must not be collapsed into FIXED.
- **ACCEPTED** — deliberately not changed. Cite the reason, and the reason must be one of: the change would invalidate a committed proof artifact; the change requires running an instrument this gap-closure is forbidden to run; or the finding's own text establishes it as hygiene with a working compensating control that is named. "Low priority" is not a reason and must not appear.
- **CARRIED** — deferred by name. Cite the `DEF-06-21-NN` entry or the ROADMAP entry criterion that now owns it. A CARRIED row whose target does not exist yet is a defect in this plan.

**Anchor the register to the verification's own accounting.** Add a short header block stating the source review's date and `diff_base`, the four counts (24 total; 1 critical, 10 warning, 13 info), and the count in each disposition class. The class counts must sum to 24; state the sum explicitly so an arithmetic slip is visible rather than latent.

**Do not invent dispositions for findings the plans did not reach.** If a summary records an outcome the register cannot classify with the four words above, stop and say so in the summary rather than forcing it into a class.

**Then wire `06-REVIEW.md`.** Append a short, clearly dated block at its end — do NOT edit any finding text, and do not alter its frontmatter counts; the review is a record of what was found on 2026-09-21 and that record stands. The block states that every finding has since been dispositioned, names `06-DISPOSITIONS.md` by path, names the gap-closure plans by number, and names the verification that flagged the file NOT WIRED.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; d=.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS.md; r=.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md; test -s "$d" || { echo "register missing"; exit 1; }; miss=""; for k in CR-01 WR-01 WR-02 WR-03 WR-04 WR-05 WR-06 WR-07 WR-08 WR-09 WR-10 IN-01 IN-02 IN-03 IN-04 IN-05 IN-06 IN-07 IN-08 IN-09 IN-10 IN-11 IN-12 IN-13; do grep -q "\b$k\b" "$d" || miss="$miss $k"; done; [ -z "$miss" ] || { echo "findings absent from the register:$miss"; exit 1; }; n=$(grep -cE "FIXED|ACCEPTED|CARRIED" "$d"); [ "$n" -ge 24 ] || { echo "only $n disposition tokens for 24 findings"; exit 1; }; grep -qiE "low priority" "$d" && { echo "\"low priority\" used as a reason - forbidden"; exit 1; }; grep -q "24" "$d" || { echo "the count reconciliation is missing"; exit 1; }; grep -q "06-DISPOSITIONS" "$r" || { echo "06-REVIEW.md does not point at the register - still NOT WIRED"; exit 1; }; grep -c "critical: 1" "$r" >/dev/null && grep -q "warning: 10" "$r" || { echo "the review frontmatter counts were altered"; exit 1; }; echo "06-21 task 1 OK"'</automated>
  </verify>
  <done>
    `06-DISPOSITIONS.md` holds 24 rows, one per finding ID, each with a disposition drawn from the closed four-word vocabulary, an evidence citation (plan number plus commit hash plus artifact path for FIXED rows, the driving condition for FIXED (undriven) rows, an allowed reason for ACCEPTED rows, a named target for CARRIED rows), and a header block whose class counts sum to 24 with the sum stated. `06-REVIEW.md` carries a dated appended block naming the register, the plans and the verification, with every finding's text and the frontmatter counts unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Carry CR-01's residue and WR-09 into ROADMAP Phase 7 and into deferred-items.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md, stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - `.planning/ROADMAP.md:1028-1104` — the Phase 7 *Entry criteria inherited from Phase 6* block in full, E1 through **E9** (note: the gap statement says E1–E8; the list actually runs to E9, so the new entries are **E10** and **E11** — count them in the file before writing, do not trust either number secondhand). Read the block's preamble, which states the reason the criteria live here: *"an item recorded where the next phase does not read is an item nobody owns."* That sentence is the whole argument for this task.
    - `.planning/ROADMAP.md` E3 (`:1059`) — the `rw` grant as Phase 7's FIRST act; E4 (`:1063`) — `tank/downloads@pre-phase5` NOT released; E6 (`:1075`) — CONF-04's Jellyfin half. E10 and E11 must reference E3 and E4 rather than restating them.
    - `.planning/ROADMAP.md:1208` — the Phase 6 status row, `Closed — 1 open requirement`. **This row is updated only to note the gap closure, never to change the CONF-04 disposition or the `Closed` wording.**
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:374-401` — WR-09 verbatim: `config.yaml:333-339` sets `write: yes` and names three controls (the overlay, the `:ro` mount D-05, the statefile sha256 D-29); all three protect `/media` or beets' own state; none protects `/downloads`, which is `:rw` at `flask.yaml:146` and holds all three registered inboxes; `01-auto` is registered with `autotag: auto` at `flask-config.yaml:80-83`; the beets-flask watchdog is the active runtime with `restart: unless-stopped`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-16-SUMMARY.md` — the final D-04 exemption register contents and its pinned baseline, which is what E10 carries.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — the existing five entries' format, and note the file's own framing: *"Out-of-scope discoveries, written down rather than silently carried."*
    - `stacks/selfhosted/arrs/beets.md` — the Phase 6 section and its conventions (dated notes, superseded dispositions kept legible under a block quote, evidence cells holding runnable commands). Locate the `import.write` and `_inbox` discussion if any (`grep -n "import.write\|01-auto\|_inbox" stacks/selfhosted/arrs/beets.md`).
  </read_first>
  <action>
**Edit 1 — two new ROADMAP Phase 7 entry criteria.** Append them to the inherited block, numbered from whatever the last existing criterion is (verify by counting in the file).

- **CR-01's residue.** State: the D-04 "no bare `beet` invocation" assertion was vacuous for the whole of Phase 6 and was repaired by gap-closure plans 06-15 and 06-16; it now carries a NAMED exemption register with a pinned baseline covering the oracle's and the incremental control's overlay-only invocations. Phase 7 must revisit the exemption rather than inherit it: any new `beet` invocation Phase 7 adds must be compliant or must be added to the register deliberately, and the pinned count moving is a red by design. Name `06-REVIEW.md` and `06-DISPOSITIONS.md` by path so the finding set is reachable from here.
- **WR-09.** State: `import.write: yes` is live in the vendored config while `/downloads` is mounted `:rw` and the `01-auto` inbox is registered with `autotag: auto` under an always-restarting watchdog. The three controls the config names all protect `/media` or beets' own state; **none of them protects `/downloads`.** What actually bounds the exposure today is that nothing automatic stages into `_inbox/` and that `tank/downloads@pre-phase5` is un-released (E4) — and "nobody has put a file there" is not a control. Phase 7 must decide `import.write` deliberately in the same commit as the `rw` grant (E3), which is where every other Phase-7 behaviour flag is already scheduled to move.

**Do not renumber existing criteria** and do not edit E1–E9's text.

**Edit 2 — update the Phase 6 status row at `:1208`,** additively. Add a dated clause naming the gap closure: that `06-VERIFICATION.md` found 1 of 6 must-haves failed (the D-04 assertion), that gap-closure plans 06-15 through 06-21 closed it, and that the 24 review findings are dispositioned in `06-DISPOSITIONS.md`. **Leave `Closed — 1 open requirement` exactly as it reads**, leave the CONF-04 two-verdict sentence exactly as it reads, and leave the `tank/downloads@pre-phase5` sentence exactly as it reads. This plan records; it does not re-close.

**Edit 3 — new `deferred-items.md` entries,** in the file's existing format (found-during / what / why deferred / urgency). Write one entry per CARRIED row in the register from task 1. At minimum:

- `DEF-06-21-01` — WR-09's uncovered `/downloads:rw` + `01-auto` write path. Say plainly why it was not fixed here: the review's stronger option is to set `import.write: no` in the vendored `config.yaml`, and any edit to `config.yaml` changes the sha256 of the exact object every CONF-01 / CONF-02 / CONF-05 assertion in this phase was measured against, plus the appdata copy the drift block compares. That is a dependency conflict with a committed proof, not a judgement that the fix is hard. Name the snapshot (E4) as the real bound and the Phase 7 `rw` grant (E3) as the scheduled decision point.
- One entry per FIXED (undriven) item that plans 06-18, 06-19 and 06-20 recorded, each naming the condition that would drive it.

**Edit 4 — `stacks/selfhosted/arrs/beets.md`.** Add the WR-09 statement to the runbook, in the file's own dated-note convention, beside wherever the config's controls are described. It must name: the uncovered path (`/downloads:rw`, the `01-auto` inbox, `autotag: auto`, the always-restarting watchdog), the three controls that do not reach it, the two things that actually bound it today, and the Phase 7 decision point. The runbook is where someone stands when they are about to drop a folder into an inbox; a control gap recorded only in a planning artifact is not recorded where it is needed.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; R=.planning/ROADMAP.md; D=.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md; B=stacks/selfhosted/arrs/beets.md; n=$(grep -cE "^  E[0-9]+\. \*\*" "$R"); [ "$n" -ge 11 ] || { echo "expected at least 11 E-criteria across the roadmap, found $n"; exit 1; }; awk "/Entry criteria inherited from Phase 6/,/^\*\*Plans\*\*/" "$R" > /tmp/p6e.txt; grep -q "E10\." /tmp/p6e.txt || { echo "E10 not added to the Phase 7 inherited block"; exit 1; }; grep -q "E11\." /tmp/p6e.txt || { echo "E11 not added to the Phase 7 inherited block"; exit 1; }; grep -q "06-DISPOSITIONS" /tmp/p6e.txt || { echo "the review/disposition register is not reachable from the Phase 7 criteria"; exit 1; }; grep -qE "D-04|exemption" /tmp/p6e.txt || { echo "CR-01 residue is not carried"; exit 1; }; grep -q "import.write" /tmp/p6e.txt || { echo "WR-09 is not carried"; exit 1; }; grep -q "Closed — 1 open requirement" "$R" || { echo "the Phase 6 status wording was changed"; exit 1; }; for k in E1 E2 E3 E4 E5 E6 E7 E8 E9; do grep -q "  $k\." /tmp/p6e.txt || { echo "existing criterion $k was lost or renumbered"; exit 1; }; done; grep -q "DEF-06-21-01" "$D" || { echo "DEF-06-21-01 missing"; exit 1; }; grep -q "01-auto" "$D" || { echo "the WR-09 deferral does not name the inbox"; exit 1; }; grep -q "01-auto" "$B" || { echo "the runbook does not carry the WR-09 statement"; exit 1; }; git diff --name-only | grep -q "REQUIREMENTS.md" && { echo "REQUIREMENTS.md must not change"; exit 1; }; git diff .planning/ROADMAP.md | grep "^-" | grep -qE "E[1-9]\. \*\*" && { echo "an existing E-criterion line was removed"; exit 1; }; echo "06-21 task 2 OK"'</automated>
  </verify>
  <done>
    The Phase 7 inherited-criteria block gained E10 (CR-01's residue: the repaired D-04 assertion, its named exemption register and pinned baseline, with `06-REVIEW.md` and `06-DISPOSITIONS.md` cited by path) and E11 (WR-09: the uncovered `/downloads:rw` + `01-auto` write path, the three controls that do not reach it, the snapshot and the empty-inbox state as the real bounds, and the `import.write` decision scheduled beside E3's `rw` grant), with E1–E9 untouched. The Phase 6 status row gained a dated additive clause and kept `Closed — 1 open requirement` and its CONF-04 and snapshot sentences verbatim. `deferred-items.md` gained `DEF-06-21-01` plus one entry per FIXED (undriven) item, each naming its driving condition. `beets.md` carries the WR-09 statement in its dated-note convention. `REQUIREMENTS.md` is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a phase artifact (`06-REVIEW.md`) → the next phase's reading list | The documented failure mode: an item recorded where the next phase does not read is an item nobody owns. This plan is entirely about crossing that boundary. |
| a disposition register → a future reader's belief about what was fixed | A register that says FIXED for something only written and never driven manufactures confidence. This is why FIXED (undriven) is a separate class. |
| this public repo → the world | `beets.md` and the ROADMAP are public. WR-09's text names paths and mount modes, which is fine; it must name no credential, no token, and no host password. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-120 | Repudiation | A Critical and ten Warnings surviving in a file nothing else references, so the finding set is lost at the next context boundary | mitigate | `06-DISPOSITIONS.md` with all 24 IDs, referenced from `06-REVIEW.md`'s appended block and from two ROADMAP Phase 7 entry criteria; the verify command asserts every ID is present and that the ROADMAP block cites the register. |
| T-06-121 | Spoofing | A disposition of FIXED recorded for a change that was written but never exercised, so a later reader believes a branch was proven | mitigate | The closed four-word vocabulary with a mandatory separate FIXED (undriven) class; each such row must name the condition that would drive it, and each gets a `deferred-items.md` entry. |
| T-06-122 | Tampering | The register being padded to 24 by inventing a disposition for a finding no plan reached | mitigate | The register is built from the six summaries' recorded outcomes, not from the plan files' intentions; the action forbids forcing an unclassifiable outcome into a class and requires the class counts to sum to 24 with the sum stated. |
| T-06-123 | Tampering | A documentation plan quietly re-closing CONF-04, ticking a requirement, or softening the `Closed — 1 open requirement` wording | mitigate | Explicit out-of-scope statement; the verify command asserts the status wording is byte-present and that `REQUIREMENTS.md` does not appear in the diff. |
| T-06-124 | Tampering | Existing ROADMAP entry criteria E1–E9 renumbered or dropped while appending E10/E11 | mitigate | The verify command asserts each of E1–E9 is still present in the block and that no `E<n>. **` line was removed in the diff. |
| T-06-125 | Elevation of Privilege | Editing `config.yaml` to "fix" WR-09 and thereby invalidating the sha256 of the object every CONF-01/02/05 proof was measured against, and desynchronising the drift block's repo-vs-appdata comparison | mitigate | WR-09 is dispositioned CARRIED with that dependency conflict stated as the reason; `config.yaml` is not in this plan's `files_modified` and must not be edited. |
| T-06-126 | Information Disclosure | A credential entering the public runbook while documenting the uncovered write path | mitigate | The WR-09 statement names paths, mount modes and inbox names only. No secret is involved in the finding and none may be added; the repo's standing rule is credentials live in `~/.claude/secrets/` by variable name only. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and touches no runtime. |
</threat_model>

<verification>
- All 24 finding IDs appear in `06-DISPOSITIONS.md`, each with a disposition from the closed vocabulary, and the class counts sum to 24.
- `06-REVIEW.md` points at the register; its finding text and frontmatter counts are unchanged.
- ROADMAP's Phase 7 inherited block carries E10 and E11, cites `06-DISPOSITIONS.md`, and retains E1–E9 verbatim.
- The Phase 6 status row still reads `Closed — 1 open requirement`.
- `deferred-items.md` carries `DEF-06-21-01` plus one entry per FIXED (undriven) item.
- `REQUIREMENTS.md` is untouched.
</verification>

<success_criteria>
- No finding in `06-REVIEW.md` is undisposed.
- The review is reachable from where the next phase reads.
- WR-09's uncovered write path is named in the runbook, not only in a planning file.
- Nothing is re-closed, re-ticked or re-worded.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-21-SUMMARY.md` when done. Quote the disposition class counts and their sum, so a re-verification can check the arithmetic without re-reading the register.
</output>
