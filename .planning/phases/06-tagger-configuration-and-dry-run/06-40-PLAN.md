---
phase: 06-tagger-configuration-and-dry-run
plan: 40
type: execute
wave: 18
depends_on: ["06-39"]
gap_closure: true
requirements: [CONF-04]
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt
autonomous: false

must_haves:
  truths:
    - "The exact pre-touch value of every fact the re-probe could move is recorded, measured, and dated — so 'it changed' is a diff and not an impression"
    - "The three pinned host paths resolve to exactly three existing files on atlantis, and none of them is in the OQ-1 DO-NOT-RESCAN set"
    - "Nothing under /mnt/tank/media/Music was written, moved, chowned, chmodded or snapshotted by this plan"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt"
      provides: "the complete before-state: 1,244-row ArtistItems census, the three pinned rows in full, the four D-34 options, per-.nfo sha256+mtime over the Music dataset, and the resolved touch list"
      contains: "SECTION 1"
      min_lines: 60
  key_links:
    - from: "artifacts/06-40-conf04-reprobe-before.txt"
      to: "scripts/check-music-consumers.sh ARTIST_PROOF_ROWS"
      via: "the three pinned internal paths, quoted byte-exactly including U+2019"
      pattern: "Jewels n"
---

<objective>
Take the complete, dated before-state for the CONF-04 Jellyfin-half re-probe, without writing
anything.

The operator has decided to drive the re-probe inside Phase 6 rather than carry CONF-04 to Phase 7
entry criterion E6. Plan 06-03 measured that a targeted Default-mode refresh does **not** re-run
Jellyfin's audio prober on a file whose mtime has not changed, and named the lever in its own
artifact: *"(b) the file's mtime changing"*. Round 5 drives that lever on exactly three files.

This plan is the read-only half. It exists so that the plan which mutates (06-41) has something to
be diffed against, and so that a failure is diagnosable rather than a shrug.

Purpose: make every fact the re-probe could move a recorded number before anything moves.
Output: one committed artifact, `06-40-conf04-reprobe-before.txt`.

⛔ THE HARD FENCE, RESTATED HERE BECAUSE IT BINDS EVERY PLAN IN THIS ROUND: the aggressive
per-item "replace all metadata" refresh mode is FORBIDDEN estate-wide (D-24, T-06-11 — Phase 1
measured it rewriting 83 of 91 `.nfo` with `SaveLocalMetadata` already off). It is not issued by
this plan, it is not a fallback for this plan, and no branch of this round may reach it. The only
scan endpoint this round issues is the same targeted `POST /Library/Media/Updated` plan 06-03
issued and proved safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-03-SUMMARY.md
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-before.txt
@.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-after.txt
@scripts/check-music-consumers.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Jellyfin before-state — census, the three pinned rows, and the D-34 options</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt</files>
  <read_first>
    - `scripts/check-music-consumers.sh` — read the `JELLYFIN ADDRESSING` header block, the
      `JELLYFIN_D34_OPTIONS` table, the `ARTIST_PROOF_ROWS` table, `jf_api()`, and sections 4a and
      4b. This file is the instrument whose verdict this round is trying to move; every path,
      field name and library id used below must come from it verbatim, not be retyped from memory.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-before.txt`
      — the artifact whose format this one mirrors, and whose census recipe is reused unchanged.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md` § D-22, D-24, D-34.
  </read_first>
  <action>
    Run from LXC 100 (`root@172.16.1.159`, from `/mnt/fast/stacks`). Every Jellyfin call is a GET.

    Resolve the Jellyfin route FRESH via `docker inspect jellyfin` on the `t3_proxy` network, as
    `check-music-consumers.sh` does. Never pin a literal address: 06-03 recorded `192.168.90.17`
    and the container is at `192.168.90.25` today. Never use the bare name `jellyfin` (resolv.conf
    carries `search deercrest.info` and it resolves to Cloudflare's public edge), and never
    `172.16.1.76` (unreachable from LXC 100 under macvlan host isolation).

    Read the admin-equivalent API key from `/mnt/fast/secrets/jellyfin-deercrest.env`, sourced
    WITHOUT `set -a`, and deliver it to curl through the `-H @<(printf ...)` process-substitution
    form the script already uses, so the key never enters argv on a host whose `/proc` is
    world-readable and which has no `hidepid`. Gate on the file being mode `600` root-owned and
    record the measured mode in the artifact.

    Capture, in this order, into `06-40-conf04-reprobe-before.txt`:

    SECTION 0 — provenance. UTC timestamp, the host the commands ran on, `GET /System/Info`
    version, the resolved route and how it was resolved, the Music library `ItemId` read back from
    `GET /Library/VirtualFolders` (assert it still equals `JELLYFIN_MUSIC_LIBRARY_ID` in the
    script; a mismatch means the library was recreated and this whole round is against the wrong
    object), and the measured mode of the credential file.

    SECTION 1 — the four D-34 options, read from the Music library object in
    `GET /Library/VirtualFolders`, using `has($k)` rather than jq's `//` (jq's `//` treats `false`
    as empty and would report a correctly-false option as ABSENT). Record all four values plus
    `CustomTagDelimiters` and `DelimiterWhitelist`. `PreferNonstandardArtistsTag` must read `true`
    — the entire hypothesis rests on it. If it reads anything else, STOP: the round is planned on
    a premise that is no longer live.

    SECTION 2 — the three pinned rows in full. For each row in `ARTIST_PROOF_ROWS`, issue the same
    `GET /Items` the script's section 4b issues (`IncludeItemTypes=Audio`, `Recursive=true`,
    `ParentId=<Music>`, `SearchTerm=<the row's exact title>`, `Fields=ArtistItems,Artists,Path`),
    select the item by EXACT `Path` match, and record: the `ArtistItems` length, the distinct-`Id`
    count, the entity `Name` list joined with ` | `, the count of entity names containing `;`, the
    flat `Artists` string list, and the item's `Id` and `DateModified` if the API returns them.
    Record the row's `target` and Jellyfin `baseline` alongside, from the table. Note explicitly in
    band that `Artists` is recorded for DIAGNOSIS ONLY and is never the thing asserted — 30 of the
    1,244 items carry a populated `Artists` with zero `ArtistItems`, and row 1's album is one of
    them.

    SECTION 3 — the full census. Reuse 06-03's recipe verbatim: `GET /Items` with
    `IncludeItemTypes=Audio&Recursive=true&ParentId=<Music>&Fields=ArtistItems,Artists,AlbumArtist,Path&Limit=5000`.
    Emit one stable, sortable line per item — internal path, `ArtistItems` length, distinct-Id
    count — so 06-42 can diff it and enumerate every row that moved. Assert `TotalRecordCount` and
    the returned array length agree, and record both. Record the row count; it was 1,244 on
    2026-09-20 and a different number today is a finding, not a nuisance.

    SECTION 4 — the OQ-1 DO-NOT-RESCAN set, recorded so the exclusion in 06-41 is checkable: list
    the six `P!nk/TRUSTFALL (2023)` rows with their current `ArtistItems` counts and Ids. These
    carry `;` inside `ARTIST` and read 3 artists from stale pre-10.10 DB state; `UseCustomTagDelimiters`
    is deliberately false, so a re-probe would likely COLLAPSE them. They are working state that
    this round must not disturb. Note that row 3 (`P!nk/The Truth About Love (2012)`) is a
    DIFFERENT album under the same artist folder and IS in scope.

    Write the artifact's endpoint block in 06-03's exact stance: list every endpoint issued, state
    that no POST/PUT/DELETE was issued by this plan, and name the two prohibited scan endpoints
    DESCRIPTIVELY ONLY. ⚠ Do not write the literal token `FullRefresh`, and do not write either
    prohibited endpoint's literal query string, anywhere in this artifact — 06-03 established that
    convention precisely so a grep for those literals across `artifacts/` stays a detector rather
    than matching prose (and see `DEF-06-39-06`: an assertion that counts a token in a file which
    discusses that token is self-invalidating).
  </action>
  <acceptance_criteria>
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt`
      exists and contains the literal headings `SECTION 0`, `SECTION 1`, `SECTION 2`, `SECTION 3`
      and `SECTION 4`.
    - Section 1 records `PreferNonstandardArtistsTag` with the value `true`, and
      `UseCustomTagDelimiters`, `SaveLocalMetadata`, `EnableRealtimeMonitor` each with the value
      `false`.
    - Section 1 records the Music library `ItemId` as `7e64e319657a9516ec78490da03edccb`, matching
      `JELLYFIN_MUSIC_LIBRARY_ID` in `scripts/check-music-consumers.sh`.
    - Section 2 contains all three pinned internal paths byte-exactly as they appear in
      `ARTIST_PROOF_ROWS`, including the U+2019 `’` in the ARTPOP path, and for each one records an
      `ArtistItems` length, a distinct-`Id` count, the entity name list, and a `;`-in-name count.
    - Section 2's recorded `ArtistItems` lengths are `0`, `1`, `1` for the ARTPOP, Teenage Dream and
      The Truth About Love rows respectively — the 2026-09-20 Jellyfin baselines. Any other value
      is recorded as-is and escalated rather than smoothed: it means something moved that nobody
      planned, and this round's premise needs re-checking before 06-41 runs.
    - Section 3's census line count equals the recorded `TotalRecordCount`, and both are recorded
      explicitly in the artifact.
    - Section 4 lists six `P!nk/TRUSTFALL (2023)` rows.
    - `grep -c 'FullRefresh'` over the artifact returns `0`, and `grep -ci 'replace all metadata'`
      returns `0`.
    - The artifact's endpoint block lists only `GET` verbs.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt; test -f "$A" || exit 1; for s in "SECTION 0" "SECTION 1" "SECTION 2" "SECTION 3" "SECTION 4"; do grep -qF "$s" "$A" || { echo "missing: $s"; exit 1; }; done; grep -qF "PreferNonstandardArtistsTag" "$A" || exit 1; grep -qF "7e64e319657a9516ec78490da03edccb" "$A" || exit 1; grep -qF "Jewels n" "$A" || exit 1; grep -qF "California Gurls" "$A" || exit 1; grep -qF "Just Give Me a Reason" "$A" || exit 1; test "$(grep -c "FullRefresh" "$A")" -eq 0 || { echo "forbidden token present"; exit 1; }; test "$(grep -ci "replace all metadata" "$A")" -eq 0 || exit 1; echo OK'</automated>
  </verify>
  <done>The artifact holds a dated, endpoint-audited Jellyfin before-state: four D-34 options, three pinned rows at their recorded baselines, a full census whose length equals its own `TotalRecordCount`, and the six DO-NOT-RESCAN rows listed by name.</done>
</task>

<task type="auto">
  <name>Task 2: The filesystem before-state on atlantis — mtimes, every `.nfo`, and the snapshot inventory</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt</files>
  <read_first>
    - `CLAUDE.md` § Storage and Permissions, and § Constraints — `0777`, `568:568`, `chmod` fails
      `EPERM` on `tank` even as real root under `aclmode=restricted`. This task changes no
      permissions and must not try to.
    - `scripts/check-music-consumers.sh` — the `exportfs_query()` / `SSH_OPTS` idiom and the
      `NFS_HOST` constant (`172.16.1.158`, atlantis, "the only place `exportfs` exists"). The same
      host is the only place `zfs` exists.
    - `scripts/freeze-music-apply.sh` — `zfs_exec()` and its `ZFS_ROUTE` handling, and the
      `zfs snapshot tank/media/Music@${SNAPTAG}` call site. This is where the dataset name
      `tank/media/Music` is established; do not guess it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-after.txt`
      § "EVIDENCE THAT NOTHING WAS WRITTEN INTO /mnt/tank/media" — the `album.nfo` mtime evidence
      this task generalises from four albums to the whole dataset.
  </read_first>
  <action>
    Run from atlantis (`root@172.16.1.158`), the Proxmox host — real root, where `zfs` lives, and
    the only vantage point from which the on-disk ownership is not distorted by LXC 100's sparse
    idmap (CLAUDE.md records `65534` appearing container-side for unmapped ids; do not read
    ownership from LXC 100 and believe it).

    Bound every remote command LINUX-SIDE. macOS has no GNU `timeout`, and `timeout N cmd | wc -l`
    silently exits 0 — so each remote string sets `set -o pipefail`, and the ssh exit status is
    read into a variable before anything is piped. A command that could not run records UNKNOWN in
    the artifact; it never records a zero.

    Append to `06-40-conf04-reprobe-before.txt`:

    SECTION 5 — the three pinned files on disk. Map each pinned internal path to its host path by
    substituting the `/media` → `/mnt/tank/media` bind (read the bind back from
    `docker inspect jellyfin` and record it, rather than assuming it). For each, record: existence,
    size in bytes, mtime to the second in UTC, owner uid:gid, and mode. Expect `568:568` and `0777`.
    ⚠ The ARTPOP path carries U+2019; build the path list by writing it to a file on atlantis over
    ssh STDIN from a quoted heredoc, never as an argv word, so no local shell or normalisation
    layer can mangle the byte sequence. Assert the file holds exactly three lines and that every
    one of them passes `test -f`.

    SECTION 6 — every `.nfo` under the dataset. Produce a `sha256sum` and an mtime for every file
    matching `*.nfo` beneath `/mnt/tank/media/Music`, sorted by path. This is the control that
    detects the one outcome this round must never produce: metadata written into the library.
    Record the count. Phase 1 measured 91; a different number today is recorded as-is and called
    out, not silently accepted. Do the same for `*.lrc` and `*.jpg` and record those counts too
    (`SaveLyricsWithMedia` and the image savers are the other two write paths the freeze gates).

    SECTION 7 — the dataset and its snapshots. Record `zfs list -o name,used,avail,mountpoint
    tank/media/Music` and `zfs list -t snapshot -o name,creation -r tank/media/Music`. Assert that
    no snapshot named `tank/media/Music@pre-06-41-conf04-reprobe` exists yet — 06-41 mints it, and
    a pre-existing one would mean this round has already run once.

    SECTION 8 — beets' own state, the D-29 layer-3 control. Record `sha256sum` and mtime for the
    beets `library.db` and `state.pickle` that the surviving tagger uses (find them under
    `/mnt/fast/appdata`; record the exact paths). An mtime change on library files cannot in
    principle move beets' `incremental` state — `incremental` keys on import SOURCE directories,
    not on library files — but "cannot in principle" is how this project has been wrong before, so
    it is measured rather than argued. This is also the check that proves the
    `tank/downloads@pre-phase5` fence (D-32) is untouched: record that snapshot's presence from
    `zfs list -t snapshot -r tank/downloads` and assert it still exists. It is NOT released by this
    round.

    Write nothing under `/mnt/tank/media`. Take no snapshot. Change no permission. This task is a
    read.
  </action>
  <acceptance_criteria>
    - The artifact contains the literal headings `SECTION 5`, `SECTION 6`, `SECTION 7`, `SECTION 8`.
    - Section 5 records three host paths under `/mnt/tank/media/Music/`, each with an mtime, a
      size, an owner of `568:568` and a mode of `0777`, and records the `docker inspect`-read
      Jellyfin bind as `/mnt/tank/media` → `/media`.
    - Section 6 records a `.nfo` count, a `.lrc` count and a `.jpg` count, and contains at least
      one `sha256`-shaped 64-hex-character token.
    - Section 7 records the `tank/media/Music` dataset line and states explicitly that
      `tank/media/Music@pre-06-41-conf04-reprobe` does NOT exist.
    - Section 8 records an absolute path and a sha256 for both `library.db` and `state.pickle`, and
      records `tank/downloads@pre-phase5` as PRESENT.
    - `zfs diff` is not run by this task and no snapshot is created by this task: `zfs list -t
      snapshot -r tank/media/Music` taken again at the end of the task returns the same set it
      returned at the start, recorded in band.
    - Every remote command in this task's transcript carries `set -o pipefail`, and every command
      that returned non-zero is recorded as UNKNOWN rather than as a measured value.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt; for s in "SECTION 5" "SECTION 6" "SECTION 7" "SECTION 8"; do grep -qF "$s" "$A" || { echo "missing: $s"; exit 1; }; done; grep -qF "/mnt/tank/media/Music/" "$A" || exit 1; grep -qF "568:568" "$A" || exit 1; grep -qF "tank/media/Music@pre-06-41-conf04-reprobe" "$A" || exit 1; grep -qF "tank/downloads@pre-phase5" "$A" || exit 1; grep -qE "[0-9a-f]{64}" "$A" || exit 1; grep -qF "pipefail" "$A" || exit 1; echo OK'</automated>
  </verify>
  <done>The artifact holds the on-disk before-state read from atlantis as real root: three pinned files with mtimes, a full `.nfo`/`.lrc`/`.jpg` hash-and-mtime manifest, the dataset's snapshot inventory proving 06-41's snapshot does not yet exist, and beets' own state fingerprinted with the D-32 fence confirmed present.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve and prove the touch list — three paths, zero of them DO-NOT-RESCAN</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt</files>
  <read_first>
    - `scripts/check-music-consumers.sh` `ARTIST_PROOF_ROWS` — the three internal paths, and the
      in-band note that "the row is selected by exact Path, so the path spelling is load-bearing".
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-03-jellyfin-artist-after.txt`
      — the `NO P!nk/TRUSTFALL (2023) PATH WAS INCLUDED. Those six are the OQ-1 DO-NOT-RESCAN set.`
      line, and the PASS 1 / PASS 2 request bodies.
    - This plan's own SECTION 5 output from task 2.
  </read_first>
  <action>
    The mutation in 06-41 touches a list of files. A list built by hand at mutation time, against a
    path carrying U+2019, on a dataset where `insensitive`/`sensitive` semantics have already bitten
    this project once, is the wrong place to discover a typo. Build and prove the list HERE, where
    nothing is written, and record it so the 06-41 checkpoint can show the operator the exact set.

    On atlantis, write the three host paths to a uniquely-named file under `/tmp` over ssh STDIN
    from a quoted heredoc (bytes pass through verbatim; no argv, no local normalisation). Then
    assert, and record each assertion's result in the artifact:

      (a) the file holds exactly 3 lines;
      (b) every line passes `test -f` — a path that does not resolve is a STOP, not a warning;
      (c) every line begins with `/mnt/tank/media/Music/`;
      (d) ZERO lines contain the substring `TRUSTFALL` — the OQ-1 DO-NOT-RESCAN exclusion, asserted
          mechanically rather than trusted to care;
      (e) the three resolved paths, when the `/mnt/tank/media` prefix is replaced by `/media`,
          are byte-identical to the three internal paths in `ARTIST_PROOF_ROWS`. Prove this by
          extracting the paths from `scripts/check-music-consumers.sh` itself and comparing, not by
          eye — the script is the source of truth and a divergence between the touch list and the
          assertion table is the exact way this round could touch the wrong file and then assert on
          a different one;
      (f) the `sha256sum` of each of the three files, recorded so 06-42 can prove the touch changed
          metadata only and not one byte of content.

    Record the full recipe in the artifact as SECTION 9, including the heredoc form, so 06-41 runs
    the identical construction rather than a paraphrase of it. Then delete the `/tmp` file and
    record that it was deleted — 06-41 rebuilds it under its own snapshot fence.

    Do not touch anything. `sha256sum` reads; it does not write. Note in band that reading a file
    updates atime only if the dataset has `atime=on`, and record `zfs get atime tank/media/Music`
    so that an atime-driven surprise in 06-42's `zfs diff` is anticipated rather than mysterious.
  </action>
  <acceptance_criteria>
    - The artifact contains the literal heading `SECTION 9`.
    - Section 9 records, each with an explicit PASS/FAIL verdict, all six assertions (a) through
      (f): the line count is `3`; all three pass `test -f`; all three begin with
      `/mnt/tank/media/Music/`; the `TRUSTFALL` substring count is `0`; the internal-path
      comparison against `scripts/check-music-consumers.sh` is byte-identical for all three; and
      three 64-hex-character `sha256` values are recorded, one per file.
    - Section 9 records the value of `zfs get -H -o value atime tank/media/Music`.
    - Section 9 states that the `/tmp` path list was deleted, and names the path that was deleted.
    - The `zfs list -t snapshot -r tank/media/Music` set is unchanged from Section 7.
    - `git status --porcelain` shows no modification under `/mnt/tank/media` (trivially true — it
      is not in the repo — and stated to make the point that this plan's only product is one file
      under `.planning/`).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt; grep -qF "SECTION 9" "$A" || exit 1; grep -qF "TRUSTFALL" "$A" || exit 1; grep -qF "atime" "$A" || exit 1; test "$(grep -oE "[0-9a-f]{64}" "$A" | sort -u | wc -l | tr -d " ")" -ge 3 || { echo "fewer than 3 distinct sha256 values recorded"; exit 1; }; test "$(git status --porcelain -- .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-40-conf04-reprobe-before.txt | wc -l | tr -d " ")" -le 1 || exit 1; echo OK'</automated>
  </verify>
  <done>The exact three-path touch list is resolved, proved against `check-music-consumers.sh`'s own table, proved to exclude the DO-NOT-RESCAN set, content-hashed, and recorded as a reusable recipe — with the scratch list deleted and nothing on `tank` written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 (ssh root) | command strings cross; unbounded remote commands can hang the entry point |
| LXC 100 → Jellyfin HTTP API | an admin-equivalent API key crosses, on a host with a world-readable `/proc` and no `hidepid` |
| workstation → atlantis (ssh root) | real root on the Proxmox host, with write authority over every dataset |
| this plan → the public git repo | every artifact it writes is committed to a PUBLIC repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-40-01 | Information disclosure | Jellyfin API key in argv | mitigate | key delivered via `-H @<(printf ...)` process substitution and sourced without `set -a`, exactly as `jf_api()` does; never `-H "Authorization: ..."` |
| T-06-40-02 | Information disclosure | artifact committed to a public repo | mitigate | credential-screen every artifact before commit: no `MediaBrowser Token`, no `X-Emby-Token` value, no `*ArrApiKey`, no MA password. Record the credential file's MODE, never its contents |
| T-06-40-03 | Tampering | an accidental write to `/mnt/tank/media/Music` | mitigate | this plan issues only GET/read commands; Section 7 and Section 9 both re-read the snapshot set to prove no snapshot and no write occurred, and Section 6 fingerprints every `.nfo` so a later write is detectable |
| T-06-40-04 | Tampering | the forbidden aggressive refresh mode reached by accident | mitigate | no POST of any kind is issued by this plan; the artifact's endpoint block lists only GET verbs and is asserted to contain zero occurrences of the forbidden mode's literal token |
| T-06-40-05 | Denial of service | an unbounded remote command hanging the run | mitigate | every remote string is bounded Linux-side with `set -o pipefail` and the ssh RC is read before piping (`timeout N cmd \| wc -l` exits 0 silently — `DEF-06-10-01` records this estate hanging for ~10 minutes on exactly that shape) |
| T-06-40-06 | Repudiation | a measured value that was never actually measured | mitigate | any command that could not run records UNKNOWN in band; "could not look" stays distinct from "nothing is wrong" (README § Health Checks) |
| T-06-40-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing — no package manager is invoked and no dependency is added |
</threat_model>

<verification>
- The artifact exists, is committed, and carries Sections 0 through 9.
- The three pinned rows read their recorded 2026-09-20 Jellyfin baselines (0, 1, 1). Anything else
  halts the round before 06-41 mutates anything.
- `PreferNonstandardArtistsTag` reads `true` and the three freeze fields read `false`.
- Zero snapshots were created, zero bytes were written under `/mnt/tank/media`, and
  `tank/downloads@pre-phase5` is still present.
- The artifact contains no credential value and zero occurrences of the forbidden refresh mode's
  literal token.
</verification>

<success_criteria>
Every fact the re-probe could move is a recorded number with a timestamp and a source command, the
touch list is resolved and mechanically proved to be the right three files and to exclude the six
DO-NOT-RESCAN rows, and the estate is byte-for-byte as it was before this plan ran.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-40-SUMMARY.md` when done.
</output>
