---
phase: 06-tagger-configuration-and-dry-run
plan: 16
type: execute
wave: 7
depends_on: ["06-15"]
gap_closure: true
files_modified:
  - scripts/quick-health-check.sh
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
closes_findings: [CR-01-detector-half, WR-10, IN-13]

must_haves:
  truths:
    - "The D-04 scan matches the variable-built `beet` invocations this phase's own scripts make — the executable invocation count is no longer zero, and the number it reports is reproducible by hand from `git grep` on the host"
    - "A zero executable invocation count is reported as UNKNOWN with a non-zero exit, in the same shape as the two sibling guards one screen above it — a vacuous D-04 scan can no longer print a green tick"
    - "Every variable-built invocation the widened pattern sees is either compliant (a throwaway `-l` plus a `-c` overlay) or on a NAMED exemption list whose count is pinned, so a new non-compliant invocation cannot arrive in silence"
    - "Both new branches are DRIVEN: the vacuity guard is driven to UNKNOWN by a controlled pattern perturbation, and the widened match is driven to RED by a temporary variable-built non-compliant line that is then removed and the file's sha256 asserted back"
    - "A remote `timeout` kill on the D-04 scan reports as a bound expiry (124), not as `git grep` having failed — the block's own dedicated 124 branch is reachable"
    - "The D-03 CLI-render branch can be driven to its `exit 3` from a scratch checkout, like every other branch in the two blocks plan 06-10 added"
  artifacts:
    - path: "scripts/quick-health-check.sh"
      provides: "the non-vacuous D-04 assertion with a pinned exemption register"
      contains: "D04_EXEMPT_BASELINE"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-16-d04-driven.txt"
      provides: "the four driven controls with before/after sha256"
  key_links:
    - from: "scripts/quick-health-check.sh D-04 block"
      to: "scripts/check-beets-config.sh, scripts/phase06-oracle.sh, scripts/phase06-incremental-control.sh"
      via: "the widened invocation pattern now matching `$BEET_BIN` / `$BEET` / `${BEET…}` forms"
      pattern: "BEET[A-Z_]*"
---

<objective>
Close the detector half of CR-01: make the D-04 "no bare `beet` invocation opens the real library" assertion actually assert something, and make it impossible for it to go quiet again.

Purpose: `06-VERIFICATION.md` reproduced the defect independently — raw 91, comment-stripped 35, invocation-shaped outside `*.md` **zero**. The block prints a green tick over an empty set, and it does so one screen below two sibling guards that already refuse exactly this reasoning for the raw count (`:1542`) and the comment-stripped count (`:1547`). This phase named "assertions that cannot fail" as its own dominant defect class. This is that class, in the deliverable the class was named in.

Two edits are required and neither alone is sufficient: widening the pattern without the guard leaves the block able to go quiet again the next time the invocation idiom changes; adding the guard without widening turns the block permanently UNKNOWN. A third edit is required because widening alone is not honest either — the oracle and the incremental control build compliant-by-overlay invocations that carry no `-l`, and those must be NAMED and COUNTED rather than quietly matched or quietly excluded.

Output: one health-entry-point script and its runbook quote, changed together, with four driven controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-15-SUMMARY.md
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the D-04 pattern, add the vacuity guard, and pin a named exemption register</name>
  <files>scripts/quick-health-check.sh</files>
  <read_first>
    - `scripts/quick-health-check.sh:1449-1594` — the D-04 block IN FULL. In particular: the contract paragraph at `:1452-1462` (why BOTH flags; `-l` alone does not redirect `statefile:`); the scope-and-exclusion paragraph at `:1464-1470` (the `*.md` exclusion and why it is COUNTED against `D04_DOC_BASELINE` rather than free); the three-counts paragraph at `:1472-1476` (*"a single number cannot distinguish 'nothing matched' from 'the pattern was wrong'"*); the no-pipe-in-the-remote-string paragraph at `:1484-1500` including its self-referential grep-count bookkeeping; the two override guards at `:1508-1517`; the remote command at `:1518-1523`; the sentinel/124/non-zero branches at `:1526-1539`; **the two sibling vacuity guards at `:1559-1563` (raw) and `:1564-1567` (comment strip) — these are the REQUIRED SHAPE for the new guard, read them before writing it**; the pattern at `:1551`; the three counts at `:1555-1557`; the per-line loop at `:1569-1582`; the doc-baseline check at `:1583-1588`; and the green line at `:1589-1592`.
    - `scripts/quick-health-check.sh:649`, `:677`, `:681` — `DRIFT_REPO_ROOT`, `D04_DOC_BASELINE` and `D04_REPO_ROOT` declarations, and the surrounding override contract. `D04_DOC_BASELINE` is the exact precedent for a pinned count whose movement is its own red; the new exemption baseline copies it.
    - `scripts/quick-health-check.sh:428-470` — the TENTH `EXIT-CODE BEHAVIOUR CHANGED` notice, which covers the D-03 and D-04 blocks, including its measured `headers 9 -> 10 / raw 12 -> 13` bookkeeping and its lettered list of new fatal conditions F, G, H, I. A new fatal condition needs an ELEVENTH notice in exactly this form, with the counts measured before and after rather than assumed.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:66-140` — CR-01 verbatim, including the reproduced counts, the five variable-built invocation sites with their file:line, and the three-part fix.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md:8-19` — the gap's `missing:` list, all three items.
    - `scripts/phase06-oracle.sh:1970`, `:1983`, `:2102` — the three oracle invocations, to read the exact text that must be exempted: each is `"$BEET_BIN" -c "$SCRATCH/overlay.yaml" <subcommand>` with no `-l`.
    - `scripts/phase06-incremental-control.sh:419`, `:421` — the two incremental-control invocations: `"$BEET" -c "$ROOT/overlay.yaml" import …`, inside a heredoc'd remote program, with no `-l`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-15-SUMMARY.md` — confirm plan 06-15 landed the three `-l /tmp/p6-cbc-throwaway.blb` flags. **The exemption baseline in this task assumes `check-beets-config.sh` is COMPLIANT, not exempt.** If 06-15 did not land, stop and say so rather than exempting it.
  </read_first>
  <action>
Three edits inside the D-04 block of `scripts/quick-health-check.sh`, plus the mandatory notice.

**Edit 1 — widen `D04_INV_RE` at `:1551` to see variable-built invocations.** The regex is evaluated LOCALLY at `:1552`, not in the remote string, so adding an alternation branch carries no pipe into the remote command and does not touch the `timeout $REMOTE_TIMEOUT.*|` invariant. Add one branch that matches a shell word which is a `BEET`-prefixed variable expansion, in any of the four forms this repo actually uses — `$BEET`, `$BEET_BIN`, `"$BEET_BIN"`, `${BEET_BIN}` — preceded by whitespace or a quote and followed by whitespace. Anchor it the same way the existing branches are: at the start of the content after the `HEAD:<path>:<line>:` prefix, after a shell separator, or after a `docker` prefix. Do not remove or narrow any existing branch; the literal-`beet` branches still catch a hand-written invocation and are what keeps the `*.md` baseline meaningful.

Update the block's three-counts paragraph and the scope paragraph in the same edit to say that the asserted set now includes variable-built invocations and to state, in one sentence, WHY the original pattern missed them: every `beet` call this repo makes from a script is assembled from a variable, so a literal-token scan asserted over an empty set for the whole of Phase 6.

**Edit 2 — the vacuity guard, modelled on its own siblings.** Add an `elif [ "$D04_N_EXE" -eq 0 ]` arm between the comment-strip guard at `:1564-1567` and the final `else` at `:1568`. It must print `⚠️  UNKNOWN`, must say in its own text that a zero here means the pattern cannot see the invocations rather than that none exist, must say explicitly that this is NOT "no bare beet invocations", and must set `EXIT_CODE=1`. Match the wording register and the two-space indentation of the two siblings exactly — they are the house shape and a third guard that reads differently invites the reader to think it means something different.

**Edit 3 — the named exemption register, pinned like the documentation baseline.** After widening, five invocation-shaped executable lines are NOT compliant by the literal `-l`-and-`-c` rule and must not be silently red or silently ignored:

- `scripts/phase06-oracle.sh` — the three lines carrying `-c "$SCRATCH/overlay.yaml"`.
- `scripts/phase06-incremental-control.sh` — the two lines carrying `-c "$ROOT/overlay.yaml"`.

Declare `D04_EXEMPT_BASELINE="${D04_EXEMPT_BASELINE:-5}"` beside `D04_DOC_BASELINE` at `:677`, with the SAME additive contract the two existing D-04 knobs have at `:1508-1517`: any non-default value prints an override warning and forces `EXIT_CODE=1`, so the knob can only make the block redder and cannot manufacture a pass. Declare a `D04_EXEMPT_RE` beside the invocation regex, keyed on the file path AND the distinguishing overlay substring (`$SCRATCH/overlay.yaml` for the oracle, `$ROOT/overlay.yaml` for the incremental control), so that a different invocation added to either file does NOT inherit the exemption.

Partition `D04_INVOKE_EXE` into exempt and asserted sets before the per-line loop. Count the exempt set; if the count differs from `D04_EXEMPT_BASELINE`, print a red naming every exempt line and set `EXIT_CODE=1` — identical in shape and reasoning to the documentation-baseline check at `:1583-1588`. Run the existing `-l`/`-c`/`/config/library.db` loop over the ASSERTED set only. Add the exempt count to the printed `counts:` line at `:1558` so all four numbers are visible, and name the exempt count on the green line at `:1590` so a green D-04 always states how many lines it did not assert over.

**State the exemption's reason in the block comment, in full, because an unexplained exemption is worse than none.** Three reasons, all of which must appear: (1) each exempt invocation passes a `-c` overlay that redirects `library`, `statefile` AND `directory` together into a throwaway root, which is the STRONGER half of D-04's rule and the half `-l` alone cannot achieve — `-l` redirects only `library`; (2) adding a `-l` that names a different path than the overlay's `library:` would split the throwaway state across two files, and adding one that names the same path is a second source of truth for one value; (3) both scripts are closed instruments whose proof runs are recorded in committed artifacts and must not be re-driven inside this phase, so a change to their invocation flags could not be re-proved here. Name the artifacts that carry those proofs so the reason is checkable.

**The ELEVENTH `EXIT-CODE BEHAVIOUR CHANGED` notice.** This edit adds new fatal conditions — a vacuous executable count, and an exemption count off its pin. Write the notice in the established form at the top of the file: the shared phrase, the ordinal, the lettered list of what now exits 1 that did not before, and the grep-count bookkeeping MEASURED before and after the edit (both `grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED'` and `grep -c 'EXIT-CODE BEHAVIOUR CHANGED'`), stated rather than rounded. Do not reword the earlier notices to make the counts tidy.
  </action>
  <acceptance_criteria>
    - `D04_INV_RE` contains an alternation branch matching `BEET[A-Z_]*`
    - An `elif [ "$D04_N_EXE" -eq 0 ]` arm exists, prints `⚠️  UNKNOWN`, states that a zero means the pattern cannot see the invocations, states this is NOT "no bare beet invocations", and sets `EXIT_CODE=1`
    - `D04_EXEMPT_BASELINE` is declared in the `${VAR:-5}` form beside `D04_DOC_BASELINE`, and a non-default value prints an override warning and sets `EXIT_CODE=1`
    - `D04_EXEMPT_RE` names both `phase06-oracle.sh` and `phase06-incremental-control.sh` AND both distinguishing overlay substrings
    - The `counts:` echo line includes an exempt figure, and the block's green line names the exempt count
    - The block comment states all three exemption reasons and names `06-11-oracle-run.txt` and `06-11-wrote-nothing.txt`
    - `grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh` returns `11`
    - `grep -n 'timeout $REMOTE_TIMEOUT.*|' scripts/quick-health-check.sh` returns only lines also containing `pipefail` or ending in `grep -q`
    - `bash -n` exits 0 and `shellcheck -S error` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/quick-health-check.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S error "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -q "BEET\[A-Z_\]\*" || { echo "the widened variable branch is not in D04_INV_RE"; exit 1; }; printf "%s\n" "$s" | grep -qE "D04_N_EXE\" -eq 0" || { echo "the vacuity guard on D04_N_EXE is missing"; exit 1; }; printf "%s\n" "$s" | grep -q "D04_EXEMPT_BASELINE" || { echo "the exemption baseline is missing"; exit 1; }; printf "%s\n" "$s" | grep -q "D04_EXEMPT_RE" || { echo "the exemption pattern is missing"; exit 1; }; printf "%s\n" "$s" | grep -q "phase06-oracle" || { echo "the oracle is not named in the exemption register"; exit 1; }; printf "%s\n" "$s" | grep -q "phase06-incremental-control" || { echo "the incremental control is not named in the exemption register"; exit 1; }; h=$(grep -c "^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED" "$f"); [ "$h" -eq 11 ] || { echo "expected 11 notice headers, found $h"; exit 1; }; bad=$(grep -n "timeout \$REMOTE_TIMEOUT.*|" "$f" | grep -v "pipefail" | grep -v "grep -q" || true); [ -z "$bad" ] || { echo "remote pipelines missing pipefail:"; echo "$bad"; exit 1; }; echo "06-16 task 1 OK"'</automated>
  </verify>
  <done>
    `D04_INV_RE` matches `$BEET`, `$BEET_BIN`, `"$BEET_BIN"` and `${BEET_BIN}` forms in addition to the literal token; an `elif [ "$D04_N_EXE" -eq 0 ]` arm prints UNKNOWN, says it is NOT "no bare beet invocations", and sets `EXIT_CODE=1`; `D04_EXEMPT_BASELINE` (default 5) and `D04_EXEMPT_RE` exist with the additive override contract, the exempt set is partitioned out before the per-line loop, the exempt count is on both the `counts:` line and the green line, and a count off the pin is a red naming every exempt line; the three-part exemption reason is stated in the block comment with the proof artifacts named; the ELEVENTH exit-code notice is present with measured counts.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-10 — preserve the timeout status; IN-13 — make the D-03 render branch driveable</name>
  <files>scripts/quick-health-check.sh</files>
  <read_first>
    - `scripts/quick-health-check.sh:1518-1539` — the remote command and the branch ladder. The remote program does `D04_RC=$?` then `if [ $D04_RC -gt 1 ]; then exit 4; fi`. `timeout` exits 124 on a kill and `124 > 1`, so the remote exits 4 and the dedicated `elif [ "$D04_RC" -eq 124 ]` at `:1532` is unreachable for this block — the operator is told `4 = 'git grep' failed` when the host wedged.
    - `scripts/quick-health-check.sh:1526-1531` — the sentinel branch, which special-cases `D04_RC -ne 124`. That condition already assumes 124 can arrive; edit 1 makes the assumption true.
    - `scripts/quick-health-check.sh:1386` — the D-03 CLI-render `ssh` with its literal `cd /mnt/fast/stacks || exit 3`.
    - `scripts/quick-health-check.sh:649` and `:681` — `DRIFT_REPO_ROOT` and `D04_REPO_ROOT`, both `${VAR:-/mnt/fast/stacks}`, and their additive contracts at `:1513-1517` (a non-default value prints a warning and forces `EXIT_CODE=1`, so it cannot produce a pass).
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:403-433` (WR-10) and `:544-552` (IN-13) — both findings with their proposed fixes.
  </read_first>
  <action>
**WR-10 — preserve the bound expiry.** In the remote program at `:1518-1523`, insert a status test for 124 BEFORE the `-gt 1` test, so a `timeout` kill exits the remote program 124 and reaches the block's dedicated 124 branch at `:1532` with its correct message. Keep the `-gt 1 → exit 4` test as the second test; `git grep` exiting 1 on no match remains a legitimate pass and must not change meaning. Add one sentence to the block comment recording why the ordering is load-bearing: this file treats "the bound expired" and "the tool failed" as distinct conditions in nine places and the TENTH notice's condition I enumerates both, so collapsing them here would make the operator's diagnosis wrong while the verdict stayed right.

**IN-13 — make the D-03 render branch driveable.** `:1386` hard-codes `cd /mnt/fast/stacks || exit 3` while the two blocks around it grew overridable roots in the same commit, for the stated reason that an undriveable branch is an unproven branch. Add `D03_REPO_ROOT="${D03_REPO_ROOT:-/mnt/fast/stacks}"` beside `D04_REPO_ROOT` at `:681`, give it the SAME additive contract — a non-default value prints an override warning and forces `EXIT_CODE=1` — and use it at `:1386`. Do not reuse `DRIFT_REPO_ROOT`: the drift block and the D-03 render are different claims and sharing a knob would make one block's override silently change another block's verdict.
  </action>
  <acceptance_criteria>
    - Inside the `D04_CMD` remote program, a test for status `124` appears at a LOWER line number than the `-gt 1` test
    - `D03_REPO_ROOT` is declared as `${D03_REPO_ROOT:-/mnt/fast/stacks}` and a non-default value prints an override warning and sets `EXIT_CODE=1`
    - No literal `cd /mnt/fast/stacks || exit 3` remains in the file
    - The block comment states why the 124-before-`-gt 1` ordering is load-bearing
    - `bash -n scripts/quick-health-check.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/quick-health-check.sh; bash -n "$f" || exit 1; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -qE "D04_RC -eq 124.*exit 124|exit 124" || { echo "the remote program does not preserve 124"; exit 1; }; awk "/D04_CMD=/,/^exit 0\"/" "$f" | grep -n "124" | head -1 | grep -q . || { echo "124 is not handled inside the D04 remote program"; exit 1; }; a=$(awk "/D04_CMD=/,/^exit 0\"/" "$f" | grep -n "124" | head -1 | cut -d: -f1); b=$(awk "/D04_CMD=/,/^exit 0\"/" "$f" | grep -n -- "-gt 1" | head -1 | cut -d: -f1); [ -n "$a" ] && [ -n "$b" ] && [ "$a" -lt "$b" ] || { echo "the 124 test does not precede the -gt 1 test"; exit 1; }; printf "%s\n" "$s" | grep -q "D03_REPO_ROOT" || { echo "D03_REPO_ROOT missing"; exit 1; }; printf "%s\n" "$s" | grep -q "cd /mnt/fast/stacks || exit 3" && { echo "the hard-coded D-03 render root survives"; exit 1; }; echo "06-16 task 2 OK"'</automated>
  </verify>
  <done>
    The D-04 remote program tests for 124 before testing `-gt 1`, so a bound expiry reaches the block's 124 branch and reports as a bound expiry; the reason is stated in one sentence in the block comment. `D03_REPO_ROOT` exists with the additive contract, is used at the D-03 render, and no literal `cd /mnt/fast/stacks || exit 3` remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Drive all four new branches red, restore, and quote the runbook back into step</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-16-d04-driven.txt, stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - `scripts/quick-health-check.sh` as tasks 1 and 2 left it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-10-PLAN.md` task 3 — the perturbation discipline this repo uses: pre-test sha256 of every script touched, drive the red, restore, assert the hash matches, re-run green, confirm `git status --porcelain` is clean of test debris. Follow it exactly.
    - `.planning/STATE.md` § Phase 5 close — the standing known red: `quick-health-check.sh` exits 1 on the pre-existing `interpolated-host-path inventory MOVED: expected=12, found=13` gate. The script's top-level exit code is therefore NON-DISCRIMINATING and the artifact must say so, naming which reds are pre-existing and which are this plan's.
    - `stacks/selfhosted/arrs/beets.md` — locate the Phase 6 section and the D-04 row (`grep -n "D-04" stacks/selfhosted/arrs/beets.md`). Read the criteria-table convention: the evidence column holds a RUNNABLE command quoted verbatim so it cannot drift from the script, and a superseded disposition is kept legible under a dated block quote rather than deleted.
    - `README.md` § Health Checks — the three rules.
  </read_first>
  <action>
Run the changed health entry point against the live estate, drive every new branch, and record everything in `artifacts/06-16-d04-driven.txt`.

**First, reproduce the counts by hand, so the artifact does not rest on the script agreeing with itself.** From the workstation, run the same scan the block runs — `ssh root@172.16.1.159` into `/mnt/fast/stacks`, `git grep -n -I -w -E 'beet' HEAD -- scripts stacks` — and record the raw count, the comment-stripped count, the invocation-shaped count, the executable subset and the exempt subset, each as a number and with the matching lines printed for the last two. Compare each against what the block prints. A disagreement between the hand count and the block's count is a finding, not a rounding difference. **Note the host reads ITS HEAD:** if plans 06-15 and 06-16 are not yet pushed and pulled, the host will not see them and the counts will be the pre-fix counts. Push and pull first, and record that you did, or the whole artifact measures the wrong tree.

**Then drive four controls. Each one: record the sha256 of every file touched, perturb, capture the output, restore, re-assert the sha256, re-run green.**

1. **The widened match goes RED on a real violation.** Append one temporary line to a tracked executable file under `scripts/` that builds a `beet` invocation from a variable with a `-c` overlay and NO `-l` — a comment-free line that the pattern must see. Commit it to a throwaway local branch or push it to the host's checkout by the same route the block reads (the block reads the host's `HEAD`, so the line must reach that HEAD to be seen — state in the artifact exactly how you got it there, and get it back out). Expect exactly one red naming that line, with the other counts unchanged. Remove it, restore HEAD, re-run green.
2. **The vacuity guard fires.** There is deliberately no override that empties the asserted set, because an override that can silence an assertion is forbidden in this repo. Drive it instead by a controlled local perturbation: temporarily replace `D04_INV_RE`'s value with a pattern that cannot match, run, and require `⚠️  UNKNOWN` naming the zero executable count plus a non-zero `EXIT_CODE`. Restore the file, assert the sha256 matches the pre-test value, re-run.
3. **The exemption pin fires.** Run once with `D04_EXEMPT_BASELINE=4`. Expect the override warning, the pin red naming all five exempt lines, and `EXIT_CODE=1` — and confirm that the override CANNOT produce a pass, which is the additive contract. Re-run without the override.
4. **The 124 branch is reachable (WR-10).** Run once with a `REMOTE_TIMEOUT` small enough that the remote `git grep` is killed. Require the block's 124 message (*the D-04 scan exceeded its …s bound and was killed*) and NOT the sentinel branch's `4 = 'git grep' failed` text. Record both the value used and the message received. If the grep completes too fast to be killed at any sane bound, say so explicitly and record the branch as undriven with the reason — do not fabricate a kill, and do not silently skip it.

**Also record the D-03 render branch (IN-13) driven to `exit 3`** by running once with `D03_REPO_ROOT` pointing at a path that does not exist on the host. Expect the override warning, the `exit 3` path reported as UNKNOWN rather than as a pass, and `EXIT_CODE=1`.

**Then update `stacks/selfhosted/arrs/beets.md` in the same commit.** The runbook quotes the D-04 rule and its evidence command. Re-quote whatever the script now holds, byte-identically; state the new counts (raw / comment-stripped / invocation-shaped / executable / exempt) as measured, not as expected; and add the exemption register — the two files, the five lines, and the three-part reason — so the exemption is discoverable from the runbook and not only from a block comment. Record the correction to the prior claim under a dated note in the file's own convention for a superseded disposition: the D-04 assertion as shipped on 2026-09-21 asserted over an empty set for the whole of Phase 6, and the green tick it printed meant nothing. Do not delete the prior claim.
  </action>
  <acceptance_criteria>
    - `artifacts/06-16-d04-driven.txt` exists and records hand-run `git grep` counts for raw / comment-stripped / invocation-shaped / executable / exempt, each compared against the block's own printed figure with agreement or disagreement stated
    - It records how the host's `/mnt/fast/stacks` HEAD was brought into step (push + pull) before any count was taken
    - Control 1: a temporary variable-built `-c`-only invocation produced exactly ONE red naming that line, was removed, and the host HEAD was restored — with before/after sha256 quoted
    - Control 2: with `D04_INV_RE` perturbed to match nothing, the run printed `⚠️  UNKNOWN` naming the zero executable count and exited non-zero; the file was restored and its sha256 asserted equal
    - Control 3: `D04_EXEMPT_BASELINE=4` produced the override warning AND a red naming all five exempt lines, and did not produce a pass
    - Control 4: the `124` branch is either recorded as driven with the bound-expiry message quoted, or explicitly recorded as `undriven` with a stated reason — the `4 = 'git grep' failed` text must NOT be what a bound expiry produces
    - `D03_REPO_ROOT` pointed at a non-existent path produced an UNKNOWN and `EXIT_CODE=1`
    - The pre-existing `interpolated-host-path inventory MOVED` red is named as pre-existing
    - `stacks/selfhosted/arrs/beets.md` contains `D04_EXEMPT`, the byte-identical new pattern quote, the measured counts, and a dated note recording the superseded 2026-09-21 claim without deleting it
    - `git status --porcelain` lists nothing outside `.planning/`, `scripts/quick-health-check.sh` and `stacks/selfhosted/arrs/beets.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-16-d04-driven.txt; test -s "$a" || { echo "artifact missing"; exit 1; }; grep -qiE "raw=" "$a" || { echo "the hand-reproduced counts are absent"; exit 1; }; grep -qE "D04_EXEMPT_BASELINE=4|exemption count" "$a" || { echo "control 3 (exemption pin) not recorded"; exit 1; }; grep -qiE "UNKNOWN" "$a" || { echo "no UNKNOWN branch recorded"; exit 1; }; grep -qiE "interpolated-host-path" "$a" || { echo "the pre-existing red is not named, so the exit code reads as discriminating when it is not"; exit 1; }; grep -qiE "sha256" "$a" || { echo "restore-and-assert hashes absent"; exit 1; }; grep -qiE "D03_REPO_ROOT" "$a" || { echo "the IN-13 driven branch is not recorded"; exit 1; }; grep -qiE "124|undriven" "$a" || { echo "the WR-10 124 branch is neither driven nor explicitly recorded as undriven"; exit 1; }; grep -q "D04_EXEMPT" stacks/selfhosted/arrs/beets.md || { echo "the runbook does not carry the exemption register"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/quick-health-check.sh|^ M stacks/selfhosted/arrs/beets.md" || true); [ -z "$d" ] || { echo "test debris:"; echo "$d"; exit 1; }; echo "06-16 task 3 OK"'</automated>
  </verify>
  <done>
    The artifact carries the hand-reproduced scan counts alongside the block's own, states how the host's HEAD was brought into step, and records five driven controls — a real variable-built violation going RED and removed, the vacuity guard firing UNKNOWN under a restored pattern perturbation, the exemption pin firing under `D04_EXEMPT_BASELINE=4` with the override proven unable to pass, the 124 branch either driven or explicitly recorded as undriven with its reason, and the D-03 render `exit 3` driven via `D03_REPO_ROOT` — each with pre/post sha256 and a return to green. The pre-existing `interpolated-host-path` red is named so the exit code is not read as discriminating. `beets.md` quotes the new pattern byte-identically, states the measured counts, carries the exemption register, and records the superseded 2026-09-21 claim under a dated note rather than deleting it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation (macOS) → LXC 100 root ssh | Every D-04 read crosses an ssh as root. macOS has no GNU `timeout`, so the bound must be Linux-side and is. |
| host `git HEAD` → the assertion | The block deliberately reads the host's HEAD, not the workstation's working tree, so a dirty checkout cannot produce a false green OR a false red. The consequence is that an unpushed fix is not asserted. |
| the pinned counts (`D04_DOC_BASELINE`, `D04_EXEMPT_BASELINE`) → the verdict | A pin that can be moved by an env var is a silencer unless the move is itself a red. |
| this public repo → the world | The driven-red control in task 3 temporarily adds a line to a tracked file and pushes it. Nothing in that line may be a credential, and it must be removed and the removal proved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-70 | Spoofing | The D-04 block reporting a green tick over an empty asserted set — the live defect this plan closes | mitigate | Widened pattern plus an `elif [ "$D04_N_EXE" -eq 0 ]` UNKNOWN guard with `EXIT_CODE=1`, driven in task 3 control 2. |
| T-06-71 | Tampering | A future non-compliant invocation joining the exemption set in silence | mitigate | `D04_EXEMPT_RE` is keyed on file path AND the distinguishing overlay substring, and `D04_EXEMPT_BASELINE` pins the count; a move is a red naming every exempt line. Driven in control 3. |
| T-06-72 | Elevation of Privilege | An env override used to silence the block during debugging | mitigate | Both new knobs follow the existing additive contract: any non-default value prints a warning and forces `EXIT_CODE=1`. The file's standing rule forbids a skip sentinel. Proven by control 3, which sets the pin wrong and still cannot pass. |
| T-06-73 | Repudiation | A wedged host reported as a broken `git grep`, sending the operator to the wrong diagnosis | mitigate | WR-10: the remote program preserves 124 before collapsing statuses to 4; driven, or explicitly recorded as undriven with its reason. |
| T-06-74 | Tampering | The temporary violation line from control 1 being left in the tracked tree or on the host's HEAD | mitigate | Pre/post sha256 on every touched file, an explicit record of how the line reached and left the host's HEAD, and a `git status --porcelain` debris check in the verify command. |
| T-06-75 | Information Disclosure | A credential entering this public repo via the temporary control line | mitigate | The control line is a `beet` invocation with an overlay path and no `-l`; it carries no token, and the threat is named here so the executor does not improvise one with a real path. |
| T-06-76 | Denial of Service | A widened regex making the local per-line loop pathological on a large grep result | accept | The asserted set is single digits; the scan is bounded Linux-side at `REMOTE_TIMEOUT` and the local matching is over at most ~100 lines. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S error` clean on `scripts/quick-health-check.sh`.
- `grep -c '^# ⚠️  EXIT-CODE BEHAVIOUR CHANGED'` returns 11.
- `grep -n 'timeout $REMOTE_TIMEOUT.*|'` returns only `pipefail`-carrying or `grep -q`-terminated lines.
- A live run prints a `counts:` line whose executable figure is non-zero and whose exempt figure is 5, and whose green line names both.
- The five driven controls are in the artifact with before/after hashes.
</verification>

<success_criteria>
- The D-04 assertion can fail, and has been made to fail, for the right reason.
- A vacuous D-04 scan is UNKNOWN and non-zero, never green.
- Every unasserted invocation is named, counted and pinned.
- The runbook and the script say the same thing, and the superseded claim is kept legible.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-16-SUMMARY.md` when done. Record the final measured counts and whether the WR-10 124 branch was driven or recorded undriven — plan 06-21's disposition register cites both.
</output>
