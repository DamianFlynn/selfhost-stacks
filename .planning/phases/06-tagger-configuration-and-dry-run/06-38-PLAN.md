---
phase: 06-tagger-configuration-and-dry-run
plan: 38
type: execute
wave: 16
depends_on: ["06-34"]   # round 3's last plan: these four fix the code plans 06-30..06-33 wrote and 06-34 dispositioned
gap_closure: true
gap_closure_round: 4
files_modified:
  - scripts/check-beets-config.sh
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt
autonomous: true
requirements: [CONF-01, CONF-02, CONF-06]
closes_findings: [R4-05]

must_haves:
  truths:
    - "The hunk that corrects a self-referential grep count no longer states a raw count of its own — the parenthetical figure is withdrawn rather than re-pinned to a new number"
    - "The comment-stripped census, which is the durable figure, still reads 2 and is still stated as a recipe rather than as a number the reader must trust"
    - "No remaining in-band numeric claim about a token count inside `check-beets-config.sh` disagrees with what its own recipe returns — each was measured, and each that a future edit would move was converted to a recipe"
    - "`ST_PLANNED_CASES=7` is untouched: it is a gated pin over an unconditional set, not a prose claim, and round 4 verified it as such"
  artifacts:
    - path: "scripts/check-beets-config.sh"
      provides: "a census paragraph that pins no raw count and an audited set of in-band count claims"
      contains: "R4-05"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt"
      provides: "every in-band numeric count claim in the file, its recipe, its measured value at HEAD, and the disposition taken — corrected, converted to a recipe, or left as an accurate dated record"
  key_links:
    - from: "the R3-07 census paragraph"
      to: "the reader who needs the figure"
      via: "a comment-stripped recipe, with no raw number stated"
      pattern: "NOT pinned"
---

<objective>
Close the one round-4 finding that lands in `scripts/check-beets-config.sh`: **WR-05** of
`06-REVIEW-GAP3.md`.

⚠ **FINDING IDs IN THIS ROUND ARE ALIASED, AND THIS IS NOT OPTIONAL.** Round 4's report reuses
round 1's `WR-*` / `IN-*` namespace, and round 1's IDs are already cited **in band in this file**
(`CR-01`, `IN-01`, `IN-03`, `IN-07`, `WR-04` ×2). Writing a bare `WR-05` here would be ambiguous to
every future reader and would make any grep for it non-discriminating. **In band, and in every
verify block, this round's findings are `R4-01 … R4-10`**, continuing the phase's per-round
convention (`CR`/`WR`/`IN` = round 1, `GC-*` = round 2, `R3-*` = round 3). Plan 06-39 carries the
mapping table. This plan owns:

| Round-4 report ID | In-band ID | Subject |
|---|---|---|
| WR-05 | **R4-05** | the hunk that corrects a self-referential grep count states a raw count that its own text falsified |

**This is the phase's signature hazard, caught for the third consecutive round, inside the fix for
the second instance of it.** Round 3's plan 06-33 corrected R3-07's census — which had printed a
code-only figure as though it were a raw grep count — and wrote, in the correcting hunk:

> *(raw is 4, and rises with prose like this)*

Measured: raw is **5**. It was 4 at the diff base, and **the recipe line that same hunk added made
it 5**. The sentence is present tense, in a file where the answer is not 4, inside the paragraph
whose entire subject is that a number written into a file that greps itself moves that number. The
hedge "and rises with prose like this" is not a fix: the hunk that wrote the hedge is what rose it.

**The comment-stripped census in the same block is correct** — the review ran the recipe and got 2,
the definition and the single call in `run_self_test`. **Only the parenthetical raw figure is
wrong.** Do not touch the rest of the paragraph, and do not re-litigate the finding it belongs to.

**The fix is to drop the number, not to correct it.** `quick-health-check.sh` applied exactly this
lesson in the same round (R3-05: *"THE TWO NUMBERS AND THE CONSTANT DELTA … ARE WITHDRAWN"*). Two
sibling files, one round, one lesson — and only one of them applied it. **Re-pinning to 5 would set
up the fourth drift**, and the fourth drift would arrive the moment the next comment mentions the
symbol.

**Say which fix you are making: R4-05 is a CLAIM CORRECTION ONLY.** No behaviour changes, no
executable line moves, and `--self-test` output must be byte-identical across this plan's commits.

Task 2 then does the small, bounded thing that stops this arriving a fourth time: **audit every
other in-band numeric count claim in this one file**, measure each against its own recipe, and
convert the ones a future edit would move. Bounded strictly to this file and to claims about token
counts inside it — this is not licence to re-measure the estate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP3.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP2.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/check-beets-config.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Withdraw the raw count from the census paragraph instead of re-pinning it</name>
  <files>scripts/check-beets-config.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt</files>
  <read_first>
    - **`scripts/check-beets-config.sh` — the file you are modifying.** Read the whole census
      paragraph and the `IF THIS FUNCTION IS EVER CALLED LIVE` paragraph below it as one continuous
      read. Anchors: `The census, given as a RECIPE rather than a number you must take on trust`,
      the recipe lines themselves, `The answer is 2`, and the clause to withdraw,
      `printed that code-only figure as though it were a raw grep count (raw is 4, and rises with prose`.
    - **Do not trust any line number in this plan or in `06-REVIEW-GAP3.md`.** Resolve every anchor
      with `/usr/bin/grep -n` first.
    - The sibling's applied lesson, which is the wording to mirror. In `scripts/quick-health-check.sh`:
      `/usr/bin/grep -n 'ARE WITHDRAWN\|deliberately unpinned\|EXIT-CODE BEHAVIOUR CHANGE\[D\]' scripts/quick-health-check.sh`.
      **Read it, do not edit it** — plan 06-35 owns that file this round and runs in parallel.
    - `06-DISPOSITIONS-GAP2.md` § Lessons 3, *"Self-referential measurement in files that grep
      themselves is this phase's signature hazard"*, and its § Corrections item 1, which records the
      three-ref measurement (`bf509f4` → 2, `fff070a` → 4, HEAD → 5). That item is the provenance of
      this finding and your correction should cite it rather than re-deriving it.
    - `06-REVIEW-GAP3.md` § WR-05 in full, including its suggested replacement wording and its
      explicit statement that **the comment-stripped census is correct**.
  </read_first>
  <action>
**Disposition, stated up front and repeated in band: R4-05 is a CLAIM CORRECTION ONLY.**

**(a) Withdraw the parenthetical.** Delete the clause stating a raw figure. Replace it with the
sibling's form: record that the round-3 hunk printed a code-only figure as though it were a raw grep
count, that **the raw count is deliberately not pinned here**, and the reason in one clause — a
number written into a file that greps itself is moved by the sentence that states it, and the
correcting hunk moved this one from 4 to 5 in the act of correcting it. Point at
`scripts/quick-health-check.sh`'s R3-05 block as the sibling precedent and at
`06-DISPOSITIONS-GAP2.md` § Corrections item 1 for the three-ref measurement. Name **R4-05**.

⛔ **Do not write 5, or 4, or any other raw figure, anywhere in the replacement** — including in a
sentence disowning it. A grep for the absence of a pinned number cannot distinguish a claim from its
retraction, which is `06-DISPOSITIONS-GAP2.md` § Lessons 4 and the reason round 2 shipped that
defect twice. If a reader needs the raw figure, the recipe gives it.

**(b) Keep the comment-stripped recipe and its answer exactly as they stand.** `The answer is 2` is
correct, is **comment-stripped** and therefore does not move when prose about the symbol is added,
and is what both the script and 06-33's verify pin. It is the durable figure and it stays.

**(c) Change nothing else in the paragraph.** The GC-13 justification correction, the polarity
precondition under `IF THIS FUNCTION IS EVER CALLED LIVE`, and the recorded refusal to wire the
function live are all round 3's and all stand. Re-litigating any of them is scope creep, and
`DEF-06-34-02` owns the refusal.

**(d) Prove it is comment-only, mechanically, the way round 3 proved its three comment-only fixes:**
zero non-comment lines in the diff, **and** `--self-test` output byte-identical across the change
(`cmp` rc 0). Record both in the artifact.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu   # -e is load-bearing: without it only the LAST test in this block decides the exit code
F=scripts/check-beets-config.sh
G=/usr/bin/grep

# (a) the withdrawn parenthetical, as a whole construction. It cannot be a substring of a
# replacement that states no number at all.
test "$($G -cF '(raw is 4, and rises with prose' $F)" -eq 0
# and it was NOT re-pinned to the obvious wrong fix, nor to anything else in that sentence's shape
test "$($G -cF 'raw is 4' $F)" -eq 0
test "$($G -cF 'raw is 5' $F)" -eq 0
test "$($G -ciF 'NOT pinned' $F)" -ge 1
test "$($G -cF 'R4-05' $F)" -ge 1
# the sibling precedent is cited rather than the lesson being re-derived
test "$($G -cF 'R3-05' $F)" -ge 1

# (b) the durable, comment-stripped figure survives, AND still measures 2. This is derived by
# running the recipe, not read off the sentence -- the sentence is exactly what is under suspicion.
test "$($G -cF 'The answer is 2' $F)" -eq 1
test "$($G -v '^[[:space:]]*#' $F | $G -c 'assert_beet_invocation_contract')" -eq 2

# (c) round 3's other work in this paragraph is untouched
test "$($G -cF 'IF THIS FUNCTION IS EVER CALLED LIVE' $F)" -eq 1
test "$($G -cF 'R3-07' $F)" -ge 1

# (d) COMMENT-ONLY, proven rather than asserted. The first test is the vacuity guard: a diff with
# no changed lines at all would satisfy the second trivially, and this phase has printed a green
# tick over an empty set twice (CR-01, GC-03).
test "$(git diff -U0 -- $F | $G -c '^[+-][^+-]' || true)" -ge 1
test "$(git diff -U0 -- $F | $G '^[+-][^+-]' | $G -cvE '^[+-][[:space:]]*#')" -eq 0

bash -n $F
rc=0; bash $F --self-test >/dev/null 2>&1 || rc=$?
test "$rc" -eq 0

ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt
test -s "$ART"
test "$($G -cF 'R4-05' "$ART")" -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
- `/usr/bin/grep -cF '(raw is 4, and rises with prose'` returns **0**, and neither `raw is 4` nor
  `raw is 5` appears anywhere in the file — the figure was withdrawn, not re-pinned.
- The replacement states that the raw count is deliberately not pinned, gives the reason in one
  clause, cites `scripts/quick-health-check.sh`'s R3-05 block and
  `06-DISPOSITIONS-GAP2.md` § Corrections item 1, and names **R4-05**.
- `The answer is 2` still appears exactly once, and the comment-stripped recipe run over the file
  still returns exactly **2**.
- `IF THIS FUNCTION IS EVER CALLED LIVE` and the R3-07 citation are untouched.
- The diff for this task has at least one changed line and **zero** changed lines that are not
  comments; `--self-test` output is byte-identical across the change (`cmp` rc 0), recorded in the
  artifact.
- `bash -n` exits 0 and `--self-test` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Audit every other in-band count claim in this one file, measure each, and convert the ones that move</name>
  <files>scripts/check-beets-config.sh, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt</files>
  <read_first>
    - **`scripts/check-beets-config.sh` in full.** This task's whole point is that a targeted read
      is what let this class survive three rounds. Read the file end to end once, then enumerate.
    - `ST_PLANNED_CASES=7` and its gate. Anchor:
      `/usr/bin/grep -n 'ST_PLANNED_CASES' scripts/check-beets-config.sh`. **This is explicitly OUT
      OF SCOPE and must not be touched.** Round 4 checked it and recorded it clean: it is
      unconditional — six `run_case` calls plus the manual case 6 — it is a **gated pin over an
      executed set**, not a prose claim about a grep, and `--self-test` exits 0 at 7. It is the one
      number in this file that is supposed to be a number. An audit that "fixes" it would break a
      working guard and would be this task's own version of the defect it is auditing.
    - `06-DISPOSITIONS-GAP2.md` § Lessons 3 and § Lessons 4 — the hazard and its two mitigations
      (state a recipe not a number; never paste a retired construction into the comment disowning
      it).
    - `06-REVIEW-GAP3.md` § *What I checked and found clean*, so you do not re-open anything round 4
      already checked in this file: the GC-01 here-strings, case 7's 70,000-character pad and its
      `bytes7 > 65536` precondition, and `ST_PLANNED_CASES=7`'s unconditionality.
  </read_first>
  <action>
**Disposition: this task is a CLAIM AUDIT.** It may correct comments and may convert a number into
a recipe. **It may not change any executable line.** If the audit turns up something that needs a
code change, record it as a finding for plan 06-39 to defer by name — do not fix it here.

**Scope, stated tightly because an unbounded audit is how a two-task plan becomes a five-task one:**
  - **one file** — `scripts/check-beets-config.sh`;
  - **one class of claim** — an in-band statement of *how many times some token appears in this
    file, or in a file this script greps*. Not claims about beets, not claims about the estate, not
    claims about other scripts' contents.

**(a) Enumerate.** Find every such claim. Start from the obvious mechanical sweep — comment lines
carrying a digit alongside a count word (`hits`, `occurrences`, `times`, `count`, `returns`,
`lines`, `exactly`, `only`) — and then read the file end to end to catch the ones phrased without a
keyword. Record the enumeration method in the artifact, including its known gaps: a mechanical
sweep over prose is not a proof of completeness and must not be written up as one.

**(b) Measure each**, with `/usr/bin/grep` by absolute path, against the tree at HEAD. For each
claim record: the claim as written, the recipe that would substantiate it, the measured value, and
whether they agree.

**(c) Disposition each, using exactly three outcomes:**
  - **ACCURATE AND STABLE** — the claim matches and nothing in the file can move it (e.g. it counts
    something outside this file that is under version control). Leave it; record why it is stable.
  - **ACCURATE BUT SELF-MOVING** — the claim matches today but a future comment mentioning the token
    would move it. **Convert it to a recipe**, using the bracketed-pattern device where the recipe
    would otherwise match itself (`EXIT-CODE BEHAVIOUR CHANGE[D]` is the file-family's precedent),
    and say in one clause why it is written that way so nobody unbrackets it.
  - **WRONG** — the claim does not match. Correct it **by withdrawal**, as task 1 did, not by
    re-pinning. If you believe a specific figure genuinely must appear, pin the **comment-stripped**
    form, never the raw one, and say which it is at the site.

**(d) Do not touch `ST_PLANNED_CASES=7`,** and record it in the audit table as an explicit
**EXCLUDED** row with its reason — a gated pin over an unconditional executed set, verified clean by
round 4. An exclusion that is not written down reads exactly like something the audit missed.

**(e) Write the audit table into `artifacts/06-38-checker-count-audit.txt`**, one row per claim, and
state at the top what the audit covered and what it did not.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
set -eu
F=scripts/check-beets-config.sh
G=/usr/bin/grep
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-38-checker-count-audit.txt

# the audit exists, names its subject, and records the deliberate exclusion rather than omitting it.
# -E and NOT -F on the alternation: `grep -F` would look for the backslash-pipe literally and
# silently return 0 -- the trap round 3's plan 06-34 wrote down and this block must not re-enter.
test -s "$ART"
test "$($G -cF 'ST_PLANNED_CASES' "$ART")" -ge 1
test "$($G -ciE 'exclusion|excluded' "$ART")" -ge 1
test "$($G -ciF 'enumeration' "$ART")" -ge 1
test "$($G -cF 'R4-05' "$ART")" -ge 1

# the excluded pin is genuinely untouched, and still gated
test "$($G -cF 'ST_PLANNED_CASES=7' $F)" -eq 1
test "$($G -cF 'ST_PLANNED_CASES' $F)" -ge 2

# round 4's checked-and-clean items in this file are unchanged: no `| grep -q` was reintroduced
# (GC-01 was the phase's only BLOCKER), and the here-string form still stands.
test "$($G -cF '| grep -q' $F)" -eq 0
test "$($G -cF 'grep -qF -- "$forb" <<<' $F)" -ge 1

# THIS TASK CHANGES NO EXECUTABLE LINE. Vacuity guard first, then the assertion.
test "$(git diff -U0 -- $F | $G -c '^[+-][^+-]' || true)" -ge 1
test "$(git diff -U0 -- $F | $G '^[+-][^+-]' | $G -cvE '^[+-][[:space:]]*#')" -eq 0

bash -n $F
rc=0; bash $F --self-test > "$(mktemp)" 2>&1 || rc=$?
test "$rc" -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
- `artifacts/06-38-checker-count-audit.txt` holds one row per in-band count claim found in
  `scripts/check-beets-config.sh`, each with the claim as written, its recipe, its measured value at
  HEAD, and one of exactly three dispositions: ACCURATE AND STABLE, ACCURATE BUT SELF-MOVING
  (converted to a recipe), or WRONG (withdrawn).
- The audit states its enumeration method **and that method's known gaps**, and does not describe
  itself as a completeness proof.
- `ST_PLANNED_CASES=7` appears exactly once in the file, is unchanged, and is listed in the audit as
  an EXCLUDED row with its reason.
- No `| grep -q` exists in the file and the GC-01 here-string form is intact.
- The diff for this task has at least one changed line and **zero** changed lines that are not
  comments.
- `bash -n` exits 0 and `--self-test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the file's own prose → the file's own grep counts | a number written into a file that greps itself is moved by the sentence that states it; three rounds, three instances, this one inside the fix for the previous one |
| a corrected claim → the next reader | a hedge ("and rises with prose like this") reads as a fix and is not one |
| an audit → a working guard | `ST_PLANNED_CASES=7` is a legitimate pin; an over-eager audit would convert a functioning gate into prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R405 | Repudiation (a present-tense count that the stating hunk falsified) | the R3-07 census paragraph in `check-beets-config.sh` | mitigate | the raw figure withdrawn, not re-pinned; the comment-stripped figure kept as the durable one; the sibling precedent cited |
| T-06-R405b | Repudiation (the same class surviving elsewhere in the same file) | every other in-band count claim | mitigate | a bounded audit with three dispositions, its enumeration method and gaps recorded, no executable line changed |
| T-06-R405c | Tampering (an audit breaking a correct guard) | `ST_PLANNED_CASES=7` | accept | excluded by name, with the reason recorded in the audit table; round 4 verified it unconditional and green at 7 |
| T-06-38-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- **No estate contact.** `--self-test` is workstation-only and is the only invocation of this
  script. No `--run`, no ssh, no docker, no live `library.db` is opened.
- **Both tasks are comment-only, and that is asserted mechanically, not claimed** — zero non-comment
  lines in each task's diff, plus byte-identical `--self-test` output across task 1.
- `bash -n scripts/check-beets-config.sh` parses clean after every task; `--self-test` exits 0 at 7
  cases throughout.
- **How these verify blocks are driven.** Before any edit, extract both `<automated>` blocks,
  `bash -n` each, and run each against the unfixed tree: task 1 must fail at the
  `(raw is 4, and rises with prose` absence test, task 2 at the artifact-exists test. Neither may
  reach its end. A block that passes pre-fix is vacuous and must be rewritten first; record both
  pre-fix transcripts in the artifact.
- No `git push`, no host `git pull`. `git diff --name-only -- scripts/` lists only
  `scripts/check-beets-config.sh`.
- This plan must not touch `scripts/quick-health-check.sh` or `scripts/phase06-oracle.sh` — plans
  06-35 and 06-36 own those this round and run in parallel — nor
  `scripts/setup-neocortex-memory.sh`, `stacks/selfhosted/neocortex-memory/` or
  `stacks/selfhosted/agentic-os/`.
- **No requirement checkbox moves. CONF-04 is not closed and is not claimed. The phase is not
  declared complete.**
- `06-VERIFICATION.md` is **stale and out of scope**. Do not read it as a gap source and do not
  touch it.
</verification>

<success_criteria>
- R4-05 closed as a claim correction only, by **withdrawal** rather than re-pinning, with no raw
  figure anywhere in the replacement.
- The comment-stripped census still measures 2 when its recipe is run, and the recipe is still what
  the file offers the reader.
- Every other in-band count claim in this file has been measured against its own recipe and
  dispositioned in the audit, with `ST_PLANNED_CASES=7` excluded by name and untouched.
- Neither task changed an executable line, proven mechanically.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-38-SUMMARY.md` when done.
State explicitly that R4-05's fix was a **CLAIM CORRECTION**, that both tasks were comment-only and
how that was proven, and list any claim the audit found that needs a **code** change — those are
findings for plan 06-39 to defer by name, not work this plan performed.
</output>
