---
phase: 06-tagger-configuration-and-dry-run
plan: 28
type: execute
wave: 3
depends_on: ["06-22", "06-23", "06-24", "06-25", "06-26", "06-27"]
gap_closure: true
gap_closure_round: 2
files_modified:
  - scripts/phase06-oracle.sh
  - scripts/check-beets-config.sh
  - stacks/selfhosted/arrs/beets.md
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-06]
closes_findings: [GC-04, GC-10]

must_haves:
  truths:
    - "No cross-file reference introduced by this phase's gap closure points at a line number — every one is a greppable anchor, so the next sibling edit above it cannot silently invalidate it"
    - "Every anchor is proven to resolve: each is grepped in the file it names during this plan's own verification and must return at least the number of hits its reference implies"
    - "The oracle's fence-duplication argument and `beets.md`'s exemption register, both of which rest entirely on those references, now point at what they claim to point at"
    - "`beets.md`'s measured-counts table is re-measured against the tree as it stands after this round, and no longer claims an agreement its own presence invalidates"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "the corrected exemption register and the re-measured counts table"
      contains: "SCRATCH_OVERLAY"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt"
      provides: "the citation audit — every cross-file reference in the five touched files, with its resolution before and after — and the re-measured count vector"
  key_links:
    - from: "scripts/phase06-oracle.sh fence comments"
      to: "scripts/phase06-incremental-control.sh's two literal fences"
      via: "a greppable anchor (PREP REFUSED / CLEANUP REFUSED) rather than a line number"
      pattern: "REFUSED"
---

<objective>
Close **GC-04** and **GC-10** — the documentation half of round 2 — and do it **last**, after every
file-editing plan in this round has landed, because a citation repaired before its target stops
moving is a citation that will be stale again by the time the round closes.

GC-04: four cross-file `file:line` citations introduced by round 1 are wrong. Verified during
planning, each by opening the target:

| citation | cited as | actually at, at planning time | what is at the cited line |
|---|---|---|---|
| `phase06-incremental-control.sh:343` and `:473` (oracle's fence comment) | the sibling's two literal `case` fences | `:387` and `:520` | `:343` is a comment; `:473` is `ROOT="$1"; PY="$2"; PROG="$3"` |
| `phase06-incremental-control.sh:473` (oracle, twice more) | "has the same shape" / "fences its cleanup the same way" | `:520` | as above — not a fence |
| `beets/beets.yaml:71-78` (`check-beets-config.sh`) | "Phase 1 measured a bare `beet config` running 11 migrations unasked" | the paragraph anchored by `MIGRATE ITS SCHEMA`, near the top of `beets.yaml` | `:71-78` is the EBUSY/EROFS single-file-mount measurement, a different subject |
| `phase06-oracle.sh` lines `2166, 2179, 2313` (`beets.md`) | the three `$SCRATCH_OVERLAY` exempt invocations | `2478`, `2491`, `2625` | off by ~310 lines |

These are not decorative. The oracle's fence-duplication argument rests entirely on *"the sibling
already does this, at these lines"*, and `beets.md`'s exemption register is the human-readable half
of `D04_EXEMPT_RE` — what a reader checks the pin against. **Two of the four went stale inside
round 1 itself**: 06-18 wrote the sibling citations and 06-19/06-20 then inserted ~130 lines above
them; 06-16 wrote the `beets.md` numbers and 06-18/06-19 then added ~450 lines to the oracle. This
round's own waves have moved all of them again, which is why every number in the table above must
be **re-derived at execution time** and none of it trusted.

One detail from planning worth inheriting, because it is the trap this plan exists to avoid: the
claim sentence in `beets.yaml` **wraps across two lines**, so `11 migrations unasked` is not a
greppable single-line anchor even though it reads like one. `MIGRATE ITS SCHEMA` is, and is unique
in that file. An anchor that does not resolve is worse than a stale line number, because it reads
as a working reference.

GC-10: `beets.md`'s measured-counts table reads raw 191 / comment-stripped 90 against a tree that
measured 199 / 98 while this plan was written — and will have moved again by the time this plan
runs. The note claims *"hand-reproduced from the host and compared against the block's own printed
figures — agreement at every position, both trees"*, which is **self-invalidating**: the note's own
body quotes `D04_INV_RE` and `BEET[A-Z_]*`, which adds `BEET`-matching lines to the raw and
comment-stripped counts. No functional impact — neither count is pinned — but the file's stated
convention for self-referential counts, used correctly by the eleventh and twelfth exit-code
notices, was not applied here.

Purpose: `DEF-06-21-08` already records that plan line citations went stale in **four consecutive
plans**, and the cross-family adjudication's standing note repeats it: *"the reviewer's claims were
sound and its line numbers were not."* This plan is where that lesson gets applied to the shipped
files rather than only recorded about the planning ones.

Output: anchors instead of line numbers at every site, each anchor proven to resolve, and a counts
table that is re-measured and does not claim an agreement it lacks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW-GAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
@scripts/phase06-oracle.sh
@scripts/check-beets-config.sh
@stacks/selfhosted/arrs/beets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace every stale cross-file citation with an anchor, and prove each anchor resolves</name>
  <files>scripts/phase06-oracle.sh, scripts/check-beets-config.sh, stacks/selfhosted/arrs/beets.md, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt</files>
  <read_first>
    - **Do not start from the table in this plan's objective.** Start by enumerating every
      cross-file `file:line` reference in the five files this round has touched, one file per grep
      so the counts are attributable:
      `grep -nE '(\.sh|\.yaml|\.md):[0-9]+' scripts/phase06-oracle.sh`, then the same for
      `scripts/check-beets-config.sh`, `scripts/quick-health-check.sh`,
      `scripts/phase06-incremental-control.sh` and `stacks/selfhosted/arrs/beets.md`; plus the
      `beets.md` table-row form, `grep -n '| [0-9]\+, [0-9]\+' stacks/selfhosted/arrs/beets.md`.
      Resolve **every** hit, not only the four the review names — this round has moved line numbers
      in four files, and a repair that fixes four of nine is the same defect in a smaller coat.
      Some hits will be references a file makes to **itself**, or to an upstream source file; judge
      each and record the judgement rather than rewriting indiscriminately.
    - The four sites the review names, located by anchor:
      `grep -n 'fences its two throwaway roots' scripts/phase06-oracle.sh`,
      `grep -n 'has the same shape' scripts/phase06-oracle.sh`,
      `grep -n 'fences its cleanup the same way' scripts/phase06-oracle.sh`,
      `grep -n 'migrations unasked' scripts/check-beets-config.sh`,
      `grep -n 'SCRATCH_OVERLAY' stacks/selfhosted/arrs/beets.md`.
    - The anchors those citations should point to, each verified in its own file **at execution
      time**:
      `grep -n 'PREP REFUSED' scripts/phase06-incremental-control.sh`,
      `grep -n 'CLEANUP REFUSED' scripts/phase06-incremental-control.sh`,
      `grep -n 'MIGRATE ITS SCHEMA' stacks/selfhosted/arrs/beets/beets.yaml`,
      `grep -n 'SCRATCH_OVERLAY' scripts/phase06-oracle.sh`,
      `grep -n 'ROOT/overlay.yaml' scripts/phase06-incremental-control.sh`.
      **Plan 06-25 changed the incremental control's temp handling and may have changed those
      messages.** Re-derive; do not assume the anchors still occur the same number of times.
    - The anchor convention this repository already uses, so the new ones match it:
      `grep -n 'KEEP THIS HEADING LITERAL' scripts/check-music-consumers.sh` and
      `grep -n '📊 6\. Summary' scripts/quick-health-check.sh`.
    - `deferred-items.md` `DEF-06-21-08` and its closing paragraph — *"`grep -n` on anchor text is
      the reliable route; a cited line number is not."*
  </read_first>
  <action>
**(a) Build the audit first, then edit.** In `artifacts/06-28-citations-and-counts.txt`, table every
cross-file reference found by the enumeration above with four columns: the citing site (file plus
its own anchor, not its line number), what it claims is there, what is **actually** at the cited
line today, and the replacement anchor — or the reason it is left alone. This table is the
deliverable a later reader checks; the edits are its consequence.

**(b) Replace each with an anchor phrased as an instruction.** Follow the form the review
recommends and the repository already uses: *"grep `phase06-incremental-control.sh` for
`PREP REFUSED` and `CLEANUP REFUSED`"* rather than a line pair. Choose anchor text that is
**unique in its target file**, sits on **one line**, and is part of the thing being pointed at (a
printed message, a variable name, a heading) rather than prose about it — prose about a thing is
the trap `DEF-06-11-02` recorded, and a wrapped sentence is the trap this plan's objective records.
Verify the uniqueness and the single-line property with `grep -c` **before** writing the anchor in.

**(c) Two specific repairs the audit will surface.**
  - `check-beets-config.sh`'s `beets/beets.yaml:71-78` becomes an anchor for the sentence actually
    making that claim in `beets.yaml`. `MIGRATE ITS SCHEMA` was verified unique and single-line
    during planning; confirm it still is, and confirm the paragraph it heads is the migrations
    claim and not the EBUSY/EROFS measurement, which is a different subject elsewhere in the file.
  - `beets.md`'s exemption register table currently carries **two** rows of line numbers — the
    oracle's three and the incremental control's two. The oracle's were stale by ~310 lines; the
    incremental control's resolved correctly at planning time and may not after 06-25. Replace
    **both** rows with anchors (`$SCRATCH_OVERLAY`, `$ROOT/overlay.yaml`) so the table is uniform
    and cannot half-rot. Keep the per-file counts, which are what the register pins.

**(d) Prove every anchor resolves, mechanically.** For each replacement anchor, record the `grep -c`
result in its target file and require it to be non-zero and — where the reference implies a count
(three oracle sites, two incremental sites) — to be at least that count.

**(e) Do not renumber, reword or relocate anything the anchors point at.** In particular, do not
touch the `REFUSED` message texts in `phase06-incremental-control.sh` (plan 06-25 owns that file),
and do not edit `06-REVIEW-GAP.md` or `06-REVIEW.md`.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
# 1. The stale citations are gone. Baselines measured while this plan was written.
test "$(grep -cF 'incremental-control.sh:343' scripts/phase06-oracle.sh)" -eq 0
test "$(grep -cF 'incremental-control.sh:473' scripts/phase06-oracle.sh)" -eq 0
test "$(grep -cF 'beets.yaml:71-78' scripts/check-beets-config.sh)" -eq 0
test "$(grep -cF '2166, 2179, 2313' stacks/selfhosted/arrs/beets.md)" -eq 0
# 2. No file:line cross-reference to the sibling script or to beets.yaml survives.
#    Grepped ONE FILE AT A TIME: `grep -c` over several files prints one line per file.
test "$(grep -cE '(phase06-incremental-control\.sh|beets\.yaml):[0-9]+' scripts/phase06-oracle.sh)" -eq 0
test "$(grep -cE '(phase06-incremental-control\.sh|beets\.yaml):[0-9]+' scripts/check-beets-config.sh)" -eq 0
# 3. The replacement anchors are present at the citing sites.
test "$(grep -cF 'PREP REFUSED' scripts/phase06-oracle.sh)" -ge 1
test "$(grep -cF 'CLEANUP REFUSED' scripts/phase06-oracle.sh)" -ge 1
test "$(grep -cF 'MIGRATE ITS SCHEMA' scripts/check-beets-config.sh)" -ge 1
test "$(grep -cF 'SCRATCH_OVERLAY' stacks/selfhosted/arrs/beets.md)" -ge 1
# 4. Every anchor RESOLVES in its target file, at least as many times as its reference implies.
test "$(grep -cF 'PREP REFUSED' scripts/phase06-incremental-control.sh)" -ge 1
test "$(grep -cF 'CLEANUP REFUSED' scripts/phase06-incremental-control.sh)" -ge 1
test "$(grep -cF 'MIGRATE ITS SCHEMA' stacks/selfhosted/arrs/beets/beets.yaml)" -eq 1
test "$(grep -cF 'SCRATCH_OVERLAY' scripts/phase06-oracle.sh)" -ge 3
test "$(grep -cF 'ROOT/overlay.yaml' scripts/phase06-incremental-control.sh)" -ge 2
# 5. The audit table exists and covers more than the four named sites.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt
test -s "$ART"
test "$(grep -ci 'GC-04' "$ART")" -ge 1
# 6. Nothing broke.
bash -n scripts/phase06-oracle.sh
bash -n scripts/check-beets-config.sh
bash scripts/phase06-oracle.sh --self-test > /dev/null 2>&1
test $? -eq 0
bash scripts/check-beets-config.sh --self-test > /dev/null 2>&1
test $? -eq 0
    </automated>
  </verify>
  <done>
`artifacts/06-28-citations-and-counts.txt` carries an audit of every cross-file reference in the
five files this round touched, each with what it claimed, what was actually there, and the
replacement anchor or the reason it was left alone. No `file:line` cross-reference to the sibling
script or to `beets.yaml` survives in `phase06-oracle.sh` or `check-beets-config.sh`, `beets.md`'s
exemption register carries anchors in both rows, and every anchor is proven to resolve on one line
in the file it names. Both self-tests still exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-measure beets.md's counts table and stop it claiming an agreement it does not have</name>
  <files>stacks/selfhosted/arrs/beets.md, .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt</files>
  <read_first>
    - The table and the sentence above it. `grep -n 'Measured counts on this plan' stacks/selfhosted/arrs/beets.md`
      and read from there through the end of the table and into the exemption-register paragraph.
    - The convention the repository applies correctly elsewhere for self-referential counts:
      `grep -n 'headers 10 -> 11' scripts/quick-health-check.sh` — the eleventh and twelfth
      exit-code notices state a count as a transition measured before and after the edit that
      changes it, precisely because the note's own text is inside the thing being counted.
    - The pipeline to reproduce, taken from the block itself rather than from memory:
      `grep -n 'git grep -n -I -w -E' scripts/quick-health-check.sh`,
      `grep -n 'D04_INV_RE=' scripts/quick-health-check.sh`,
      `grep -n 'D04_EXEMPT_RE=' scripts/quick-health-check.sh`, and the comment-strip expression
      just above `D04_INV_RE`. **Extract the regexes from the file rather than retyping them** —
      plan 06-23 widened `D04_INV_RE` in this same round, so any copy written into a plan is
      already old.
    - `06-REVIEW-GAP.md` — GC-10 in full, including its measured table and its observation about
      why the claim is self-invalidating.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-23-d04-assert-vacuity.txt`
      — plan 06-23 recorded the same count vector earlier in this round. The two measurements are
      the comparison this task reports on.
  </read_first>
  <action>
**(a) Re-measure, from the file's own current regexes.** Reproduce the block's whole pipeline
against this repository's `HEAD` **after** every other plan in this round has landed: the
`git grep`, the comment strip, `D04_INV_RE`, the `.md` split and `D04_EXEMPT_RE`. Record all seven
figures. The pinned ones — invocation-shaped, executable, asserted, exempt, documentation — must be
10 / 8 / 3 / 5 / 2; plan 06-23 asserts the widening did not move them. If any pinned figure has
moved, **stop and report it**: it means a plan in this round changed what the detector sees, which
is a finding, not a number to write down.

**(b) Fix the two unpinned rows, and fix the reason they went wrong.** `raw` and
`comment-stripped` count the whole repository's `beet`/`BEET*` mentions, so they move with every
commit — including the commit that writes them down. Two acceptable shapes; choose one and say
which and why:
  - state each as a measured transition in the eleventh/twelfth notice's form (`raw N -> M`), with
    the commit the measurement was taken at named; or
  - drop the two unpinned rows from the table entirely, keep the five pinned ones, and add one
    sentence saying the raw and comment-stripped counts are deliberately not recorded because they
    are unpinned, self-referential and change with every commit — which is exactly why quoting them
    produced a claim that was false within a day.
  The second is the stronger shape and is recommended. Either way the table must stop implying that
  the two unpinned rows are checkable facts about the tree.

**(c) Rewrite the claim sentence.** *"hand-reproduced from the host and compared against the block's
own printed figures — agreement at every position, both trees"* is what made this a finding rather
than a typo: it asserts a verification its own presence invalidates. Replace it with what is true
and checkable — which figures are pinned, which are not, what was measured, at which commit, and on
which tree. Name GC-10.

**(d) Record the measurement in the artifact**, with the exact commands, so a later reader can
re-run rather than re-derive it. Compare against plan 06-23's earlier vector and state whether they
agree; if they do not, the difference is the lines this round added, and saying so is the point of
recording both.
  </action>
  <verify>
    <automated>
cd /Users/damian/Development/damianflynn/selfhost-stacks
set -eu   # -e is load-bearing: without it only the LAST test in this block decides
M=stacks/selfhosted/arrs/beets.md
# 1. The stale unpinned figures are gone. Baselines: each was 1 while this plan was written.
test "$(grep -cF '| raw | 191 |' $M)" -eq 0
test "$(grep -cF '| comment-stripped | 90 |' $M)" -eq 0
# 2. The self-invalidating claim is gone. Baseline: 1.
test "$(grep -cF 'agreement at every position, both trees' $M)" -eq 0
# 3. The pinned figures survive and the register is still there.
test "$(grep -cF 'invocation-shaped' $M)" -ge 1
test "$(grep -cF 'D04_DOC_BASELINE' $M)" -ge 1
# 4. GC-10 is named where the correction was made.
test "$(grep -cF 'GC-10' $M)" -ge 1
# 5. Re-measure the pinned vector from the file's OWN current regexes and assert the pins.
INV_RE="$(sed -n "s/^[[:space:]]*D04_INV_RE='\(.*\)'$/\1/p" scripts/quick-health-check.sh)"
EX_RE="$(sed -n "s/^[[:space:]]*D04_EXEMPT_RE='\(.*\)'$/\1/p" scripts/quick-health-check.sh)"
test -n "$INV_RE"
test -n "$EX_RE"
git grep -n -I -w -E -e 'beet' -e 'BEET[A-Z_]*' HEAD -- scripts stacks > /tmp/06-28-hits.txt
grep '^HEAD:' /tmp/06-28-hits.txt > /tmp/06-28-raw.txt
grep -vE '^HEAD:[^:]*:[0-9]*:[[:space:]]*(#|//)' /tmp/06-28-raw.txt > /tmp/06-28-kept.txt
grep -E "$INV_RE" /tmp/06-28-kept.txt > /tmp/06-28-inv.txt || true
grep '\.md:' /tmp/06-28-inv.txt > /tmp/06-28-doc.txt || true
grep -v '\.md:' /tmp/06-28-inv.txt > /tmp/06-28-exe.txt || true
grep -E "$EX_RE" /tmp/06-28-exe.txt > /tmp/06-28-exempt.txt || true
grep -vE "$EX_RE" /tmp/06-28-exe.txt > /tmp/06-28-assert.txt || true
test "$(grep -c '^HEAD:' /tmp/06-28-inv.txt)" -eq 10
test "$(grep -c '^HEAD:' /tmp/06-28-exe.txt)" -eq 8
test "$(grep -c '^HEAD:' /tmp/06-28-exempt.txt)" -eq 5
test "$(grep -c '^HEAD:' /tmp/06-28-assert.txt)" -eq 3
test "$(grep -c '^HEAD:' /tmp/06-28-doc.txt)" -eq 2
# 6. The artifact records the measurement and the comparison with 06-23's vector.
ART=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-28-citations-and-counts.txt
test "$(grep -ci 'GC-10' "$ART")" -ge 1
test "$(grep -cF '06-23' "$ART")" -ge 1
    </automated>
  </verify>
  <done>
`beets.md`'s counts table no longer carries the stale 191 / 90 and no longer claims an agreement its
own presence invalidates. The five pinned figures are re-measured from the file's own current
regexes and still read 10 / 8 / 3 / 5 / 2. The artifact records the exact commands, the resulting
vector, and whether it agrees with plan 06-23's earlier measurement — with the difference explained
if it does not.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a reader trusting a cross-file citation → the claim it supports | a stale citation launders an unchecked claim into a checked-looking one; the oracle's fence-duplication argument and `beets.md`'s exemption register both rest entirely on theirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-GC04 | Repudiation (a reference that cannot be checked reads as one that was) | four cross-file citations | mitigate | replaced with anchors, each proven unique and single-line in its target, with the whole reference set audited rather than the four named |
| T-06-GC10 | Repudiation (a note asserting a verification its own presence invalidates) | `beets.md` counts table | mitigate | unpinned rows removed or stated as measured transitions; pinned vector re-measured from the file's own regexes |
| T-06-28-SC | Tampering | npm/pip/cargo installs | accept | this plan installs nothing |
</threat_model>

<verification>
- `bash -n` clean on both scripts touched; both `--self-test`s exit 0.
- Every replacement anchor returns a non-zero `grep -c` in the file it names, grepped one file at a
  time so the count is attributable.
- The pinned count vector is 10 / 8 / 3 / 5 / 2 after the whole round.
- No estate contact, no `git push`, no host `git pull`.
- `06-REVIEW-GAP.md` and `06-REVIEW.md` are not edited by this plan.
</verification>

<success_criteria>
- GC-04 closed: no `file:line` cross-reference to the sibling script or to `beets.yaml` survives in
  `phase06-oracle.sh` or `check-beets-config.sh`; `beets.md`'s register uses anchors in both rows;
  every anchor resolves on one line; the audit covers every reference found by enumeration, not
  only the four the review named.
- GC-10 closed: the counts table is re-measured, the unpinned rows are handled honestly, and the
  self-invalidating claim sentence is gone.
- If any **pinned** figure moved during this round, it is reported as a finding rather than written
  into the table.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-28-SUMMARY.md` when done.
Include a NOT-DRIVEN register and, if any pinned count moved, a clearly-flagged new finding for
plan 06-29.
</output>
