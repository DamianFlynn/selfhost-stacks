---
phase: 06-tagger-configuration-and-dry-run
plan: 15
type: execute
wave: 6
depends_on: ["06-14"]
gap_closure: true
files_modified:
  - scripts/check-beets-config.sh
autonomous: true
requirements: [CONF-01, CONF-02, CONF-05]
closes_findings: [CR-01-violation-half, WR-04, WR-05, IN-01, IN-03, IN-04, IN-07]

must_haves:
  truths:
    - "Every `beet` invocation `check-beets-config.sh` makes carries BOTH a throwaway `-l` outside `/config/library.db` AND a `-c` overlay — so the real library cannot be opened by the script that reads the config (D-04), rather than merely not being changed by it"
    - "Arm 2's no-op overlay is proven EMPTY at the moment it is used, not merely proven to exist — a stale file left in the container's world-writable /tmp can no longer silently outrank the vendored config"
    - "The script's own `READ-ONLY BY CONTRACT` header names its one write, so the header is true as written rather than true-in-spirit"
    - "`check-beets-config.sh --self-test` still exits 0 and now drives SIX cases, the sixth of which is red for the `-l`-and-overlay contract itself"
    - "A live run still exits 0 with `FAILURES total: 0` and the D-29 layer-3 hashes byte-identical either side, re-measured after the change — not carried forward from the 2026-09-21T22:04Z run"
  artifacts:
    - path: "scripts/check-beets-config.sh"
      provides: "the D-04-compliant, truncation-proven, header-honest config assertion"
      contains: "p6-cbc-throwaway.blb"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-15-check-beets-config-rerun.txt"
      provides: "the post-change self-test and live-run transcripts with the driven red"
  key_links:
    - from: "scripts/check-beets-config.sh"
      to: "scripts/quick-health-check.sh D-04 block"
      via: "the three beet_exec lines becoming compliant so the widened D-04 pattern (plan 06-16) reports them green rather than red"
      pattern: "-l /tmp/p6-cbc-throwaway.blb"
---

<objective>
Close the live half of CR-01: the script this phase committed to *read* the beets config opens the real `/config/library.db` three times per run with no `-l`, which is a direct violation of the D-04 rule plan 06-10 wrote into the estate's health entry point.

Purpose: CR-01 has two halves — a broken detector (plan 06-16) and a real violation (this plan). Fixing only the detector would turn the phase's own deliverable red; fixing only the violation would leave the detector blind to the next one. This plan makes the violation go away for the right reason: the invocations become compliant, so the D-29 hash comparison proves the real DB was *never opened* rather than merely *never changed*. Four cheap same-file findings that share the section or the header ride along (WR-04, IN-01, IN-03, IN-04, IN-07), because they are one commit's worth of work in a file that is about to be re-run and re-proved anyway.

Output: one script, changed and re-proved by its own `--self-test` plus a live run, with a driven red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
@scripts/check-beets-config.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the three arm-2 invocations D-04 compliant, and make the no-op overlay provably empty</name>
  <files>scripts/check-beets-config.sh</files>
  <read_first>
    - `scripts/check-beets-config.sh:8-14` — the `READ-ONLY BY CONTRACT` paragraph, verbatim, including the sentence *"No import runs, no library is modified"* and the D-29 layer-3 claim. This paragraph is amended by this task and must be read before it is edited.
    - `scripts/check-beets-config.sh:105-120` — the plain-constant block: `EXEC_USER_FLAG`, `BEET_BIN=/venv/bin/beet`, `PY_BIN`, `OVERLAY=/tmp/phase06-check-beets-config-noop.yaml`, `LIBRARY_DB=/config/library.db`, `STATEFILE=/config/state.pickle`, `REMOTE_TIMEOUT=120`. The new throwaway path is declared here, beside them, as a plain constant for the same reason they are.
    - `scripts/check-beets-config.sh:58-80` — the override contract: exactly one env override, `EXTRA_FORBIDDEN_SUBSTRINGS`, ADDITIVE, and the explicit standing rule *"THERE IS NO SENTINEL THAT SKIPS A CHECK — not in this file and not anywhere in this repository. Do not add one."* The new constant must not become an override.
    - `scripts/check-beets-config.sh:686-700` — `beet_exec()` and `hash_beets_state()`: how the remote command is assembled (`timeout ${REMOTE_TIMEOUT} docker exec ${EXEC_USER_FLAG} ${CONTAINER} $*`) and how `BEET_EXEC_RC` is captured.
    - `scripts/check-beets-config.sh:756-822` — ARM 2 in full: the `touch ${OVERLAY}` at `:765` with its three-line comment claiming *"empty is the only value that leaves arm 2 an honest reading"*, the three `${BEET_BIN} -c ${OVERLAY} config …` calls at `:769`, `:785`, `:796`, the T-06-33 redaction no-op, and the D-29 layer-3 before/after hash comparison at `:809-820` (`:709-711` is the "before" read).
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:263-282` (WR-05) and `:239-261` (WR-04) — the two findings this task closes, with their stated mitigations and the exact fixes proposed.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md:65-72` (D-04) — why BOTH flags, and the accepted consequence that the CLI arm cannot answer "what is in the library".
    - `scripts/quick-health-check.sh:1452-1462` — the D-04 rule as the estate's health entry point states it, including the reason `-l` alone is insufficient (`statefile:` is a separate pickle, not covered by `-l`).
  </read_first>
  <action>
Three edits to `scripts/check-beets-config.sh`, all inside ARM 2 and its header, plus one new constant.

**Edit 1 — declare the throwaway library path as a plain constant.** Beside `OVERLAY` at `:112`, add `THROWAWAY_DB="/tmp/p6-cbc-throwaway.blb"`. Plain constant, not `${VAR:-default}`, for the reason `:73-77` already gives about `BEET_BIN` and `CONTAINER`: an override on it could point the read somewhere that manufactures a pass. Add a one-sentence comment stating that `.blb` is beets' own library extension and that the path is inside the container, not on the LXC.

**Edit 2 — add `-l ${THROWAWAY_DB}` to all three `beet_exec` calls.** The three lines are `:769` (`config -d`), `:785` (`config -p -d`) and `:796` (`config -d -c`). Each becomes `${BEET_BIN} -l ${THROWAWAY_DB} -c ${OVERLAY} config …`, with `-l` FIRST so the flag order matches the D-04 rule's own wording and so a reader scanning for `-l` finds it before the subcommand. Do not change any subcommand, any flag after `config`, or the order of the three calls — arm 2's answers must stay the same answers, and the T-06-33 `cmp -s` at `:799` compares `arm2.dump` against the third call's output, so a changed dump on only one of the three would read as a credential.

State in a comment above the first call, in three sentences, WHY `-l` is added and what it buys that D-29 does not: `beet` opens the library for every subcommand including `config` (Phase 1 measured a bare `beet config` running 11 migrations unasked, `stacks/selfhosted/arrs/beets/beets.yaml:71-78`); with `-l` the real `/config/library.db` is never opened at all, so the D-29 layer-3 hash comparison at `:809-820` upgrades from *proving nothing changed* to *corroborating that nothing was opened*; and the overlay is still required because `-l` alone does not redirect `statefile:`.

**Do NOT redirect `statefile:` through the overlay in this plan.** The overlay must stay empty — that is exactly what edit 3 asserts and what arm 2's honesty depends on. `state.pickle` is only written by an *import*, and this script runs none; the D-29 layer-3 hash of `STATEFILE` remains the proof of that and stays unchanged.

**Edit 3 — make the no-op overlay provably empty (WR-04).** Replace `beet_exec "touch ${OVERLAY}"` at `:765` with a truncating create, and then ASSERT the result rather than assume it: issue the truncate, then a second bounded `beet_exec` that reads the file's size, and require the size to be exactly `0`. A non-zero size, an unreadable file, or a failed size read is `fail "ARM 2: UNKNOWN, not green — …"` and must NOT fall through to the three `config` calls, because a non-empty overlay outranks the vendored config and would make arm 2 report something other than what the container assembles. Use `sh -c` with a redirect that truncates (a `>` redirection or `truncate -s 0`); do not use `touch`, which guarantees existence and not emptiness.

**Edit 4 — amend the header's `READ-ONLY BY CONTRACT` paragraph (`:8-14`).** It currently claims *"Nothing is installed anywhere and nothing is written."* That is false as written: this script writes `${OVERLAY}` into the container, and now truncates it. Name the exception explicitly — the one write is a zero-byte overlay at a fixed path inside the container's `/tmp`, it is truncated rather than created-if-absent, its emptiness is asserted before it is used, and nothing removes it. Do not soften the rest of the paragraph; the library and statefile claims remain true and are now stronger.
  </action>
  <acceptance_criteria>
    - `grep -c` over comment-stripped `scripts/check-beets-config.sh` for `${BEET_BIN} -l ${THROWAWAY_DB} -c ${OVERLAY} config` returns exactly `3`
    - `THROWAWAY_DB="/tmp/p6-cbc-throwaway.blb"` is present as a plain assignment; no `${THROWAWAY_DB:-` form appears anywhere in the file
    - No comment-stripped line matches `${BEET_BIN} -c ${OVERLAY} config` — the `-c`-only shape is gone from all three sites
    - No `touch ${OVERLAY}` remains; a truncating form (`truncate -s 0` or a `>` redirect on `${OVERLAY}`) is present
    - A zero-size assertion on `${OVERLAY}` exists, its failure branch calls `fail` with the literal `UNKNOWN, not green`, and it is at a LOWER line number than the first `config -d` call
    - The string `nothing is written` no longer appears anywhere in the file
    - `bash -n scripts/check-beets-config.sh` exits 0; `shellcheck -S warning` exits 0; `bash scripts/check-beets-config.sh --self-test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/check-beets-config.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); n=$(printf "%s\n" "$s" | grep -c "BEET_BIN} -l \${THROWAWAY_DB} -c \${OVERLAY} config"); [ "$n" -eq 3 ] || { echo "expected 3 compliant beet invocations, found $n"; exit 1; }; printf "%s\n" "$s" | grep -qE "^\s*THROWAWAY_DB=\"/tmp/p6-cbc-throwaway\.blb\"" || { echo "THROWAWAY_DB is not a plain constant at the expected value"; exit 1; }; printf "%s\n" "$s" | grep -q "THROWAWAY_DB=\${THROWAWAY_DB" && { echo "THROWAWAY_DB was made overridable - forbidden"; exit 1; }; printf "%s\n" "$s" | grep -qE "BEET_BIN} -c \$\{OVERLAY\} config" && { echo "a non-compliant -c-only invocation survives"; exit 1; }; printf "%s\n" "$s" | grep -q "touch \${OVERLAY}" && { echo "the touch survives - it does not truncate"; exit 1; }; printf "%s\n" "$s" | grep -qE "truncate -s 0|> *\$\{OVERLAY\}" || { echo "no truncating create found"; exit 1; }; grep -q "nothing is written" "$f" && { echo "the header still claims nothing is written"; exit 1; }; bash "$f" --self-test >/dev/null || { echo "--self-test regressed"; exit 1; }; echo "06-15 task 1 OK"'</automated>
  </verify>
  <done>
    All three `beet` invocations in `check-beets-config.sh` carry `-l /tmp/p6-cbc-throwaway.blb` before their `-c ${OVERLAY}`; `THROWAWAY_DB` is a plain constant with no `${VAR:-}` form; the no-op overlay is truncated and its zero size is asserted with a fail-closed branch before any `config` call; the `READ-ONLY BY CONTRACT` header names the one write; `bash -n`, `shellcheck -S warning` and the existing `--self-test` all still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Four same-file hygiene findings, and a sixth self-test case that drives the new contract red</name>
  <files>scripts/check-beets-config.sh</files>
  <read_first>
    - `scripts/check-beets-config.sh` as task 1 left it.
    - `scripts/check-beets-config.sh:345-362` — `expect_eq()` and `expect_ne()`. `expect_ne` is defined and never called (IN-01); `grep -c '\bexpect_ne\b'` returns 1.
    - `scripts/check-beets-config.sh:400-412` — the hand-rolled `UK` check that was `expect_ne`'s intended consumer, with its two branches (`has_uk == true` → red, `has_uk == false` → pass) and the deliberate absence of a third branch.
    - `scripts/check-beets-config.sh:480-495` — the forbidden-substring loop, including `local IFS=':'` and the unquoted `for forb in $EXTRA_FORBIDDEN_SUBSTRINGS` (IN-07): field splitting is intended, pathname expansion is not.
    - `scripts/check-beets-config.sh:665-678` — the readiness gate loop and its failure message rendering `$((READY_ATTEMPTS * READY_SLEEP))`; `READY_ATTEMPTS=6`, `READY_SLEEP=5` at `:118-119`, and the loop sleeps only BETWEEN attempts, so 5 sleeps of 5 s = 25 s, not 30 s (IN-03).
    - `scripts/check-beets-config.sh:73` — the override-contract paragraph naming `EXEC_USER`, against `:107` where the variable is actually `EXEC_USER_FLAG` (IN-04).
    - `scripts/check-beets-config.sh:497-610` — the whole `--self-test` harness: `synthetic_correct_dump()`, `run_self_test()`, the nested `run_case()` taking name / expected-red-count / dump-text and comparing `ARM1_FAILS`, the five existing cases and their expected red counts (22, 1, 1, 2, 0), the `st_failures` tally, and the final banner text `all 5 cases behaved as expected (4 of them red)`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-REVIEW.md:439-496` — IN-01, IN-03, IN-04 and IN-07 as written, with the fix each proposes.
  </read_first>
  <action>
**IN-01 — `expect_ne` dead code.** Route the `UK` check at `:406-410` through `expect_ne` rather than deleting the helper, so the two helpers stay symmetric and the `UNKNOWN` arm `expect_ne` already carries applies to the `UK` case too. Preserve the existing failure text verbatim — the sentence explaining that MusicBrainz stores `GB`, so `UK` matches nothing and fails SILENTLY, is the load-bearing part of that finding and must survive the refactor word for word. If routing through `expect_ne` cannot preserve both the `has_uk == true` red and the `has_uk == false` pass messages, delete `expect_ne` instead and record that choice in the summary; a helper called once with a mangled message is worse than no helper.

**IN-03 — the overstated wait.** In the readiness-gate failure message at `:673`, change `$((READY_ATTEMPTS * READY_SLEEP))` to `$(( (READY_ATTEMPTS - 1) * READY_SLEEP ))`, which renders 25 s for the committed `READY_ATTEMPTS=6` / `READY_SLEEP=5`. Leave the rest of that message alone, including the sentence about a rejected config killing the watchdog while the page keeps serving.

**IN-04 — the header names a constant that does not exist.** `:73` lists `EXEC_USER`; the variable at `:107` is `EXEC_USER_FLAG`. Change the HEADER to say `EXEC_USER_FLAG`, not the variable — the variable name is referenced at its use site and renaming it is the wider blast radius. This is the exact failure mode that same header spends a paragraph warning about, so a grep for the documented name must now return at least two hits.

**IN-07 — glob expansion in the forbidden-substring loop.** At `:486-487`, field splitting on `:` is intended; pathname expansion against the repo root is not. Disable globbing for the duration of the loop (`set -f` before and `set +f` after, restoring the prior state), or read the value into an array with `IFS=':' read -r -a`. Whichever form is used, add a one-line comment saying which of the two shell behaviours is wanted and which is being suppressed, because the next reader will otherwise re-introduce it.

**The sixth self-test case — and this is the part that makes this plan's own change falsifiable.** The existing harness drives `assert_effective_config`, which is a pure function over a dump; it cannot see invocation flags. Add a SIXTH case to `run_self_test()` that is a *source* assertion rather than a dump assertion: it must read the script's own file, strip comments, and require that every line matching `${BEET_BIN}` carries both `-l ${THROWAWAY_DB}` and `-c ${OVERLAY}` — then drive it RED by feeding the same checker a synthetic line that carries only `-c`, and require that synthetic line to be rejected. Keep the existing five cases and their expected red counts unchanged.

Update the harness's own arithmetic and banner in the same edit: the `expect $expect_reds red` accounting, the `$st_failures of 5 cases` message, the final `all 5 cases behaved as expected (4 of them red)` banner, and the usage text at `:62` that says *"five synthetic dumps, four of which MUST go red"*. Six cases now, five of them red. A harness whose banner says five while it runs six is the same class of defect this whole gap-closure exists to remove.
  </action>
  <acceptance_criteria>
    - `grep -c '\bexpect_ne\b' scripts/check-beets-config.sh` returns `0` or a number `>= 2` — never exactly `1`
    - If `expect_ne` is retained, the string `MusicBrainz stores GB, so UK matches nothing and fails SILENTLY` is byte-present
    - `READY_ATTEMPTS - 1` appears in the readiness-gate failure message
    - `EXEC_USER_FLAG` appears in the header override-contract paragraph; no comment line matches `EXEC_USER` without the `_FLAG` suffix
    - The forbidden-substring loop contains `set -f` or `read -r -a`
    - `bash scripts/check-beets-config.sh --self-test` exits 0 and prints `all 6 cases behaved as expected (5 of them red)`
    - The string `five synthetic dumps` no longer appears in the file
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; f=scripts/check-beets-config.sh; bash -n "$f" || exit 1; command -v shellcheck >/dev/null 2>&1 && { shellcheck -S warning "$f" || exit 1; }; grep -q "EXEC_USER_FLAG" "$f" || exit 1; grep -nE "^#.*[^_]EXEC_USER[^_]" "$f" && { echo "the header still names EXEC_USER"; exit 1; }; grep -q "READY_ATTEMPTS - 1" "$f" || { echo "IN-03 arithmetic not corrected"; exit 1; }; s=$(grep -v "^[[:space:]]*#" "$f"); printf "%s\n" "$s" | grep -qE "set -f|read -r -a" || { echo "IN-07 glob suppression missing"; exit 1; }; printf "%s\n" "$s" | grep -qE "all 6 cases|of 6 cases" || { echo "the self-test banner still claims five cases"; exit 1; }; grep -q "five synthetic dumps" "$f" && { echo "the usage text still says five"; exit 1; }; out=$(bash "$f" --self-test 2>&1); echo "$out" | tail -5; echo "$out" | grep -qE "all 6 cases behaved as expected \(5 of them red\)" || { echo "the six-case self-test did not report 6/5-red"; exit 1; }; echo "06-15 task 2 OK"'</automated>
  </verify>
  <done>
    `expect_ne` is either called or deleted (choice recorded in the summary, `UK` failure text preserved verbatim either way); the readiness message renders 25 s; the header says `EXEC_USER_FLAG`; the forbidden-substring loop splits without globbing and says so; `--self-test` runs SIX cases, five of them red, with the sixth asserting the `-l`+`-c` contract over the script's own source and proven red against a synthetic `-c`-only line; the usage text and both banners agree with the new count.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the script live, with the D-29 hashes and a driven red</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-15-check-beets-config-rerun.txt</files>
  <read_first>
    - `scripts/check-beets-config.sh` as tasks 1 and 2 left it.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/` — list the directory and read the most recent `check-beets-config` transcript if one exists, to match its layout and its level of quoting rather than inventing a new artifact shape.
    - `stacks/selfhosted/arrs/beets.md` around the quoted 2026-09-21T22:04Z re-run (`grep -n "FAILURES total" stacks/selfhosted/arrs/beets.md`) — the previous live result this run must be compared against, line for line.
    - `README.md` § Health Checks — the three rules: fail closed with "could not look" kept distinct from "nothing is wrong"; bound remote commands Linux-side; assert rather than report.
  </read_first>
  <action>
Run the changed script against the live estate and capture everything into `artifacts/06-15-check-beets-config-rerun.txt`.

**Record, in this order, and quote the actual output rather than summarising it:**

1. `bash scripts/check-beets-config.sh --self-test` — full transcript, exit status, and the six-case banner.
2. `bash scripts/check-beets-config.sh` — full transcript and exit status. The expected result is exit 0 with `FAILURES total: 0`, matching the 2026-09-21T22:04Z run quoted in `stacks/selfhosted/arrs/beets.md`. Quote BOTH D-29 layer-3 hash lines (before and after) verbatim, with their paths, and state explicitly whether they are identical to each other AND whether they are identical to the values the previous run recorded. Those are two different claims: the first says this run moved nothing, the second says nothing moved between the two runs.
3. **The new evidence this change buys, stated plainly.** With `-l` in place, the real `/config/library.db` is not opened at all. Record how that is visible: the three `config` calls now read `/tmp/p6-cbc-throwaway.blb`, so confirm — by one bounded `docker exec` read — whether that throwaway file exists in the container after the run and what its size is. Either answer is informative and both must be reported: beets creating a fresh empty library there is the direct evidence the redirect took effect; beets not creating it at all is also acceptable for a `config` subcommand and must be stated as such rather than treated as a failure. Do NOT assert a particular answer — report the measurement and say which reading it supports.
4. **A driven red for the overlay-emptiness guard (task 1, edit 3).** Write a single non-empty line into `${OVERLAY}` inside the container by one bounded `docker exec`, re-run the script, and require it to refuse arm 2 with the UNKNOWN-not-green message and NOT to proceed to the three `config` calls. Then truncate the overlay again and re-run to green. Record both transcripts. This is the only new fail-closed branch in this plan that can be driven against the live estate; drive it.
5. **A driven red for the source assertion (task 2, case 6).** The self-test's sixth case already carries its own synthetic negative. Quote the case-6 lines from the transcript in 1 above so the red is visible in the artifact rather than only in the tally.

**Restore-and-assert discipline, non-negotiable.** Take a sha256 of `scripts/check-beets-config.sh` before step 4 and after step 4, quote both, and assert they are equal — nothing in this task edits the script. Confirm `${OVERLAY}` is left at size 0 in the container. Confirm `git status --porcelain` carries no debris beyond the intended `scripts/` and `.planning/` paths.

**Do not run the oracle, the incremental control, or any import.** This task's only remote actions are this script's own reads plus the two overlay writes named above.
  </action>
  <acceptance_criteria>
    - `artifacts/06-15-check-beets-config-rerun.txt` exists and is non-empty
    - It contains `FAILURES total: 0` from a post-change live run, with the run's exit status quoted
    - It quotes both `D-29 layer 3` lines (before and after) with their sha256 values, and states BOTH comparisons: identical within this run, and against the 2026-09-21T22:04Z run
    - It contains `p6-cbc-throwaway.blb` with a measured existence-and-size result and a statement of which reading it supports
    - It contains a transcript in which `${OVERLAY}` was made non-empty and the script printed `UNKNOWN, not green` for arm 2 WITHOUT proceeding to `config -d`, followed by a restored-and-green transcript
    - It quotes a pre-perturbation and post-perturbation sha256 of `scripts/check-beets-config.sh` and states they are equal
    - It quotes the six-case self-test banner and case 6's red line
    - `git status --porcelain` lists nothing outside `.planning/` and `scripts/check-beets-config.sh`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -o pipefail; a=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-15-check-beets-config-rerun.txt; test -s "$a" || { echo "artifact missing"; exit 1; }; grep -q "FAILURES total: 0" "$a" || { echo "the live green result is not recorded"; exit 1; }; grep -c "D-29 layer 3" "$a" | grep -qE "^[2-9]|^[0-9]{2}" || { echo "both D-29 hash reads are not quoted"; exit 1; }; grep -q "p6-cbc-throwaway.blb" "$a" || { echo "the throwaway library measurement is not recorded"; exit 1; }; grep -qiE "UNKNOWN, not green" "$a" || { echo "the driven overlay red is not recorded"; exit 1; }; grep -qE "all 6 cases behaved as expected" "$a" || { echo "the six-case self-test transcript is not in the artifact"; exit 1; }; grep -qiE "sha256" "$a" || { echo "the restore-and-assert hashes are absent"; exit 1; }; d=$(git status --porcelain | grep -vE "^\?\? \.planning/|^ M \.planning/|^ M scripts/check-beets-config.sh" || true); [ -z "$d" ] || { echo "test debris:"; echo "$d"; exit 1; }; echo "06-15 task 3 OK"'</automated>
  </verify>
  <done>
    The artifact holds the six-case self-test transcript with case 6's red visible, a live run at exit 0 / `FAILURES total: 0`, both D-29 layer-3 hash lines quoted with an explicit two-part comparison (identical within this run, and against the previous run), the throwaway-library measurement reported without a pre-judged verdict, and the overlay-emptiness guard driven RED and back to GREEN with a before/after sha256 of the unchanged script. No oracle, incremental control or import was run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation (macOS) → LXC 100 root ssh → `docker exec` into `beets-flask` | Every command in this script crosses two hops as root. A quoting error at either hop becomes a command the container runs. |
| container `/tmp` (world-writable) → confuse's config priority order | `${OVERLAY}` sits ABOVE the vendored config. Anything that can write that fixed path can change what arm 2 reports. |
| `/config/library.db` + `/config/state.pickle` → two beets versions (2.12.0 in flask, 2.13.1 in the dormant CLI arm) | One database, two engines. An open by the wrong engine migrates the schema (Phase 1 measured 11 unasked migrations). |
| this public repo → the world | No credential, token or password may enter the tree. `THROWAWAY_DB` is a path, not a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-60 | Tampering | `beet config` opening the real `library.db` under 2.12.0 and migrating its schema | mitigate | `-l ${THROWAWAY_DB}` on all three invocations, asserted present three times by the task-1 verify command and by the new self-test case 6 over the script's own source. |
| T-06-61 | Spoofing | A stale or attacker-written `${OVERLAY}` outranking the vendored config, so arm 2 reports a config the container does not assemble | mitigate | Truncating create plus an asserted zero-size read with a fail-closed branch BEFORE any `config` call; driven red in task 3 step 4. |
| T-06-62 | Elevation of Privilege | A future `-l` value made overridable, letting an env var point the read at the real library or another container's library | mitigate | `THROWAWAY_DB` declared a plain constant; the verify command fails if a `${THROWAWAY_DB:-}` form appears. The file's standing rule at `:73-77` forbids a skip sentinel. |
| T-06-63 | Repudiation | The `READ-ONLY BY CONTRACT` header claiming "nothing is written" while the script writes the overlay, so a later reader trusts a false invariant | mitigate | Header amended to name the one write; the verify command fails if the old claim survives. |
| T-06-64 | Information Disclosure | A beets credential surfacing in the redacted-vs-unredacted dump comparison (T-06-33) and reaching a public repo | mitigate | The three `config` calls keep their existing flags and order so `cmp -s` at `:799` still compares like with like; a difference remains a hard stop. |
| T-06-65 | Denial of Service | An unbounded remote command hanging the workstation run | mitigate | Every `beet_exec` is already `timeout ${REMOTE_TIMEOUT} …` Linux-side; the two new overlay reads/writes go through the same wrapper and inherit the bound. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. `shellcheck` is used only if already present on the workstation. |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S warning` clean on `scripts/check-beets-config.sh`.
- `bash scripts/check-beets-config.sh --self-test` exits 0 with six cases, five red.
- `bash scripts/check-beets-config.sh` exits 0 with `FAILURES total: 0` and identical D-29 layer-3 hashes.
- `grep -c '${BEET_BIN} -l ${THROWAWAY_DB} -c ${OVERLAY} config'` over comment-stripped source returns exactly 3.
- `git diff --stat` shows exactly one script plus one artifact.
</verification>

<success_criteria>
- No `beet` invocation in `check-beets-config.sh` can open `/config/library.db`.
- Arm 2's overlay is proven empty at the moment of use, with a driven red.
- The self-test grew a case that fails when the `-l`+`-c` contract is violated in this file's own source.
- The live result is re-measured after the change, not carried forward.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-15-SUMMARY.md` when done. Record the `expect_ne` choice (called vs deleted) and the throwaway-library measurement result explicitly, because plan 06-16 pins an exemption baseline that assumes these three lines are compliant.
</output>
