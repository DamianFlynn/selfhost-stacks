---
phase: 06-tagger-configuration-and-dry-run
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CONF-04, CONF-06]
must_haves:
  truths:
    - "CONF-06's requirement text is NOT rewritten; the correction that `beet import --pretend` cannot produce the intended tree lands as a dated traceability addendum, following the precedent TAGR-05 set at REQUIREMENTS.md:276 (D-33)"
    - "`beet move -p` is named in the requirement record as the destination-path oracle, with its source citation, so the next reader cannot re-derive `--pretend` from the criterion text alone"
    - "CONF-04's write side is recorded as the `ARTISTS` tag plus a Jellyfin library option, not `;` in `ARTIST` — D-34's amendment to D-23, with the proof that beets has no write-delimiter key"
    - "PROJECT.md's claim that `tank/downloads@pre-phase5` must not be destroyed before Phase 6 signs off is corrected: Phase 6 writes nothing, so it produces no evidence that Phase 5's changes were correct (D-32)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-06 and CONF-04 traceability addenda"
      contains: "beet move -p"
    - path: ".planning/PROJECT.md"
      provides: "the corrected snapshot-release condition"
      contains: "pre-phase5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "scripts/phase06-oracle.sh"
      via: "the named oracle instrument"
      pattern: "beet move -p"
---

<objective>
Land the three record corrections Phase 6 owns, before any measurement is taken against them. Two are traceability addenda in `REQUIREMENTS.md` (D-33, D-34); one is a wording correction in `PROJECT.md` (D-32).

Purpose: D-33 exists because `.planning/REQUIREMENTS.md:276` already recorded, on 2026-09-11, that `--pretend` never calls `lookup_candidates` — and CONF-06 was written anyway. The fix for a requirement whose instrument is wrong is an addendum that names the right instrument, not a rewrite that erases the history of getting it wrong. D-32 exists because releasing the only undo for 4,750 renames, 751 tag writes and 26,005 chowns on the word of a paper phase is the wrong trade.

Output: two addenda and one correction, each dated, each keeping the superseded text visible. No code, no host access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONF-06 and CONF-04 traceability addenda in REQUIREMENTS.md (D-33, D-34)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 140-160 — the CONF-01…CONF-06 requirement statements, so the addenda quote the text they qualify.
    - .planning/REQUIREMENTS.md line 276 — the TAGR-05 row. This is the EXACT precedent D-33 follows: an `**ADDENDUM <date> (<plan>, <decisions>)**` block appended to the traceability row, ending with "The text above is deliberately not rewritten."
    - .planning/REQUIREMENTS.md lines 280-285 — the CONF-01…CONF-06 traceability rows currently reading `Pending`; the addenda attach to the CONF-06 and CONF-04 rows.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md § D-33 and § D-34 — the locked decision text, including the exact source citations to carry across.
  </read_first>
  <action>
Append two addenda to `.planning/REQUIREMENTS.md`, each attached to its traceability row in the same style as the TAGR-05 row at line 276. Do not edit the CONF-04 or CONF-06 requirement statements at lines 152-156 — the whole point of the precedent is that the requirement text stands and the correction is traceable beside it.

**Addendum A — CONF-06 (D-33).** Mark it `**ADDENDUM 2026-09-20 (06-02, D-33)**`. It must state:
- The instrument named in the requirement, `beet import --pretend`, **cannot** produce the intended tree. In beets 2.12.0 the pretend pipeline is exactly two stages, `read_tasks → log_files`; `log_files` prints `Album: <source dir>` then one `  <source file>` line per item. `lookup_candidates` is not in the pipeline, so no MusicBrainz query runs, no match exists and no destination path is ever computed. Cite `beets/importer/session.py@v2.12.0:201-240` and `beets/importer/stages.py@v2.12.0:266-274`.
- This is **not a new finding**: the same fact is recorded against TAGR-05 at line 276 on 2026-09-11, and CONF-06 was written after it. Say so explicitly — a fact that was already written down and then not used is a different failure from a fact nobody knew.
- **The destination-path oracle is `beet move -p`**, which prints `Source … -> Destination` pairs via `item.destination()` — the same code path a real import uses, so it evaluates the full `paths:` stanza, `%aunique{}`, `replace:`, `asciify_paths`, `legalize_path` and `max_filename_length`. Cite `beets/ui/commands/move.py@v2.12.0`. Both instruments are read-only.
- `--pretend` is **kept**, for what it genuinely proves: which folders are offered as tasks, i.e. `incremental`, `ignore`, `ignore_hidden`, `clutter`, `singletons` and album grouping — and it proves that without reaching `finalize`, the only place `save_history()`/`save_progress()` are called (`beets/importer/tasks.py@v2.12.0:310-320`). That is why it still serves D-31.
- The trap to watch, stated as a warning sign: `--pretend` prints one line per file and exits 0, so a reader takes it as "the tree is right". The discriminator is that a `--pretend` transcript contains **no ` -> ` and no `/mnt/tank/media/Music/` substring at all**.
- Close with "The text above is deliberately not rewritten."

**Addendum B — CONF-04 (D-34, amending D-23).** Mark it `**ADDENDUM 2026-09-20 (06-02, D-34)**`. It must state:
- CONF-04's **write side** is the `ARTISTS` tag (ID3 `TXXX:ARTISTS`, Vorbis `ARTISTS`), **not** `;` in `ARTIST`. beets builds `item.artist` by concatenating MusicBrainz artist-credit `joinphrase` values (`"".join(artist_parts)`, cite `beetsplug/musicbrainz.py@v2.12.0:341-367`), and **no delimiter, separator or join key exists anywhere in `config_default.yaml` at 2.12.0 or 2.13.1** (measured 2026-09-20 by grep over both files). beets therefore cannot be configured to emit `;` in `ARTIST`. The `;` named in the requirement is a property of the **existing** library's 12 files, not of anything the pipeline will produce.
- CONF-04's **read side** becomes a decision rather than an observation. The Jellyfin Music library measures `UseCustomTagDelimiters=False` and `PreferNonstandardArtistsTag=False` (`GET /Library/VirtualFolders`, 2026-09-20), and 10.11.11's prober splits on custom delimiters only when the former is true (`MediaBrowser.Providers/MediaInfo/AudioFileProber.cs@v10.11.11:227-246`). D-34 **enables `PreferNonstandardArtistsTag`** so Jellyfin reads the tag beets actually writes and agrees with Music Assistant (`TAG_SPLITTER=";"`, `ARTISTS` preferred) **by construction rather than by coincidence**. `UseCustomTagDelimiters` is deliberately NOT enabled: `/`, `|` and `\` come along with `;` across 1,244 files unless `CustomTagDelimiters` is narrowed and `DelimiterWhitelist` populated, and `AC/DC` is the canonical casualty.
- ⚠ Record that this is a **live-service config change git does not capture**, that it is written into `stacks/selfhosted/arrs/beets.md` and asserted by `scripts/check-music-consumers.sh` (plan 06-12), and that without the assertion it is one UI click from silently reverting.
- Record that D-20's exhaustive delimiter search was **answered offline** against Phase 1's `pre-project.ndjson.gz` over all 1,244 library files: 6 carry `;` in `ARTIST` (all in `P!nk/TRUSTFALL (2023)`, including a three-artist track) and 6 carry `;` in `ARTISTS` (P!nk, Lady Gaga ×2 with a four-artist case, Katy Perry). **D-21's fenced write with the scoped `rw` grant is therefore NOT required and must not fire** — the `:ro` invariant stays unbroken for the whole phase.
- Close with "The text above is deliberately not rewritten."

Also update the CONF-04 and CONF-06 status cells from `Pending` to `Pending (addendum 2026-09-20)` so the addendum is discoverable from the status column, not only from the row body.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/REQUIREMENTS.md; grep -q "beet move -p" "$f" || { echo "missing beet move -p"; exit 1; }; grep -q "lookup_candidates" "$f" || exit 1; grep -q "ADDENDUM 2026-09-20 (06-02, D-33)" "$f" || exit 1; grep -q "ADDENDUM 2026-09-20 (06-02, D-34)" "$f" || exit 1; grep -q "PreferNonstandardArtistsTag" "$f" || exit 1; grep -c "The text above is deliberately not rewritten" "$f" | awk "{if (\$1 < 3) {print \"expected >=3, got \" \$1; exit 1}} END{print \"REQUIREMENTS addenda OK\"}"'</automated>
  </verify>
  <done>
    Both addenda exist, each carrying its source citations (`session.py@v2.12.0:201-240`, `stages.py@v2.12.0:266-274`, `move.py@v2.12.0`, `musicbrainz.py@v2.12.0:341-367`, `AudioFileProber.cs@v10.11.11:227-246`), each closing with the precedent's closing sentence. The CONF-04 and CONF-06 requirement statements at lines 152-156 are byte-identical to their pre-plan state — verify with `git diff` that only the traceability rows changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct PROJECT.md's snapshot-release condition (D-32)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md — the § Constraints and § Key Decisions sections, and whichever line states that `tank/downloads@pre-phase5` "must not be destroyed before Phase 6 signs off". Locate it with `grep -n 'pre-phase5' .planning/PROJECT.md` before editing.
    - .planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md § D-32 — the locked decision and its reasoning.
    - stacks/selfhosted/arrs/beets.md lines 70-77 — the house style for a retracted claim: stated as a retraction, not a quiet replacement.
  </read_first>
  <action>
Correct the `tank/downloads@pre-phase5` release condition in `.planning/PROJECT.md`, in place, dated, with the superseded reading kept visible.

The corrected condition: the snapshot is **KEPT**, and its release moves to **after Phase 7's pilot passes**. The reasoning, which must be written down rather than left implicit: **Phase 6 writes nothing, so it produces no evidence that Phase 5's changes were correct** — only Phase 7's real import exercises them. Releasing the only undo for 4,750 renames, 751 tag writes and 26,005 chowns on the word of a paper phase, immediately before the phase that first writes at scale, is the wrong trade. Disk cost on 9 T free is negligible.

Keep the superseded sentence visible — the previous wording said "must not be destroyed before Phase 6 signs off" — and mark the correction `(corrected 2026-09-20, plan 06-02, D-32)`. Do not delete the old claim: this repo's standing style is that a retracted claim stays legible so a mechanical grep for it keeps returning a hit with its retraction attached.

Do not touch any other PROJECT.md content. In particular do not update the 144-folder backlog denominator or the `mac-music-archive` figures — those are recorded Deferred Ideas and not this phase's to move.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; f=.planning/PROJECT.md; grep -q "pre-phase5" "$f" || exit 1; grep -q "Phase 7" "$f" || exit 1; grep -qi "corrected 2026-09-20" "$f" || { echo "missing dated correction marker"; exit 1; }; git diff --numstat -- "$f" | awk "{if (\$2 > 4) {print \"unexpectedly large deletion count: \" \$2; exit 1}} END{print \"PROJECT.md correction OK\"}"'</automated>
  </verify>
  <done>
    `grep -n 'pre-phase5' .planning/PROJECT.md` shows both the superseded wording and the dated correction pointing the release at Phase 7's pilot. `git diff --numstat -- .planning/PROJECT.md` shows a small, additive change (deletions ≤ 4). No other PROJECT.md claim moved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future executor | A requirement whose named instrument cannot measure it produces a confident false pass. This is the boundary D-33 is defending. |
| git repo → public internet | Public repo; the addenda quote source line numbers and measurements, never credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Repudiation | CONF-06's instrument silently changed by rewriting the requirement text | mitigate | Addendum, not rewrite, per the TAGR-05 precedent at `REQUIREMENTS.md:276`; the closing sentence "The text above is deliberately not rewritten" is asserted by the verify command. |
| T-06-07 | Tampering | The `tank/downloads@pre-phase5` snapshot released on a paper phase's sign-off, destroying the only undo for 4,750 renames, 751 tag writes and 26,005 chowns | mitigate | D-32's dated correction in PROJECT.md moving the release to after Phase 7's pilot. |
| T-06-08 | Information Disclosure | Addendum text quoting a live measurement that includes a credential or an internal token | mitigate | The addenda quote endpoint names, option names and counts only. No API key, JWT or hostname credential appears. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and runs no package manager. |
</threat_model>

<verification>
- `git diff --stat` shows exactly two files changed.
- `git diff -- .planning/REQUIREMENTS.md | grep '^-' | grep -v '^---'` shows only the two status-cell lines being replaced — no requirement statement is deleted.
- Both task verify commands exit 0.
</verification>

<success_criteria>
- CONF-06 carries a D-33 addendum naming `beet move -p` as the oracle and `--pretend` as the incremental/grouping instrument, with citations.
- CONF-04 carries a D-34 addendum recording the `ARTISTS` write side, the `PreferNonstandardArtistsTag` decision, the rejection of `UseCustomTagDelimiters`, and that D-21's `rw` fallback is not required and must not fire.
- PROJECT.md's snapshot-release condition points at Phase 7's pilot, with the superseded wording still legible.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-02-SUMMARY.md` when done.
</output>
