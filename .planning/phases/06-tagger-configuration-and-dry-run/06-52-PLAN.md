---
phase: 06-tagger-configuration-and-dry-run
plan: 52
type: execute
wave: 5
depends_on: ["06-51"]
files_modified:
  - .planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt
  - .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
  - .planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [CONF-04, CONF-06]
gap_closure: true
round: 6

must_haves:
  truths:
    - "What is actually held is MEASURED from `zfs list -t snapshot` on atlantis, not read from a plan's prose"
    - "The question of whether anything still depends on the fence is answered with reasoning and evidence, not with 'the round is over'"
    - "The operator answers an explicit go/no-go, and `hold` is a first-class outcome the plan can reach without failing"
    - "`tank/downloads@pre-phase5` is confirmed PRESENT and is not touched on any branch — it is a different fence with a different owner"
    - "ALL THREE of the round's primary records — the register, `ROADMAP.md` and `.planning/STATE.md` — are closed with what R6-08 and R6-09 actually did; none is left claiming a pending outcome after this plan runs"
    - "The ROADMAP plan count and the `**Plans**: … waves` header are closed together, at the point the plans have actually executed, per D-R6-M4"
  artifacts:
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt"
      provides: "The measured snapshot inventory, the dependency reasoning, the operator's verbatim answer, and whatever was or was not executed"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md"
      provides: "DEF-06-52-01 recording the decision and updating DEF-06-45-01's status"
      contains: "DEF-06-52-01"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md"
      provides: "The ROUND CLOSE section: R6-08's and R6-09's actual outcomes folded into the round's primary register, with the Counts reconciliation extended to cover them"
      contains: "ROUND CLOSE"
    - path: ".planning/STATE.md"
      provides: "The round-6 block's two PENDING operator actions replaced with their real outcomes — the third record 06-50 could not close"
  key_links:
    - from: "DEF-06-45-01's stated release condition"
      to: "the operator's answer in this plan"
      via: "an explicit go/no-go presented with the measured inventory"
      pattern: "DEF-06-45-01"
    - from: "06-50's PENDING R6-08 / R6-09 entries in STATE.md"
      to: "their real outcomes"
      via: "task 3's hand edit at wave 5, after both operator plans have run"
      pattern: "R6-08"
---

<objective>
Settle `DEF-06-45-01`: whether `tank/media/Music@pre-06-41-conf04-reprobe` is released or kept —
and, as the round's last plan, close the round's three primary records with what actually happened.

Purpose: that snapshot is round 5's **only undo** for its three writes. `DEF-06-45-01` deliberately
did not bundle its release into the round's closure, "so that a later tidy-up does not read 'the
round is over' as 'the fence can go'." Its stated release condition has two clauses — the round's
outcome accepted (it now is: `negative-carry-e6`, 2026-09-24T14:30:37Z) **and** Phase 7's pilot
fence planned, so the estate is never without an undo across the boundary. The second clause is
the one this plan must actually evaluate rather than assume.

Output: one captured decision record, one `DEF-` entry, and the round's close-out across the
register, `ROADMAP.md` and `.planning/STATE.md`.

**Why this is its own plan and its own gate.** Destroying a snapshot is irreversible and
instantaneous. It is bundled with nothing — not with the git sync (plan 06-51), not with the round
record. `hold` is an outcome this plan can reach **without failing**: if the reasoning lands on
"recommend holding", the plan succeeds by recording that, and `DEF-06-45-01` stays open with a
sharper revisit condition than it had.

**The disposition word for a non-destroy outcome, stated once so it is not invented later.** The
register's disposition vocabulary is closed at four words — `FIXED` / `FIXED (undriven)` /
`ACCEPTED` / `CARRIED` — and task 3 forbids inventing a fifth. `hold`, `defer` and `06-51`'s `halt`
are all *successful* plan outcomes that leave the underlying item open with a named revisit
condition, which is exactly what `CARRIED` means. **So: `halt` → `CARRIED`, `hold` → `CARRIED`,
`defer` → `CARRIED`.** Write that mapping into the register when task 3 fills the rows. `release`
maps to `FIXED`. There is no deadlock here and no fifth word is needed.

**Scope fence.** The only `zfs` verb this plan may issue on any branch is **`zfs list`**, a read —
unless the operator explicitly answers `release`, in which case exactly one `zfs destroy` of exactly
one named snapshot is permitted. **`zfs rollback` is forbidden on every branch** and is not offered
as an option: rolling back a live shared dataset discards whatever Jellyfin, Music Assistant or the
operator has written since, and it is not an executor's command to run. `tank/downloads@pre-phase5`
is **not touched** on any branch — different fence, different owner (D-32, Phase 7 entry criterion
E4). No CONF-04 work of any kind: no re-probe, no mtime touch, no refresh, no checkbox move.

**The `HEAD --` rule, stated once and binding on every byte-identity criterion in this plan.** A
bare `git diff --exit-code <path>` compares the working tree against the **index**, not a commit.
With an edit *staged* it exits **0** — driven: `git diff --exit-code protected.txt` exits 0 while
`git diff --exit-code HEAD -- protected.txt` exits 1. This plan stages and commits while it runs.
**Every byte-identity guarantee here is written `git diff --exit-code HEAD -- <path>`.** Capture the
full base-commit hash with `git rev-parse HEAD` before the first write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: measure what is held, reason about what depends on it, then ask go/no-go</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — `DEF-06-45-01` in
      full: what the snapshot fences, that it was taken in the same remote step as the mutation,
      that the round changed 0 bytes of content so its referenced-unique space is essentially nil,
      its two-clause release condition, and the explicit note that `tank/downloads@pre-phase5` is a
      **different** fence. Also `DEF-06-45-02` (the measured negative) and `DEF-06-45-03` (the E5
      hypothesis), because both bear on whether anything still needs the undo. And `DEF-06-51-01`,
      written at wave 4, for what actually happened to R6-08.
    - `.planning/ROADMAP.md`, **Phase 7 — TWO separate blocks, and reading only one of them gives
      the wrong answer.** Locate each by its heading text, not by a line number (citations into this
      file go stale because the planner shifts it in its own commit):
        * **`**Success Criteria** (what must be TRUE):`** under `### Phase 7: Pilot — 12 Albums End
          to End` — **item 1 is where Phase 7's pilot snapshot fence is actually committed**
          ("Twelve bucket-A albums are imported inside a snapshot fence — ZFS snapshot of the Music
          dataset and a copy of `library.db` taken together — with rollback exercised at least
          once"). This is `DEF-06-45-01`'s second release clause and the crux of this decision.
          Verified present at planning time by direct read.
        * **`**Entry criteria inherited from Phase 6**`** — the E1…E12 block, for E4 (the
          `tank/downloads@pre-phase5` fence), E5 and E6. **The pilot-fence commitment is NOT in this
          block.** Read it for the inherited-criteria context, not for clause two.
      ⚠ Searching only the entry-criteria block and finding no pilot fence would produce a
      **"not planned" answer by omission**. That biases toward `hold`/`defer` rather than toward a
      destroy, so it is a safe error rather than a dangerous one — but it is still a wrong reading,
      and this plan's own rule is that a section is located by its heading text. Read both.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` § *Outstanding
      operator actions* — the sentence naming this release as a separate decision.
    - `CLAUDE.md` § *Infrastructure Warnings* and § *Storage and Permissions*, plus the project note
      that ZFS frees space asynchronously and `zfs list` can lag a large delete by ~20 s — so a
      space argument, if anyone makes one, is read correctly.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-42-conf04-reprobe-after.txt`
      SECTION J and `artifacts/06-43-conf04-verdict.txt` SECTION O — the two places the snapshot was
      last asserted PRESENT, so this task's measurement can be compared against a prior one rather
      than standing alone.
  </read_first>
  <action>
**Measure first. Reason second. Ask third. Issue no write on any branch of this task.**

Read the snapshot state from **atlantis** (`172.16.1.158`) as real root — LXC 100 is unprivileged
and has no `zfs`. Bound every remote command Linux-side with `timeout`, put `set -o pipefail` in any
piped remote string, and read the ssh return code. A non-zero return code, a timeout or a missing
capture is **COULD NOT LOOK** and is a stop, kept distinct from "nothing is wrong". (Every query in
this task has a non-empty healthy answer, so an empty result here **is** a could-not-look.)

Capture raw into `artifacts/06-52-snapshot-decision.txt` under a `MEASURED STATE` heading:
  a. `zfs list -t snapshot -o name,used,refer,creation -r tank/media/Music` — the full snapshot set
     on the dataset, so `@pre-06-41-conf04-reprobe` is seen **in its context** rather than looked up
     by name. Record every row, including any snapshot nobody expected.
  b. The same for `tank/downloads`, so `@pre-phase5` is confirmed PRESENT in the same transcript.
  c. Assert `tank/media/Music@pre-06-41-conf04-reprobe` is present using an **exact whole-line**
     match (`grep -qxF` against the name column), never a substring — a name merely *containing*
     the string must not satisfy the test. This is the assertion form 06-40 used for the same
     snapshot and it is reused deliberately.
  d. The snapshot's `used` value, stated as the real cost of holding it.
  e. `zfs list -o name,used,avail,refer tank tank/media/Music` — the pool and dataset free space, so
     a space argument is evaluated against a measurement.

Then write a `DEPENDENCY REASONING` section. This is the part that cannot be skipped, and it is
prose with evidence, not a checklist tick. Answer, each with its evidence cited by file and section:
  1. **Does anything still need this undo?** Round 5 wrote three file **mtimes** and zero bytes of
     content — `zfs diff` against this very fence returned three `M` entries, zero non-`M`, and the
     three files' content sha256 were byte-identical either side. So what the snapshot can undo is
     three timestamps. State that plainly, and state the counter-consideration honestly: an undo
     whose cost is nil is cheap to keep, and the argument for destroying it must therefore be an
     argument, not a tidy-up reflex.
  2. **Is `DEF-06-45-01`'s second release clause satisfied?** Is Phase 7's pilot fence **planned**?
     **Answer from Phase 7's `**Success Criteria**` item 1**, which is where the fence is committed,
     and quote the committing sentence verbatim in the artifact so the answer is evidence rather
     than a reading. Then state separately what the `**Entry criteria inherited from Phase 6**`
     block does and does not say about it, so a later reader can see both were consulted. If the
     fence turns out **not** to be committed anywhere, say so — `DEF-06-45-01`'s stated condition is
     then **not met**, which is a strong argument for `hold` regardless of anything else. Do not
     reach "not planned" by having searched only one heading.
  3. **Does any open item reference this snapshot as its evidence or its undo?** Check
     `DEF-06-45-02`, `DEF-06-45-03`, `06-VERIFICATION.md`'s gap record, and Phase 7 entry criteria
     E5 and E6. Name each and say whether it depends on the snapshot **existing** or only on the
     **artifacts** that were taken inside it.
  4. **What is lost if it is destroyed and something later needs it?** The three mtimes are
     unrecoverable to their pre-06-41 values. State whether that matters, and to whom.
  5. **What is lost if it is kept?** Its measured `used` value, against the pool's measured free
     space. Do not assert "negligible" — compute it.

Then present the gate with the measured state, the five answers, and a **stated recommendation**.
The recommendation is the executor's honest reading of (1)–(5), and it is allowed to be `hold`.
Do not present `release` as the default and do not frame `hold` as a failure.
  </action>
  <decision>Release `tank/media/Music@pre-06-41-conf04-reprobe`, or keep it held?</decision>
  <context>It is round 5's only undo for three file mtimes (zero bytes of content). `DEF-06-45-01`'s release condition has two clauses: the round's outcome accepted (it is — `negative-carry-e6`) AND Phase 7's pilot fence planned, so the estate is never without an undo across the boundary. Clause two is read from Phase 7's **Success Criteria item 1** and measured in this task, not assumed. `tank/downloads@pre-phase5` is a different fence and is not in scope.</context>
  <options>
    <option id="hold">
      <name>Hold — keep the snapshot, sharpen the revisit condition</name>
      <pros>Costs essentially nothing (the round wrote 0 bytes of content, so referenced-unique space is nil, and `tank` has ~9 T free). Keeps an undo across the Phase 6/7 boundary. Correct whenever Phase 7's own pilot fence is not yet planned — which is `DEF-06-45-01`'s own stated condition.</pros>
      <cons>One more standing snapshot to remember, and one more thing a future tidy-up could destroy by accident.</cons>
    </option>
    <option id="release">
      <name>Release — destroy exactly this one snapshot</name>
      <pros>Closes `DEF-06-45-01`. One fewer standing object.</pros>
      <cons>Irreversible. The three pre-06-41 mtimes become unrecoverable. Correct only if Phase 7's pilot fence is planned and nothing open depends on this snapshot existing.</cons>
    </option>
    <option id="defer">
      <name>Defer — decide at Phase 7's planning, when its own fence is on the table</name>
      <pros>Puts the decision where both clauses can be evaluated together, which is what `DEF-06-45-01` actually asks for.</pros>
      <cons>Leaves the item open through another phase boundary.</cons>
    </option>
  </options>
  <resume-signal>Select: `hold`, `release`, or `defer`. Any other answer is treated as `hold`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt && test -s "$A" && test "$(/usr/bin/grep -cF 'MEASURED STATE' "$A")" -ge 1 && test "$(/usr/bin/grep -cF 'DEPENDENCY REASONING' "$A")" -ge 1 && test "$(/usr/bin/grep -cF 'pre-phase5' "$A")" -ge 1 && test "$(/usr/bin/grep -cF 'Success Criteria' "$A")" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - The artifact records measurements (a)–(e) as captured output with the command that produced
      each, not as narration.
    - `tank/media/Music@pre-06-41-conf04-reprobe` is asserted present by **exact whole-line** match,
      and the transcript shows the assertion form used.
    - `tank/downloads@pre-phase5` is confirmed **PRESENT** in the same transcript, with a one-clause
      statement that it is a different fence and is not in this plan's scope.
    - The `DEPENDENCY REASONING` section answers all five questions, each with a cited evidence
      source (file plus section or heading, never a bare line number).
    - **Question 2's answer is read from Phase 7's `**Success Criteria** item 1`** — the block where
      the pilot snapshot fence is actually committed — and the committing sentence is quoted
      **verbatim** in the artifact. The answer states whether `DEF-06-45-01`'s second release clause
      is **met** or **not met**.
      ⚠ **The `**Entry criteria inherited from Phase 6**` block is NOT where this answer comes
      from.** It is consulted separately, and the artifact records what it does and does not say,
      for E4/E5/E6 context only. A Question-2 answer sourced from the entry-criteria block does
      **not** satisfy this criterion: the fence is not committed there, so reading only that block
      produces a "not planned" answer by omission. (The earlier form of this criterion pointed at
      the entry criteria while the action pointed at Success Criteria item 1 — the two disagreed and
      the acceptance text was the one that was wrong.)
    - Question 5 states the snapshot's measured `used` value and the pool's measured free space.
      The word "negligible" may appear only **after** the computed figures, never instead of them.
    - The executor's recommendation is stated explicitly before the gate, and the gate offers all
      three options with `hold` presented first.
    - Every remote command is bounded Linux-side with `timeout`, carries `set -o pipefail` where
      piped, and has its ssh return code recorded. Any could-not-look is a recorded stop.
    - **No write of any kind was issued in this task.** The transcript contains no `zfs destroy`,
      no `zfs rollback`, no `zfs snapshot`, no `touch`, and no HTTP verb.
    - The operator's answer is recorded **verbatim** with a UTC timestamp.
    - The artifact carries no credential, no API key and no NUL byte (`file` reports text).
  </acceptance_criteria>
  <done>The snapshot inventory is measured, the dependency question is answered with evidence read from Phase 7's Success Criteria item 1, and the operator has chosen `hold`, `release` or `defer` — with nothing yet executed.</done>
</task>

<task type="auto">
  <name>Task 2: execute the chosen branch — at most one destroy — and record the decision</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt, .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md</files>
  <read_first>
    - `artifacts/06-52-snapshot-decision.txt` as written by task 1 — the measured values and the
      operator's verbatim answer. Re-read them; do not carry numbers forward in your head.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — `DEF-06-45-01`,
      `DEF-06-51-01` (written at wave 4) and the file's established entry shape.
  </read_first>
  <action>
**On `hold` or `defer` — which are complete, successful outcomes, not failures:**
Issue **nothing** to the estate. Append `DEF-06-52-01` recording: the measured inventory with the
snapshot's `used` value and the pool's free space; the five dependency answers in condensed form;
the operator's verbatim answer and timestamp; and `DEF-06-45-01`'s new status — **still OPEN**, with
a **sharpened revisit condition** stated concretely (name the specific event that should trigger the
next look, e.g. Phase 7's pilot-fence planning step, rather than restating the original two-clause
condition verbatim). Say plainly that nothing was destroyed. Record the disposition mapping from the
objective — **`hold` → `CARRIED`, `defer` → `CARRIED`** — so task 3 has a legal word and does not
invent a fifth.

**On `release` only:**
1. **Re-assert immediately before destroying.** Re-run the exact-whole-line presence assertion from
   task 1 (c) and confirm the snapshot is still present and still the only one matching that name.
   A presence check taken at the gate and not re-taken at the act is a check taken at the wrong
   moment.
2. Issue **exactly one** `zfs destroy` naming **exactly one** snapshot in full,
   `tank/media/Music@pre-06-41-conf04-reprobe`, with **no** `-r`, **no** `-R` and **no** `-f`, and
   with no glob, wildcard or variable that could expand to more than that one name. Fence the
   command at its call site: assert the literal target string equals the expected name before
   issuing it, and refuse on any mismatch. Capture the raw output and the return code.
3. **Re-list afterwards** with the same `zfs list -t snapshot` over both `tank/media/Music` and
   `tank/downloads`, and assert: the target is **absent**; `tank/downloads@pre-phase5` is still
   **PRESENT**; and **no other snapshot disappeared** — compare the full before and after name sets
   and record any difference. Note that ZFS frees space asynchronously and `zfs list` can lag a
   large delete by ~20 s, so a space figure that has not moved yet is not evidence of failure;
   judge on the name set, not on `avail`.
4. Append `DEF-06-52-01` recording all of the above, with `DEF-06-45-01` marked **CLOSED by this
   entry**, and state in one clause what closing means: the fence is gone and the three pre-06-41
   mtimes are unrecoverable. Record the disposition mapping **`release` → `FIXED`**.

**On every branch:** do not touch `tank/downloads@pre-phase5`. Do not issue `zfs rollback`. Do not
run `quick-health-check.sh`. Do not touch `.planning/REQUIREMENTS.md`, and do not tick CONF-04.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-52-snapshot-decision.txt && D=.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md && test "$(/usr/bin/grep -c '^## DEF-06-52-01' "$D")" -eq 1 && test "$(/usr/bin/grep -cF 'DEF-06-45-01' "$D")" -ge 2 && test "$(/usr/bin/grep -cE 'zfs [r]ollback' "$A")" -eq 0 && test "$(/usr/bin/grep -cE 'zfs [d]estroy' "$A")" -le 1 && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md && git diff --exit-code HEAD -- scripts/</automated>
  </verify>
  <acceptance_criteria>
    - **The forbidden rollback verb appears nowhere in the transcript.** The artifact writes every
      mention of it — including the prohibition sentence itself — in bracketed form
      (`zfs [r]ollback`), so the detector is not an occurrence of what it measures. Assert
      `/usr/bin/grep -cE 'zfs [r]ollback'` returns **0**. Drive that recipe against a control string
      containing the real verb first and observe **1**, so a non-detecting recipe cannot pass. A raw
      `-c 'zfs rollback'` equality would be self-invalidating here and is forbidden — that is the
      `DEF-06-39-06` defect, and plan 06-38 already shipped one assertion that could never pass for
      exactly this reason.
    - The same bracketing discipline applies to the destroy verb: narrative mentions are written
      `zfs [d]estroy`, so `/usr/bin/grep -cE 'zfs [d]estroy'` counts only lines showing a command
      that was actually issued. **Drive this recipe too**, in both directions: against a control
      containing the real unbracketed verb observe **1**, and against a control containing only the
      bracketed narrative form observe **0**. Record both drives. Without them, the `0` on the
      `hold`/`defer` branch is indistinguishable from a `0` produced by a broken pattern.
    - On `hold` or `defer`: `/usr/bin/grep -cE 'zfs [d]estroy'` returns **0**, the transcript shows
      no write verb of any kind, and the summary records the outcome as a success rather than a skip.
    - On `release`: `/usr/bin/grep -cE 'zfs [d]estroy'` returns exactly **1** — one issued command,
      naming one snapshot in full, with no `-r`, `-R`, `-f`, glob or wildcard.
    - On `release`: the before/after snapshot name sets are both captured, the target is absent
      after, `tank/downloads@pre-phase5` is present after, and the set difference is exactly the one
      destroyed name — stated as a computed comparison, not asserted.
    - `deferred-items.md` gains exactly one new `## DEF-06-52-01` heading; no existing `DEF-` entry
      is renumbered, reworded or removed (heading count = base + 1, every base heading still
      present, measured against the captured **base commit**).
    - `DEF-06-52-01` names `DEF-06-45-01` and gives it an explicit status — `CLOSED` or
      `still OPEN` — and on the OPEN branch states a **sharpened** revisit condition naming a
      specific triggering event, not a restatement of the original wording.
    - `DEF-06-52-01` records the branch's **register disposition word** from the closed four-word
      set: `hold`/`defer` → `CARRIED`, `release` → `FIXED`. Task 3 reads this rather than choosing.
    - The operator's answer is quoted verbatim with its UTC timestamp in both the artifact and the
      `DEF-` entry.
    - `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` and
      `git diff --exit-code HEAD -- scripts/` both return **0**. The `HEAD --` is load-bearing.
    - The CONF-04 checkbox is still unticked: `/usr/bin/grep -c '^\- \[ \] \*\*CONF-04'
      .planning/REQUIREMENTS.md` returns **1**.
    - The artifact carries no credential, no API key and no NUL byte.
  </acceptance_criteria>
  <done>The operator's choice is executed exactly — at most one snapshot destroyed, or nothing at all — and `DEF-06-52-01` records the decision, its evidence, `DEF-06-45-01`'s resulting status and the branch's register disposition word.</done>
</task>

<task type="auto">
  <name>Task 3: close the round's THREE primary records — the register, the ROADMAP and STATE.md</name>
  <files>.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md` — written by
      plan 06-50 at wave 3. Read its **`ROUND CLOSE`** section, which 06-50 created as an explicit
      placeholder; its `R6-08` and `R6-09` mapping-table rows, which 06-50 wrote as **PENDING**
      because both plans postdate the register; and its **Counts** section, whose reconciliation
      06-50 scoped to `R6-01`…`R6-05` and named this task as the owner of its extension. You are
      filling all three.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` — `DEF-06-51-01`
      (plan 06-51's recorded outcome: proceeded or halted, why, and `DEF-06-39-05`'s conditional
      status) and `DEF-06-52-01` (this plan's, written in task 2, including the branch's register
      disposition word). **Read the entries, not this plan's expectations of them.**
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-51-host-sync.txt` — the
      six-row sha256 verdict, the untracked-debris removal block and the three-field
      container-fingerprint verdict, so R6-08's outcome is quoted from the transcript rather than
      paraphrased from a summary.
    - `.planning/ROADMAP.md` — the Phase 6 **disposition paragraph** and its round-6 block, added by
      plan 06-50; the Phase 6 **status row** and the phase-status table's **header row**; and the
      `**Plans**: 45 plans in 23 waves` header near the Phase 6 section. Locate all by
      heading/marker text, never by line number.
    - `.planning/STATE.md` — the round-6 block that plan 06-50 appended at wave 3, and specifically
      its **R6-08 and R6-09 PENDING entries** and its `Plan:` line. You are replacing those
      pending entries with the real outcomes. Read the block before editing it.
  </read_first>
  <action>
**Why this task exists, and what changed about it.** The round's two live-estate plans (06-51 at
wave 4, this one at wave 5) both execute *after* 06-50 writes the round's records at wave 3, so
without this task those records would describe R6-08 and R6-09 as planned rather than as happened.
That is a mild instance of the exact failure `06-50`'s own objective names: *a finding recorded
where the next phase does not read is a finding nobody owns.*

**The round's cross-AI review found that the earlier fix reached two of the three records and missed
the third.** The register and `ROADMAP.md` got the pending-then-close treatment; `.planning/STATE.md`
did not, and it appeared in no later plan's `files_modified`, so nothing repaired it. `STATE.md` is
what the next session reads as *position*. **This task now closes all three.**

**Under D-R6-M4, this task also closes the plan count.** The operator decided the `Plans Complete`
column means **executed**, not authored, so 06-50 wrote the measured executed count at wave 3 and
this plan — the round's last — closes it to `52/52`. The `**Plans**: … plans in … waves` header
moves with it, for the same reason, and is owned here.

**This task runs on every branch — `hold`, `defer` and `release` alike, and whether 06-51 proceeded
or halted.** Its content is determined by whatever the earlier tasks and 06-51 produced; it makes no
decision of its own and is not gated.

**(a) Close the register.** Replace `06-DISPOSITIONS-GAP4.md`'s `ROUND CLOSE` placeholder with the
two outcomes, and change the `R6-08` and `R6-09` mapping-table rows from `PENDING` to their real
dispositions, **read from `DEF-06-51-01` and `DEF-06-52-01`** and drawn from the register's own
closed four-word vocabulary (`FIXED` / `FIXED (undriven)` / `ACCEPTED` / `CARRIED`). Do not invent a
fifth word and do not choose one yourself — the mapping is already written down: **`halt` →
`CARRIED`, `hold` → `CARRIED`, `defer` → `CARRIED`, `proceed`/`release` → `FIXED`.** Record:
  - **R6-08** — whether the operator answered `proceed` or `halt`, verbatim and timestamped; the
    commit the host moved from and to (or that it did not move); the six-row sha256 verdict; the
    three-field no-redeploy verdict with the container count on each side; the two-file untracked
    debris removal and its computed set difference; and whether `DEF-06-45-05` item 2 is now CLOSED
    or still OPEN. State `DEF-06-39-05`'s status **as `DEF-06-51-01` recorded it** — `unblocked but
    not closed` on `proceed`, `still BLOCKED` on `halt`. ⚠ Do **not** write "unblocked"
    unconditionally: on a halt the stale deployed scripts that block the live run are still stale,
    and a false readiness line here is what a later executor would act on.
  - **R6-09** — the operator's answer verbatim and timestamped; whether the snapshot was destroyed,
    held or deferred; `DEF-06-45-01`'s resulting status; and, on `hold`/`defer`, the sharpened
    revisit condition.
  - **Extend the Counts reconciliation.** 06-50 scoped it to the five review findings
    (`R6-01`…`R6-05`) and explicitly named this task as the owner of its extension. Add
    `R6-08`/`R6-09`'s two dispositions to the disposition tally as a **separate, clearly labelled
    line** — round items, not review findings — and restate the totals so the section describes the
    table as it now stands. A reconciliation that is never re-run after the table grows is a check
    that stopped checking.
  - **Re-read the register's PENDING prose.** 06-50 was required to write every such sentence as a
    dated statement about the moment of writing. Confirm each one is still true now that the rows are
    filled; if any reads in the bare present tense, correct it to the dated form. This prose sits
    **outside** the `^\|` table-row anchor, so the mechanical row check below cannot catch it.
  - **If either row's disposition disagrees with what 06-50's Counts section predicted**, say so
    explicitly and re-state the reconciliation rather than smoothing it — 06-50's counts were
    written before these two plans ran, and a register that quietly re-totals is worse than one
    that shows its arithmetic moving.

**(b) Close the ROADMAP.**
  1. Append **one or two sentences** to the Phase 6 disposition paragraph's round-6 block naming
     R6-08's and R6-09's outcomes and pointing at `DEF-06-51-01` / `DEF-06-52-01` for the detail.
     This is a narrative append only. Use the paired `R6-NN (WR-NN)` form for any review-finding
     reference, as 06-50 did — `ROADMAP.md` already carries six `WR-02` occurrences with two
     referents and a bare one would be the third.
  2. **Close the plan count to `52/52`** in the Phase 6 status row, per **D-R6-M4**. All seven
     round-6 plans have now executed — a `halt` or a `hold` is an *executed* outcome, not a
     non-execution — and this edit is the round's close-out act. State in one clause, in the
     narrative cell, that the numerator was closed by this plan at wave 5 as its own last act, so
     the one-plan overlap (this plan closing while writing its own completion) is visible rather
     than silent.
  3. **Update the `**Plans**:` header**, which is this plan's under L3 and D-R6-M4. It currently
     reads `45 plans in 23 waves`. After round 6 it reads **52 plans in 28 waves** — 45 + 7 plans,
     23 + 5 waves (round 6 ran waves 1–5). **Derive both figures rather than copying them**: count
     the `06-NN-PLAN.md` files in the phase directory for the plan total, and add round 6's five
     waves to the recorded 23. Record both derivations in the plan summary. Leaving this header at
     `45 plans in 23 waves` would leave `ROADMAP.md` carrying two disagreeing counts, which is the
     defect L3 names; it was unowned until this plan, and that is why it went stale.
  4. ⛔ **The status word stays `In Progress`.** `06-VERIFICATION.md` scores this phase
     `gaps_found`, 5/6, and `REQUIREMENTS.md`'s CONF-04 box is unticked. Do not touch
     `.planning/REQUIREMENTS.md` and do not tick CONF-04. **Do not gut or rewrite the narrative
     cell** — it is a 33,000-character record and appending is the only safe operation on it.

**(c) Close `.planning/STATE.md` — the record 06-50 could not close.** Replace the round-6 block's
**PENDING** R6-08 and R6-09 entries with their real outcomes, in the same condensed form the block
already uses:
  - R6-08: the operator's verbatim answer and timestamp; proceeded or halted; on `proceed`, the
    commit the host moved from and to and the six-row sha256 verdict in one clause; the
    no-redeploy verdict; `DEF-06-45-05` item 2's CLOSED/OPEN status; and `DEF-06-39-05`'s status
    **as recorded** (conditional, per (a) above).
  - R6-09: the operator's verbatim answer and timestamp; destroyed / held / deferred;
    `DEF-06-45-01`'s resulting status.
  - Update the `Plan:` line to `52 of 52`, matching the ROADMAP status row closed in (b)(2).
  - Keep the block's existing emphatic statements intact: **re-verification has NOT been performed
    by this round**, `REQUIREMENTS.md` was not touched, the unticked CONF-04 box stands, and
    `/gsd-verify 06` is the next step and the verifier's call, not this plan's.
  - Do **not** rewrite the rest of the block, and do **not** delete any pre-existing line other than
    the two PENDING entries and the `Plan:` line being replaced.

⚠ **Hand-edit all three files. Do not use any `state.*` or `roadmap.*` SDK verb** — a
`roadmap.update-plan-progress` call in this project once wiped a 3,221-character narrative cell
while `--stat` looked correct, and four recorded `state.*` writes have orphaned multi-line fields.
After editing, run `git diff` on all three files and read the **BODY** of the diff, not `--stat`,
confirming no pre-existing line was truncated and no continuation orphaned.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && R=.planning/phases/06-tagger-configuration-and-dry-run/06-DISPOSITIONS-GAP4.md && test "$(/usr/bin/grep -cF 'ROUND CLOSE' "$R")" -ge 1 && test "$(/usr/bin/grep -cF 'DEF-06-51-01' "$R")" -ge 1 && test "$(/usr/bin/grep -cF 'DEF-06-52-01' "$R")" -ge 1 && test "$(/usr/bin/grep -cE '^\|.*R6-0[89].*PEN[D]ING' "$R")" -eq 0 && test "$(/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| 52/52 \|' .planning/ROADMAP.md)" -eq 1 && test "$(/usr/bin/grep -E '^\| 6\. Tagger Configuration and Dry Run \|' .planning/ROADMAP.md | /usr/bin/awk -F'|' '{gsub(/^[ \t]+|[ \t]+$/,"",$4); print tolower($4)}')" = "in progress" && test "$(/usr/bin/grep -cF '52 plans in 28 waves' .planning/ROADMAP.md)" -eq 1 && test "$(/usr/bin/grep -cF '45 plans in 23 waves' .planning/ROADMAP.md)" -eq 0 && test "$(/usr/bin/grep -cF '52 of 52' .planning/STATE.md)" -ge 1 && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md && git diff --exit-code HEAD -- scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `06-DISPOSITIONS-GAP4.md`'s `ROUND CLOSE` section is populated: it names R6-08 and R6-09, and
      cites `DEF-06-51-01` and `DEF-06-52-01` by name.
    - Neither the `R6-08` nor the `R6-09` mapping-table row still reads `PENDING`. **Measure this
      with the token written bracketed** so the criterion is not an occurrence of what it counts:
      `/usr/bin/grep -cE '^\|.*R6-0[89].*PEN[D]ING'` returns **0**. The `^\|` anchor restricts the
      count to table rows so prose cannot inflate it. **Drive the recipe first**: against a control
      row containing the real word it must return **1**, and only then is a **0** against the
      register evidence that the rows were filled rather than evidence that the recipe matches
      nothing. A `-eq 0` that passes because the pattern is broken is the `DEF-06-45-04` class, and
      it is not to be re-introduced while closing a round about it.
    - **No PENDING prose survives in the bare present tense.** Search the register for the real word
      **without** the `^\|` anchor and read every remaining prose occurrence: each must be a dated
      statement about wave 3, not a standing claim. The `^\|` row check above cannot see these.
    - Each of R6-08 and R6-09 carries exactly one disposition from the closed four-word set, taken
      from `DEF-06-51-01` / `DEF-06-52-01` via the stated mapping (`halt`/`hold`/`defer` →
      `CARRIED`, `proceed`/`release` → `FIXED`). No fifth word is introduced.
    - **The Counts reconciliation is extended, not left describing a subset.** The section carries a
      separate labelled line adding `R6-08`/`R6-09`'s dispositions as round items, the five-finding
      reconciliation is still shown intact, and the restated totals match the table as it now
      stands.
    - R6-08's entry records the operator's verbatim answer with a UTC timestamp, the six-row sha256
      verdict, the three-field container comparison, the two-file untracked-debris removal, and
      `DEF-06-45-05` item 2's CLOSED/OPEN status. **`DEF-06-39-05`'s status is recorded
      conditionally and matches `DEF-06-51-01`** — `unblocked but not closed` on `proceed`,
      `still BLOCKED` on `halt`. An unconditional "unblocked" fails this criterion.
    - R6-09's entry records the operator's verbatim answer with a UTC timestamp and
      `DEF-06-45-01`'s resulting status.
    - The outcomes are quoted from `DEF-06-51-01` / `DEF-06-52-01` / `artifacts/06-51-host-sync.txt`,
      not paraphrased from a plan summary. The register names its source for each figure.
    - `.planning/ROADMAP.md`'s Phase 6 disposition paragraph gains a round-6 closing sentence naming
      both outcomes and pointing at the two `DEF-` entries, with any review-finding reference in the
      paired `R6-NN (WR-NN)` form.
    - **The Phase 6 status row's plan count now reads `52/52`**, closed by this plan:
      `/usr/bin/grep -cE '^\| 6\. Tagger Configuration and Dry Run \| 52/52 \|'` returns **1**, and
      the narrative cell carries the one-clause note that this plan closed it at wave 5 as its own
      last act.
    - **The status CELL — not the whole row — is case-insensitively `in progress`.** Extract field 4
      and compare:
      `/usr/bin/grep -E '^\| 6\. Tagger Configuration and Dry Run \|' .planning/ROADMAP.md |
       /usr/bin/awk -F'|' '{gsub(/^[ \t]+|[ \t]+$/,"",$4); print tolower($4)}'`
      must equal exactly `in progress`.
      ⚠ **Scope and casing are both load-bearing.** A case-sensitive `Complete` search across the
      whole row cannot fail: the row is one 33,000+-character line containing **0** `Complete` but
      **10** case-insensitive matches in its narrative cell, all legitimate history — so a row-wide
      case-insensitive check would fail on correct content and the row-wide case-sensitive one
      passes vacuously and could never catch a status word written in caps. Field 4, lower-cased, is
      the form that can actually fail, and it does not require rewriting the narrative cell this
      plan is forbidden to gut. **Drive it** against a control row whose status cell reads
      `COMPLETE` and observe it print `complete`.
    - **The `**Plans**:` header is updated and derived, not copied.**
      `/usr/bin/grep -cF '52 plans in 28 waves'` returns **1** and
      `/usr/bin/grep -cF '45 plans in 23 waves'` returns **0**. The plan summary records how each
      figure was derived: the plan total counted from `06-NN-PLAN.md` files in the phase directory,
      the wave total as 23 + round 6's 5.
    - **`.planning/STATE.md`'s round-6 block no longer states R6-08 or R6-09 as pending.** Assert
      positively: the block contains the operator's two verbatim answers with timestamps,
      `DEF-06-51-01`, `DEF-06-52-01`, `DEF-06-45-05`, `DEF-06-45-01` and `DEF-06-39-05` with its
      conditional status. Assert negatively: the bracketed `PEN[D]ING` token's real form no longer
      appears in the round-6 block's R6-08/R6-09 entries. Read the block by eye as well — a
      paraphrased pending claim will not be caught by grep.
    - `.planning/STATE.md`'s `Plan:` line reads `52 of 52` and matches the ROADMAP status row.
    - `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` and
      `git diff --exit-code HEAD -- scripts/` both return **0**. The `HEAD --` is load-bearing.
    - `git diff` on `ROADMAP.md`, on the register and on `STATE.md` shows **zero deletions of
      pre-existing content lines** other than the `PENDING` row cells, the `ROUND CLOSE`
      placeholder, the two `STATE.md` pending entries, the `Plan:` line and the `**Plans**:` header
      figures being replaced. Read the diff **body**; a `--stat`-only check does not satisfy this
      criterion.
    - No `state.*` or `roadmap.*` SDK verb was invoked; the summary states this explicitly.
    - Measured after writing, `/usr/bin/grep -cE 'Full[R]efresh'` over the lines this task ADDED to
      any of the three files returns **0**. Drive the recipe against a control containing the real
      token first and observe **1**.
  </acceptance_criteria>
  <done>All three of the round's primary records — the register, the ROADMAP and `STATE.md` — say what R6-08 and R6-09 actually did; the plan count and the waves header are closed together at the point the plans have run; no status word or requirement checkbox moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → atlantis (`172.16.1.158`) over ssh as real root | The Proxmox host. `zfs` commands here act on pools that every service in the estate depends on. |
| a snapshot name string → `zfs destroy` | One `-r` or one glob turns a single-object destroy into a recursive one. |
| this plan → Phase 5's independent fence | `tank/downloads@pre-phase5` lives on the same host and must not be collateral. |
| the round's three primary records → every future session | `STATE.md` is read as position, `ROADMAP.md` as index, the register as the finding record. A pending entry left unclosed in any one of them is read as fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06R6-32 | Denial of service / data loss | `tank/media/Music` live dataset | mitigate | `zfs rollback` is forbidden on every branch, is not offered as an option, and its absence is asserted by a grep driven against a control containing the real verb. Rolling back a live shared dataset would discard whatever Jellyfin, MA or the operator wrote since the snapshot. |
| T-06R6-33 | Data loss | other snapshots on the pool | mitigate | Exactly one `zfs destroy`, exactly one fully-qualified name, no `-r`/`-R`/`-f`, no glob or wildcard; a literal-equality fence at the call site; and a computed before/after name-set difference proving only that one name went. The destroy-verb counter is driven in both directions so its 0 on the hold branch is evidence rather than an artefact. |
| T-06R6-34 | Data loss | `tank/downloads@pre-phase5` (Phase 5's only undo) | mitigate | Listed in the same transcript before and after, asserted PRESENT on every branch, and named in the scope fence as out of scope. |
| T-06R6-35 | Tampering | acting on a stale presence check | mitigate | The exact-whole-line presence assertion is re-taken immediately before the destroy, not carried from the gate. |
| T-06R6-36 | Repudiation | a destroy attributed to an operator who did not authorise it | mitigate | The gate is `checkpoint:decision`, blocking; the answer is recorded verbatim with a UTC timestamp in both the artifact and the `DEF-` entry; any answer outside the closed set is treated as `hold`. |
| T-06R6-37 | Denial of service | an unbounded remote command on the Proxmox host | mitigate | Every remote command bounded Linux-side with `timeout`, `set -o pipefail` in piped strings, ssh RC read; a could-not-look is a stop kept distinct from "nothing is wrong". |
| T-06R6-38 | Information disclosure | credentials in a committed transcript | mitigate | No credential on any command line; the transcript is screened for keys, tokens and NUL bytes before commit. |
| T-06R6-46 | Repudiation | a round record left claiming a pending outcome | mitigate | Task 3 closes **all three** records — register, `ROADMAP.md` and `.planning/STATE.md` — on every branch, runs ungated, and asserts both positively (real outcomes present) and negatively (the pending token gone from the STATE round-6 block and from the register's two rows). `.planning/STATE.md` is in this plan's `files_modified` for exactly this reason. |
| T-06R6-47 | Repudiation | a false readiness claim surviving a safety halt | mitigate | `DEF-06-39-05`'s status is copied from `DEF-06-51-01` conditionally — `unblocked but not closed` on `proceed`, `still BLOCKED` on `halt` — and an unconditional "unblocked" fails the acceptance criterion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install of any kind. Row retained so the absence is stated. |
</threat_model>

<verification>
- Snapshot inventory measured on atlantis, both datasets, before any decision.
- Five dependency questions answered with cited evidence; clause two of `DEF-06-45-01` evaluated
  against **Phase 7's Success Criteria item 1**, with the committing sentence quoted verbatim, and
  the entry-criteria block consulted separately for E4/E5/E6 context only.
- `zfs rollback` absent from the transcript; at most one `zfs destroy` with no recursion flag; both
  verb counters driven in both directions.
- `tank/downloads@pre-phase5` present on every branch.
- All three primary records closed: register `ROUND CLOSE` + both rows + extended Counts;
  ROADMAP narrative + `52/52` + `52 plans in 28 waves`; `STATE.md` round-6 block + `52 of 52`.
- Status cell case-insensitively `in progress`, proven by a driven field-4 extraction.
- `deferred-items.md` gains exactly one entry; `REQUIREMENTS.md` and `scripts/` byte-unchanged
  under `HEAD --`.
</verification>

<success_criteria>
- The operator made an informed go/no-go against measured state and explicit reasoning read from
  the block that actually commits the Phase 7 fence.
- `hold` and `defer` are reachable outcomes that complete the plan successfully, and each has a
  stated register disposition word.
- `DEF-06-45-01` has an explicit status with evidence, and nothing else on the pool moved.
- No primary record is left describing a round-6 outcome as pending, and none describes an outcome
  that did not happen.
</success_criteria>

<output>
Create `.planning/phases/06-tagger-configuration-and-dry-run/06-52-SUMMARY.md` when done.
</output>
