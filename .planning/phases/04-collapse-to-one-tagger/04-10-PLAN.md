---
phase: 04-collapse-to-one-tagger
plan: 10
type: execute
wave: 5
depends_on: [04-06, 04-09]
files_modified:
  - stacks/selfhosted/arrs/sabnzbd/audio.bash
  - stacks/selfhosted/arrs/sabnzbd.yaml
  - stacks/selfhosted/arrs/sabnzbd/beets-config.yaml
  - scripts/quick-health-check.sh
autonomous: true
requirements: [TAGR-04, TAGR-05]
tags: [vendoring, audio.bash, drift-check, beets-config]

must_haves:
  truths:
    - "D-08: audio.bash is vendored at stacks/selfhosted/arrs/sabnzbd/audio.bash — first committed byte-identical to the live host file (sha256 fdcddca234b2…), then stripped in a separate commit"
    - "D-10: the only difference from the live file is the removal of line 285 (the `beet -c … import -q` invocation); the SAB_PP_STATUS guard (lines 27-35) is byte-identical; no other tag-writing or file-deleting step is removed, and the inventory of the remaining ones is recorded"
    - "D-09: sabnzbd.yaml bind-mounts the appdata audio.bash at /config/scripts/audio.bash `:ro`, and the beets-config.yaml mount moves from `:rw` to `:ro` in the same change (orchestrator decision on D-09's discretion)"
    - "D-11 / TAGR-05: the vendored sabnzbd beets-config.yaml keeps its SAFE-01 keys and bind mount as a defused guard, and its plugins line reads `plugins: embedart musicbrainz`; its header no longer claims anything invokes it"
    - "D-13 / F6: quick-health-check.sh gains the FIRST vendored-file drift block — one bounded ssh comparing `git show HEAD:<repo path>` with the appdata copy host-side for all three vendored files (sabnzbd audio.bash, sabnzbd beets-config.yaml, survivor config.yaml) — fail-closed, never info(), with 'could not look' distinct from 'drift'"
    - "D-13: the drift block's first run is red on real state (repo stripped/fixed vs host not yet installed), an additive expectation override makes a green file red, and an unreachable appdata root reports UNKNOWN with exit 1"
  artifacts:
    - path: "stacks/selfhosted/arrs/sabnzbd/audio.bash"
      provides: "vendored post-processing hook minus line 285"
      min_lines: 338
    - path: "scripts/quick-health-check.sh"
      provides: "vendored-file drift block"
      contains: "DRIFT_EXPECT"
  key_links:
    - from: "scripts/quick-health-check.sh drift block"
      to: "/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash"
      via: "host-side sha256 vs git show HEAD"
      pattern: "git show"
    - from: "stacks/selfhosted/arrs/sabnzbd.yaml"
      to: "/config/scripts/audio.bash"
      via: "single-file bind :ro"
      pattern: "audio.bash:/config/scripts/audio.bash:ro"
---

<objective>
The repo half of TAGR-04/TAGR-05 on the sabnzbd path: vendor audio.bash (verbatim, then strip line 285 in
its own commit), mount both vendored files `:ro`, fix the defused config's plugins line, and build the
first vendored-file drift assertion — whose first run is red precisely because the host has not yet been
given these files (plan 04-11 installs them).

Purpose: D-08's reason — upstream `setup.bash` re-downloads audio.bash and nothing detects divergence.
Runs after 04-09 so the D-17 negative control used the live broken config first.
Output: four repo files, pushed; driven-control transcripts. No host appdata or container changes here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md
@README.md
@stacks/selfhosted/arrs/sabnzbd.yaml
@stacks/selfhosted/arrs/sabnzbd/beets-config.yaml
@scripts/quick-health-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor audio.bash verbatim, then strip line 285 in a separate commit (D-08, D-10)</name>
  <files>stacks/selfhosted/arrs/sabnzbd/audio.bash</files>
  <read_first>
    - /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash on LXC 100 (read, never edit)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § audio.bash anatomy (line map)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § NEW audio.bash (byte-identity: NO provenance header in this file)
  </read_first>
  <action>
    `scp -p root@172.16.1.159:/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash stacks/selfhosted/arrs/sabnzbd/audio.bash`. Assert `sha256sum` starts `fdcddca234b2` and `wc -l` = 339 (if the live file changed since 04-01, HALT and report — something rewrote it). Credential screen before committing (the repo is public): `grep -niE 'api_?key|token|passw|secret|bearer|://[^/ ]*:[^/ ]*@'` and inspect every hit — variable references (e.g. `$arrApiKey` sourced from extended.conf) are fine; any literal value HALTS the plan. Commit 1 with only this path: `chore(04-10): vendor arr-scripts audio.bash verbatim (sha256 fdcddca2…, D-08)`.
    Strip: assert line 285 is exactly the invocation `beet -c /config/scripts/beets-config.yaml -l /config/scripts/library.blb -d "$1" import -q "$1"` (leading tabs allowed) via `sed -n '285p'`; delete that one line only (`sed -i '' '285d'` on macOS BSD sed, or an equivalent that touches no other byte). Checks: `diff <(git show HEAD:stacks/selfhosted/arrs/sabnzbd/audio.bash) stacks/selfhosted/arrs/sabnzbd/audio.bash` shows exactly one `<` line and no `>` line; `sed -n '27,35p'` identical between the two; `bash -n`; `grep -cE '^\s*beet ' ` = 0. Record the D-10 inventory in the SUMMARY (from the anatomy table): still active under today's extended.conf — clean() (deletes non-audio files and MP3s when FLAC present), verify() (`rm -rf` on flac -t failure), detectsinglefilealbums, beets() body minus the invocation (log lines + transient beets-match), chmod at 334-335 (baseline Exit(1)); inactive — r128gain replaygain (ReplaygainTagging=false), conversion, AudioQualityMatch; residual supply-chain input — scripts_init.bash `pip install -U beets` on every boot (Phase 8). Commit 2: `fix(04-10): strip the beets invocation from audio.bash line 285 (TAGR-04, D-10)`.
  </action>
  <verify>
    <automated>f=stacks/selfhosted/arrs/sabnzbd/audio.bash; bash -n $f && test "$(grep -cE '^\s*beet ' $f)" = 0 && test "$(git diff HEAD~1 -- $f | grep -cE '^-[^-]')" = 1 && test "$(git diff HEAD~1 -- $f | grep -cE '^\+[^+]')" = 0 && test "$(wc -l < $f | tr -d ' ')" = 338</automated>
  </verify>
  <acceptance_criteria>
    - C3-a: commit 1's blob sha256 starts `fdcddca234b2`; commit 2's diff removes exactly one line (the invocation) and adds none; lines 27-35 byte-identical; `bash -n` 0; 338 lines.
    - The file has no provenance header added (first line identical to the live file's first line).
    - Credential screen hits listed with verdicts; zero literal secrets.
  </acceptance_criteria>
  <done>The hook is under version control, reviewable, and differs from upstream by exactly the one line D-10 names.</done>
</task>

<task type="auto">
  <name>Task 2: sabnzbd.yaml :ro mounts and NOTE blocks; beets-config.yaml plugins line and header (D-09, D-11, TAGR-05)</name>
  <files>stacks/selfhosted/arrs/sabnzbd.yaml, stacks/selfhosted/arrs/sabnzbd/beets-config.yaml</files>
  <read_first>
    - stacks/selfhosted/arrs/sabnzbd.yaml lines 28-100
    - stacks/selfhosted/arrs/sabnzbd/beets-config.yaml (whole file)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § sabnzbd.yaml (text that becomes false), § beets-config.yaml
  </read_first>
  <action>
    sabnzbd.yaml: after the beets-config.yaml mount line, add a NOTE block for audio.bash in the four-part house style (what/consumer — SABnzbd `[[music]] script = audio.bash`; why vendored — setup.bash re-download, D-08, provenance: RandomNinjaAtk arr-scripts audio.bash, live sha256 fdcddca2… vendored verbatim then line 285 stripped, and no header is carried in the file because byte-identity is asserted; what breaks if reverted — stock setup.bash restores the beets invocation; how much the mount protects — `:ro` blocks both the rename overwrite and in-place truncation from inside the container, the drift block in quick-health-check.sh detects host-side edits) and the D-10 inventory in one short paragraph, then `- /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash:/config/scripts/audio.bash:ro  # Vendored post-processing hook (see above)`. Change the beets-config.yaml mount's `:rw` to `:ro`. Correct the now-false text: lines 56-61 (no longer "handed to beets on EVERY music download" — nothing invokes it after the strip; it is a defused guard, D-11); lines 80-90 (`:ro` now closes the truncation half; replace the plan 01-09 claim with a pointer to the drift block by its section heading text in quick-health-check.sh — paraphrase the withdrawn claim, do not quote it); lines 93-94 (the soulbeet comparison and the "knowingly broken … Phase 4 owns it" sentence — now fixed by 04-10).
    beets-config.yaml: line 35 `plugins: embedart` → `plugins: embedart musicbrainz` (the only non-comment change). Replace the KNOWN DEFECT block (24-34) with a dated "FIXED <today> by plan 04-10 (TAGR-05)" block keeping the beets 2.4.0 explanation and noting the D-17 measurement from 04-09 (zero vs ≥1 MusicBrainz candidates). Header 3-8: nothing invokes this config since audio.bash line 285 was stripped; it is kept as a defused guard (D-11) so a stock setup.bash re-download cannot bring back a config with scrub/lastgenre/embedart auto on. Line 13: replace the soulbeet reference with the survivor's `../beets/config.yaml` (same hybrid shape). Keep `log: /config/scripts/beets.log` (keep-with-reason: D-10 scope; nothing writes it now). Re-screen for credentials (file's own rule, line 22).
    Gate: `docker compose -f stacks/selfhosted/arrs/compose.yaml config --quiet` → 0. Commit (`feat(04-10): :ro vendored mounts; beets-config declares musicbrainz (D-09, TAGR-05)`).
  </action>
  <verify>
    <automated>test "$(grep -v '^\s*#' stacks/selfhosted/arrs/sabnzbd.yaml | grep -c 'audio.bash:/config/scripts/audio.bash:ro')" = 1 && test "$(grep -v '^\s*#' stacks/selfhosted/arrs/sabnzbd.yaml | grep -c 'beets-config.yaml:/config/scripts/beets-config.yaml:ro')" = 1 && grep -cx 'plugins: embedart musicbrainz' stacks/selfhosted/arrs/sabnzbd/beets-config.yaml && docker compose -f stacks/selfhosted/arrs/compose.yaml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - Both mounts present `:ro` in non-comment lines; `grep -v '^\s*#' sabnzbd.yaml | grep -c 'beets-config.yaml:rw'` = 0.
    - `grep -c 'Plan 01-09 folds' stacks/selfhosted/arrs/sabnzbd.yaml` = 0; `grep -c 'soulbeet' stacks/selfhosted/arrs/sabnzbd.yaml stacks/selfhosted/arrs/sabnzbd/beets-config.yaml` = 0 each.
    - beets-config.yaml: `git diff` of non-comment lines shows only the plugins change; `grep -c 'auto: no'` unchanged (3).
    - compose `config --quiet` exit 0.
  </acceptance_criteria>
  <done>The compose definition makes both vendored files read-only in the container, and the defused config declares musicbrainz.</done>
</task>

<task type="auto">
  <name>Task 3: The vendored-file drift block (D-13) — push, pull, and drive it red on real state</name>
  <files>scripts/quick-health-check.sh</files>
  <read_first>
    - scripts/quick-health-check.sh lines 1-60 (EXIT-CODE notices), 172-234 (WR-10 gate; pipe rule; REMOTE_TIMEOUT), 413-437 (count site with RC branching), 466-560 (Traefik dashboard check; freeze fold-in order: empty → 124 → other RC → assert), 734-746 (failure tail)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Code Example 6
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § quick-health-check.sh (analogs A-E, P4)
  </read_first>
  <action>
    Insert after the Traefik-dashboard check (inside the WR-10 reachability gate; no second probe) a block "Vendored-file drift (D-13)". Workstation-side overrides in `${VAR:-default}` form, interpolated into the remote string the way REMOTE_TIMEOUT is: `DRIFT_APPDATA_ROOT` (default `/mnt/fast/appdata`); and three ADDITIVE expectations `DRIFT_EXPECT_AUDIO_BASH`, `DRIFT_EXPECT_SABNZBD_BEETS_CONFIG`, `DRIFT_EXPECT_SURVIVOR_BEETS_CONFIG` (default empty) — when set, the host hash must ALSO equal the override, so an override can only turn a file red, never green. One `ssh -n $SSH_OPTS root@172.16.1.159` whose remote string starts `set -o pipefail;`, `cd /mnt/fast/stacks || exit 3`, and for each pair (repo `stacks/selfhosted/arrs/sabnzbd/audio.bash` ↔ `$DRIFT_APPDATA_ROOT/arrs/sabnzbd/config/scripts/audio.bash`; repo `stacks/selfhosted/arrs/sabnzbd/beets-config.yaml` ↔ `…/arrs/sabnzbd/config/scripts/beets-config.yaml`; repo `stacks/selfhosted/arrs/beets/config.yaml` ↔ `…/arrs/beets/config/config.yaml`) computes `timeout $REMOTE_TIMEOUT git show HEAD:<repo> | sha256sum` and `timeout $REMOTE_TIMEOUT sha256sum <host>`, exiting 4/5 on failure, and prints `<label> repo=<hex> host=<hex>`. Capture `DRIFT_RC=$?` on the very next line with no local pipe. Branch in the S1 order: empty output (unless RC 124) → `⚠️  UNKNOWN`; RC 124 → UNKNOWN (timeout); any other non-zero → UNKNOWN "could not look" naming the RC; else parse exactly three lines (fewer → UNKNOWN) and for each: repo≠host or (override set and host≠override) → `❌ <label> DRIFTED — repo=<hex> host=<hex>` (and `expected-override=<hex>` when set), `EXIT_CODE=1`; all equal → `✅ vendored files match (3): audio.bash, sabnzbd beets-config.yaml, survivor config.yaml`. Every UNKNOWN sets `EXIT_CODE=1`. Never `info()`. Add a fourth header notice containing the literal `EXIT-CODE BEHAVIOUR CHANGED` stating the new fatal block and its conditions, and add "the vendored-file drift block" to the failure tail list. macOS bash 3.2 compatible; `/bin/bash -n`.
    Commit (`feat(04-10): fail-closed vendored-file drift block (D-13)`), push, `git -C /mnt/fast/stacks pull --ff-only`, record host HEAD.
    Driven controls from the workstation (transcripts in SUMMARY): (a) plain run → drift block `❌` for audio.bash AND sabnzbd beets-config.yaml (host still unstripped/broken), `✅`-equal for the survivor config (installed in 04-09), overall exit 1 — the real negative control; (b) `DRIFT_EXPECT_SURVIVOR_BEETS_CONFIG=$(printf '0%.0s' {1..64})` → the survivor line also `❌` naming both hashes and the override; (c) `DRIFT_APPDATA_ROOT=/nonexistent-04` → `⚠️  UNKNOWN` could-not-look, exit 1, and no `✅` printed for the block.
  </action>
  <verify>
    <automated>/bin/bash -n scripts/quick-health-check.sh && test "$(grep -c 'EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh)" -ge 5 && grep -v '^\s*#' scripts/quick-health-check.sh | grep -c 'DRIFT_EXPECT_AUDIO_BASH' && test "$(grep -v '^\s*#' scripts/quick-health-check.sh | awk '/Vendored-file drift/,/^fi$/' | grep -c 'info ')" = 0</automated>
  </verify>
  <acceptance_criteria>
    - C3-d: control (a) exit 1 with two `DRIFTED` lines naming audio.bash and sabnzbd beets-config.yaml with both hashes; (b) exit 1 with a third `DRIFTED` naming the override; (c) exit 1 with `UNKNOWN`.
    - The remote string begins `set -o pipefail;` and `DRIFT_RC=$?` is on the line directly after the ssh assignment.
    - `grep -c 'EXIT-CODE BEHAVIOUR CHANGED'` increased by exactly 1; failure tail mentions the drift block.
  </acceptance_criteria>
  <done>A divergence between any vendored file and its runtime copy now fails the routine health check, and the block has been seen to fail for real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party upstream script → public repo | vendoring a script that sources credential-bearing config |
| repo ↔ runtime copy | two copies of an executable hook that post-processes every download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Information disclosure | audio.bash in public repo | mitigate | credential screen before commit 1; HALT on any literal secret |
| T-04-10-02 | Tampering | silent revert by setup.bash / in-container write | mitigate | `:ro` single-file binds (rename and truncation both blocked); fail-closed drift block host-side |
| T-04-10-03 | Tampering | failed downloads post-processed | mitigate | SAB_PP_STATUS guard lines 27-35 asserted byte-identical |
| T-04-10-04 | Repudiation | drift false-green | mitigate | "could not look" ≠ drift; additive overrides only; three driven controls |
| T-04-10-05 | Tampering (supply chain) | `pip install -U beets` at boot | accept | recorded residual (Phase 8 owns the hook's shape); nothing invokes beets after the strip |
</threat_model>

<verification>
Two-commit vendoring with a one-line diff; compose validates; drift block red on real state and on both controls.
</verification>

<success_criteria>
TAGR-04's strip and TAGR-05's sabnzbd half exist in git, and divergence from the host is detected, not assumed away.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-10-SUMMARY.md` when done.
</output>
