---
phase: 04-collapse-to-one-tagger
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/normalise-dj-tags.py
autonomous: true
requirements: [TAGR-04]
tags: [mutagen, wav, id3, self-test, carry-in]

must_haves:
  truths:
    - "D-23: normalise-dj-tags.py writes the album field to a WAV through mutagen.wave.WAVE + add_tags() + tags.setall, never mutagen.File(easy=True), and reads it back through tags.getall"
    - "D-23: `--self-test` builds three synthetic WAVs (untagged, ASCII-tagged, non-ASCII-tagged) and asserts the written album reads back with APIC, TDRC, TIT2, TPE1 and TRCK byte-equal before and after — exit 0"
    - "D-23: the self-test is capable of failing — run against the pre-fix write path it exits 1 (RED), and against a scratch copy with the WAV predicate disabled it exits 1"
    - "D-23: when a RIFF LIST/INFO IPRD value disagrees with the new album, the dry-run and apply NDJSON record carries an `info_iprd` field — the two-container disagreement is a recorded finding, and only ID3 is written"
    - "FLAC and MP3 behaviour is unchanged: the WAV branch is dispatched by one predicate and every other format keeps its existing path"
  artifacts:
    - path: "scripts/normalise-dj-tags.py"
      provides: "WAV ID3 read/write branch, RIFF INFO reader, --self-test"
      contains: "def is_wave"
  key_links:
    - from: "read_tags()/write_tags()"
      to: "mutagen.wave.WAVE"
      via: "is_wave(path) dispatch"
      pattern: "mutagen\\.wave\\.WAVE"
    - from: "NDJSON record builder"
      to: "RIFF INFO IPRD reader"
      via: "info_iprd field"
      pattern: "info_iprd"
---

<objective>
Repair DEF-03-09 per D-23: the tool writes 0 of 134 WAV files because easy mode yields a raw
frame-keyed ID3 on WAVE. Fix the write and read paths with mutagen's WAVE API, write ID3 only, surface
the stale RIFF `IPRD` as a recorded NDJSON finding, and add the three-case regression test as a
`--self-test` mode (the estate's convention; there is no pytest in this repo).

Purpose: the named normalisation tool (Phase 3 D-13) must be able to write every audio format the estate
holds. Independent of every other plan in this phase.
Output: patched `scripts/normalise-dj-tags.py`; RED and GREEN transcripts from inside
`lscr.io/linuxserver/beets:2.13.1-ls349` (the only place mutagen 1.48.1 exists).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/03-tagger-spike/deferred-items.md
@scripts/normalise-dj-tags.py
</context>

<conventions>
- Runs happen on LXC 100 from `/mnt/fast/stacks` after `git pull --ff-only`, inside the resident image:
  `docker run --rm --pull never --network none --entrypoint python3 -v /mnt/fast/stacks/scripts:/w:ro lscr.io/linuxserver/beets:2.13.1-ls349 /w/normalise-dj-tags.py --self-test`.
  `--pull never` is mandatory: if the image is absent, the run must refuse, never pull.
- Delivery: stage `scripts/normalise-dj-tags.py` only; `git push origin main`; on LXC 100
  `git -C /mnt/fast/stacks pull --ff-only` (halt and report if it is not a fast-forward; never stash or
  reset the host checkout); record host `rev-parse --short HEAD` = local.
- Scratch on LXC 100 lives under `/mnt/fast/scratch-04/02/` (never `/tmp`, never inside the repo);
  delete it at the end and assert it is gone.
- Never relax `assert_inside_scratch` / `resolve_target_or_die` for the real run. The self-test writes
  only inside its own `tempfile.mkdtemp()` directory.
</conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): add --self-test with three synthetic WAV cases; prove it fails on today's write path</name>
  <files>scripts/normalise-dj-tags.py</files>
  <read_first>
    - scripts/normalise-dj-tags.py (whole file; especially read_tags() ~475-511, write_tags() ~611-635, id3v2_frame_ids() ~440-472, assert_frame_set_unchanged() ~562-596, parse_args() ~659, NDJSON writer ~949-982, FIELD_FRAME ~193, SCRATCH_ROOT ~186)
    - scripts/spike03-wrtag-arms.sh lines 389-418 (the repo's only self-test: table-driven ok/bad, fails counter, reason per row)
    - .planning/phases/03-tagger-spike/deferred-items.md DEF-03-09
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Code Example 7
  </read_first>
  <behavior>
    - Case untagged: stdlib `wave` writes ~0.1 s silent 16-bit mono PCM; no ID3. Writing album="Self Test Album" then reading back returns "Self Test Album".
    - Case ASCII: same PCM + ID3 via WAVE.add_tags() with TIT2, TPE1, TRCK, TDRC, APIC (a few bytes of PNG header as data) and TALB="Old Album". After writing album, TALB reads back new; the five other frames are byte-equal before/after.
    - Case non-ASCII: as ASCII but TPE1 contains non-ASCII (e.g. "Beyoncé Knowles – Ñ") and the file carries a hand-written RIFF LIST/INFO chunk with IPRD="Old Album". After writing, TALB is new, five frames byte-equal, the RIFF IPRD is still "Old Album", and the NDJSON record builder emits `info_iprd` = "Old Album".
    - Self-test prints one ok/bad line per case with a reason and exits 1 if any case fails, 0 if all pass.
  </behavior>
  <action>
    Add a `--self-test` flag handled BEFORE the required positional `target` is enforced (argparse: make `target` `nargs="?"` and error explicitly when absent and `--self-test` is not given, preserving today's exit 2 for a missing target). The self-test builds its three WAVs in `tempfile.mkdtemp()`, calls the SAME `read_tags()` / `write_tags()` / NDJSON-record-building function the real run uses (factor the record dict construction at ~967-982 into a helper, e.g. `build_record(...)`, if it is inline, so the self-test exercises the real code path), and compares frames by serialised bytes (for each frame id in APIC, TDRC, TIT2, TPE1, TRCK: the list of `frame._pprint()`-independent byte payloads — use `tags.getall(fid)` and compare `[f.HashKey for f]` plus APIC `.data`, TPE1/TIT2/TRCK/TDRC `.text`). Add a small RIFF chunk walker `read_riff_info(path) -> dict` (~20 lines of `struct` over `RIFF`/`WAVE`, descending into `LIST` chunks of type `INFO`, returning 4-char keys → decoded text; do not import the `chunk` module) and a writer used ONLY by the self-test to inject `LIST/INFO/IPRD` into the non-ASCII case. Do not change the write path yet. Commit (`test(04-02): add WAV self-test (RED)`), push, host pull, then run the self-test in the image per conventions and capture the transcript: it MUST exit 1 with the ASCII and non-ASCII cases failing on the current easy-mode path (TypeError "not a Frame instance" or album not read back). Record the transcript in the SUMMARY — this RED run is the driven negative control.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/normalise-dj-tags.py && grep -c -- '--self-test' scripts/normalise-dj-tags.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile scripts/normalise-dj-tags.py` exits 0.
    - `grep -v '^\s*#' scripts/normalise-dj-tags.py | grep -c 'def read_riff_info'` = 1.
    - Running the script with no arguments still exits 2 (argparse error), measured in the image.
    - The in-image `--self-test` run at this commit exits 1 and prints at least one `bad` line naming a WAV case; transcript in SUMMARY with the host HEAD SHA it ran against.
  </acceptance_criteria>
  <done>A regression test exists that is proven to fail on the defect it guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): WAV branch via mutagen.wave.WAVE, info_iprd finding, second negative control</name>
  <files>scripts/normalise-dj-tags.py</files>
  <read_first>
    - scripts/normalise-dj-tags.py (as committed by Task 1)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § scripts/normalise-dj-tags.py (the WAV backstop blind spot)
  </read_first>
  <behavior>
    - All three self-test cases pass: exit 0.
    - A scratch copy with `is_wave` forced to return False exits 1 (the WAV case falls back to easy mode).
    - MP3 and FLAC code paths are byte-for-byte unchanged in behaviour (no edit inside their branches beyond the dispatch).
  </behavior>
  <action>
    Add `is_wave(path)` (suffix `.wav`, case-insensitive) and dispatch on it at the top of `read_tags()` and `write_tags()`, ahead of the existing MP3 branch and the easy-mode fallback. WAV read: `w = mutagen.wave.WAVE(path)`; if `w.tags is None`, values are all None (do not add tags on read); per field use `w.tags.getall(FIELD_FRAME[field])` with the same empty/whitespace normalisation the MP3 branch uses. WAV write: `add_tags()` when `w.tags is None`; per field reuse the MP3 branch's encoding-selection logic (reuse existing frame encoding if the new value encodes, else encoding 1) and `w.tags.setall(frame_id, [getattr(mutagen.id3, frame_id)(encoding=..., text=[new])])`; `w.save()`. Write ID3 ONLY — never modify the RIFF INFO chunk (D-23). Because `assert_frame_set_unchanged()` reads ID3 only at file offset 0 and silently returns for WAV, add a WAV-specific backstop: capture the ID3 frame-id set via `WAVE(path).tags.keys()` before and after the write and fail the file (into the existing `.failed` ledger shape: path, stage, exception, detail) if any frame id other than the written ones appeared or vanished. In the NDJSON record, when `is_wave(path)` and `read_riff_info(path).get("IPRD")` is non-empty and differs from the new album value, add `"info_iprd": <that value>` (both dry-run and apply). Update the module docstring/usage text to state WAV support and the ID3-only decision with the reason (both consumers read ID3; IPRD left stale and reported). Commit (`fix(04-02): write WAV album via mutagen.wave (DEF-03-09, D-23)`), push, host pull, run the self-test in the image → exit 0. Then the second negative control: on LXC 100 copy the script to `/mnt/fast/scratch-04/02/`, edit only the copy so `is_wave` returns False, run the self-test from that copy (mount the scratch dir instead of scripts/) → exit 1. Delete `/mnt/fast/scratch-04/02/` and assert it is gone. Record all three transcripts (RED from Task 1, GREEN, disabled-predicate) with the host HEAD they ran against.
  </action>
  <verify>
    <automated>timeout 180 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; cd /mnt/fast/stacks && timeout 150 docker run --rm --pull never --network none --entrypoint python3 -v /mnt/fast/stacks/scripts:/w:ro lscr.io/linuxserver/beets:2.13.1-ls349 /w/normalise-dj-tags.py --self-test"</automated>
  </verify>
  <acceptance_criteria>
    - In-image `--self-test` at the pushed HEAD exits 0 with three `ok` lines.
    - Disabled-predicate scratch copy exits 1 with a `bad` line for at least the ASCII case.
    - `grep -v '^\s*#' scripts/normalise-dj-tags.py | grep -c 'mutagen.wave.WAVE'` ≥ 1 and `grep -c '"info_iprd"'` ≥ 1.
    - `git diff <Task1 commit> HEAD -- scripts/normalise-dj-tags.py` shows no removed line inside the MP3 branch's frame-writing loop (the existing `handle.setall(frame_id, ...)` line is unchanged).
    - `/mnt/fast/scratch-04/02` does not exist after the task (`test ! -e` over ssh returns 0).
  </acceptance_criteria>
  <done>The tool writes WAV albums as ID3, preserves every other frame, and reports rather than hides the stale IPRD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo script → container | the script runs as root inside a throwaway container with the scripts dir mounted read-only |
| tag writer → audio files | a future real run rewrites user audio; the self-test only touches its own tempdir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | write_tags() WAV branch | mitigate | WAV frame-id before/after backstop fails the file into the `.failed` ledger; five-frame byte equality proven by self-test |
| T-04-02-02 | Tampering | self-test file writes | mitigate | writes confined to `tempfile.mkdtemp()`; scratch fence for the real run left unrelaxed |
| T-04-02-03 | Tampering (supply chain) | container image | mitigate | `--pull never` on the resident `2.13.1-ls349`; `--network none`; scripts mounted `:ro` |
| T-04-02-04 | Repudiation | IPRD/ID3 divergence | mitigate | `info_iprd` NDJSON field makes the disagreement a recorded finding |
</threat_model>

<verification>
RED transcript (exit 1) → GREEN transcript (exit 0) → disabled-predicate transcript (exit 1), each with host HEAD.
</verification>

<success_criteria>
DEF-03-09 closed by behaviour: the three-case self-test passes in the beets image and is proven able to fail.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-02-SUMMARY.md` when done.
</output>
