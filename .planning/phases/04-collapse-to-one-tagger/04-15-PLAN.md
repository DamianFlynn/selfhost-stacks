---
phase: 04-collapse-to-one-tagger
plan: 15
type: execute
wave: 10
depends_on: [04-14]
files_modified:
  - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
autonomous: false
gap_closure: true
requirements: [TAGR-04]
tags: [e2e, sabnzbd, criterion-3, d-12, d-31, human-checkpoint, gap-closure]

must_haves:
  truths:
    - "D-12: one real SABnzbd music job completing after the stripped audio.bash went live is observed end to end from a stamp set BEFORE it, and its audio bytes are captured while the job is still under /mnt/tank/downloads/incomplete — structurally before SABnzbd's move and therefore before the post-processing hook"
    - "Every file present in both the PRE-HOOK and COMPLETION snapshots is byte-identical by sha256, or the plan records FAIL naming the file position and the condition violated"
    - "D-31: the two pre-declared residual Audio.txt lines appear, no SUCCESS: Matched with beets line appears, no library.blb, no library.blb backup, no beets.log and no unexpected .bak is created under sabnzbd /config, and the job folder is left in complete/nzb/music"
    - "The window prerequisite holds at both ends: ReplaygainTagging=\"false\" is present and extended.conf's sha256 and mtime are unchanged from window open to window close"
    - "If no music job arrives in the window, or the job yields no valid PRE-HOOK snapshot under the amended section 1 item 4, criterion 3 is recorded OPEN — not FAIL and not passed on the strength of the side-effect evidence"
  artifacts:
    - path: ".planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md"
      provides: "section 7, the window-2 result, with exactly one machine-readable window-2 verdict line"
      contains: "Verdict (window 2)"
  key_links:
    - from: "d12b.stamp on LXC 100"
      to: "every find -newer side-effect read"
      via: "a single stamp for the whole window"
      pattern: "newer"
    - from: "pre.sha256 taken under incomplete/"
      to: "post.sha256 taken at the history row"
      via: "judge-d12b.sh sha256 set comparison, attributed through history.path and history.storage"
      pattern: "BYTES-OK|FAIL|UNPROVEN"
---

<objective>
Re-run criterion 3's behavioural half on one real music job, with the instrument that plan 04-14 fixed
and drove to all three of its outcomes.

This is the second observation window. The first (04-12) produced two real jobs whose every
side-effect signal was clean — 0 `library.blb`, 0 `.bak`, 0 `beets.log`, 0 `SUCCESS: Matched with
beets`, 25/25 files byte-identical, `extended.conf` unchanged, 0 `MUSICBRAINZ_*` keys — but neither
job had a valid PRE-HOOK snapshot, so the one condition that had to be proven by bytes was UNPROVEN
and § 5 mandated OPEN.

Purpose: the estate side of this criterion is proven twice over and needs nothing. Only the
measurement changes. The verdict is an **outcome, not a goal** — this plan is written so it can land
on PASS, on FAIL, or on OPEN, and § 5's pre-declared rules decide which. Nothing in it is satisfiable
only by a PASS.
Output: § 7 of `04-D12-EVIDENCE.md`, holding exactly one `Verdict (window 2):` line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-14-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-12-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-12-PLAN.md
</context>

<conventions>
- Read-only over the estate: `stat`, `sha256sum`, `ffprobe`, `find`, `ls`, and
  `sqlite3 "file:…?mode=ro"`. Writes go only to `/mnt/fast/scratch-04/15/`. Never `/tmp` on LXC 100.
- No SABnzbd or Lidarr API call from the agent, and no API key on argv. The job is the operator's act
  in the Lidarr UI, or an organic arrival.
- This repo is public. The job is identified in every tracked file by the first 12 characters of the
  sha256 of its folder basename. `script_line` is quoted truncated at 60 characters, which stops before
  the release name; `storage` is quoted as the `/downloads/complete/nzb/music/` prefix only. `Verified
  Track:` lines in `Audio.txt` carry track names and are never quoted.
- **Timezones** (`04-D12-EVIDENCE.md` § 5, measured): SAB `completed` is epoch **UTC**;
  `sabnzbd.log`, `Audio.txt` and `beets.log` headers are container-local **Europe/Dublin, UTC+1 in
  September**. Convert one side and state which. `Audio.txt` resolves only to the second.
- `Audio.txt` is at `/mnt/fast/appdata/arrs/sabnzbd/config/logs/Audio.txt` — under `config/logs/`,
  **not** `config/scripts/`.
- `grep -cx 'ReplaygainTagging="false"'` returns **0 on the correct outcome**, because line 27 of
  `extended.conf` carries a trailing comment. Use the anchored form `grep -c '^ReplaygainTagging="false"'`
  (04-12 deviation 1). Never edit `extended.conf` to satisfy a grep — its sha256 and mtime must be
  unchanged, and that is itself an assertion.
- Do not re-derive the ReplayGain question. `Audio.txt` logs `Replaygain Tagging: ENABLED` from a
  hardcoded literal at `audio.bash:86`; the real gate is line 325, a case-sensitive `= TRUE` that
  `"false"` cannot satisfy (04-12 deviation 3). It is not a prerequisite failure.
- Capture each producer's exit status **before** reading its output. A non-zero status makes that item
  UNKNOWN, never "absent".
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Arm window 2 — assert the contract was amended first, stamp, pre-state, extended.conf open side, launch the sha256-verified watcher</name>
  <files>(host only: /mnt/fast/scratch-04/15/)</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-14-SUMMARY.md — the two script sha256 values, the four-control results, and the design table. The hashes asserted here come from this file.
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md — §§ 1, 3 and 5 including plan 04-14's two amendments; § 2's pre-declared residual lines and the `Audio.txt` baseline counts.
    - .planning/phases/04-collapse-to-one-tagger/04-12-PLAN.md Task 1 — the arming sequence being repeated, including the pre-state values recorded at window open.
    - /mnt/fast/scratch-04/watch-d12b.sh — the instrument about to be launched, as it actually exists on the host.
  </read_first>
  <action>
    Assert, before anything else, that the contract amendment landed **before** this window: the commit
    touching `04-D12-EVIDENCE.md` with message `docs(04-14): amend the D-12 PRE-HOOK definition before the
    second window` is an ancestor of `HEAD`, and its committer date is earlier than the stamp created
    below. Record both timestamps. If the amendment is not present, stop — the window would be judged by
    a contract that could still be edited to fit it.

    Assert the estate side is still what 04-11 left, read-only: `cmp` of
    `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` against the repo's vendored
    `stacks/selfhosted/arrs/sabnzbd/audio.bash` returns 0, and
    `grep -cE '^[[:space:]]*beet ' /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` returns 0.
    **Do not edit `audio.bash` or any estate file in this plan.** The estate is already correct; the gap
    is in the instrument.

    Assert the instrument is the one that was proven: `sha256sum /mnt/fast/scratch-04/watch-d12b.sh` and
    `/mnt/fast/scratch-04/judge-d12b.sh` equal the values recorded in `04-14-SUMMARY.md`. A mismatch stops
    the plan — the self-tested instrument and the running instrument must be the same bytes.

    Create the window: `mkdir -p /mnt/fast/scratch-04/15 && touch /mnt/fast/scratch-04/15/d12b.stamp`;
    record its UTC mtime, its epoch and its inode.

    Record the pre-state, each with its producer's exit status captured first: the listing of
    `/mnt/fast/appdata/arrs/sabnzbd/config/scripts` (name, size, mtime); the count and listing of
    `find /mnt/fast/appdata/arrs/sabnzbd/config -name '*.bak'` (expected: exactly one,
    `/mnt/fast/appdata/arrs/sabnzbd/config/sabnzbd.ini.bak`, 9,837 B, 2026-08-17 — the § 1 item 3
    pre-declared exception); the newest `category='music'` row in
    `/mnt/fast/appdata/arrs/sabnzbd/config/admin/history1.db` read with
    `sqlite3 "file:…?mode=ro"` (completed epoch, status, `script_line` truncated at 60 characters); the
    total music row count; and the byte size, line count and the three residual-line counts of
    `Audio.txt` — `tracks with Beets`, `Unable to match using beets to a musicbrainz release`, and
    `SUCCESS: Matched with beets`. Re-measure these counts rather than carrying 04-12's closing values
    (100 / 89 / 0) forward; quote both the re-measured value and 04-12's, and note any difference.

    Assert the § 3 window prerequisite, open side: `grep -c '^ReplaygainTagging="false"'` on
    `/mnt/fast/appdata/arrs/sabnzbd/config/extended.conf` = 1, and record its sha256, mtime and size.
    Compare with the § 3 baseline — sha256 `54c5433bba38c61e372425c34cc0c8aad2001922ca1fb6bd55e98dc1ada1b49d`,
    mtime `2025-10-24T08:41:54Z`, size 2,955 B. A difference is recorded, and the line must still read false.

    Also record the pre-state of the two watched trees: the count of directory entries under
    `/mnt/tank/downloads/incomplete` (04-12 recorded 6 stale January orphan directories there — they must
    land in `START_INCOMPLETE` and never be sampled) and under
    `/mnt/tank/downloads/complete/nzb/music`.

    Launch the watcher bounded to 7 days, detached, with the stamp's epoch:
    `cd /mnt/fast/scratch-04/15 && STAMP_EPOCH=<epoch> OUT=/mnt/fast/scratch-04/15/watch nohup setsid
    timeout 604800 bash /mnt/fast/scratch-04/watch-d12b.sh > watch.out 2>&1 < /dev/null &`. Then confirm
    liveness from the PID the **script itself** wrote to `$OUT/watch.pid`, not from the launcher's `$!`
    (`setsid` may fork — 04-12 recorded that defence working). Confirm `START_INCOMPLETE` and
    `START_MUSIC` were written and that their entry counts match the pre-state counts just recorded.

    Run `bash scripts/quick-health-check.sh` from the workstation → exit 0, so the window opens on a green
    estate. The sole expected `❌` is the report-only `Traefik dashboard: Not accessible` entry in the
    04-01 ROUTINE BASELINE; the census counters and the `✅ vendored files match (3)` drift line must be
    present.
  </action>
  <verify>
    <automated>ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; timeout 60 bash -c 'test -f /mnt/fast/scratch-04/15/d12b.stamp && test -f /mnt/fast/scratch-04/15/watch/watch.pid && kill -0 \$(cat /mnt/fast/scratch-04/15/watch/watch.pid) && test \$(grep -c \"^ReplaygainTagging=\\\"false\\\"\" /mnt/fast/appdata/arrs/sabnzbd/config/extended.conf) = 1 && test \$(grep -cE \"^[[:space:]]*beet \" /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash) = 0 && test -s /mnt/fast/scratch-04/15/watch/START_INCOMPLETE -o -f /mnt/fast/scratch-04/15/watch/START_INCOMPLETE && echo ARMED'"</automated>
  </verify>
  <acceptance_criteria>
    - The 04-14 amendment commit is an ancestor of HEAD and its committer date is earlier than the stamp mtime; both values are quoted in the SUMMARY.
    - Both script sha256 values equal those recorded in `04-14-SUMMARY.md`, quoted side by side.
    - `cmp` of appdata `audio.bash` against the repo copy returns 0, and `grep -cE '^[[:space:]]*beet '` returns 0. No estate file was modified by this task.
    - The stamp exists; its UTC time, epoch and inode are recorded, along with all five pre-state values and both watched-tree entry counts.
    - `extended.conf` open side: `grep -c '^ReplaygainTagging="false"'` = 1, with sha256, mtime and size recorded and compared against the § 3 baseline.
    - The watcher's self-recorded PID is alive; `START_INCOMPLETE` and `START_MUSIC` exist and their entry counts match the pre-state counts.
    - `quick-health-check.sh` exit 0 recorded, with the census counters and the drift line quoted.
  </acceptance_criteria>
  <done>Every side effect after this point is attributable to a job after the stamp, and the next music job's audio bytes will be captured while it is still in the incomplete tree.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator triggers one music download, or closes the window</name>
  <what-built>The stripped `audio.bash` is live and unchanged (04-10, 04-11), both standing guards are green, the D-12 contract has been amended and committed ahead of this window (04-14), the fixed watcher has been driven to PASS, FAIL and UNPROVEN on synthetic fixtures (04-14 Task 2), and window 2 is armed with a stamp and a running read-only watcher (Task 1).</what-built>
  <how-to-verify>
    1. In Lidarr, run a manual search for one monitored album that is not yet downloaded, so Lidarr hands it to SABnzbd in the `music` category. Alternatively do nothing and wait for the next organic music job — they arrive roughly every 1-5 days, and the watcher runs for up to 7 days.
    2. When SABnzbd's History shows that music job has finished post-processing (any status), reply with its approximate finish time.
    3. If you decide to stop waiting before any music job arrives, reply "window closed". Criterion 3 is then recorded **OPEN** — the instrument could not look, which is not the same as the estate being dirty, and it is explicitly not a FAIL.
    4. One job is enough. Unlike window 1, the PRE-HOOK snapshot is taken minutes before the hook rather than racing it, so a short job is no longer a problem.
  </how-to-verify>
  <resume-signal>Reply "job done ~HH:MM" or "window closed"</resume-signal>
  <files>(none)</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md — § 5's window rules, including the amended definition of an invalid PRE-HOOK snapshot
  </read_first>
  <action>
    Present the four points above and wait. Do not call the SABnzbd or Lidarr API from the agent: the
    trigger is the operator's act in the Lidarr UI or an organic job, and SABnzbd is never scripted with
    an API key on argv. Do not touch the stamp, the watcher or any estate file while waiting. Record the
    operator's reply **verbatim**, uncorrected, in the SUMMARY — 04-12 set that precedent and it is worth
    keeping.
  </action>
  <verify>
    <human-check>Operator reports a music job has finished post-processing in SABnzbd History with an approximate time, or reports the window closed.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator's reply is recorded verbatim in the SUMMARY, with its Europe/Dublin-to-UTC conversion stated separately rather than silently applied.
  </acceptance_criteria>
  <done>A music job has completed after the stamp, or the operator has closed the window and criterion 3 will be recorded OPEN.</done>
</task>

<task type="auto">
  <name>Task 3: Collect window 2, judge the bytes, assert the close, write section 7 with one verdict</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md — §§ 1-6 including both 04-14 amendments, and § 6's existing single `Verdict: OPEN` line, which must survive this task byte-identical.
    - .planning/phases/04-collapse-to-one-tagger/04-12-PLAN.md Task 3 — the collection sequence being repeated: the eight numbered reads, the producer-status-first rule, and the no-mtime-provenance rule.
    - .planning/phases/04-collapse-to-one-tagger/04-12-SUMMARY.md — the six deviations, each of which is a trap this task can fall into again.
    - Task 1's recorded pre-state values, `extended.conf` open values, and the stamp epoch.
  </read_first>
  <action>
    Stop the watcher first: `kill -TERM $(cat /mnt/fast/scratch-04/15/watch/watch.pid)`, then `kill -0`
    must fail. Record whether it had already exited on its own, the tail of `watch.out`, and the byte
    counts of `watch.out` and `watch/watch.err` — quote the byte counts, because an empty error log and an
    unreadable one look identical.

    **If the reply was "window closed":** still assert the `extended.conf` close side below, then write § 7
    with `Verdict (window 2): OPEN — no music job arrived between <stamp UTC> and <close UTC>`, and skip to
    cleanup. This is not a failure of the estate and must not be written as one.

    Otherwise, on LXC 100, read-only, capturing each producer's exit status before reading its output (a
    non-zero status makes that item UNKNOWN, never "absent"):

    1. SAB history rows with `category='music'` and `completed` after the stamp epoch: completed UTC,
       status, and `substr(script_line,1,60)`. The expected shape is the F8 baseline — `Completed` with
       `script_line` beginning `Exit(1): chmod: changing permissions of '/downloads/complete`. That is
       **not** a regression (`audio.bash:334` runs `chmod 777` which fails EPERM on `tank` under the global
       `set -e`), and per D-31 it is not fixed here.
    2. `find /mnt/fast/appdata/arrs/sabnzbd/config/scripts -newer /mnt/fast/scratch-04/15/d12b.stamp -printf '%p\n'`
       with RC 0, plus a second name-targeted `find` for `library.blb*`, `beets.log` and `beets-match`, plus
       `test ! -e /mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-match`. None of those names may appear.
       `audio.bash` must be visible in the same directory listing as the positive control, so an empty
       result is a reading rather than a failure to look. The `scripts` directory entry itself appearing is
       the pre-declared § 2 row 3 — the transient `beets-match` created at line 281 and removed at 298-301.
    3. `find /mnt/fast/appdata/arrs/sabnzbd/config -name '*.bak' -newer <stamp>` with RC 0 → empty. Positive
       control: the same `find` without `-newer` returns exactly `sabnzbd.ini.bak`, the § 1 item 3
       pre-declared exception. Any other new `.bak` fails item 3; a newer `sabnzbd.ini.bak` is recorded by
       name and attributed to a SABnzbd settings save, and does not.
    4. New `Audio.txt` lines since Task 1's line count. The job must add exactly the two pre-declared D-31
       residual lines — `Matching N tracks with Beets` and `ERROR: Unable to match using beets to a
       musicbrainz release` — and must add **zero** `SUCCESS: Matched with beets`. Quote the delta, not the
       totals. Record the job's `Matching` line time and convert it from Europe/Dublin to UTC, stating the
       direction of the conversion.
    5. The job folder still exists under `/mnt/tank/downloads/complete/nzb/music/`, i.e. it was left in place.
    6. **Untagged, by bytes.** Run `/mnt/fast/scratch-04/judge-d12b.sh` against
       `/mnt/fast/scratch-04/15/watch`. Record per job: the attribution token, `pre.time`, `vanished.time`,
       the destination folder's `firstseen.time`, `post.time`, the history row's completed time, and the
       counts `files_in_both`, `byte_identical`, `changed`, `pre_only`, `completion_only`, `moved`, with the
       judge's status token. Validity under the amended § 1 item 4 requires an attributed `pre.sha256` with
       `pre.time` earlier than `vanished.time`; state that ordering with both values. Then state the
       structural argument in one sentence — the snapshot was taken while the folder was still at the
       incomplete path, SABnzbd cannot invoke the hook before moving it, so the snapshot precedes the hook
       without reference to `Audio.txt`'s one-second resolution. Record the destination `firstseen.time`
       against the `Matching` line as the corroborating measurement that design (b) would still have raced.
       File names stay in scratch; the evidence records counts and positions.
       Then run `ffprobe -v error -show_entries format_tags -of json` on each COMPLETION file and record the
       **set of tag keys**, never the values. Any `MUSICBRAINZ_*` key in a byte-identical file is recorded
       as "came with the release (bytes unchanged since PRE-HOOK)". File mtimes are never used as provenance.
    7. `extended.conf` close side: `grep -c '^ReplaygainTagging="false"'` = 1, and its sha256, mtime and size
       equal Task 1's open values. If the line is absent at either end, or the file changed between open and
       close, PASS is unavailable per § 3.
    8. Re-run `bash scripts/quick-health-check.sh` from the workstation → still exit 0, census counters at
       target, drift block green, `beets databases: 1`.

    Write § 7, headed `## 7. Result — window 2 (plan 04-15)`, in the same table shape as § 6, holding: the
    stamp, window open and close times, the SAB row (category and truncated `script_line` only, `storage`
    quoted as its `/downloads/complete/nzb/music/` prefix), the find outputs with their positive controls,
    the `Audio.txt` delta with the two residual lines quoted verbatim, the PRE-HOOK/vanished/COMPLETION
    times with the validity ordering, the per-file comparison counts, the `extended.conf` open and close
    values, the ffprobe tag-key sets, and exactly one verdict line beginning at column 1:

    - `Verdict (window 2): PASS` only if every § 1 pass condition holds, the PRE-HOOK snapshot is valid
      under the amended § 1 item 4, and the § 3 prerequisite held at both ends.
    - `Verdict (window 2): FAIL — <which condition>` if any § 1 item is violated: a file present in both
      snapshots whose sha256 differs, or an audio file present only at COMPLETION matching no PRE-HOOK
      file, or a `SUCCESS: Matched with beets` line, or a new `library.blb`/`beets.log`/unexpected `.bak`.
    - `Verdict (window 2): OPEN — <reason>` if no job arrived, or the judge returned `UNPROVEN`, or the § 3
      prerequisite changed without a byte change.

    Record, as § 6 did, any weaker corroborating evidence **separately and explicitly not as the pass
    condition** — the absence of a regenerated beets database, the absence of `MUSICBRAINZ_*` keys, and
    whether Lidarr imported. None of it may upgrade a verdict.

    **Do not touch § 6.** After the edit, `grep -cE '^Verdict: (PASS|OPEN|FAIL)'` over the file must still
    return exactly 1 — 04-13's verify and the phase verification both depend on that invariant — and
    `grep -cE '^Verdict \(window 2\): (PASS|OPEN|FAIL)'` must return exactly 1. Insert a one-line dated
    pointer immediately **above** § 6's existing verdict line, as a new line, noting that the window-1
    record stands verbatim and criterion 3's current verdict is § 7's. Do not alter the window-1 verdict
    line itself.

    Remove `/mnt/fast/scratch-04/15` behind the S5 containment fence — `realpath -e` each path, refuse
    anything resolving outside it, delete files individually, `rmdir` directories deepest-first, **no
    `rm -rf`** — and assert it absent with `/mnt/fast/stacks` and `/mnt/fast/appdata` printed as positive
    controls. This also deletes the job-to-folder mapping in `names.tsv`. Delete
    `/mnt/fast/scratch-04/watch-d12b.sh` and `judge-d12b.sh` in the same pass, then the empty parent. Prove
    no watcher process remains using the self-match-proof method from 04-12 deviation 6: write the pattern
    to a file, match with `grep -F -f` over a `ps` snapshot captured to a second file, with a positive
    control on the same snapshot.

    Screen added lines: 0 runs of 40+ characters, 0 matches for `user_token|discogs\.env|/secrets/|token=|apikey|api_key`,
    and no release or job folder name. Commit only this file: `docs(04-15): D-12 window 2 evidence`.
  </action>
  <verify>
    <automated>f=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; test -f "$f" && test "$(grep -cE '^Verdict: (PASS|OPEN|FAIL)' "$f")" = 1 && test "$(grep -cE '^Verdict \(window 2\): (PASS|OPEN|FAIL)' "$f")" = 1 && test "$(grep -c '## 7. Result — window 2' "$f")" = 1 && ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "timeout 30 bash -c 'test -d /mnt/fast/stacks -a -d /mnt/fast/appdata || { echo UNKNOWN-host-tree-not-visible; exit 3; }; test ! -e /mnt/fast/scratch-04/15 && test ! -e /mnt/fast/scratch-04/watch-d12b.sh && echo CLEANED'"</automated>
  </verify>
  <acceptance_criteria>
    - § 7 exists, headed `## 7. Result — window 2 (plan 04-15)`, and contains exactly one line matching `^Verdict \(window 2\): (PASS|OPEN|FAIL)`.
    - `grep -cE '^Verdict: (PASS|OPEN|FAIL)'` still returns 1, and `git diff HEAD~1` shows the window-1 verdict line unchanged (0 deletions on that line).
    - For a PASS verdict: each of the eight reads is quoted with its observed value and its producer's exit status; the judge returned `BYTES-OK` for the job; `pre.time` is quoted as earlier than `vanished.time` with both values; every file present in both snapshots is byte-identical; `extended.conf` is unchanged open to close.
    - For a FAIL verdict: the violated § 1 condition is named, with the file's position in the listing (never its name) and both sha256 values.
    - For an OPEN verdict: the reason and the window bounds are stated, and the SUMMARY states plainly that OPEN is "could not look", not "the estate is dirty".
    - The `Audio.txt` delta shows exactly +1 `Matching N tracks with Beets`, +1 `ERROR: Unable to match using beets to a musicbrainz release`, and +0 `SUCCESS: Matched with beets` for the job, with both residual lines quoted verbatim.
    - The § 7 section contains no mtime-based provenance argument: `grep -ci 'mtime'` within § 7 is limited to the `extended.conf` open and close rows.
    - No release or job folder name appears anywhere in the added text; jobs are identified by 12-character sha256 prefix.
    - `/mnt/fast/scratch-04/15`, `watch-d12b.sh` and `judge-d12b.sh` are all absent; no watcher process remains, proven with a self-match-proof instrument carrying a positive control; no `rm -rf` was executed.
    - No estate file was modified by this plan: `git diff --name-only` for the commit lists exactly `04-D12-EVIDENCE.md`.
  </acceptance_criteria>
  <done>Criterion 3's behavioural half is decided by one real job against a contract fixed before the job — PASS, FAIL, or honestly OPEN — with "untagged" proven by bytes captured before the hook could run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| download pipeline → evidence (public repo) | a real job's release and folder name could leak into a tracked file |
| watcher → live download tree | a background reader runs as root on LXC 100 for up to 7 days over real download data |
| evidence author → verdict | a verdict could be written to close a phase rather than to record a measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15-01 | Information disclosure | 04-D12-EVIDENCE.md § 7, SUMMARY | mitigate | 12-character sha256 prefix identification; `script_line` truncated at 60 characters; `storage` quoted as prefix only; ffprobe tag **keys** not values; `Verified Track:` lines never quoted; `names.tsv` deleted with scratch; added lines screened for 40-character runs and credential patterns |
| T-04-15-02 | Information disclosure | SABnzbd API key | mitigate | no API call at all — history read via `sqlite3 "file:…?mode=ro"`, and the job is the operator's act in Lidarr |
| T-04-15-03 | Tampering | watch-d12b.sh over the live download tree | mitigate | read-only verbs only, writes confined to `/mnt/fast/scratch-04/15`; the running instrument's sha256 asserted equal to the one 04-14 self-tested, including its SC-4 read-only control |
| T-04-15-04 | Repudiation | a verdict fitted to the desired outcome | mitigate | the contract was amended and committed before the stamp, asserted in Task 1; § 5's pre-declared PASS/FAIL/OPEN rules decide; the judge was driven to FAIL and UNPROVEN on fixtures in 04-14; weaker side-effect evidence is recorded separately and explicitly cannot upgrade the verdict; § 6's window-1 record is left byte-identical |
| T-04-15-05 | Denial of service | watcher left running, or scratch left behind | mitigate | bounded by `timeout 604800`; stopped by `SIGTERM` with liveness proven by a self-match-proof `grep -F -f` over a captured `ps` snapshot with a positive control; scratch removed behind the S5 fence with no `rm -rf` |
</threat_model>

<verification>
Exactly one window-2 verdict line, the window-1 record untouched, evidence tied to one stamp and one
PRE-HOOK/COMPLETION pair whose ordering is structural, the § 3 prerequisite asserted at both ends, and
both standing guards still green after the job.
</verification>

<success_criteria>
Criterion 3's behavioural half is measured on a real job (PASS or FAIL) or honestly recorded OPEN —
never assumed, never inferred from timestamps, and never upgraded on the strength of clean side effects.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-15-SUMMARY.md` when done.
It must state the window-2 verdict word in its own frontmatter or headline, because plan 04-16 selects
`beets.md`'s heading from it.
</output>
