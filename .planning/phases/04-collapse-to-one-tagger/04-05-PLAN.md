---
phase: 04-collapse-to-one-tagger
plan: 05
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - stacks/selfhosted/arrs/beets.md
  - .gitignore
  - scripts/spike03-wrtag-arms.sh
autonomous: true
requirements: [TAGR-03]
tags: [docs, sweep]

must_haves:
  truths:
    - "D-14: stacks/selfhosted/arrs/beets.md carries the Phase 3 finding verbatim in a dated in-band correction section, and states the wrtag rule's stated cause was reversed"
    - "D-24: beets.md's 'Standing action, carried forward' (Discogs token) is amended in-band and dated to record the operator's dismissal — never deleted, and no rotation step is taken"
    - "D-07: beets.md header links and banner no longer point at deleted files; .gitignore's comment no longer names a deleted file as its example; spike03-wrtag-arms.sh is KEPT with a dated header note naming the pre-deletion commit that reproduces its default input"
    - "D-07: an interim sweep verdict table covers every `git ls-files | xargs grep -il 'wrtag\\|soulbeet'` hit outside .planning/, each with verdict deleted / corrected / keep-with-reason and the owning plan"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "D-14 verbatim correction; D-24 amendment"
      contains: "works on none of v0.20.0, v0.33.0 or v0.34.0"
    - path: "scripts/spike03-wrtag-arms.sh"
      provides: "keep-with-reason header note"
      contains: "git show"
  key_links:
    - from: "scripts/spike03-wrtag-arms.sh header"
      to: "PRE_DELETION_SHA recorded in 04-03-SUMMARY.md"
      via: "reproduction instruction"
      pattern: "WRTAG_YAML="
---

<objective>
Land the D-14 correction in the durable estate record (`beets.md`), convert its Discogs standing action to
the D-24 dismissal, fix the repo-level stale references that are true now, and record the interim D-07
sweep verdict table (the final sweep runs in 04-13).

Purpose: CONTEXT § Specifics — land the correction verbatim so a paraphrase cannot re-create the reversed
cause. The "Two beets, one library" table is NOT rewritten here: it describes runtime configs that only
become true after 04-11, and is corrected by 04-13.
Output: three edited files; the interim verdict table in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-03-SUMMARY.md
@.planning/phases/03-tagger-spike/03-WRTAG-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: beets.md — header/banner links, D-14 verbatim correction, D-24 amendment of the standing action</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md lines 1-50 (header links 3-5, banner 13, table 27-47), 940-1039 (Phase 3 section, in-band correction 981-990, Standing action 1004-1039)
    - 04-CONTEXT.md D-14 (verbatim sentence) and D-24 (operator's words)
    - .planning/phases/03-tagger-spike/03-DECISION.md lines 1330-1362
  </read_first>
  <action>
    (1) Header lines 3-5: remove or re-point links to `soulbeet.yaml` / `soulbeet/beets_config.yaml` / `music/wrtag.yaml` (deleted in 04-03) — name them as deleted in Phase 4 with the PRE_DELETION_SHA from 04-03-SUMMARY for history. Banner line 13: amend so it no longer asserts facts about wrtag's library mount that are moot; state the wrtag and soulbeet definitions were deleted (04-03) and host retirement follows in this phase. Do NOT rewrite the "Two beets, one library" table (27-47) — add one dated line above it: "Superseded by Phase 4 — rewritten in § Phase 4 when the runtime change lands (plan 04-13)".
    (2) After the Phase 3 section (following the 981-990 correction), add `### Correction: the wrtag pin's stated cause was reversed (<today>, Phase 4 D-14)` stating: the retired Renovate rule blamed three path-format fields, two of which (`.Release.Date.Year`, `.Release.Media`) are present at v0.20.0; the real defects are that v0.20.0 forces `d.Track.Position = -1` and that `.Media.Position` is absent from its `Data` struct; then the D-14 sentence verbatim; then "Unpinning buys nothing" with the three-row answer from 03-DECISION § Handoff (would not deploy / v0.33.0 refuses at startup / v0.34.0 is a path-format no-op); cite `03-WRTAG-EVIDENCE.md`. Paraphrase the retired rule's text, never quote it.
    (3) Standing action 1004-1039: prepend a dated in-band amendment block: "Dismissed by the operator <date of D-24 = 2026-09-06>, Phase 4 D-24", the operator's words verbatim, "recorded as a choice, not an oversight or a silence", pointer to PROJECT.md Key Decisions; leave the original text standing below it. Take no action on the token.
    Commit (`docs(04-05): beets.md D-14 correction and D-24 dismissal`).
  </action>
  <verify>
    <automated>grep -c 'works on none of v0.20.0, v0.33.0 or v0.34.0' stacks/selfhosted/arrs/beets.md && grep -c 'D-24' stacks/selfhosted/arrs/beets.md && test "$(grep -cE '\]\((\.\./)?(soulbeet\.yaml|soulbeet/beets_config\.yaml|\.\./music/wrtag\.yaml)\)' stacks/selfhosted/arrs/beets.md)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'works on none of v0.20.0, v0.33.0 or v0.34.0'` = 1 in beets.md; `grep -c 'Track.Position = -1'` ≥ 1.
    - No markdown link target in beets.md resolves to a deleted file: for every `](path)` target in lines 1-20, `test -e stacks/selfhosted/arrs/<path>` succeeds or the link was removed.
    - The Standing action section's original heading still exists (`grep -c 'Standing action, carried forward'` ≥ 1) and a `Dismissed by the operator` line precedes its original text.
    - Lines 27-47 table body unchanged except the one added "Superseded" line (`git diff` inspection recorded).
  </acceptance_criteria>
  <done>The durable record carries the correct cause verbatim and answers the Discogs question.</done>
</task>

<task type="auto">
  <name>Task 2: .gitignore example, spike03-wrtag-arms.sh keep-with-reason note, interim D-07 sweep table</name>
  <files>.gitignore, scripts/spike03-wrtag-arms.sh</files>
  <read_first>
    - .gitignore lines 38-56
    - scripts/spike03-wrtag-arms.sh lines 1-60 (header convention), 140-150 (WRTAG_YAML default at ~147), line ~48 (renovate.json5:91 citation)
    - 04-RESEARCH.md § D-07 sweep table; 04-PATTERNS.md § .gitignore, § spike03-wrtag-arms.sh
  </read_first>
  <action>
    .gitignore line ~45: replace the example `stacks/selfhosted/music/wrtag.yaml` with `stacks/selfhosted/arrs/sabnzbd/beets-config.yaml`; leave lines 49-50 (the `**/beets-config/*.yaml` rule) untouched. Verify `git check-ignore -v stacks/selfhosted/arrs/sabnzbd/beets-config.yaml stacks/selfhosted/arrs/beets/config.yaml` returns nothing.
    spike03-wrtag-arms.sh: add a dated header note block (in the file's `# ` comment style, after the existing header) titled `KEPT AFTER PHASE 4 (<today>)`: the script is Phase 3 criterion-3's instrument cited by 03-WRTAG-EVIDENCE.md; its default `WRTAG_YAML` now points at a file deleted by plan 04-03; reproduce with `git show <PRE_DELETION_SHA>:stacks/selfhosted/music/wrtag.yaml > <scratch>` and `WRTAG_YAML=<scratch>`; the `renovate.json5:91` citation at ~48 refers to a rule deleted in Phase 4. Change no executable line. `bash -n scripts/spike03-wrtag-arms.sh`.
    Sweep: run `git ls-files | xargs grep -il 'wrtag\|soulbeet'` and `git grep -nE 'library\.blb|line 285|beets\.log|01-09 folds|Two beets|soulbeet\.deercrest'`; for every hit OUTSIDE `.planning/` record in the SUMMARY: file, lines, verdict (deleted / corrected / keep-with-reason), reason, and owning plan (e.g. check-music-freeze.sh → 04-06; sabnzbd.yaml and sabnzbd/beets-config.yaml → 04-10; spike03-image-headroom.sh → 04-07; beets.md table → 04-13). `.planning/**` hits are keep (provenance) as one row.
    Commit (`docs(04-05): sweep .gitignore and spike03-wrtag-arms.sh (D-07)`).
  </action>
  <verify>
    <automated>test "$(grep -c 'music/wrtag.yaml' .gitignore)" = 0 && bash -n scripts/spike03-wrtag-arms.sh && grep -c 'KEPT AFTER PHASE 4' scripts/spike03-wrtag-arms.sh && test -z "$(git check-ignore stacks/selfhosted/arrs/beets/config.yaml)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'music/wrtag.yaml' .gitignore` = 0; `grep -c 'beets-config/\*.yaml' .gitignore` unchanged.
    - `git diff scripts/spike03-wrtag-arms.sh | grep -E '^[+-][^+#-]' | grep -v '^+\s*#'` returns nothing (only comment lines added).
    - SUMMARY sweep table lists every non-.planning hit from the grep with a verdict and owner; its row count equals `git ls-files | grep -v '^\.planning/' | xargs grep -il 'wrtag\|soulbeet' | wc -l` plus the widened-grep files.
  </acceptance_criteria>
  <done>Every stale reference has a verdict and an owner; the ones this plan owns are corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public repo | beets.md is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information disclosure | beets.md D-24 amendment | mitigate | no token value, prefix or file content; wording limited to the decision |
| T-04-05-02 | Repudiation | removed measurement reproducibility | mitigate | spike03-wrtag-arms.sh kept; pre-deletion SHA recorded for `git show` reproduction |
| T-04-05-03 | Tampering | .gitignore | mitigate | `git check-ignore` proves neither tracked beets config is masked |
</threat_model>

<verification>
Both verify commands pass; sweep table complete with owners.
</verification>

<success_criteria>
The durable record is corrected verbatim, and no stale reference is left without a verdict.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-05-SUMMARY.md` when done.
</output>
