---
phase: 04-collapse-to-one-tagger
plan: 11
type: execute
wave: 6
depends_on: [04-10]
files_modified:
  - stacks/selfhosted/arrs/sabnzbd.yaml
  - scripts/check-music-freeze.sh
  - scripts/quick-health-check.sh
autonomous: true
requirements: [TAGR-04, TAGR-05]
tags: [sabnzbd, recreate, boot-proof, deletion, promotion]

must_haves:
  truths:
    - "D-09: sabnzbd is recreated with both vendored files mounted :ro and proven healthy — `[custom-init] scripts_init.bash: exited 0`, container running, HTTP OK on :8084, both mounts RW=false, both files' sha256 unchanged after boot; any EROFS lines are recorded, and :ro is kept if the container is healthy"
    - "A failed boot is classified before any rollback: EROFS (read-only lines naming a vendored file) may roll that one mount back to :rw; NETWORK (pip/git/curl resolution or connection errors with no EROFS line) never touches :ro — one retry after an in-container connectivity check, then HALT"
    - "The recreate never pulls: the sabnzbd.yaml tag at host HEAD is asserted resident first (PR #310 race), and the queue is idle; the SAB API key never appears on argv"
    - "D-11 / D-32 / REVIEWS row 4: every sabnzbd scripts/library.blb*, scripts/beets.log and .config/beets/* file is deleted only from a per-file ledger row carrying a confirmed fence copy (existing MANIFEST sha256 match, or a fresh .backup/cp -p + integrity_check ok + MANIFEST line taken now); files are removed one by one and the directory by rmdir, so an unledgered file blocks the delete"
    - "REVIEWS row 1: the tagger census and the vendored-file drift block first run GREEN as candidates, and only then are promoted into the fatal routine path (TAGGER_CENSUS_PROMOTED=1, VENDORED_DRIFT_PROMOTED=1) in one commit with a new EXIT-CODE notice; after promotion no env can disable either"
    - "D-21 / D-25 / D-13: after promotion the ROUTINE check-music-freeze.sh exits 0 (tagger definitions 1, beets databases 1 = the survivor, tagger databases 0, retired paths 0, rw on Music non-tagger 0 and tagger-capable 0, Jellyfin printed separately) and the routine quick-health-check.sh exits 0 with the drift block reporting all three vendored files matching; post-promotion driven controls show both blocks now fail the routine run"
    - "TAGR-05 live: every beets config on the host (sabnzbd beets-config.yaml, survivor config.yaml) has a plugins line containing musicbrainz, and config.yaml.old is absent"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "census promoted into the routine path"
      contains: "TAGGER_CENSUS_PROMOTED=1"
    - path: "scripts/quick-health-check.sh"
      provides: "drift block promoted; EXIT-CODE notice"
      contains: "VENDORED_DRIFT_PROMOTED=1"
  key_links:
    - from: "appdata audio.bash"
      to: "repo stacks/selfhosted/arrs/sabnzbd/audio.bash at host HEAD"
      via: "cp onto the existing inode; drift block"
      pattern: "audio.bash"
    - from: "deletion ledger"
      to: "fence MANIFEST.txt"
      via: "per-file sha256 match or fresh copy + integrity_check ok"
      pattern: "integrity_check"
---

<objective>
Make the sabnzbd path real on the host: install the stripped audio.bash and the fixed defused config,
recreate sabnzbd with both files `:ro` and prove it boots, then delete sabnzbd's beets state from a
per-file fence ledger now that nothing can regenerate it — and, in the same wave that turns them green,
promote the tagger census and the vendored-file drift block from candidates into the fatal routine check.

Purpose: criteria 3 (the strip is live) and 5 (one library.db, measured and standing-guarded). The D-12
real-job proof follows in 04-12.
Output: host changes with evidence; the promotion commit to both health scripts; sabnzbd.yaml edited only
if the `:ro` boot proof fails for EROFS (measured rollback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-06-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-10-SUMMARY.md
@stacks/selfhosted/arrs/sabnzbd.yaml
@scripts/check-music-freeze.sh
@scripts/quick-health-check.sh
</context>

<conventions>
- Preconditions (halt if unmet): 04-09 recorded the D-17 negative control against the live broken config;
  host HEAD = origin HEAD and includes 04-10's commits.
- Install by `cp` onto the existing appdata file (same inode — a rename would detach a single-file bind
  mount); never `mv` or `install`. Record sha256 before/after.
- SAB API reads (queue only): read the key inside the process from `sabnzbd.ini` into a shell variable and
  pass the URL via `curl -sK <(printf 'url = "http://127.0.0.1:8084/api?mode=queue&output=json&apikey=%s"\n' "$KEY")`
  — `printf` is a builtin, so the key is in no argv. Never `ps`/`pgrep -a` during it. Print only
  `noofslots`/`status`, never the key.
- Deletion guard S5 as in 04-07 (exact-path allow-list, realpath equality, `..` refusal, list-then-delete),
  applied per ledger file. Never `rm -rf` in this plan.
- Promotion happens only after both candidate runs are green on the estate as it stands (REVIEWS row 1);
  nothing is promoted over a red or UNKNOWN candidate.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Install both vendored files and recreate sabnzbd with a boot proof; classify any init failure before rollback (D-09, C3-b)</name>
  <files>stacks/selfhosted/arrs/sabnzbd.yaml (only on a measured EROFS :ro boot failure)</files>
  <read_first>
    - 04-RESEARCH.md Pitfalls 7 and 8, Assumption A1, § Runtime State Inventory (scripts_init.bash runs `pip install -U beets` and git/curl on every boot)
    - stacks/selfhosted/arrs/sabnzbd.yaml at host HEAD
    - `docker logs sabnzbd` baseline boot lines (read the last boot's `[custom-init]` lines before recreating)
  </read_first>
  <action>
    Pre-checks: (1) the `image:` tag on sabnzbd.yaml line ~32 at host HEAD is resident (`docker image inspect <tag>` succeeds) — if not (e.g. PR #310 merged and pulled), HALT and report; do not bundle a version change into the boot proof. (2) queue idle via the conventions' curl -K method: `noofslots` = 0; if not, poll every 5 min up to 60 min, then HALT. (3) record the current container start time and the last boot's `[custom-init]` lines.
    Install: `cp` repo `stacks/selfhosted/arrs/sabnzbd/audio.bash` onto `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` and repo `stacks/selfhosted/arrs/sabnzbd/beets-config.yaml` onto `…/scripts/beets-config.yaml`; both sha256 must equal the repo files; `audio.bash` must stay executable (`test -x`).
    Stamp `date -u`, then `docker compose -f stacks/selfhosted/arrs/compose.yaml config --quiet` and `docker compose -f stacks/selfhosted/arrs/compose.yaml up -d --no-deps --pull never sabnzbd`. Wait up to 15 minutes (scripts_init.bash pip/git/curl on boot) for `docker logs --since <stamp> sabnzbd` to contain `scripts_init.bash: exited 0`. Boot proof, all recorded: that line; every line matching `Read-only file system|EROFS|lsiown|chmod:` verbatim; `docker inspect -f '{{.State.Status}}' sabnzbd` = running; `curl -s -L -o /dev/null -w '%{http_code}' http://127.0.0.1:8084/` = 200; `docker inspect` mounts for `/config/scripts/audio.bash` and `/config/scripts/beets-config.yaml` both `RW=false`; `docker exec sabnzbd sha256sum /config/scripts/audio.bash /config/scripts/beets-config.yaml` equal to the repo files (LSIO's chown/chmod passes did not alter them).
    Failure classification BEFORE any rollback (REVIEWS "Nobody raised"): if `scripts_init.bash: exited 0` does not appear, or the container is not running, classify from the logs since the stamp — EROFS class: a `Read-only file system`/`EROFS` line naming `/config/scripts/audio.bash` or `/config/scripts/beets-config.yaml`; NETWORK class: pip/git/curl errors such as `Could not resolve host`, `Temporary failure in name resolution`, `Failed to connect`, `Connection timed out`, pip `ConnectionError`/`NewConnectionError`, with NO EROFS line; otherwise UNCLASSIFIED. Then: EROFS → the D-09 rollback: flip ONLY the offending mount back to `:rw` in sabnzbd.yaml with a dated NOTE line quoting the measured failure, commit/push/pull, recreate the same way, re-run the proof, and record "`:ro` measured-and-deferred for <file>". NETWORK → do NOT touch `:ro`: check outbound reach from inside (`docker exec sabnzbd curl -sI -o /dev/null -w '%{http_code}' https://pypi.org/simple/` and the same for `https://github.com`, RC and code recorded); once both answer, recreate once more the same way and re-run the proof; if it fails again for NETWORK, HALT and report (the boot proof is blocked by an upstream outage, not by `:ro`; Task 2 must not run). UNCLASSIFIED → HALT and report with the log lines. If EROFS lines appear but the container is healthy and init exited 0, keep `:ro`.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; timeout 20 docker inspect -f '{{.State.Status}}' sabnzbd | grep -qx running && timeout 20 docker inspect -f '{{range .Mounts}}{{.Destination}}={{.RW}} {{end}}' sabnzbd | grep -o '/config/scripts/[a-z.-]*=[a-z]*' && cd /mnt/fast/stacks && cmp -s stacks/selfhosted/arrs/sabnzbd/audio.bash /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash && echo INSTALLED"</automated>
  </verify>
  <acceptance_criteria>
    - C3-b: logs contain `scripts_init.bash: exited 0` after the stamp; status running; HTTP 200; mounts `/config/scripts/audio.bash=false` and `/config/scripts/beets-config.yaml=false` (or the recorded EROFS rollback for one file); in-container sha256 of both = repo.
    - Any boot failure carries a recorded classification (EROFS / NETWORK / UNCLASSIFIED) with the log lines that decided it, made before any mount was changed; a NETWORK failure left both mounts `:ro`.
    - Pre-check evidence (tag resident, queue `noofslots` 0) recorded; no image was pulled (`docker image ls` digest for the tag unchanged).
    - No SAB API key string appears in the SUMMARY or in any command line (method recorded).
  </acceptance_criteria>
  <done>sabnzbd runs the stripped hook from a read-only mount and boots cleanly, and a network outage cannot be mistaken for a `:ro` failure.</done>
</task>

<task type="auto">
  <name>Task 2: Per-file fence ledger, then delete sabnzbd's beets state from the ledger only (D-11, D-32, REVIEWS row 4)</name>
  <files>(host only: /mnt/fast/safety/music-pre-project/library-db/, /mnt/fast/safety/music-pre-project/audit/, MANIFEST.txt, sabnzbd appdata deletions)</files>
  <read_first>
    - 04-01-SUMMARY.md (listing of `library.blb*` names, `.config/beets/` contents, fence re-confirmation rows, coverage preview)
    - /mnt/fast/safety/music-pre-project/library-db/MANIFEST.txt on LXC 100 (line format; existing sha256 values)
    - scripts/freeze-music-apply.sh lines 295-304 and 408-445 (the .backup + verify + MANIFEST method)
  </read_first>
  <action>
    (1) Fresh listing, now (the stripped hook is live, so nothing regenerates these): every regular file matching `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb*`, the file `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets.log`, and every entry (files and directories, recursive) under `/mnt/fast/appdata/arrs/sabnzbd/config/.config/beets/`. The `find` producing it must exit 0 and must also list `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` when asked in the same pass (positive control); otherwise UNKNOWN and HALT.
    (2) Ledger: for every FILE in the listing record `live path | size | live sha256 | SQLite? (first 16 bytes) | fence file | fence sha256 | MANIFEST line no. | integrity | source`. Source `existing` when some MANIFEST line carries the file's live sha256: recompute that fence file's sha256 (must equal MANIFEST) and, for SQLite, `PRAGMA integrity_check` on the fence file = `ok`. Otherwise take a fresh fence copy now: SQLite → `sqlite3 "<live>" ".backup '<fence>/library-db/<name>'"` where `<name>` follows the MANIFEST naming convention (source path with `/` → `_`, prefixed as 04-01 did; append a UTC stamp if that name already exists — never overwrite a fence file), then `PRAGMA integrity_check` = `ok`; non-SQLite (`beets.log`, any config/state file) → `cp -p` into `<fence>/audit/` with a UTC stamp and sha256 equal to the live file's; then append a MANIFEST line in the file's existing format (fence sha256, source path, live sha256, UTC time, note `phase 04 D-32 ledger`), source `fresh-04-11`. Write the ledger table into the SUMMARY (paths and hashes only).
    (3) Refusal rule: no file is deleted unless its ledger row is complete (fence file present, fence sha256 = MANIFEST, integrity `ok` for SQLite). Immediately before deleting, re-list with the same command; if any file appears that has no ledger row, ledger it first (step 2) — never delete an unledgered file.
    (4) Delete from the ledger only: each file individually through the S5 guard (realpath equals the ledger path exactly; no globs; no `..`), `test ! -e` after each; then `rmdir` each now-empty directory under `.config/beets/` deepest first, and finally `rmdir /mnt/fast/appdata/arrs/sabnzbd/config/.config/beets`. If any `rmdir` fails because something remains, HALT and report what remains — never `rm -rf`.
    (5) Record the counts: ledger rows, `existing` vs `fresh-04-11`, files deleted, directories removed.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "d=/mnt/fast/appdata/arrs/sabnzbd/config; test -f \$d/scripts/audio.bash || { echo 'UNKNOWN positive control audio.bash not visible'; exit 3; }; left=\$(timeout 30 find \$d/scripts -maxdepth 1 \( -name 'library.blb*' -o -name beets.log \) -print); rc=\$?; [ \$rc -eq 0 ] || { echo \"UNKNOWN find rc=\$rc\"; exit 3; }; [ -z \"\$left\" ] || { echo \"PRESENT: \$left\"; exit 1; }; test ! -e \$d/.config/beets || { echo 'PRESENT .config/beets'; exit 1; }; grep -c 'phase 04 D-32 ledger' /mnt/fast/safety/music-pre-project/library-db/MANIFEST.txt; echo LEDGER-DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY carries the ledger with one complete row per deleted file (row count = files deleted); every `fresh-04-11` row has a MANIFEST line tagged `phase 04 D-32 ledger` whose fence sha256 matches the fence file, and every SQLite row has integrity `ok`.
    - No `library.blb*` or `beets.log` remains under sabnzbd `scripts/` (producer-status-first `find`, `audio.bash` positive control); `.config/beets` absent; no `rm -rf` in the transcript.
    - C5-b: every deleted sabnzbd path has a recorded fence confirmation preceding its delete.
  </acceptance_criteria>
  <done>sabnzbd's beets state is gone, and every file that went has a verified copy in the fence — including the regenerated backups the Phase 1 fence never held.</done>
</task>

<task type="auto">
  <name>Task 3: Candidate runs green → promote both guards into the routine check → routine green → post-promotion driven controls (D-13, D-21, D-25, C4-a, REVIEWS rows 1 and 17)</name>
  <files>scripts/check-music-freeze.sh, scripts/quick-health-check.sh</files>
  <read_first>
    - scripts/check-music-freeze.sh § 6b and its CANDIDATE → PROMOTED header block (as landed by 04-06)
    - scripts/quick-health-check.sh lines 1-160 (EXIT-CODE notices — copy the shape of the fourth), the REMOTE_TIMEOUT block (~200-234), the drift block and its CANDIDATE → PROMOTED comment (as landed by 04-10), 734-746 (failure tail)
    - 04-06-SUMMARY.md (candidate elapsed seconds and prune list)
  </read_first>
  <action>
    (1) Candidate runs must be green on the estate as it stands: on LXC 100 `cd /mnt/fast/stacks && time CENSUS_CANDIDATE=1 bash scripts/check-music-freeze.sh` → RC 0 with `tagger definitions: 1`, `beets databases: 1` (the survivor's fresh DB, at SURVIVOR_DB), `tagger databases: 0`, `retired paths present: 0`, `rw on Music, non-tagger: 0`, `rw on Music, tagger-capable: 0`, `rw on Music, Jellyfin D-21: 1`, sabnzbd listed as tagger-capable (reported); record elapsed seconds. From the workstation `VENDORED_DRIFT_CANDIDATE=1 bash scripts/quick-health-check.sh` → drift block `✅ vendored files match (3)`. If either is non-zero or UNKNOWN, record which assertion and why and STOP — do not relax a check and do not promote.
    (2) Promotion commit (REVIEWS row 1): set `TAGGER_CENSUS_PROMOTED=1` in check-music-freeze.sh and update its CANDIDATE → PROMOTED block to "PROMOTED <today> by plan 04-11 after the first green candidate run (<UTC>)"; set `VENDORED_DRIFT_PROMOTED=1` in quick-health-check.sh and update its block likewise; add a sixth header notice containing the literal `EXIT-CODE BEHAVIOUR CHANGED AGAIN — PHASE 4: THE TAGGER CENSUS AND A FOURTH FATAL BLOCK (VENDORED-FILE DRIFT) WERE PROMOTED <today> (plan 04-11, D-13/D-21/D-25)`, in the existing notices' shape, stating every new exit-1 condition: a census assertion (tagger definitions ≠ 1; beets databases ≠ 1 or not SURVIVOR_DB; a tagger database; a retired path present; any container other than Jellyfin, in any state, holding rw reaching Music) or the census blind (UNKNOWN: docker unreadable, find RC not 0/1 or timed out, Jellyfin positive control missing); a vendored file DRIFTED; the drift block UNKNOWN (no output, RC 124, other RC) or run with a non-default DRIFT_APPDATA_ROOT; and that `CENSUS_CANDIDATE`/`VENDORED_DRIFT_CANDIDATE` are now ignored and cannot disable either block. REMOTE_TIMEOUT (REVIEWS row 17): if 04-06's recorded candidate elapsed time or step (1)'s exceeds 60 s, raise the default in `REMOTE_TIMEOUT="${REMOTE_TIMEOUT:-120}"` to 200 in this same commit and state the measured figure and the reason in the notice and in the REMOTE_TIMEOUT block; otherwise leave it at 120 and record both figures in the SUMMARY. `bash -n` on the host script, `/bin/bash -n` on the workstation script. Commit (`feat(04-11): promote the tagger census and vendored-file drift into the routine check (D-13, D-21, D-25)`), push, `git -C /mnt/fast/stacks pull --ff-only`, record host HEAD.
    (3) Routine runs, NO env: on LXC 100 `bash scripts/check-music-freeze.sh` → exit 0 AND the census summary lines are printed (proving 6b now runs unprompted); from the workstation `time bash scripts/quick-health-check.sh` → exit 0, the freeze block shows the census counters, the drift block shows `✅ vendored files match (3)`; record wall time (must be ≤ 200 s).
    (4) Post-promotion driven controls (each must go red through the ROUTINE path, then clean up): (p1) on LXC 100 create `/mnt/fast/scratch-04/11/exists` and run `RETIRED_DB_PATHS=/mnt/fast/scratch-04/11/exists bash scripts/check-music-freeze.sh` with no CENSUS_CANDIDATE → exit 1 naming it; (p2) `CENSUS_CANDIDATE=0 bash scripts/check-music-freeze.sh` → census lines still printed (the env cannot disable 6b); (p3) workstation `DRIFT_EXPECT_SURVIVOR_BEETS_CONFIG=$(printf '0%.0s' {1..64}) bash scripts/quick-health-check.sh` with no VENDORED_DRIFT_CANDIDATE → exit 1 with the survivor line `DRIFTED`; (p4) `VENDORED_DRIFT_CANDIDATE=0 bash scripts/quick-health-check.sh` → the drift block still runs. Remove `/mnt/fast/scratch-04/11` (S5 guard) and assert gone.
    (5) C4-a live: `grep -E '^plugins:' /mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml /mnt/fast/appdata/arrs/beets/config/config.yaml` → both contain `musicbrainz`; `test ! -e /mnt/fast/appdata/arrs/beets/config/config.yaml.old` (with `config.yaml` present as positive control). Record all transcripts ANSI-stripped.
  </action>
  <verify>
    <automated>test "$(grep -cx 'TAGGER_CENSUS_PROMOTED=1' scripts/check-music-freeze.sh)" = 1 && test "$(grep -cx 'VENDORED_DRIFT_PROMOTED=1' scripts/quick-health-check.sh)" = 1 && test "$(grep -c 'EXIT-CODE BEHAVIOUR CHANGED' scripts/quick-health-check.sh)" = 6 && timeout 200 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "cd /mnt/fast/stacks || exit 3; out=\$(timeout 150 bash scripts/check-music-freeze.sh 2>&1); rc=\$?; [ \$rc -eq 0 ] || { echo \"routine freeze rc=\$rc\"; exit 1; }; printf '%s\n' \"\$out\" | grep -q 'tagger definitions: *1 ' && printf '%s\n' \"\$out\" | grep -q 'beets databases: *1 ' && printf '%s\n' \"\$out\" | grep -q 'retired paths present: *0 ' && echo ROUTINE-CENSUS-GREEN" && q=$(bash scripts/quick-health-check.sh 2>&1); qrc=$?; [ "$qrc" -eq 0 ] && printf '%s\n' "$q" | grep -q 'vendored files match (3)' && echo ROUTINE-QHC-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Step (1): both candidate runs green before any promotion edit (transcripts with UTC times precede the promotion commit).
    - C5-a: routine freeze exit 0 with the census counters at target and Jellyfin on its own line; routine quick-health-check exit 0 with wall time ≤ 200 s recorded.
    - C3-d green: routine drift block names all three files matching.
    - Promotion commit: both constants = 1; `grep -c 'EXIT-CODE BEHAVIOUR CHANGED'` = 6; REMOTE_TIMEOUT decision recorded with both measured figures (raised to 200 in the same commit iff either exceeded 60 s).
    - Post-promotion controls (p1)–(p4) each behaved as stated; `/mnt/fast/scratch-04/11` absent.
    - C4-a: both host plugins lines contain `musicbrainz`; `config.yaml.old` absent.
  </acceptance_criteria>
  <done>The estate holds one beets database and no retired tagger state, and both new guards are now part of the routine health check — green, and seen to fail through the routine path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 → sabnzbd container | a recreate of a 24/7 service with network-dependent init |
| host → SAB API | API key used for a read-only queue query |
| appdata → fence → deletion | files leave appdata only after a verified copy |
| health scripts → every future operator | promotion changes what can fail the routine check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Information disclosure | SAB API key | mitigate | read into a variable in-process; `curl -K <(printf …)`; never argv/echo/ps |
| T-04-11-02 | Denial of service | sabnzbd recreate | mitigate | idle-queue precondition; resident-tag + `--pull never`; boot proof; failure classified (EROFS / NETWORK) before any rollback; NETWORK never weakens `:ro` |
| T-04-11-03 | Tampering | failed downloads | mitigate | SAB_PP_STATUS guard byte-identical (asserted in 04-10); in-container sha = repo after boot |
| T-04-11-04 | Tampering / Repudiation | host deletes | mitigate | per-file ledger: MANIFEST sha match or fresh `.backup`/`cp -p` + integrity ok + MANIFEST line; file-by-file S5 deletes; `rmdir` so unledgered files block; never `rm -rf` |
| T-04-11-05 | Tampering (supply chain) | pip -U beets at boot | accept | recorded residual; beets is never invoked by the hook after the strip |
| T-04-11-06 | Repudiation | promotion over a red check | mitigate | candidate runs must be green first; post-promotion controls prove the routine path now fails on both blocks |
</threat_model>

<verification>
Boot proof (with classification on failure); ledger-gated deletions; candidate green → promotion → routine freeze exit 0 and quick-health-check exit 0 with drift green; post-promotion controls red through the routine path.
</verification>

<success_criteria>
Criteria 3 (static half, live) and 5 are true on the estate and asserted by standing checks that entered the routine path only once they were green.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS rows 1, 4 and 17 and the "Nobody raised" item; the full table is in 04-01-PLAN.md.
- **Row 1**: the new Task 3 runs both candidates green, then promotes both constants in one commit with the sixth EXIT-CODE notice. Routine green and post-promotion red controls follow.
- **Row 4**: the new Task 2 builds a per-file fence ledger (existing MANIFEST match, or a fresh `.backup`/`cp -p` + integrity + MANIFEST line) and deletes file by file, then `rmdir`s the directory; it never runs `rm -rf`.
- **Row 17**: the REMOTE_TIMEOUT default is raised to 200 in the promotion commit iff a measured candidate run exceeded 60 s.
- **Nobody raised**: Task 1 classifies a failed `scripts_init.bash` as EROFS, NETWORK or UNCLASSIFIED before any rollback. NETWORK never touches `:ro`.
- `files_modified` gains both health scripts. The wave is unchanged: 04-06 and 04-10 already precede this plan.
