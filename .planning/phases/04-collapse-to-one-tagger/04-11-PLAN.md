---
phase: 04-collapse-to-one-tagger
plan: 11
type: execute
wave: 6
depends_on: [04-10]
files_modified:
  - stacks/selfhosted/arrs/sabnzbd.yaml
autonomous: true
requirements: [TAGR-04, TAGR-05]
tags: [sabnzbd, recreate, boot-proof, deletion]

must_haves:
  truths:
    - "D-09: sabnzbd is recreated with both vendored files mounted :ro and proven healthy — `[custom-init] scripts_init.bash: exited 0`, container running, HTTP OK on :8084, both mounts RW=false, both files' sha256 unchanged after boot; any EROFS lines are recorded, and :ro is kept if the container is healthy"
    - "The recreate never pulls: the sabnzbd.yaml tag at host HEAD is asserted resident first (PR #310 race), and the queue is idle; the SAB API key never appears on argv"
    - "D-11 / D-32: after the stripped audio.bash is live, sabnzbd's scripts/library.blb, its backups, scripts/beets.log and .config/beets/ are deleted — each after its fence copy is re-asserted — so nothing can regenerate them"
    - "D-21 / D-25 / D-13: afterwards check-music-freeze.sh exits 0 (tagger definitions 1, beets databases 1 = the survivor, retired paths 0, rw on Music non-tagger 0, Jellyfin printed separately) and quick-health-check.sh's drift block reports all three vendored files matching"
    - "TAGR-05 live: every beets config on the host (sabnzbd beets-config.yaml, survivor config.yaml) has a plugins line containing musicbrainz, and config.yaml.old is absent"
  artifacts: []
  key_links:
    - from: "appdata audio.bash"
      to: "repo stacks/selfhosted/arrs/sabnzbd/audio.bash at host HEAD"
      via: "cp onto the existing inode; drift block"
      pattern: "audio.bash"
---

<objective>
Make the sabnzbd path real on the host: install the stripped audio.bash and the fixed defused config,
recreate sabnzbd with both files `:ro` and prove it boots, then delete sabnzbd's beets state now that nothing
can regenerate it — turning every standing guard green.

Purpose: criteria 3 (the strip is live) and 5 (one library.db, measured). The D-12 real-job proof follows in 04-12.
Output: host changes with evidence; sabnzbd.yaml edited only if the `:ro` boot proof fails (measured rollback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-10-SUMMARY.md
@stacks/selfhosted/arrs/sabnzbd.yaml
</context>

<conventions>
- Preconditions (halt if unmet): 04-09 recorded the D-17 negative control against the live broken config;
  host HEAD = origin HEAD and includes 04-10's commits.
- Install by `cp` onto the existing appdata file (same inode — a rename would detach a single-file bind
  mount); never `mv` or `install`. Record sha256 before/after.
- SAB API reads (queue only): read the key inside the process from `sabnzbd.ini` into a shell variable and
  pass the URL via `curl -sK <(printf 'url = "http://127.0.0.1:8084/api?mode=queue&output=json&apikey=%s"\n' "$KEY")`
  — `printf` is a builtin, so the key is in no argv. Never `ps`/`pgrep -a` during it. Print only
  `noofslots`/`status`, never the key.
- Deletion guard S5 as in 04-07 (exact-path allow-list, realpath equality, `..` refusal, list-then-delete).
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Install both vendored files and recreate sabnzbd with a boot proof (D-09, C3-b)</name>
  <files>stacks/selfhosted/arrs/sabnzbd.yaml (only on a measured :ro boot failure)</files>
  <read_first>
    - 04-RESEARCH.md Pitfalls 7 and 8, Assumption A1
    - stacks/selfhosted/arrs/sabnzbd.yaml at host HEAD
    - `docker logs sabnzbd` baseline boot lines (read the last boot's `[custom-init]` lines before recreating)
  </read_first>
  <action>
    Pre-checks: (1) the `image:` tag on sabnzbd.yaml line ~32 at host HEAD is resident (`docker image inspect <tag>` succeeds) — if not (e.g. PR #310 merged and pulled), HALT and report; do not bundle a version change into the boot proof. (2) queue idle via the conventions' curl -K method: `noofslots` = 0; if not, poll every 5 min up to 60 min, then HALT. (3) record the current container start time and the last boot's `[custom-init]` lines.
    Install: `cp` repo `stacks/selfhosted/arrs/sabnzbd/audio.bash` onto `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` and repo `stacks/selfhosted/arrs/sabnzbd/beets-config.yaml` onto `…/scripts/beets-config.yaml`; both sha256 must equal the repo files; `audio.bash` must stay executable (`test -x`).
    Stamp `date -u`, then `docker compose -f stacks/selfhosted/arrs/compose.yaml config --quiet` and `docker compose -f stacks/selfhosted/arrs/compose.yaml up -d --no-deps --pull never sabnzbd`. Wait up to 15 minutes (scripts_init.bash pip/git/curl on boot) for `docker logs --since <stamp> sabnzbd` to contain `scripts_init.bash: exited 0`. Boot proof, all recorded: that line; every line matching `Read-only file system|EROFS|lsiown|chmod:` verbatim; `docker inspect -f '{{.State.Status}}' sabnzbd` = running; `curl -s -L -o /dev/null -w '%{http_code}' http://127.0.0.1:8084/` = 200; `docker inspect` mounts for `/config/scripts/audio.bash` and `/config/scripts/beets-config.yaml` both `RW=false`; `docker exec sabnzbd sha256sum /config/scripts/audio.bash /config/scripts/beets-config.yaml` equal to the repo files (LSIO's chown/chmod passes did not alter them).
    Rollback (D-09 discretion): if the container is not running or scripts_init did not exit 0 because of a `:ro` file, flip ONLY the offending mount back to `:rw` in sabnzbd.yaml with a dated NOTE line quoting the measured failure, commit/push/pull, recreate the same way, re-run the proof, and record "`:ro` measured-and-deferred for <file>". If EROFS lines appear but the container is healthy, keep `:ro`.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; timeout 20 docker inspect -f '{{.State.Status}}' sabnzbd | grep -qx running && timeout 20 docker inspect -f '{{range .Mounts}}{{.Destination}}={{.RW}} {{end}}' sabnzbd | grep -o '/config/scripts/[a-z.-]*=[a-z]*' && cd /mnt/fast/stacks && cmp -s stacks/selfhosted/arrs/sabnzbd/audio.bash /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash && echo INSTALLED"</automated>
  </verify>
  <acceptance_criteria>
    - C3-b: logs contain `scripts_init.bash: exited 0` after the stamp; status running; HTTP 200; mounts `/config/scripts/audio.bash=false` and `/config/scripts/beets-config.yaml=false` (or the recorded rollback for one file); in-container sha256 of both = repo.
    - Pre-check evidence (tag resident, queue `noofslots` 0) recorded; no image was pulled (`docker image ls` digest for the tag unchanged).
    - No SAB API key string appears in the SUMMARY or in any command line (method recorded).
  </acceptance_criteria>
  <done>sabnzbd runs the stripped hook from a read-only mount and boots cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete sabnzbd's beets state (D-11, D-32) and turn every guard green (D-13, D-21, D-25, C4-a)</name>
  <files>(host only)</files>
  <read_first>
    - 04-01-SUMMARY.md (listing of `library.blb*` names, `.config/beets/` contents, fence re-confirmation rows)
    - scripts/check-music-freeze.sh § 6b (as landed by 04-06)
  </read_first>
  <action>
    List the current `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb*` and `/mnt/fast/appdata/arrs/sabnzbd/config/.config/beets/` contents. Re-assert fence confirmations: `library.blb` and `.config/beets/library.db` rows from 04-01 are `y`/`y` (Phase 1 copies — D-04 does not require fresh archives of these regenerated/empty DBs); `beets.log`: if its sha256 differs from 04-01's fenced copy (jobs appended since), `cp -p` a new copy into the fence `audit/` with a MANIFEST line first. Build the explicit allow-list from the listing (each `library.blb*` file by exact name, `scripts/beets.log`, the directory `.config/beets`) and delete with the S5 guard; `test ! -e` each.
    Run on LXC 100 `bash scripts/check-music-freeze.sh` → exit 0 with `tagger definitions: 1`, `beets databases: 1`, `retired paths present: 0`, `rw on Music, non-tagger: 0`, `rw on Music, Jellyfin D-21: 1`, sabnzbd still listed as tagger-capable (reported). If it is non-zero for any reason, record which assertion and why — do not relax a check. From the workstation `bash scripts/quick-health-check.sh` → exit 0 with the drift block `✅ vendored files match (3)` and the freeze block showing the census counters.
    C4-a live: `grep -E '^plugins:' /mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml /mnt/fast/appdata/arrs/beets/config/config.yaml` → both contain `musicbrainz`; `test ! -e /mnt/fast/appdata/arrs/beets/config/config.yaml.old`. Record all transcripts ANSI-stripped.
  </action>
  <verify>
    <automated>timeout 200 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; cd /mnt/fast/stacks && timeout 150 bash scripts/check-music-freeze.sh >/dev/null 2>&1 && test ! -e /mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb && test ! -e /mnt/fast/appdata/arrs/sabnzbd/config/.config/beets && echo GREEN" && bash scripts/quick-health-check.sh >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - C5-a: freeze exit 0 with the five counters at target and Jellyfin on its own line; quick-health-check exit 0.
    - C3-d green: drift block names all three files matching.
    - C4-a: both host plugins lines contain `musicbrainz`; `config.yaml.old` absent.
    - C5-b: every deleted sabnzbd path has a recorded fence confirmation (or a fresh beets.log copy + MANIFEST line) preceding its delete.
  </acceptance_criteria>
  <done>The estate holds one beets database, no retired tagger state, and every standing guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 → sabnzbd container | a recreate of a 24/7 service with network-dependent init |
| host → SAB API | API key used for a read-only queue query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Information disclosure | SAB API key | mitigate | read into a variable in-process; `curl -K <(printf …)`; never argv/echo/ps |
| T-04-11-02 | Denial of service | sabnzbd recreate | mitigate | idle-queue precondition; resident-tag + `--pull never`; boot proof with measured rollback to `:rw` |
| T-04-11-03 | Tampering | failed downloads | mitigate | SAB_PP_STATUS guard byte-identical (asserted in 04-10); in-container sha = repo after boot |
| T-04-11-04 | Tampering | host deletes | mitigate | exact-name allow-list from a fresh listing; fence confirmation / fresh beets.log copy before each rm |
| T-04-11-05 | Tampering (supply chain) | pip -U beets at boot | accept | recorded residual; beets is never invoked by the hook after the strip |
</threat_model>

<verification>
Boot proof; deletions; freeze exit 0; quick-health-check exit 0 with drift green.
</verification>

<success_criteria>
Criteria 3 (static half, live) and 5 are true on the estate and asserted by standing checks.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md` when done.
</output>
