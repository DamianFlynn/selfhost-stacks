---
phase: 04-collapse-to-one-tagger
plan: 08
type: execute
wave: 3
depends_on: [04-03, 04-06]
files_modified:
  - scripts/spike03-discogs-probe.py
  - stacks/selfhosted/arrs/beets/beets.yaml
  - stacks/selfhosted/arrs/beets/config.yaml
autonomous: true
requirements: [TAGR-05]
tags: [beets, probe, vendoring, survivor]

must_haves:
  truths:
    - "F14 / D-16 / D-24: spike03-discogs-probe.py has an explicit `--mb-only` mode that does not require DISCOGS_USER_TOKEN, never calls the Discogs identity endpoint, never reads the `discogs` config view, and REFUSES to run if the discogs plugin is loaded — and that refusal branch has been driven (a scratch config that actually loads discogs is refused, naming discogs)"
    - "D-27: stacks/selfhosted/arrs/beets/config.yaml is a vendored minimal beets config declaring `plugins: musicbrainz`, `library: /config/library.db` and the three SAFE-01 `auto: no` keys — nothing else"
    - "D-27: beets.yaml bind-mounts the appdata copy of that config over /config/config.yaml `:ro`, and D-02's `restart: \"no\"`, `profiles: [\"manual\"]` and `/mnt/tank/media:/media:ro` are byte-unchanged"
    - "The new config contains no non-comment `/mnt/tank/media` string, no `image:` key, and is not git-ignored (three tools scan every yaml under stacks/selfhosted/); freeze section 2 is unchanged versus 04-06's recorded routine run"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets/config.yaml"
      provides: "vendored survivor beets config (D-27)"
      contains: "plugins: musicbrainz"
    - path: "scripts/spike03-discogs-probe.py"
      provides: "--mb-only mode"
      contains: "--mb-only"
  key_links:
    - from: "stacks/selfhosted/arrs/beets/beets.yaml"
      to: "/mnt/fast/appdata/arrs/beets/config/config.yaml"
      via: "single-file bind mount :ro"
      pattern: "/mnt/fast/appdata/arrs/beets/config/config.yaml:/config/config.yaml:ro"
---

<objective>
Prepare, in the repo only, everything the criterion-4 probe (04-09) needs: the probe's MB-only mode, so
the dismissed Discogs credential is never dragged into the survivor (F14, D-24), and the survivor's
vendored minimal beets config with its bind mount (D-27).

Purpose: TAGR-05 "every remaining beets config declares musicbrainz" becomes checkable in git; D-19's
probe must run through the mounts the shipped definition declares.
Output: three repo files, pushed and pulled; driven MB-only refusal transcripts. No host appdata is
changed here (install is 04-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-06-SUMMARY.md
@scripts/spike03-discogs-probe.py
@stacks/selfhosted/arrs/beets/beets.yaml
@stacks/selfhosted/arrs/sabnzbd/beets-config.yaml
@stacks/selfhosted/arrs/sabnzbd.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: --mb-only mode in spike03-discogs-probe.py, every Discogs site guarded</name>
  <files>scripts/spike03-discogs-probe.py</files>
  <read_first>
    - scripts/spike03-discogs-probe.py (sections: 100-111 THE THREE REFUSALS doc, 259-273 REQUIRED_ENV and --require-plugin doctrine, 461-483 require_env/SECRETS, 500-541 config + load_plugins + refusal 1, 690-750 record emission incl. "source", 816-866 epilog/argparse, 870-910 main incl. identity probe ~895 and discogs config reads ~897-898, 938-940 banner, ~1072 summary)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § scripts/spike03-discogs-probe.py (the site table)
  </read_first>
  <action>
    Add `--mb-only` (store_true) in the argparse block beside `--require-plugin`, with help text stating it drops the Discogs token requirement and identity probe and refuses if discogs is loaded. Guard every site in the PATTERNS table: REQUIRED_ENV becomes a function of the flag (MB-only requires SPIKE_CONFIG and SPIKE_DB only); `require_env()` does not look up or append DISCOGS_USER_TOKEN to SECRETS; the `config["discogs"]["user_token"]` assignment is skipped; refusal 1 gains an inverse check placed immediately after `loaded = sorted(p.name for p in plugins.find_plugins())` and before any identity probe or lookup — in MB-only mode, if `discogs` is among the loaded plugins, refuse with exit 2 and a message naming `discogs` as loaded and MB-only as the reason (Pitfall 9); the `discogs_identity_probe(...)` call is skipped; the unconditional `config["discogs"]["index_tracks"]` / `["search_limit"]` reads (~897-898) are skipped; banner and summary print `MB-only: no identity probe` instead of `identity[...]`; epilog lists the MB-only requirements. The `--require-plugin` gate is unchanged (MB-only runs still state their plugin expectation on the command line). Do not change behaviour when `--mb-only` is absent. `python3 -m py_compile`.
    Commit (`feat(04-08): MB-only mode for the tag_album probe (F14)`).
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/spike03-discogs-probe.py && grep -c -- '--mb-only' scripts/spike03-discogs-probe.py</automated>
  </verify>
  <acceptance_criteria>
    - `py_compile` exits 0; `grep -v '^\s*#' … | grep -c 'mb_only'` ≥ 7 (one per guarded site: env, secrets, token assignment, refusal inverse, identity probe, discogs config reads, banner/summary).
    - `git diff` shows no change to the non-MB-only path's behaviour: every edited site is inside an `if args.mb_only` / `if not args.mb_only` branch or a function taking the flag.
    - In-image smoke after Task 2's push (record in SUMMARY): `docker run --rm --pull never --network none --entrypoint python3 -v /mnt/fast/stacks/scripts:/w:ro lscr.io/linuxserver/beets:2.13.1-ls349 /w/spike03-discogs-probe.py --mb-only --ledger /tmp/x /nonexistent` with no SPIKE_* env → exits non-zero naming SPIKE_CONFIG and NOT naming DISCOGS_USER_TOKEN.
    - The refuse-if-discogs-loaded branch is driven in Task 2 (control (r)).
  </acceptance_criteria>
  <done>The probe can prove MusicBrainz behaviour without touching the dismissed credential.</done>
</task>

<task type="auto">
  <name>Task 2: Vendored survivor config (D-27) and its :ro bind mount; push and pull; drive the MB-only refusal (REVIEWS row 13)</name>
  <files>stacks/selfhosted/arrs/beets/config.yaml, stacks/selfhosted/arrs/beets/beets.yaml</files>
  <read_first>
    - stacks/selfhosted/arrs/sabnzbd/beets-config.yaml lines 1-22 (header/provenance shape) and 110-144 (the SAFE-01 block to copy verbatim)
    - stacks/selfhosted/arrs/sabnzbd.yaml lines 56-95 (NOTE-block house style, single-file bind inside a directory bind)
    - stacks/selfhosted/arrs/beets/beets.yaml (whole, as left by 04-03)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § NEW stacks/selfhosted/arrs/beets/config.yaml (three hard content constraints)
    - 04-01-SUMMARY.md (sha256 of the host config.yaml it replaces) and 04-06-SUMMARY.md (the routine run's `declared rw reaching Music` value)
  </read_first>
  <action>
    Create `stacks/selfhosted/arrs/beets/config.yaml`: header comment in the sabnzbd beets-config.yaml shape — consumed by container `beets` (survivor, dormant, manual profile); VENDORED <today> (plan 04-08, TAGR-05 / D-27); repo copy is for review, the appdata copy `/mnt/fast/appdata/arrs/beets/config/config.yaml` is authoritative at runtime and is drift-checked byte-for-byte by quick-health-check.sh's vendored-file block, which plan 04-10 builds as a candidate check and plan 04-11 promotes into the routine check (name both plans; do not claim the check exists at this commit); AUTHORED, not seeded: it REPLACES a host file that was a pasted compose service definition (name its sha256 from 04-01-SUMMARY, fenced in `/mnt/fast/safety/music-pre-project/audit/`); `library:` is explicit so the path the survivor opens is declared, not defaulted (D-28's fresh DB is created there); everything else about the survivor's configuration belongs to Phase 6; credential screen statement (none present). Body, nothing else: `plugins: musicbrainz`, `library: /config/library.db`, and the `scrub:`/`lastgenre:`/`embedart:` blocks with `auto: no` and their explanatory comments copied verbatim from the sabnzbd config's SAFE-01 block. Constraints: no non-comment line containing `/mnt/tank/media`; no `image:` key; path not under a `beets-config/` directory.
    beets.yaml: after the `/mnt/fast/appdata/arrs/beets/config:/config:rw` line insert a NOTE block (four parts: what it is/who consumes it; why vendored — D-27, TAGR-05, the replaced host file was not a beets config; what breaks if reverted — LSIO's default config arms scrub/lastgenre and omits musicbrainz; how much `:ro` protects and that LSIO's boot-time `lsiown -R` may log EROFS on it, to be measured by 04-09) followed by `- /mnt/fast/appdata/arrs/beets/config/config.yaml:/config/config.yaml:ro  # Vendored beets config (see above)`. Leave `restart`, `profiles`, and the `/mnt/tank/media:/media:ro` line and its NOTE byte-identical.
    Gates: `docker compose -f stacks/selfhosted/arrs/beets/beets.yaml --env-file stacks/selfhosted/arrs/.env --profile manual config --quiet` → 0; `git check-ignore stacks/selfhosted/arrs/beets/config.yaml` → nothing.
    Commit (`feat(04-08): vendored survivor beets config declaring musicbrainz (D-27)`), push, `git -C /mnt/fast/stacks pull --ff-only` on LXC 100, record host HEAD, then:
    (s) the Task 1 in-image smoke;
    (r) the MB-only refusal control (REVIEWS row 13): on LXC 100 create `/mnt/fast/scratch-04/08/` containing `discogs-loaded.yaml` with `plugins: musicbrainz discogs`, `library: /s/x.blb` and a `discogs:` mapping whose `user_token` is the fixed non-credential literal `phase04-fake-not-a-credential` (the discogs plugin raises during setup without a token and would then not load, which would make the control vacuous; a user token makes setup construct its client offline). Run `docker run --rm --pull never --network none --entrypoint python3 -e SPIKE_CONFIG=/s/discogs-loaded.yaml -e SPIKE_DB=/s/x.blb -v /mnt/fast/scratch-04/08:/s -v /mnt/fast/stacks/scripts:/w:ro lscr.io/linuxserver/beets:2.13.1-ls349 /w/spike03-discogs-probe.py --mb-only --require-plugin musicbrainz --ledger /s/led /s` → MUST exit 2 with a refusal naming `discogs` as loaded; it must not name DISCOGS_USER_TOKEN and must emit no ledger records. Paired positive: the same command against `/s/mb-only.yaml` (identical but `plugins: musicbrainz` and no `discogs:` mapping) must NOT refuse for discogs (it may stop later for the empty folder; record its exit and first error line). If discogs does not appear among the loaded plugins in the refusing run, HALT and report — do not weaken the branch or the control. Remove `/mnt/fast/scratch-04/08` (S5 guard: realpath equals that exact path) and assert gone;
    (c) on the host `bash scripts/check-music-freeze.sh` (routine, no env) — its section 2 `declared rw reaching Music` value must equal 04-06's recorded routine value (the new yaml adds no library mount), and its exit code and `❌`/`⚠️` set stay within the 04-01 ROUTINE BASELINE;
    (y) parse the new config inside the resident image: `docker run --rm --pull never --network none -v /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml:/c.yaml:ro --entrypoint python3 lscr.io/linuxserver/beets:2.13.1-ls349 -c "import yaml; d=yaml.safe_load(open('/c.yaml')); print(sorted(d))"` → prints exactly `['embedart', 'lastgenre', 'library', 'plugins', 'scrub']`.
  </action>
  <verify>
    <automated>c=stacks/selfhosted/arrs/beets/config.yaml; b=stacks/selfhosted/arrs/beets/beets.yaml; test -f $c && test -f $b && test "$(grep -v '^\s*#' $c | grep -c '/mnt/tank/media')" = 0 && test "$(grep -cE '^\s*image:' $c)" = 0 && grep -cx 'plugins: musicbrainz' $c && grep -c '/mnt/fast/appdata/arrs/beets/config/config.yaml:/config/config.yaml:ro' $b && grep -c '/mnt/tank/media:/media:ro' $b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cx 'plugins: musicbrainz'` = 1 and `grep -cx 'library: /config/library.db'` = 1 in config.yaml; `grep -c 'auto: no'` = 3.
    - In-image parse prints exactly `['embedart', 'lastgenre', 'library', 'plugins', 'scrub']`.
    - beets.yaml: new mount line present once; `restart: "no"`, `profiles: ["manual"]`, `/mnt/tank/media:/media:ro` each present once (unchanged).
    - compose `config --quiet` exit 0; `git check-ignore` empty; host HEAD = origin HEAD recorded.
    - Control (r): discogs-loaded run exits 2 naming `discogs`, no DISCOGS_USER_TOKEN in its output, no ledger records; paired run not refused for discogs; `/mnt/fast/scratch-04/08` absent afterwards.
    - Freeze section 2 `declared rw reaching Music` value equal to 04-06's recorded routine value; routine run within the 04-01 baseline.
  </acceptance_criteria>
  <done>The survivor's declared config names musicbrainz in git, and the probe is ready to run MB-only — with its discogs refusal seen to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → survivor container | vendored config governs a tagger with read access to the library |
| probe → MusicBrainz / Discogs | outbound API calls; Discogs must not be reached in MB-only mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information disclosure | Discogs token | mitigate | MB-only never reads DISCOGS_USER_TOKEN, never appends to SECRETS, refuses if discogs loaded (driven with a fixed non-credential literal, `--network none`) (D-24 kept out of scope) |
| T-04-08-02 | Tampering | survivor config | mitigate | SAFE-01 `auto: no` keys verbatim; `:ro` bind; no `/mnt/tank/media` string |
| T-04-08-03 | Elevation of privilege | beets.yaml | mitigate | D-02/Phase 1 D-20 lines asserted unchanged; nothing grants rw on Music |
| T-04-08-04 | Information disclosure | compose validation | mitigate | `config --quiet` only |
</threat_model>

<verification>
py_compile, in-image smoke, driven MB-only refusal, in-image YAML key list, compose config quiet, freeze section 2 unchanged versus 04-06.
</verification>

<success_criteria>
TAGR-05's survivor half is declared in git; the probe instrument exists in the shape D-16 needs, and its new refusal has been proven capable of firing.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-08-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS rows 5 and 13; the full table is in 04-01-PLAN.md.
- **Row 5**: `depends_on` is now `[04-03, 04-06]`, so the plan moves from wave 2 to wave 3. Task 2 (c) compares section 2 against 04-06's recorded routine run, which therefore always exists. The survivor config header also names 04-10 (candidate) and 04-11 (promotion) for the drift check.
- **Row 13**: Task 2 (r) drives the refuse-if-discogs-loaded branch with a scratch config that actually loads discogs, and pairs it with a positive run.
