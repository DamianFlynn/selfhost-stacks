---
phase: 04-collapse-to-one-tagger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
autonomous: true
requirements: [TAGR-03, TAGR-04]
tags: [fence, baseline, sqlite, host-readonly]

must_haves:
  truths:
    - "D-35: /mnt/fast/appdata/media/wrtag/data/wrtag.db has an sqlite3 .backup copy in /mnt/fast/safety/music-pre-project/library-db/ whose PRAGMA integrity_check prints exactly ok, with a sha256 line appended to MANIFEST.txt — BEFORE any plan deletes media/wrtag"
    - "D-04 / D-28 / D-32: the Phase 1 fence copies of the survivor's library.db and musiclibrary.blb and of sabnzbd's two primary beets DBs are re-confirmed by sha256 against MANIFEST.txt AND PRAGMA integrity_check ok, recorded per source path; and a per-file coverage preview records which sabnzbd library.blb* / .config/beets/* files already have a MANIFEST sha256 match (plan 04-11 fences the rest at deletion time)"
    - "Pitfall 13: sabnzbd beets.log (the only record of the 1 Aug / 8 Aug / 6 Sep skips) is copied into the fence audit area, NOT into the public repo, with a MANIFEST sha256 line"
    - "The survivor's host config.yaml (compose junk, F4) and config.yaml.old (LSIO default) are copied into the fence audit area with sha256 lines before plan 04-07/04-09 replace or delete them"
    - "D-29: the probe album 'Garth Brooks-Scarecrow-CD-FLAC-2001-FLACME-xpost' is re-asserted present with exactly 12 FLAC files and no disc subdirectory, with its container-side path derived from beets.yaml's declared volume line, or its absence is recorded as a blocker for plan 04-09"
    - "Invariant 1 baseline: the routine check-music-freeze.sh and quick-health-check.sh exit codes and every ❌/⚠️ line are recorded before any Phase 4 change, so every later wave boundary can be compared against the pre-phase state"
    - "D-12 / D-31: 04-D12-EVIDENCE.md exists BEFORE any job runs and pre-declares the residual Audio.txt lines, the transient beets-match file, the baseline 'Completed' + 'Exit(1): chmod' SAB row shape as NOT regressions, the pre-hook byte-comparison method for 'untagged', and the ReplaygainTagging=\"false\" window prerequisite"
  artifacts:
    - path: ".planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md"
      provides: "Pre-declared D-12 evidence checklist with the measured baseline"
      contains: "Unable to match using beets"
  key_links:
    - from: "/mnt/fast/safety/music-pre-project/library-db/MANIFEST.txt"
      to: "wrtag.db fence copy"
      via: "appended sha256 line"
      pattern: "wrtag"
---

<objective>
Close the two fence gaps research found (F2: wrtag.db not fenced; Pitfall 13: beets.log is irreplaceable
evidence). Re-confirm every Phase 1 fence copy this phase relies on before deleting anything. Record the
measured baseline, including the routine health checks' pre-phase state, that every later plan compares
against. Write the D-12 evidence checklist before any job can be read after the fact.

Purpose: D-04's precondition ("delete only after confirming the fence holds a copy") fails for wrtag.db
as written (D-35). Nothing irreversible happens in this plan; it is what makes every later deletion
legitimate, and what lets every wave boundary be checked against "no worse than before the phase".
Output: fence copies + MANIFEST lines on LXC 100; `04-D12-EVIDENCE.md`; baseline values in
`04-01-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@scripts/freeze-music-apply.sh
</context>

<conventions>
- Every host command runs over `ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159` with a
  Linux-side `timeout` and `set -o pipefail` in the remote string; read the ssh RC on the next line.
- This repo is PUBLIC. Nothing written to the repo may contain release folder names beyond the D-29
  album (already public in CONTEXT.md), credential values, or verbatim beets.log / SAB history dumps.
  SAB `script_line` values are recorded truncated before the release name.
- READ-ONLY on the estate except the fence writes named in Task 2. No container is started, stopped,
  pulled or recreated. No `beet` command is run.
- Stage commits by explicit path only. The working tree carries unrelated changes
  (`.planning/phases/03-tagger-spike/03-ERGONOMICS-SHEET.md`, `body.txt`) — never `git add -A`/`.`.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Measure the baseline, read-only (F1, F2, F3, F5, F8, PR #310, routine health-check state)</name>
  <files>(none — host reads only; values go to 04-01-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md (§ ⚠ Facts F1–F15, § Runtime State Inventory, § Code Example 8)
    - .planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md (rows C3-c, C4-b, C5-b)
    - .planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md § Consensus Summary rows 1, 4
    - scripts/freeze-music-apply.sh lines 295-304, 408-445 (fence copy + MANIFEST convention)
    - stacks/selfhosted/arrs/beets/beets.yaml (the downloads volume line, for item e)
  </read_first>
  <action>
    On LXC 100, read-only, record into the SUMMARY:
    (a) host checkout: `git -C /mnt/fast/stacks rev-parse --short HEAD` and `git -C /mnt/fast/stacks status --porcelain | wc -l`;
    (b) containers, NO status filter: `docker ps -a --format '{{.Names}}'` — capture its RC first (non-zero or empty output = UNKNOWN, never zero); count of lines matching `wrtag|soulbeet|^beets$` (expected 0) and total container count;
    (c) resident images for `sentriz/wrtag`, `lscr.io/linuxserver/beets`, `metasauce/beets-flask`, `ghcr.io/linuxserver/sabnzbd` with image IDs;
    (d) for each file below: absolute path, size, mtime, sha256 — `/mnt/fast/appdata/media/wrtag/data/wrtag.db`; `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb` and every `library.blb*` sibling (list the actual `.bak` names — do not guess the suffix); `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets.log`; every file under `/mnt/fast/appdata/arrs/sabnzbd/config/.config/beets/` (recursive); `/mnt/fast/appdata/arrs/beets/config/library.db`; `/mnt/fast/appdata/arrs/beets/config/musiclibrary.blb`; `/mnt/fast/appdata/arrs/beets/config/config.yaml`; `/mnt/fast/appdata/arrs/beets/config/config.yaml.old`; `/mnt/fast/appdata/arrs/beets/config/state.pickle` (if present); `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` (expect sha256 prefix `fdcddca234b2`, 339 lines); `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml` (expect equal to `sha256sum stacks/selfhosted/arrs/sabnzbd/beets-config.yaml` in the local repo); `/mnt/fast/appdata/arrs/sabnzbd/config/extended.conf` (sha256, mtime, and the literal `ReplaygainTagging=` line); list of `/mnt/fast/appdata/arrs/soulbeet/` recursively (expect `data/` empty + `beets_config.yaml`), and sha256 of its `beets_config.yaml` compared with the repo copy;
    (e) D-29 presence: `find /mnt/tank/downloads -maxdepth 5 -type d -name 'Garth Brooks-Scarecrow-CD-FLAC-2001-FLACME-xpost'` must return exactly one path; in it, count `*.flac` (expect 12) and count subdirectories containing audio (expect 0). Derive the container-side path from the survivor's declared downloads volume line in `stacks/selfhosted/arrs/beets/beets.yaml` (replace that line's host prefix with its container prefix as declared; do not assume `/mnt/tank/downloads` maps to `/downloads`) and record both paths and the volume line used. If absent or not 12/single-disc, record `D-29 BLOCKER` prominently in the SUMMARY — plan 04-09 must not substitute an album without an operator ruling;
    (f) SAB baseline (F8): `sqlite3 "file:/mnt/fast/appdata/arrs/sabnzbd/config/admin/history1.db?mode=ro"` selecting `datetime(completed,'unixepoch')`, `status`, `substr(script_line,1,60)` for the last 8 rows with `category='music'` — record only those three columns (the truncated script_line must stop before any release name); also record the `PRAGMA table_info(history)` column names (plan 04-12 maps a history row to its job folder through the final-path column);
    (g) beets.log skip evidence: `grep -c` of lines dated 2026-08-01, 2026-08-08 and 2026-09-06 containing `skip`, and the single line naming Scarecrow (quote that one line only);
    (h) `gh pr view 310 --json state,title,labels` and `gh issue view 306 --json state` on the workstation;
    (i) ROUTINE BASELINE (REVIEWS row 1 — the pre-phase state every wave boundary is compared against): on LXC 100 `cd /mnt/fast/stacks && timeout 150 bash scripts/check-music-freeze.sh` — record its exit code, elapsed seconds, and every line containing `❌` or `⚠️` (ANSI stripped); on the workstation `bash scripts/quick-health-check.sh` — record its exit code and every `❌`/`⚠️` line. Record the host HEAD they ran against. If either is non-zero today, that is the baseline, recorded as-is — this plan does not fix it.
    Any "could not look" (ssh non-zero, RC 124, empty output) is recorded as UNKNOWN, never as absence.
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; timeout 30 sha256sum /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash | cut -c1-12" | grep -qx fdcddca234b2 && echo BASELINE-AUDIO-OK</automated>
  </verify>
  <acceptance_criteria>
    - 04-01-SUMMARY.md contains a table with one row per path in (d), each with a 64-hex sha256 or the literal `ABSENT` / `UNKNOWN`.
    - SUMMARY records host HEAD short SHA and the tagger-container count from an unfiltered `docker ps -a`.
    - SUMMARY records D-29 as `PRESENT 12 FLAC single-disc <container path> (derived from beets.yaml line <n>)` or `D-29 BLOCKER`.
    - SUMMARY records ≥1 SAB music row with status and a script_line prefix, with no release folder name in it, and the history table's column names.
    - SUMMARY has a `ROUTINE BASELINE` section with both scripts' exit codes, the freeze script's elapsed seconds, and their `❌`/`⚠️` lines (or `none`).
    - `git status --porcelain` shows no repo file changed by this task.
  </acceptance_criteria>
  <done>Every later plan has a measured before-value to compare against, including the routine health-check state; nothing on the estate changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fence wrtag.db (D-35), beets.log (Pitfall 13) and the survivor configs; re-confirm Phase 1 fence copies; preview sabnzbd backup coverage (D-04/D-28/D-32)</name>
  <files>(host only: /mnt/fast/safety/music-pre-project/library-db/, /mnt/fast/safety/music-pre-project/audit/, MANIFEST.txt)</files>
  <read_first>
    - scripts/freeze-music-apply.sh lines 295-304 (sqlite3 .backup), 408-433 (verify: sha OR integrity_check ok), 437-445 (MANIFEST header)
    - /mnt/fast/safety/music-pre-project/library-db/MANIFEST.txt on LXC 100 (read its header and existing line format before appending)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Code Example 5
  </read_first>
  <action>
    On LXC 100:
    1. Read MANIFEST.txt's header and match its line format exactly when appending. Check whether an `audit/` sibling directory exists under `/mnt/fast/safety/music-pre-project/`; if not, create it with the same owner/mode as `library-db/` (record both).
    2. D-35: `sqlite3 /mnt/fast/appdata/media/wrtag/data/wrtag.db ".backup '<fence>/library-db/media_wrtag_data_wrtag.db'"`; then `sqlite3 <copy> 'PRAGMA integrity_check;'` must print exactly `ok`; append a MANIFEST line with the copy's sha256, source path, UTC timestamp and the note `phase 04 D-35`. Refuse to proceed if the destination already exists with different content (do not overwrite a fence file).
    3. Pitfall 13: `cp -p` sabnzbd `scripts/beets.log` to `<fence>/audit/sabnzbd-beets.log.<UTC stamp YYYYmmddTHHMMSSZ>`; append a MANIFEST line with its sha256 (must equal the source's).
    4. `cp -p` the survivor's `config.yaml` and `config.yaml.old` to `<fence>/audit/arrs-beets-config.yaml.<stamp>` and `<fence>/audit/arrs-beets-config.yaml.old.<stamp>`; MANIFEST lines with sha256.
    5. Re-confirm (read-only) the Phase 1 copies: for each of survivor `library.db`, survivor `musiclibrary.blb`, sabnzbd `scripts/library.blb`, sabnzbd `.config/beets/library.db`, find its MANIFEST line by source path, recompute the fence file's sha256 and compare to MANIFEST, and run `PRAGMA integrity_check` on the fence copy (must print `ok`). Record per source: `fence file | MANIFEST sha matches (y/n) | integrity ok (y/n)`. Per D-04 these Phase 1 copies are the confirmation for the survivor DBs — do NOT take fresh archives of them.
    6. Coverage preview for plan 04-11's deletion ledger (REVIEWS row 4), read-only: for EVERY file Task 1 (d) listed under sabnzbd `scripts/library.blb*` and `.config/beets/`, record whether some MANIFEST line carries that file's live sha256 (`covered by MANIFEST line <n>` / `not covered`). Take NO fresh copies here — `library.blb*` is regenerated on every music job until the stripped audio.bash is live, so plan 04-11 fences the uncovered files at deletion time.
    7. Confirm no container mounts `/mnt/fast/safety`: `bash /mnt/fast/stacks/scripts/check-music-freeze.sh` section 6 line `no running container mounts any path under /mnt/fast/safety` (record its exit code and the fence-section lines only; its full routine state was recorded in Task 1 (i)).
    If any integrity check is not `ok` or any MANIFEST sha mismatches, record it as a BLOCKER for the deletion of that source in plan 04-07 / 04-11 and continue with the rest.
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; D=/mnt/fast/safety/music-pre-project/library-db; test \"\$(timeout 30 sqlite3 \$D/media_wrtag_data_wrtag.db 'PRAGMA integrity_check;')\" = ok && grep -c 'media_wrtag_data_wrtag.db' \$D/MANIFEST.txt"</automated>
  </verify>
  <acceptance_criteria>
    - Remote `sqlite3 <fence>/library-db/media_wrtag_data_wrtag.db 'PRAGMA integrity_check;'` prints exactly `ok`.
    - `grep -c media_wrtag_data_wrtag.db <fence>/library-db/MANIFEST.txt` ≥ 1, and that line's sha256 equals `sha256sum` of the copy.
    - `<fence>/audit/` contains exactly one `sabnzbd-beets.log.*`, one `arrs-beets-config.yaml.*`, one `arrs-beets-config.yaml.old.*` from this run, each with a MANIFEST line whose sha256 equals the file's.
    - SUMMARY carries the 4-row re-confirmation table with no `n` entries, or each `n` is marked BLOCKER.
    - SUMMARY carries the coverage-preview table with one row per sabnzbd `library.blb*` and `.config/beets/*` file (row count equals the number of such files Task 1 listed).
    - No file under `/mnt/fast/appdata` changed: sha256 of every path in Task 1 (d) is unchanged.
  </acceptance_criteria>
  <done>Every database and config this phase deletes has a verified copy in the fence or is named as uncovered for 04-11's ledger, and wrtag.db's copy exists for the first time.</done>
</task>

<task type="auto">
  <name>Task 3: Write the D-12 evidence checklist before any job is read (D-12, D-31, F7, F8)</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § audio.bash anatomy, Pitfalls 2 and 3, Code Example 8
    - .planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md row C3-c
    - .planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md § Consensus Summary row 11
    - 04-01-SUMMARY.md values from Task 1 (d) extended.conf and (f)
  </read_first>
  <action>
    Create `04-D12-EVIDENCE.md` with these sections, dated and attributed to plan 04-01:
    (1) **Pass condition, stated before the job**: one real `music` SAB job completing after the stripped audio.bash is live (plan 04-11) such that ALL of: SAB history status `Completed` and script_line beginning `Exit(1): chmod: changing permissions of '/downloads/complete/nzb/music/` (the F8 baseline — `audio.bash:334` `chmod 777 "$1"` EPERM on tank under `set -e`, present on every music job since at least 2026-08-01, NOT fixed in this phase per D-31); no `library.blb`, no `library.blb*` backup, no `beets.log` under `/mnt/fast/appdata/arrs/sabnzbd/config/scripts` newer than the stamp; no `.bak` files created anywhere under sabnzbd `/config`; and **untagged, proven by bytes, not inferred** — every audio file present in both the job's PRE-HOOK snapshot and its COMPLETION snapshot is byte-identical by sha256. The PRE-HOOK snapshot is the first stable snapshot (two consecutive identical size/mtime passes) that plan 04-12's read-only watcher takes of the job folder, and it is valid only if it precedes the job's `Matching N tracks with Beets` line in Audio.txt (the stage where the stripped invocation used to run). The COMPLETION snapshot is taken at the first watcher poll after the job's SAB history row appears. `MUSICBRAINZ_*` tags present in a byte-identical file came with the release and are recorded as such; files present in PRE-HOOK but absent at COMPLETION are recorded (clean() deletes non-audio files and MP3s when FLAC is present) and are not a failure; any file whose bytes changed, or any audio file that appears only at COMPLETION, is a FAIL. File mtimes are NOT used as tag provenance.
    (2) **Pre-declared residual lines (D-31/F7), which are NOT evidence beets ran**: `Matching N tracks with Beets` and `ERROR: Unable to match using beets to a musicbrainz release` in `/config/logs/Audio.txt`, because the `beets()` function body and the `BeetsTagging` gate at lines 322-324 remain by D-10's letter; and `/config/scripts/beets-match`, which line 281 touches and lines 297-300 remove within the same run — its transient existence and the resulting `/config/scripts` directory mtime change are expected. `beets()`'s `rm` of `library.blb` at lines 272-275 is existence-guarded, so an absent `library.blb` does not abort the hook.
    (3) **Window prerequisite, asserted not assumed**: "untagged" depends on `extended.conf` holding `ReplaygainTagging="false"` for the whole window (r128gain is the only other tag writer; research § D-10 inventory). Plan 04-12 asserts that line at window open AND at window close, with `extended.conf`'s sha256 and mtime unchanged between them (baseline values from Task 1 (d)). If the line is absent at either end or the file changed, PASS is unavailable and criterion 3 is recorded OPEN with the reason.
    (4) **Baseline rows** from Task 1 (f): the table of date/status/truncated script_line.
    (5) **Window rules** (orchestrator decision): if no music job arrives in the window plan 04-12 opens, criterion 3 is recorded OPEN, not passed. If a job arrives but no valid PRE-HOOK snapshot exists for it (watcher not running, or the first stable snapshot is later than the job's `Matching` line), the untagged condition is UNPROVEN and criterion 3 is recorded OPEN, not passed.
    (6) An empty **Result** section with placeholders for: stamp time (UTC), SAB row, `find -newer` output, Audio.txt lines, PRE-HOOK/COMPLETION snapshot times and per-file comparison summary, extended.conf open/close values, ffprobe tag-key sets, verdict — to be filled by plan 04-12. Note the timezone trap: SAB `completed` is UTC, `sabnzbd.log`/`Audio.txt` are Europe/Dublin.
    No release names except the D-29 album; no credential values.
    Commit with only this path staged: `docs(04-01): D-12 evidence checklist and baseline`.
  </action>
  <verify>
    <automated>f=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; test -f $f && grep -c 'Unable to match using beets' $f && grep -c 'beets-match' $f && grep -c 'Exit(1): chmod' $f && grep -c 'PRE-HOOK' $f && grep -c 'ReplaygainTagging' $f</automated>
  </verify>
  <acceptance_criteria>
    - File exists; each of `Matching`, `Unable to match using beets`, `beets-match`, `Exit(1): chmod`, `ReplaygainTagging`, `OPEN`, `PRE-HOOK`, `byte-identical`, `UNPROVEN` appears ≥1 time.
    - The file states that file mtimes are not used as tag provenance (`grep -c 'mtimes are NOT used'` = 1).
    - The Result section exists and is unfilled (contains the literal `PENDING — filled by 04-12`).
    - `git log -1 --name-only` lists only `04-D12-EVIDENCE.md`.
  </acceptance_criteria>
  <done>A verifier reading plan 04-12's evidence cannot mistake the residual log lines or the baseline Exit(1) for a regression, and "untagged" has a pre-declared byte-level test with an honest OPEN path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 appdata → fence | copies of live SQLite DBs and logs cross into the recovery fence |
| host evidence → public repo | measured values are transcribed into a public repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | fence writes | mitigate | never overwrite an existing fence file; `.backup` + `PRAGMA integrity_check` = ok before a MANIFEST line is appended |
| T-04-01-02 | Information disclosure | 04-D12-EVIDENCE.md / SUMMARY | mitigate | script_line truncated before release names; beets.log copied to the fence, never the repo; only the D-29 line quoted |
| T-04-01-03 | Repudiation | later deletions | mitigate | per-source re-confirmation table and per-file coverage preview recorded here; plans 04-07/04-11 check them before any `rm` |
| T-04-01-04 | Tampering | live DB read | accept | `sqlite3 .backup` and `mode=ro` reads take shared locks only; wrtag has no container writing it |
| T-04-01-05 | Repudiation | wave-boundary regressions | mitigate | routine-check baseline (exit codes + ❌/⚠️ lines) recorded before any change |
</threat_model>

<verification>
- Remote integrity check on the wrtag fence copy prints `ok`; MANIFEST carries its line.
- `04-D12-EVIDENCE.md` committed with pre-declared lines and the pre-hook byte-comparison method.
- SUMMARY baseline table complete; D-29 status explicit; routine baseline recorded.
</verification>

<success_criteria>
The fence holds a verified copy of every database and config this phase will delete (wrtag.db for the
first time), coverage of the regenerated sabnzbd files is known, the routine checks' pre-phase state is
recorded, and the D-12 pass condition exists in writing before any job is observed.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md` when done.
</output>

## Review Response

Revised 2026-09-11 against `04-REVIEWS.md` § Consensus Summary. Every graded row is addressed. The
refuted items are not re-introduced. The lines at `audio.bash:272-275` are existence-guarded, so no
sacrificial `library.blb` is kept and 04-12's pass line is not widened. The survivor's zero-DB state
in wave 3 is harmless. The 6b census already `cd`s to REPO_ROOT.

| Row | Severity | What changed | Where |
|-----|----------|--------------|-------|
| 1 | HIGH | The census (6b) and drift blocks become **candidate** checks, gated by the in-script constants `TAGGER_CENSUS_PROMOTED=0` / `VENDORED_DRIFT_PROMOTED=0`. Plans invoke them directly with `CENSUS_CANDIDATE=1` / `VENDORED_DRIFT_CANDIDATE=1`, which can only add a failure. They are promoted into the fatal routine path only by 04-11, in the commit where they first run green. Every wave boundary compares the routine run against the pre-phase baseline, and the success-producing overrides are removed (`SURVIVOR_DB`/`APPDATA_ROOT` become constants; a non-default `DRIFT_APPDATA_ROOT` forces red) | 04-01 T1(i) baseline; 04-06 T1–T3; 04-07 T1; 04-09 T3; 04-10 T3; 04-11 T3 |
| 2 | HIGH | Absence verifications capture producer status first and require a positive control (e.g. `sabnzbd` present in the container list, the dashboard title, `audio.bash` present). They distinguish success-with-empty-output, explicit not-found (`No such image`), transport error and timeout. The error branch is driven with `DOCKER_HOST=unix:///nonexistent-04.sock` and `GH_TOKEN=invalid-04` | 04-07 T1 action + T2 verify; 04-09 T3 verify; 04-13 T2 verify |
| 3 | HIGH | Both D-17 HALT preconditions (album present; live broken-config sha = 04-01) move before `up`. Start → fresh DB → probes → `down` runs as one host process under `trap cleanup EXIT` plus INT/TERM/HUP traps. Long steps run as `cmd & wait $!` so signals land promptly, under an outer Linux-side `timeout -k`. A driven TERM-after-up control proves the failure path, and an independent dormancy check runs regardless of the lifecycle's exit | 04-09 T1–T3 |
| 4 | MEDIUM | A per-file fence ledger covers every `library.blb*`, `beets.log` and `.config/beets/*` file. A MANIFEST sha match counts as fenced; otherwise the file gets a fresh `.backup`/`cp -p`, `integrity_check ok` and a MANIFEST line. Deletion is file-by-file from the ledger, then `rmdir`; `rm -rf` is never used, so an unledgered file blocks the delete. 04-01 T2 adds a coverage preview | 04-01 T2 step 6; 04-11 T2 |
| 5 | MEDIUM | 04-08 `depends_on: [04-03, 04-06]` → wave 3. Waves re-derived; no other plan's wave moves | 04-08 frontmatter; ROADMAP |
| 6 | MEDIUM | 04-13 writes `## Phase 4 — one tagger` only on `Verdict: PASS`; on OPEN or FAIL it writes `## Phase 4 — interim status`, which states that the phase is not closed | 04-13 T1 |
| 7 | MEDIUM | Classification is separated from write authorisation. Every container in every state with rw reaching Music fails except Jellyfin, and a new counter `rw on Music, tagger-capable` is fatal. The control is a created and then exited tagger-class fixture container, pointed at a scratch library root via the additive `CENSUS_EXTRA_LIBRARY_ROOTS` so the real library is never mounted rw | 04-06 T1 (iv), T3 (e) |
| 8 | MEDIUM | The census `find` captures its RC (`|| FIND_RC=$?`, the house `|| true` shape at `check-music-freeze.sh:361/366/442`). RC 0/1 are accepted; 124 or anything else → UNKNOWN; a missing Jellyfin positive-control DB → UNKNOWN, fail | 04-06 T1 (ii) |
| 9 | MEDIUM | The expected-red lists name `beets databases ≠ 1` with sabnzbd's `library.blb`, `.config/beets/library.db` and their `.bak` until 04-11, labelled as the candidate check. The executor is told never to narrow the classifier to match the prose | 04-07 T1; 04-09 T3 |
| 10 | MEDIUM | 04-06's verify captures the candidate run's RC and asserts it equals 1. It no longer pipes an expected exit 1 through `pipefail` | 04-06 T2 verify |
| 11 | MEDIUM | 04-12 adds a read-only watcher that takes PRE-HOOK and COMPLETION sha256 snapshots of the job's audio files. `ReplaygainTagging="false"` and `extended.conf`'s sha256/mtime are asserted at window open and close. The mtime-provenance reasoning is dropped. The checklist declares the method in advance | 04-01 T3; 04-12 T1, T3 |
| 12 | LOW | `tagger definitions` is driven red by a fixture yaml staged in a scratch clone of the host checkout (REPO_ROOT follows the script location) | 04-06 T3 (d) |
| 13 | LOW | `--mb-only`'s refuse-if-discogs-loaded branch is driven in the resident image, `--network none`. The scratch config loads `discogs` with a fixed non-credential token, so the plugin actually loads | 04-08 T2 |
| 14 | LOW | `is_tagger_mount` matches `beets-config.yaml`, `beets_config.yaml`, `library.db*`, `*.blb*`, `wrtag.db*` and `soulbeet.db*`. The fixture in (e) uses the underscore name | 04-06 T1, T3 (e) |
| 15 | LOW | If Renovate has moved the repo past ls349, 04-09 is named as D-30's deliberate redeploy. It records the digest and image ID, and `beet version` = 2.13.1 is asserted in a `--rm` container before `up` | 04-09 T1; 04-13 T2 |
| 16 | LOW | The `state.pickle` baseline is taken post-`up` (after the fresh DB exists), and the post-probe comparison uses it | 04-09 T2, T3 |
| 17 | LOW | 04-06 records the candidate run's elapsed time (`comfortably` = ≤ 60 s, without dropping the Jellyfin control). If 04-06 or 04-11 measures more, 04-11's promotion commit raises `REMOTE_TIMEOUT`'s default to 200 and says so in the new EXIT-CODE notice | 04-06 T2 (a); 04-11 T3 |
| — | LOW (refuted-as-HIGH, accepted hardening) | The fresh DB is created with `beet -l /config/library.db stats`; every other `beet` call uses a throwaway `-l` under `probe-04` (including `version`) | 04-09 T2 |
| — | Nobody raised | 04-11 classifies a failed `scripts_init.bash` as **network** (pip/git/curl resolution or connection errors, no EROFS line) or **EROFS** (read-only lines naming a vendored file) before any rollback. Network → no `:ro` rollback; one retry after an in-container connectivity check, then HALT | 04-11 T1 |
