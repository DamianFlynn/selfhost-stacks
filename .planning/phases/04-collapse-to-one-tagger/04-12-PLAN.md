---
phase: 04-collapse-to-one-tagger
plan: 12
type: execute
wave: 7
depends_on: [04-11]
files_modified:
  - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
autonomous: false
requirements: [TAGR-04]
tags: [e2e, sabnzbd, criterion-3, human-checkpoint]

must_haves:
  truths:
    - "D-12: one real SABnzbd music job completing after the stripped audio.bash went live is observed end to end from a stamp set BEFORE it: SAB history `Completed` with the baseline `Exit(1): chmod …` script line (D-31/F8, not a regression), no library.blb, no library.blb backup, no beets.log and no .bak created under sabnzbd /config, and the folder left in complete/nzb/music"
    - "REVIEWS row 11: 'untagged' is proven by bytes, not inferred — a read-only watcher takes a PRE-HOOK sha256 snapshot of the job's audio files (first stable snapshot, before the job's `Matching N tracks with Beets` line) and a COMPLETION snapshot when the SAB history row appears; every file present in both is byte-identical; file mtimes are not used as provenance"
    - "REVIEWS row 11: `ReplaygainTagging=\"false\"` is asserted at window open and window close with extended.conf's sha256 and mtime unchanged; if not, PASS is unavailable"
    - "D-31: Audio.txt shows the two pre-declared residual lines and no `SUCCESS: Matched with beets` line; the transient beets-match file is absent after the job"
    - "Orchestrator decision: the job is triggered by the operator (Lidarr search) or arrives organically; SABnzbd is never scripted with an API key on argv; if no job arrives in the window, or no valid PRE-HOOK snapshot exists, criterion 3 is recorded OPEN, not passed"
  artifacts:
    - path: ".planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md"
      provides: "filled Result section with verdict"
      contains: "Verdict"
  key_links:
    - from: "d12.stamp on LXC 100"
      to: "find -newer evidence"
      via: "single stamp for every side-effect check"
      pattern: "newer"
    - from: "watch-d12.sh PRE-HOOK / COMPLETION snapshots"
      to: "Result section per-file comparison"
      via: "sha256 equality per audio file"
      pattern: "PRE-HOOK"
---

<objective>
Prove criterion 3 by behaviour (D-12): a genuine music download completes through the stripped hook with no
tagger side effects, judged against the checklist written in 04-01 before any job was observed. The judge
includes the pre-declared byte-level test for "untagged" (REVIEWS row 11).

Purpose: a static grep proves the file, not the behaviour — the evidence class this project has repeatedly
recorded as insufficient. A tag-provenance argument from file mtimes assumes its conclusion; a before/after
byte comparison on the same files does not.
Output: the filled Result section of `04-D12-EVIDENCE.md` with a PASS / OPEN / FAIL verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
</context>

<conventions>
- Everything this plan runs on LXC 100 is READ-ONLY on the estate: `stat`, `sha256sum`, `ffprobe`,
  `sqlite3 "file:…?mode=ro"`, `find`, `ls`. Writes go only to `/mnt/fast/scratch-04/12/` (never `/tmp`,
  which is tmpfs on LXC 100).
- No SABnzbd or Lidarr API calls from the agent (orchestrator decision).
- Release folder names never enter the repo: the watcher's job→folder mapping stays in host scratch and
  is deleted with it; the evidence file quotes the category, truncated script_line, counts and tag-key
  sets only.
- Timezones: SAB `completed` is UTC; `Audio.txt`/`sabnzbd.log` are Europe/Dublin — convert before comparing.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Arm the window — stamp, pre-state, extended.conf asserted, watcher smoke-tested and launched, guards green</name>
  <files>(host only: /mnt/fast/scratch-04/12/)</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md (the pass condition, sections 1, 3, 5)
    - 04-RESEARCH.md § Code Example 8 (SAB history read, timezone trap)
    - 04-01-SUMMARY.md (extended.conf baseline sha256/mtime; history table column names)
  </read_first>
  <action>
    On LXC 100: confirm `cmp` of appdata audio.bash vs repo = 0 and `grep -cE '^\s*beet ' /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` = 0 (the stripped hook is what will run). `mkdir -p /mnt/fast/scratch-04/12 && touch /mnt/fast/scratch-04/12/d12.stamp`; record its UTC mtime. Record: listing of `/mnt/fast/appdata/arrs/sabnzbd/config/scripts` (name, size, mtime); `find /mnt/fast/appdata/arrs/sabnzbd/config -name '*.bak' | wc -l` (find RC captured); the newest `category='music'` row in `history1.db` (mode=ro: completed time, status, script_line truncated at 60 chars); the line count of `/mnt/fast/appdata/arrs/sabnzbd/config/logs/Audio.txt`.
    Window prerequisite, OPEN side (REVIEWS row 11): `grep -cx 'ReplaygainTagging="false"' /mnt/fast/appdata/arrs/sabnzbd/config/extended.conf` = 1, and record its sha256 and mtime (compare with 04-01's baseline; a difference is recorded, and the line must still read false).
    Watcher: write `watch-d12.sh` in the workstation scratchpad, `bash -n` it, `scp` it to `/mnt/fast/scratch-04/12/`. Behaviour, read-only: inputs `MUSIC_DIR` (default `/mnt/tank/downloads/complete/nzb/music`), `HIST=/mnt/fast/appdata/arrs/sabnzbd/config/admin/history1.db`, `OUT=/mnt/fast/scratch-04/12/watch`, the stamp's epoch, and `START_LIST` (the MUSIC_DIR names present at arming, saved to OUT). Every 2 s: list MUSIC_DIR; for each directory not in START_LIST and not prefixed `_UNPACK_` or `_FAILED_`, append one `date -u +%FT%T.%3NZ` + `stat -c '%n|%s|%Y|%i'` line per regular audio file (flac, mp3, m4a, ogg, opus, wav, aac; case-insensitive) to `OUT/<n>/trail.log`; when two consecutive passes at least 2 s apart show identical stat lines and no PRE-HOOK snapshot exists yet, write `sha256sum` of every audio file to `OUT/<n>/pre.sha256` and its UTC time to `pre.time`. Every 5 s: read `SELECT completed, <final-path column> FROM history WHERE category='music' AND completed > <stamp epoch>` from `HIST` (mode=ro; the final-path column as recorded by 04-01 from `PRAGMA table_info(history)`); for each new row whose folder has a PRE-HOOK snapshot, write `OUT/<n>/post.sha256` and `post.time` from the files present then; record the row's completed time. It exits after the first job has both snapshots plus 5 more minutes (in case a second job is mid-flight), or when killed. It never writes outside OUT.
    Smoke it before arming (the watcher is the instrument, so it is seen to work): `MUSIC_DIR=/mnt/fast/scratch-04/12/fake-music` with an empty START_LIST, start it in the background, create `fake-music/job1/` holding two small files named `a.flac`/`b.flac` (random bytes), and within 15 s `watch/job1/pre.sha256` must exist with two lines; stop it and delete `fake-music` and that `watch/` output.
    Launch for real: `cd /mnt/fast/scratch-04/12 && nohup setsid timeout 604800 bash watch-d12.sh > watch.out 2>&1 < /dev/null &`, record `$!` into `watch.pid`, and confirm `kill -0 $(cat watch.pid)` succeeds (bounded: 7 days).
    Run `bash scripts/quick-health-check.sh` from the workstation → exit 0 (the window opens on a green estate, with the census and drift blocks promoted by 04-11).
  </action>
  <verify>
    <automated>timeout 30 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "test -f /mnt/fast/scratch-04/12/d12.stamp && stat -c %y /mnt/fast/scratch-04/12/d12.stamp && kill -0 \$(cat /mnt/fast/scratch-04/12/watch.pid) && test \$(grep -cx 'ReplaygainTagging=\"false\"' /mnt/fast/appdata/arrs/sabnzbd/config/extended.conf) = 1 && echo ARMED"</automated>
  </verify>
  <acceptance_criteria>
    - Stamp exists; its UTC time and the four pre-state values are in the SUMMARY; quick-health-check exit 0 recorded.
    - extended.conf at open: `ReplaygainTagging="false"` present, sha256 and mtime recorded.
    - Watcher smoke produced `pre.sha256` with two lines within 15 s; the real watcher's PID is alive; its script is not in the repo.
  </acceptance_criteria>
  <done>Every later side effect can be attributed to a job after the stamp, and the job's audio bytes will be captured before the hook's tagging stage.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator triggers one music download, or reports the window closed</name>
  <what-built>The stripped audio.bash is live (04-11), all guards are green, the evidence window is armed with a stamp, and a read-only watcher is recording the job's audio bytes (Task 1).</what-built>
  <how-to-verify>
    1. In Lidarr, run a manual search for one monitored album that is not yet downloaded (Lidarr hands it to SABnzbd in the `music` category). Alternatively, do nothing and wait for the next organic music job (they arrive roughly every 1–5 days; the watcher runs for up to 7 days).
    2. When SABnzbd's History shows that music job finished post-processing (any status), reply with its approximate finish time.
    3. If you decide to stop waiting before any music job arrives, reply "window closed" — criterion 3 will be recorded OPEN, not passed.
  </how-to-verify>
  <resume-signal>Reply "job done ~HH:MM" or "window closed"</resume-signal>
  <files>(none)</files>
  <action>Present the three steps above and wait. Do not call the SABnzbd or Lidarr API from the agent: the trigger is the operator's act in the Lidarr UI or an organic job, and SABnzbd is never scripted with an API key on argv (orchestrator decision). Do not touch the stamp or the watcher from Task 1 while waiting.</action>
  <verify>
    <human-check>Operator reports a music job has finished post-processing in SABnzbd History (with an approximate time), or reports the window closed.</human-check>
  </verify>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
  </read_first>
  <acceptance_criteria>
    - Operator reply recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>A music job has completed after the stamp, or the operator has closed the window, so criterion 3 will be recorded OPEN.</done>
</task>

<task type="auto">
  <name>Task 3: Collect the D-12 evidence, compare PRE-HOOK and COMPLETION bytes, assert the window's close, fill the Result section</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md (sections 1-6)
    - Task 1 pre-state values, extended.conf open values, and watcher layout
  </read_first>
  <action>
    Stop the watcher first if still running (`kill -TERM $(cat watch.pid)`; then `kill -0` must fail), and record whether it had exited on its own and the tail of `watch.out`.
    If the reply was "window closed": still assert the extended.conf close side (below), fill Result with `Verdict: OPEN — no music job arrived between <stamp> and <now>`; skip to cleanup.
    Otherwise, on LXC 100 (read-only), capturing each producer's status before reading its output (non-zero → UNKNOWN for that item, never "absent"): (1) SAB history rows with `category='music'` and `completed` after the stamp (UTC): status and `substr(script_line,1,60)`; (2) `find /mnt/fast/appdata/arrs/sabnzbd/config/scripts -newer /mnt/fast/scratch-04/12/d12.stamp -printf '%p\n'` (RC must be 0; `audio.bash` must be visible in the same directory as positive control) — must not list `library.blb`, any `library.blb*`, `beets.log`; and `test ! -e …/scripts/beets-match`; (3) `find /mnt/fast/appdata/arrs/sabnzbd/config -newer <stamp> -name '*.bak'` (RC 0) → empty; (4) new Audio.txt lines since Task 1's line count (Europe/Dublin timestamps — convert before comparing to SAB's UTC): must contain the `Matching N tracks with Beets` and `ERROR: Unable to match using beets to a musicbrainz release` lines and must NOT contain `SUCCESS: Matched with beets`; record the job's `Matching` line time in UTC; (5) the job folder exists under `/mnt/tank/downloads/complete/nzb/music/`.
    (6) UNTAGGED, by bytes (REVIEWS row 11): map the history row to its watcher folder through the final-path column. PRE-HOOK validity: `pre.sha256` exists, the trail shows the two identical passes it was taken on, and `pre.time` < the job's `Matching` line time in UTC — if any of these fails, the untagged condition is UNPROVEN. COMPLETION: `post.sha256` exists with `post.time` ≥ the row's completed time. Compare per file: every file in both → sha256 equal (count them); files in PRE-HOOK only → recorded as removed (clean()/verify() behaviour, expected; count them); any file whose sha256 differs, or any audio file present only at COMPLETION → FAIL, naming the condition (file names stay in scratch; the evidence records counts and the file's position in the listing). Then `ffprobe -v error -show_entries format_tags -of json` on each COMPLETION file (ffprobe is on LXC 100) → record the set of tag keys; any `MUSICBRAINZ_*` key in a byte-identical file is recorded as "came with the release (bytes unchanged since PRE-HOOK)". File mtimes are not used as provenance.
    (7) Window prerequisite, CLOSE side: `grep -cx 'ReplaygainTagging="false"'` on extended.conf = 1, and its sha256 and mtime equal the Task 1 open values — otherwise PASS is unavailable (record OPEN with the reason, or FAIL if a byte change was also seen).
    (8) re-run quick-health-check.sh → still exit 0 (no DB regenerated; census green).
    Fill the Result section: stamp, SAB row (no release name — quote the category and the truncated script_line only), find outputs, Audio.txt lines (the two residual lines quoted; no release names), PRE-HOOK/COMPLETION times, per-file comparison counts, extended.conf open/close values, ffprobe tag-key sets, and exactly one verdict: `Verdict: PASS` only if every pass condition in section 1 holds, the PRE-HOOK snapshot is valid and the window prerequisite held at both ends; `Verdict: OPEN — <reason>` when no job arrived, the PRE-HOOK snapshot is missing/invalid, or the window prerequisite changed without a byte change; otherwise `Verdict: FAIL — <which condition>`. Remove `/mnt/fast/scratch-04/12` (S5 guard) and assert gone (this also deletes the job→folder mapping). Commit only this file: `docs(04-12): D-12 real-job evidence`.
  </action>
  <verify>
    <automated>f=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; test -f $f && test "$(grep -cE '^Verdict: (PASS|OPEN|FAIL)' $f)" = 1 && test "$(grep -c 'PENDING — filled by 04-12' $f)" = 0 && timeout 30 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "test -d /mnt/fast/scratch-04 -o -d /mnt/fast/appdata || { echo 'UNKNOWN host tree not visible'; exit 3; }; test ! -e /mnt/fast/scratch-04/12 && echo CLEANED"</automated>
  </verify>
  <acceptance_criteria>
    - C3-c: Result section filled; exactly one `Verdict:` line; for PASS, each of (1)-(8) is quoted with its observed value, the PRE-HOOK snapshot precedes the `Matching` line, every file in both snapshots is byte-identical, and extended.conf is unchanged open→close; for OPEN, the reason and window bounds are stated.
    - The Result section contains no mtime-based provenance argument (`grep -ci 'mtime' ` in the Result section is limited to the extended.conf open/close values).
    - No release folder name appears in the file other than the D-29 album (check: the diff adds no path under `complete/nzb/music/` beyond the directory itself).
    - The watcher is not running and `/mnt/fast/scratch-04/12` is absent.
  </acceptance_criteria>
  <done>Criterion 3 is decided by an observed job against a pre-declared condition — PASS, FAIL, or honestly OPEN — with "untagged" proven by bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| download pipeline → evidence (public) | job metadata could leak release names into the repo |
| watcher → download tree | a background reader runs on the Docker host for up to 7 days |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12-01 | Information disclosure | 04-D12-EVIDENCE.md | mitigate | truncated script_line; tag-key sets and counts, not values or names; job→folder mapping kept in host scratch and deleted |
| T-04-12-02 | Information disclosure | SAB API key | mitigate | no API use at all: history via `mode=ro` sqlite; the job is triggered by the operator in Lidarr |
| T-04-12-03 | Repudiation | false pass | mitigate | pass condition pre-declared in 04-01; single stamp; PRE-HOOK/COMPLETION byte comparison; extended.conf asserted at both ends; OPEN recorded if no job or no valid baseline |
| T-04-12-04 | Tampering / DoS | watcher | mitigate | read-only commands only; writes confined to scratch; bounded by `timeout 604800`; smoke-tested before use; stopped and removed at collection |
</threat_model>

<verification>
Verdict line present; evidence tied to one stamp and one PRE-HOOK/COMPLETION pair; window prerequisite asserted at both ends; guards still green after the job.
</verification>

<success_criteria>
Criterion 3 is measured on a real job (PASS/FAIL) or recorded OPEN — never assumed, and "untagged" is never inferred from timestamps.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-12-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS row 11; the full table is in 04-01-PLAN.md.
- Task 1 arms a smoke-tested, read-only watcher that captures PRE-HOOK and COMPLETION sha256 snapshots, and asserts `ReplaygainTagging="false"` together with extended.conf's sha256/mtime at open.
- Task 3 compares bytes per file, requires the PRE-HOOK snapshot to precede the `Matching` line, and re-asserts extended.conf at close.
- The mtime-provenance step is removed. A missing or invalid baseline yields OPEN, not PASS. The absence reads in Task 3 now capture producer status first (row 2 hygiene).
