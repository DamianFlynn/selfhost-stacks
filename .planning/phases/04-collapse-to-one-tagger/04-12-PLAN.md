---
phase: 04-collapse-to-one-tagger
plan: 12
type: execute
wave: 7
depends_on: [04-11]
files_modified:
  - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
autonomous: false
requirements: [TAGR-04]
tags: [e2e, sabnzbd, criterion-3, human-checkpoint]

must_haves:
  truths:
    - "D-12: one real SABnzbd music job completing after the stripped audio.bash went live is observed end to end from a stamp set BEFORE it: SAB history `Completed` with the baseline `Exit(1): chmod …` script line (D-31/F8, not a regression), no library.blb, no library.blb backup, no beets.log and no .bak created under sabnzbd /config, and the folder left in complete/nzb/music"
    - "D-31: Audio.txt shows the two pre-declared residual lines and no `SUCCESS: Matched with beets` line; the transient beets-match file is absent after the job"
    - "Orchestrator decision: the job is triggered by the operator (Lidarr search) or arrives organically; SABnzbd is never scripted with an API key on argv; if no job arrives in the window, criterion 3 is recorded OPEN, not passed"
  artifacts:
    - path: ".planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md"
      provides: "filled Result section with verdict"
      contains: "Verdict"
  key_links:
    - from: "d12.stamp on LXC 100"
      to: "find -newer evidence"
      via: "single stamp for every side-effect check"
      pattern: "newer"
---

<objective>
Prove criterion 3 by behaviour (D-12): a genuine music download completes through the stripped hook with no
tagger side effects, judged against the checklist written in 04-01 before any job was observed.

Purpose: a static grep proves the file, not the behaviour — the evidence class this project has repeatedly
recorded as insufficient.
Output: the filled Result section of `04-D12-EVIDENCE.md` with a PASS / OPEN / FAIL verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arm the window — stamp, pre-state, guards green</name>
  <files>(host only: /mnt/fast/scratch-04/12/)</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md (the pass condition)
    - 04-RESEARCH.md § Code Example 8 (SAB history read, timezone trap)
  </read_first>
  <action>
    On LXC 100: confirm `cmp` of appdata audio.bash vs repo = 0 and `grep -cE '^\s*beet ' /mnt/fast/appdata/arrs/sabnzbd/config/scripts/audio.bash` = 0 (the stripped hook is what will run). `mkdir -p /mnt/fast/scratch-04/12 && touch /mnt/fast/scratch-04/12/d12.stamp`; record its UTC mtime. Record: listing of `/mnt/fast/appdata/arrs/sabnzbd/config/scripts` (name, size, mtime); `find /mnt/fast/appdata/arrs/sabnzbd/config -name '*.bak' | wc -l`; the newest `category='music'` row in `history1.db` (mode=ro: completed time, status, script_line truncated at 60 chars); the line count of `/mnt/fast/appdata/arrs/sabnzbd/config/logs/Audio.txt`. Run `bash scripts/quick-health-check.sh` from the workstation → exit 0 (the window opens on a green estate).
  </action>
  <verify>
    <automated>timeout 30 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "test -f /mnt/fast/scratch-04/12/d12.stamp && stat -c %y /mnt/fast/scratch-04/12/d12.stamp"</automated>
  </verify>
  <acceptance_criteria>
    - Stamp exists; its UTC time and the four pre-state values are in the SUMMARY; quick-health-check exit 0 recorded.
  </acceptance_criteria>
  <done>Every later side effect can be attributed to a job after the stamp.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator triggers one music download, or reports the window closed</name>
  <what-built>The stripped audio.bash is live (04-11), all guards are green, and the evidence window is armed with a stamp (Task 1).</what-built>
  <how-to-verify>
    1. In Lidarr, run a manual search for one monitored album that is not yet downloaded (Lidarr hands it to SABnzbd in the `music` category). Alternatively, do nothing and wait for the next organic music job (they arrive roughly every 1–5 days).
    2. When SABnzbd's History shows that music job finished post-processing (any status), reply with its approximate finish time.
    3. If you decide to stop waiting before any music job arrives, reply "window closed" — criterion 3 will be recorded OPEN, not passed.
  </how-to-verify>
  <resume-signal>Reply "job done ~HH:MM" or "window closed"</resume-signal>
  <files>(none)</files>
  <action>Present the three steps above and wait. Do not call the SABnzbd or Lidarr API from the agent: the trigger is the operator's act in the Lidarr UI or an organic job, and SABnzbd is never scripted with an API key on argv (orchestrator decision). Do not touch the stamp from Task 1 while waiting.</action>
  <verify>
    <human-check>Operator reports a music job has finished post-processing in SABnzbd History (with an approximate time), or reports the window closed.</human-check>
  </verify>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
  </read_first>
  <acceptance_criteria>
    - Operator reply recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>A music job has completed after the stamp, or the operator has closed the window, so criterion 3 will be recorded OPEN.</done>
</task>

<task type="auto">
  <name>Task 3: Collect the D-12 evidence and fill the checklist's Result section</name>
  <files>.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md (sections 1-6)
    - Task 1 pre-state values
  </read_first>
  <action>
    If the reply was "window closed": fill Result with `Verdict: OPEN — no music job arrived between <stamp> and <now>`; skip to cleanup.
    Otherwise, on LXC 100 (read-only): (1) SAB history rows with `category='music'` and `completed` after the stamp (UTC): status and `substr(script_line,1,60)`; (2) `find /mnt/fast/appdata/arrs/sabnzbd/config/scripts -newer /mnt/fast/scratch-04/12/d12.stamp -printf '%p\n'` — must not list `library.blb`, any `library.blb*`, `beets.log`; and `test ! -e …/scripts/beets-match`; (3) `find /mnt/fast/appdata/arrs/sabnzbd/config -newer <stamp> -name '*.bak'` → empty; (4) new Audio.txt lines since Task 1's line count (Europe/Dublin timestamps — convert before comparing to SAB's UTC): must contain the `Matching N tracks with Beets` and `ERROR: Unable to match using beets to a musicbrainz release` lines and must NOT contain `SUCCESS: Matched with beets`; (5) the job folder exists under `/mnt/tank/downloads/complete/nzb/music/`; `ffprobe -v error -show_entries format_tags -of json` on each audio file — record the set of tag keys; any `MUSICBRAINZ_*` key present is cross-checked against the file mtime being ≤ the SAB `completed` time minus the post-processing duration (beets never ran, so any such tag came with the release) and noted, not silently accepted; (6) re-run quick-health-check.sh → still exit 0 (no DB regenerated). Fill the Result section: stamp, SAB row (no release name — quote the category and the truncated script_line only), find outputs, Audio.txt lines (the two residual lines quoted; no release names), ffprobe summary as tag-key sets, and `Verdict: PASS` only if every pass condition in section 1 holds; otherwise `Verdict: FAIL — <which condition>`. Remove `/mnt/fast/scratch-04/12` and assert gone. Commit only this file: `docs(04-12): D-12 real-job evidence`.
  </action>
  <verify>
    <automated>grep -cE '^Verdict: (PASS|OPEN|FAIL)' .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md && test "$(grep -c 'PENDING — filled by 04-12' .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - C3-c: Result section filled; exactly one `Verdict:` line; for PASS, each of (1)-(6) is quoted with its observed value; for OPEN, the window bounds are stated.
    - No release folder name appears in the file other than the D-29 album (check: the diff adds no path under `complete/nzb/music/` beyond the directory itself).
    - `/mnt/fast/scratch-04/12` absent.
  </acceptance_criteria>
  <done>Criterion 3 is decided by an observed job against a pre-declared condition — PASS, FAIL, or honestly OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| download pipeline → evidence (public) | job metadata could leak release names into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12-01 | Information disclosure | 04-D12-EVIDENCE.md | mitigate | truncated script_line; tag-key sets not values; no release names |
| T-04-12-02 | Information disclosure | SAB API key | mitigate | no API use at all: history via `mode=ro` sqlite; the job is triggered by the operator in Lidarr |
| T-04-12-03 | Repudiation | false pass | mitigate | pass condition pre-declared in 04-01; single stamp; OPEN recorded if no job |
</threat_model>

<verification>
Verdict line present; evidence tied to one stamp; guards still green after the job.
</verification>

<success_criteria>
Criterion 3 is measured on a real job (PASS/FAIL) or recorded OPEN — never assumed.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-12-SUMMARY.md` when done.
</output>
