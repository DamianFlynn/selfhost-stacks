---
phase: 04-collapse-to-one-tagger
plan: 07
type: execute
wave: 3
depends_on: [04-01, 04-03, 04-06]
files_modified:
  - scripts/spike03-image-headroom.sh
autonomous: true
requirements: [TAGR-03, TAGR-04]
tags: [teardown, deletion, dns, github-issue]

must_haves:
  truths:
    - "D-04 / D-35: /mnt/fast/appdata/media/wrtag and /mnt/fast/appdata/arrs/soulbeet are deleted on LXC 100 only after the fence copy (wrtag.db) or git copy (soulbeet beets_config.yaml; data/ empty) is re-asserted immediately before the rm"
    - "D-28: the survivor's old library.db and musiclibrary.blb, and config.yaml.old, are deleted only after their Phase 1 / 04-01 fence copies are re-asserted; Music/__ and the survivor's config.yaml are untouched here"
    - "D-04: an unfiltered `docker ps -a` shows no container named for wrtag, soulbeet or beets; the sentriz/wrtag:v0.20.0 image is removed by name (never prune) after proving no container references it; spike03-image-headroom.sh no longer protects it from reaping"
    - "D-33: the wrtag.deercrest.info Cloudflare DNS record is removed host-side with Traefik's DNS-01 token read inside the process — never on argv, never printed, never in the repo — and `dig` returns nothing while a control name still resolves"
    - "D-26: GitHub issue #306 is CLOSED with a comment citing the four D-26 facts and answering the issue's own salvage proposal (Pitfall 14); #305 stays open; #307 untouched"
  artifacts:
    - path: "scripts/spike03-image-headroom.sh"
      provides: "KEEP_PATTERNS without the retired wrtag image"
  key_links:
    - from: "each host rm"
      to: "fence MANIFEST / 04-01 confirmation table"
      via: "re-assert immediately before delete"
      pattern: "integrity_check"
---

<objective>
Retire the non-sabnzbd runtime state: delete the wrtag and soulbeet appdata trees and the survivor's two
old databases (making room for D-28's fresh one), remove the retired image and its reap-protection,
delete the public DNS record, and close #306 with evidence.

Purpose: D-04 — "one library.db" must be a measured fact about the estate, not a claim about the repo.
sabnzbd's databases are deliberately NOT deleted here: until the stripped audio.bash is live (04-11),
every music job would regenerate them.
Output: host deletions with pre/post evidence; one script edit; DNS and issue changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-03-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-06-SUMMARY.md
@scripts/spike03-wrtag-arms.sh
@scripts/check-music-consumers.sh
</context>

<conventions>
- Preconditions (halt if any is unmet): 04-01's confirmation table has no BLOCKER for the paths deleted
  here; 04-06's first run recorded `retired path PRESENT` for them (the guard existed and fired before
  deletion); host HEAD = origin HEAD.
- Deletion guard (S5, the CR-01 pattern from spike03-wrtag-arms.sh `fence_eval`): for each target,
  `realpath -e`, require the resolved path to equal one entry of an explicit allow-list typed in this
  plan, refuse any `..` component, print the full `find <target> | wc -l` count and a listing, then
  delete, then `test ! -e`. No globs in `rm` arguments; no variable that could be empty.
- Secrets: the Cloudflare token is read with the bash builtin `read -r TOK < file` inside a script fed
  to `ssh root@172.16.1.159 bash -s` on stdin; passed to curl only as `-H @<(printf 'Authorization:
  Bearer %s\n' "$TOK")`. Never `$(cat …)` on a command line, never `echo "$TOK"`, never `ps`/`pgrep -a`/
  `top -c` while the script runs (DEF-03-21).
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Fence-gated deletion of the wrtag/soulbeet trees and the survivor's old databases (D-04, D-28, D-35)</name>
  <files>(host only)</files>
  <read_first>
    - scripts/spike03-wrtag-arms.sh lines 242-286 (fence_eval) and 389-418 (its self-test)
    - 04-01-SUMMARY.md (confirmation table, sha256 baselines) and 04-03-SUMMARY.md (PRE_DELETION_SHA, soulbeet config sha)
    - 04-06-SUMMARY.md (first-run red transcript)
  </read_first>
  <action>
    Allow-list, exactly these five: `/mnt/fast/appdata/media/wrtag`, `/mnt/fast/appdata/arrs/soulbeet`, `/mnt/fast/appdata/arrs/beets/config/musiclibrary.blb`, `/mnt/fast/appdata/arrs/beets/config/library.db`, `/mnt/fast/appdata/arrs/beets/config/config.yaml.old`.
    Before each delete, re-assert and record: wrtag — fence copy `media_wrtag_data_wrtag.db` prints `ok` for `PRAGMA integrity_check` and its MANIFEST sha matches the file; the live wrtag.db sha256 equals 04-01's (unchanged since fencing) — if it changed, take a new `.backup` + integrity + MANIFEST line first. soulbeet — `data/` is empty (`find data -mindepth 1 | wc -l` = 0) and `beets_config.yaml` sha256 equals `git show <PRE_DELETION_SHA>:stacks/selfhosted/arrs/soulbeet/beets_config.yaml | sha256sum`. Survivor library.db and musiclibrary.blb — live sha256 equals 04-01's AND equals the fence copy's MANIFEST lineage recorded in 04-01 (unchanged since the Phase 1 fence), fence copy `integrity_check` = ok; if a live sha differs from 04-01 (something opened it), take a fresh `.backup` + integrity + MANIFEST line before deleting. config.yaml.old — fence audit copy sha256 equals the live file's.
    Also assert no container references these paths: `docker ps -aq | xargs -r docker inspect --format '{{.Name}} {{range .Mounts}}{{.Source}} {{end}}' | grep -E 'media/wrtag|arrs/soulbeet|arrs/beets/config'` returns only lines for no container (expected: none, since the survivor is not running).
    Delete with the guard. Then run `bash /mnt/fast/stacks/scripts/check-music-freeze.sh` and record: `retired path PRESENT` now names only the three sabnzbd paths; beets databases fails because SURVIVOR_DB is absent (expected until 04-09); nothing else newly red. Do NOT touch `/mnt/fast/appdata/arrs/beets/config/Music/__`, `config.yaml`, or `state.pickle`.
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "for p in /mnt/fast/appdata/media/wrtag /mnt/fast/appdata/arrs/soulbeet /mnt/fast/appdata/arrs/beets/config/musiclibrary.blb /mnt/fast/appdata/arrs/beets/config/library.db /mnt/fast/appdata/arrs/beets/config/config.yaml.old; do test ! -e \$p || { echo PRESENT \$p; exit 1; }; done; test -d /mnt/fast/appdata/arrs/beets/config/Music && echo GONE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - All five allow-listed paths: `test ! -e` true; `Music/` and `config.yaml` still exist with sha256/`du -s` unchanged from 04-01.
    - SUMMARY carries, per target, the pre-delete re-assertion values (sha/integrity) and entry counts.
    - Post-delete freeze run: exactly the sabnzbd retired paths remain named; transcript recorded.
  </acceptance_criteria>
  <done>The retired taggers' state and the survivor's stale databases are gone, each after its copy was proven.</done>
</task>

<task type="auto">
  <name>Task 2: Containers, image and reap-protection (D-04, Pitfall 11)</name>
  <files>scripts/spike03-image-headroom.sh</files>
  <read_first>
    - scripts/spike03-image-headroom.sh lines 1-40 (usage, inventory/prune gate) and 100-120 (KEEP_PATTERNS)
  </read_first>
  <action>
    On LXC 100: `docker ps -a --format '{{.Names}} {{.Image}}'` with NO status filter — record any line matching `wrtag|soulbeet|^beets ` (expected none); `docker network ls` and `docker volume ls` filtered for `wrtag|soulbeet|music` (expected none). `docker ps -a -q --filter ancestor=sentriz/wrtag:v0.20.0` must be empty; then `docker image rm sentriz/wrtag:v0.20.0` (by name; never `docker image prune`); record freed size and `df -h /`. Record the NAMES (never values) of variables in `stacks/selfhosted/arrs/.env` that no tracked yaml references any more (e.g. `SOULBEET_SECRET_KEY`), found with `grep -o '^[A-Z_]*=' | cut -d= -f1` against `git grep`; leave the file unchanged (optional hygiene, nothing breaks).
    Repo: in spike03-image-headroom.sh remove the `"sentriz/wrtag"` entry from KEEP_PATTERNS and update the comment above it (it no longer protects three spike images; wrtag was retired in Phase 4; beets-flask stays for Phase 5; both beets entries stay). `bash -n`. Commit (`chore(04-07): stop protecting the retired wrtag image from reaping (Pitfall 11)`), push, host pull, record HEAD.
  </action>
  <verify>
    <automated>bash -n scripts/spike03-image-headroom.sh && test "$(grep -v '^\s*#' scripts/spike03-image-headroom.sh | grep -c 'sentriz/wrtag')" = 0 && timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; test \$(timeout 30 docker ps -a --format '{{.Names}}' | grep -ciE 'wrtag|soulbeet') = 0 && ! timeout 30 docker image inspect sentriz/wrtag:v0.20.0 >/dev/null 2>&1 && echo CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - C5-c: unfiltered `docker ps -a` name count for `wrtag|soulbeet` = 0; `docker image inspect sentriz/wrtag:v0.20.0` fails.
    - `grep -v '^\s*#' scripts/spike03-image-headroom.sh | grep -c 'sentriz/wrtag'` = 0; `metasauce/beets-flask`, `lscr.io/linuxserver/beets`, `linuxserver/beets` still in KEEP_PATTERNS.
    - Unreferenced `.env` variable names listed in SUMMARY with no values.
  </acceptance_criteria>
  <done>No container, image or reap exemption of the retired taggers remains.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the wrtag DNS record (D-33) and close #306 with evidence (D-26)</name>
  <files>(none — Cloudflare API and GitHub)</files>
  <read_first>
    - scripts/check-music-consumers.sh ~411 and scripts/check-jellyfin-transcode.sh ~613 (secret-to-curl-without-argv pattern, S7)
    - .planning/phases/03-tagger-spike/deferred-items.md DEF-03-21 § The correct technique
    - 04-RESEARCH.md Pitfall 14; `gh issue view 306` body
  </read_first>
  <action>
    DNS (host-side, per conventions): feed a short bash script to `ssh root@172.16.1.159 bash -s` that: `read -r TOK < /mnt/fast/appdata/traefik/secrets/cf_dns_api_token`; looks up the zone id for `deercrest.info` (`GET https://api.cloudflare.com/client/v4/zones?name=deercrest.info`); lists records with `name=wrtag.deercrest.info` and prints ONLY count, id, type and proxied (never content of the auth header); requires exactly 1 record (if 0, report "already absent"; if >1, stop and report); issues `DELETE .../dns_records/<id>`; lists again and prints the count (must be 0); prints `success` fields only. The script body contains the token file PATH, never a value; nothing is written to disk. Then on the workstation: `dig +short wrtag.deercrest.info @1.1.1.1` must be empty (retry every 60 s for up to 10 minutes for resolver cache), while the control `dig +short beets.deercrest.info @1.1.1.1` is non-empty (proves the resolver answered — distinguishes "could not look"). If dig never empties, record UNKNOWN with the API count as the authority.
    Issue #306: verify the four facts first and record each: (1) image gone — `docker manifest inspect ghcr.io/terry90/soulbeet:latest` on the workstation fails (record the error text), (2) never deployed — 04-01's unfiltered `docker ps -a` and image list had none, (3) data directory was empty — 04-01 listing, (4) definition and `arrs/soulbeet/beets_config.yaml` deleted in commit <04-03 commit SHA>, recoverable at <PRE_DELETION_SHA>, and the appdata tree deleted in Task 1. Then `gh issue close 306 --comment "<text>"` where the text states those four facts and answers the issue's own proposal: folding soulbeet's config into the manual beets config is deliberately NOT done here, because Phase 6 (CONF-01…05) owns the survivor's configuration and Phase 3 set the direction (beets engine, reviewable-queue front end); the config remains recoverable from git at the named SHA. No credentials, no host secret paths. Do not touch #305 or #307.
  </action>
  <verify>
    <automated>test -z "$(dig +short wrtag.deercrest.info @1.1.1.1)" && test -n "$(dig +short beets.deercrest.info @1.1.1.1)" && gh issue view 306 --json state -q .state | grep -qx CLOSED && echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - C5-d: Cloudflare list for `wrtag.deercrest.info` returns count 0 after the delete; `dig` empty with the control name resolving.
    - C1-b: `gh issue view 306 --json state,comments` → `CLOSED`; the last comment contains `ghcr.io/terry90/soulbeet`, the 04-03 commit SHA, and `Phase 6`.
    - `gh issue view 305 --json state -q .state` = `OPEN`.
    - No transcript line in the SUMMARY contains a string of the token's shape (screen: no 40-char `[A-Za-z0-9_-]` run); `history`/argv never held the token (method recorded).
  </acceptance_criteria>
  <done>The retired tagger has no public hostname and its issue is closed with reasons that stop it being reopened on a whim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator intent → `rm -rf` on appdata | irreversible deletion on the Docker host |
| LXC 100 → Cloudflare API | a DNS-edit-capable token crosses into a process |
| agent → GitHub (public) | closing comment is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | host rm | mitigate | explicit five-path allow-list, `realpath -e` equality, `..` refusal, count+list before delete, `test ! -e` after |
| T-04-07-02 | Repudiation / Tampering | data with no copy | mitigate | fence copy re-asserted (sha + integrity) immediately before each delete; fresh `.backup` if the live DB changed |
| T-04-07-03 | Information disclosure | Cloudflare token | mitigate | `read -r` builtin + `-H @<(printf …)`; script via stdin; no argv, no echo, no `ps`/`pgrep -a` |
| T-04-07-04 | Tampering | DNS zone | mitigate | exact-name lookup; require exactly one record before DELETE; stop on >1 |
| T-04-07-05 | Information disclosure | #306 comment | mitigate | facts only; no secret paths or values |
| T-04-07-06 | Denial of service | image removal | mitigate | `docker image rm` by name after ancestor filter empty; never `prune` |
</threat_model>

<verification>
Five paths absent; freeze output names only sabnzbd retired paths; image gone; DNS count 0; #306 CLOSED.
</verification>

<success_criteria>
Everything of wrtag and soulbeet is gone from the estate, each deletion preceded by a proven copy.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-07-SUMMARY.md` when done.
</output>
