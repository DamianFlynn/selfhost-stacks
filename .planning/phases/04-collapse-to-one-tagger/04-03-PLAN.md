---
phase: 04-collapse-to-one-tagger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - stacks/selfhosted/arrs/soulbeet.yaml
  - stacks/selfhosted/arrs/soulbeet/beets_config.yaml
  - stacks/selfhosted/music/compose.yaml
  - stacks/selfhosted/music/wrtag.yaml
  - stacks/selfhosted/arrs/compose.yaml
  - stacks/selfhosted/arrs/beets/beets.yaml
  - renovate.json5
  - scripts/check-renovate.sh
autonomous: false
requirements: [TAGR-03, TAGR-04]
tags: [renovate, compose, deletion, validator]

must_haves:
  truths:
    - "D-01 / D-05: soulbeet.yaml, soulbeet/beets_config.yaml and the whole stacks/selfhosted/music/ directory are deleted from git, not commented out; arrs/beets/beets.yaml is the only tagger definition"
    - "D-06 / D-34: arrs/compose.yaml no longer carries `#  - soulbeet.yaml`, and the survivor line reads exactly `#  - beets/beets.yaml` — still commented out (D-02)"
    - "D-02 / D-03: beets.yaml image is `lscr.io/linuxserver/beets:2.13.1-ls349`; `restart: \"no\"`, `profiles: [\"manual\"]` and `/mnt/tank/media:/media:ro` are byte-unchanged"
    - "D-14: the wrtag `<0.30.0` rule is deleted from renovate.json5 and no `sentriz/wrtag` string remains; the Jellyfin rule's description no longer cites a wrong packageRules index"
    - "D-15 as amended by D-30: two beets rules follow the Jellyfin rule — a versioning-only regex rule for `-lsNNN`, then a manual-review rule for minor+major with patch automerge preserved and NO allowedVersions"
    - "Criterion 2: `renovate-config-validator --strict --no-global renovate.json5` exits 0 with the pinned 44.80.0, and a scratch copy with a misspelled key exits 1"
    - "D-22: check-renovate.sh validates with `--no-global`; lines 90/100 cannot abort on zero grep matches; lines 115/138/157 count with awk, so an empty Renovate branch list prints `No pending Renovate PRs` and exits 0 (proven with a PATH-stubbed git)"
  artifacts:
    - path: "renovate.json5"
      provides: "wrtag rule removed; two beets rules"
      contains: "ls(?<build>"
    - path: "scripts/check-renovate.sh"
      provides: "D-22 repairs"
      contains: "--no-global"
    - path: "stacks/selfhosted/arrs/beets/beets.yaml"
      provides: "survivor at the measured tag"
      contains: "2.13.1-ls349"
  key_links:
    - from: "renovate.json5 beets rules"
      to: "stacks/selfhosted/arrs/beets/beets.yaml image line"
      via: "matchPackageNames lscr.io/linuxserver/beets"
      pattern: "lscr.io/linuxserver/beets"
    - from: "scripts/check-renovate.sh"
      to: "renovate-config-validator"
      via: "--no-global invocation"
      pattern: "renovate-config-validator --no-global"
---

<objective>
The repo half of retirement (TAGR-03, part of TAGR-04): delete the losing tagger definitions, clean the
include list, move the survivor to the measured tag, delete the wrtag pin and add the effective two-rule
beets policy, and repair the Renovate health check that has to validate this very file.

Purpose: ROADMAP criteria 1 and 2. The D-03 tag bump lands in the SAME push as the beets Renovate rules
so Renovate's first proposal is the expected `2.13.1-ls350` patch (D-30), not a minor from `2.5.1-ls295`.
Output: deletions, three edited configs, repaired `check-renovate.sh`, validator transcripts, host pulled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/03-tagger-spike/03-DECISION.md
@renovate.json5
@scripts/check-renovate.sh
@stacks/selfhosted/arrs/compose.yaml
@stacks/selfhosted/arrs/beets/beets.yaml
</context>

<conventions>
- Stage by explicit path only (`git rm` for deletions). Never `git add -A`: the tree carries unrelated
  `03-ERGONOMICS-SHEET.md` and `body.txt` changes.
- `docker compose ... config` ALWAYS with `--quiet` (without it compose renders `.env` secrets).
- Never `npx --yes` (forbidden by name at check-renovate.sh:56-58). The validator lives in a workstation
  scratch prefix outside the repo, deleted at the end of the plan.
- Validator gate per VALIDATION row C2-a: exit 0 on the real file AND exit 1 on the negative control.
</conventions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the renovate npm package (RESEARCH § Package Legitimacy Audit: [ASSUMED])</name>
  <what-built>Nothing yet. slopcheck was unavailable in check-only mode and mis-detected the ecosystem, so `renovate` is [ASSUMED] by protocol. This gate is never auto-approvable and is exempt from end-of-phase human-verify batching.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/renovate and confirm: the repository link is github.com/renovatebot/renovate; version 44.80.0 is listed; weekly downloads are in the hundreds of thousands.
    2. Confirm the install will be `npm install --prefix <scratch outside the repo> renovate@44.80.0 --ignore-scripts --no-audit --no-fund` (no lifecycle scripts run; nothing installed globally or into the repo).
    3. Reply "approved" to proceed, or name a concern.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the concern</resume-signal>
  <files>(none)</files>
  <action>Present the three verification steps above to the operator and stop. Do not run `npm install`, `npx`, or any registry command before the reply. If the reply names a concern, record it and do not proceed to Task 2; the fallback route (a pinned `renovate/renovate` image run on the workstation) needs a new operator decision.</action>
  <verify>
    <human-check>Operator replied "approved" after checking npmjs.com/package/renovate (repository github.com/renovatebot/renovate, version 44.80.0 listed).</human-check>
  </verify>
  <read_first>
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Package Legitimacy Audit, § Standard Stack (validator installation)
  </read_first>
  <acceptance_criteria>
    - Operator reply "approved" recorded verbatim in the SUMMARY before any `npm install` runs.
  </acceptance_criteria>
  <done>The operator has approved the exact package and version, or the plan has stopped with the concern recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the losers, fix the include list, bump the survivor, rewrite renovate.json5 — validator-gated</name>
  <files>stacks/selfhosted/arrs/soulbeet.yaml, stacks/selfhosted/arrs/soulbeet/beets_config.yaml, stacks/selfhosted/music/compose.yaml, stacks/selfhosted/music/wrtag.yaml, stacks/selfhosted/arrs/compose.yaml, stacks/selfhosted/arrs/beets/beets.yaml, renovate.json5</files>
  <read_first>
    - renovate.json5 (whole file; rule at 90-104; "Auto-label by stack: Music" ~415-423; Jellyfin rule ~451-469)
    - stacks/selfhosted/arrs/compose.yaml lines 28-42
    - stacks/selfhosted/arrs/beets/beets.yaml (whole)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Code Examples 1-2, Pitfalls 4-6
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § renovate.json5, § compose.yaml
  </read_first>
  <action>
    1. Install the validator: `S=$(mktemp -d)`; `npm install --prefix "$S" renovate@44.80.0 --ignore-scripts --no-audit --no-fund`. Baseline: `"$S/node_modules/.bin/renovate-config-validator" --strict --no-global renovate.json5` on the UNCHANGED file → record exit (expected 0).
    2. Record `git rev-parse HEAD` as PRE_DELETION_SHA and `sha256sum stacks/selfhosted/arrs/soulbeet/beets_config.yaml` (expected prefix `83028925`) in the SUMMARY — plan 04-05 and plan 04-07's #306 comment cite them.
    3. `git rm` stacks/selfhosted/arrs/soulbeet.yaml, stacks/selfhosted/arrs/soulbeet/beets_config.yaml, stacks/selfhosted/music/compose.yaml, stacks/selfhosted/music/wrtag.yaml (D-01, D-05); confirm both directories no longer exist in the working tree.
    4. arrs/compose.yaml: delete the line `#  - soulbeet.yaml` (D-06); change `#  - beets.yaml` to `#  - beets/beets.yaml` keeping the two spaces after `#` (D-34, stays commented per D-02); leave the listenarr and boxarr comment lines untouched.
    5. beets.yaml line 6: `lscr.io/linuxserver/beets:2.5.1-ls295` → `lscr.io/linuxserver/beets:2.13.1-ls349` (D-03). Change nothing else in this file in this plan.
    6. renovate.json5 (D-14, D-30): delete the whole wrtag rule object (current lines 90-104). Verdict for the "Auto-label by stack: Music" rule (matches only `stacks/selfhosted/music/**`, now deleted): delete it, and record the verdict in the SUMMARY as a D-07 item the `wrtag|soulbeet` grep cannot see. Append immediately after the Jellyfin rule object two new objects in this file's one-key-per-line array style: (a) versioning-only — description stating that default docker versioning treats `-ls349` as a compatibility suffix so no update was ever proposed, and that a rule may not combine versioning with matchUpdateTypes; `matchDatasources: ["docker"]`, `matchPackageNames: ["lscr.io/linuxserver/beets"]`, `versioning: "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)-ls(?<build>\\d+)$"`; (b) manual-review — description stating minor and major require manual review, patch automerge preserved, NO allowedVersions ceiling because the retired wrtag `<0.30.0` pin (deleted in Phase 4) enforced a broken state, and that beets 2.4.0 (a MINOR) turned MusicBrainz into a plugin and silently disabled autotagging; inherits the documented addLabels cosmetic wart; `matchDatasources: ["docker"]`, `matchPackageNames: ["lscr.io/linuxserver/beets"]`, `matchUpdateTypes: ["minor","major"]`, `automerge: false`, `addLabels: ["stack:arrs","major-update","manual-review-required"]`. In the Jellyfin rule's description replace index references by description (Pitfall 6): keep the `packageRules[2]` reference only if index 2 is still the automerge-minor rule after the edit; replace `packageRules[5] only catches MAJOR` with a reference to the major-update rule by its own description text; replace `see packageRules[3], the wrtag <0.30.0 pin added on a misdiagnosis on 2026-06-24, which has been enforcing a broken state ever since` with a paraphrase naming the retired wrtag pin, deleted in Phase 4. Verify the indices with node: `node -e` using `require("$S/node_modules/json5")` to parse renovate.json5 and print each packageRules index with its description's first 60 characters; record the list.
    7. Gates: `--strict --no-global renovate.json5` → exit 0; copy to `$S/cur/renovate.json5` and run `--strict` there with no argument (discovery mode) → exit 0; negative control: a scratch copy with `"automerg": false` inserted into the new manual-review rule → exit 1 (record the error text). `docker compose -f stacks/selfhosted/arrs/compose.yaml config --quiet` → 0; `docker compose -f stacks/selfhosted/arrs/beets/beets.yaml --env-file stacks/selfhosted/arrs/.env --profile manual config --quiet` → 0.
    8. Commit (`feat(04-03): retire wrtag and soulbeet definitions; beets Renovate policy (TAGR-03)`) with only these paths. Keep `$S` for Task 3.
  </action>
  <verify>
    <automated>test ! -e stacks/selfhosted/music && test ! -e stacks/selfhosted/arrs/soulbeet.yaml && test "$(grep -c 'sentriz/wrtag' renovate.json5)" = 0 && test "$(grep -c '^#  - beets/beets.yaml$' stacks/selfhosted/arrs/compose.yaml)" = 1 && test "$(grep -c 'soulbeet' stacks/selfhosted/arrs/compose.yaml)" = 0 && grep -c 'image: lscr.io/linuxserver/beets:2.13.1-ls349' stacks/selfhosted/arrs/beets/beets.yaml</automated>
  </verify>
  <acceptance_criteria>
    - C1-a: `git ls-files stacks | xargs grep -lE '^\s*image:\s*(lscr\.io/linuxserver/beets|sentriz/wrtag|ghcr\.io/terry90/soulbeet|metasauce/beets-flask)'` prints exactly `stacks/selfhosted/arrs/beets/beets.yaml`; `git ls-files | grep -E 'soulbeet|selfhosted/music/'` prints nothing.
    - C1-c: `grep -c '^#  - beets/beets.yaml$'` = 1, `grep -c '^#  - beets.yaml$'` = 0, `grep -c soulbeet` = 0 on arrs/compose.yaml.
    - beets.yaml: `grep -c 'restart: "no"'` = 1, `grep -c 'profiles: \["manual"\]'` = 1, `grep -c '/mnt/tank/media:/media:ro'` = 1.
    - C2-a/C2-b: `grep -c 'sentriz/wrtag' renovate.json5` = 0; `grep -c 'ls(?<build>' renovate.json5` = 1; no single packageRules object contains both `versioning` and `matchUpdateTypes` (checked by the node index dump); validator `--strict --no-global` exit 0, discovery-mode `--strict` exit 0, `automerg` control exit 1 — all three transcripts in the SUMMARY.
    - `grep -c 'packageRules\[3\], the wrtag' renovate.json5` = 0.
    - Both `docker compose ... config --quiet` exit 0.
  </acceptance_criteria>
  <done>Exactly one tagger definition in git, and a Renovate config that validates and actually governs the survivor.</done>
</task>

<task type="auto">
  <name>Task 3: Repair check-renovate.sh (D-22, F12, F13) with driven controls; push and pull</name>
  <files>scripts/check-renovate.sh</files>
  <read_first>
    - scripts/check-renovate.sh (whole file; lines 5, 55-72, 85-105, 110-160)
    - .planning/phases/02.1-jellyfin-transcode-retention*/02.1-08-SUMMARY.md lines 150-175 (PATH-stub harness and route table shape) — locate with `ls .planning/phases | grep 02.1`
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md § Code Example 3
  </read_first>
  <action>
    Edit: line 62 `renovate-config-validator "$CONFIG_FILE"` → `renovate-config-validator --no-global "$CONFIG_FILE"` (F13); lines 90 and 100 wrap each `grep -r…` in `{ grep … || true; }` before the pipe so zero matches cannot abort under `set -euo pipefail` (F12); lines 115, 138 and 157 replace `echo "$X" | grep -v '^$' | wc -l | tr -d ' '` with `printf '%s\n' "$X" | awk 'NF{n++} END{print n+0}'` for POSTGRES_COUNT, REDIS_COUNT and RENOVATE_COUNT. Add a comment above line 157 stating that the old form aborted exactly when zero Renovate PRs were pending. Do NOT fix the BSD-sed `\s` at lines 101-103 — record it in the SUMMARY as out of scope (A6). `bash -n scripts/check-renovate.sh`.
    Driven controls, all stubs in `$S/stub` (not committed, deleted after):
    (a) Line 157 — a fake `git` on PATH that exits 0 for `fetch` and prints nothing for `branch -r` (delegate every other subcommand to the real git by absolute path): run the FIXED script → prints `No pending Renovate PRs` and exits 0; run `git show HEAD:scripts/check-renovate.sh` (the committed PRE-fix file; these controls run before this task's commit) saved to `$S/old.sh` under the same stub from the repo root → exits non-zero before that message (proves the defect was real).
    (b) Empty-input: in `$S/empty/` create `renovate.json5` (copy of the real one) and `stacks/selfhosted/x/compose.yaml` containing only `services: {}`; run the fixed script from that dir with the git stub → exits 0 and reports postgres/redis counts 0 with no abort at lines 90/100.
    (c) Real run: `PATH="$S/node_modules/.bin:$PATH" bash scripts/check-renovate.sh` from the repo root → prints `validates (renovate-config-validator)`, `Validator route: local`, exits 0.
    Record a route table (stub input | script exit | verdict line) in the 02.1-08 shape.
    Commit (`fix(04-03): check-renovate.sh pipefail aborts and --no-global (D-22)`), `git push origin main`, then on LXC 100 `git -C /mnt/fast/stacks pull --ff-only` (halt if not fast-forward; never stash/reset) and record host HEAD = local HEAD. `rm -rf "$S"`.
  </action>
  <verify>
    <automated>bash -n scripts/check-renovate.sh && grep -c 'renovate-config-validator --no-global' scripts/check-renovate.sh && test "$(grep -v '^\s*#' scripts/check-renovate.sh | grep -c "grep -v '\^\$' | wc -l")" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' scripts/check-renovate.sh | grep -c "grep -v '^\$'"` = 0 and `grep -c "awk 'NF{n++} END{print n+0}'"` = 3.
    - Control (a) fixed: exit 0 + `No pending Renovate PRs`; old: non-zero exit. Control (b): exit 0. Control (c): exit 0 with route `local`. Route table in SUMMARY.
    - `git log origin/main -1 --format=%h` equals local HEAD; host `git -C /mnt/fast/stacks rev-parse --short HEAD` equals it.
    - `$S` removed; `git status --porcelain scripts/` empty.
  </acceptance_criteria>
  <done>The tool that validates renovate.json5 validates it in repo semantics and no longer fails on a healthy estate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workstation scratch | third-party package executed locally to validate config |
| repo → Renovate SaaS | the pushed config governs automated PRs estate-wide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install of renovate | mitigate | blocking-human legitimacy checkpoint (Task 1); exact pin 44.80.0; `--ignore-scripts`; scratch prefix outside repo, deleted after; never `npx --yes` |
| T-04-03-01 | Denial of service | renovate.json5 | mitigate | `--strict --no-global` exit 0 + discriminating negative control before push; a config error would silently stop every Renovate run |
| T-04-03-02 | Elevation of privilege | beets.yaml | mitigate | acceptance asserts `/mnt/tank/media:/media:ro`, `restart: "no"`, `profiles: ["manual"]` byte-unchanged (Phase 1 D-20) |
| T-04-03-03 | Information disclosure | compose validation | mitigate | `config --quiet` only; `.env` never rendered |
| T-04-03-04 | Tampering | beets version policy | mitigate | minor/major manual review; no allowedVersions ceiling (the wrtag lesson) |
</threat_model>

<verification>
Validator exit 0 in both modes + negative control exit 1; C1-a/C1-c greps; check-renovate.sh route table; host pulled.
</verification>

<success_criteria>
ROADMAP criteria 1 (repo half) and 2 are satisfied by executed evidence; Renovate governs the survivor.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-03-SUMMARY.md` when done.
</output>
