---
phase: 04-collapse-to-one-tagger
plan: 13
type: execute
wave: 8
depends_on: [04-02, 04-04, 04-05, 04-12]
files_modified:
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [TAGR-03, TAGR-04, TAGR-05]
tags: [closure, docs, sweep, renovate-observation]

must_haves:
  truths:
    - "D-25: beets.md gains a dated `## Phase 4 — one tagger` closure section whose three counters (tagger definitions = 1, beets library.db = 1, rw library mounts on non-tagger containers = 0 excluding the D-21 consumer exception, Jellyfin printed separately) are pasted ANSI-stripped from an EXECUTED run of check-music-freeze.sh with its command and UTC timestamp, and which names the standing check that keeps asserting them"
    - "D-01 / D-02: beets.md's 'Two beets, one library' living text now describes the one survivor (dormant, 2.13.1-ls349, vendored config declaring musicbrainz, one fresh library.db) and the defused sabnzbd guard config"
    - "D-07: a final repo-wide `wrtag|soulbeet` sweep plus the widened grep has a verdict for every hit outside .planning/, with zero hits left in the 'corrected' state unfixed"
    - "D-30: the Renovate outcome is observed and recorded — the soulbeet lookup failure gone from Dependency Dashboard #3, no config-error issue, and the ls-build patch PR recorded as EXPECTED (or 'not yet observed' if Renovate has not run)"
    - "Follow-ups are recorded, not fixed: the estate-wide -lsNNN Renovate silence (F9), pip -U beets at sabnzbd boot (Phase 8), the baseline chmod Exit(1) (D-31), Music/__ (Phase 6), and the census revision beets-flask will need (Phase 5)"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "Phase 4 closure section with executed census counters"
      contains: "## Phase 4"
  key_links:
    - from: "beets.md § Phase 4"
      to: "scripts/check-music-freeze.sh § 6b"
      via: "pasted executed run + named standing check"
      pattern: "check-music-freeze.sh"
---

<objective>
Close the phase the way Phases 1, 2 and 02.1 closed: measure criterion 5 by an executed run, record it beside
the stack with the standing check that keeps it true, correct the living text that is now true, finish the
D-07 sweep, and observe what Renovate did with the new config.

Purpose: "strictly better" must survive the phase as a guarded fact, not a snapshot or a claim about a diff.
Output: `beets.md` closure; final sweep table and Renovate observations in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-05-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md
@stacks/selfhosted/arrs/beets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Executed census run → beets.md § Phase 4 closure; living-text correction (D-25, D-01, D-02)</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md lines 1-50 (living header/table), 199-238 (Phase 1 closure with a pasted executed run — the shape), 953-961 (one-line outcome table), 1004-1039 (end of file)
    - 04-D12-EVIDENCE.md (verdict), 04-09-SUMMARY.md (probe counts, fresh DB facts), 04-11-SUMMARY.md (boot proof, green runs), 04-03-SUMMARY.md (validator transcripts)
  </read_first>
  <action>
    Host `git pull --ff-only`. Run on LXC 100 `bash /mnt/fast/stacks/scripts/check-music-freeze.sh` and capture the output with ANSI stripped and the UTC timestamp; it must exit 0 — if not, record the failing assertion and stop (a red closure run is a gap for /gsd-verify-work, not something to paste as green).
    Append `## Phase 4 — one tagger (<today>)` after the last section, in the Phase 1 closure shape: one paragraph on what is true now and why, with the narrative pointer to `.planning/phases/04-collapse-to-one-tagger/`; "From `bash scripts/check-music-freeze.sh` on LXC 100, <UTC>, ANSI stripped, otherwise unedited." followed by the pasted `📊 7. Summary` census lines (tagger definitions, beets databases, retired paths present, rw on Music non-tagger, rw on Music Jellyfin D-21, tagger-capable containers); a criterion table (| criterion | evidence | where |): 1 — one definition (C1-a) + #306 closed; 2 — validator `--strict --no-global` exit 0 and negative control exit 1; 3 — the D-12 verdict verbatim (PASS/FAIL/OPEN); 4 — MusicBrainz candidates 0 (broken) vs ≥1 (fixed) on the D-29 album, via the amended instrument; 5 — the counters above. Name the standing guards: check-music-freeze.sh § 6b and quick-health-check.sh's vendored-file drift block, both in the routine health check. A "Recorded, not fixed" list: estate-wide `-lsNNN` Renovate silence on every other LinuxServer image (F9 — deferred by the D-30 ruling); `pip install -U beets` on every sabnzbd boot (Phase 8, INGS-01); `audio.bash` `chmod` Exit(1) on every music job (D-31); `Music/__` accidental import (Phase 6); the census's tagger-definition expectation must be revised when beets-flask lands (Phase 5).
    Living text: rewrite the "Two beets, one library" table (27-47) and remove the "Superseded" line 04-05 added: rows for the survivor (`arrs/beets/beets.yaml`, dormant, `2.13.1-ls349` or the recorded ls-build, config `arrs/beets/config.yaml` vendored and `:ro`, `plugins: musicbrainz`, one fresh `library.db`) and for the sabnzbd defused guard (`arrs/sabnzbd/beets-config.yaml`, `:ro`, `plugins: embedart musicbrainz`, nothing invokes it since audio.bash line 285 was stripped). Keep all dated historical sections unchanged.
    Commit (`docs(04-13): Phase 4 closure with executed census (D-25)`), push.
  </action>
  <verify>
    <automated>grep -c '^## Phase 4' stacks/selfhosted/arrs/beets.md && grep -c 'tagger definitions:' stacks/selfhosted/arrs/beets.md && grep -c 'ANSI stripped, otherwise unedited' stacks/selfhosted/arrs/beets.md && test "$(grep -c 'Superseded by Phase 4' stacks/selfhosted/arrs/beets.md)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - C5-a/D-25: the Phase 4 section contains a UTC timestamp, the literal command, and the six census lines with `tagger definitions:          1`, `beets databases:             1`, `retired paths present:       0`, `rw on Music, non-tagger:     0` (whitespace as printed).
    - The criterion table has five rows, row 3 quoting the D-12 verdict exactly as in 04-D12-EVIDENCE.md.
    - `grep -c 'soulbeet/beets_config.yaml' stacks/selfhosted/arrs/beets.md` counts only occurrences inside dated historical sections (record the line numbers).
  </acceptance_criteria>
  <done>Criterion 5 is stated as a measured outcome beside the stack, with the checks that keep it true named.</done>
</task>

<task type="auto">
  <name>Task 2: Final D-07 sweep, Renovate observation, full suite (D-07, D-30, C2-b, C2-c)</name>
  <files>(none unless a sweep hit owned by this plan remains)</files>
  <read_first>
    - 04-05-SUMMARY.md (interim sweep table and owners)
    - 04-VALIDATION.md rows C2-b, C2-c and § Sampling Rate (full suite)
  </read_first>
  <action>
    Sweep: `git ls-files | grep -v '^\.planning/' | xargs grep -il 'wrtag\|soulbeet'` and `git grep -nE 'library\.blb|line 285|beets\.log|01-09 folds|Two beets|soulbeet\.deercrest' -- ':!.planning'`. For every hit record file:line, verdict (keep-with-reason / corrected-in-<plan>), and reason. Any hit whose interim verdict was "corrected" but is still present is a GAP: record it for /gsd-verify-work (do not edit files other plans own).
    Renovate (C2-b, C2-c): `gh issue view 3 --json body -q .body` → assert no `terry90/soulbeet` and no `music/wrtag.yaml` text; `gh issue list --state open --search 'Action Required in:title'` → no config-error issue; `gh pr list --state all --search 'linuxserver/beets'` → record the ls-build PR (e.g. `2.13.1-ls350`) as EXPECTED per D-30, whether it automerged, and that the host is not redeployed by it (deploy drift is expected); if Renovate has not run since 04-03's push, record "not yet observed" — never "working".
    Full suite: validator — reinstall the same pinned `renovate@44.80.0` with `--ignore-scripts` into a scratch prefix (approved at 04-03's legitimacy gate; same version, no new package), run `--strict --no-global renovate.json5` → 0 and `PATH=<prefix>/node_modules/.bin:$PATH bash scripts/check-renovate.sh` → 0 with route `local`; delete the prefix. `bash scripts/quick-health-check.sh` → 0. On LXC 100 the D-23 self-test from 04-02 → 0. Record every exit code and the host HEAD they ran against.
  </action>
  <verify>
    <automated>bash scripts/quick-health-check.sh >/dev/null && ! gh issue view 3 --json body -q .body | grep -q 'terry90/soulbeet' && echo SUITE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Sweep table in SUMMARY covers every hit; zero GAP rows, or each GAP is listed for verification.
    - C2-c: dashboard body has no `terry90/soulbeet`; no open config-error issue; the ls-build PR state recorded as expected / not yet observed.
    - Full suite exit codes all 0 (validator, check-renovate route local, quick-health-check, D-23 self-test), host HEAD recorded; scratch prefix deleted.
  </acceptance_criteria>
  <done>No stale reference is unexplained, Renovate's reaction is observed rather than assumed, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estate evidence → public beets.md | pasted check output is published |
| npm registry → workstation | validator reinstall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13-01 | Information disclosure | pasted census | mitigate | only the summary counter lines are pasted (paths and counts, no release names or secrets) |
| T-04-SC | Tampering | validator reinstall | mitigate | same pinned version approved at 04-03's blocking-human gate; `--ignore-scripts`; scratch prefix deleted |
| T-04-13-02 | Repudiation | closure claims | mitigate | executed run with timestamp; red run is recorded as a gap, never pasted as green |
</threat_model>

<verification>
Closure section present with executed counters; sweep table complete; full suite green.
</verification>

<success_criteria>
The phase goal — one tagger, one database, no idle rw holder — is stated as a measured outcome and guarded by standing checks.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md` when done.
</output>
