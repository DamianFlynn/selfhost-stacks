---
phase: 04-collapse-to-one-tagger
plan: 13
type: execute
wave: 8
depends_on: [04-02, 04-04, 04-05, 04-12]
files_modified:
  - stacks/selfhosted/arrs/beets.md
autonomous: true
requirements: [TAGR-03, TAGR-04, TAGR-05]
tags: [closure, docs, sweep, renovate-observation]

must_haves:
  truths:
    - "D-25 / REVIEWS row 6: only when 04-D12-EVIDENCE.md says `Verdict: PASS` does beets.md gain a dated `## Phase 4 — one tagger` closure section; on OPEN or FAIL it gains `## Phase 4 — interim status` instead, stating the phase is NOT closed and why. Either section carries the census counters (tagger definitions = 1, beets databases = 1, rw on Music non-tagger = 0 and tagger-capable = 0 excluding the D-21 consumer exception, Jellyfin printed separately) pasted ANSI-stripped from an EXECUTED routine run of check-music-freeze.sh with its command and UTC timestamp, and names the standing checks that keep asserting them"
    - "D-01 / D-02: beets.md's 'Two beets, one library' living text now describes the one survivor (dormant, its recorded tag, vendored config declaring musicbrainz, one fresh library.db) and the defused sabnzbd guard config"
    - "D-07: a final repo-wide `wrtag|soulbeet` sweep plus the widened grep has a verdict for every hit outside .planning/, with zero hits left in the 'corrected' state unfixed"
    - "D-30 / REVIEWS row 15: the Renovate outcome is observed and recorded — the soulbeet lookup failure gone from Dependency Dashboard #3, no config-error issue, the ls-build patch PR recorded as EXPECTED (or 'not yet observed'), and whether 04-09 was D-30's deliberate redeploy (tag + digest) or the host stayed on ls349"
    - "REVIEWS row 2: the dashboard and suite checks capture producer status first and require a positive control (the issue's title is the Dependency Dashboard); a failed `gh` call is UNKNOWN, never a clean dashboard"
    - "Follow-ups are recorded, not fixed: the estate-wide -lsNNN Renovate silence (F9), pip -U beets at sabnzbd boot (Phase 8), the baseline chmod Exit(1) (D-31), Music/__ (Phase 6), and the census revision beets-flask will need (Phase 5)"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "Phase 4 closure (PASS) or interim status (OPEN/FAIL) with executed census counters"
      contains: "## Phase 4"
  key_links:
    - from: "beets.md § Phase 4"
      to: "scripts/check-music-freeze.sh § 6b"
      via: "pasted executed routine run + named standing check"
      pattern: "check-music-freeze.sh"
    - from: "04-D12-EVIDENCE.md Verdict line"
      to: "beets.md § Phase 4 heading"
      via: "PASS → closure; OPEN/FAIL → interim status"
      pattern: "Verdict: PASS"
---

<objective>
Close the phase the way Phases 1, 2 and 02.1 closed: measure criterion 5 by an executed run, record it beside
the stack with the standing check that keeps it true, correct the living text that is now true, finish the
D-07 sweep, and observe what Renovate did with the new config. Publish a closure only if criterion 3
passed; otherwise publish an honest interim status (REVIEWS row 6).

Purpose: "strictly better" must survive the phase as a guarded fact, not a snapshot or a claim about a diff —
and a section headed as the closure must not sit on top of an OPEN or FAILED criterion.
Output: `beets.md` closure or interim status; final sweep table and Renovate observations in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-05-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-11-SUMMARY.md
@stacks/selfhosted/arrs/beets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Executed census run → beets.md § Phase 4 closure (PASS) or interim status (OPEN/FAIL); living-text correction (D-25, D-01, D-02, REVIEWS row 6)</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md lines 1-50 (living header/table), 199-238 (Phase 1 closure with a pasted executed run — the shape), 953-961 (one-line outcome table), 1004-1039 (end of file)
    - 04-D12-EVIDENCE.md (the single `Verdict:` line), 04-09-SUMMARY.md (probe counts, fresh DB facts, tag / deliberate-redeploy record), 04-11-SUMMARY.md (boot proof, ledger, promotion, green runs), 04-03-SUMMARY.md (validator transcripts)
  </read_first>
  <action>
    Host `git pull --ff-only`. Read the D-12 verdict: exactly one line matching `^Verdict: (PASS|OPEN|FAIL)` in 04-D12-EVIDENCE.md — anything else is a defect in 04-12: stop and record it. Run on LXC 100 the ROUTINE `bash /mnt/fast/stacks/scripts/check-music-freeze.sh` (no env — the census is promoted by 04-11, so its lines must appear unprompted) and capture the output with ANSI stripped and the UTC timestamp; it must exit 0 and contain the census lines — if not, record the failing assertion and stop (a red closure run is a gap for /gsd-verify-work, not something to paste as green).
    Heading by verdict (REVIEWS row 6): `Verdict: PASS` → append `## Phase 4 — one tagger (<today>)`; `Verdict: OPEN` or `Verdict: FAIL` → append `## Phase 4 — interim status (<today>): criterion 3 <OPEN|FAIL>` whose first paragraph states in plain words that Phase 4 is NOT closed, which criterion-3 condition is open or failed (quoted from the evidence), and what closes it (a real music job judged by 04-D12-EVIDENCE.md's pre-declared condition). Both shapes follow the Phase 1 closure shape: one paragraph on what is true now and why, with the narrative pointer to `.planning/phases/04-collapse-to-one-tagger/`; "From `bash scripts/check-music-freeze.sh` on LXC 100, <UTC>, ANSI stripped, otherwise unedited." followed by the pasted `📊 7. Summary` census lines (tagger definitions, beets databases, tagger databases, retired paths present, rw on Music non-tagger, rw on Music tagger-capable, rw on Music Jellyfin D-21, tagger-capable containers); a criterion table (| criterion | evidence | where |): 1 — one definition (C1-a) + #306 closed; 2 — validator `--strict --no-global` exit 0 and negative control exit 1; 3 — the D-12 verdict verbatim (PASS/FAIL/OPEN); 4 — MusicBrainz candidates 0 (broken) vs ≥1 (fixed) on the D-29 album, via the amended instrument; 5 — the counters above. Name the standing guards: check-music-freeze.sh § 6b and quick-health-check.sh's vendored-file drift block, both promoted into the routine health check by 04-11. A "Recorded, not fixed" list: estate-wide `-lsNNN` Renovate silence on every other LinuxServer image (F9 — deferred by the D-30 ruling); `pip install -U beets` on every sabnzbd boot (Phase 8, INGS-01); `audio.bash` `chmod` Exit(1) on every music job (D-31); `Music/__` accidental import (Phase 6); the census's tagger-definition expectation must be revised when beets-flask lands (Phase 5).
    Living text (true after 04-11 whatever criterion 3's verdict): rewrite the "Two beets, one library" table (27-47) and remove the "Superseded" line 04-05 added: rows for the survivor (`arrs/beets/beets.yaml`, dormant, the tag recorded in 04-09-SUMMARY, config `arrs/beets/config.yaml` vendored and `:ro` (or the recorded measured rollback), `plugins: musicbrainz`, one fresh `library.db`) and for the sabnzbd defused guard (`arrs/sabnzbd/beets-config.yaml`, `:ro`, `plugins: embedart musicbrainz`, nothing invokes it since audio.bash line 285 was stripped). Keep all dated historical sections unchanged.
    Commit (PASS: `docs(04-13): Phase 4 closure with executed census (D-25)`; OPEN/FAIL: `docs(04-13): Phase 4 interim status — criterion 3 <OPEN|FAIL> (D-25)`), push. If the verdict was OPEN or FAIL, the SUMMARY states that the phase is not closable and that /gsd-verify-work must treat criterion 3 as the open gap.
  </action>
  <verify>
    <automated>e=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; b=stacks/selfhosted/arrs/beets.md; test -f $e && test -f $b && v=$(grep -oE '^Verdict: (PASS|OPEN|FAIL)' $e); test "$(printf '%s\n' "$v" | grep -c .)" = 1 && case "$v" in 'Verdict: PASS') grep -q '^## Phase 4 — one tagger' $b ;; 'Verdict: OPEN'|'Verdict: FAIL') grep -q '^## Phase 4 — interim status' $b && test "$(grep -c '^## Phase 4 — one tagger' $b)" = 0 ;; *) false ;; esac && grep -c 'tagger definitions:' $b && grep -c 'ANSI stripped, otherwise unedited' $b && test "$(grep -c 'Superseded by Phase 4' $b)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - REVIEWS row 6: heading is `## Phase 4 — one tagger` iff the evidence says `Verdict: PASS`; otherwise `## Phase 4 — interim status` with a "not closed" statement and no `## Phase 4 — one tagger` heading anywhere.
    - C5-a/D-25: the section contains a UTC timestamp, the literal command, and the census lines with `tagger definitions:          1`, `beets databases:             1`, `retired paths present:       0`, `rw on Music, non-tagger:     0`, `rw on Music, tagger-capable: 0` (whitespace as printed), from a routine (no env) run.
    - The criterion table has five rows, row 3 quoting the D-12 verdict exactly as in 04-D12-EVIDENCE.md.
    - `grep -c 'soulbeet/beets_config.yaml' stacks/selfhosted/arrs/beets.md` counts only occurrences inside dated historical sections (record the line numbers).
  </acceptance_criteria>
  <done>Criterion 5 is stated as a measured outcome beside the stack, with the checks that keep it true named — and the section's heading tells the truth about criterion 3.</done>
</task>

<task type="auto">
  <name>Task 2: Final D-07 sweep, Renovate observation, full suite with producer-status-first checks (D-07, D-30, C2-b, C2-c, REVIEWS rows 2 and 15)</name>
  <files>(none unless a sweep hit owned by this plan remains)</files>
  <read_first>
    - 04-05-SUMMARY.md (interim sweep table and owners)
    - 04-09-SUMMARY.md (tag used; whether it was D-30's deliberate redeploy)
    - 04-VALIDATION.md rows C2-b, C2-c and § Sampling Rate (full suite)
    - .planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md § Consensus Summary rows 2 and 15
  </read_first>
  <action>
    Sweep: `git ls-files | grep -v '^\.planning/' | xargs grep -il 'wrtag\|soulbeet'` and `git grep -nE 'library\.blb|line 285|beets\.log|01-09 folds|Two beets|soulbeet\.deercrest' -- ':!.planning'`. For every hit record file:line, verdict (keep-with-reason / corrected-in-<plan>), and reason. Any hit whose interim verdict was "corrected" but is still present is a GAP: record it for /gsd-verify-work (do not edit files other plans own).
    Renovate (C2-b, C2-c), producer status first (REVIEWS row 2): `t=$(gh issue view 3 --json title -q .title); rc=$?` — rc must be 0 and the title must contain `Dependency Dashboard` (positive control), else UNKNOWN; then `b=$(gh issue view 3 --json body -q .body); rc=$?` — rc 0 and non-empty, else UNKNOWN; only then assert the body has no `terry90/soulbeet` and no `music/wrtag.yaml` text. `gh issue list --state open --search 'Action Required in:title' --json number -q length` — rc must be 0 (a failure is UNKNOWN); `0` means no config-error issue. Drive the error branch once: the title read under `GH_TOKEN=invalid-04` must fail and be reported UNKNOWN, never as a clean dashboard; record the transcript. `gh pr list --state all --search 'linuxserver/beets'` (rc captured) → record the ls-build PR (e.g. `2.13.1-ls350`) as EXPECTED per D-30, and whether it automerged; record from 04-09-SUMMARY whether 04-09 was D-30's deliberate redeploy (tag + digest) or the host stayed on `2.13.1-ls349` (REVIEWS row 15) — any ls-build merged after 04-09 is recorded as expected repo-side drift, not deployed; if Renovate has not run since 04-03's push, record "not yet observed" — never "working".
    Full suite: validator — reinstall the same pinned `renovate@44.80.0` with `--ignore-scripts` into a scratch prefix (approved at 04-03's legitimacy gate; same version, no new package), run `--strict --no-global renovate.json5` → 0 and `PATH=<prefix>/node_modules/.bin:$PATH bash scripts/check-renovate.sh` → 0 with route `local`; delete the prefix. `bash scripts/quick-health-check.sh` → 0 (census and drift blocks promoted). On LXC 100 the D-23 self-test from 04-02 → 0. Record every exit code and the host HEAD they ran against.
  </action>
  <verify>
    <automated>q=$(bash scripts/quick-health-check.sh 2>&1); qrc=$?; [ "$qrc" -eq 0 ] || { echo "quick-health-check rc=$qrc"; exit 1; }; t=$(gh issue view 3 --json title -q .title); trc=$?; [ "$trc" -eq 0 ] || { echo "UNKNOWN gh title rc=$trc"; exit 3; }; printf '%s' "$t" | grep -qi 'dependency dashboard' || { echo "UNKNOWN issue 3 is not the dashboard: $t"; exit 3; }; b=$(gh issue view 3 --json body -q .body); brc=$?; [ "$brc" -eq 0 ] && [ -n "$b" ] || { echo "UNKNOWN gh body rc=$brc"; exit 3; }; if printf '%s' "$b" | grep -q 'terry90/soulbeet'; then echo 'DASHBOARD STILL NAMES SOULBEET'; exit 1; fi; echo SUITE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Sweep table in SUMMARY covers every hit; zero GAP rows, or each GAP is listed for verification.
    - C2-c: dashboard read with rc 0 and title positive control, body has no `terry90/soulbeet`; `Action Required` search rc 0 with count 0; the `GH_TOKEN=invalid-04` run reported UNKNOWN; the ls-build PR state recorded as expected / not yet observed; the 04-09 redeploy decision (tag + digest, or ls349) recorded.
    - Full suite exit codes all 0 (validator, check-renovate route local, quick-health-check, D-23 self-test), host HEAD recorded; scratch prefix deleted.
  </acceptance_criteria>
  <done>No stale reference is unexplained, Renovate's reaction is observed rather than assumed, and the full suite is green by checks that cannot pass on a failed instrument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estate evidence → public beets.md | pasted check output is published |
| npm registry → workstation | validator reinstall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13-01 | Information disclosure | pasted census | mitigate | only the summary counter lines are pasted (paths and counts, no release names or secrets) |
| T-04-SC | Tampering | validator reinstall | mitigate | same pinned version approved at 04-03's blocking-human gate; `--ignore-scripts`; scratch prefix deleted |
| T-04-13-02 | Repudiation | closure claims | mitigate | executed routine run with timestamp; red run recorded as a gap, never pasted as green; closure heading only on `Verdict: PASS` |
| T-04-13-03 | Repudiation | false-green dashboard check | mitigate | gh status captured first, title positive control, error branch driven with an invalid token |
</threat_model>

<verification>
Closure or interim-status section present per the verdict, with executed counters; sweep table complete; full suite green.
</verification>

<success_criteria>
The phase goal — one tagger, one database, no idle rw holder — is stated as a measured outcome and guarded by standing checks, and the phase is called closed only if criterion 3 passed.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS rows 2, 6 and 15; the full table is in 04-01-PLAN.md.
- **Row 6**: Task 1 writes the closure heading only on `Verdict: PASS`; on OPEN or FAIL it writes the interim-status heading, which states the phase is not closed. The verify enforces the mapping.
- **Row 2**: the Task 2 verify captures `gh` status first, uses the dashboard title as a positive control, and drives the error branch with `GH_TOKEN=invalid-04`.
- **Row 15**: Task 2 records whether 04-09 was D-30's deliberate redeploy.
- Task 1's census paste now comes from a routine (no env) run, proving the census was promoted.
