---
phase: 04-collapse-to-one-tagger
plan: 16
type: execute
wave: 11
depends_on: [04-15]
files_modified:
  - stacks/selfhosted/arrs/beets.md
autonomous: true
gap_closure: true
requirements: [TAGR-04]
tags: [closure, interim-status, docs, d-25, gap-closure]

must_haves:
  truths:
    - "stacks/selfhosted/arrs/beets.md's Phase 4 heading is selected by the machine-readable window-2 verdict line in 04-D12-EVIDENCE.md, and the mapping is enforced by this plan's own verify rather than trusted to the author"
    - "A closure heading appears if and only if the window-2 verdict is PASS; an OPEN or FAIL verdict keeps an interim-status heading naming the actual verdict word"
    - "The criterion-3 row of beets.md's five-criteria table quotes the window-2 verdict word and names its three source plans, asserted mechanically, so a correct heading cannot sit above a stale row"
    - "Both standing guards are re-run against the landed text and their identifying lines are quoted, with check-music-freeze.sh executed directly on LXC 100 rather than inferred from the fold-in"
    - "The window-1 OPEN record is kept rather than overwritten, and no dated historical section is deleted"
    - "No release name, job folder name or credential enters this public repo through the added text"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "the Phase 4 status section headed to match the window-2 verdict, with criterion 3 recording window 2's outcome"
      contains: "window 2"
  key_links:
    - from: "04-D12-EVIDENCE.md § 7 verdict line"
      to: "beets.md Phase 4 heading"
      via: "machine-readable verdict-to-heading mapping asserted in verify"
      pattern: "Verdict \\(window 2\\)"
    - from: "04-D12-EVIDENCE.md § 7 verdict line"
      to: "beets.md criterion-table row 3"
      via: "the row must carry the literal token 'window 2: <verdict>' and the source list 04-12, 04-14, 04-15"
      pattern: "window 2: (PASS|OPEN|FAIL)"
---

<objective>
Close the estate record for criterion 3 with whatever window 2 actually measured.

`stacks/selfhosted/arrs/beets.md` carries, at line 1126, the heading
`## Phase 4 — interim status (2026-09-13): criterion 3 OPEN`, written by plan 04-13 precisely because a
closure heading over an OPEN criterion is the defect round 1's REVIEWS row 6 exists to prevent. Plan
04-15 has now recorded a window-2 verdict, supported by a machine-readable observation block that its
own verify checked the verdict against. This plan transcribes it.

Purpose: the heading is chosen by a verdict line, not by an author's judgement, and this plan's own
`<verify>` enforces the mapping — the same shape 04-13 used and proved. A PASS earns the closure
heading; an OPEN or a FAIL does not, and neither is written as though it were something else. The
verify also enforces the criterion-table row, because a correct heading above a stale row leaves the
file contradicting itself while the machine check still prints green (round 2 row 16).
Output: `beets.md`'s Phase 4 section, headed correctly, with criterion 3's row recording window 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-15-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the heading from the verdict line and record window 2 in the criterion table</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md lines 1124-1210 — the current Phase 4 section as it actually stands: the interim heading at **1126**, the opening paragraph at 1128-1137, the "Nothing failed / OPEN is not FAIL" mechanism paragraph at 1139-1150 which names the two candidate watcher fixes and whose "What closes it" sentence begins at **1146**, the executed census block at 1155-1181, and the five-criteria table at 1183-1191 whose row 3 is at line **1189**.
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md — § 7, for the window-2 verdict line quoted verbatim and the `OBS window2` block behind it, and § 6, whose window-1 verdict stands unchanged.
    - .planning/phases/04-collapse-to-one-tagger/04-15-SUMMARY.md — the measured values behind the verdict.
    - .planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md § "THE HEADLINE" — the heading-selection discipline this task repeats, and the reason it exists.
  </read_first>
  <action>
    Re-derive the verdict rather than taking it from the orchestrator or from a SUMMARY headline: read the
    single line matching `^Verdict \(window 2\): (PASS|OPEN|FAIL) — ` from `04-D12-EVIDENCE.md`, assert
    there is exactly one, and quote it. Extract the token with a delimited match — `sed -nE 's/^Verdict
    \(window 2\): (PASS|OPEN|FAIL) —.*/\1/p'` — never with a bare prefix match, which would read
    `PASSING` as `PASS`. 04-12 recorded what happens when a handed-down figure is trusted: the one fact
    the whole verdict turned on was wrong.

    Apply exactly this mapping to the heading at line 1126, replacing it in place:
    - **PASS** → `## Phase 4 — one tagger (<today's date>)`. This is the closure heading form named in
      `04-PATTERNS.md` § beets.md Analog B; its current count in the file is 0.
    - **OPEN** → `## Phase 4 — interim status (<today's date>): criterion 3 OPEN`
    - **FAIL** → `## Phase 4 — interim status (<today's date>): criterion 3 FAIL`

    Then update the section body for the verdict that was actually reached.

    **If PASS:** rewrite the opening paragraph at 1128-1137 so it no longer says the phase is not closed.
    It must state that criterion 3 was closed by a second observation window; quote the window-2 verdict
    line verbatim; and state in two or three sentences what changed — the PRE-HOOK snapshot is now taken
    while the job is still under `/downloads/incomplete`, which is structurally before SABnzbd's move and
    therefore before the hook, so the ordering no longer depends on comparing a millisecond watcher clock
    against a second-resolution log; and the snapshot is published only when the folder's inventory is
    unchanged across the hash pass and is still the same inventory when the folder vanishes, so a
    baseline that is merely *a* stable set rather than *the final* set is reported UNPROVEN rather than
    mistaken for a byte failure. **Keep the window-1 record**: retain the 1139-1150 mechanism paragraph,
    re-headed or re-worded as the history of why the first window could not close it, with its
    measurement intact. Deleting it would delete the finding, which is the more durable half of window 1.

    **If OPEN or FAIL:** keep the section's interim framing and its existing mechanism paragraph. Replace
    the "What closes it" sentence beginning at 1146 — which currently names the two candidate watcher
    fixes as future work — with a dated in-band note recording that the incomplete-tree fix was built and
    self-tested against eight synthetic controls (plan 04-14), that a second window ran (plan 04-15), and
    what its verdict was and why. For OPEN, state plainly that OPEN means the instrument could not look,
    not that the estate is dirty, and name what is still needed. For FAIL, name the violated condition and
    state that this is a real finding about the estate requiring a follow-up phase decision, not an
    instrument problem — and say which job it was found on, by 12-character prefix, if more than one job
    ran.

    In every case, update the five-criteria table's row 3 at line 1189. It currently reads
    `**3 — a real music job completes with no tagger**` with the cell `**\`Verdict: OPEN\`** — quoted verbatim
    above. Two real jobs ran clean; the byte proof could not be taken` and the source cell
    `04-12, \`04-D12-EVIDENCE.md\``. The new cell must carry the literal token `window 2: <VERDICT>` — the
    exact string, so a grep can find it and so window 1's own `OPEN` elsewhere in the row cannot satisfy
    the check by accident — state in one clause what window 2 measured, and keep a clause recording that
    window 1 ran clean but could not take the byte proof. The source cell becomes
    `04-12, 04-14, 04-15, \`04-D12-EVIDENCE.md\``.

    Leave every other part of the file untouched: the executed census block at 1155-1181, the
    `tagger-capable containers: 2` explanation, the "What keeps these true" standing-guards section at
    1193 onward, and every dated historical Phase 1/2/3 section. Amend in band and dated; never delete a
    dated historical statement.

    Screen the added lines before committing: after deleting every 64-character lowercase hexadecimal run,
    0 runs of 40 or more characters; 0 matches for
    `user_token|discogs\.env|/secrets/|token=|apikey|api_key`; and no release or job folder name — the
    window-2 job is referred to only as "one music job" or by its 12-character sha256 prefix. Commit only
    this file: `docs(04-16): record the window-2 verdict for criterion 3 in beets.md`.
  </action>
  <verify>
    <automated>set -o pipefail; e=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; b=stacks/selfhosted/arrs/beets.md; test -f "$e" -a -f "$b" || { echo FAIL-input-missing; exit 1; }; test "$(grep -cE '^Verdict \(window 2\): (PASS|OPEN|FAIL) — ' "$e")" = 1 || { echo NO-SINGLE-DELIMITED-VERDICT; exit 1; }; V=$(sed -nE 's/^Verdict \(window 2\): (PASS|OPEN|FAIL) —.*/\1/p' "$e"); test -n "$V" || { echo FAIL-verdict-unparsable; exit 1; }; C=$(grep -c '^## Phase 4 — one tagger' "$b") || true; I=$(grep -cE '^## Phase 4 — interim status' "$b") || true; IV=$(grep -cE "^## Phase 4 — interim status .*: criterion 3 $V\$" "$b") || true; if [ "$V" = PASS ]; then test "$C" = 1 || { echo FAIL-PASS-without-closure-heading; exit 1; }; test "$I" = 0 || { echo FAIL-PASS-but-$I-interim-headings-remain; exit 1; }; else test "$C" = 0 || { echo FAIL-closure-heading-over-a-$V-verdict; exit 1; }; test "$I" = 1 || { echo FAIL-expected-one-interim-heading-found-$I; exit 1; }; test "$IV" = 1 || { echo FAIL-interim-heading-does-not-name-$V; exit 1; }; fi; R=$(grep -c '^| \*\*3 — a real music job completes with no tagger\*\*' "$b") || true; test "$R" = 1 || { echo FAIL-criterion-row-3-appears-$R-times; exit 1; }; grep '^| \*\*3 — a real music job completes with no tagger\*\*' "$b" | grep -q "window 2: $V" || { echo FAIL-row-3-does-not-quote-window-2-$V; exit 1; }; grep '^| \*\*3 — a real music job completes with no tagger\*\*' "$b" | grep -q '04-12, 04-14, 04-15' || { echo FAIL-row-3-source-cell-stale; exit 1; }; test "$(grep -c 'window 2' "$b")" -ge 1 || { echo FAIL-no-window-2-mention; exit 1; }; test "$(grep -c 'ANSI stripped, otherwise unedited' "$b")" = 1 || { echo FAIL-census-provenance-line-changed; exit 1; }; test -z "$(diff <(git show HEAD~1:"$b" | sed -n '/^📊 7\. Summary/,/^```/p') <(sed -n '/^📊 7\. Summary/,/^```/p' "$b"))" || { echo FAIL-executed-census-block-edited; exit 1; }; test "$(git show --name-only --format= HEAD | grep -c .)" = 1 || { echo FAIL-commit-touches-more-than-one-file; exit 1; }; git show --name-only --format= HEAD | grep -q 'stacks/selfhosted/arrs/beets.md' || { echo FAIL-commit-is-not-beets-md; exit 1; }; test "$(git show --diff-filter=D --name-only --format= HEAD | grep -c .)" = 0 || { echo FAIL-commit-deletes-a-file; exit 1; }; echo "MAP-OK($V)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one line in `04-D12-EVIDENCE.md` matches `^Verdict \(window 2\): (PASS|OPEN|FAIL) — `, asserted, and the SUMMARY quotes it verbatim as re-derived by this plan rather than carried in. The token is extracted with a delimited `sed` match, so `PASSING` cannot parse as `PASS`.
    - The heading mapping holds, asserted by the verify: for PASS, `grep -c '^## Phase 4 — one tagger'` = 1 and `grep -cE '^## Phase 4 — interim status'` = **0** — the count is over *any* interim heading, not only ones naming criterion 3, so a leftover interim heading of another shape cannot survive a closure; for OPEN or FAIL the counts are 0 and 1 and the interim heading's trailing word equals the verdict word.
    - **The criterion-table row is asserted, not just the heading:** row 3 exists exactly once, contains the literal token `window 2: <VERDICT>`, and contains the source list `04-12, 04-14, 04-15`. A correct heading above a stale row now fails the gate rather than printing `MAP-OK`.
    - `grep -c 'window 2' stacks/selfhosted/arrs/beets.md` >= 1, asserted.
    - The executed census block is unchanged: `grep -c 'ANSI stripped, otherwise unedited'` = 1 and the `📊 7. Summary` counter block is proven byte-identical to the pre-edit version by a `diff` against `git show HEAD~1`, asserted — not merely inspected.
    - No dated historical section is deleted: the commit touches exactly `stacks/selfhosted/arrs/beets.md`, deletes no file, and its deletions are confined to the heading, the opening paragraph, the "What closes it" sentence and the criterion-3 row — the first three asserted mechanically, the last confirmed by reading `git diff` in the SUMMARY.
    - Added lines screened: after 64-character hex digests are filtered out, 0 runs of 40+ characters; 0 credential-pattern matches; 0 release or job folder names.
  </acceptance_criteria>
  <done>The durable estate record beside the stack states criterion 3's real outcome, with a heading and a criterion row that a machine chose and checked against the verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run both standing guards against the landed text and record the phase's actual closing state</name>
  <files>(none — measurement only)</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md — as written by Task 1, so the guards are re-run against the landed text rather than the intended text
    - .planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md — the four criteria already verified, which this task does not re-litigate
    - .planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md § "Full suite" — the suite shape and the exit codes it recorded, and deviation 2, the `tail -25` that truncated the exact line an acceptance criterion named
  </read_first>
  <action>
    Run and capture the exit code of each, quoting the identifying line rather than inferring it from the
    exit code — 04-13 deviation 2 recorded a `tail -25` truncating the exact line an acceptance criterion
    named, leaving an exit code that was reachable for more than one reason:
    - `bash scripts/quick-health-check.sh` from the workstation. Expect exit 0. Quote the
      `✅ vendored files match (3)` drift line and the `📊` census counters, and confirm the only `❌` is the
      report-only `Traefik dashboard: Not accessible` entry from the 04-01 ROUTINE BASELINE.
    - `bash scripts/check-music-freeze.sh` **directly on LXC 100**, not inferred from the fold-in. The
      fold-in is real — `scripts/quick-health-check.sh:697` runs this script over `ssh -n` — so the guard
      does execute host-side on every routine run, and round 2 downgraded the claim that it never runs.
      What the previous revision genuinely lacked was any assertion over the **named command**, so run it
      directly and assert exit 0 with `FAILURES total: 0` and the census counters at target.
      **Bind the run to the guard's own version, not to Task 1's commit.** Assert that the host checkout at
      `/mnt/fast/stacks` contains the commit that last touched `scripts/check-music-freeze.sh`, using
      `git merge-base --is-ancestor`, and record both that commit and the host HEAD. Do **not** require the
      host to contain Task 1's documentation commit: a docs-only edit to `beets.md` does not change the
      guard being executed, and coupling the two would force a push-and-pull cycle for no measurement
      (round 2, astra C11 downgraded). If the guard's own commit is missing host-side, push and pull first —
      04-13 deviation 1 recorded a host pull that would otherwise have run against a checkout missing the
      very artefacts being reasoned from.
    - `grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash` → 0, re-derived here as the
      static half of criterion 3, which has been verified throughout and is not re-planned.

    Then state, in the SUMMARY and in one short table, the phase's actual closing position: which of the
    five ROADMAP criteria hold, what criterion 3's window-2 verdict is, and — if it is not PASS — exactly
    what remains and what it would take, without proposing it as work for this plan.

    **If criterion 3 is FAIL, expect a guard to be red and record it as such.** A FAIL means beets state
    was regenerated or a tagger artefact appeared, which is precisely what `check-music-freeze.sh` exists to
    detect; a non-zero exit from it in that branch is the guard working, not a problem to be re-run away.
    Record the exit code and the failing lines verbatim, and say plainly that the red is the consequence of
    the finding rather than a regression introduced by this plan.

    Record two carry-forwards explicitly so they are met as decisions rather than silences: `DEF-04-01` (the
    unqualified "beets has no `undo` command" claim in `CLAUDE.md:152` and `.planning/PROJECT.md:187`)
    remains **deferred to Phase 7** and was not touched by this plan; `beets.md`'s two open config gaps
    (`match.preferred.countries`, and `fromfilename`/`edit` for bucket C) remain **deferred to Phase 6**.
    Neither is a gap of this closure.

    Do not modify `.planning/STATE.md` or `.planning/ROADMAP.md`, and do not call any `gsd-sdk query state.*`
    or `roadmap.*` verb — the orchestrator owns both, and 04-12 and 04-13 each recorded holding that line.
  </action>
  <verify>
    <automated>set -o pipefail; Q=$(mktemp); bash scripts/quick-health-check.sh > "$Q" 2>&1; QHC=$?; test "$(grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash)" = 0 || { echo FAIL-beet-invocation-present; rm -f "$Q"; exit 1; }; test -s "$Q" || { echo UNKNOWN-quick-health-check-produced-no-output; rm -f "$Q"; exit 3; }; test "$QHC" = 0 || { echo FAIL-quick-health-check-exit-$QHC; rm -f "$Q"; exit 1; }; grep -q 'vendored files match (3)' "$Q" || { echo FAIL-drift-line-absent; rm -f "$Q"; exit 1; }; rm -f "$Q"; G=$(git log -1 --format=%H -- scripts/check-music-freeze.sh); test -n "$G" || { echo FAIL-guard-commit-unknown; exit 1; }; ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "timeout 60 git -C /mnt/fast/stacks merge-base --is-ancestor $G HEAD"; ARC=$?; test "$ARC" = 0 || { echo FAIL-host-checkout-lacks-guard-commit-rc-$ARC; exit 1; }; FZ=$(ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "timeout 200 bash /mnt/fast/stacks/scripts/check-music-freeze.sh 2>&1"); FRC=$?; if [ -z "$FZ" ] && [ "$FRC" -ne 124 ]; then echo UNKNOWN-freeze-guard-unreachable-or-silent; exit 3; fi; test "$FRC" != 124 || { echo UNKNOWN-freeze-guard-exceeded-its-200s-bound; exit 3; }; test "$FRC" = 0 || { echo FAIL-check-music-freeze-exit-$FRC; exit 1; }; printf '%s\n' "$FZ" | grep -qE 'FAILURES total:[[:space:]]+0' || { echo FAIL-freeze-guard-nonzero-failures; exit 1; }; printf '%s\n' "$FZ" | grep -qE 'beets databases:[[:space:]]+1' || { echo FAIL-census-counter-drifted; exit 1; }; echo SUITE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `quick-health-check.sh` exit code recorded as 0, asserted, with the `✅ vendored files match (3)` line and the census counters quoted from the captured output, not inferred from the exit code. The verify asserts the captured output is non-empty **before** reading the exit code, so a silent run is UNKNOWN rather than green.
    - **`check-music-freeze.sh` is executed by name on LXC 100 and asserted**, with exit 0, `FAILURES total: 0` and `beets databases: 1` all matched in its captured output. The empty-output branch is tested first and the 200-second timeout branch is distinguished, both returning UNKNOWN (exit 3) rather than a pass.
    - The host checkout at `/mnt/fast/stacks` is asserted to contain the commit that last touched `scripts/check-music-freeze.sh`, by `git merge-base --is-ancestor`, so the guard that ran is the guard in the repo. It is deliberately **not** bound to Task 1's documentation commit, and the SUMMARY states that choice and its reason.
    - `grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash` = 0, asserted and quoted.
    - The SUMMARY carries the five-criterion closing table with criterion 3's window-2 verdict word.
    - If criterion 3 is FAIL and a standing guard is red as a consequence, its non-zero exit code and failing lines are recorded verbatim, and the SUMMARY states the red is the guard detecting this phase's own finding. In that branch this verify's `FAIL-check-music-freeze-exit-<n>` token is the expected result and the deviation is recorded rather than the guard re-run to green.
    - DEF-04-01 is named as still deferred to Phase 7, and the two `beets.md` config gaps as still deferred to Phase 6.
    - `.planning/STATE.md` and `.planning/ROADMAP.md` appear in no commit from this plan.
  </acceptance_criteria>
  <done>Both standing guards are green against the landed text — or red for a reason this plan names — and the phase's real closing position is written down rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence → public estate record | text copied from the evidence file into a tracked, public document |
| author → heading | a heading could assert a closure the measurement does not support |
| author → criterion table | a row could keep an outdated verdict under a correct heading |
| repo → host checkout | a guard could be run against bytes other than the ones in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16-01 | Information disclosure | beets.md added text | mitigate | the job is referred to as "one music job" or by 12-character sha256 prefix; no `storage` path beyond the `/downloads/complete/nzb/music/` prefix; added lines screened for credential patterns and for 40-character runs **after** 64-character hex digests are filtered out |
| T-04-16-02 | Repudiation | a closure heading over a non-PASS verdict | mitigate | the heading is selected by the single machine-readable `^Verdict \(window 2\): … — ` line, extracted with a delimited match so `PASSING` cannot parse as `PASS`; the mapping is enforced by this plan's `<verify>`, with the PASS branch asserting **zero** interim headings of any shape; the verdict is re-derived from the evidence file rather than carried from a SUMMARY |
| T-04-16-03 | Tampering | loss of the window-1 record, or a stale criterion row | mitigate | the window-1 mechanism paragraph is retained in every branch; the executed census block is proven byte-identical by a `diff` against `git show HEAD~1`; the commit is asserted to delete no file; and criterion row 3 is asserted to carry the literal `window 2: <VERDICT>` token and the `04-12, 04-14, 04-15` source list, so a correct heading cannot sit above an outdated row |
| T-04-16-04 | Tampering | measurement run against a stale host checkout | mitigate | the host checkout is asserted to contain the commit that last touched `scripts/check-music-freeze.sh` — the guard actually being executed — rather than a documentation commit that does not change it; host HEAD and the guard commit are both recorded |
| T-04-16-05 | Repudiation | a guard result suppressed to keep a closure tidy | mitigate | the FAIL branch requires a red guard to be recorded verbatim with its exit code and named as the consequence of the finding; re-running until green is explicitly prohibited |
</threat_model>

<verification>
The heading matches the verdict by machine-checked mapping, with the PASS branch requiring zero interim
headings of any shape; the criterion-3 row is asserted to quote the window-2 verdict and its three source
plans; the census block and every dated historical section survive byte-identical; and both standing
guards are executed by name — `check-music-freeze.sh` directly on LXC 100 against a checkout asserted to
contain the guard's own commit — with their identifying lines matched rather than inferred from an exit
code.
</verification>

<success_criteria>
The estate's durable record beside the stack states what criterion 3 actually measured — closure only if
the measurement earned it, and a red guard recorded rather than tidied away if the measurement found one.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-16-SUMMARY.md` when done.
</output>
