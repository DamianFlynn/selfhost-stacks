---
phase: 04-collapse-to-one-tagger
plan: 16
type: execute
wave: 11
depends_on: [04-15]
files_modified:
  - stacks/selfhosted/arrs/beets.md
autonomous: true
gap_closure: true
requirements: [TAGR-04]
tags: [closure, interim-status, docs, d-25, gap-closure]

must_haves:
  truths:
    - "stacks/selfhosted/arrs/beets.md's Phase 4 heading is selected by the machine-readable window-2 verdict line in 04-D12-EVIDENCE.md, and the mapping is enforced by this plan's own verify rather than trusted to the author"
    - "A closure heading appears if and only if the window-2 verdict is PASS; an OPEN or FAIL verdict keeps an interim-status heading naming the actual verdict word"
    - "The criterion-3 row of beets.md's five-criteria table quotes the window-2 verdict verbatim, and the window-1 OPEN record is kept rather than overwritten"
    - "No release name, job folder name or credential enters this public repo through the added text"
  artifacts:
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "the Phase 4 status section headed to match the window-2 verdict, with criterion 3 recording window 2's outcome"
      contains: "window 2"
  key_links:
    - from: "04-D12-EVIDENCE.md § 7 verdict line"
      to: "beets.md Phase 4 heading"
      via: "machine-readable verdict-to-heading mapping asserted in verify"
      pattern: "Verdict \\(window 2\\)"
---

<objective>
Close the estate record for criterion 3 with whatever window 2 actually measured.

`stacks/selfhosted/arrs/beets.md` carries, at line 1126, the heading
`## Phase 4 — interim status (2026-09-13): criterion 3 OPEN`, written by plan 04-13 precisely because a
closure heading over an OPEN criterion is the defect REVIEWS row 6 exists to prevent. Plan 04-15 has now
recorded a window-2 verdict. This plan transcribes it.

Purpose: the heading is chosen by a verdict line, not by an author's judgement, and this plan's own
`<verify>` enforces the mapping — the same shape 04-13 used and proved. A PASS earns the closure heading;
an OPEN or a FAIL does not, and neither is written as though it were something else.
Output: `beets.md`'s Phase 4 section, headed correctly, with criterion 3's row recording window 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md
@.planning/phases/04-collapse-to-one-tagger/04-15-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the heading from the verdict line and record window 2 in the criterion table</name>
  <files>stacks/selfhosted/arrs/beets.md</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md lines 1124-1210 — the current Phase 4 section as it actually stands: the interim heading at 1126, the "Nothing failed / OPEN is not FAIL" paragraph at 1139-1150 which names the two candidate watcher fixes, the executed census block at 1155-1181, and the five-criteria table at 1185-1191 whose row 3 is at line 1189.
    - .planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md — § 7, for the window-2 verdict line quoted verbatim, and § 6, whose window-1 verdict stands unchanged.
    - .planning/phases/04-collapse-to-one-tagger/04-15-SUMMARY.md — the measured values behind the verdict.
    - .planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md § "THE HEADLINE" — the heading-selection discipline this task repeats, and the reason it exists.
  </read_first>
  <action>
    Re-derive the verdict rather than taking it from the orchestrator or from a SUMMARY headline: read the
    single line matching `^Verdict \(window 2\): (PASS|OPEN|FAIL)` from `04-D12-EVIDENCE.md`, assert there is
    exactly one, and quote it. 04-12 recorded what happens when a handed-down figure is trusted — the one
    fact the whole verdict turned on was wrong.

    Apply exactly this mapping to the heading at line 1126, replacing it in place:
    - **PASS** → `## Phase 4 — one tagger (<today's date>)`. This is the closure heading form named in
      `04-PATTERNS.md` § beets.md Analog B; its current count in the file is 0.
    - **OPEN** → `## Phase 4 — interim status (<today's date>): criterion 3 OPEN`
    - **FAIL** → `## Phase 4 — interim status (<today's date>): criterion 3 FAIL`

    Then update the section body for the verdict that was actually reached.

    **If PASS:** rewrite the opening paragraph at 1128-1137 so it no longer says the phase is not closed.
    It must state that criterion 3 was closed by a second observation window; quote the window-2 verdict
    line verbatim; and state in one or two sentences what changed — the PRE-HOOK snapshot is now taken
    while the job is still under `/downloads/incomplete`, which is structurally before SABnzbd's move and
    therefore before the hook, so the ordering no longer depends on comparing a millisecond watcher clock
    against a second-resolution log. **Keep the window-1 record**: retain the 1139-1150 mechanism paragraph,
    re-headed or re-worded as the history of why the first window could not close it, with its measurement
    intact. Deleting it would delete the finding, which is the more durable half of window 1.

    **If OPEN or FAIL:** keep the section's interim framing and its existing mechanism paragraph. Replace
    the "What closes it" sentence at 1146-1150 — which currently names the two candidate watcher fixes as
    future work — with a dated in-band note recording that the incomplete-tree fix was built and self-tested
    (plan 04-14), that a second window ran (plan 04-15), and what its verdict was and why. For OPEN, state
    plainly that OPEN means the instrument could not look, not that the estate is dirty, and name what is
    still needed. For FAIL, name the violated condition and state that this is a real finding about the
    estate requiring a follow-up phase decision, not an instrument problem.

    In every case, update the five-criteria table's row 3 at line 1189. It currently reads
    `**3 — a real music job completes with no tagger**` with the cell `**\`Verdict: OPEN\`** — quoted verbatim
    above. Two real jobs ran clean; the byte proof could not be taken` and the source cell
    `04-12, \`04-D12-EVIDENCE.md\``. The new cell must quote the **window-2** verdict word, state in one
    clause what window 2 measured, and keep a clause recording that window 1 ran clean but could not take
    the byte proof. The source cell becomes `04-12, 04-14, 04-15, \`04-D12-EVIDENCE.md\``.

    Leave every other part of the file untouched: the executed census block at 1155-1181, the
    `tagger-capable containers: 2` explanation, the "What keeps these true" standing-guards section, and
    every dated historical Phase 1/2/3 section. Amend in band and dated; never delete a dated historical
    statement.

    Screen the added lines before committing: 0 runs of 40 or more characters, 0 matches for
    `user_token|discogs\.env|/secrets/|token=|apikey|api_key`, and no release or job folder name — the
    window-2 job is referred to only as "one music job" or by its 12-character sha256 prefix. Commit only
    this file: `docs(04-16): record the window-2 verdict for criterion 3 in beets.md`.
  </action>
  <verify>
    <automated>e=.planning/phases/04-collapse-to-one-tagger/04-D12-EVIDENCE.md; b=stacks/selfhosted/arrs/beets.md; V=$(grep -oE '^Verdict \(window 2\): (PASS|OPEN|FAIL)' "$e" | awk '{print $NF}'); test "$(grep -cE '^Verdict \(window 2\): (PASS|OPEN|FAIL)' "$e")" = 1 || { echo 'NO-SINGLE-VERDICT'; exit 1; }; C=$(grep -c '^## Phase 4 — one tagger' "$b"); I=$(grep -cE '^## Phase 4 — interim status .*: criterion 3 (OPEN|FAIL)' "$b"); if [ "$V" = PASS ]; then test "$C" = 1 -a "$I" = 0 && echo "MAP-OK(pass)"; else test "$C" = 0 -a "$I" = 1 && grep -qE "^## Phase 4 — interim status .*: criterion 3 $V\$" "$b" && echo "MAP-OK($V)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one line in `04-D12-EVIDENCE.md` matches `^Verdict \(window 2\): (PASS|OPEN|FAIL)`, and the SUMMARY quotes it verbatim as re-derived by this plan rather than carried in.
    - The heading mapping holds, asserted by the verify: for PASS, `grep -c '^## Phase 4 — one tagger'` = 1 and `grep -cE '^## Phase 4 — interim status'` = 0; for OPEN or FAIL, the counts are 0 and 1 and the interim heading's trailing word equals the verdict word.
    - `beets.md`'s criterion-table row 3 quotes the window-2 verdict word and names `04-12, 04-14, 04-15` as its sources.
    - `grep -c 'window 2' stacks/selfhosted/arrs/beets.md` >= 1.
    - The executed census block is unchanged: `grep -c 'ANSI stripped, otherwise unedited'` = 1 and the nine counter lines under `📊 7. Summary` are byte-identical to before the edit, confirmed with `git diff`.
    - `git diff --numstat` shows the deleted lines are confined to the heading, the opening paragraph, the "What closes it" sentence and the criterion-3 row — no dated historical section is deleted.
    - Added lines screened: 0 runs of 40+ characters, 0 credential-pattern matches, 0 release or job folder names.
    - The commit touches exactly `stacks/selfhosted/arrs/beets.md` and deletes no file.
  </acceptance_criteria>
  <done>The durable estate record beside the stack states criterion 3's real outcome, with a heading a machine chose from the verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the standing guards and record the phase's actual closing state</name>
  <files>(none — measurement only)</files>
  <read_first>
    - stacks/selfhosted/arrs/beets.md — as written by Task 1, so the guards are re-run against the landed text rather than the intended text
    - .planning/phases/04-collapse-to-one-tagger/04-VERIFICATION.md — the four criteria already verified, which this task does not re-litigate
    - .planning/phases/04-collapse-to-one-tagger/04-13-SUMMARY.md § "Full suite" — the suite shape and the exit codes it recorded
  </read_first>
  <action>
    Run and capture the exit code of each, quoting the identifying line rather than inferring it from the
    exit code — 04-13 deviation 2 recorded a `tail -25` truncating the exact line an acceptance criterion
    named, leaving an exit code that was reachable for more than one reason:
    - `bash scripts/quick-health-check.sh` from the workstation. Expect exit 0. Quote the
      `✅ vendored files match (3)` drift line and the `📊` census counters, and confirm the only `❌` is the
      report-only `Traefik dashboard: Not accessible` entry from the 04-01 ROUTINE BASELINE.
    - `bash scripts/check-music-freeze.sh` on LXC 100, after asserting the host checkout at
      `/mnt/fast/stacks` is at or ahead of the commit Task 1 pushed. If origin is behind the workstation,
      push first — 04-13 deviation 1 recorded a host pull that would otherwise have run against a checkout
      missing the very artefacts being reasoned from. Expect exit 0 with the census counters at target and
      `FAILURES total: 0`.
    - `grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash` → 0, re-derived here as the
      static half of criterion 3, which has been verified throughout and is not re-planned.

    Then state, in the SUMMARY and in one short table, the phase's actual closing position: which of the
    five ROADMAP criteria hold, what criterion 3's window-2 verdict is, and — if it is not PASS — exactly
    what remains and what it would take, without proposing it as work for this plan.

    Record two carry-forwards explicitly so they are met as decisions rather than silences: `DEF-04-01` (the
    unqualified "beets has no `undo` command" claim in `CLAUDE.md:152` and `.planning/PROJECT.md:187`)
    remains **deferred to Phase 7** and was not touched by this plan; `beets.md`'s two open config gaps
    (`match.preferred.countries`, and `fromfilename`/`edit` for bucket C) remain **deferred to Phase 6**.
    Neither is a gap of this closure.

    Do not modify `.planning/STATE.md` or `.planning/ROADMAP.md`, and do not call any `gsd-sdk query state.*`
    or `roadmap.*` verb — the orchestrator owns both, and 04-12 and 04-13 each recorded holding that line.
  </action>
  <verify>
    <automated>set -o pipefail; bash scripts/quick-health-check.sh > /tmp/qhc-04-16.out 2>&1; QHC=$?; test "$(grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash)" = 0 && test "$QHC" = 0 && grep -q 'vendored files match (3)' /tmp/qhc-04-16.out && echo SUITE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `quick-health-check.sh` exit code recorded as 0, with the `✅ vendored files match (3)` line and the census counters quoted from the captured output, not inferred from the exit code.
    - `check-music-freeze.sh` on LXC 100 exit code recorded as 0 with `FAILURES total: 0`, run against a host checkout asserted to contain Task 1's commit.
    - `grep -cE '^[[:space:]]*beet ' stacks/selfhosted/arrs/sabnzbd/audio.bash` = 0, quoted.
    - The SUMMARY carries the five-criterion closing table with criterion 3's window-2 verdict word.
    - DEF-04-01 is named as still deferred to Phase 7, and the two `beets.md` config gaps as still deferred to Phase 6.
    - `.planning/STATE.md` and `.planning/ROADMAP.md` appear in no commit from this plan.
  </acceptance_criteria>
  <done>The standing guards are green against the landed text, and the phase's real closing position is written down rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence → public estate record | text copied from the evidence file into a tracked, public document |
| author → heading | a heading could assert a closure the measurement does not support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16-01 | Information disclosure | beets.md added text | mitigate | the job is referred to as "one music job" or by 12-character sha256 prefix; no `storage` path beyond the `/downloads/complete/nzb/music/` prefix; added lines screened for 40-character runs and for `user_token\|discogs\.env\|/secrets/\|token=\|apikey\|api_key` |
| T-04-16-02 | Repudiation | a closure heading over a non-PASS verdict | mitigate | the heading is selected by the single machine-readable `^Verdict (window 2):` line, the mapping is enforced by this plan's `<verify>` rather than trusted to the author, and the verdict is re-derived from the evidence file rather than carried from a SUMMARY |
| T-04-16-03 | Tampering | loss of the window-1 record | mitigate | the window-1 mechanism paragraph is retained in every branch; `git diff --numstat` is asserted to delete no dated historical section; § 6 of the evidence file is not touched at all |
| T-04-16-04 | Tampering | measurement run against a stale host checkout | mitigate | the host checkout is asserted to contain Task 1's commit before `check-music-freeze.sh` runs, pushing first if origin is behind (04-13 deviation 1) |
</threat_model>

<verification>
The heading matches the verdict by machine-checked mapping; the criterion-3 row quotes the window-2
verdict; the census block and every dated historical section survive byte-identical; both standing guards
exit 0 against the landed text.
</verification>

<success_criteria>
The estate's durable record beside the stack states what criterion 3 actually measured — closure only if
the measurement earned it.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-16-SUMMARY.md` when done.
</output>
