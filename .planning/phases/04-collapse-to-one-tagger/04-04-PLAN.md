---
phase: 04-collapse-to-one-tagger
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - .planning/research/STACK.md
autonomous: true
requirements: [TAGR-03, TAGR-05]
tags: [docs, amendment, sweep]

must_haves:
  truths:
    - "D-20: ROADMAP Phase 4 criterion 4 carries a dated in-band amendment that quotes the original `--pretend` wording, cites beets/importer/session.py v2.13.1 (pretend replaces the pipeline with log_files, lookup_candidates never called), and names the substitute — the MB-only tag_album() probe plus a hand-read `beet import -t`, with the D-17 negative control, on the D-29 album — stating the substance is kept, not reduced"
    - "F10: ROADMAP criterion 5 carries a dated in-band note that the rw counter is stated as 0 excluding the D-21 consumer exception (Jellyfin), printed separately"
    - "D-20 / D-27: REQUIREMENTS TAGR-05 traceability row carries an ADDENDUM in the 02.1-11 shape: the survivor's newly vendored config is in scope and the proving instrument is the probe, not --pretend"
    - "D-24: PROJECT.md records the Discogs token rotation as a reasoned dismissal — a choice, not an oversight or a silence — with the operator's words verbatim and pointers to 03-DECISION.md § 9 and DEF-03-21; no step rotates or touches the token"
    - "D-07 / D-14 / F15: CLAUDE.md and its generator source .planning/research/STACK.md no longer tell a reader to keep or fix the wrtag pin, no longer carry the retired ~1,000-folder figure, and both carry the Phase 3 finding verbatim"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "criterion 4 and 5 amendments"
      contains: "session.py"
    - path: "CLAUDE.md"
      provides: "corrected wrtag finding"
      contains: "works on none of v0.20.0, v0.33.0 or v0.34.0"
    - path: ".planning/research/STACK.md"
      provides: "dated in-band correction so CLAUDE.md regeneration keeps it"
      contains: "works on none of v0.20.0, v0.33.0 or v0.34.0"
  key_links:
    - from: "CLAUDE.md lines 159-343 (GSD:stack region)"
      to: ".planning/research/STACK.md"
      via: "gsd-tools generate-claude-md"
      pattern: "GSD:stack-start source:research/STACK.md"
---

<objective>
Land the phase's documentary corrections that are TRUE AT COMMIT TIME (after 04-03's repo deletions):
the D-20 amendments that make criterion 4 satisfiable without reducing it, the F10 honesty note on
criterion 5, the D-24 reasoned dismissal, and the D-07/D-14 corrections to CLAUDE.md and its generator
source.

Purpose: a success criterion nobody can satisfy as written produces false `gaps_found`; a CLAUDE.md that
tells the next agent to keep the wrtag pin re-creates the defect.
Output: five edited documents. Nothing about host runtime state is claimed as done here — host teardown
is plans 04-07/04-11 and is recorded in `beets.md` by 04-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/03-tagger-spike/03-DECISION.md
@.planning/phases/04-collapse-to-one-tagger/04-03-SUMMARY.md
</context>

<conventions>
- In-band, dated, original wording kept visible (S8). A WITHDRAWN claim is paraphrased, never quoted,
  so a grep for it keeps returning zero (quick-health-check.sh:238-241 convention).
- The D-14 sentence is copied verbatim from 04-CONTEXT.md D-14 / 03-DECISION.md § Handoff to Phase 4.
- State only what is true at commit time. Do not describe host teardown, the fresh survivor DB or the
  sabnzbd strip as done.
- Stage by explicit path only.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: D-20 amendments — ROADMAP criteria 4 and 5, REQUIREMENTS TAGR-05 addendum</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 258-273 (the 02.1-11 amendment — the exact shape to copy) and lines 501-535 (Phase 4)
    - .planning/REQUIREMENTS.md lines 120-131 and 250-270 (line 256 TRAN-05 ADDENDUM shape; TAGR-05 row ~267)
    - .planning/phases/03-tagger-spike/03-DECISION.md lines 1307-1328 (session.py excerpt)
  </read_first>
  <action>
    ROADMAP: under criterion 4, add an indented blockquote `**Amended <today> by plan 04-04 (D-20).**` that: quotes the original clause ("…a `--pretend` run on one known-good album returns a MusicBrainz candidate instead of skipping"); explains that in beets v2.13.1 `ImportSession.run()` appends only `stagefuncs.log_files` when `pretend` is set, so `lookup_candidates` is never called and zero candidates are returned by construction; names the substitute instrument — the `tag_album()` probe in `scripts/spike03-discogs-probe.py --mb-only` run inside the survivor container on `Garth Brooks-Scarecrow-CD-FLAC-2001-FLACME-xpost` (D-29, replacing D-18's album, which is gone), first against the live `plugins: embedart` config (must return zero MusicBrainz candidates, D-17) then against the fixed config (must return ≥1), paired with one agent-driven `beet import -t -W -C` aborted at the prompt, both on a throwaway `-l`; and states "The substance is kept, not reduced". Under criterion 5 add a second dated blockquote: the rw counter is reported as "0 excluding the documented D-21 consumer exception", with Jellyfin (the only rw holder over `/mnt/tank/media` across every container state, measured 2026-09-11) printed on its own line — the WRIT-01 shape, not a gap. Replace the line `**Research**: not needed — retirement is deletion plus documented config changes.` with `**Research**: done 2026-09-11 — 04-RESEARCH.md (live-estate measurements F1–F15)`; do not touch the `**Plans**:` list (the planner maintains it).
    REQUIREMENTS: leave TAGR-05's requirement text unchanged. In its traceability row append `**ADDENDUM <today> (04-04, D-20/D-27)** —` stating: "every remaining beets config" includes the survivor's newly vendored `stacks/selfhosted/arrs/beets/config.yaml` (D-27) alongside `stacks/selfhosted/arrs/sabnzbd/beets-config.yaml`; the proving instrument is the probe pair named in ROADMAP criterion 4's amendment, not `--pretend`; "The text above is deliberately not rewritten."
    Commit (`docs(04-04): amend criterion 4/5 and TAGR-05 in-band (D-20)`).
  </action>
  <verify>
    <automated>grep -c 'Amended .* by plan 04-04' .planning/ROADMAP.md && grep -c 'lookup_candidates' .planning/ROADMAP.md && grep -c 'ADDENDUM .*04-04' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP: `grep -c 'Amended .* by plan 04-04'` = 2 (criteria 4 and 5); the Phase 4 section contains `session.py`, `lookup_candidates`, `Scarecrow`, `substance is kept, not reduced`, `excluding the documented D-21 consumer exception`.
    - The original criterion 4 line containing `a \`--pretend\` run on one known-good album` is still present (not rewritten).
    - `grep -c 'Research\*\*: not needed' .planning/ROADMAP.md` for the Phase 4 section = 0.
    - REQUIREMENTS: TAGR-05 requirement line text unchanged (`git diff` shows no change on lines 129-130); the TAGR-05 traceability row contains `ADDENDUM` and `beets/config.yaml`.
  </acceptance_criteria>
  <done>Criterion 4 is satisfiable by a named instrument at undiminished strength; criterion 5 states its exception honestly.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md — entry-points table and the D-24 reasoned dismissal</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 56-80 (entry-points table, rows ~63 soulbeet and ~64 wrtag; file count at ~72), 145-196 (Constraints → Reversibility), 197-220 (Key Decisions; the `sec=sys` reasoned-dismissal row ~214 and ~215)
    - .planning/phases/03-tagger-spike/03-DECISION.md lines 1708-1746 (§ 9 standing action) and deferred-items.md DEF-03-21
    - 04-CONTEXT.md D-24 (operator's words)
  </read_first>
  <action>
    Entry-points table: amend the soulbeet and wrtag rows in-band and dated — soulbeet: definition deleted from the repo by plan 04-03 (issue #306 closure pending plan 04-07); wrtag: definition and its Renovate pin deleted by plan 04-03, and a pointer to the D-14 corrected finding (the pin's stated cause was reversed — paraphrase, do not quote the rule's text). Keep the original cells readable (strike-through or "was:" prefix per the file's existing style). Keep the 7,451 file count at ~72 unchanged. In Constraints → Reversibility, add a dated note that the "four beets databases" statement is being reduced to one by Phase 4 and point at `stacks/selfhosted/arrs/beets.md` § Phase 4 for the measured outcome (added by plan 04-13) — do not state the outcome here. In Key Decisions add a row in exactly the `sec=sys` form: bold title "Discogs token rotation: dismissed by the operator (Phase 4 D-24)"; "Recorded as a **choice, not an oversight or a silence**, so a future security review meets a reasoned answer"; the operator's words verbatim: "Discogs token rotation is not important at all, there is nothing there that has any value, i am not concerned"; pointers to `03-DECISION.md` § 9 and `deferred-items.md` DEF-03-21 where the four causes remain on record; a "Revisit if" cell (e.g. the Discogs account gains a paid subscription, stored payment details or a write-capable use). No token value, no token prefix, no path to a token file.
    Commit (`docs(04-04): PROJECT.md entry points and D-24 dismissal`).
  </action>
  <verify>
    <automated>grep -c 'not an oversight or a silence' .planning/PROJECT.md && grep -c 'there is nothing there that has any value' .planning/PROJECT.md && grep -c '7,451' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'not an oversight or a silence' .planning/PROJECT.md` ≥ 2 (the existing sec=sys row plus the new one).
    - `grep -c 'there is nothing there that has any value'` = 1; `grep -c 'DEF-03-21'` ≥ 1.
    - Soulbeet and wrtag rows each contain `04-03`.
    - No 40-character alphanumeric run (token-shaped string) was added: `git diff .planning/PROJECT.md | grep -E '^\+.*[A-Za-z0-9]{40}'` returns nothing.
  </acceptance_criteria>
  <done>The dismissal is on the record in the project's decision table, and the entry-points table matches the repo.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md and .planning/research/STACK.md — D-07/D-14 corrections in both (F15)</name>
  <files>CLAUDE.md, .planning/research/STACK.md</files>
  <read_first>
    - CLAUDE.md (whole file; GSD markers at ~91, ~157, ~159, ~343; hits at 102, 163-168, 198, 206, 212-236, 234, 276-279, 283, 293-294, 309, 315-316, 325-327, 332, 338)
    - .planning/research/STACK.md (whole file; ~195, ~412-415, ~471)
    - stacks/selfhosted/arrs/beets.md lines 981-990 (in-band correction shape)
    - 04-RESEARCH.md § D-07 sweep table (CLAUDE.md row)
  </read_first>
  <action>
    In BOTH files, at each location carrying the claim (use `grep -n 'wrtag\|0.30.0\|1,000 folders\|soulbeet'` to enumerate; the table in 04-PATTERNS.md § CLAUDE.md maps CLAUDE.md 234/276-279/294 to STACK.md 195/412-415/471):
    (a) headline finding 1 and the "What NOT to Use" wrtag rows: replace the instruction to hold or fix the `<0.30.0` pin with the fact that the pin and the wrtag definition were deleted in Phase 4 (plan 04-03), followed by the D-14 sentence verbatim: "this repository's `WRTAG_PATH_FORMAT` works on none of v0.20.0, v0.33.0 or v0.34.0 — it renders `-1 - ` on every single-disc track and hard-errors on multi-disc at v0.20.0, and is refused at startup by both current tags — and the sole cause of the startup refusal is the `Disc N/` **subdirectory**, proven by an ablation that changes nothing else and validates at both current tags.";
    (b) the installation block's "wrtag: EITHER fix the pin, OR remove the stack entirely" lines: replace with one line stating wrtag was removed (Phase 4) — do not keep an instruction to unpin;
    (c) the "7,451 files across ~1,000 folders" figure: keep the 7,451 file count, replace the folder figure with 144 (the measured denominator, 03-DECISION § AMENDMENT 03-07 §1), paraphrasing the withdrawn figure rather than quoting it;
    (d) the "beets vs wrtag" head-to-head and recommendation: add a one-line dated note that the decision was made (Phase 3) and executed (Phase 4) — the comparison table itself stays as a dated research record;
    (e) CLAUDE.md line ~102 (inside the PROJECT.md-generated region, #306): make it match PROJECT.md after Task 2.
    KEEP line ~309's `1-115` split instruction unchanged. In STACK.md additionally add a short dated section "Corrections after Phase 4 (<today>)" in the beets.md:981-990 shape stating what changed and why, with the original research wording left standing where it was correct against what it measured. Do not edit the GSD marker comments themselves.
    Commit (`docs(04-04): correct wrtag pin guidance in CLAUDE.md and STACK.md (D-07, D-14)`).
  </action>
  <verify>
    <automated>for f in CLAUDE.md .planning/research/STACK.md; do test "$(grep -c 'works on none of v0.20.0, v0.33.0 or v0.34.0' $f)" -ge 1 && test "$(grep -c 'EITHER fix the pin' $f)" = 0 && test "$(grep -c '1,000 folders' $f)" = 0 || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - For each of CLAUDE.md and STACK.md: `works on none of v0.20.0, v0.33.0 or v0.34.0` ≥1; `EITHER fix the pin` = 0; `1,000 folders` = 0; `drop the Renovate rule pinning` = 0.
    - `grep -c '1-115' CLAUDE.md` unchanged from before the edit (record both counts).
    - `grep -c 'GSD:stack-start source:research/STACK.md' CLAUDE.md` = 1 and `grep -c 'GSD:project-start source:PROJECT.md' CLAUDE.md` = 1.
    - STACK.md contains a heading matching `Corrections after Phase 4`.
  </acceptance_criteria>
  <done>No document in the repo instructs a reader to keep, fix or unpin wrtag, and the correction survives CLAUDE.md regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → public repo | text is published; it steers future agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information disclosure | PROJECT.md D-24 row | mitigate | no token value, prefix or file path; diff screened for 40-char runs |
| T-04-04-02 | Repudiation | criterion amendments | mitigate | original wording kept and quoted; amendment dated and attributed; substance-kept statement |
| T-04-04-03 | Tampering | CLAUDE.md regeneration | mitigate | correction also lands in STACK.md, the generator source (F15) |
</threat_model>

<verification>
The three verify commands pass; `git diff --stat` touches only the five files.
</verification>

<success_criteria>
Criterion 4 is satisfiable at full strength; no repo document preserves the wrtag misdiagnosis; D-24 is a recorded answer.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-04-SUMMARY.md` when done.
</output>
