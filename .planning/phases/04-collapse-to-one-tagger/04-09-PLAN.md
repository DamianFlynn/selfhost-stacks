---
phase: 04-collapse-to-one-tagger
plan: 09
type: execute
wave: 4
depends_on: [04-07, 04-08]
files_modified:
  - stacks/selfhosted/arrs/beets/beets.yaml
autonomous: true
requirements: [TAGR-05]
tags: [beets, probe, musicbrainz, survivor, criterion-4, lifecycle]

must_haves:
  truths:
    - "D-27: the vendored survivor config is installed at /mnt/fast/appdata/arrs/beets/config/config.yaml, byte-identical to the repo, and `beet version` inside the survivor (no -c, throwaway -l) reports `plugins: musicbrainz`"
    - "D-28: the survivor creates ONE fresh library.db at /config/library.db — by svc-beets on start or by `beet -l /config/library.db stats` (explicit path, never a bare `beet`) — integrity ok, 0 items, no migration .bak, untouched by every probe run"
    - "D-30 / REVIEWS row 15: the survivor runs at the tag beets.yaml ships; if Renovate has moved the repo past 2.13.1-ls349, this plan is D-30's deliberate redeploy — the new tag's digest and image ID are recorded and `beet version` = 2.13.1 is asserted in a --rm container BEFORE `up`; any other upstream version halts"
    - "D-19 / D-02 / REVIEWS row 3: both HALT preconditions (D-29 album present; live broken sabnzbd config sha = 04-01) are asserted BEFORE `up`, and start → fresh DB → probes → down run in ONE host process whose EXIT/INT/TERM/HUP trap always runs `compose down`; a driven TERM-after-up control proves the failure path; an independent producer-status-first check then shows no `beets` container in any state"
    - "D-17: against the LIVE broken sabnzbd config (`plugins: embedart`, sha unchanged since 04-01) the probe records ZERO MusicBrainz candidates; against a copy differing ONLY in `plugins: embedart musicbrainz` it records ≥1 — same album, same throwaway -l; `beet version` plugin lines differ accordingly"
    - "D-16: a hand-read agent-driven `beet import -t -W -C` on the fixed overlay shows a MusicBrainz candidate and is aborted at the prompt; every `beet` invocation except the explicit D-28 create uses a throwaway `-l` under /config/probe-04"
    - "REVIEWS row 16: the `state.pickle` baseline is taken AFTER `up` and the fresh DB, and is unchanged after every probe run"
  artifacts: []
  key_links:
    - from: "probe-04/broken.yaml"
      to: "/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml (live, still broken)"
      via: "verbatim copy + probe-only import/statefile keys, sha asserted before up"
      pattern: "plugins: embedart"
    - from: "lifecycle.sh trap"
      to: "docker compose … --profile manual down"
      via: "trap cleanup EXIT + INT/TERM/HUP → exit"
      pattern: "trap"
---

<objective>
Stand up the survivor exactly as this phase ships it, let it create the one fresh library.db (D-28), and run
the amended criterion-4 instrument: the D-17 negative control against the live broken config, then the
fixed config, then the hand-read cross-check. The survivor is returned to dormancy (D-02) by a trap
that fires on success, failure, interruption and timeout alike (REVIEWS row 3).

Purpose: turns "the missing musicbrainz plugin caused the skips" from inference into a measurement, and
proves the container this phase ships. MUST run before plan 04-11 installs the fixed sabnzbd config.
Output: host evidence in the SUMMARY (transcripts); possibly a measured `:ro`→`:rw` flip in beets.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-07-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-08-SUMMARY.md
@scripts/spike03-discogs-probe.py
@stacks/selfhosted/arrs/beets/beets.yaml
</context>

<conventions>
- Survivor lifecycle from `/mnt/fast/stacks` (RESEARCH Pattern 2): `F=stacks/selfhosted/arrs/beets/beets.yaml`,
  `E=stacks/selfhosted/arrs/.env`, `docker compose -f "$F" --env-file "$E" --profile manual …`. Never
  `config` without `--quiet`. `up` ALWAYS with `--pull never`.
- `beet` is NOT read-only. Every `beet` call carries an explicit `-l`: the throwaway
  `/config/probe-04/probe.blb` for probe/import runs, `/config/probe-04/version.blb` for `version`, and
  `-l /config/library.db` ONLY for the one deliberate D-28 create. Never a bare `beet`. Run as
  `docker exec -u abc`.
- The survivor is started ONLY inside `/mnt/fast/scratch-04/09/lifecycle.sh`, run as one host process
  under a Linux-side `timeout -k 120 2700`. Its `cleanup()` runs `compose down` from an EXIT trap, with
  INT/TERM/HUP traps that `exit` so the EXIT trap fires. Every step that can take more than a few seconds
  runs as `timeout <N> <cmd> & wait $!`, because bash defers traps while a foreground child runs but
  `wait` is interrupted at once. The lifecycle script lives in the workstation scratchpad and on the host
  scratch path only — never in the repo, never under appdata.
- MusicBrainz throttles with HTTP 503 (DEF-03-07): a positive run with zero candidates AND a 503 in its log
  is re-run after 60 s (max 3), never read as zero.
- Evidence published to the repo quotes only the D-29 album name; no other release names.
- D-17 ordering: the sabnzbd appdata `beets-config.yaml` must still be the broken one (sha = 04-01's) when
  broken.yaml is copied AND again immediately before `up` — if it differs, HALT before starting anything.
- Absence proofs (REVIEWS row 2): producer status captured first, positive control in the same
  enumeration, RC 124 and other failures are UNKNOWN.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Everything before `up` — tag/redeploy decision, HALT preconditions, config install, overlays, lifecycle script (D-27, D-29, D-30, REVIEWS rows 3 and 15)</name>
  <files>stacks/selfhosted/arrs/beets/beets.yaml (read only in this task)</files>
  <read_first>
    - stacks/selfhosted/arrs/beets/beets.yaml and stacks/selfhosted/arrs/beets/config.yaml at host HEAD
    - 04-RESEARCH.md § Pattern 2 (known side effects of `up`), Assumptions A1/A3/A4, Pitfall 10, Code Example 4
    - 04-01-SUMMARY.md (D-29 container path and the beets.yaml line it was derived from; broken-config sha; junk config.yaml sha; ROUTINE BASELINE)
    - 04-07-SUMMARY.md (survivor DBs deleted; config.yaml untouched)
    - /mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml on LXC 100 (its existing `import:` mapping)
  </read_first>
  <action>
    Nothing in this task starts a container on the survivor's definition.
    (1) Host `git pull --ff-only`; record HEAD. Image tag (D-30, REVIEWS row 15): read the image tag in beets.yaml at HEAD. If `2.13.1-ls349` → confirm resident and proceed. If Renovate has automerged a newer `2.13.1-lsNNN` (the expected D-30 PR) → this plan IS D-30's deliberate redeploy, and the SUMMARY says so in those words: confirm `df -h /` shows ≥ 5 GiB free, `docker pull` that exact tag, record its RepoDigest and image ID; leave `2.13.1-ls349` resident (04-02/04-13 self-tests use it). If the upstream version is anything other than 2.13.1 → HALT (a minor/major merged despite the manual-review rule is a D-30 failure). Either way, assert the version before `up`: `docker run --rm --pull never --network none --entrypoint beet <tag> -l /tmp/version-04.blb version` (explicit throwaway `-l` inside a `--rm` container, nothing on the host) must print `beets version 2.13.1`.
    (2) HALT preconditions, before anything starts (REVIEWS row 3): `/config/library.db`, `musiclibrary.blb`, `config.yaml.old` absent on the host (04-07); current host `config.yaml` sha256 = the junk sha from 04-01 (fenced); D-29 — the album directory at the container path 04-01 recorded (and its host path) exists with exactly 12 `*.flac` and no audio in subdirectories — if not, HALT (no substitute without an operator ruling); the live sabnzbd `beets-config.yaml` sha256 = 04-01's — if not, HALT (the D-17 negative control can no longer run against the live config).
    (3) Overlays: create `/mnt/fast/appdata/arrs/beets/config/probe-04/` (owner matching `/config`). Copy the live sabnzbd config to `probe-04/broken.yaml`; in the copy only, set probe-only keys: `statefile: /config/probe-04/state.pickle` (top-level) and, inside the EXISTING `import:` mapping (do not add a second `import:` key), `write: no`, `copy: no`, `move: no`, `log: /config/probe-04/import.log`; also point any top-level `library:`/`directory:`/`log:` path that falls outside `/config/probe-04` into it. Create `fixed.yaml` from broken.yaml changing ONLY `plugins: embedart` → `plugins: embedart musicbrainz`; `diff broken.yaml fixed.yaml` must show exactly one changed line. Copy `/mnt/fast/stacks/scripts/spike03-discogs-probe.py` into probe-04/.
    (4) Install D-27: `cp /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml /mnt/fast/appdata/arrs/beets/config/config.yaml` (cp onto the existing file — same inode; never `mv`); sha256 must equal the repo file. Record a PRE-UP listing of `/mnt/fast/appdata/arrs/beets/config` (maxdepth 1: name, size, mtime), `state.pickle` sha256 if present, and `du -s Music`.
    (5) Write `lifecycle-04-09.sh` in the workstation scratchpad, `bash -n` it, `scp` it to `/mnt/fast/scratch-04/09/lifecycle.sh` (create `/mnt/fast/scratch-04/09/out/`). Its shape: `set -euo pipefail`; constants F, E, CFG=/mnt/fast/appdata/arrs/beets/config, P=/config/probe-04, ALBUM (container path), HOSTALBUM, EXPECT_BROKEN_SHA, EXPECT_CFG_SHA, OUT=/mnt/fast/scratch-04/09/out; `cleanup()` runs `timeout 120 docker compose -f "$F" --env-file "$E" --profile manual down`, then enumerates `docker ps -a --format '{{.Names}}'` with its RC captured and prints `CLEANUP: dormant` only when rc = 0, `sabnzbd` is listed (positive control) and no line is exactly `beets` — otherwise `CLEANUP: NOT DORMANT` or `CLEANUP: UNKNOWN rc=<n>`; `trap cleanup EXIT`, `trap 'exit 130' INT`, `trap 'exit 143' TERM`, `trap 'exit 129' HUP`; distinct exit codes per HALT (10 album, 11 broken-config sha moved, 12 installed config ≠ repo, 20 not running / EROFS on the config mount, 21 fresh-DB assertion, 30 overlay plugin lines not as expected, 31 gate control did not refuse, 32 negative ≠ 0, 33 positive < 1 after the 503 rule, 34 hand-read showed no candidate, 35 survivor DB / probe.blb / state.pickle assertion); an optional `LIFECYCLE_FORCE=term-after-up` that, once the container is running, starts `sleep 60 & wait $!` and sends itself `kill -TERM $$` from a background `( sleep 3; kill -TERM $$ ) &` (the driven failure-path control). Its sequence is Task 2's (a)–(h), in order, with every transcript written under OUT.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; C=/mnt/fast/appdata/arrs/beets/config; cmp -s \$C/config.yaml /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml || { echo 'config not installed'; exit 1; }; cd \$C/probe-04 && test \$(diff broken.yaml fixed.yaml | grep -c '^[<>]') = 2 && grep -q 'plugins: embedart musicbrainz' fixed.yaml && bash -n /mnt/fast/scratch-04/09/lifecycle.sh && grep -q 'trap cleanup EXIT' /mnt/fast/scratch-04/09/lifecycle.sh && echo PRE-UP-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Tag decision recorded: `2.13.1-ls349`, or `D-30 deliberate redeploy` with the new tag, RepoDigest and image ID; `--rm` container `beet … -l /tmp/version-04.blb version` printed `beets version 2.13.1` before any `up`.
    - HALT preconditions recorded as passed BEFORE any container start: album 12 FLAC single-disc at the 04-01 path; live sabnzbd config sha = 04-01; old survivor DBs absent.
    - `diff broken.yaml fixed.yaml` shows exactly 2 `<`/`>` lines; appdata config.yaml byte-identical to the repo (`cmp` 0); PRE-UP listing recorded.
    - lifecycle.sh passes `bash -n`, contains `trap cleanup EXIT` and the INT/TERM/HUP traps, and is not under the repo or appdata.
  </acceptance_criteria>
  <done>Every condition that could force a HALT has been checked while nothing is running, and the one process that will start the survivor is written with its cleanup guaranteed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the trap-guarded lifecycle — survivor up, fresh DB, the criterion-4 probe pair and hand-read, down; then drive the TERM-after-up control (D-16, D-17, D-19, D-28, REVIEWS rows 3 and 16)</name>
  <files>stacks/selfhosted/arrs/beets/beets.yaml (only if the measured :ro flip below is required)</files>
  <read_first>
    - scripts/spike03-discogs-probe.py (argparse ~820-866: positional FOLDER list, required --ledger, --require-plugin, --mb-only; record fields ~690-750 incl. "source")
    - 04-RESEARCH.md § Code Example 4, Pitfall 10, Assumption A3
    - /mnt/fast/scratch-04/09/lifecycle.sh (as written in Task 1)
  </read_first>
  <action>
    Run `timeout -k 120 2700 bash /mnt/fast/scratch-04/09/lifecycle.sh` over `ssh -n` (the bound executes Linux-side; the workstation has no `timeout`). The script performs, in order:
    (a) re-assert the two HALT preconditions and the installed config (exit 10/11/12 before `up` — cheap, and the downloads tree is live);
    (b) `config --quiet` → 0; `up -d --pull never beets`; wait up to 120 s for `running`; save `docker logs beets` from start: any `lsiown`/`chmod`/`Read-only file system` lines verbatim, and whether `svc-beets` (`beet web`) is restarting (A3 — expected: `web` is not a loaded plugin). If the container does not reach `running` because of the `:ro` config mount, exit 20 with the EROFS lines;
    (c) fresh DB (D-28): if `/config/library.db` does not exist, `docker exec -u abc beets beet -l /config/library.db stats` (explicit path — never a bare `beet`). Assert: file exists; `sqlite3 "file:$CFG/library.db?mode=ro" 'PRAGMA integrity_check;'` = ok; `SELECT count(*) FROM items` = 0; no `library.db*` sibling other than the DB itself (no migration .bak); record size, mtime, sha256 and the creator (svc-beets if it existed at the first check — compare mtime with the container's StartedAt — else the explicit create);
    (d) POST-UP BASELINE (REVIEWS row 16): the maxdepth-1 listing of `$CFG` and the sha256 of `$CFG/state.pickle` (or `absent`), recorded now, after `up` and the fresh DB; record every difference from Task 1's PRE-UP listing as an LSIO/s6/svc-beets side effect;
    (e) `docker exec -u abc beets beet -l /config/probe-04/version.blb version` (no -c) → must list `musicbrainz` in its plugins line (C4-a live, survivor); `docker inspect beets` → `/media` RW=false and `/config/config.yaml` RW per the declared mode;
    (f) the probe arms, each as `timeout <N> <cmd> & wait $!` with `-u abc`, `-l /config/probe-04/probe.blb` / `SPIKE_DB=/config/probe-04/probe.blb`, full transcripts to OUT: (1) `beet -c /config/probe-04/broken.yaml -l … version` → plugins line lists embedart only, and `beet -c /config/probe-04/fixed.yaml -l … version` → embedart and musicbrainz; if either differs, exit 30 — do NOT drop `--require-plugin` or edit the live sabnzbd file to make it pass; (2) gate control: probe `--mb-only --require-plugin musicbrainz` with `SPIKE_CONFIG=/config/probe-04/broken.yaml` → REFUSES naming musicbrainz as not loaded (else exit 31); (3) negative (D-17): probe `--mb-only --require-plugin embedart --ledger /config/probe-04/neg` with broken.yaml on the album → completes; count ledger records with `"source": "MusicBrainz"` via `python3 -c` json parsing on the host → must be 0, and the run shows no MusicBrainz HTTP request (else exit 32); (4) positive: probe `--mb-only --require-plugin musicbrainz --ledger /config/probe-04/pos` with fixed.yaml on the same album → MusicBrainz record count ≥ 1 (503 rule applies; else exit 33); record the top candidate's album, track count and distance/score; (5) hand-read: `printf 'b\n' | docker exec -i -u abc beets beet -c /config/probe-04/fixed.yaml -l /config/probe-04/probe.blb import -t -W -C "<album container path>"` → transcript shows a MusicBrainz candidate (match header with similarity/source) and the abort; nothing imported (else exit 34);
    (g) assertions after the arms (else exit 35): `SELECT count(*) FROM items` on probe.blb = 0; survivor `/config/library.db` sha256 unchanged since (c); `state.pickle` sha256 equal to the POST-UP baseline from (d);
    (h) normal exit 0 → the EXIT trap runs `down` and prints `CLEANUP: dormant`.
    Record every step's exit code and UTC time, and the script's own exit code and last `CLEANUP:` line. If it exits 20: flip ONLY the `/config/config.yaml` mount to `:rw` in beets.yaml with a dated NOTE line quoting the measured EROFS failure, commit/push/pull, and re-run the lifecycle once; otherwise keep `:ro` and record any EROFS lines as tolerated (the D-09 rule). Any other non-zero exit: record it, let Task 3's dormancy check run, and stop — do not re-run with weakened assertions.
    Then the driven failure-path control (REVIEWS row 3): record library.db's sha256, run `LIFECYCLE_FORCE=term-after-up timeout -k 120 600 bash /mnt/fast/scratch-04/09/lifecycle.sh` → it must exit 143 and print `CLEANUP: dormant`; then re-assert library.db integrity ok and 0 items (record whether its sha moved while svc-beets had it open).
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; D=/mnt/fast/appdata/arrs/beets/config; test \"\$(timeout 20 sqlite3 \"file:\$D/library.db?mode=ro\" 'PRAGMA integrity_check;')\" = ok && test \"\$(timeout 20 sqlite3 \"file:\$D/library.db?mode=ro\" 'SELECT count(*) FROM items;')\" = 0 && grep -q 'CLEANUP: dormant' /mnt/fast/scratch-04/09/out/lifecycle.log && grep -q 'CLEANUP: dormant' /mnt/fast/scratch-04/09/out/force-term.log && echo LIFECYCLE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Lifecycle exit 0 with `CLEANUP: dormant`; survivor reached `running` with the recorded tag; `docker inspect` showed `/media` RW=false.
    - Fresh library.db: integrity ok, 0 items, no `.bak` sibling; creator recorded; created via `beet -l /config/library.db stats` if svc-beets had not already.
    - POST-UP baseline recorded (listing + state.pickle sha) and every PRE-UP → POST-UP difference listed.
    - `beet -l /config/probe-04/version.blb version` (no -c) output contains `musicbrainz`.
    - C4-b: negative MusicBrainz count = 0 and positive ≥ 1, same album, same `-l`, configs differing by one line; `beet version` overlay transcripts differ exactly in the musicbrainz plugin; the gate control refused; hand-read transcript contains a candidate line and the abort; probe.blb items = 0.
    - Survivor `/config/library.db` sha256 unchanged across the probe arms; `state.pickle` sha equal to the POST-UP baseline.
    - Driven control: `LIFECYCLE_FORCE=term-after-up` run exited 143 with `CLEANUP: dormant`.
  </acceptance_criteria>
  <done>Criterion 4 (as amended) is measured on the container this phase ships, and the survivor's return to dormancy has been seen to happen on both the success and the signal path.</done>
</task>

<task type="auto">
  <name>Task 3: Independent dormancy proof and cleanup; candidate census and routine check (D-02, Pitfall 10, REVIEWS rows 1, 2, 9, 16)</name>
  <files>(host only)</files>
  <read_first>
    - Task 1 PRE-UP listing and Task 2 POST-UP baseline (this plan's working notes)
    - 04-01-SUMMARY.md § ROUTINE BASELINE
  </read_first>
  <action>
    Run this regardless of how Task 2 ended (it is the belt to the trap's braces). Idempotent `docker compose -f "$F" --env-file "$E" --profile manual down`; then the dormancy proof exactly as the verify below does (producer status first, `sabnzbd` positive control, no `beets` in any state), and `docker network ls` (RC captured) shows no `beets_default`. Drive its error branch once: the same remote check with `DOCKER_HOST=unix:///nonexistent-04.sock` exported at the front → it MUST print `UNKNOWN` and exit 3, never `DORMANT`; record both transcripts.
    Remove `/mnt/fast/appdata/arrs/beets/config/probe-04` and `/mnt/fast/scratch-04/09` with the S5 guard (realpath equals that exact path; no `..`); `test ! -e` each (with `$CFG/config.yaml` present as the positive control). Compare the survivor tree listing (maxdepth 1) with the POST-UP baseline from Task 2 (d): the only allowed difference is `probe-04` gone; `state.pickle` sha256 must equal the POST-UP value (REVIEWS row 16 — a leak from a probe run shows here, s6/svc-beets noise does not, because the baseline was taken after `up`). `du -s Music` equal to Task 1's.
    CANDIDATE census (REVIEWS rows 1 and 9): `CENSUS_CANDIDATE=1 bash scripts/check-music-freeze.sh` with RC captured → exactly 1, with this complete expected-red set: `retired path PRESENT` for exactly the three sabnzbd paths (`scripts/library.blb`, `scripts/beets.log`, `.config/beets`); `beets databases:` ≠ 1, listing the survivor's fresh `/mnt/fast/appdata/arrs/beets/config/library.db` PLUS every sabnzbd `scripts/library.blb*` and `.config/beets/library.db*` file (primaries and `.bak`) — expected until plan 04-11; `tagger databases: 0`; `tagger definitions: 1`; all three `rw on Music` counters at target. Anything outside this list: STOP and investigate; never narrow the classifier to match. ROUTINE run (no env) on the host and `bash scripts/quick-health-check.sh` on the workstation → within the 04-01 ROUTINE BASELINE.
    Write the evidence summary (tag used and whether this was D-30's deliberate redeploy, EROFS outcome, fresh-DB facts, the five probe transcripts' key lines, the trap control) into 04-09-SUMMARY.md.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "names=\$(timeout 30 docker ps -a --format '{{.Names}}'); rc=\$?; [ \$rc -eq 0 ] || { echo \"UNKNOWN docker ps rc=\$rc\"; exit 3; }; printf '%s\n' \"\$names\" | grep -qx sabnzbd || { echo 'UNKNOWN positive control sabnzbd not listed'; exit 3; }; if printf '%s\n' \"\$names\" | grep -qx beets; then echo 'SURVIVOR NOT DORMANT'; exit 1; fi; test -f /mnt/fast/appdata/arrs/beets/config/config.yaml || { echo 'UNKNOWN positive control config.yaml not visible'; exit 3; }; test ! -e /mnt/fast/appdata/arrs/beets/config/probe-04 && test ! -e /mnt/fast/scratch-04/09 && echo DORMANT"</automated>
  </verify>
  <acceptance_criteria>
    - Dormancy proof prints `DORMANT` (rc 0 enumeration, `sabnzbd` listed, no `beets`); the `DOCKER_HOST=unix:///nonexistent-04.sock` run printed `UNKNOWN` and exited 3; no `beets_default` network.
    - probe-04 and the host scratch gone; `state.pickle` sha equal to the POST-UP baseline; listing differs from POST-UP only by `probe-04`; `Music/` size unchanged.
    - Candidate census: RC 1 with exactly the expected-red set (three sabnzbd retired paths; survivor fresh DB plus every sabnzbd beets DB listed); routine host and workstation runs within the 04-01 ROUTINE BASELINE.
  </acceptance_criteria>
  <done>The survivor is dormant again (D-02) by independent evidence, leaving exactly one new artefact: its fresh library.db.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| survivor container → library | reads /media (ro) and downloads (rw) |
| probe → musicbrainz.org | outbound API calls from the survivor |
| lifecycle process → Docker host | a script starts a container on the estate and must stop it on every exit path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | download tree via `import` | mitigate | `-W -C`, overlay `write/copy/move: no`, abort at prompt; probe.blb item count asserted 0 |
| T-04-09-02 | Tampering | survivor library.db | mitigate | every probe/version run uses a throwaway `-l`; the one create uses explicit `-l /config/library.db`; survivor DB sha asserted unchanged |
| T-04-09-03 | Elevation of privilege | library mount | mitigate | `/media` RW=false asserted via `docker inspect`; nothing flips it |
| T-04-09-04 | Tampering (supply chain) | image | mitigate | `up --pull never`; any Renovate ls-build is pulled only as D-30's named deliberate redeploy, digest recorded, `beet version` = 2.13.1 asserted before `up`; non-2.13.1 halts |
| T-04-09-05 | Information disclosure | Discogs token | mitigate | `--mb-only` only; token never enters the container |
| T-04-09-06 | Tampering | `rm -rf probe-04` / scratch | mitigate | S5 guard: exact realpath match, `..` refusal, positive control |
| T-04-09-07 | Denial of service / Elevation | survivor left running after a failure | mitigate | HALT preconditions before `up`; EXIT/INT/TERM/HUP trap runs `down`; `cmd & wait $!` so signals land; Linux-side `timeout -k`; driven TERM control; independent producer-status-first dormancy check |
</threat_model>

<verification>
Pre-up preconditions; fresh DB facts; five transcripts with counts 0 / ≥1; trap proven on success and TERM paths; survivor dormant by independent check; probe-04 removed; candidate census as expected; routine check within baseline.
</verification>

<success_criteria>
ROADMAP criterion 4 (amended) measured on the container this phase ships, criterion 5's "one library.db" has its one DB, and no exit path can leave the survivor running.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS rows 1, 2, 3, 9, 15 and 16, plus the accepted `beet -l` hardening; the full table is in 04-01-PLAN.md.
- **Row 3**: both HALT preconditions moved into Task 1, before `up`, and are re-asserted inside the lifecycle before `up`. Start → probe → down is one trap-guarded process with `& wait $!` and a Linux-side `timeout -k`. The TERM-after-up control is driven, and Task 3 is an independent dormancy check that runs however Task 2 ended.
- **Rows 1, 2, 9**: Task 3 uses a candidate census with the full expected-red set, and the routine run is compared against the baseline. The dormancy verify captures producer status first, uses a positive control, and drives its error branch.
- **Row 15**: this plan is named as D-30's deliberate redeploy when the repo tag moved; the digest and image ID are recorded and `beet version` = 2.13.1 is checked before `up`.
- **Row 16**: the `state.pickle` baseline is taken post-`up` in Task 2 (d) and compared in Tasks 2 (g) and 3.
- **Hardening**: `beet -l /config/library.db stats` for the create; `version` runs on a throwaway `-l`.
