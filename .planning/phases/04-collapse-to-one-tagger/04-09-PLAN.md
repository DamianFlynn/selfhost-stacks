---
phase: 04-collapse-to-one-tagger
plan: 09
type: execute
wave: 4
depends_on: [04-07, 04-08]
files_modified:
  - stacks/selfhosted/arrs/beets/beets.yaml
autonomous: true
requirements: [TAGR-05]
tags: [beets, probe, musicbrainz, survivor, criterion-4]

must_haves:
  truths:
    - "D-27: the vendored survivor config is installed at /mnt/fast/appdata/arrs/beets/config/config.yaml, byte-identical to the repo, and `beet version` inside the survivor with no -c reports `plugins: musicbrainz`"
    - "D-28: the survivor at 2.13.1-ls349 (or the Renovate ls-build D-30 expected) creates ONE fresh library.db at /config/library.db — integrity ok, 0 items, no migration .bak — and it is untouched by every probe run"
    - "D-19: the probe runs inside the survivor container started on its `manual` profile through the mounts beets.yaml declares, and the container is stopped and removed afterwards (unfiltered `docker ps -a` count 0)"
    - "D-17: against the LIVE broken sabnzbd config (`plugins: embedart`, sha unchanged since 04-01) the probe records ZERO MusicBrainz candidates; against a copy differing ONLY in `plugins: embedart musicbrainz` it records ≥1 — same album, same throwaway -l; `beet version` plugin lines differ accordingly"
    - "D-16: a hand-read agent-driven `beet import -t -W -C` on the fixed overlay shows a MusicBrainz candidate and is aborted at the prompt; both instruments use a throwaway `-l` under /config/probe-04, never the survivor's library"
    - "D-29 / D-18: the album is `Garth Brooks-Scarecrow-CD-FLAC-2001-FLACME-xpost`, re-asserted present (12 FLAC, single disc) immediately before the probe"
  artifacts: []
  key_links:
    - from: "probe-04/broken.yaml"
      to: "/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml (live, still broken)"
      via: "verbatim copy + probe-only import/statefile keys"
      pattern: "plugins: embedart"
---

<objective>
Stand up the survivor exactly as this phase ships it, let it create the one fresh library.db (D-28), and run
the amended criterion-4 instrument: the D-17 negative control against the live broken config, then the
fixed config, then the hand-read cross-check — then return the survivor to dormancy (D-02).

Purpose: turns "the missing musicbrainz plugin caused the skips" from inference into a measurement, and
proves the container this phase ships. MUST run before plan 04-11 installs the fixed sabnzbd config.
Output: host evidence in the SUMMARY (transcripts); possibly a measured `:ro`→`:rw` flip in beets.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-07-SUMMARY.md
@.planning/phases/04-collapse-to-one-tagger/04-08-SUMMARY.md
@scripts/spike03-discogs-probe.py
@stacks/selfhosted/arrs/beets/beets.yaml
</context>

<conventions>
- Survivor lifecycle from `/mnt/fast/stacks` (RESEARCH Pattern 2): `F=stacks/selfhosted/arrs/beets/beets.yaml`,
  `E=stacks/selfhosted/arrs/.env`, `docker compose -f "$F" --env-file "$E" --profile manual …`. Never
  `config` without `--quiet`. `up` ALWAYS with `--pull never`.
- `beet` is NOT read-only: every probe/import invocation carries `-l /config/probe-04/probe.blb`. Run as
  `docker exec -u abc`. The survivor's own `/config/library.db` is opened only deliberately (Task 1).
- MusicBrainz throttles with HTTP 503 (DEF-03-07): a positive run with zero candidates AND a 503 in its log
  is re-run after 60 s (max 3), never read as zero.
- Evidence published to the repo quotes only the D-29 album name; no other release names.
- D-17 ordering: the sabnzbd appdata `beets-config.yaml` must still be the broken one (sha = 04-01's) when
  broken.yaml is copied — if it already differs, HALT: the D-17 negative control can no longer be run
  against the live config.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Install the D-27 config, start the survivor, create the fresh library.db (D-19, D-27, D-28, D-30)</name>
  <files>stacks/selfhosted/arrs/beets/beets.yaml (only if the :ro flip below is required)</files>
  <read_first>
    - stacks/selfhosted/arrs/beets/beets.yaml and stacks/selfhosted/arrs/beets/config.yaml at host HEAD
    - 04-RESEARCH.md § Pattern 2 (known side effects of `up`), Assumptions A1/A3
    - 04-07-SUMMARY.md (survivor DBs deleted; config.yaml untouched)
  </read_first>
  <action>
    Host `git pull --ff-only`; record HEAD. Image tag race (D-30): read the image tag in beets.yaml at HEAD. If `2.13.1-ls349` → proceed. If Renovate has already automerged a newer `2.13.1-lsNNN` (the expected D-30 PR) → record it, confirm `df -h /` shows ≥ 5 GiB free, `docker pull` that exact tag, record its digest, and proceed (the probe then proves the version the repo ships; record the ls-build delta against D-03). If the upstream version is anything other than 2.13.1 → HALT and report (a minor/major merged despite the manual-review rule is a D-30 failure).
    Preconditions: `/config/library.db`, `musiclibrary.blb`, `config.yaml.old` absent (04-07); current host `config.yaml` sha256 = the junk sha from 04-01 (fenced). Install: `cp /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml /mnt/fast/appdata/arrs/beets/config/config.yaml` (cp onto the existing file — same inode; never `mv`); sha256 must equal the repo file. Record a pre-up listing of `/mnt/fast/appdata/arrs/beets/config` (maxdepth 1, name size mtime) and `du -s Music`.
    `config --quiet` → 0; `up -d --pull never beets`. Record `docker logs beets` from start: any `lsiown`/`chmod`/`Read-only file system` lines verbatim, and whether `svc-beets` (`beet web`) is restarting (A3 — expected: `web` is not a loaded plugin). If the container does not reach `running` because of the `:ro` config mount, flip that one mount to `:rw` in beets.yaml with a dated NOTE line stating the measured EROFS failure, commit/push/pull, and retry once; otherwise keep `:ro` and record the EROFS lines as tolerated (the D-09 rule).
    Fresh DB (D-28, deliberately and separately from the probe): if `/config/library.db` does not yet exist, `docker exec -u abc beets beet stats` (default config → `library: /config/library.db`). Assert: file exists; `sqlite3 "file:/mnt/fast/appdata/arrs/beets/config/library.db?mode=ro" 'PRAGMA integrity_check;'` = ok; `SELECT count(*) FROM items` = 0; no `library.db*` sibling other than the DB itself (no migration .bak); record size, mtime, sha256, and which process created it (svc-beets vs the explicit exec — compare mtime to the container start). Then `docker exec -u abc beets beet -l /config/library.db version` (explicitly the survivor DB, read path only) → must list `plugins: musicbrainz` (C4-a live, survivor). `docker inspect beets` → `/media` RW=false and `/config/config.yaml` RW per the final mount mode.
    Run `bash scripts/check-music-freeze.sh`: `beets databases` now 1 (the survivor) plus the sabnzbd ones still present → still red only on the sabnzbd retired paths; record.
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; D=/mnt/fast/appdata/arrs/beets/config; test \"\$(timeout 20 sqlite3 \"file:\$D/library.db?mode=ro\" 'PRAGMA integrity_check;')\" = ok && test \"\$(timeout 20 sqlite3 \"file:\$D/library.db?mode=ro\" 'SELECT count(*) FROM items;')\" = 0 && cmp -s \$D/config.yaml /mnt/fast/stacks/stacks/selfhosted/arrs/beets/config.yaml && echo FRESH-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Survivor `running` with the image tag recorded; `docker inspect` shows `/media` RW=false.
    - Fresh library.db: integrity ok, 0 items, no `.bak` sibling; creation evidence recorded.
    - `beet version` (no -c) output contains `plugins: musicbrainz`.
    - Appdata config.yaml byte-identical to the repo file (`cmp` 0).
    - EROFS/lsiown lines recorded verbatim or "none observed".
  </acceptance_criteria>
  <done>The shipped survivor is running at the pinned version with one fresh, empty library.db and a config that declares musicbrainz.</done>
</task>

<task type="auto">
  <name>Task 2: The criterion-4 probe pair and the hand-read cross-check (D-16, D-17, D-18/D-29)</name>
  <files>(host only: /mnt/fast/appdata/arrs/beets/config/probe-04/)</files>
  <read_first>
    - scripts/spike03-discogs-probe.py (argparse ~820-866: positional FOLDER list, required --ledger, --require-plugin, --mb-only; record fields ~690-750 incl. "source")
    - 04-RESEARCH.md § Code Example 4, Pitfall 10
    - /mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets-config.yaml on LXC 100 (its existing `import:` mapping)
  </read_first>
  <action>
    Re-assert D-29: the album directory (container path recorded by 04-01) exists with 12 `*.flac` and no audio in subdirectories; if not, HALT (no substitute without an operator ruling).
    Create `/mnt/fast/appdata/arrs/beets/config/probe-04/` (owner matching `/config`, i.e. the abc uid). Assert the live sabnzbd `beets-config.yaml` sha256 = 04-01's (still broken) — else HALT per conventions. Copy it to `probe-04/broken.yaml`; in the copy only, set probe-only keys: `statefile: /config/probe-04/state.pickle` (top-level) and, inside the EXISTING `import:` mapping (do not add a second `import:` key), `write: no`, `copy: no`, `move: no`, `log: /config/probe-04/import.log`; also point any top-level `library:`/`directory:`/`log:` path that falls outside `/config/probe-04` into it. Create `fixed.yaml` from broken.yaml changing ONLY `plugins: embedart` → `plugins: embedart musicbrainz`. `diff broken.yaml fixed.yaml` must show exactly one changed line. Copy `/mnt/fast/stacks/scripts/spike03-discogs-probe.py` into probe-04/.
    Run, all with `-u abc` and `-l /config/probe-04/probe.blb` / `SPIKE_DB=/config/probe-04/probe.blb`, and capture full transcripts:
    (1) `beet -c /config/probe-04/broken.yaml -l … version` → plugins line lists embedart only; `beet -c /config/probe-04/fixed.yaml -l … version` → lists embedart and musicbrainz (the `-c` overlay's `plugins:` overrides the base config's — this proves it).
    (2) Gate control: probe `--mb-only --require-plugin musicbrainz` with `SPIKE_CONFIG=/config/probe-04/broken.yaml` → REFUSES, naming musicbrainz as not loaded.
    (3) Negative (D-17): probe `--mb-only --require-plugin embedart --ledger /config/probe-04/neg` with broken.yaml on the album → completes; count ledger records with `"source": "MusicBrainz"` (use `python3 -c` json parsing on the host; jq if present) → must be 0, and the run must show no MusicBrainz HTTP request (no source plugin loaded).
    (4) Positive: probe `--mb-only --require-plugin musicbrainz --ledger /config/probe-04/pos` with fixed.yaml on the same album → MusicBrainz record count ≥ 1 (503 rule applies); record the top candidate's album, track count and distance/score.
    (5) Hand-read: `printf 'b\n' | docker exec -i -u abc beets beet -c /config/probe-04/fixed.yaml -l /config/probe-04/probe.blb import -t -W -C "<album container path>"` → transcript shows a MusicBrainz candidate (match header with similarity/source) and the abort; nothing imported.
    Record for each run: exit code, UTC time, and (for 3/4) the ledger record counts.
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "cd /mnt/fast/appdata/arrs/beets/config/probe-04 && test \$(diff broken.yaml fixed.yaml | grep -c '^[<>]') = 2 && grep -c 'plugins: embedart musicbrainz' fixed.yaml"</automated>
  </verify>
  <acceptance_criteria>
    - C4-b: negative MusicBrainz count = 0 and positive ≥ 1, same album, same `-l`, configs differing by one line (diff shows exactly 2 `<`/`>` lines).
    - `beet version` transcripts differ exactly in the musicbrainz plugin; the gate control refused.
    - Hand-read transcript contains a candidate line and the abort; `SELECT count(*) FROM items` on probe.blb = 0 after the run.
    - Survivor `/config/library.db` sha256 unchanged from Task 1 (D-16: the probe never touched it).
  </acceptance_criteria>
  <done>Criterion 4 (as amended) is measured: the plugin line alone flips zero candidates to a MusicBrainz match.</done>
</task>

<task type="auto">
  <name>Task 3: Return the survivor to dormancy and clean up (D-02, Pitfall 10)</name>
  <files>(host only)</files>
  <read_first>
    - Task 1 pre-up listing recorded in this plan's working notes
  </read_first>
  <action>
    `docker compose -f "$F" --env-file "$E" --profile manual down`; unfiltered `docker ps -a --format '{{.Names}}' | grep -cx beets` → 0; `docker network ls | grep -c beets_default` → 0. Remove `/mnt/fast/appdata/arrs/beets/config/probe-04` with the S5 guard (realpath equals that exact path; no `..`); `test ! -e`. Compare the survivor tree listing (maxdepth 1) with Task 1's pre-up listing: the only differences allowed are the fresh `library.db` and any file LSIO init creates (record each, e.g. none expected; `state.pickle` must be unchanged because statefile pointed into probe-04). `du -s Music` unchanged. Run `bash scripts/check-music-freeze.sh` → only the sabnzbd retired paths remain red; `beets databases` includes the survivor plus sabnzbd's. Write the evidence summary (tag used, EROFS outcome, fresh-DB facts, the five transcripts' key lines) into 04-09-SUMMARY.md.
  </action>
  <verify>
    <automated>timeout 60 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; test \$(timeout 20 docker ps -a --format '{{.Names}}' | grep -cx beets) = 0 && test ! -e /mnt/fast/appdata/arrs/beets/config/probe-04 && echo DORMANT"</automated>
  </verify>
  <acceptance_criteria>
    - No `beets` container in any state; probe-04 gone; `state.pickle` sha unchanged; `Music/` size unchanged.
    - Freeze run transcript recorded with the survivor counted as the one beets DB outside sabnzbd.
  </acceptance_criteria>
  <done>The survivor is dormant again (D-02), leaving exactly one new artefact: its fresh library.db.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| survivor container → library | reads /media (ro) and downloads (rw) |
| probe → musicbrainz.org | outbound API calls from the survivor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | download tree via `import` | mitigate | `-W -C`, overlay `write/copy/move: no`, abort at prompt; probe.blb item count asserted 0 |
| T-04-09-02 | Tampering | survivor library.db | mitigate | every probe run uses throwaway `-l`; survivor DB sha asserted unchanged |
| T-04-09-03 | Elevation of privilege | library mount | mitigate | `/media` RW=false asserted via `docker inspect`; nothing flips it |
| T-04-09-04 | Tampering (supply chain) | image | mitigate | `up --pull never`; any Renovate ls-build is pulled only explicitly, with digest recorded; non-2.13.1 halts |
| T-04-09-05 | Information disclosure | Discogs token | mitigate | `--mb-only` only; token never enters the container |
| T-04-09-06 | Tampering | `rm -rf probe-04` | mitigate | S5 guard: exact realpath match, `..` refusal |
</threat_model>

<verification>
Fresh DB facts; five transcripts with counts 0 / ≥1; survivor dormant; probe-04 removed.
</verification>

<success_criteria>
ROADMAP criterion 4 (amended) measured on the container this phase ships, and criterion 5's "one library.db" has its one DB.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-09-SUMMARY.md` when done.
</output>
