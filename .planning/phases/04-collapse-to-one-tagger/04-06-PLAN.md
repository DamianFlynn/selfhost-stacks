---
phase: 04-collapse-to-one-tagger
plan: 06
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - scripts/check-music-freeze.sh
  - scripts/quick-health-check.sh
autonomous: true
requirements: [TAGR-03, TAGR-04]
tags: [health-check, census, negative-control]

must_haves:
  truths:
    - "D-21: check-music-freeze.sh classifies a container as tagger-capable by what it MOUNTS (a beets config or beets/wrtag database path), not by name; `sabnzbd` is therefore classified tagger-capable and printed separately from the rw-on-library list (Pitfall 15)"
    - "D-21: the census enumerates `docker ps -aq` (ALL states, incl. created/exited); when docker cannot be read every census counter prints UNKNOWN and FAILURES increments"
    - "D-21 / D-25: a new census asserts tagger definitions = 1 (git), beets databases = 1 at SURVIVOR_DB, every retired path absent, rw on /mnt/tank/media on non-tagger containers = 0 excluding Jellyfin, with Jellyfin printed on its own line (F10)"
    - "D-21: RETIRED_DB_PATHS is ADDITIVE (can only make the check red); the first run on the un-retired estate exits 1 naming each retired path that still exists — the driven negative control against real state"
    - "The `📊 7. Summary` anchor is not renumbered and quick-health-check.sh's selector is widened to the new counter labels, so the fold-in shows them and never lands in its WR-09 UNKNOWN branch"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "section 6b tagger census; mount-based classifier; env overrides"
      contains: "RETIRED_DB_PATHS"
    - path: "scripts/quick-health-check.sh"
      provides: "selector widened to census counters"
      contains: "tagger definitions"
  key_links:
    - from: "scripts/quick-health-check.sh fold-in selector"
      to: "check-music-freeze.sh 📊 7. Summary labels"
      via: "grep -E label list"
      pattern: "tagger definitions\\|beets databases\\|retired paths present"
---

<objective>
Build the standing guard for this phase's outcome (D-21, D-25) and land it BEFORE any deletion, so that its
first run on today's un-retired estate is its own driven negative control.

Purpose: DEF-03-01 — the harness reported `tagger-class writers: 0` while structurally blind to beets in
sabnzbd. After this plan, a resurrected tagger, a stray beets database or a new `.bak` turns the routine
health check red.
Output: patched `check-music-freeze.sh` and `quick-health-check.sh`; red-run transcripts (expected red
until plan 04-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/03-tagger-spike/deferred-items.md
@README.md
@scripts/check-music-freeze.sh
@scripts/check-jellyfin-transcode.sh
@scripts/quick-health-check.sh
</context>

<conventions>
- README § Health Checks, three rules: "could not look" (UNKNOWN, exit 1) is distinct from "nothing is
  wrong"; remote commands bounded Linux-side; assert with `fail()`, never report with `info()` (CR-01).
- check-music-freeze.sh is host-resident bash (`set -euo pipefail`, GNU tools). quick-health-check.sh is
  macOS `/bin/bash` 3.2: no `timeout`, no GNU sed, ANSI via `$'\033'`.
- Delivery: explicit-path staging; `git push origin main`; `git -C /mnt/fast/stacks pull --ff-only` on LXC
  100 (halt if not fast-forward); record host HEAD.
- Scratch on LXC 100: `/mnt/fast/scratch-04/06/`, removed at the end.
- EXPECTED RED WINDOW: from this plan's push until plan 04-11, the freeze fold-in in quick-health-check.sh
  is red. That is the intended state and is recorded, not "fixed".
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Section 6b tagger census + mount-based classifier + env overrides in check-music-freeze.sh</name>
  <files>scripts/check-music-freeze.sh</files>
  <read_first>
    - scripts/check-music-freeze.sh (whole file; constants 71-81, helpers 116-121, section 1 ~185-247 incl. MOUNT_ROWS and count_lines, section 2 prose ~252-280, section 6 ~480-515, summary 517-549)
    - scripts/check-jellyfin-transcode.sh lines 121-133 (ENV OVERRIDES doctrine), 150-154 (override block), 205-228 (override-can-only-make-it-red rule), 636-653 (assert_enc wording), 740-774 (cross-file contract comment; UNKNOWN summary lines)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § scripts/check-music-freeze.sh (analogs A-E, cross-file anchor warning)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md Pitfalls 12 and 15, § Anti-Patterns (find -xdev)
  </read_first>
  <action>
    Constants: replace `TAGGER_PATTERN='beets|soulbeet|wrtag|lidarr'` with `RENAMER_PATTERN='lidarr'` (Lidarr's renamer writes paths/tags but mounts no beets state, so it stays name-classified) and add a mount-based classifier `is_tagger_mount SRC` returning true when SRC (the mount's host `.Source`) is a file named `beets-config.yaml`, `library.db`, `*.blb` or `wrtag.db`, or is a directory under `/mnt/fast/appdata` containing, within `-maxdepth 3`, any of those names or a `config.yaml` whose content has a top-level `plugins:`/`library:` key. A container is tagger-class if any mount satisfies `is_tagger_mount` OR its name matches RENAMER_PATTERN. Use this in section 1 where `$TAGGER_PATTERN` was matched (section 1 keeps its running-container scope and its existing summary labels).
    Env overrides block (header doctrine copied from check-jellyfin-transcode.sh, `${VAR:-default}` form): `SURVIVOR_DB="${SURVIVOR_DB:-/mnt/fast/appdata/arrs/beets/config/library.db}"`; `RETIRED_DB_PATHS="${RETIRED_DB_PATHS:-}"` — colon-separated EXTRA retired paths APPENDED to a built-in list, never replacing it (state in the header: an override can only make this check red, never green); `APPDATA_ROOT="${APPDATA_ROOT:-/mnt/fast/appdata}"`. Built-in retired list: `/mnt/fast/appdata/media/wrtag`, `/mnt/fast/appdata/arrs/soulbeet`, `/mnt/fast/appdata/arrs/beets/config/musiclibrary.blb`, `/mnt/fast/appdata/arrs/beets/config/config.yaml.old`, `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb`, `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets.log`, `/mnt/fast/appdata/arrs/sabnzbd/config/.config/beets`.
    New section `6b. Tagger census (Phase 4 — D-21, D-25)` inserted after section 6 and BEFORE `📊 7. Summary` — do NOT renumber 7. It computes and asserts: (i) TAGGER_DEFS = `git ls-files stacks | xargs grep -lE '^\s*image:\s*(lscr\.io/linuxserver/beets|sentriz/wrtag|ghcr\.io/terry90/soulbeet|metasauce/beets-flask)'` count, fail unless exactly 1 AND it is `stacks/selfhosted/arrs/beets/beets.yaml` (a header note says Phase 5 must revise this when beets-flask lands); (ii) DB discovery: `find "$APPDATA_ROOT"` WITHOUT `-xdev` (Pitfall 12: `-xdev` is blind across child datasets), pruning heavy trees you measure at execution (record the prune list and the elapsed time — the whole script must stay well under quick-health-check's 120 s REMOTE_TIMEOUT), candidates named `library.db*`, `*.blb*`, `wrtag.db*`; classify each as a beets DB when `sqlite3 "file:<p>?mode=ro"` lists both `items` and `albums` tables, and as a tagger DB when named `wrtag.db*`; unreadable/non-sqlite candidates are listed as `unclassified` and fail. Fail unless beets DBs = 1 and that one is SURVIVOR_DB; fail for every tagger DB. Positive control: the find must also return the Jellyfin appdata SQLite DB whose path you read from the Phase 1 fence MANIFEST (add that exact filename to the candidate set as control-only); if it is not found, print `UNKNOWN — census blind` and fail. (iii) each path in built-in + RETIRED_DB_PATHS: `test -e` → `fail "retired path PRESENT: <path>"`. (iv) All-states mount census: enumerate `docker ps -aq` (if docker errors, set CENSUS_OBSERVED=0); for every container list rw mounts whose source is `/mnt/tank/media` or under it; Jellyfin (CONSUMER_PATTERN) is counted into its own counter; any other non-tagger rw holder fails; also list tagger-capable containers (by is_tagger_mount) separately with their mode on `/mnt/tank/media` (expected: sabnzbd, none). In 📊 7. Summary append lines, each printing `UNKNOWN` instead of a number when its input was not observed: `  tagger definitions:          N   (target 1)`, `  beets databases:             N   (target 1 = SURVIVOR_DB)`, `  retired paths present:       N   (target 0)`, `  rw on Music, non-tagger:     N   (target 0, excluding the D-21 consumer exception)`, `  rw on Music, Jellyfin D-21:  N   (documented consumer exception, printed separately)`, `  tagger-capable containers:   N   (mounts a beets config/DB; reported)`. Update the header "What it covers" list, the fold-in description, and section-2 prose that names `arrs/soulbeet.yaml`. `bash -n`.
    Commit (`feat(04-06): behaviour-based tagger census in check-music-freeze.sh (D-21)`).
  </action>
  <verify>
    <automated>bash -n scripts/check-music-freeze.sh && test "$(grep -v '^\s*#' scripts/check-music-freeze.sh | grep -c 'TAGGER_PATTERN')" = 0 && grep -v '^\s*#' scripts/check-music-freeze.sh | grep -c 'docker ps -aq' && grep -c '^echo "📊 7\. Summary"' scripts/check-music-freeze.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` exits 0; `grep -v '^\s*#' … | grep -c TAGGER_PATTERN` = 0; `grep -v '^\s*#' … | grep -c 'docker ps -aq'` ≥ 1; `grep -v '^\s*#' … | grep -c -- '-xdev'` = 0 in section 6b.
    - `grep -c '^echo "📊 7\. Summary"'` = 1 (anchor unchanged).
    - Each of the six new summary labels appears exactly once in a non-comment `echo`.
    - `grep -c 'RETIRED_DB_PATHS:-' ` = 1 and the header states the override is additive.
  </acceptance_criteria>
  <done>The harness can see tagging state by behaviour, across every container state, and will go red on a resurrected tagger or stray database.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the quick-health-check selector; push; drive the negative controls on the real estate</name>
  <files>scripts/quick-health-check.sh (and scripts/check-music-freeze.sh only for the back-pointer comment, if Task 1 omitted it)</files>
  <read_first>
    - scripts/quick-health-check.sh lines 1-60 (EXIT-CODE notices), 491-557 (freeze fold-in; selector at 539-540), 734-746 (failure tail)
    - scripts/check-jellyfin-transcode.sh lines 740-755 (cross-file contract comment to copy)
  </read_first>
  <action>
    In the freeze fold-in, widen the selector at ~540 from `tagger-class|unclassified|declared rw|ownership mismatches` to also match `tagger definitions|beets databases|retired paths present|rw on Music|tagger-capable`, and add a cross-file contract comment (check-jellyfin-transcode.sh:740-755 shape) listing every selected label and the anchor `📊 7. Summary`, stating that renaming any of them in check-music-freeze.sh must change this file in the same commit. If Task 1 did not already add a one-line comment beside the new summary lines in check-music-freeze.sh pointing back at this selector, add it in this task's commit (same plan, so file ownership is unchanged). No new fatal block is added here, so no new EXIT-CODE notice is required; do update the header sentence describing what the freeze block asserts to mention the Phase 4 census. `/bin/bash -n scripts/quick-health-check.sh` (bash 3.2).
    Commit (`feat(04-06): fold census counters into quick-health-check (D-25)`), push, host `pull --ff-only`, record host HEAD.
    Driven controls on LXC 100 (record full transcripts, ANSI stripped, in the SUMMARY):
    (a) FIRST RUN on the un-retired estate: `bash /mnt/fast/stacks/scripts/check-music-freeze.sh` → MUST exit 1 and MUST print `retired path PRESENT:` for each built-in retired path that 04-01 recorded as existing (wrtag tree, soulbeet tree, musiclibrary.blb, config.yaml.old, sabnzbd library.blb, beets.log, .config/beets), `beets databases:` > 1, `tagger definitions: 1`, `rw on Music, Jellyfin D-21: 1`, `rw on Music, non-tagger: 0`, and sabnzbd listed as tagger-capable. Any retired path NOT named while present = the census is defective: fix before proceeding. Record elapsed time (`time`).
    (b) Additive override: create `/mnt/fast/scratch-04/06/exists`; run with `RETIRED_DB_PATHS=/mnt/fast/scratch-04/06/exists` → exit 1 and the scratch path is named in addition to the built-in ones.
    (c) Blind docker: put a stub `docker` that exits 1 at the front of PATH (`/mnt/fast/scratch-04/06/stub/docker`) → exit 1 and the census counters print `UNKNOWN`, never `0`.
    (d) From the workstation: `bash scripts/quick-health-check.sh` → exits 1 with the freeze block `❌ BROKEN` listing the `retired path PRESENT` lines (expected until 04-11).
    Remove `/mnt/fast/scratch-04/06` and assert gone.
  </action>
  <verify>
    <automated>/bin/bash -n scripts/quick-health-check.sh && grep -c 'retired paths present' scripts/quick-health-check.sh && timeout 200 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "set -o pipefail; cd /mnt/fast/stacks && timeout 150 bash scripts/check-music-freeze.sh 2>&1 | grep -c 'retired path PRESENT'"</automated>
  </verify>
  <acceptance_criteria>
    - Control (a): exit 1; `grep -c 'retired path PRESENT'` ≥ the number of retired paths 04-01 recorded as present; runtime under 60 s recorded.
    - Control (b): exit 1 and output contains `/mnt/fast/scratch-04/06/exists`.
    - Control (c): exit 1 and every census counter line contains `UNKNOWN`.
    - Control (d): workstation exit 1 and freeze block shows `retired path PRESENT`.
    - `/mnt/fast/scratch-04/06` absent at the end.
  </acceptance_criteria>
  <done>The guard is live, proven able to fail on real state, and the phase's red window is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 (ssh) | health check executes host-resident code |
| docker socket / appdata → census | census reads container metadata and SQLite files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Repudiation | census blind spots | mitigate | all-states enumeration; no `-xdev`; Jellyfin-DB positive control; UNKNOWN never 0 |
| T-04-06-02 | Tampering | env overrides | mitigate | RETIRED_DB_PATHS additive only; documented "can only make it red" |
| T-04-06-03 | Tampering | SQLite reads | mitigate | `mode=ro` URIs; read-only by contract (no writes in the script) |
| T-04-06-04 | Denial of service | fold-in timeout | mitigate | runtime measured; heavy trees pruned; REMOTE_TIMEOUT bound unchanged |
</threat_model>

<verification>
Four driven controls recorded with exit codes; anchor intact; selector widened.
</verification>

<success_criteria>
A standing, fail-closed guard for criterion 5 exists and has already failed on the real estate for the right reasons.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-06-SUMMARY.md` when done.
</output>
