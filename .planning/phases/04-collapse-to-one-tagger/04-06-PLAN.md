---
phase: 04-collapse-to-one-tagger
plan: 06
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - scripts/check-music-freeze.sh
  - scripts/quick-health-check.sh
autonomous: true
requirements: [TAGR-03, TAGR-04]
tags: [health-check, census, negative-control, candidate]

must_haves:
  truths:
    - "D-21: check-music-freeze.sh classifies a container as tagger-capable by what it MOUNTS (a beets/wrtag/soulbeet config or database path, incl. beets-config.yaml, beets_config.yaml, library.db*, *.blb*, wrtag.db*, soulbeet.db*), not by name; `sabnzbd` is therefore classified tagger-capable and printed separately (Pitfall 15)"
    - "Classification never authorises a write: across `docker ps -aq` (ALL states, incl. created/exited) every container holding rw reaching Music fails, Jellyfin (the D-21 consumer exception) is the sole exception and is printed on its own line; a created and an exited tagger-class fixture are both seen to fail"
    - "D-21 / D-25: section 6b asserts tagger definitions = 1 (git), beets databases = 1 at the constant SURVIVOR_DB, no tagger database, every retired path absent; when docker or the appdata walk cannot be read (docker error, find RC other than 0/1, timeout, or the Jellyfin positive-control DB missing) the census prints UNKNOWN and FAILURES increments"
    - "REVIEWS row 1: section 6b is a CANDIDATE check — gated by the in-script constant TAGGER_CENSUS_PROMOTED=0 and run only when the caller sets CENSUS_CANDIDATE=1 — so the routine check-music-freeze.sh / quick-health-check.sh result stays at its 04-01 pre-phase baseline; plan 04-11 promotes it"
    - "Every env override in 6b can only make the check red (RETIRED_DB_PATHS and CENSUS_EXTRA_LIBRARY_ROOTS are additive; CENSUS_CANDIDATE only adds a section); SURVIVOR_DB and APPDATA_ROOT are constants, not overrides"
    - "The candidate's first run on the un-retired estate exits exactly 1 naming each retired path that still exists and every beets DB — the driven negative control against real state; tagger definitions = 1 is also driven red with a scratch-clone fixture"
    - "The `📊 7. Summary` anchor is not renumbered and quick-health-check.sh's selector is widened to the new counter labels in advance (inert until promotion), so the fold-in shows them and never lands in its WR-09 UNKNOWN branch"
  artifacts:
    - path: "scripts/check-music-freeze.sh"
      provides: "section 6b tagger census (candidate); mount-based classifier; additive env overrides"
      contains: "TAGGER_CENSUS_PROMOTED"
    - path: "scripts/quick-health-check.sh"
      provides: "selector widened to census counters"
      contains: "tagger definitions"
  key_links:
    - from: "scripts/quick-health-check.sh fold-in selector"
      to: "check-music-freeze.sh 📊 7. Summary labels"
      via: "grep -E label list"
      pattern: "tagger definitions\\|beets databases\\|retired paths present"
    - from: "check-music-freeze.sh section 6b"
      to: "TAGGER_CENSUS_PROMOTED / CENSUS_CANDIDATE gate"
      via: "run 6b iff promoted or candidate"
      pattern: "CENSUS_CANDIDATE"
---

<objective>
Build the standing guard for this phase's outcome (D-21, D-25) and run it against the real estate BEFORE
any deletion, so its first run is its own driven negative control. It does so as a candidate check that
plans invoke directly; it enters the fatal routine path only when plan 04-11 promotes it, in the wave that
turns it green (REVIEWS row 1).

Purpose: DEF-03-01 — the harness reported `tagger-class writers: 0` while structurally blind to beets in
sabnzbd. After promotion, a resurrected tagger, a stray beets database, a new `.bak`, or any non-Jellyfin
container holding rw on Music in any state turns the routine health check red. Until promotion, the
routine check stays at its pre-phase state, so a stall between waves 2 and 6 leaves no permanent red.
Output: patched `check-music-freeze.sh` and `quick-health-check.sh`; candidate red-run transcripts and
fixture-control transcripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collapse-to-one-tagger/04-CONTEXT.md
@.planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md
@.planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md
@.planning/phases/04-collapse-to-one-tagger/04-VALIDATION.md
@.planning/phases/04-collapse-to-one-tagger/04-REVIEWS.md
@.planning/phases/04-collapse-to-one-tagger/04-01-SUMMARY.md
@.planning/phases/03-tagger-spike/deferred-items.md
@README.md
@scripts/check-music-freeze.sh
@scripts/check-jellyfin-transcode.sh
@scripts/quick-health-check.sh
</context>

<conventions>
- README § Health Checks, three rules: "could not look" (UNKNOWN, exit 1) is distinct from "nothing is
  wrong"; remote commands bounded Linux-side; assert with `fail()`, never report with `info()` (CR-01).
- check-music-freeze.sh is host-resident bash (`set -euo pipefail`, GNU tools). quick-health-check.sh is
  macOS `/bin/bash` 3.2: no `timeout`, no GNU sed, ANSI via `$'\033'`.
- Delivery: explicit-path staging; `git push origin main`; `git -C /mnt/fast/stacks pull --ff-only` on LXC
  100 (halt if not fast-forward; never stash or reset); record host HEAD.
- Scratch on LXC 100: `/mnt/fast/scratch-04/06/`, removed at the end and asserted gone. Never `/tmp`
  (LXC 100 `/tmp` is tmpfs), never inside the host checkout (it must stay clean for `pull --ff-only`).
- CANDIDATE, NOT FATAL (REVIEWS row 1): nothing in this plan changes what the ROUTINE invocation
  (no env) exits with. Every red run below is a candidate run with `CENSUS_CANDIDATE=1`. The routine run
  is compared against the 04-01 `ROUTINE BASELINE` at the end of every task that pushes.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Section 6b tagger census (candidate-gated) + mount-based classifier + additive overrides in check-music-freeze.sh</name>
  <files>scripts/check-music-freeze.sh</files>
  <read_first>
    - scripts/check-music-freeze.sh (whole file; REPO_ROOT/cd at 68-69, constants 71-81, helpers 116-121, touches_library ~133-141, section 1 ~185-247 incl. MOUNT_ROWS and count_lines, section 2 prose ~252-280, the house `|| true` find pattern at 361/366/442, section 6 ~480-515, summary 517-549)
    - scripts/check-jellyfin-transcode.sh lines 121-133 (ENV OVERRIDES doctrine), 150-154 (override block), 205-228 (override-can-only-make-it-red rule), 636-653 (assert_enc wording), 740-774 (cross-file contract comment; UNKNOWN summary lines)
    - .planning/phases/04-collapse-to-one-tagger/04-PATTERNS.md § scripts/check-music-freeze.sh (analogs A-E, cross-file anchor warning)
    - .planning/phases/04-collapse-to-one-tagger/04-RESEARCH.md Pitfalls 12 and 15, § Anti-Patterns (find -xdev), § Runtime State Inventory (census method: find RC 1 = unreadable subpaths)
    - 04-01-SUMMARY.md (the actual `.bak` names under sabnzbd scripts/ and .config/beets/; the Jellyfin DB path in the Phase 1 MANIFEST)
  </read_first>
  <action>
    Constants: replace `TAGGER_PATTERN='beets|soulbeet|wrtag|lidarr'` with `RENAMER_PATTERN='lidarr'` (Lidarr's renamer writes paths/tags but mounts no beets state, so it stays name-classified). Add constants (NOT env overrides — per REVIEWS row 1 no override may produce success): `SURVIVOR_DB="/mnt/fast/appdata/arrs/beets/config/library.db"`, `APPDATA_ROOT="/mnt/fast/appdata"`, `CENSUS_FIND_TIMEOUT=90`, and `TAGGER_CENSUS_PROMOTED=0` with a header block "CANDIDATE → PROMOTED (Phase 4, REVIEWS row 1)" stating: while 0, section 6b runs only when the caller sets `CENSUS_CANDIDATE=1`; the routine fold-in from quick-health-check.sh sets nothing, so the routine result stays at its pre-phase state and quick-health-check never inherits a permanent red (its own header says it is built not to); plan 04-11 sets this to 1 in the commit where the candidate run is first green, after which 6b always runs and `CENSUS_CANDIDATE` is ignored (it can never disable 6b).
    Classifier: add `is_tagger_mount SRC` returning true when SRC (a mount's host `.Source`) is a FILE whose basename matches `beets-config.yaml`, `beets_config.yaml`, `library.db*`, `*.blb*`, `wrtag.db*` or `soulbeet.db*` (REVIEWS row 14 — soulbeet mounted `beets_config.yaml` with an underscore and keeps `soulbeet.db`), or is a DIRECTORY under `$APPDATA_ROOT` containing, within `-maxdepth 3`, a file with one of those basenames or a `config.yaml` with a top-level `plugins:` or `library:` key. A container is tagger-capable if any of its mounts satisfies `is_tagger_mount` OR its name matches RENAMER_PATTERN. Use this in section 1 where `$TAGGER_PATTERN` was matched; section 1 keeps its running-container scope, its labels and its pass/fail semantics (a tagger-class and an unclassified rw holder both fail there, so reclassification cannot change the routine exit). Record in a comment that section 1's `docker ps -q || true` is blind when docker errors, and that 6b's UNKNOWN covers that once promoted (recorded, not changed here, so the routine path is untouched).
    Env overrides block (header doctrine copied from check-jellyfin-transcode.sh, `${VAR:-default}` form), each stated in the header as able only to make the check red: `CENSUS_CANDIDATE="${CENSUS_CANDIDATE:-0}"` (adds section 6b); `RETIRED_DB_PATHS="${RETIRED_DB_PATHS:-}"` (colon-separated EXTRA retired paths APPENDED to the built-in list, never replacing it); `CENSUS_EXTRA_LIBRARY_ROOTS="${CENSUS_EXTRA_LIBRARY_ROOTS:-}"` (colon-separated EXTRA roots treated as reaching Music, APPENDED to the built-in `touches_library` set — used only to drive the fixture control without ever mounting the real library rw). Built-in retired list: `/mnt/fast/appdata/media/wrtag`, `/mnt/fast/appdata/arrs/soulbeet`, `/mnt/fast/appdata/arrs/beets/config/musiclibrary.blb`, `/mnt/fast/appdata/arrs/beets/config/config.yaml.old`, `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/library.blb`, `/mnt/fast/appdata/arrs/sabnzbd/config/scripts/beets.log`, `/mnt/fast/appdata/arrs/sabnzbd/config/.config/beets`.
    New section `6b. Tagger census (Phase 4 — D-21, D-25)` inserted after section 6 and BEFORE `📊 7. Summary` — do NOT renumber 7. If `TAGGER_CENSUS_PROMOTED` is 0 and `CENSUS_CANDIDATE` is not `1`, print exactly one line `  6b. Tagger census: CANDIDATE — not in the routine check until plan 04-11 promotes it (run with CENSUS_CANDIDATE=1)` and set nothing else (no census summary lines are printed, no counter touched). Otherwise compute and assert:
    (i) TAGGER_DEFS = `git ls-files stacks | xargs grep -lE '^\s*image:\s*(lscr\.io/linuxserver/beets|sentriz/wrtag|ghcr\.io/terry90/soulbeet|metasauce/beets-flask)'` (capture git's RC; non-zero → UNKNOWN), fail unless exactly 1 AND it is `stacks/selfhosted/arrs/beets/beets.yaml`, naming every file found (a header note says Phase 5 must revise this when beets-flask lands).
    (ii) DB discovery (REVIEWS row 8): `CANDIDATES="$(timeout "$CENSUS_FIND_TIMEOUT" find "$APPDATA_ROOT" <prunes> -type f \( <names> \) -print 2>/dev/null)" || FIND_RC=$?` with FIND_RC initialised to 0 — the house `|| true` shape (lines 361/366/442) but keeping the status. FIND_RC 0 or 1 is accepted (1 = unreadable subpaths, measured in RESEARCH § Runtime State Inventory); 124 → census timed out → UNKNOWN; any other value → UNKNOWN. No `-xdev` (Pitfall 12: blind across child datasets). Prune only heavy trees you measure at execution and record the prune list. Candidate names: `library.db*`, `*.blb*`, `wrtag.db*`, `soulbeet.db*`, plus every backup-name pattern 04-01 recorded under sabnzbd if not already covered by those globs, plus any `*.bak` file whose first 16 bytes are the SQLite header (a non-SQLite `.bak` is not a candidate). Classify each with `sqlite3 "file:<p>?mode=ro"`: beets DB when it lists all four of `items`, `albums`, `item_attributes`, `album_attributes`; tagger DB when named `wrtag.db*` or `soulbeet.db*`; other-sqlite when readable but neither (listed, not failed — e.g. another app's `library.db`); unclassified when unreadable or not SQLite despite a DB-shaped name (fail). Fail unless beets DBs = 1 and that one is SURVIVOR_DB; list every beets DB by path; fail for every tagger DB. Positive control: the result set must contain the Jellyfin appdata SQLite DB whose path is recorded in the Phase 1 fence MANIFEST (add that exact filename to the candidate names as control-only); if FIND_RC is not 0/1 or that DB is absent, print `UNKNOWN — census blind` and fail, and print every DB counter as `UNKNOWN`.
    (iii) each path in built-in + RETIRED_DB_PATHS: `test -e` → `fail "retired path PRESENT: <path>"`.
    (iv) All-states mount census (REVIEWS row 7): `ALL_IDS="$(docker ps -aq)" || DOCKER_RC=$?`; non-zero RC, empty output, or no `jellyfin` among the enumerated names → CENSUS_OBSERVED=0 and every mount counter prints `UNKNOWN`. `docker inspect` every id (RC captured) into rows `name|state|source|destination|rw`. For every rw row whose source satisfies `touches_library` or lies at/under a CENSUS_EXTRA_LIBRARY_ROOTS entry: Jellyfin (CONSUMER_PATTERN) → its own counter; EVERY other container fails regardless of classification (Phase 1 D-20: nobody holds rw on Music, in any state), printed as `fail "rw on Music: <name> [<state>] <src>:<dst> (tagger-capable|non-tagger)"` and counted into the matching one of two counters. Separately list tagger-capable containers (by is_tagger_mount, all states) with their state and their mode on Music (expected: sabnzbd, running, none).
    In 📊 7. Summary, only when 6b ran, append these lines, each printing `UNKNOWN` instead of a number when its input was not observed: `  tagger definitions:          N   (target 1)`, `  beets databases:             N   (target 1 = SURVIVOR_DB)`, `  tagger databases:            N   (target 0 — wrtag.db*/soulbeet.db*)`, `  retired paths present:       N   (target 0)`, `  rw on Music, non-tagger:     N   (target 0, excluding the D-21 consumer exception)`, `  rw on Music, tagger-capable: N   (target 0 — Phase 1 D-20, any container state)`, `  rw on Music, Jellyfin D-21:  N   (documented consumer exception, printed separately)`, `  tagger-capable containers:   N   (mounts a beets/wrtag/soulbeet config or DB; reported)`, and beside them a one-line comment pointing at quick-health-check.sh's selector (cross-file contract). Update the header "What it covers" list (add 6b with its candidate status), the fold-in description, and section-2 prose that names `arrs/soulbeet.yaml`. `bash -n`.
    Commit (`feat(04-06): behaviour-based tagger census as a candidate section (D-21, REVIEWS row 1)`).
  </action>
  <verify>
    <automated>f=scripts/check-music-freeze.sh; test -f $f && bash -n $f && test "$(grep -v '^\s*#' $f | grep -c 'TAGGER_PATTERN')" = 0 && grep -v '^\s*#' $f | grep -c 'docker ps -aq' && grep -c '^echo "📊 7\. Summary"' $f && grep -cx 'TAGGER_CENSUS_PROMOTED=0' $f && test "$(grep -v '^\s*#' $f | grep -cE 'SURVIVOR_DB=.\$\{SURVIVOR_DB|APPDATA_ROOT=.\$\{APPDATA_ROOT')" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` exits 0; `grep -v '^\s*#' … | grep -c TAGGER_PATTERN` = 0; `grep -v '^\s*#' … | grep -c 'docker ps -aq'` ≥ 1; no `-xdev` in section 6b (`awk '/6b\. Tagger census/,/📊 7\. Summary/' … | grep -v '^\s*#' | grep -c -- '-xdev'` = 0).
    - `grep -c '^echo "📊 7\. Summary"'` = 1 (anchor unchanged); `grep -cx 'TAGGER_CENSUS_PROMOTED=0'` = 1.
    - Each of the eight new summary labels appears exactly once in a non-comment `echo`.
    - `grep -c 'RETIRED_DB_PATHS:-'` = 1, `grep -c 'CENSUS_EXTRA_LIBRARY_ROOTS:-'` = 1, `grep -c 'CENSUS_CANDIDATE:-'` = 1; `SURVIVOR_DB` and `APPDATA_ROOT` are plain constants (no `${…:-…}` form); the header states every override can only make the check red.
    - `grep -c 'beets_config.yaml'` ≥ 1 and `grep -c 'soulbeet.db'` ≥ 1 inside `is_tagger_mount`; `grep -c 'FIND_RC'` ≥ 2.
  </acceptance_criteria>
  <done>The harness can see tagging state by behaviour across every container state, can go red on a resurrected tagger, stray database or any non-Jellyfin rw holder — and does so only when invoked as a candidate until 04-11 promotes it.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the quick-health-check selector; push; drive the candidate red on the real estate; prove the routine path unchanged</name>
  <files>scripts/quick-health-check.sh (and scripts/check-music-freeze.sh only for the back-pointer comment, if Task 1 omitted it)</files>
  <read_first>
    - scripts/quick-health-check.sh lines 1-60 (EXIT-CODE notices; line 16-17 "does not inherit a permanent red"), 491-557 (freeze fold-in; selector at 539-540), 734-746 (failure tail)
    - scripts/check-jellyfin-transcode.sh lines 740-755 (cross-file contract comment to copy)
    - 04-01-SUMMARY.md § ROUTINE BASELINE and the retired paths it recorded as present
  </read_first>
  <action>
    In the freeze fold-in, widen the selector at ~540 from `tagger-class|unclassified|declared rw|ownership mismatches` to also match `tagger definitions|tagger databases|beets databases|retired paths present|rw on Music|tagger-capable`, and add a cross-file contract comment (check-jellyfin-transcode.sh:740-755 shape) listing every selected label and the anchor `📊 7. Summary`, stating that renaming any of them in check-music-freeze.sh must change this file in the same commit, and that the census labels only appear once plan 04-11 promotes section 6b (until then the widened selector matches nothing new and the routine output is unchanged). If Task 1 did not add the one-line back-pointer beside the new summary lines in check-music-freeze.sh, add it in this task's commit. Update the header sentence describing what the freeze block asserts to mention the Phase 4 census as a candidate section that plan 04-11 promotes. No new fatal block and no EXIT-CODE notice here (the routine exit behaviour does not change in this plan). `/bin/bash -n scripts/quick-health-check.sh` (bash 3.2).
    Commit (`feat(04-06): pre-widen the freeze selector for the census counters (D-25)`), push, host `pull --ff-only`, record host HEAD.
    Driven controls on LXC 100 (record full transcripts, ANSI stripped, in the SUMMARY):
    (a) CANDIDATE FIRST RUN on the un-retired estate: `cd /mnt/fast/stacks && time CENSUS_CANDIDATE=1 bash scripts/check-music-freeze.sh` — capture its RC separately from its output. It MUST exit exactly 1 and MUST print `retired path PRESENT:` for each built-in retired path that 04-01 recorded as existing (wrtag tree, soulbeet tree, musiclibrary.blb, config.yaml.old, sabnzbd library.blb, beets.log, .config/beets); `beets databases:` > 1 listing the survivor's old library.db and musiclibrary.blb and every sabnzbd `library.blb*` and `.config/beets/library.db*` file; `tagger databases: 1` (wrtag.db); `tagger definitions: 1`; `rw on Music, Jellyfin D-21: 1`; `rw on Music, non-tagger: 0`; `rw on Music, tagger-capable: 0`; sabnzbd listed as tagger-capable; the Jellyfin positive-control DB found. Any retired path NOT named while present, or any beets DB not listed = the census is defective: fix before proceeding. Record elapsed seconds (REVIEWS row 17): "comfortably under REMOTE_TIMEOUT" means ≤ 60 s WITH the Jellyfin control found; if it is over 60 s, first try narrower prunes that keep the control, then record the final figure — plan 04-11 raises REMOTE_TIMEOUT in its promotion commit if the figure stays over 60 s. Never meet the budget by dropping the positive control.
    (b) Additive override: create `/mnt/fast/scratch-04/06/exists`; run with `CENSUS_CANDIDATE=1 RETIRED_DB_PATHS=/mnt/fast/scratch-04/06/exists` → exit 1 and the scratch path is named in addition to the built-in ones.
    (c) Blind docker: put a stub `docker` that exits 1 at the front of PATH (`/mnt/fast/scratch-04/06/stub/docker`), run with `CENSUS_CANDIDATE=1` → exit 1 and every mount counter prints `UNKNOWN`, never `0`.
    (f) ROUTINE PATH UNCHANGED (REVIEWS row 1): on LXC 100 run `bash scripts/check-music-freeze.sh` with NO env — its output contains the `6b. Tagger census: CANDIDATE` line and no `retired path PRESENT` line, and its exit code and set of `❌`/`⚠️` lines are a subset of (or equal to) the 04-01 ROUTINE BASELINE; from the workstation `bash scripts/quick-health-check.sh` with no env → exit code and `❌`/`⚠️` set within the 04-01 baseline. A new red here is a defect in this plan: fix it before proceeding.
    Leave `/mnt/fast/scratch-04/06` for Task 3.
  </action>
  <verify>
    <automated>/bin/bash -n scripts/quick-health-check.sh && grep -c 'retired paths present' scripts/quick-health-check.sh && timeout 200 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "cd /mnt/fast/stacks || exit 3; out=\$(CENSUS_CANDIDATE=1 timeout 90 bash scripts/check-music-freeze.sh 2>&1); rc=\$?; [ \$rc -eq 1 ] || { echo \"candidate rc=\$rc, want exactly 1\"; exit 1; }; printf '%s\n' \"\$out\" | grep -q 'retired path PRESENT' || { echo 'candidate named no retired path'; exit 1; }; r=\$(timeout 90 bash scripts/check-music-freeze.sh 2>&1); printf '%s\n' \"\$r\" | grep -q 'Summary' || { echo 'UNKNOWN routine run printed no summary'; exit 3; }; printf '%s\n' \"\$r\" | grep -q 'Tagger census: CANDIDATE' || { echo 'routine run lacks the CANDIDATE line'; exit 1; }; if printf '%s\n' \"\$r\" | grep -q 'retired path PRESENT'; then echo 'candidate leaked into the routine path'; exit 1; fi; echo CANDIDATE-RED-ROUTINE-UNCHANGED"</automated>
  </verify>
  <acceptance_criteria>
    - Control (a): RC exactly 1 (not 124, not 2); `grep -c 'retired path PRESENT'` ≥ the number of retired paths 04-01 recorded as present; every beets DB 04-01 listed is named; Jellyfin control found; elapsed seconds recorded with the prune list.
    - Control (b): exit 1 and output contains `/mnt/fast/scratch-04/06/exists`.
    - Control (c): exit 1 and every mount counter line contains `UNKNOWN`.
    - Control (f): routine host run prints the CANDIDATE line and no `retired path PRESENT`; routine host and workstation exit codes and `❌`/`⚠️` sets within the 04-01 ROUTINE BASELINE (diff recorded).
  </acceptance_criteria>
  <done>The census is proven to fail on real state for the right reasons, and the routine check is provably still at its pre-phase state.</done>
</task>

<task type="auto">
  <name>Task 3: Fixture controls — tagger definitions driven red (row 12); created/exited tagger holding rw is failed, not exempted (rows 7, 14)</name>
  <files>(host only: /mnt/fast/scratch-04/06/)</files>
  <read_first>
    - scripts/check-music-freeze.sh lines 68-69 (REPO_ROOT follows the script's own location, so a clone's copy censuses the clone)
    - 04-06 Task 1 section 6b (as committed)
  </read_first>
  <action>
    (d) tagger definitions = 1 driven red (REVIEWS row 12): `git clone --quiet --no-hardlinks /mnt/fast/stacks /mnt/fast/scratch-04/06/repo`; in the clone create `stacks/selfhosted/zz-fixture-04/fixture.yaml` containing a `services:` mapping with one service whose `image:` is `metasauce/beets-flask:v2.0.0-rc6`, and `git -C /mnt/fast/scratch-04/06/repo add` it (staged is enough for `git ls-files`; never commit or push from the clone). Run `CENSUS_CANDIDATE=1 bash /mnt/fast/scratch-04/06/repo/scripts/check-music-freeze.sh` → exit 1 with `tagger definitions:          2` and a fail line naming `stacks/selfhosted/zz-fixture-04/fixture.yaml`. The host checkout at `/mnt/fast/stacks` is untouched (`git -C /mnt/fast/stacks status --porcelain` empty before and after).
    (e) created/exited tagger-class fixture (REVIEWS rows 7 and 14), cleanup guaranteed with `trap 'docker rm -f census-fixture-04 >/dev/null 2>&1' EXIT` in the single host shell that runs this control: create `/mnt/fast/scratch-04/06/fakemusic/` and an empty file `/mnt/fast/scratch-04/06/beets_config.yaml`; `docker create --pull never --name census-fixture-04 --network none --entrypoint true -v /mnt/fast/scratch-04/06/fakemusic:/media:rw -v /mnt/fast/scratch-04/06/beets_config.yaml:/config/beets_config.yaml:ro lscr.io/linuxserver/beets:2.13.1-ls349` (state `created`; the real library is never mounted rw — the scratch root is declared as reaching Music only through `CENSUS_EXTRA_LIBRARY_ROOTS`). Run `CENSUS_CANDIDATE=1 CENSUS_EXTRA_LIBRARY_ROOTS=/mnt/fast/scratch-04/06/fakemusic bash /mnt/fast/stacks/scripts/check-music-freeze.sh` → exit 1; census-fixture-04 is listed as tagger-capable (matched by the underscore `beets_config.yaml`), a fail line `rw on Music: census-fixture-04 [created] … (tagger-capable)` is printed, and `rw on Music, tagger-capable: 1`. Then `docker start census-fixture-04` (runs `true`, exits → state `exited`) and re-run → the same fail with `[exited]`. Then the trap removes the fixture; assert it is gone with producer status first: `names=$(timeout 30 docker ps -a --format '{{.Names}}'); rc=$?` — rc must be 0, the list must contain `sabnzbd` (positive control), and must not contain `census-fixture-04`; otherwise UNKNOWN / PRESENT.
    Remove `/mnt/fast/scratch-04/06` (S5 guard: realpath equals that exact path, no `..`) and assert it is gone. Re-run the Task 2 (f) routine comparison once more → still within the 04-01 baseline (the fixture left no residue).
  </action>
  <verify>
    <automated>timeout 90 ssh -n -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "names=\$(timeout 30 docker ps -a --format '{{.Names}}'); rc=\$?; [ \$rc -eq 0 ] || { echo \"UNKNOWN docker ps rc=\$rc\"; exit 3; }; printf '%s\n' \"\$names\" | grep -qx sabnzbd || { echo 'UNKNOWN positive control sabnzbd not listed'; exit 3; }; if printf '%s\n' \"\$names\" | grep -qx census-fixture-04; then echo 'FIXTURE PRESENT'; exit 1; fi; test -d /mnt/fast/stacks/scripts || { echo 'UNKNOWN host checkout not visible'; exit 3; }; test ! -e /mnt/fast/scratch-04/06 && test -z \"\$(git -C /mnt/fast/stacks status --porcelain)\" && echo FIXTURES-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - Control (d): clone run exit 1, `tagger definitions:          2`, fail line names `zz-fixture-04/fixture.yaml`; host checkout porcelain empty before and after.
    - Control (e): created run and exited run each exit 1 with a `rw on Music: census-fixture-04 [created]` / `[exited]` fail line and `rw on Music, tagger-capable: 1`; fixture classified tagger-capable via `beets_config.yaml`.
    - Fixture container absent (producer-status-first check, positive control `sabnzbd`); `/mnt/fast/scratch-04/06` absent; routine comparison still within the 04-01 baseline.
  </acceptance_criteria>
  <done>Every new assertion in the census has been seen to fail, including the one that forbids an idle tagger holding rw, and nothing was left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → LXC 100 (ssh) | health check executes host-resident code |
| docker socket / appdata → census | census reads container metadata and SQLite files |
| fixture container → estate | a created/exited container briefly exists on the Docker host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Repudiation | census blind spots | mitigate | all-states enumeration with RC captured; no `-xdev`; find RC 0/1 only; Jellyfin-DB and jellyfin-container positive controls; UNKNOWN never 0 |
| T-04-06-02 | Tampering | env overrides | mitigate | only additive overrides (RETIRED_DB_PATHS, CENSUS_EXTRA_LIBRARY_ROOTS, CENSUS_CANDIDATE); SURVIVOR_DB/APPDATA_ROOT are constants, so no override can produce green |
| T-04-06-03 | Tampering | SQLite reads | mitigate | `mode=ro` URIs; read-only by contract (no writes in the script) |
| T-04-06-04 | Denial of service | fold-in timeout / permanent red | mitigate | candidate gate keeps the routine path at its baseline until 04-11; runtime measured against a 60 s budget; find bounded by CENSUS_FIND_TIMEOUT |
| T-04-06-05 | Elevation of privilege | fixture container | mitigate | mounts only a scratch dir rw (never the library), `--network none`, `--entrypoint true`, `--pull never`, removed by an EXIT trap and asserted absent |
</threat_model>

<verification>
Candidate controls (a)-(e) recorded with exit codes; routine path proven unchanged against the 04-01 baseline; anchor intact; selector widened.
</verification>

<success_criteria>
A fail-closed guard for criterion 5 exists, has failed on the real estate and on fixtures for the right reasons, and waits as a candidate — the routine health check never enters a red window because of it.
</success_criteria>

<output>
Create `.planning/phases/04-collapse-to-one-tagger/04-06-SUMMARY.md` when done.
</output>

## Review Response

This plan addresses REVIEWS rows 1, 7, 8, 10, 12, 14 and 17; the full table is in 04-01-PLAN.md.
- **Row 1**: section 6b is gated by `TAGGER_CENSUS_PROMOTED=0` and runs only under `CENSUS_CANDIDATE=1`. The old "EXPECTED RED WINDOW" convention is gone. Task 2 (f) and Task 3 prove the routine path stays within the 04-01 baseline. `SURVIVOR_DB` and `APPDATA_ROOT` are no longer overrides.
- **Row 7**: every container in every state with rw reaching Music fails except Jellyfin, and there is a new fatal counter `rw on Music, tagger-capable`. Task 3 (e) adds the created/exited fixture.
- **Row 8**: `find` runs as `|| FIND_RC=$?`. RC 0/1 are accepted; anything else is UNKNOWN, and a missing Jellyfin control DB is UNKNOWN.
- **Row 10**: the verify captures the candidate's RC and asserts it equals 1; the expected exit 1 is no longer piped through `pipefail`.
- **Row 12**: Task 3 (d) adds the scratch-clone definition fixture.
- **Row 14**: `is_tagger_mount` also matches `beets_config.yaml` and `soulbeet.db*`, and the fixture uses the underscore name.
- **Row 17**: Task 2 (a) sets a 60 s budget and records the prune list; the REMOTE_TIMEOUT decision is handed to 04-11's promotion commit.
