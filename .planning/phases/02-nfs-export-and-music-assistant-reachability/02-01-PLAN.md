---
phase: 02-nfs-export-and-music-assistant-reachability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CONS-01, CONS-02, CONS-03]

must_haves:
  truths:
    - "The Music dataset is confirmed mounted and is itself a mount point, so the `mountpoint` export option (D-33 M1) can be relied on rather than assumed"
    - "No competing export table exists on atlantis — `/etc/exports`, `/etc/exports.d/*` and `zfs sharenfs` on `tank` are all confirmed clear, or the operator has ruled on the conflict (D-19)"
    - "`terraform plan` in `infra/` is clean before any Phase 2 code is added, or the operator has ruled on the drift (D-17, D-18)"
    - "The NUC's measured source address on the route to 172.16.1.158 is recorded, not assumed from NETWORK.md (D-20)"
    - "The exact Music Assistant add-on version and channel is recorded, and it is the stable add-on, not the BETA (D-21, D-56)"
    - "The HAOS `ha` CLI answers over SSH through the `bash -lc` wrapper with protection mode off (D-22)"
    - "Music Assistant has an admin user capable of minting a long-lived token, or `/setup` has been completed at an operator checkpoint (D-43)"
    - "Every Music Assistant provider that already exists is enumerated and recorded, so a later album match cannot be credited to Spotify (D-30)"
    - "A pre-change `quick-health-check.sh` transcript exists as the before half of the blast-radius argument (D-44)"
  artifacts:
    - path: ".planning/phases/02-nfs-export-and-music-assistant-reachability/02-01-SUMMARY.md"
      provides: "Stage 0 measured-state record replacing RESEARCH.md's [ASSUMED] A1-A13"
      contains: "Stage 0"
  key_links:
    - from: "atlantis 172.16.1.158"
      to: "/mnt/tank/media/Music"
      via: "mountpoint -q, zfs list mounted"
      pattern: "mountpoint"
    - from: "LXC 100 172.16.1.159"
      to: "Music Assistant 172.16.1.31:8095 and Jellyfin 192.168.90.31:8096"
      via: "HTTP reachability probe deciding where check-music-consumers.sh runs"
      pattern: "8095|8096"
---

<objective>
Measure everything `02-RESEARCH.md` had to assume. The research was written with **no live estate
access** (§ Session Limitation), so every claim about current state carries an `[ASSUMED]` tag and
assumptions A1-A13 are unverified. This plan converts them to measurements and halts on the three
conditions that would make the rest of the phase unsafe or unmeasurable.

Purpose: three of this phase's failure modes are silent. A second export table makes `exportfs -v`
unable to tell you what a client actually gets (D-19). Pre-existing Terraform drift gets swept into
this phase's apply, reverting unrelated host config and making criterion 1's "a re-apply is clean"
meaningless (D-17). A wrong assumption about the NUC's source address makes every denied mount
indistinguishable from a dozen other faults (D-20). None of the three announce themselves.

Output: a measured-state record in the SUMMARY, and either a green light for plans 02/04 or a
blocking operator decision.

**This plan is read-only with exactly one exception.** Every probe is a read, and `ha core info`
style calls read Supervisor state without mutating it. The exception is **Task 4**, which fires only
if Task 3 finds Music Assistant has no admin user at all: at that blocking operator checkpoint the
account is created and a long-lived token minted (D-43, D-53). That mutation touches MA's own state
on the NUC and nothing else — not atlantis, not `infra/`, not `/mnt/tank/media/Music`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nfs-export-and-music-assistant-reachability/02-CONTEXT.md
@.planning/phases/02-nfs-export-and-music-assistant-reachability/02-RESEARCH.md
@CLAUDE.md
@NETWORK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe atlantis for the export preconditions and any competing share</name>
  <files>none (read-only probes; findings recorded in the SUMMARY)</files>
  <read_first>
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-RESEARCH.md` § "Empirical Decision Protocol" → Stage 0 (the literal probe commands 0.1-0.5) and § "Assumptions Log" (A1, A2, A3, A4)
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-CONTEXT.md` → D-19 (blocking on any existing export or non-`off` `sharenfs`), D-51 (`no_root_squash` forbidden), and § Specific Ideas (the `.DS_Store` / unnamed SMB writer)
    - `scripts/check-music-freeze.sh` lines 123-131 — the `zfs_query` remote-delegation helper and the `-o BatchMode=yes -o ConnectTimeout=5` SSH convention every remote call in this phase uses
    - `CLAUDE.md` § Constraints — `acltype=nfsv4` + `aclmode=restricted`, the `0777` reality, and that knfsd does not export NFSv4 ACLs
  </read_first>
  <action>
    Over `ssh -o BatchMode=yes -o ConnectTimeout=5 root@172.16.1.158`, run RESEARCH.md's Stage 0
    probes 0.1 through 0.5 and record each result verbatim.

    Measure, in this order:

    1. **Dataset and mount point (A3).** `zfs list -o name,mountpoint,mounted tank/media/Music` must
       report `mounted` = `yes`, and `mountpoint -q /mnt/tank/media/Music` must succeed. This gates
       the `mountpoint` export option — with `mountpoint` set, an unmounted dataset means the export
       silently does not exist. Anything else **halts the phase**; do not plan around it.
    2. **Child datasets (A4).** `zfs list -r -o name,mountpoint tank/media/Music`. Expect exactly one
       row. A child dataset would be invisible to clients and would be the only reason to revisit
       `crossmnt`, which RESEARCH.md deliberately excludes.
    3. **Ownership as inherited from Phase 1 WRIT-04.** `stat -c '%U:%G %a %n' /mnt/tank/media/Music`.
       Expect `568:568` and mode `0777`. This is the value that becomes `anonuid`/`anongid`. Run this
       **from atlantis, never from LXC 100** — LXC 100 is unprivileged with a sparse idmap and
       reports unmapped ids as `65534`, which is the misreading that cost Phase 1 a full correction.
    4. **Competing export tables (A2, D-19).** `cat /etc/exports`, `ls -la /etc/exports.d/`,
       `exportfs -v`, and `zfs get -r -s local,received sharenfs tank`. All four must be empty or
       `off`. Two export tables for one path is a silent-misbehaviour bug and `exportfs -v` alone
       cannot tell you which one a client is getting.
    5. **Server package (A1).** `dpkg -s nfs-kernel-server` and `apt-cache policy nfs-kernel-server`.
       Record whether it is installed and confirm the candidate's origin is a Debian archive mirror
       (`deb.debian.org` or the Proxmox-configured Debian mirror), not a third-party repo — this is
       the T-02-SC provenance check.
    6. **Listening ports before the change.** `ss -lntp | grep -E ':(111|2049)'` — expect nothing.
       This is the before half of blast-radius B-3.
    7. **The unnamed SMB writer.** Enumerate what else already serves this tree: `systemctl
       list-units --type=service --state=running | grep -Ei 'smb|samba|nmb|nfs'` and
       `ls /etc/samba/ 2>/dev/null`. Phase 1 found a `.DS_Store` owned `65534:65534` in the library
       root, which means a macOS client already has write access over some share nobody has named.
       This plan is already enumerating what serves the tree, so **name it** — closing it is
       explicitly deferred (a WRIT-class change out of scope here), but recording an identified
       target beats leaving an open mystery.

    If probe 4 returns anything non-empty, or probe 1 fails, **stop and escalate to Task 2's
    checkpoint** rather than continuing.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=5 root@172.16.1.158 'set -e; zfs list -H -o mounted tank/media/Music | grep -qx yes && mountpoint -q /mnt/tank/media/Music && test -z "$(cat /etc/exports 2>/dev/null | grep -v "^#" | grep -v "^[[:space:]]*$")" && test -z "$(ls -A /etc/exports.d/ 2>/dev/null)" && zfs get -H -r -o value -s local,received sharenfs tank | grep -qv . ; echo "stage0-atlantis-clean=$?"'</automated>
  </verify>
  <acceptance_criteria>
    - `zfs list -H -o mounted tank/media/Music` outputs exactly `yes`
    - `mountpoint -q /mnt/tank/media/Music` exits 0
    - `stat -c '%U:%G %a' /mnt/tank/media/Music` outputs `apps:apps 777` (uid/gid 568:568)
    - `zfs list -r -o name tank/media/Music` returns exactly one row (`tank/media/Music`)
    - `cat /etc/exports` and `ls -A /etc/exports.d/` both produce no non-comment, non-blank content, OR the finding is carried into Task 2's checkpoint
    - `zfs get -r -s local,received sharenfs tank` produces no rows, OR the finding is carried into Task 2's checkpoint
    - `apt-cache policy nfs-kernel-server` candidate origin string contains a Debian archive host, recorded verbatim in the SUMMARY
    - `ss -lntp` shows no listener on port 111 or 2049 before any change
    - The running-service enumeration output for `smb|samba|nmb|nfs` is recorded verbatim in the SUMMARY, even when empty
  </acceptance_criteria>
  <done>All seven probes have recorded, verbatim results in the SUMMARY. Probes 1 and 4 either pass cleanly or are carried into Task 2 as a blocking finding.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Terraform drift gate and the Stage 0 blocking-findings decision</name>
  <files>none (read-only `terraform plan`; the operator's ruling is recorded in the SUMMARY)</files>
  <read_first>
    - `infra/host.tf` — the existing host-config `null_resource`, so the drift shown in a plan can be attributed rather than guessed at
    - `infra/lxc-selfhost.tf` and `infra/lxc-mpe.tf` — the other two resources a plan diff could touch
    - `infra/terraform.tfvars` — confirm the SSH/API credentials Terraform needs are present before running plan (this file is gitignored; do not print its contents)
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-CONTEXT.md` → D-17, D-18, D-19, D-53
  </read_first>
  <action>
    Run `cd infra && terraform init -input=false && terraform plan -detailed-exitcode -input=false`
    and capture both the full output and the exit code. `-detailed-exitcode` returns **0 = no
    changes**, **1 = error**, **2 = changes present**.

    Then put that result in front of the operator alongside any competing-export or `sharenfs` finding
    from Task 1, and record their ruling. Do **not** resolve drift autonomously and do **not** apply
    anything in this task — it measures and escalates, nothing more. Record the chosen option id in
    the SUMMARY, and if `targeted` is chosen, record the exact `-target=` expression that plan 02-03
    will measure criterion 1b with.
  </action>
  <decision>
    Whether Phase 2 may add Terraform to `infra/`, given (a) the current `terraform plan` result and
    (b) any competing export or `sharenfs` finding from Task 1.
  </decision>
  <context>
    Run `cd infra && terraform init -input=false && terraform plan -detailed-exitcode` first and put
    the output in front of the operator. `-detailed-exitcode` returns **0 = no changes**, **1 = error**,
    **2 = changes present**.

    Exit code 0 with no Task 1 findings is the clean path: record it and release plans 02 and 04.

    Exit code 2 means pre-existing drift. This is not autonomously resolvable. Reverting drift on a
    host running 78+ containers is not a safe unilateral act, and the drift may be deliberate — this
    estate has a documented Renovate deploy-drift problem and `infra/` carries a committed
    `terraform.tfstate`. More sharply: if drift is swept into this phase's apply, criterion 1's "a
    re-apply is clean" stops measuring anything about the export.

    A Task 1 probe-4 hit (an existing export, or `sharenfs` set anywhere on `tank`) is the second
    blocking condition, for a different reason: `/etc/exports.d` and `zfs sharenfs` each generate an
    export table, and mixing them is the documented way to get a silently wrong one.
  </context>
  <options>
    <option id="clean">
      <name>Clean — plan exits 0, Task 1 probe 4 empty</name>
      <pros>Nothing to decide. Criterion 1's clean re-apply will measure only this phase's resource.</pros>
      <cons>None.</cons>
    </option>
    <option id="reconcile">
      <name>Reconcile the drift — apply the existing config first, then add Phase 2</name>
      <pros>`infra/` becomes authoritative again, which is CLAUDE.md's Infrastructure Rule.</pros>
      <cons>Applies unrelated changes to a host running 78+ containers, in a phase that was scoped to add a reader and change no content.</cons>
    </option>
    <option id="update-code">
      <name>Update the Terraform to match reality — absorb the drift into the code</name>
      <pros>No host change. Removes the diff without touching the running estate.</pros>
      <cons>Ratifies whatever drifted, including anything that drifted by accident.</cons>
    </option>
    <option id="targeted">
      <name>Accept the drift and use a targeted apply — `terraform apply -target=null_resource.nfs_music_export`</name>
      <pros>Touches only this phase's resource. Fastest safe path.</pros>
      <cons>Criterion 1's "a re-apply is clean" must then be measured as `terraform plan -detailed-exitcode -target=...`, and that narrowing must be recorded, not silently assumed.</cons>
    </option>
    <option id="reconcile-shares">
      <name>Resolve a competing export / `sharenfs` finding before anything else</name>
      <pros>Removes the two-tables-for-one-path failure before it can produce a wrong result nobody can read.</pros>
      <cons>Blocks the phase until it is understood who created the existing share and why.</cons>
    </option>
  </options>
  <verify>
    <automated>cd infra && terraform init -input=false -backend=false >/dev/null && terraform plan -detailed-exitcode -input=false; echo "tf-plan-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd infra && terraform plan -detailed-exitcode` exit code is captured and recorded verbatim in the SUMMARY (0, 1, or 2)
    - If exit code is 2, the full plan output is presented to the operator and the operator's selected option id is recorded in the SUMMARY
    - If Task 1 probe 4 returned any content, the operator's ruling on it is recorded in the SUMMARY
    - If the `targeted` option is selected, the SUMMARY records the exact `-target=` expression that criterion 1 will be measured with
    - The SUMMARY states, in one sentence, which option was chosen and therefore what "a re-apply is clean" will mean for plan 02-03
  </acceptance_criteria>
  <resume-signal>Reply with one of: `clean`, `reconcile`, `update-code`, `targeted`, or `reconcile-shares`</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Measure the NUC, Music Assistant, and the audit script's reachability</name>
  <files>none (read-only probes; findings recorded in the SUMMARY)</files>
  <read_first>
    - `~/.claude/skills/ha-deercrest/SKILL.md` — the SSH invocation, the mandatory `bash -lc` wrapper without which the `ha` CLI returns `unauthorized`, and the tailnet caveat that the NUC's LAN IP is unreachable from the tailnet while HA is the standby subnet router
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-RESEARCH.md` § "Environment Availability" (the A1-A13 table this task closes) and § "Empirical Decision Protocol" Stage 5-6 (the `/api-docs/commands.json` and `auth/login` → `auth/token/create` shapes)
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-PATTERNS.md` § "`scripts/check-music-consumers.sh`" → the four-row credential table and the "Where it runs" question this task's probes resolve
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-CONTEXT.md` → D-20, D-21, D-22, D-30, D-43, D-56
  </read_first>
  <action>
    Reach the NUC using the ha-deercrest skill's SSH pattern — `ssh -i $HA_SSH_KEY -o BatchMode=yes
    -p $HA_SSH_PORT $HA_SSH_USER@$HA_SSH_HOST`, with every `ha` invocation wrapped as
    `bash -lc '<cmd>'`. Use `bash`, not `zsh`: `zsh -lc` prints the MOTD banner first and breaks JSON
    parsing. If the CLI returns `unauthorized`, protection mode has drifted back on for the Advanced
    SSH & Web Terminal add-on — that is D-22's finding and it is fixed in the add-on UI (Info tab →
    ⋮), not worked around.

    Measure:

    1. **The NUC's real source address toward atlantis (D-20, A5).** Do not take `172.16.1.31` from
       NETWORK.md. Determine it on the NUC with `ip route get 172.16.1.158` and read the `src` field.
       The NUC is *also* a subnet router for `172.16.1.0/24` with a tailnet interface — exactly the
       topology where a source address surprises you, and a denied mount is otherwise
       indistinguishable from a dozen other faults. If the measured `src` is not `172.16.1.31`, that
       value — not the documented one — is what the export line must carry, and the discrepancy is a
       finding that changes plan 02.
    2. **Reachability to atlantis:2049 (B-4).** From the NUC, confirm the path to `172.16.1.158` port
       2049 is not filtered. HAOS ships a busybox userland, so `nc` may be absent; if it is, record
       that and note the fallback is to attempt the mount in plan 05 and read the error.
    3. **The Music Assistant add-on version and channel (D-21, D-56, A7).** `ha apps --raw-json`
       (`ha addons` is deprecated upstream) plus Music Assistant's own `/info` endpoint at
       `http://172.16.1.31:8095/info`. Record the **exact version string and whether it is the stable
       add-on or the BETA**. 2.10 changes the NFS provider's subfolder handling — Route B's ground —
       and adds a `scan_errors.incomplete` guard. The phase is proven against stable. Because add-on
       auto-update stays on (D-56), this recorded version is the value every later criterion is
       stamped with.
    4. **MA admin user (D-43, A8).** Determine whether MA has an admin user at all, by attempting
       `auth/login` against `POST http://172.16.1.31:8095/api` with the operator's credentials.
       Distinguish the two failure shapes explicitly — a *rejected credential* (an account exists,
       the password is wrong) is not an *un-onboarded server* (no account exists), and only the
       second is D-43's case. Every assertion in this phase depends on being able to mint a
       long-lived token. **This task measures and records only; it creates nothing.** If no account
       exists, record that finding and escalate to **Task 4's blocking operator checkpoint**, which
       completes `/setup` and then returns here to finish measurements 4 and 5. Per D-43 that
       checkpoint is a one-off config gap on software already installed and running, not new scope,
       and it does not end the phase — blocking through it would stall Phase 7's CONS-04 too.
    5. **Existing MA providers (D-30).** `config/providers` — enumerate and record every provider that
       already exists, with its `domain` and `instance_id`. An album matching from Spotify must not
       be able to pass a test about an NFS export, and the only way to prevent that is to know what
       was there first.
    6. **Where `check-music-consumers.sh` will run.** Probe, from LXC 100 (`root@172.16.1.159`):
       reachability of Music Assistant at `172.16.1.31:8095` and of Jellyfin at `192.168.90.31:8096`
       (a `t3_proxy` address that publishes no host port and refuses `localhost:8096`). Also confirm
       `curl` and `jq` are present on LXC 100. These three results decide the `# Where it runs:`
       header that plan 04 writes.
    7. **HAOS busybox capabilities (A13).** Check for `sha256sum`, `showmount` and `findmnt` on the
       NUC. `sha256sum` is the estate's hash and D-11's read-through proof needs it on both ends;
       record `md5sum` as the fallback if absent. `findmnt` is how criterion 1e proves NFSv4 was
       actually negotiated rather than assumed.
    8. **The blast-radius before-snapshot (D-44).** Run `bash scripts/quick-health-check.sh` from the
       workstation and capture the full transcript. Run it **now, in its current form** — the
       consumers block is not folded in until plan 09 precisely so this before-run is not already red.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 "command -v curl >/dev/null && command -v jq >/dev/null && curl -s -o /dev/null -w %{http_code} --max-time 10 http://172.16.1.31:8095/info" | grep -qE "^(200|401|403)$" && echo "ma-reachable-from-lxc100=yes"'</automated>
  </verify>
  <acceptance_criteria>
    - `ip route get 172.16.1.158` run on the NUC has its `src` value recorded verbatim in the SUMMARY, and the SUMMARY states explicitly whether it equals `172.16.1.31`
    - `ha apps --raw-json` returns parseable JSON (proving the `bash -lc` wrapper works and protection mode is off), and the Music Assistant add-on slug, version and stable/BETA channel are recorded verbatim
    - `http://172.16.1.31:8095/info` returns an HTTP status in {200, 401, 403} from LXC 100 — any of these proves reachability; a connection timeout does not
    - Jellyfin at `192.168.90.31:8096` returns an HTTP status from LXC 100, and the same request from the workstation is recorded as failing or unreachable (this is what makes the script host-resident)
    - `command -v curl` and `command -v jq` both succeed on `root@172.16.1.159`
    - `config/providers` output is recorded in the SUMMARY as a list of `domain` + `instance_id` pairs, even if empty
    - Presence or absence of `sha256sum`, `showmount` and `findmnt` on the NUC is each recorded as a yes/no, with the chosen D-11 hash algorithm named
    - A full `bash scripts/quick-health-check.sh` transcript, ANSI stripped and otherwise unedited, is captured in the SUMMARY with its exit code
    - The `auth/login` result is recorded as exactly one of: succeeded, rejected-credential (an account exists), or no-account-exists — and if it is no-account-exists, the SUMMARY records that Task 4's blocking checkpoint was raised (D-43, D-53)
  </acceptance_criteria>
  <done>All eight measurements recorded — measurements 4 and 5 may be completed after Task 4's checkpoint when no MA account exists. RESEARCH.md's A1, A2, A3, A4, A5, A6, A7, A8, A12 and A13 are each marked measured-true, measured-false, or measured-with-a-different-value in the SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Conditional — complete Music Assistant's `/setup` if Task 3 found no admin user</name>
  <files>none in-repo (MA's own state on the NUC; the account name — never the password — is recorded in the SUMMARY)</files>
  <read_first>
    - The in-progress `02-01-SUMMARY.md` record of **Task 3 measurement 4**, which is the only thing that decides whether this checkpoint fires at all
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-CONTEXT.md` → D-43 (complete MA's `/setup` at an operator checkpoint and continue — do not abandon the phase), D-53 (this is one of the five named blocking operator checkpoints for this phase), D-39 (where the resulting token is written)
    - `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-RESEARCH.md` § "Assumptions Log" → **A8**, flagged high-risk and unmeasured, and § "Empirical Decision Protocol" Stage 5-6 for the `auth/login` → `auth/token/create` shapes this unblocks
    - `~/.claude/skills/ha-deercrest/SKILL.md` — the address MA is reached on and the tailnet caveat, so the operator is handed a URL that actually resolves for them
  </read_first>
  <action>
    **Fire condition.** This checkpoint fires **only if** Task 3 measurement 4 recorded that Music
    Assistant has **no admin user**. If Task 3 recorded a successful `auth/login`, or recorded a
    rejected credential against an account that does exist, record this task in the SUMMARY as
    `not-fired — MA admin user already present` and continue to the plan's output. Do not present
    anything to the operator in that case.

    When it does fire: MA's first-run onboarding cannot be driven from the API without an existing
    credential — that absence *is* the gap — so creating the account is the operator's step, at MA's
    `/setup` page. Everything either side of it is automated here. Before handing off, confirm MA is
    reachable from LXC 100 and genuinely un-onboarded rather than merely rejecting a wrong password,
    and hand the operator the exact URL. After they confirm, mint the long-lived token over
    `auth/login` → `auth/token/create`, write it per D-39, and **re-run Task 3 measurements 4 and 5**
    so the provider enumeration (D-30) is recorded against a working credential rather than left
    empty.

    **This is the one mutating step in an otherwise read-only plan.** It creates an MA admin account
    and a token. It touches nothing on atlantis, nothing in `infra/`, and nothing under
    `/mnt/tank/media/Music`. Record the account name in the SUMMARY and never the password or the
    token value — this repo is public and has one prior credential exposure on record.

    Per D-43 this blocks only until the page is completed: MA is already installed and running in the
    estate, so this is a one-off config gap rather than new scope, and treating it as a phase-ending
    blocker would stall Phase 7's CONS-04 as well.
  </action>
  <instructions>
    Music Assistant at `http://172.16.1.31:8095` has no admin user, so no long-lived token can be
    minted — and every assertion in this phase, plus Phase 7's CONS-04, depends on one.

    I have already confirmed MA is running, reachable from LXC 100, and un-onboarded rather than
    rejecting a bad password.

    1. Open `http://172.16.1.31:8095/setup` in a browser on the LAN.
    2. Create the admin account. Pick a username you are content to see recorded in a public
       repository — I will record the **account name only**, never the password.
    3. Keep the password in your own secret store. Nothing about it enters this repo.

    I will mint the long-lived token myself over `auth/login` → `auth/token/create` and write it to
    `/mnt/fast/secrets/` on LXC 100 per D-39, then finish the provider enumeration Task 3 could not
    complete without it.
  </instructions>
  <verification>
    `POST http://172.16.1.31:8095/api` with `auth/login` returns a token rather than an auth error,
    and the follow-up `config/providers` call returns HTTP 200 — which is also what completes Task 3's
    measurement 5.
  </verification>
  <verify>
    <automated>ssh -o BatchMode=yes -o ConnectTimeout=10 root@172.16.1.159 'curl -s -o /dev/null -w %{http_code} --max-time 10 http://172.16.1.31:8095/info' | grep -qE '^(200|401|403)$' && echo "ma-setup-checkpoint-precondition-ok"</automated>
    <human-check>Operator confirms either that MA already had an admin user (checkpoint not fired), or that they completed `/setup` and the named account now exists.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records this task as exactly one of `not-fired — MA admin user already present` or `fired — /setup completed at operator checkpoint`; a missing record is a failure, not a pass
    - If fired: the SUMMARY names the account that now exists and contains neither its password nor any token value
    - If fired: `auth/login` against `POST http://172.16.1.31:8095/api` succeeds afterwards and a long-lived token is minted and written per D-39
    - If fired: Task 3 measurements 4 and 5 are re-run against the working credential, and the `config/providers` enumeration is recorded as `domain` + `instance_id` pairs (D-30)
    - `git grep -nE '(Bearer [A-Za-z0-9._-]{20,}|MA_PASS=|eyJ[A-Za-z0-9_-]{10,})' -- .planning/phases/02-nfs-export-and-music-assistant-reachability/` returns nothing
    - Nothing on atlantis, nothing in `infra/`, and nothing under `/mnt/tank/media/Music` was changed by this task
  </acceptance_criteria>
  <resume-signal>Reply `setup-complete <account-name>` when the MA admin account exists, or `not-needed` if Task 3 already found one</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → atlantis (SSH) | Operator credentials cross into a host that will serve the export |
| workstation → NUC (SSH) | `HA_SSH_KEY` crosses into the house's automation hub |
| LXC 100 → MA / Jellyfin HTTP | Bearer tokens will cross this boundary from plan 04 onward; this plan only proves the path |
| probe output → SUMMARY.md (committed, public repo) | Measured state is written into a public repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information disclosure | Probe transcripts committed to a public repo | mitigate | Record credentials by variable name only (`HA_SSH_KEY`, `MA_USER`). Never paste `infra/terraform.tfvars` contents, an `auth/login` request body, or a returned token into the SUMMARY. The repo is public and has one prior credential exposure on record — a value committed here stays recoverable via `git log -S` after redaction. |
| T-02-06 | Elevation of privilege | Pre-existing export table on atlantis | mitigate | Task 1 probe 4 reads `/etc/exports`, `/etc/exports.d/*`, `exportfs -v` and `zfs sharenfs`; any hit routes to Task 2's blocking checkpoint (D-19) rather than being merged silently. |
| T-02-SC | Tampering | `nfs-kernel-server` provenance | mitigate | `apt-cache policy nfs-kernel-server` confirms the candidate origin is a Debian archive mirror, not a third-party repo, before plan 02 authors the install. No language-ecosystem packages are added by this phase, so the registry slopcheck path does not apply. |
| T-02-10 | Information disclosure | Unnamed SMB/Samba share already serving `/mnt/tank/media/Music` | accept | Task 1 probe 7 **names** it (Phase 1's `65534:65534` `.DS_Store` proves a macOS writer exists). Closing it is a WRIT-class change deliberately out of scope for a consumer-reachability phase and may be a share the household uses. Recorded as an identified target, not left as an open mystery. |
| T-02-04 | Spoofing | NUC source address on the route to atlantis | mitigate | D-20: the address is measured with `ip route get`, not taken from NETWORK.md, so the export's single-host restriction is built on an observation. |
</threat_model>

<verification>
- `ssh -o BatchMode=yes root@172.16.1.158 'mountpoint -q /mnt/tank/media/Music'` exits 0
- `ssh -o BatchMode=yes root@172.16.1.158 'ls -A /etc/exports.d/ 2>/dev/null | wc -l'` outputs `0`, or the operator ruled on the finding
- `cd infra && terraform plan -detailed-exitcode` exit code recorded; if 2, an operator option id is recorded
- `ha apps --raw-json` over `bash -lc` returns parseable JSON
- `bash scripts/quick-health-check.sh` before-transcript captured with its exit code
- `git grep -nE '(Bearer [A-Za-z0-9._-]{20,}|MA_PASS=|HA_TOKEN=)' -- .planning/phases/02-nfs-export-and-music-assistant-reachability/` returns nothing
- Task 4 is recorded in the SUMMARY as either `not-fired` or `fired`, never absent (D-43, D-53)
</verification>

<success_criteria>
- The Music dataset is confirmed mounted and a mount point, so `mountpoint` (M1) is a usable option rather than an assumption
- No competing export table or `sharenfs` value exists on `tank`, or the operator has ruled on it
- `terraform plan`'s exit code is recorded and, if non-zero, an explicit operator option governs what "clean re-apply" will mean in plan 03
- The NUC's measured source address toward atlantis is recorded and plan 02's export line is known to be correct or known to need a different value
- The exact MA add-on version and channel is recorded — the value every later criterion is stamped with (D-21, D-56)
- MA has an admin user and a long-lived token can be minted — either it already did, or Task 4's blocking operator checkpoint created one and the fact is recorded (D-43, D-53)
- Existing MA providers are enumerated so no later album match can be miscredited (D-30)
- The `# Where it runs:` question for `check-music-consumers.sh` is answered by measurement, not left implicit
- A pre-change `quick-health-check.sh` transcript exists (D-44 before half)
</success_criteria>

<output>
Create `.planning/phases/02-nfs-export-and-music-assistant-reachability/02-01-SUMMARY.md` when done.

It must contain a **Stage 0 measured-state table** with one row per RESEARCH.md assumption A1-A13,
each marked measured-true / measured-false / measured-with-a-different-value, plus the verbatim
transcripts for probe 4 (competing exports), the `terraform plan` result, the NUC `ip route get`
output, the MA version string, and the `quick-health-check.sh` before-snapshot.

It must also record **Task 4's disposition** — `not-fired` or `fired` — and, if fired, the name of the
MA account that now exists (never its password, never the token).
</output>
