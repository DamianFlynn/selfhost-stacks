---
phase: 03-tagger-spike
plan: 11
type: execute
wave: 8
depends_on: ["03-07", "03-08", "03-09", "03-10"]
files_modified:
  - .planning/phases/03-tagger-spike/03-DECISION.md
  - .planning/phases/03-tagger-spike/03-spike-beets.yaml
  - .planning/phases/03-tagger-spike/03-spike-beets-config.yaml
  - .planning/phases/03-tagger-spike/03-spike-beets-flask.yaml
  - stacks/selfhosted/arrs/beets.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [TAGR-01, TAGR-02, TAGR-06]

must_haves:
  truths:
    - "The record names ONE tagger and states concretely what happens to Mastermix, DMC and Music Factory content under it"
    - "The decision is recorded along two independently scored axes — engine and front end — and the front-end verdict is not derived from the engine verdict"
    - "All three thresholds carry a tested/returned line and none was moved after the evidence"
    - "beets-flask is evaluated by name on axis two with its frictions recorded"
    - "The scratch tree is gone, both spike containers are absent from docker ps -a, the images are either retained with a stated reason or pruned, and df -h / is recorded"
    - "The library is byte-identical to the Phase 1 baseline"
  artifacts:
    - path: ".planning/phases/03-tagger-spike/03-DECISION.md"
      provides: "The completed two-axis decision record with all thresholds resolved and the Phase 4 handoff"
      contains: "Axis two"
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "A pointer from the durable stack record to the Phase 3 decision"
      contains: "03-DECISION"
  key_links:
    - from: ".planning/phases/03-tagger-spike/03-DECISION.md"
      to: "criterion 4"
      via: "a named tool plus a named mechanism for unmatched DJ content"
      pattern: "bootleg|import -A|RESEARCH_LINK"
    - from: "stacks/selfhosted/arrs/beets.md"
      to: ".planning/phases/03-tagger-spike/03-DECISION.md"
      via: "a relative link added to the durable record"
      pattern: "03-DECISION"
---

<objective>
Close the spike: complete the two-axis decision record, resolve all three thresholds, answer criterion
4 concretely, and clean up after the phase.

Criterion 4 is the one that matters most here: **the record must name one tagger and state
concretely what happens to Mastermix / DMC / Music Factory content under it.** A tagger whose answer
is *"that content lives outside the tool"* has failed the requirement — the single writer must be
able to own the whole tree. Criterion 6 requires two independently scored axes and states plainly
that a record answering only axis one has not met TAGR-06. Criterion 8 requires an honest ergonomics
verdict in the operator's own terms.

And the phase must clean up after itself. Everything it created is throwaway by construction: the
scratch tree, two spike containers, two spike compose files, a runtime config containing a
credential, and up to four pulled images on a host whose root filesystem this phase had to rescue.

Purpose: TAGR-01, TAGR-02 and TAGR-06 are met by this record or not at all. Phase 4 deletes the
loser and cites this evidence; Phase 6 configures the winner and grants it `rw`.
Output: a completed `03-DECISION.md`, a pointer from `beets.md`, a torn-down estate, and a closure
verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-tagger-spike/03-CONTEXT.md
@.planning/phases/03-tagger-spike/03-DECISION.md
@.planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md
@.planning/phases/03-tagger-spike/03-WRTAG-EVIDENCE.md
@.planning/phases/03-tagger-spike/03-NORMALISER-BAKEOFF.md
@.planning/phases/03-tagger-spike/03-BEETS-FLASK.md
@.planning/phases/03-tagger-spike/03-ERGONOMICS-SHEET.md
@stacks/selfhosted/arrs/beets.md
@scripts/check-music-freeze.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the two-axis decision record and resolve criteria 4, 5, 6 and 7</name>
  <files>.planning/phases/03-tagger-spike/03-DECISION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 3: Tagger Spike" — all eight criteria; this task answers 4, 5,
      6 and 7 and carries 8's verdict from plan 03-10.
    - Every evidence artefact produced by this phase: `03-DISCOGS-EVIDENCE.md` (criteria 1 and 2,
      T1 and T2), `03-WRTAG-EVIDENCE.md` (criterion 3 and the `tags.Clear` finding),
      `03-NORMALISER-BAKEOFF.md` (D-13, A3), `03-BEETS-FLASK.md` (criterion 7),
      `03-ERGONOMICS-SHEET.md` (criteria 6 and 8, T3), `03-SAMPLE.md`, `03-NORMALISATION.md`,
      `03-PROBE-SMOKE.md`.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Criterion 4* — the four-row table of
      unmatched-content answers with their sources, and § *New capability found during the audit*
      for `autotag: "bootleg"`.
    - `.planning/phases/03-tagger-spike/03-CONTEXT.md` § *Specific Ideas* — the `WRTAG_RESEARCH_LINK`
      quote is asked for verbatim, and the strict/loose gap is a deliverable in its own right.
    - `stacks/selfhosted/arrs/beets.md` lines 1-15 — the house style for the leading blockquote.
  </read_first>
  <action>
Complete `.planning/phases/03-tagger-spike/03-DECISION.md`, replacing every remaining `PENDING`.

`## Axis one — the engine` — score beets against wrtag on the evidence, not on argument. The
required inputs: criterion 1's strict and loose Discogs rates at the stated `index_tracks` setting
with the gap between them; the DMC stratum's rate reported separately; criterion 2's request count,
429 count and 143-folder extrapolation; criterion 3's six wrtag arms; the D-12 claim audit with rows
3, 3a, 3b, 3c, 5, 6, 8 and 15 now carrying measured verdicts; and D-13's bake-off result. Name one
engine and state the single fact that decided it. If the deciding fact is the one the research
predicted — wrtag is MusicBrainz-only and there is no non-MB backend, which the audit found still
**holds** — say so plainly rather than dressing it up; the spike's job was to produce a number, and
a number that confirms the direction is still a number.

`## Axis two — the front end` — score the raw terminal prompt against the reviewable queue **on the
scoring sheet's own figures**: wall-clock, interventions and context switches per album per front
end, the `stuck_points` verbatim, and the criterion 8 week-six verdict. **This verdict must not be
derived from axis one's.** Criterion 6 exists precisely because these are different questions and
the second may matter more to whether the project finishes. If axis two's answer is a front end that
does not pair naturally with axis one's engine, record that tension rather than resolving it by
assumption — that tension is itself the finding.

`## Criterion 4 — what happens to Mastermix / DMC / Music Factory content` — complete the four-row
table with the winner's row stating a **named mechanism**, not a posture:
  - under beets (terminal): `beet import -A` (as-is) after normalisation, with
    `--set albumtype=dj` and a `paths: albumtype:dj:` rule routing it out of `Compilations/`;
  - under beets-flask: an inbox with `autotag: "bootleg"` — *"Import as-is using the meta data of
    files, and group albums using the metadata… Effectively `beet import ... --group-albums -A`"* —
    with what it actually did on the DJ folder in plan 03-10;
  - under wrtag: `WRTAG_RESEARCH_LINK`, quoted verbatim from `stacks/selfhosted/music/wrtag.yaml:73`
    — a hyperlink to a Discogs search for the human to resolve manually. Note that whoever
    configured that line already knew this content lives on Discogs, and wrtag's answer was to hand
    the operator a URL. **Criterion 4 calls that shape of answer a failure**;
  - under wrtag (side effect): `tags.WriteTags(destPath, destTags, tags.Clear)` at `wrtag.go:267`
    wipes every tag not in wrtag's computed set unless a `keep` rule is configured — **148 `TKEY`
    and 45 `EnergyLevel` files at stake**, with its provenance stated as a source read rather than a
    runtime observation, per plan 03-08.
Then state, in one paragraph and in concrete terms, what happens to Mastermix, DMC and Music Factory
content under the **named winner**, using the DMC stratum's separately-reported rate. Do not answer
"that content lives outside the tool".

`## Criterion 5 — thresholds` — confirm all three rows carry a tested/returned line, and add a
sentence naming the commit that put the thresholds in place and the commits that put the evidence in
place, so the ordering is auditable rather than claimed. If any threshold **fired**, say so and say
what it changed about the decision.

`## Criterion 7 — beets-flask by name` — reproduce the completed frictions table from
`03-BEETS-FLASK.md` with the trial's own observations, and the `bootleg` inbox finding. Include the
architecture confirmation: `config/beets/config.yaml` is a normal beets config (one `library`) and
`config/beets-flask/config.yaml` defines `gui.inbox.folders.*` each with its own `autotag` policy —
**one shared `library.db` across N policy-carrying inbox folders**, which is exactly the Phase 5
structure the roadmap needs.

`## Criterion 8 — the week-six verdict` — the operator's own words from the scoring sheet, with the
`stuck_points` that produced it recorded beside it.

`## Handoff to Phase 4` — finalise the three items Phase 4 must not rediscover:
  1. ROADMAP Phase 4 criterion 4's `--pretend` test is **unsatisfiable as written**; it needs
     rewording to `beet import -t` or a `tag_album()` probe.
  2. `renovate.json5:91`'s stated evidence is wrong: `.Release.Media` and `.Track.Position` both
     exist at v0.20.0 and `.Release.Date.Year` was never reworked; the real defect is
     `d.Track.Position = -1` plus a lazily-evaluated `.Media.Position`. TAGR-03 must cite
     `03-WRTAG-EVIDENCE.md`, not the field list.
  3. The version this spike would deploy, with OD-5's three-tag evidence and the recorded
     v0.33.0-to-v0.34.0 path-format no-op.

`## Handoff to Phase 5, 6 and 7` — the D-13 normalisation-tool verdict (binding regardless of
engine); the confirmed one-shared-`library.db`-across-N-inboxes architecture as the Phase 5 shape;
the `beets-flask` "laggy past some hundred folders" limitation against a 143-folder backlog as a
**Phase 9** risk that this trial did not exercise; and the DUPE-01 flag — Phase 1 measured 828
duplicate groups / 19.4% duplication and this phase re-confirmed `dj-mixes` is a byte-for-byte
duplicate subset of `unsorted`, which makes Phase 7's diff join last-wins and needs a roadmap
decision before Phase 7.

Finally, add a pointer to `stacks/selfhosted/arrs/beets.md` — a short section linking to
`../../../.planning/phases/03-tagger-spike/03-DECISION.md` and stating the one-line outcome, so the
durable stack record points at the decision. `beets.md` already anticipates this, and
`stacks/selfhosted/music/wrtag.yaml:20-22` already says *"Phase 4 owns retirement"*. Edit
`beets.md` **only** — do not touch `wrtag.yaml`, `beets/beets.yaml` or `renovate.json5` (D-03).
  </action>
  <verify>
    <automated>bash -c 'grep -c PENDING .planning/phases/03-tagger-spike/03-DECISION.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c PENDING .planning/phases/03-tagger-spike/03-DECISION.md` returns `0`.
    - `## Axis one` names exactly one engine and cites at least three distinct evidence artefacts by
      filename.
    - `## Axis two` names exactly one front end and cites `03-ERGONOMICS-SHEET.md` figures —
      wall-clock, interventions and context switches — rather than restating axis one's reasoning.
      The two verdicts are written as independent scores.
    - `## Criterion 4`'s table has four completed rows, the wrtag row quotes
      `WRTAG_RESEARCH_LINK=https://www.discogs.com/search/?q={query},https://musicbrainz.org/search?query={query}`
      verbatim, and the closing paragraph names a concrete mechanism for Mastermix, DMC and Music
      Factory content under the winner. The phrase "lives outside the tool" appears only as the
      described failure mode, never as the answer.
    - `## Criterion 5` shows T1, T2 and T3 each with Tested and Returned filled, and names the
      commits establishing the ordering. `git log -p -- .planning/phases/03-tagger-spike/03-DECISION.md`
      shows no change to the threshold **definitions** since the plan-03-02 commit.
    - `## Criterion 7`'s frictions table has 8 rows with observations, and the architecture
      confirmation paragraph is present.
    - `## Criterion 8` quotes the operator's own words.
    - `## Handoff to Phase 4` contains all three numbered items.
    - `stacks/selfhosted/arrs/beets.md` contains a relative link to
      `.planning/phases/03-tagger-spike/03-DECISION.md`.
    - `git diff --name-only` for this plan lists `stacks/selfhosted/arrs/beets.md` but **not**
      `stacks/selfhosted/music/wrtag.yaml`, `stacks/selfhosted/arrs/beets/beets.yaml` or
      `renovate.json5`.
  </acceptance_criteria>
  <done>One engine and one front end named on independently scored axes, criterion 4 answered with a named mechanism for DJ content, all three thresholds resolved without any being moved, and the Phase 4–7 handoffs recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Tear down the scratch tree, both spike containers and the throwaway configs</name>
  <files>.planning/phases/03-tagger-spike/03-DECISION.md</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-VALIDATION.md` § *Sampling Rate* → the phase gate: freeze
      check green; scratch tree deleted and its removal confirmed; both spike containers removed and
      confirmed absent from `docker ps -a`; the pulled images either retained deliberately with a
      stated reason or pruned; `df -h /` recorded.
    - `.planning/phases/03-tagger-spike/03-REAP-LIST.md` — the `## After` figures from plan 03-01,
      for the before/after comparison.
    - `scripts/check-music-freeze.sh` lines 123-131 — the `zfs_query()` delegation shape, for the
      snapshot cleanup and the final `zfs diff`.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Scratch mechanics* — reflinked clones free
      space only when the last reference goes, and **ZFS frees space asynchronously; `zfs list` can
      lag a large delete by ~20 s**, so a size check taken immediately after the delete is not a
      measurement.
  </read_first>
  <action>
Tear down in this order, recording each step:

1. **Containers.** `docker compose -f .planning/phases/03-tagger-spike/03-spike-beets.yaml --profile
   manual down` and the same for `03-spike-beets-flask.yaml`. Then confirm absence from
   `docker ps -a` **with no status filter** — the estate's documented `created`-state blind spot
   means a status-filtered query would miss a container that exists but never ran.
2. **The credential-bearing runtime config.** Delete
   `/mnt/fast/spike-03/beets-config/spike-runtime.yaml` (the `beet import -t` config written with
   `umask 077` in plan 03-07, the only file in this phase carrying the token in plaintext) and any
   other file under `/mnt/fast/spike-03/` containing it. Verify by grepping the token's value across
   `/mnt/fast/spike-03/` and getting zero hits, then delete the whole directory.
3. **The scratch tree.** Delete `/mnt/tank/downloads/spike-03` entirely — `raw/`, `normalised/`,
   `normalised-metadata/`, `wrtag-src/`, `bf-inbox/`, `bf-clean/`, `smoke/`. Confirm removal.
   Re-check `zfs list tank/downloads` from atlantis **after a deliberate wait**, because ZFS frees
   asynchronously and an immediately-taken figure is not a measurement.
4. **The snapshot.** Decide and record: keep `tank/downloads@spike-03-t0` or destroy it. Recommend
   **destroying it**, because its only purpose was to be `normalise-dj-tags.py`'s rollback and the
   tree it protected no longer exists — and a snapshot pinning deleted blocks holds space
   indefinitely. `tank/downloads@pre-project` stays; it is Phase 1's fence and is not this phase's
   to touch. Delegate to atlantis and report the route.
5. **The spike compose files.** Delete `.planning/phases/03-tagger-spike/03-spike-beets.yaml`,
   `03-spike-beets-config.yaml` and `03-spike-beets-flask.yaml` in a commit. D-03 called them
   throwaway; leaving them is how a fourth tagging path gets read into the repo six weeks later,
   which is the exact failure mode this project exists to prevent. Their content is already
   reproduced where it matters in the summaries and the decision record.
6. **The images.** For each of `lscr.io/linuxserver/beets:2.13.1-ls349`, `sentriz/wrtag:v0.33.0`,
   `sentriz/wrtag:v0.34.0` and `metasauce/beets-flask:v2.0.0-rc6`, decide **retain with a stated
   reason** or **prune**, and record the decision per image. A defensible split: retain the winner's
   image because Phase 6 will want it, prune the losers'. Do not blanket-prune and do not blanket-
   retain; state the reason either way. `sentriz/wrtag:v0.20.0` predates this phase — leave it for
   Phase 4 to decide, and say so.
7. **Record `df -h /`** and compare against the `## After` figure in `03-REAP-LIST.md`. If the phase
   left the root filesystem worse than it found it, say so and remediate.

Append a `## Teardown` section to `03-DECISION.md` with every step, its command, its result, and the
route for anything delegated to atlantis. Keep the artefacts — the scripts under `scripts/`, the
NDJSON at the fence, and every `.md` in the phase directory — those are the deliverable.

Retain, deliberately, and say why: `scripts/normalise-dj-tags.py` (D-08 requires it committed and
regenerable), `scripts/spike03-discogs-probe.py`, `scripts/spike03-variant-survey.sh`,
`scripts/spike03-wrtag-arms.sh` and `scripts/spike03-image-headroom.sh` — criterion 1's paired
figures must be reproducible and auditable, which is the whole reason D-08 asked for a committed
script with a dry-run mode.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.159 "docker ps -a --format {{.Names}} | grep -c spike; test ! -d /mnt/tank/downloads/spike-03; df -BG --output=avail / | tail -1"</automated>
  </verify>
  <acceptance_criteria>
    - `docker ps -a --format '{{.Names}}'` (no status filter) contains neither `beets-spike` nor
      `beets-flask-spike`.
    - `/mnt/tank/downloads/spike-03` does not exist, and neither does `/mnt/fast/spike-03`.
    - Grepping the Discogs token's value across `/mnt/fast/` and `/mnt/tank/downloads/` returns zero
      hits, and `/mnt/fast/secrets/discogs.env` is still present and still `600 root` — the phase
      cleaned up its copies, not the source of truth.
    - `ssh root@172.16.1.158 "zfs list -t snapshot -o name -H tank/downloads"` reflects the recorded
      snapshot decision, and `tank/downloads@pre-project` and `tank/media/Music@pre-project` are
      both **still present** — Phase 1's fence is untouched.
    - `git ls-files .planning/phases/03-tagger-spike/` lists no `03-spike-*.yaml` file.
    - `docker images --format '{{.Repository}}:{{.Tag}}'` matches the per-image retain/prune
      decisions recorded in `## Teardown`, with a reason recorded for every one of the four.
    - `df -BG --output=avail /` on LXC 100 is **≥ 8G** and is recorded beside plan 03-01's `## After`
      figure.
    - `docker ps -a | wc -l` equals the container count recorded in `03-REAP-LIST.md` — the phase
      created two containers and removed two.
  </acceptance_criteria>
  <done>Nothing this phase created is still running, mounted, or holding a credential; the scratch tree and its snapshot are resolved; the images carry a recorded decision each; `/` is no worse than the phase found it.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator confirms the decision and the phase closes clean</name>
  <files>.planning/phases/03-tagger-spike/03-DECISION.md</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-DECISION.md` — the completed record.
    - `.planning/ROADMAP.md` § "Phase 3: Tagger Spike" — the eight criteria, to check each off.
    - `.planning/phases/03-tagger-spike/03-VALIDATION.md` § *Validation Sign-Off*.
  </read_first>
  <what-built>
A completed two-axis decision record backed by seven evidence artefacts, all three pre-committed
thresholds resolved without any being moved, and a torn-down estate: no spike container, no scratch
tree, no credential copy, a recorded per-image decision, and `/` at or above its 8 GB floor.

Phase 1's fence is untouched: both `@pre-project` snapshots present, `check-music-freeze.sh` green,
`zfs diff tank/media/Music@pre-project` clean from atlantis.
  </what-built>
  <how-to-verify>
1. Read `03-DECISION.md` end to end. It should tell you which engine, which front end, and why —
   with the "why" being numbers from this library's own content, not argument.
2. Check criterion 4 specifically: does the record state, concretely, what happens to your Mastermix,
   DMC and Music Factory content under the named winner? If the answer amounts to *"that content
   lives outside the tool"*, the requirement that matters most here has not been met and the phase
   should not close.
3. Check that axis two's verdict stands on the scoring sheet's own numbers rather than being derived
   from axis one. If a record answers only "which engine", it has not met TAGR-06.
4. Check that your week-six verdict is recorded in **your** words and still reads true now that the
   trial is over.
5. Confirm the teardown from your own side: `ssh root@172.16.1.159 "docker ps -a | grep spike"`
   should return nothing, and `ls /mnt/tank/downloads/spike-03` should fail.
6. Confirm the library is untouched: `bash scripts/quick-health-check.sh` should be green and
   `bash scripts/check-music-freeze.sh` on LXC 100 should exit 0 with `tagger-class writers: 0`.
7. Say whether the decision stands. A "no" here is a legitimate outcome — the evidence exists either
   way, and a decision the operator does not believe is exactly the failure mode this phase's
   ergonomics axis was added to prevent.
  </how-to-verify>
  <action>
Present the completed `03-DECISION.md` to the operator alongside a short closing report: the two
axis verdicts, the criterion 4 answer for Mastermix / DMC / Music Factory, the three threshold
results, the teardown evidence, and the `df -h /` figure against plan 03-01's baseline. Then stop.

The executor does not decide this. Criterion 8's verdict and the confirmation that the decision
stands are the operator's, and this project's documented failure mode is a tool that gets abandoned
because nobody actually believed in it — a decision ratified by an agent would reproduce exactly that.

After the operator responds, record their confirmation or their objection **verbatim and dated** in
`03-DECISION.md`. If they object, do not close the phase: capture the objection, leave the artefacts
in place, and report which criterion the objection bears on so a gap-closure plan can be scoped.

If they confirm: update `.planning/ROADMAP.md`'s Phase 3 row to Complete with the plan count and tick
its checkbox in the Phases list; update `.planning/STATE.md` with the two-axis outcome, the T1/T2/T3
results, the D-13 normalisation verdict, and the Phase 4 handoff items — including that ROADMAP
Phase 4 criterion 4's `--pretend` test is unsatisfiable as written and that `renovate.json5:91`'s
stated evidence is wrong. Do not edit `renovate.json5` itself; TAGR-03 owns it.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.159 "cd /mnt/fast/stacks &amp;&amp; git pull --ff-only &amp;&amp; bash scripts/check-music-freeze.sh"</automated>
    <human-check>Operator has read the decision record, confirmed criterion 4 is answered concretely, and stated whether the decision stands</human-check>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-music-freeze.sh` on LXC 100 exits 0 with `tagger-class writers: 0`,
      `unclassified: 0`, `consumer-class: 1` and `declared rw reaching Music: 0`.
    - `ssh root@172.16.1.158 "zfs diff tank/media/Music@pre-project"` returns clean — the library is
      byte-identical to the Phase 1 baseline.
    - The Phase 1 ownership and mode counts are unchanged from the recorded baseline.
    - `bash scripts/quick-health-check.sh` from the workstation is green.
    - All eight ROADMAP criteria are checked off in the summary with the artefact that satisfies each.
    - The operator's confirmation, or their objection, is recorded verbatim and dated in
      `03-DECISION.md`.
    - ROADMAP.md's Phase 3 row is updated to Complete with the plan count, and `.planning/STATE.md`
      records the two-axis outcome, the T1/T2/T3 results, and the Phase 4 handoff items.
  </acceptance_criteria>
  <done>The operator has read the record, confirmed criterion 4 is answered with a concrete mechanism, and their verdict is recorded verbatim; the freeze harness is green, the library is byte-identical to the Phase 1 baseline, and ROADMAP.md and STATE.md carry the outcome and the Phase 4 handoff.</done>
  <resume-signal>Type "decision stands" to close Phase 3, or state the objection</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch tree → the estate | Teardown deletes on a live host; the delete must not overreach into Phase 1's fence |
| credential copies → disk | One runtime config carried the Discogs token in plaintext and must be provably gone |
| decision record → Phases 4–7 | Downstream phases act on this record; an unsupported claim here becomes an unsupported action later |
| LXC 100 → atlantis | Snapshot destruction crosses this boundary as root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-65 | Information disclosure | The `beet import -t` runtime config carrying the token | mitigate | Deleted explicitly and by name, then the whole `/mnt/fast/spike-03/` tree removed; verified by grepping the token's value across `/mnt/fast/` and `/mnt/tank/downloads/` for zero hits. `/mnt/fast/secrets/discogs.env` itself is left intact at `600 root` — the phase cleans up its copies, not the source. D-04's standing action to **rotate the token on Discogs at project close** is carried forward, because it passed through a conversation transcript |
| T-03-66 | Denial of service | Over-deleting during teardown | mitigate | The delete is scoped to `/mnt/tank/downloads/spike-03` and `/mnt/fast/spike-03` by exact path. `tank/downloads@pre-project` and `tank/media/Music@pre-project` are asserted **still present** afterwards; only `@spike-03-t0` is a candidate for destruction and only with a recorded decision, delegated to atlantis with its route reported |
| T-03-67 | Denial of service | LXC 100 root filesystem left worse than found | mitigate | `df -BG --output=avail /` asserted **≥ 8G** at close and compared against plan 03-01's recorded `## After` figure; per-image retain/prune decisions recorded individually rather than a blanket sweep; container count compared to the recorded baseline |
| T-03-68 | Repudiation | A decision record that outruns its evidence | mitigate | Every verdict cites the artefact that supports it by filename; criterion 4's answer must name a mechanism; axis two must cite the scoring sheet's own figures; all `PENDING` markers must be gone, asserted by `grep -c PENDING` returning `0`; threshold definitions asserted unchanged with `git log -p` |
| T-03-69 | Tampering | A throwaway compose file surviving into the repo | mitigate | All three spike YAML files are deleted in a commit and their absence asserted with `git ls-files`. A spike compose left under version control is how a fourth tagging path gets read into the estate six weeks later — the precise failure this project exists to prevent |
| T-03-70 | Tampering | `/mnt/tank/media/Music` | mitigate | Final gate: `check-music-freeze.sh` exit 0 with all four counts at target, plus a clean `zfs diff tank/media/Music@pre-project` from atlantis and unchanged Phase 1 ownership/mode counts — two independent instruments for "the library is byte-identical to the baseline" |
| T-03-71 | Tampering | Frozen stack definitions (D-03) | mitigate | Only `stacks/selfhosted/arrs/beets.md` is edited, and only to add a pointer. `wrtag.yaml`, `beets/beets.yaml` and `renovate.json5` are asserted absent from the diff; Phase 4 owns them |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan; it completes documents and removes containers, images and directories. The phase-wide audit is in 03-RESEARCH.md § *Package Legitimacy Audit* (3 OK, 0 SUS, 0 SLOP) and the only install of the phase happened inside the beets-flask container in plan 03-10 |
</threat_model>

<verification>
- `grep -c PENDING 03-DECISION.md` returns 0; both axes named and independently supported.
- Criterion 4 answered with a named mechanism; the `WRTAG_RESEARCH_LINK` quoted verbatim.
- T1/T2/T3 all tested/returned; threshold definitions provably unchanged since the plan-03-02 commit.
- No `spike` container in `docker ps -a`; `/mnt/tank/downloads/spike-03` and `/mnt/fast/spike-03` gone.
- Token value returns zero hits across `/mnt/fast/` and `/mnt/tank/downloads/`; `discogs.env` intact at `600 root`.
- Both `@pre-project` snapshots present; `zfs diff tank/media/Music@pre-project` clean from atlantis.
- `df -BG --output=avail /` ≥ 8G; per-image decisions recorded.
- `check-music-freeze.sh` exit 0; `quick-health-check.sh` green.
</verification>

<success_criteria>
TAGR-01, TAGR-02 and TAGR-06 are met: one tagger and one front end chosen on numbers from this
library's own content, against thresholds committed before the evidence and none of them moved;
criterion 4 answered with a concrete mechanism for Mastermix, DMC and Music Factory content; the
ergonomics verdict in the operator's own terms; and the estate left exactly as this phase found it,
plus a decision Phase 4 can act on.
</success_criteria>

<output>
Create `.planning/phases/03-tagger-spike/03-11-SUMMARY.md` when done.
</output>
