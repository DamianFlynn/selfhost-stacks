---
phase: 03-tagger-spike
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - scripts/spike03-variant-survey.sh
  - .planning/phases/03-tagger-spike/03-SAMPLE.md
autonomous: true
requirements: [TAGR-01]

must_haves:
  truths:
    - "A folder-to-stratum map exists for every audio-bearing DJ folder, derived from Phase 1's existing ffprobe fence rather than from new I/O against tank"
    - "Every stratum carries its prevalence across the full committed 143-folder backlog, not only its count inside the drawn sample, so plan 03-07 can weight its per-stratum rates"
    - "The draw is deterministic and repeatable: the sort key that selects N folders from a larger stratum is stated in the artefact"
    - "The ~20-folder sample is drawn across dj-mixes and unsorted as one de-duplicated population, and includes DMC folders"
    - "No two sampled folders share the same audio, proven by audio_md5 against the QUAL-01 snapshot"
    - "The sample exists as reflinked copies under tank/downloads with a ZFS snapshot behind them, and the originals are untouched"
    - "A before-state tag snapshot of the scratch copies exists and can be diffed"
  artifacts:
    - path: "scripts/spike03-variant-survey.sh"
      provides: "Folder-to-stratum derivation over the Phase 1 ffprobe fence"
      min_lines: 100
    - path: ".planning/phases/03-tagger-spike/03-SAMPLE.md"
      provides: "The committed variant survey, the per-stratum backlog weights, the drawn sample with strata and its sort key, and the corrections it supersedes"
      contains: "audio_md5"
  key_links:
    - from: "scripts/spike03-variant-survey.sh"
      to: "/mnt/fast/safety/music-pre-project/dj-mixes-ffprobe/"
      via: "read-only pass over the Phase 1 SAFE-03 capture"
      pattern: "dj-mixes-ffprobe"
    - from: ".planning/phases/03-tagger-spike/03-SAMPLE.md § Stratum prevalence"
      to: "03-DECISION.md § The estimator T1 is tested against"
      via: "the per-stratum backlog weights that plan 03-07 multiplies its per-stratum rates by"
      pattern: "prevalence|weight"
    - from: "the scratch tree"
      to: "tank/downloads@spike-03-t0"
      via: "zfs snapshot taken from atlantis before the first write into normalised/"
      pattern: "spike-03-t0"
---

<objective>
Establish the sample this phase measures on: formalise the variant survey, draw the widened
de-duplicated ~20-folder sample, and materialise it as reflinked copies behind a ZFS snapshot.

Three corrections land here and must be carried, not quietly applied. The DJ population is **60
audio-bearing folders, not 84** — 24 are empty shells including the "45 GB 1-115 set" that does not
exist on disk. `dj-mixes` is a **byte-for-byte duplicate subset of `unsorted`** (separate inodes,
link count 1), so a naive draw over both trees samples the same audio twice. And `dj-mixes` contains
**no DMC content at all**, while criterion 4 names DMC explicitly — which is why **OD-3** widens the
population to `dj-mixes` ∪ `unsorted` as ONE population, decided before any evidence exists.

This plan also produces the **weights** that plan 03-07's headline rate is computed with. The
estimator was fixed in plan 03-02 before any of this ran: T1 is tested against a per-stratum rate
rolled up by each stratum's prevalence across the committed 143-folder backlog, never against the
unweighted sample average. The sample is stratified and deliberately never random — it exists to
surface failure modes — so its unweighted average is skewed by construction and is not a population
estimate. This plan's job is to measure the prevalences that turn it into one.

**This plan depends on 03-02 and cannot run before it.** The sample must not be drawn before the
thresholds, the estimator and the OD-3 population that govern it are committed and pushed; drawing
first would make every one of them retroactive and break criterion 5's chain of custody outright.

Purpose: the Discogs number this phase produces is only as good as the sample it is measured on,
and D-06's original population cannot answer criterion 4.
Output: `scripts/spike03-variant-survey.sh`, a committed `03-SAMPLE.md`, and a scratch tree at
`/mnt/tank/downloads/spike-03` with `raw/` populated and a `@spike-03-t0` snapshot behind it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tagger-spike/03-CONTEXT.md
@.planning/phases/03-tagger-spike/03-RESEARCH.md
@.planning/phases/03-tagger-spike/03-PATTERNS.md
@.planning/phases/03-tagger-spike/03-DECISION.md
@scripts/diff-music-tags.sh
@scripts/snapshot-music-tags.sh
@scripts/check-music-freeze.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/spike03-variant-survey.sh and emit the folder-to-stratum map</name>
  <files>scripts/spike03-variant-survey.sh</files>
  <read_first>
    - `scripts/diff-music-tags.sh` lines 20-160 — the `FLATTEN_JQ` streaming flatten over ffprobe
      JSON (normalise key case, merge `format.tags` with audio-stream tags, emit one compact record).
      This is the exact operation the survey needs and it is already proven. Also read lines 26-32
      for the exit-code contract and 103-112 for the scratch-beside-the-input pattern.
    - `scripts/snapshot-music-tags.sh` lines 1-72, 102-163 and 350-400 — the header block order,
      the tool/precondition gate that names missing tools and exits 1, and the summary-then-exit
      convention.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Headline finding 4* (the V1…V6 variant
      table with its measured counts) and § *Sample selection and normalisation* → *Where the
      variant inventory comes from* — the survey **formalises** a census that already exists; it
      does not discover it.
    - `.planning/phases/03-tagger-spike/03-PATTERNS.md` § *Variant survey artefact* — in particular
      the shape warning: the fence holds **raw ffprobe JSON, one file per audio file, in a mirrored
      tree**, with no `audio_md5` envelope, whereas `diff-music-tags.sh` consumes NDJSON *with* that
      envelope. The two formats are not interchangeable.
  </read_first>
  <action>
Create `scripts/spike03-variant-survey.sh`, host-resident on LXC 100, run from `/mnt/fast/stacks`
after `git pull --ff-only`. `#!/usr/bin/env bash`, `set -euo pipefail`, the repo's fixed header
section order, colour helpers and run banner per `check-music-freeze.sh:83-88,144-156`.

**READ-ONLY BY CONTRACT.** Its only input is the Phase 1 SAFE-03 capture at
`FENCE="${FENCE:-/mnt/fast/safety/music-pre-project/dj-mixes-ffprobe}"` — one raw ffprobe JSON per
audio file in a mirrored tree. It performs **no new I/O against `tank`** and contains no `mv`, `rm`,
`chown` or `chmod` against any scan root. State that in the header.

Never use `find -xdev` anywhere: `/mnt/fast/appdata/*` are 39 separate ZFS datasets and a sweep from
`/mnt/fast` silently skips them and exits 0. Enumerate `$FENCE` by explicit path.

For each folder under `$FENCE`, aggregate across its files and classify into exactly one stratum,
using these rules in order (first match wins), matching 03-RESEARCH.md finding 4:
  - **V3** — `album` missing or empty in the majority of the folder's files. This is the only
    stratum whose un-normalised Discogs rate is provably 0%, because the beets Discogs plugin
    issues no query when artist and album are both empty.
  - **V4** — `artist` missing or empty in the majority of files (and `album` present).
  - **V2** — `artist` is one of `VA`, `Various`, `Various Artists` (case-insensitive). These flip
    `va_likely` to true via beets' `VA_ARTISTS` tuple, making the query `"VA <album>"` rather than
    `<album>`.
  - **V5** — `album` present but noisy: matches any of ` WEB`, ` - Disc `, `Vol.`, a leading
    `Mastermix - `, a trailing space, or an underscore.
  - **V1** — `artist` is exactly `Mastermix` and `album` is a clean issue string. The best case.
  - **V6** — real per-track artists (more than one distinct non-placeholder `artist` in the folder).

Emit, per folder, a compact JSON record to an NDJSON file at
`OUT="${OUT:-/mnt/fast/safety/music-pre-project/spike-03-variant-survey.ndjson}"` carrying:
`folder`, `tree` (`dj-mixes` or `unsorted`), `n_files`, `stratum`, `artist_values` (histogram),
`album_sample`, `has_album_count`, `has_artist_count`, `fmt_histogram` (extension counts — record
format per folder, because **134 of 764 files are WAV** and WAV tagging is materially weaker in
every tool here; a WAV-dominant stratum is a finding, not noise), `has_disc_tag_count`,
`tkey_count`, `energylevel_count`, and `scan_bearing` (true if the folder has a `.covers`
subdirectory or a sibling scan — D-06 excludes the 27 scan-bearing folders from the draw). Assemble
every record with `jq`, never by string concatenation: album titles in this collection contain `:`,
quotes and trailing spaces.

Write the NDJSON to the fence, **not** into the repo — 03-PATTERNS.md § *Blocking Findings* 3
records that this repo has zero committed non-Markdown data artefacts, and the machine-readable
output belongs beside Phase 1's other captures.

Summary block prints per-stratum folder and file counts, the per-stratum backlog prevalence and
weight, the WAV-file total, and the scan-bearing count. Exit 1 if any fence folder classifies into no
stratum (that means the rules do not cover the
data and the survey is not trustworthy); exit 2 on usage or a missing fence; exit 0 otherwise.

Extend the same survey to the `unsorted` tree, which OD-3 brings into the population. `unsorted` has
no ffprobe fence, so for those folders run `ffprobe` read-only over the audio files directly and
apply the identical classifier. Report which route produced each record via the `tree` field, so a
reader knows which folders came from the fence and which from a live read.

**Then extend it once more, to `complete/nzb/music`'s 23 folders, so the whole committed 143-folder
backlog is classified.** This is what makes plan 03-07's weighted roll-up possible: the estimator
committed in 03-02 multiplies each stratum's measured rate by that stratum's prevalence **across the
backlog**, and a weight cannot be computed from a tree that was never classified. Use the identical
classifier and the identical read-only live-`ffprobe` route already specified for `unsorted`. Record
the cost honestly in the summary: this is 23 additional folders of **local** `ffprobe` reads. It
issues **zero** Discogs requests, adds **zero** folders to the draw, and touches nothing — the
weighted estimator is arithmetic over data the phase already had to collect, not new measurement of
the sample.

Emit a second, aggregate NDJSON record set — or a trailing summary object, clearly marked — carrying
**per-stratum backlog prevalence**: for each stratum, `backlog_folders` (the count of folders in
`unsorted` ∪ `music`, the committed 143-folder denominator, that classify into it) and
`backlog_weight` (`backlog_folders / 143`, to four decimal places). The weights must sum to `1.0000`
within rounding, and `sum(backlog_folders)` must equal exactly `143`. `dj-mixes` folders are
**excluded** from the weight denominator because `dj-mixes` is a byte-for-byte duplicate subset of
`unsorted` and counting it would double-weight the same audio; state that in the output.
  </action>
  <verify>
    <automated>bash -n scripts/spike03-variant-survey.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/spike03-variant-survey.sh` exits 0.
    - Comment-stripped grep proves the read-only contract:
      `grep -v "^\s*#" scripts/spike03-variant-survey.sh | grep -cE "\brm |\bmv |\bchown |\bchmod "` returns `0`.
    - Comment-stripped grep proves the dataset hazard is avoided:
      `grep -v "^\s*#" scripts/spike03-variant-survey.sh | grep -c -- "-xdev"` returns `0`.
    - Run on LXC 100, the survey classifies **60** `dj-mixes` folders covering **764** audio files,
      matching Phase 1's fence exactly, and exits 0 with no unclassified folder.
    - `jq -s '[.[]|select(.tree=="dj-mixes")]|length'` over the NDJSON returns `60`;
      `jq -s '[.[]|select(.tree=="dj-mixes")|.n_files]|add'` returns `764`.
    - `jq -s '[.[]|.fmt_histogram.wav // 0]|add'` over the `dj-mixes` records returns `134`.
    - `jq -s 'group_by(.stratum)|map({s:.[0].stratum,n:length})'` returns records for at least
      V1, V2, V3, V5 and V6 — a stratum with zero folders means the classifier missed a variant.
    - At least one `unsorted` record has a `folder` matching `DMC`, proving the widened tree was
      actually surveyed.
    - The whole committed backlog is classified: `jq -s '[.[]|select(.tree=="unsorted" or .tree=="music")]|length'`
      over the NDJSON returns **143**, and `jq -s '[.[]|select(.tree=="music")]|length'` returns `23`.
    - The per-stratum weight record exists, `sum(backlog_folders)` equals exactly `143`, and
      `sum(backlog_weight)` equals `1.0000` within rounding tolerance. A weight set that does not sum
      to 1 means a folder was classified into no stratum or into two, and the weighted roll-up in
      plan 03-07 would be silently wrong.
  </acceptance_criteria>
  <done>Every audio-bearing DJ folder in both trees carries exactly one stratum, the counts reproduce Phase 1's fence, and the machine-readable survey sits beside the other Phase 1 captures.</done>
</task>

<task type="auto">
  <name>Task 2: Draw the widened de-duplicated sample and commit 03-SAMPLE.md</name>
  <files>.planning/phases/03-tagger-spike/03-SAMPLE.md</files>
  <read_first>
    - The NDJSON produced by Task 1 at
      `/mnt/fast/safety/music-pre-project/spike-03-variant-survey.ndjson`.
    - `.planning/phases/03-tagger-spike/03-DECISION.md` § *OD-3* — the widening was committed
      before evidence and this task executes it; the draw must match the population stated there.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Stratification (D-06)* — the suggested
      draw (V1×4, V2×5, V3×4, V5×4, V6×3) and the exclusion of the 27 scan-bearing folders; and
      § *Headline finding 5* for the DMC folder names in `unsorted`
      (`VA-DMC-Commercial.Collection.498/499`, `VA-DMC.Commercial.Collection.305/306/310/312/327/332`,
      `VA-DMC-Essential.Hits.233/234/235`).
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Assumptions Log* A5 — the DMC folders'
      shape (flat, audio at top level) must be **verified before drawing**, not assumed.
    - `stacks/selfhosted/arrs/beets.md` lines 44-58 — the house style for a committed census table
      followed immediately by the retraction of the superseded figure.
  </read_first>
  <action>
Draw **20 folders from `dj-mixes` plus 4 DMC folders from `unsorted`**, as ONE de-duplicated
population per OD-3. Stratified, never random: with six conventions in play, a random draw can miss
a variant entirely and would not reveal that it had.

**The draw must be deterministic and repeatable.** "Stratified, never random" says which strata are
drawn from; it does not say *which* N folders come out of a stratum larger than N, and an unstable
`find` directory-read order would make two runs of the same script produce two different samples.
Fix it: within each stratum, sort the eligible folders with `LC_ALL=C sort` on the **full folder
path** (byte-ordered, locale-independent — a locale-dependent sort is not reproducible across hosts),
then take the first N. Use `audio_md5` of the folder's first sorted file as the documented
tie-breaker if two paths ever collide. State the sort key, the tie-breaker and the `LC_ALL=C` in
`03-SAMPLE.md` explicitly, so a reader can re-derive the identical 24 folders from the survey NDJSON
without re-running anything.

Target allocation, adjusted by the actual stratum sizes the survey returns: V1×4, V2×5, V3×4, V5×4,
V6×3 from `dj-mixes`, plus 4 DMC folders from `unsorted` classified into whichever strata they fall.
Exclude every folder whose `scan_bearing` is true (D-06: the scan-bearing 27 may skew toward
better-preserved rips and bias the rate upward). If a stratum has fewer folders than its target,
reallocate to the next-largest stratum and record the reallocation and its reason.

**De-duplication is mandatory and must be proven, not assumed.** `dj-mixes` is a byte-for-byte
duplicate subset of `unsorted`. Join the drawn folders against the QUAL-01 NDJSON at
`/mnt/fast/safety/music-pre-project/tags/pre-project.ndjson.gz` (9,736 records, `audio_md5`-keyed —
the encoded bitstream, so filenames differing between the trees cannot fool it) and assert that the
set of `audio_md5` values in the `dj-mixes` draw and the set in the `unsorted` DMC draw are
**disjoint**. Filenames differ between the trees; the bitstream does not. If any overlap exists,
drop the `unsorted` folder and draw another, recording the swap.

Verify the DMC folders' shape before committing to them (A5): each must be flat, with audio files at
the top level, structurally comparable to a `dj-mixes` folder. Record `find <folder> -mindepth 2
-type d` output per DMC candidate in the artefact.

Create `.planning/phases/03-tagger-spike/03-SAMPLE.md` with:
  - A leading blockquote naming the population: 60 audio-bearing `dj-mixes` folders ∪ the DMC
    folders in `unsorted`, widened per OD-3 **before any evidence was collected**.
  - `## The census` — a Markdown table of stratum, folder count, file count, WAV count and
    scan-bearing count, in the `beets.md:44-52` style.
  - `## Corrections this survey supersedes` — in the `beets.md:55-58` house style, each stated as a
    retraction rather than a quiet replacement: **60 audio-bearing folders, not 84** (24 empty
    shells, including `VA-Now_That.s_What_I_Call_Music__1-115_2023` — the "45 GB 1-115 set" that
    PROJECT.md and CLAUDE.md tell a later phase to split per volume and which is not on disk);
    **`dj-mixes` is a duplicate subset of `unsorted`**, separate inodes, link count 1; and
    **there is no `artist`/`album` swap** — `album` is a correct-ish issue string in 657 of 764
    files while `artist` is a placeholder, and 107 files have no `album` at all.
  - `## Stratum prevalence across the 143-folder backlog` — the weight table plan 03-07 multiplies
    by. One row per stratum: `stratum`, `backlog_folders` (out of `unsorted` 120 + `music` 23 = 143),
    `backlog_weight`, `sampled_folders`, and `estimable` (`yes` if `sampled_folders > 0`, `no`
    otherwise). Below it: the sums, which must be exactly `143` and `1.0000`; a sentence recording
    that `dj-mixes` is excluded from the weight denominator because it duplicates `unsorted`
    byte-for-byte; and a pointer to `03-DECISION.md § The estimator T1 is tested against`, which
    committed this method before any of these numbers existed.
    **Any stratum with `backlog_weight > 0` and `sampled_folders == 0` is a hole in the estimator and
    must be named as one**, with its weight stated — plan 03-07 has to bound its contribution rather
    than silently drop it.
  - `## The draw` — one row per sampled folder: `folder`, `tree`, `stratum`, `n_files`,
    `fmt_histogram`, `has_album_count`, `has_artist_count`, `scan_bearing`. Head the table with the
    sort key (`LC_ALL=C` byte-ordered full folder path), the tie-breaker, and the per-stratum target
    counts, so the draw is re-derivable.
  - `## De-duplication proof` — the `audio_md5` disjointness assertion with the counts on both
    sides and the count of shared keys (must be `0`), and the two instruments that produced them.
  - `## The bucket-A and hard-shape candidates for plan 03-10` — name the concrete albums for the
    D-10 slots so plan 03-10 does not have to rediscover them: two clean mainstream from
    `nzb/music` (research observed `Ed Sheeran - ÷ (2017) FLAC` and `Lady Gaga - The Fame (2008)
    FLAC`), one various-artist compilation from `nzb/unsorted` (a `Now That's What I Call Music`
    volume), and one flat multi-disc set (research observed `Def Leppard - CD Collection Volume 1 -
    7CD`; **verify it is genuinely flat before naming it**, and record the `find -mindepth 2 -type d`
    output either way). The fifth slot, the DJ folder Discogs will not match, is deliberately left
    `TBD — plan 03-07` because D-10 requires it to be chosen after criterion 1 runs.

The Markdown artefact is committed; the NDJSON stays at the fence. Do not place either under any
`data/` directory — `.gitignore` line `**/data/*` matches at any depth and would swallow it silently.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-tagger-spike/03-SAMPLE.md &amp;&amp; git ls-files --error-unmatch .planning/phases/03-tagger-spike/03-SAMPLE.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-SAMPLE.md` is tracked by git (`git ls-files --error-unmatch` succeeds), proving
      `.gitignore` did not swallow it.
    - The `## The draw` table has **24** data rows: 20 with `tree = dj-mixes` and 4 with
      `tree = unsorted`.
    - Every drawn row has `scan_bearing = false`.
    - At least four distinct `stratum` values appear among the `dj-mixes` rows.
    - At least 3 of the 4 `unsorted` rows have a `folder` value containing `DMC`.
    - `## De-duplication proof` states a shared-`audio_md5` count of exactly `0`, and names both
      instruments that produced it (the QUAL-01 NDJSON join and a second independent reading).
    - `## Corrections this survey supersedes` contains the literals `60`, `84`, `1-115` and `657`.
    - `## Stratum prevalence across the 143-folder backlog` exists; its `backlog_folders` column sums
      to exactly `143` and its `backlog_weight` column sums to `1.0000` within rounding; every
      stratum row carries an `estimable` verdict; and any stratum with weight but no sampled folder
      is named explicitly as a hole.
    - The sort key is stated in `## The draw`'s header and includes the literal `LC_ALL=C`. Re-running
      the draw logic against the same survey NDJSON reproduces the identical 24 folders in the
      identical order — assert by running it twice and diffing the two folder lists to empty.
    - The `## The bucket-A and hard-shape candidates` section names four concrete album paths and
      leaves the fifth slot as `TBD — plan 03-07`.
  </acceptance_criteria>
  <done>A committed, stratified, de-duplicated 24-folder sample spanning both trees and including DMC, with the three superseded figures retracted in place.</done>
</task>

<task type="auto">
  <name>Task 3: Materialise the scratch tree with reflinked copies, snapshot it, and take the before-state</name>
  <files>.planning/phases/03-tagger-spike/03-SAMPLE.md</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Scratch mechanics (D-07)* — the exact
      `cp -a --reflink=always` shape, why `always` and not `auto`, and the two fallbacks behind the
      reflink (`unsorted` itself and `tank/downloads@pre-project`).
    - `scripts/check-music-freeze.sh` lines 123-131 — the `zfs_query()` delegation function: try
      local first, then `ssh -o BatchMode=yes -o ConnectTimeout=5 root@172.16.1.158`, and
      **report the route taken**. An unreachable host is UNKNOWN, never a skip and never a pass.
    - `scripts/snapshot-music-tags.sh` lines 102-127 — its argument surface, in particular `--roots`,
      and its output/ledger paths.
    - `.planning/phases/03-tagger-spike/03-PATTERNS.md` § *Delegating to the Proxmox host*.
  </read_first>
  <action>
On LXC 100, create the scratch roots. Two of them, deliberately on different pools:
  - `SCRATCH_TANK=/mnt/tank/downloads/spike-03` with subdirectories `raw/`, `normalised/`,
    `normalised-metadata/`, `wrtag-src/`, `bf-inbox/`, `bf-clean/`. It is a **sibling of
    `complete/`**, therefore the same ZFS dataset as the sources, which is what makes
    `cp --reflink` work.
  - `SCRATCH_FAST=/mnt/fast/spike-03` with `out/`, `beets-config/`, `bf-config/`, `wrtag-lib/`.
    Outputs, logs and container configs live here so they do not consume `/` on LXC 100 and never
    touch the system temp directory, which is tmpfs backed by host RAM and has previously taken the
    whole 28 GB box down.

Copy each of the 24 sampled folders from `/mnt/tank/downloads/complete/nzb/dj-mixes` or
`/mnt/tank/downloads/complete/nzb/unsorted` into `$SCRATCH_TANK/raw/` with
`cp -a --reflink=always`. **`always`, never `auto`** — with `auto`, a cross-dataset mistake degrades
silently to a full copy and the run learns nothing; fail loud instead. Do not use `rsync`: it is not
installed on LXC 100 at all, and `rsync -a` fails on `tank` regardless because of
`aclmode=restricted`. Do not attempt `chmod` or `chown` on anything under `tank` — both fail from
LXC 100 for two different reasons (`aclmode=restricted` and the sparse idmap) and everything there
is already `0777`, so read access is not in question.

Then reflink `$SCRATCH_TANK/raw/<folder>` to `$SCRATCH_TANK/normalised/<folder>` for all 24 —
**D-07: normalisation runs on COPIES, never on originals**, and the copy-of-a-copy keeps `raw/` as
a clean re-run source independent of `complete/`.

Take `zfs snapshot tank/downloads@spike-03-t0` **from atlantis** — `zfs` does not and cannot exist
inside LXC 100, which is unprivileged. Use the `zfs_query()` shape: try local, fall back to
`ssh -o BatchMode=yes -o ConnectTimeout=5 root@172.16.1.158`, and **print which route produced the
result**. If atlantis is unreachable, the snapshot state is UNKNOWN and this task fails — do not
proceed without it, because that snapshot is `normalise-dj-tags.py`'s only rollback.

Take the before-state tag capture over the copies:
`bash scripts/snapshot-music-tags.sh --roots /mnt/tank/downloads/spike-03/normalised` writing to
`$SCRATCH_FAST/out/spike03-before.ndjson.gz`. This is the BEFORE side of plan 03-05's field-loss gate.

Append to `03-SAMPLE.md` a `## Scratch tree` section recording: both scratch roots; the reflink
command used verbatim; the `du -sh` and `df` figures for `tank/downloads` before and after (noting
that ZFS frees space asynchronously and `zfs list` can lag a large delete by ~20 s); the snapshot
name and **the route it was taken over**; the before-snapshot path and its record count; and the
`stat -c '%i'` inode of one source folder and its `raw/` copy, showing they differ (a reflink is a
separate inode sharing blocks, not a hard link).
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.158 "zfs list -t snapshot -o name -H tank/downloads | grep -c spike-03-t0"</automated>
  </verify>
  <acceptance_criteria>
    - `ssh root@172.16.1.159 "ls -1 /mnt/tank/downloads/spike-03/raw | wc -l"` returns `24`, and the
      same for `/mnt/tank/downloads/spike-03/normalised`.
    - `ssh root@172.16.1.158 "zfs list -t snapshot tank/downloads@spike-03-t0"` exits 0, and the
      summary records that the query was delegated over ssh because `zfs` is absent on LXC 100.
    - Per-folder file counts under `raw/` equal the `n_files` recorded for that folder in
      `03-SAMPLE.md`'s draw table — no folder copied short.
    - The originals are untouched: `find /mnt/tank/downloads/complete/nzb/dj-mixes -newer
      /mnt/tank/downloads/spike-03 -type f` returns no lines.
    - `$SCRATCH_FAST/out/spike03-before.ndjson.gz` exists and
      `zcat ... | wc -l` equals the total `n_files` across all 24 drawn folders, with zero entries
      in the snapshot's `.failed` ledger.
    - Nothing was written to the system temp directory:
      `ssh root@172.16.1.159 "ls -1 /tmp | grep -c spike-03"` returns `0`.
    - `bash scripts/check-music-freeze.sh` exits 0 with `tagger-class writers: 0` and
      `declared rw reaching Music: 0` — nothing in this plan goes near `/mnt/tank/media/Music`.
  </acceptance_criteria>
  <done>24 reflinked folders in `raw/` and `normalised/`, a `@spike-03-t0` snapshot taken from atlantis with its route reported, a before-state capture with zero failures, and the originals provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LXC 100 (unprivileged) → atlantis (Proxmox host) | `zfs` operations cross this boundary over ssh as root; an unreachable host must fail closed |
| scratch tree → the real backlog under `complete/nzb/` | Copies are the mutation target; originals must never be written |
| survey output → the repo (public) | Committed artefacts are permanent and recoverable via `git log -S` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | The original backlog under `complete/nzb/` | mitigate | D-07: every mutation target is a `cp -a --reflink=always` copy under `/mnt/tank/downloads/spike-03`; asserted with `find <originals> -newer <scratch>` returning zero lines. Two independent fallbacks exist behind the reflink (`unsorted` itself and `tank/downloads@pre-project`) |
| T-03-11 | Tampering | `/mnt/tank/media/Music` | mitigate | This plan mounts nothing and touches no path under `media/`; asserted by `check-music-freeze.sh` exit 0 with `tagger-class writers: 0` and `declared rw reaching Music: 0` (D-20) |
| T-03-12 | Denial of service | LXC 100 host RAM | mitigate | `/tmp` on LXC 100 is tmpfs backed by host RAM and a large spill there has previously killed sshd on both host and LXC. All outputs go to `/mnt/fast/spike-03/out`; asserted by a `/tmp` grep returning 0 |
| T-03-13b | Repudiation | A stratified coverage sample narrated as a population estimate | mitigate | The survey classifies the **whole** committed 143-folder backlog and emits per-stratum prevalence, so plan 03-07 can weight its per-stratum rates rather than averaging a deliberately skewed sample. The estimator was committed in plan 03-02, which this plan depends on and which is pushed to `origin`, so the weighting cannot read as post-hoc. Strata with weight but no sampled folder are named as holes rather than dropped |
| T-03-13c | Repudiation | A draw that cannot be reproduced | mitigate | Within each stratum, eligible folders are `LC_ALL=C`-sorted by full path before the top N are taken, with `audio_md5` as the documented tie-breaker; the key is recorded in `03-SAMPLE.md` and asserted by running the draw twice and diffing to empty. An unstable `find` order would otherwise make "stratified, never random" unrepeatable in practice |
| T-03-13 | Repudiation | The measured sample | mitigate | De-duplication is proven by an `audio_md5` join against the QUAL-01 NDJSON with a required shared-key count of `0`, not asserted from filenames — filenames differ between the trees, the encoded bitstream does not |
| T-03-14 | Information disclosure | Committed survey artefact | mitigate | `03-SAMPLE.md` carries folder names, counts and strata only. No credential is read or written by this plan; `DISCOGS_USER_TOKEN` is not touched here at all |
| T-03-15 | Denial of service | ZFS free space on `tank` | accept | `tank` has 9.00 T available and the whole `dj-mixes` tree is 25 G; reflinked clones consume near-zero additional space within the dataset. The scratch tree is deleted in plan 03-11 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. The phase-wide audit is in 03-RESEARCH.md § *Package Legitimacy Audit* (3 OK / 0 SUS / 0 SLOP) |
</threat_model>

<verification>
- 24 folders present in both `raw/` and `normalised/`, per-folder file counts matching the draw table.
- `tank/downloads@spike-03-t0` exists, queried from atlantis, with the route recorded.
- Shared `audio_md5` count between the two trees' draws is `0`.
- Originals untouched: `find` by `-newer` returns nothing.
- `bash scripts/check-music-freeze.sh` exits 0.
- `zfs diff tank/media/Music@pre-project` run from atlantis returns clean.
</verification>

<success_criteria>
The phase has a defensible sample: every DJ folder classified, the ~20 widened to 24 across both
trees with DMC included per OD-3, de-duplication proven on `audio_md5`, and the whole sample sitting
as reflinked copies behind a ZFS snapshot with a diffable before-state.
</success_criteria>

<output>
Create `.planning/phases/03-tagger-spike/03-03-SUMMARY.md` when done.
</output>
