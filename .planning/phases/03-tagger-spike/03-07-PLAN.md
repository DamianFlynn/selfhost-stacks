---
phase: 03-tagger-spike
plan: 07
type: execute
wave: 5
depends_on: ["03-02", "03-05", "03-06"]
files_modified:
  - .planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md
  - .planning/phases/03-tagger-spike/03-DECISION.md
autonomous: true
requirements: [TAGR-01]

must_haves:
  truths:
    - "A strict Discogs match rate exists over the normalised sample with the un-normalised rate recorded beside it"
    - "The loose candidate-appears-at-all rate and the gap to the strict rate are both reported as first-class numbers"
    - "The strict scoring is run at index_tracks no and yes, and the setting that produced the headline number is stated"
    - "T1 and T2 are each marked tested with what they returned, and neither threshold was moved"
    - "Request count and wall-clock are extrapolated to the pre-committed 143-folder denominator, with the 429 count recorded beside them"
  artifacts:
    - path: ".planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md"
      provides: "Criterion 1 and 2 evidence: four-cell rates, the strict/loose gap, timing, requests, 429s, and the T1/T2 verdicts"
      contains: "index_tracks"
  key_links:
    - from: "03-DISCOGS-EVIDENCE.md"
      to: "03-DECISION.md T1/T2 rows"
      via: "tested/returned values written back into the pre-committed threshold table"
      pattern: "T1|T2"
    - from: "the four-cell run"
      to: "/mnt/tank/downloads/spike-03/raw and /normalised"
      via: "the same probe over the un-normalised and normalised arms"
      pattern: "spike-03"
---

<objective>
Produce criterion 1's and criterion 2's numbers, each with two independent instruments, and write
the T1 and T2 verdicts back into the pre-committed threshold table.

Criterion 1 requires a Discogs match rate over the normalised sample **with the un-normalised number
recorded beside it**, because beets builds its Discogs query from the existing `artist` and `album`
tags and a rate measured on raw tags under-reports. Criterion 2 requires the run timed and
extrapolated against the authenticated rate ceiling into a stated hours-for-the-backlog figure.

Two rules govern everything here. **D-05:** a "match" means a Discogs candidate appears **and** its
track count agrees with the folder — a confident wrong match is the worst possible outcome on this
content, and stub entries of 5–10 tracks against 15–20 track releases are exactly how that happens.
T1's 40% applies to that strict figure. **The paired-instrument rule:** every number gets two
independent readings before it is written down, and if the two disagree neither is recorded until
the disagreement is explained. Phase 1 produced six passes it had not earned, every one from partial
data stated as settled.

Purpose: this is the phase's headline number and the input to axis one.
Output: `03-DISCOGS-EVIDENCE.md`, and T1/T2 filled in `03-DECISION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tagger-spike/03-DECISION.md
@.planning/phases/03-tagger-spike/03-SAMPLE.md
@.planning/phases/03-tagger-spike/03-NORMALISATION.md
@.planning/phases/03-tagger-spike/03-PROBE-SMOKE.md
@.planning/phases/03-tagger-spike/03-RESEARCH.md
@.planning/phases/03-tagger-spike/03-CONTEXT.md
@scripts/spike03-discogs-probe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the four-cell probe matrix at both index_tracks settings and score loose/strict</name>
  <files>.planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Criterion 1* — the four-cell paired-run
      table (un-normalised × normalised, MB-only × MB+Discogs), the loose and strict scoring
      definitions, the `index_tracks` interference note (Mastermix/DMC releases commonly use index
      tracks and headings, which changes the candidate's reported track count), the rate-denominator
      rule (per folder as the headline, cross-checked per distinct `audio_md5`), and the
      § *Code Examples* → *Scoring D-05 strictly from the NDJSON* `jq` expressions.
    - `.planning/phases/03-tagger-spike/03-CONTEXT.md` D-05 and § *Specific Ideas* — the strict/loose
      gap is a deliverable in its own right, and the denominator must be stated because the 828
      duplicate groups mean a naive per-folder rate can double-count the same audio.
    - `.planning/phases/03-tagger-spike/03-SAMPLE.md` — the 24 drawn folders with their strata, trees
      and `n_files`, and the de-duplication proof.
    - `.planning/phases/03-tagger-spike/03-PROBE-SMOKE.md` — the confirmed A1 verdict and the
      observed `x-discogs-ratelimit` value.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Common Pitfalls* 4 — never run two probes
      concurrently against the same 60 req/min budget.
  </read_first>
  <action>
Run the probe over both arms — `/mnt/tank/downloads/spike-03/raw` (un-normalised) and
`/mnt/tank/downloads/spike-03/normalised` — across **four cells**, each into its own NDJSON and
stderr file under `/mnt/fast/spike-03/out/`:

  - `raw` × `plugins: musicbrainz` — the baseline
  - `normalised` × `plugins: musicbrainz` — isolates the normalisation effect independent of source
  - `raw` × `plugins: musicbrainz discogs` — **the "raw tags" figure criterion 1 demands**
  - `normalised` × `plugins: musicbrainz discogs` — **the headline figure T1 applies to**

The MB-only cells cost nothing extra (same probe, different `plugins:` line) and answer whether the
normalisation helps at all independent of source, which the Discogs number alone cannot.

**Run the two Discogs cells serially, never concurrently.** They share one 60 req/min budget and a
concurrent pair would 429 each other and corrupt criterion 2's timing (Pitfall 4). Use `setsid` +
`nohup` to detach a long run — `tmux` and `screen` are both absent on LXC 100.

Then repeat the **strict scoring** of the two Discogs cells at `discogs.index_tracks: yes` as well as
`no`. A track-count rule is meaningless without stating how tracks were counted, and Mastermix/DMC
releases on Discogs commonly use index tracks and headings, which changes the candidate's reported
track count. Report both, and **state explicitly which setting produced the headline number**.

Score with `jq` over the NDJSON, per the D-05 definitions:
  - **Loose rate** = folders where any candidate has `source == "Discogs"`.
  - **Strict rate** = folders where some candidate has `source == "Discogs"` **and**
    `n_tracks == n_files` **and** `extra_items == 0` **and** `extra_tracks == 0`.
  - **The gap** between them, as a count and a percentage — a deliverable in its own right, not a
    footnote. It measures how much manual verification the winner implies.

**Denominators, both reported.** The headline is **per folder**, because that is the unit of work
and the unit T1 was written against. Cross-check **per distinct `audio_md5`** using the QUAL-01
NDJSON at `/mnt/fast/safety/music-pre-project/tags/pre-project.ndjson.gz`. State both, and state
that the sample was drawn from one de-duplicated population precisely so a naive union over
`dj-mixes` and `unsorted` could not double-count the same audio.

**Break the rate out three ways** as well as reporting the headline: by stratum (V1…V6), by tree
(`dj-mixes` Mastermix versus `unsorted` DMC — the DMC stratum is reported **separately**, per OD-3's
widened population, so criterion 4 can speak to DMC concretely), and by dominant file format
(**134 of 764 `dj-mixes` files are WAV** and WAV tagging is materially weaker in every tool here; if
a stratum is WAV-dominant, that is a finding, not noise — research Open Question 5).

**Second instrument, mandatory.** Run `beet -c <spike config> -l <throwaway .blb> import -t` (timid)
interactively on **3 folders drawn from different strata**, transcribe the on-screen candidate list
and its track counts by hand, and compare to the probe's NDJSON for those three. Timid mode
suppresses the strong-MBID early return in `tag_album`, so it sees the same candidate set. **If they
disagree, no rate is recorded** until the disagreement is explained — the likely cause is that
plugin loading differs between the two paths, which would invalidate the whole measurement.

Generate the `beet import -t` runtime config with `umask 077` under
`/mnt/fast/spike-03/beets-config/`, containing the Discogs `user_token` from the environment. It is
**never committed** and is deleted in plan 03-11. The repo is public and one prior credential
exposure there is still recoverable via `git log -S`.

Begin `.planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md` with:
  - `## The four cells` — a 2×2 table of loose and strict rates, per folder, with the `index_tracks`
    setting and `search_limit` in force stated in the header.
  - `## The strict/loose gap` — count and percentage, with a sentence on what it implies for manual
    verification load.
  - `## By stratum`, `## By tree (Mastermix vs DMC)`, `## By dominant format` — three breakdown
    tables. The DMC rate is reported separately and is **not** merged into the T1 figure.
  - `## Denominator statement` — per folder as the headline, per distinct `audio_md5` as the
    cross-check, both numbers given.
  - `## Second instrument` — the three hand-transcribed folders, the probe's records for the same
    three, and the agreement verdict.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.159 "for f in /mnt/fast/spike-03/out/cell-*.ndjson; do printf '%s ' \"$f\"; jq -s 'group_by(.folder)|length' \"$f\"; done"</automated>
  </verify>
  <acceptance_criteria>
    - Four NDJSON files exist, one per cell, and each covers **24** distinct folders:
      `jq -s 'group_by(.folder)|length'` returns `24` for every cell.
    - The strict rate is computed by the exact D-05 predicate — assert by running the documented
      `jq` expression against the NDJSON and comparing to the number written in the artefact; the
      two must be identical.
    - Both `index_tracks` settings are present in the artefact, each with its own strict rate, and
      exactly one is labelled as producing the headline number.
    - `grep -c '429'` on every Discogs cell's stderr returns `0`. If it does not, the affected cell's
      rate is **not** recorded and the run is repeated after the budget resets.
    - The three hand-transcribed `beet import -t` folders agree with the probe on source set and
      track counts. The agreement verdict appears verbatim in `## Second instrument`.
    - `## By tree` reports the DMC rate separately from the `dj-mixes` rate, and the artefact states
      that the DMC figure is not merged into T1's population.
    - The probe wrote nothing to either arm:
      `find /mnt/tank/downloads/spike-03/raw /mnt/tank/downloads/spike-03/normalised -newer <stamp> -type f`
      returns zero lines, cross-checked by `sha256sum` agreement on five files before and after.
    - `03-DISCOGS-EVIDENCE.md` contains no `Authorization` header and no token value; the stderr
      screen is recorded as performed.
  </acceptance_criteria>
  <done>Four cells scored loose and strict at both `index_tracks` settings, broken out by stratum, tree and format, agreeing with a hand-transcribed second instrument, with zero 429s.</done>
</task>

<task type="auto">
  <name>Task 2: Time the run, extrapolate to 143 folders, and record the request count from two instruments</name>
  <files>.planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Criterion 2 instrument* — the per-folder
      request arithmetic derived from source (1 `search` + ≤ `search_limit` release fetches +
      ≤ `search_limit` process-cached master fetches → 6 to 11 per folder), the extrapolation
      formula, and the `urllib3` DEBUG second instrument.
    - `.planning/phases/03-tagger-spike/03-DECISION.md` § *Denominators, committed before the run* —
      **143 folders**, committed in writing before this run happened. Use that denominator; do not
      recompute it here.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Common Pitfalls* 4 and § *Assumptions Log*
      A6 and A7.
  </read_first>
  <action>
From the timed run, record for the `normalised` × `musicbrainz discogs` cell:
  - **Wall-clock total and per folder** — instrument A is `time` around the probe process;
    instrument B is the per-folder UTC timestamps embedded in the NDJSON. The two must agree within
    a stated tolerance, and both figures are printed.
  - **Discogs requests** — instrument A is the arithmetic derived from source (search +
    `search_limit` releases + `search_limit` cached masters); instrument B is
    `grep -c 'api\.discogs\.com'` on the probe's own stderr, which counts what actually left the
    process independently of beets' bookkeeping. Report `M`, the measured requests per folder.
    **Do not assume 6..11** — measure it.
  - **429 count**, from the probe's in-process counter cross-checked against
    `grep -c '429'` on the stderr. Record it beside the timing whether it is zero or not: a
    429-saturated run does not error, it sleeps for potentially hours while looking healthy
    (`backoff_enabled = True`, `MAX_ATTEMPTS = 100`, `2**i` jittered), so without this number the
    extrapolation is uninterpretable.
  - **The observed `x-discogs-ratelimit`** from the startup identity probe. If it is not 60, record
    that as a correction to research assumption A6 and use the observed value in the arithmetic.
  - **The `search_limit` in force** — stated explicitly, because request count scales linearly with
    it (A7).

Extrapolate with the committed denominator:
  - `total_requests = 143 * M`
  - `api_floor_minutes = 143 * M / <observed ceiling>`
  - `wall_clock_minutes = (measured_seconds_for_24_folders / 60) * (143 / 24)`
Report both the pure-API floor and the projected wall-clock, and state plainly that wall-clock will
exceed the floor (MusicBrainz is separately rate-limited at 1 req/s, ffprobe reads cross ZFS, and
there is per-folder Python overhead) — which is why both figures are recorded rather than one.

Carry the correction into the artefact in the house style: **retire "7,451 files across ~1,000
folders"**. The file count was right; the folder count was off by roughly 8×, and Discogs requests
scale with folders, not files — so criterion 2's extrapolation shrinks by about an order of magnitude
against what PROJECT.md assumed.

Then write the verdicts back into `.planning/phases/03-tagger-spike/03-DECISION.md`'s pre-committed
threshold table, replacing the `PENDING` cells:
  - **T1** — Tested: yes. Returned: the strict rate over the normalised sample at the stated
    `index_tracks` setting, and whether it is above or below 40%. **Do not move the threshold.** If
    the strict rate is below 40%, record T1 as **fired** and carry that into axis one honestly; the
    phase exists to produce a number, including an inconvenient one.
  - **T2** — Tested: yes. Returned: the projected wall-clock for 143 folders and the observed 429
    count, and whether either limb of the threshold (> 4 hours, or any 429) fired.

Append to `03-DISCOGS-EVIDENCE.md`:
  - `## Timing` — wall-clock from both instruments with the agreement statement.
  - `## Requests` — `M` from both instruments with the agreement statement, plus `search_limit`
    and the observed rate ceiling.
  - `## Extrapolation to 143 folders` — the arithmetic written out, both figures, and the retraction
    of the "~1,000 folders" figure.
  - `## T1 and T2 verdicts` — tested/returned for each, with a sentence confirming neither threshold
    was changed after the evidence, and a pointer to the commit that predates it.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.159 "grep -c 'api\.discogs\.com' /mnt/fast/spike-03/out/cell-normalised-mbdiscogs.stderr"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'api\.discogs\.com'` on the headline cell's stderr returns a positive integer, and
      that number divided by 24 is recorded as `M` and matches the source-derived arithmetic within
      a stated tolerance. If the two disagree, neither is recorded until explained.
    - `grep -c '429'` on that stderr returns `0`, and the probe's in-process 429 counter agrees.
    - The wall-clock from `time` and the wall-clock derived from NDJSON per-folder timestamps agree
      within the stated tolerance; both are written into the artefact.
    - The extrapolation in `## Extrapolation to 143 folders` uses **143** as the denominator, and
      `git log -1 --format=%ct -- .planning/phases/03-tagger-spike/03-DECISION.md` predates
      `git log -1 --format=%ct -- .planning/phases/03-tagger-spike/03-DISCOGS-EVIDENCE.md`.
    - `03-DECISION.md`'s T1 row no longer reads `PENDING` and contains a numeric strict rate, the
      `index_tracks` setting, and an above/below-40% verdict.
    - `03-DECISION.md`'s T2 row no longer reads `PENDING` and contains a projected wall-clock figure
      and a 429 count.
    - `git log -p -- .planning/phases/03-tagger-spike/03-DECISION.md` shows the T1/T2 **threshold**
      text unchanged between the plan-03-02 commit and this one — only the tested/returned cells moved.
    - `bash scripts/check-music-freeze.sh` exits 0.
  </acceptance_criteria>
  <done>Criterion 2's figures exist from two independent instruments against the pre-committed 143 denominator, and T1 and T2 are marked tested with what they returned and no threshold moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe process → `api.discogs.com` | Authenticated outbound HTTP; the token is in the `Authorization` header |
| probe stderr → committed artefacts | Debug output contains full HTTP request lines |
| runtime `beet import -t` config → disk | The only file in this phase that contains the token in plaintext |
| measurement → the recorded decision | An unearned number here decides the tagger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-37 | Information disclosure | The `beet import -t` runtime config | mitigate | Written with `umask 077` under `/mnt/fast/spike-03/beets-config/`, outside the repo, never committed, deleted in plan 03-11. Acceptance asserts no token appears in any committed artefact. The repo is public and a prior exposure there is still recoverable via `git log -S` |
| T-03-38 | Information disclosure | `probe.stderr` quoted into a summary | mitigate | Screened for `Authorization` and for the token before any of it is quoted; only counts (`grep -c`) are transcribed, never raw request lines (ASVS V7) |
| T-03-39 | Repudiation | Moving a threshold after the evidence | mitigate | T1/T2 threshold text is asserted unchanged with `git log -p` on `03-DECISION.md`; only the tested/returned cells move. The decision-record commit is asserted to predate the evidence commit |
| T-03-40 | Repudiation | A number recorded from one instrument | mitigate | Every figure — rate, requests, wall-clock — has a named second independent instrument and a recorded agreement verdict. Disagreement blocks recording. This is the direct control for Phase 1's six unearned passes |
| T-03-41 | Denial of service | The shared 60 req/min budget | mitigate | Discogs cells run **serially**, never concurrently; `setsid`+`nohup` for detachment because `tmux`/`screen` are absent; 429 count asserted `0` before a rate is recorded, and a 429-bearing cell is re-run rather than reported |
| T-03-42 | Tampering | The sample copies | mitigate | `tag_album()` constructs no `ImportSession`; asserted with `find -newer` on both arms returning zero files, cross-checked by `sha256sum` agreement on five files |
| T-03-43 | Tampering | `/mnt/tank/media/Music` | mitigate | The container mounts no path under `/mnt/tank/media`; `check-music-freeze.sh` exit 0 after the run |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs; all packages ship in the audited `lscr.io/linuxserver/beets:2.13.1-ls349` image (03-RESEARCH.md § *Package Legitimacy Audit*: 3 OK, 0 SUS, 0 SLOP) |
</threat_model>

<verification>
- Four cells, 24 folders each, scored loose and strict at both `index_tracks` settings.
- Zero 429s on every recorded cell.
- Hand-transcribed `beet import -t` on three folders agrees with the probe.
- Requests and wall-clock each agree between two independent instruments.
- The extrapolation uses the pre-committed 143 denominator and the decision-record commit predates it.
- T1 and T2 filled in with tested/returned; threshold text provably unchanged.
- Nothing written to either sample arm; `check-music-freeze.sh` exit 0.
</verification>

<success_criteria>
Criterion 1 and criterion 2 are met with numbers this project can defend: strict and loose rates on
normalised and un-normalised arms, the gap between them, both `index_tracks` settings, DMC reported
separately, and a backlog extrapolation against a denominator committed before the run — every
figure carrying two agreeing instruments.
</success_criteria>

<output>
Create `.planning/phases/03-tagger-spike/03-07-SUMMARY.md` when done.
</output>
