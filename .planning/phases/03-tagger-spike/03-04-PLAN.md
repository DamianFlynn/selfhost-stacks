---
phase: 03-tagger-spike
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - .planning/phases/03-tagger-spike/03-spike-beets.yaml
  - .planning/phases/03-tagger-spike/03-spike-beets-config.yaml
autonomous: true
requirements: [TAGR-01, TAGR-02]

must_haves:
  truths:
    - "A throwaway beets container runs beets 2.13.1 on LXC 100 with both the musicbrainz and discogs plugins provably loaded"
    - "The container has no mount reaching /mnt/tank/media/Music at all, read-write or read-only"
    - "The Discogs token reaches the container through env_file only — never argv, never a committed file"
    - "Neither stacks/selfhosted/arrs/beets/beets.yaml nor stacks/selfhosted/music/wrtag.yaml nor renovate.json5 was edited"
    - "The spike config sets import.copy yes and import.move no explicitly, and no bare beet invocation ever ran"
  artifacts:
    - path: ".planning/phases/03-tagger-spike/03-spike-beets.yaml"
      provides: "Throwaway compose for the axis-one beets engine, outside the frozen stacks"
      contains: "2.13.1-ls349"
    - path: ".planning/phases/03-tagger-spike/03-spike-beets-config.yaml"
      provides: "Spike beets config: plugins list with musicbrainz explicit, copy not move, search_limit stated"
      contains: "musicbrainz"
  key_links:
    - from: ".planning/phases/03-tagger-spike/03-spike-beets.yaml"
      to: "/mnt/fast/secrets/discogs.env"
      via: "env_file, never -e and never a command line"
      pattern: "env_file"
    - from: "the spike container"
      to: "beet -c <spike config> -l <throwaway .blb> config -d"
      via: "explicit config and library on every invocation"
      pattern: "spike-probe.blb"
---

<objective>
Stand up the throwaway beets container that hosts axis one's measurements, and prove — not assume —
that both metadata plugins are loaded.

Three constraints shape this plan. **D-03:** `stacks/selfhosted/arrs/beets/beets.yaml` and
`stacks/selfhosted/music/wrtag.yaml` are not edited; both were deliberately frozen in Phase 1 behind
`restart: "no"` + `profiles: ["manual"]`, and the spike measures rather than half-deploys a decision
nobody has made. **D-20:** nobody holds `rw` on `/mnt/tank/media/Music` during Phases 1–3 — this
container mounts Music not at all, which is stronger than `:ro`. **OD-6:** both Python artefacts run
*inside* this container, because LXC 100 has `python3` 3.13.5 with no `pip3`, no `python3-venv`, a
missing `ensurepip` (so `python3 -m venv` fails outright) and `EXTERNALLY-MANAGED` present (so PEP
668 blocks system pip) — a host-resident script importing `mutagen` is impossible.

**D-02** fixes the version: beets is measured at **2.13.1**, not the installed `2.5.1-ls295`, for
symmetry with D-01 — both engines measured at what would actually be deployed. 2.13.1 also carries
what later phases depend on: album-level YAML in the `edit` plugin, `beet modify` `+=`/`-=`, and
`importsource` for resumable imports.

Purpose: every number in criteria 1 and 2 comes out of this container, and a customised `plugins:`
list that omits `musicbrainz` silently disables autotagging while presenting as "no match found".
That failure mode must be closed by assertion before any measurement runs.
Output: a throwaway compose file and spike beets config in the phase directory, a running
`beets-spike` container, and a recorded `beet config -d` proving both plugins loaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tagger-spike/03-CONTEXT.md
@.planning/phases/03-tagger-spike/03-RESEARCH.md
@.planning/phases/03-tagger-spike/03-PATTERNS.md
@.planning/phases/03-tagger-spike/03-DECISION.md
@stacks/selfhosted/arrs/beets/beets.yaml
@stacks/selfhosted/music/wrtag.yaml
@scripts/check-music-freeze.sh
@scripts/check-music-consumers.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the throwaway beets spike compose and its beets config</name>
  <files>.planning/phases/03-tagger-spike/03-spike-beets.yaml, .planning/phases/03-tagger-spike/03-spike-beets-config.yaml</files>
  <read_first>
    - `stacks/selfhosted/arrs/beets/beets.yaml` lines 1-45 — the content analog: `security_opt:
      [no-new-privileges:true]`, `restart: "no"`, `profiles: ["manual"]`, the comment-above-the-mount
      style, and the `:ro` on anything reaching Music. **Read it; do not edit it (D-03).**
    - `stacks/selfhosted/music/wrtag.yaml` lines 20-22 and 39-58 — the other half of the pattern: a
      **deleted** rather than commented-out library mount with the reason recorded in place, and the
      "Phase 4 owns retirement" pointer. **Read it; do not edit it (D-03).**
    - `.planning/phases/03-tagger-spike/03-PATTERNS.md` § *Throwaway compose files* — the location
      decision table (the phase directory wins over `stacks/selfhosted/spike-03/`) and **the `.env`
      trap**: compose resolves `.env` from the compose file's own directory, the stack `.env` files
      are gitignored, and a spike compose placed elsewhere gets an empty `$TZ`/`$PUID`/`$PGID` with
      only a warning.
    - `.planning/phases/03-tagger-spike/03-PATTERNS.md` § *Shared Patterns* → *Secrets* — the
      doctrine at `check-music-consumers.sh:150-172` and the gate at `:468-514`.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *The Discogs plugin needs no install in
      the LSIO image* — `python3-discogs-client` is already baked into
      `lscr.io/linuxserver/beets:2.13.1-ls349`, so enabling Discogs is config only.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Common Pitfalls* 1 and 5.
  </read_first>
  <action>
Create `.planning/phases/03-tagger-spike/03-spike-beets.yaml`. It lives in the **phase directory**,
not under `stacks/selfhosted/{arrs,music}/` (D-03) and not under `stacks/selfhosted/spike-03/` — a
directory under `stacks/` looks permanent, and a future reviewer reading it as a fourth tagging path
is precisely the failure mode this project exists to prevent. A leading comment must say so, and say
that plan 03-11 deletes this file.

Service `beets-spike`, `container_name: beets-spike`, image **`lscr.io/linuxserver/beets:2.13.1-ls349`**
per **D-02**. That exact tag was verified published 2026-08-29 and `2.13.1-ls350` returns 404, so the
pin exists as written. The unsuffixed `2.13.1` **floats** across ls-revisions — it moved ls344→ls349
in five weeks — and `:latest`/`:nightly` are prohibited outright.
`security_opt: [no-new-privileges:true]`, `restart: "no"`, `profiles: ["manual"]`.

**Write literal values for `TZ`, `PUID` and `PGID` — do not copy `$TZ`/`$PUID`/`$PGID` from
`beets.yaml`.** This file has no `.env` sibling and those variables would expand to empty with only
a warning. Use `TZ: Europe/Dublin`, `PUID: 568`, `PGID: 568` (`568:568` is `apps:apps` per CLAUDE.md
§ Storage and Permissions). Add `BEETSDIR: /spike-config` so no invocation can fall back to the
image default. Put a comment above them recording the `.env` trap.

`env_file: [/mnt/fast/secrets/discogs.env]` — absolute path, outside the repo, mode `600 root`.
Never `-e DISCOGS_USER_TOKEN=...`, never a command line: this host runs 103 containers with a
readable process table, which is why D-04 wrote the file via stdin in the first place. Reference the
variable **`DISCOGS_USER_TOKEN`** by name only; the value never appears in this repo.

Volumes, each with a comment above it stating why:
  - `/mnt/fast/spike-03/beets-config:/spike-config:rw` — throwaway config and the throwaway
    `library.db`. Explicitly **not** `/mnt/fast/appdata/arrs/beets/config`, which holds a real
    library the phase must not migrate.
  - `/mnt/fast/stacks/scripts:/spike-scripts:ro` — OD-6's delivery mechanism for the two Python
    artefacts; read-only so the container cannot alter the repo checkout.
  - `/mnt/tank/downloads/spike-03:/mnt/tank/downloads/spike-03:rw` — **identical path inside and
    outside**, so a path recorded by beets means the same thing on both sides.
  - `/mnt/fast/spike-03:/mnt/fast/spike-03:rw` — probe output, NDJSON and stderr, on a pool that is
    not `/`.
  - **No mount of `/mnt/tank/media` or `/mnt/tank/media/Music` at any mode.** Record that as a
    deleted-not-commented mount in the `wrtag.yaml:39-58` style, citing D-20 (nobody holds rw on
    Music during Phases 1–3) and noting that absence is stronger than `:ro`.

Drop the Traefik label block and any published port entirely — a throwaway spike gets no public
hostname and no Authelia chain.

Then create `.planning/phases/03-tagger-spike/03-spike-beets-config.yaml`, the spike beets config,
copied into `/mnt/fast/spike-03/beets-config/spike.yaml` at run time. It contains **no credential**.
Required keys, each with a comment giving its reason:
  - `plugins: musicbrainz discogs importsource fromfilename` — **`musicbrainz` listed explicitly**.
    Since beets 2.4.0 MusicBrainz is a plugin, and a customised list that omits it silently disables
    all MusicBrainz autotagging; the symptom presents as "no match found", not as a config error.
    This is the exact defect TAGR-05 exists to close.
  - `directory:` and `library:` both pointing under `/mnt/fast/spike-03/beets-config/` — never the
    image default.
  - `import: { copy: yes, move: no, write: no, incremental: no, resume: no }`. The estate's real
    beets configs set `import.move: yes`, which *moves* files out of the source; a spike config that
    inherited it and met a real path would destroy the source. State `move: no` explicitly rather
    than relying on a default.
  - `discogs: { index_tracks: no, search_limit: 5 }` — **both stated explicitly rather than left to
    default**, because the request count scales linearly with `search_limit` and the strict
    track-count rule is meaningless without stating how tracks were counted. Plan 03-07 runs the
    strict scoring at `index_tracks` both `no` and `yes`.
  - `musicbrainz: { searchlimit: 5 }` and `match: { preferred: { countries: ['GB','US'] } }`.
  - `scrub: { auto: no }`, `lastgenre: { auto: no }`, `embedart: { auto: no }`, `fetchart: { auto:
    no }`, `replaygain: { auto: no }` — beets' auto keys default to **on**, so an absent key is an
    armed key. Phase 1 found five configs in this estate with exactly that problem.
The Discogs `user_token` is deliberately absent from this file. Plan 03-06's probe injects it in
Python from the environment; plan 03-07's `beet import -t` cross-check generates a runtime config
with `umask 077` under `/mnt/fast/spike-03/beets-config/` that is never committed.
  </action>
  <verify>
    <automated>docker compose -f .planning/phases/03-tagger-spike/03-spike-beets.yaml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .planning/phases/03-tagger-spike/03-spike-beets.yaml config` exits 0 and
      emits **no** `variable is not set` warning on stderr — proving the `.env` trap was avoided.
    - The rendered config's image is exactly `lscr.io/linuxserver/beets:2.13.1-ls349`. Assert with
      `docker compose -f ... config --format json | jq -r '.services["beets-spike"].image'`.
    - The rendered config contains **zero** volume entries whose source path starts with
      `/mnt/tank/media`. Assert with
      `docker compose -f ... config --format json | jq '[.services["beets-spike"].volumes[].source] | map(select(startswith("/mnt/tank/media"))) | length'` returning `0`.
    - The rendered config's `env_file` list contains `/mnt/fast/secrets/discogs.env`, and the
      rendered `environment` map contains **no** key matching `TOKEN` or `SECRET`:
      `docker compose -f ... config --format json | jq '[.services["beets-spike"].environment | keys[] | select(test("TOKEN|SECRET"))] | length'` returns `0`.
    - `TZ`, `PUID` and `PGID` render to the literals `Europe/Dublin`, `568` and `568` — not empty
      strings.
    - `git diff --name-only` for this plan's commits lists neither
      `stacks/selfhosted/arrs/beets/beets.yaml`, `stacks/selfhosted/music/wrtag.yaml` nor
      `renovate.json5` (D-03).
    - `03-spike-beets-config.yaml` parses as YAML and `yq '.plugins'` (or a Python `yaml.safe_load`)
      returns a value containing both `musicbrainz` and `discogs`; `.import.move` is `false` and
      `.import.copy` is `true`; `.discogs.index_tracks` and `.discogs.search_limit` are both present.
    - Neither file contains the Discogs token: a grep for the token's value returns nothing, and no
      key named `user_token` appears in `03-spike-beets-config.yaml`.
  </acceptance_criteria>
  <done>Two throwaway artefacts exist in the phase directory, render without variable warnings, mount Music nowhere, carry no credential, and leave the frozen stacks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Start the container and prove both metadata plugins are loaded and reachable</name>
  <files>.planning/phases/03-tagger-spike/03-spike-beets.yaml</files>
  <read_first>
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Code Examples* → *Confirming the Discogs
      plugin is loaded and reachable, without importing anything* — the `beet -c ... -l ... config -d`
      invocation and the 01-07 finding that `beet config -d` prints a plugin's defaults **only when
      that plugin is loaded**, so absence of the block is a loading failure and not a safe default.
    - `.planning/phases/03-tagger-spike/03-RESEARCH.md` § *Common Pitfalls* 1 — a bare `beet`
      invocation opens the *default* library and runs schema migrations against it; in Phase 1 that
      migrated a database from 36,864 to 57,344 bytes and left 11 root-owned `.bak` files in an
      `apps:apps` tree.
    - `scripts/check-music-consumers.sh` lines 468-514 — WR-13, the permission assertion as a
      **gate, not a remark**: `stat -c '%a %U'` must equal `600 root` before sourcing, because `.`
      executes arbitrary shell and the exact state the assertion detects is the state in which the
      script would hand somebody root code execution. And no `set -a`.
    - `scripts/check-music-freeze.sh` lines 1-60 and 200-215 — how the harness enumerates *running*
      containers and their mounts by parsing `docker inspect`, not by grepping YAML.
  </read_first>
  <action>
On LXC 100, from `/mnt/fast/stacks` after `git pull --ff-only`:

Copy `.planning/phases/03-tagger-spike/03-spike-beets-config.yaml` to
`/mnt/fast/spike-03/beets-config/spike.yaml`.

Before anything sources the secret, run the gate: `stat -c '%a %U' /mnt/fast/secrets/discogs.env`
must return exactly `600 root`. If it does not, **refuse** — do not source it, do not start the
container, and report the observed value. This is a gate, not a remark. Do not use `set -a`
anywhere: exporting the token puts it in `/proc/<pid>/environ` of every child, and this plan spawns
`docker`, which is exactly the wrong child. Compose reads `env_file` itself; the shell never needs
the value.

Bring the container up with the `manual` profile:
`docker compose -f .planning/phases/03-tagger-spike/03-spike-beets.yaml --profile manual up -d`.

Prove the version: `docker exec beets-spike beet --version` must report `2.13.1`.

Prove the plugins are loaded — **every** beets invocation in this phase passes an explicit
`-c /spike-config/spike.yaml` **and** an explicit `-l /spike-config/spike-probe.blb`. Never rely on
`BEETSDIR`, never invoke bare `beet`. Run
`docker exec beets-spike beet -c /spike-config/spike.yaml -l /spike-config/spike-probe.blb config -d`
and capture the output to `/mnt/fast/spike-03/out/beet-config-d.txt`. A `discogs:` block with
`search_limit: 5` proves the Discogs plugin is loaded; a `musicbrainz:` block proves MusicBrainz is.
Absence of either block is a **loading failure**, not a safe default — stop and fix rather than
proceeding to measure.

Prove Discogs auth independently of beets, one request only: from inside the container, issue a
single `GET https://api.discogs.com/oauth/identity` with the token supplied from the environment via
a header file or a variable expansion **inside** the container, never on the `docker exec` argv.
Assert HTTP 200 and record the `x-discogs-ratelimit` response header value. Research assumption A6
is that the authenticated ceiling is still 60/min; this re-verifies it at the start rather than
trusting a 2026-08-19 reading, and criterion 2's whole extrapolation depends on it. Record the
username field but **screen the response** before quoting any of it — do not paste the token or any
`Authorization` header into a summary.

Prove the container cannot reach Music: `docker inspect beets-spike --format '{{json .Mounts}}'`
must contain no `Source` under `/mnt/tank/media`. Then run `bash scripts/check-music-freeze.sh` and
require exit 0 with `tagger-class writers: 0` and `declared rw reaching Music: 0` — the harness
enumerates *running* containers rather than YAML, so this is proof rather than assertion.

Prove Pitfall 1 did not fire: assert that no `.bak` file appeared anywhere under
`/mnt/fast/appdata/arrs/beets/` and that the mtime of every `library.db` under `/mnt/fast/appdata/`
is unchanged from before the container started. Capture the before-state mtimes first.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes root@172.16.1.159 "docker exec beets-spike beet -c /spike-config/spike.yaml -l /spike-config/spike-probe.blb config -d | grep -cE '^(discogs|musicbrainz):'"</automated>
  </verify>
  <acceptance_criteria>
    - `docker exec beets-spike beet --version` output contains `2.13.1`.
    - `beet -c ... -l ... config -d` output contains **both** a `discogs:` block and a
      `musicbrainz:` block; the grep above returns `2`.
    - The same output shows `search_limit: 5` under `discogs` and `index_tracks: false` (or `no`),
      confirming the explicit settings took effect rather than silent defaults.
    - `GET /oauth/identity` returned HTTP `200` and the recorded `x-discogs-ratelimit` value is
      captured in the summary. If it is not `60`, that is recorded as a correction to A6 and
      criterion 2's arithmetic uses the observed value.
    - `stat -c '%a %U' /mnt/fast/secrets/discogs.env` returned exactly `600 root` before anything
      sourced it, and the run transcript shows no `set -a`.
    - `docker inspect beets-spike --format '{{json .Mounts}}' | jq '[.[] | select(.Source | startswith("/mnt/tank/media"))] | length'`
      returns `0`.
    - `bash scripts/check-music-freeze.sh` exits 0, reporting `tagger-class writers: 0` and
      `declared rw reaching Music: 0`.
    - `find /mnt/fast/appdata/arrs/beets -name '*.bak' -newermt '-1 hour' | wc -l` returns `0`, and
      the mtime of every `library.db` under `/mnt/fast/appdata/` matches the pre-start capture.
    - No artefact committed by this plan contains the token: grep the token's value across
      `git diff` for this plan's commits and get zero hits.
  </acceptance_criteria>
  <done>`beets-spike` runs beets 2.13.1 with both plugins provably loaded, Discogs auth confirmed at a recorded rate ceiling, Music unreachable from it, and no real beets database touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/mnt/fast/secrets/discogs.env` → container environment | The token crosses into a container on a host with 103 containers and a readable process table |
| spike container → `/mnt/tank/media/Music` | D-20 forbids any rw holder during Phases 1–3; the mount layer is the control |
| spike container → real beets databases under `/mnt/fast/appdata` | A bare `beet` invocation migrates whatever database it finds |
| beets Discogs plugin → `api.discogs.com` | Outbound authenticated HTTP carrying a bearer credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information disclosure | `DISCOGS_USER_TOKEN` in the process table | mitigate | `env_file: /mnt/fast/secrets/discogs.env` only; never `-e`, never a `docker exec` argv, never a config file in git. `stat -c '%a %U'` must equal `600 root` before use — a **refusal**, not a remark. No `set -a`, so the token is not exported into `/proc/<pid>/environ` of the `docker` child |
| T-03-17 | Information disclosure | Token committed to a public repo | mitigate | `03-spike-beets-config.yaml` carries no `user_token` key; the probe injects it from the environment at run time and the `beet import -t` runtime config is written under `/mnt/fast/spike-03/` with `umask 077` and never committed. The repo is public and has one prior credential exposure still recoverable via `git log -S` |
| T-03-18 | Tampering | `/mnt/tank/media/Music` | mitigate | No mount of `/mnt/tank/media` at any mode — absence is stronger than `:ro`. Asserted from `docker inspect .Mounts` (running container, not YAML) and by `check-music-freeze.sh` exit 0 with `tagger-class writers: 0` |
| T-03-19 | Tampering | Real beets `library.db` files under `/mnt/fast/appdata` | mitigate | Every invocation passes explicit `-c` **and** `-l`; `BEETSDIR` is pinned to the throwaway `/spike-config`; the appdata beets config path is deliberately not mounted. Asserted by a `.bak` sweep and an mtime comparison on every `library.db` under `/mnt/fast/appdata/` |
| T-03-20 | Tampering | The real backlog under `complete/nzb/` | mitigate | `import.move: no` and `import.copy: yes` set explicitly, overriding the estate's `import.move: yes`; only `/mnt/tank/downloads/spike-03` is mounted, not `complete/` |
| T-03-21 | Elevation of privilege | Container escape / added capabilities | mitigate | `security_opt: [no-new-privileges:true]`, no `privileged`, no added capabilities, `restart: "no"`, `profiles: ["manual"]`, no published port and no Traefik labels |
| T-03-22 | Tampering | Frozen stack definitions (D-03) | mitigate | `git diff --name-only` must list none of `stacks/selfhosted/arrs/beets/beets.yaml`, `stacks/selfhosted/music/wrtag.yaml`, `renovate.json5` |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | **No pip install occurs here** — `python3-discogs-client` 2.9 is already baked into `lscr.io/linuxserver/beets:2.13.1-ls349` (verified in the LSIO Dockerfile at that tag), so enabling Discogs is config only. `beets` 2.13.1, `mutagen` 1.48.1 and `python3-discogs-client` 2.9 were audited in 03-RESEARCH.md § *Package Legitimacy Audit*: 3 OK, 0 SUS, 0 SLOP; no `[ASSUMED]` or `[SLOP]` package is involved, so no blocking legitimacy checkpoint is required |
</threat_model>

<verification>
- `beet --version` reports 2.13.1; `beet -c … -l … config -d` shows both `discogs:` and `musicbrainz:` blocks.
- Discogs `/oauth/identity` returns 200; `x-discogs-ratelimit` recorded.
- `docker inspect beets-spike` shows zero mounts under `/mnt/tank/media`.
- `bash scripts/check-music-freeze.sh` exits 0.
- No `.bak` files and no `library.db` mtime change under `/mnt/fast/appdata/`.
- No frozen stack file appears in this plan's diff.
</verification>

<success_criteria>
Axis one has a measurement host: beets 2.13.1 in a throwaway container, both plugins provably
loaded, Discogs reachable at a recorded rate ceiling, Music unreachable, and every real beets
database untouched.
</success_criteria>

<output>
Create `.planning/phases/03-tagger-spike/03-04-SUMMARY.md` when done.
</output>
