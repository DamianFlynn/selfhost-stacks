---
phase: 07-pilot-12-albums-end-to-end
plan: 09
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
autonomous: false
requirements: [IMPT-01, QUAL-04]

must_haves:
  truths:
    - "D-16: the fence is BOTH datasets — `tank/media/Music@pre-07-pilot` AND `fast/appdata/arrs@pre-07-pilot` — taken in ONE remote step, each fail-if-exists, both listed back by exact whole-line match and asserted BEFORE any write; the order they were taken in is stated (C11: two pools, one step, not one atomic snapshot)"
    - "Criterion 1: a file copy of `library.db` and `state.pickle` is taken together with the snapshots, sha256 recorded — and the SNAPSHOT'S OWN VIEW of both files (`.zfs/snapshot/pre-07-pilot/beets/config/`) is sha256-equal to those safety copies, not merely listed with `ls -l` (G-04)"
    - "G-05: the post-grant effective `import.copy/move/write` readings are GATED, compared by parsed value — STOP if `move` is true (or `copy`/`write` not true); and inbox emptiness is RE-COUNTED after `compose up`, not carried from Task 1"
    - "G-06: a PARTIAL fence (the `tank` snapshot taken, the `fast` snapshot failed) is its own recorded state (`STOP STATE 07-09-PARTIAL-FENCE`); nothing is destroyed on the executor's judgement; a re-entry reconciles it and offers the operator `resume-partial-fence`, which takes only the missing snapshot — D-18 resumability preserved"
    - "⛔ D-16: `zfs [r]ollback` on `fast/appdata/arrs` is FORBIDDEN on every branch (it carries Sonarr/Radarr/Lidarr state); database recovery is a FILE RESTORE out of `/mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/`, only — the fence is written at this call site"
    - "E3: the `rw` grant is the FIRST write act on the library path, deployed only after the fence is listed back, never before"
    - "C9: the repo `config.yaml` and `flask-config.yaml` are installed to their appdata paths in the SAME step as the deploy, sha256 asserted equal to the repo"
    - "C4 / D-21: after deploy the watchdog registration line lists exactly TWO inboxes (`02-review`, `03-asis`) and no `01-auto`; the readiness gate is `docker exec -u abc … test -r /config/config.yaml` plus that line, never `.State.Status` alone"
    - "D-23: after deploy `docker inspect beets-flask` shows `/mnt/tank/media → /media` RW=true and `quick-health-check.sh` is run END TO END once (DEF-06-39-05), every block's verdict recorded"
    - "`hold` is a first-class outcome; D-18: an abandoned run holds the fence and writes a STOP state"
    - "P7R2-01 / P7R2-08b: every 07-09 outcome is a NAMED ledger token at the start of its own line — success `STATE 07-09-GRANTED`, stops `STOP STATE 07-09-{HOLD, REVIEW-FIRST, FF-FAIL, FENCE-FAIL, PARTIAL-FENCE, GRANT-FAIL, IMPORT-KEYS-FAIL, INBOX-NOT-EMPTY, WATCHDOG-ABSENT, QHC-RED}` — each run's artifact section copies the tokens it wrote (`LEDGER: …`), and Task 2's verify is keyed on THIS run's `DISPOSITION:` line and the ledger's NEWEST 07-09 token, never on ledger history, so a historical stop token cannot pass a later run"
    - "P7R2-04 / D-18: every post-grant STOP (`GRANT-FAIL`, `IMPORT-KEYS-FAIL`, `INBOX-NOT-EMPTY`, `WATCHDOG-ABSENT`, `QHC-RED`) leaves the fence held and the writer stopped, and has a re-entry: Task 1 reconciles that state and offers `resume-post-grant`, which re-runs the grant and its gates without re-taking or destroying the fence"
    - "P7R2-08c: the plan may run more than once; each run writes under its own `## RUN <n>` section and both verifies read only the newest section, so an earlier run's transcript can neither fail nor pass this run"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt"
      provides: "Pre-state, the gate answer, the fence transcript, the deploy transcript, the first end-to-end qhc run"
      contains: "pre-07-pilot"
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt"
      provides: "The D-18 STOP-state ledger every import plan appends to; its newest 07-09 token is what plan 07-10 gates on"
      contains: "07-09-"
  key_links:
    - from: "fence listed back"
      to: "docker compose up -d with the D-22 commit"
      via: "same plan, strictly after, with the listing asserted"
      pattern: "listed back"
---

<objective>
Take the two-dataset fence and deploy the D-22 grant behind it — the phase's first write — then run
the routine health check end to end once.

Purpose: E3 makes the grant a deliberate first act with the fence already in place; D-16 makes the
fence both the library and beets' own state, because rolling back the tree alone leaves `incremental`
state claiming the work is done. C11: `tank` and `fast` are different pools, so "one remote step" is
achievable and "one atomic snapshot" is not — the step takes `tank/media/Music@pre-07-pilot` first
(the dataset the grant exposes) then `fast/appdata/arrs@pre-07-pilot`, lists both back, and nothing
can write `library.db` in the gap because the mount is still `:ro` and no import is possible.

⛔ **Scope fence, stated once and repeated at the call site (CONVENTIONS §6).** The `zfs` verbs this
plan may issue are `list` and `snapshot` (exactly two named snapshots). `zfs [r]ollback` is forbidden
on every branch and on every dataset; on `fast/appdata/arrs` it is forbidden absolutely (Sonarr,
Radarr and Lidarr state live there); recovery of `library.db` / `state.pickle` is a file copy out of
the snapshot's `.zfs/snapshot/pre-07-pilot/` directory. `zfs [d]estroy` is not issued.
`tank/downloads@pre-phase5` is not touched (E4, D-17). No import is run by this plan.

The single budgeted code review (D-31) is offered at this gate: this is the last point at which a
finding in the phase's own instruments (plans 07-02 … 07-08) costs nothing but a pause.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: measure the pre-grant state, reason, then ask proceed / review-first / hold</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-41-PLAN.md` Task 2 and `artifacts/06-41-conf04-reprobe-drive.txt` — the one-remote-step fence shape and its recorded form (command issued / exit status / listed back / PASS).
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-52-PLAN.md` — measure / reason / ask, `hold` first, "any other answer is treated as hold", exact whole-line `grep -qxF`.
    - `scripts/freeze-music-apply.sh` — its two explicit `zfs snapshot` calls and read-back loop (skip-if-exists there; fail-if-exists here).
    - `.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-08-d22-measure.txt` and the D-22 commit (`git log --grep='D-22 — grant /media rw'`).
    - `.planning/STATE.md` § Blockers/Concerns — the host checkout divergence entry (RESOLVED 2026-09-25) and why `pull --ff-only` must be possible.
    - `stacks/selfhosted/arrs/beets/flask.yaml` READINESS GATE comment; `stacks/selfhosted/arrs/compose.yaml` (beets/flask.yaml is included; beets/beets.yaml is not).
  </read_first>
  <action>
**Measure first. Reason second. Ask third. Issue no write on any branch of this task.** Every remote
command bounded Linux-side, `set -o pipefail` in piped strings, ssh RC recorded; an empty answer to
a question that has a non-empty healthy answer is COULD NOT LOOK and a stop.

**Run sections (P7R2-01, P7R2-08c).** This plan may run more than once (re-entry after a stop).
Each run begins by appending the heading `## RUN <n> — <UTC>` to `07-09-fence-and-grant.txt`
(n = the number of earlier `## RUN ` headings + 1) and writes everything for this run below it;
earlier sections are never edited. Both task verifies read ONLY the newest run's section, so an
earlier run's fence transcript cannot fail this run's Task 1 check and an earlier run's evidence
cannot pass this run's Task 2 check.

**Ledger tokens (P7R2-01, P7R2-04).** Read the ledger (if it exists) with the ONE extraction every
consumer uses (this plan's Task 2 verify, plan 07-10 Task 1 and its verify):
`LC_ALL=C /usr/bin/grep -oE '^(STOP )?STATE 07-09-[A-Z][A-Z-]*[A-Z]' <ledger> | tail -n 1`.
Write `NEWEST 07-09 TOKEN: <token | none>` and `GOVERNING 07-09 TOKEN: <token | none>`, where the
governing token is the newest 07-09 token that is not `07-09-HOLD`, `07-09-REVIEW-FIRST` or
`07-09-FF-FAIL` (a hold, and a push/fast-forward failure that stops before any fence step, record a
state; they do not change it). The expected readings in (d)–(g) are keyed on the
GOVERNING token, as (d) states.

Record under `MEASURED STATE`:
(a) instruments landed: plans 07-01 … 07-08 each have a SUMMARY; `git merge-base --is-ancestor`
    confirms the commits adding `07-EVIDENCE-MAP.md`, `07-SAMPLE.md` and `07-EXPECTED-TREE.txt`
    and the D-22 commit are ancestors of workstation `HEAD`;
(b) `git status --porcelain` clean on the workstation; `git rev-list --count origin/main..HEAD`
    (commits to push) and `HEAD..origin/main` (must be 0);
(c) host checkout: `git -C /mnt/fast/stacks status --porcelain` (must be empty), `rev-parse HEAD`,
    and after `git -C /mnt/fast/stacks fetch origin` the ahead/behind counts vs `origin/main`
    (host must be 0 ahead, so `pull --ff-only` can succeed);
(d) from atlantis as real root: `zfs list -t snapshot -H -o name -r tank/media/Music` and
    `-r fast/appdata/arrs` and `-r tank/downloads` in full; assert `tank/media/Music@pre-07-pilot`
    and `fast/appdata/arrs@pre-07-pilot` are ABSENT (exact whole-line count 0) and
    `tank/downloads@pre-phase5` PRESENT; `zfs list -o name,used,avail,refer tank fast
    tank/media/Music fast/appdata/arrs`; `zfs get -H -o value mountpoint fast/appdata/arrs`
    (must be `/mnt/fast/appdata/arrs`, read from atlantis — LXC 100's view is not the disk's).
    The expected reading depends on the GOVERNING 07-09 token (G-06, P7R2-04):
    - `none` (nothing was ever taken): the fresh reading above.
    - `07-09-PARTIAL-FENCE` (G-06): `tank/media/Music@pre-07-pilot` PRESENT,
      `fast/appdata/arrs@pre-07-pilot` ABSENT; then also record
      `zfs get -H -p -o value written tank/media/Music` (expected 0 — the library is still `:ro`)
      and the live `library.db`/`state.pickle` sha256 (expected the fixture values). Write
      `PARTIAL FENCE RECONCILIATION: CONSISTENT` only when all four agree, else `PARTIAL FENCE
      RECONCILIATION: INCONSISTENT — <reading>` (then only `hold` is offered).
    - a post-grant stop — `07-09-GRANT-FAIL`, `07-09-IMPORT-KEYS-FAIL`, `07-09-INBOX-NOT-EMPTY`,
      `07-09-WATCHDOG-ABSENT` or `07-09-QHC-RED` (P7R2-04): both pre-07-pilot names PRESENT (exact
      whole-line); `zfs get -H -p -o value written tank/media/Music` 0 (no import has run); both
      fence safety copies under `/mnt/fast/safety/phase07/fence/` present with the sha256 recorded
      at the fence; `docker inspect -f '{{.State.Running}}' beets-flask` reads `false` (the stop
      left the writer stopped); `/media` RW recorded (true unless the grant failed before the
      recreate); for (e), because the container is stopped, copy `library.db` to
      `/mnt/fast/safety/phase07/reentry/` and read the item count from the COPY with `sqlite3`
      `mode=ro` under whichever host's `python3` is present (record which) — expected 0; for (f)
      record the inbox counts (the operator's fix may be what emptied them; Task 2 re-counts). Write
      `POST-GRANT RECONCILIATION: CONSISTENT (<token>)` only when all of these agree, else
      `POST-GRANT RECONCILIATION: INCONSISTENT — <reading>` (then only `hold` is offered).
    - `07-09-FENCE-FAIL`: record which pre-07-pilot names are present, as listed; a failed fence
      step (e.g. a snapshot view that differed from its safety copy) has no in-plan remedy, so only
      `hold` is offered and the operator decides out of band; nothing is destroyed.
    - `07-09-GRANTED`: this plan has already completed. Write `DISPOSITION: already-granted`,
      present no gate, and take nothing further (Task 2 writes nothing; a second fence or grant is
      never taken).
    Any other combination of present/absent names is COULD NOT RECONCILE and a stop;
(e) `docker exec beets-flask sha256sum /config/library.db /config/state.pickle` (still the
    fixture values) and a read-only item count via `docker exec beets-flask /venv/bin/python -c`
    with `sqlite3` `mode=ro` (expected 0);
(f) from atlantis: audio-file counts under `_inbox/01-auto`, `_inbox/02-review`, `_inbox/03-asis`
    (all expected 0);
(g) `docker inspect beets-flask` `/media` RW (expected false now); the current watchdog
    registration line from `docker logs beets-flask` (three inboxes now);
(h) the appdata file modes/owners of `/mnt/fast/appdata/arrs/beets/config/config.yaml` and
    `…/beets-flask/config.yaml` (`stat -c '%a %u:%g'`), so the install preserves them;
(i) Jellyfin and Music Assistant reachable (one read each through the consumers script's patterns).

Then `DEPENDENCY REASONING`, answering with cited evidence: 1. Are all instruments the pilot trusts
hardened and committed (D-11/12 in 07-02, D-25 in 07-03, D-27 in 07-04, D-30 in 07-05)? 2. Is the
evidence map committed before any write (D-31)? 3. Is the host able to fast-forward, so the deploy
does not need a divergence resolved as a side effect? 4. Is there room: tank/fast `avail` against
the twelve folders' total size (from 07-07's manifest) plus copy duplication? 5. The dormant-arm
interpretation from plan 07-08 (flask-only rw) — state it plainly for the operator. 6. The
single budgeted review: what it would cover now (the diffs of 07-02 … 07-08) and what it would not
(07-14/07-15's later consumer edits).

**Every stop inside this task** — a COULD NOT LOOK, a COULD NOT RECONCILE, an INCONSISTENT
reconciliation, a governing `07-09-FENCE-FAIL` — presents the gate with `hold` as its only option,
so it still records an answer and a disposition and passes this task's verify (P7R2-08).

Recommendation stated explicitly. Present `hold` first. Offer `resume-partial-fence` ONLY when
`PARTIAL FENCE RECONCILIATION: CONSISTENT` is on record in this run's section, and
`resume-post-grant` ONLY when `POST-GRANT RECONCILIATION: CONSISTENT` is. Record the answer verbatim
with UTC time on an `OPERATOR ANSWER` line, followed by ONE normalised line
`DISPOSITION: hold|review-first|proceed|grant-both|resume-partial-fence|resume-post-grant` (an
unrecognised answer, or a resume option that was not offered, normalises to `hold`). With
`resume-post-grant`, record the operator's stated fix for the cause verbatim (e.g. the commit that
corrected the config, or the stray audio they removed).
  </action>
  <decision>Take the two-dataset fence and deploy the D-22 grant now?</decision>
  <context>This is the phase's first write. The fence precedes the grant in the same plan; no import happens here. `tank/downloads@pre-phase5` is out of scope. The D-22 commit grants rw to beets-flask only (the dormant CLI arm stays ro) — say `grant-both` to amend that before deploy.</context>
  <options>
    <option id="hold">
      <name>Hold — take nothing, deploy nothing</name>
      <pros>Costs nothing; right if any measurement above is a could-not-look, the host cannot fast-forward, or an instrument plan is incomplete.</pros>
      <cons>The pilot does not start.</cons>
    </option>
    <option id="review-first">
      <name>Review first — run the ONE budgeted `/gsd-code-review 07` over plans 07-02 … 07-08, then re-gate</name>
      <pros>A defect in an instrument is found before it is trusted with a writable library; the cheapest point for a finding (D-31's kept lesson).</pros>
      <cons>Adds a pause; the budget is then spent (no second review round).</cons>
    </option>
    <option id="proceed">
      <name>Proceed — push, pull on the host, fence, grant, verify</name>
      <pros>Starts the pilot with every precondition measured.</pros>
      <cons>Opens the library to writes for good (D-23).</cons>
    </option>
    <option id="grant-both">
      <name>Proceed, but grant rw to the dormant CLI arm too</name>
      <pros>Literal reading of the canonical file list.</pros>
      <cons>Creates an unsanctioned second writer (D-04 forbids it the real library) and reds the freeze census when started.</cons>
    </option>
    <option id="resume-partial-fence">
      <name>Resume a partial fence — take ONLY the missing `fast/appdata/arrs@pre-07-pilot`, then continue as `proceed` (offered only after a CONSISTENT partial-fence reconciliation)</name>
      <pros>Completes the fence without destroying or re-taking the `tank` snapshot (D-18).</pros>
      <cons>The two snapshots are taken in different sessions; the gap is recorded (C11) and the `written` = 0 reading is what makes it safe.</cons>
    </option>
    <option id="resume-post-grant">
      <name>Resume after a post-grant stop — keep the fence, re-run the grant and every one of its gates (offered only after a CONSISTENT post-grant reconciliation, P7R2-04)</name>
      <pros>The stopped writer is brought back only through the same install, readiness, import-key and inbox gates; nothing is re-taken or destroyed (D-18).</pros>
      <cons>Needs the operator's fix for the cause first (a config commit, or the stray audio removed); if the cause persists the same STOP recurs.</cons>
    </option>
  </options>
  <resume-signal>Select: `hold`, `review-first`, `proceed`, `grant-both` or (only if offered) `resume-partial-fence` / `resume-post-grant` (state the fix you made). Any other answer is treated as `hold`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt && test -s "$A" && R="$(/usr/bin/awk '/^## RUN /{b=""} {b=b $0 "\n"} END{printf "%s", b}' "$A")" && printf '%s' "$R" | /usr/bin/grep -qE '^## RUN [0-9]+ ' && printf '%s' "$R" | /usr/bin/grep -qE '^NEWEST 07-09 TOKEN: ' && test "$(printf '%s' "$R" | /usr/bin/grep -cE 'zfs (snapshot|destroy|rollback) ')" -eq 0 && D="$(printf '%s' "$R" | /usr/bin/grep -E '^DISPOSITION: ' | tail -n 1 | sed 's/^DISPOSITION: //')" && case "$D" in already-granted) printf '%s' "$R" | /usr/bin/grep -qxF 'GOVERNING 07-09 TOKEN: 07-09-GRANTED' ;; hold) printf '%s' "$R" | /usr/bin/grep -qF 'MEASURED STATE' && printf '%s' "$R" | /usr/bin/grep -qF 'OPERATOR ANSWER' ;; review-first|proceed|grant-both|resume-partial-fence|resume-post-grant) printf '%s' "$R" | /usr/bin/grep -qF 'MEASURED STATE' && printf '%s' "$R" | /usr/bin/grep -qF 'DEPENDENCY REASONING' && printf '%s' "$R" | /usr/bin/grep -qF 'pre-phase5' && printf '%s' "$R" | /usr/bin/grep -qF 'OPERATOR ANSWER' && { test "$D" != resume-partial-fence || printf '%s' "$R" | /usr/bin/grep -qF 'PARTIAL FENCE RECONCILIATION: CONSISTENT'; } && { test "$D" != resume-post-grant || printf '%s' "$R" | /usr/bin/grep -qF 'POST-GRANT RECONCILIATION: CONSISTENT'; } ;; *) false ;; esac</automated>
  </verify>
  <acceptance_criteria>
    - This run's section starts `## RUN <n> — <UTC>` and records `NEWEST 07-09 TOKEN:` and `GOVERNING 07-09 TOKEN:` read by the stated extraction command.
    - Measurements (a)–(i) recorded with commands and RCs, against the reading the GOVERNING token expects: fresh (both pre-07-pilot names ABSENT by exact whole-line count); after `07-09-PARTIAL-FENCE` a `PARTIAL FENCE RECONCILIATION: CONSISTENT|INCONSISTENT` line with its four readings (tank present, fast absent, `written` 0, fixture sha256s); after a post-grant stop a `POST-GRANT RECONCILIATION: CONSISTENT|INCONSISTENT` line with its readings (both present, `written` 0, safety copies, writer stopped, item count 0 from a copy); `tank/downloads@pre-phase5` PRESENT.
    - All six reasoning questions answered with cited evidence; recommendation stated before the options; `hold` first; every in-task stop offered `hold` only.
    - No write issued: this run's section contains no unbracketed snapshot/destroy/rollback verb (count 0 over the newest `## RUN` section only, recipe driven against a control containing `zfs snapshot x` first).
    - The operator's answer recorded verbatim with a UTC timestamp on a line beginning `OPERATOR ANSWER`, followed by one normalised `DISPOSITION:` line (or `DISPOSITION: already-granted` with no gate when the governing token is `07-09-GRANTED`).
  </acceptance_criteria>
  <done>The operator has answered with the full pre-grant state in front of them; nothing has been written.</done>
</task>

<task type="auto">
  <name>Task 2: on `proceed` / `grant-both` — push, host pull, the ONE-step fence, then the grant</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `artifacts/07-09-fence-and-grant.txt` (task 1 — the answer and measured values; re-read, do not carry numbers in your head).
    - `stacks/selfhosted/arrs/beets/beets.yaml` notes on the s6/`lsiown` boot race (gate on `docker exec -u abc … test -r /config/config.yaml`).
    - `scripts/quick-health-check.sh` header (how it is run, `REMOTE_TIMEOUT`).
  </read_first>
  <action>
**Ledger grammar (P7R2-01, P7R2-08b).** Create `artifacts/07-pilot-ledger.txt` if it does not
exist, with a header stating its purpose (the D-18 written state every import plan appends to; a
later session reads this instead of re-deriving) and its grammar: every state token starts its own
line, as `STOP STATE <plan>-<NAME>: <state>` for a stop or `STATE <plan>-<NAME>: <state>` for a
success. Otherwise append. Copy every ledger line this run writes, verbatim, into this run's
artifact section prefixed `LEDGER: `. Every outcome below writes exactly one named 07-09 token.

**On `already-granted`:** write nothing (Task 1 recorded it). **On `hold`:** write nothing to the
estate; ledger `STOP STATE 07-09-HOLD: <the reconciled state, stated truthfully>; albums landed:
none — held at 07-09 on <answer, timestamp>`. The state text follows the GOVERNING token (P7R2-19 —
it never says "no fence taken" while a snapshot exists): fresh entry → "no fence taken, no grant";
after `07-09-PARTIAL-FENCE` → "tank/media/Music@pre-07-pilot held (partial fence),
fast/appdata/arrs@pre-07-pilot missing, no grant"; after a post-grant stop → "both pre-07-pilot
snapshots held, grant deployed, writer stopped"; after `07-09-FENCE-FAIL` → the names present as
listed, "no grant". Stop. **On `review-first`:** the same, with token `STOP STATE
07-09-REVIEW-FIRST: …`, plus record that `/gsd-code-review 07` is the next action and this plan is
re-run after it. **On `grant-both`:** first make a one-line
amendment commit changing `beets.yaml`'s mount to `:rw` and qhc's CLI-render expectation to `rw`
with a dated note citing the operator's answer, then continue as `proceed`.

**On `proceed` (or `grant-both`, or `resume-partial-fence` / `resume-post-grant` with their step-2 variants), in this order, stopping at the first failure:**

1. `git push origin main` (the operator's answer is the consent). Host:
   `git -C /mnt/fast/stacks pull --ff-only`; assert host `HEAD` == workstation `HEAD`. A failed
   push or fast-forward is a STOP before any fence (nothing to undo): record it and write the ledger
   `STOP STATE 07-09-FF-FAIL: <reading>; nothing taken by this run, no grant; albums landed: none`.

2. **THE FENCE — ONE remote invocation on atlantis as real root.** The remote program, in order:
   assert `zfs list -t snapshot -H -o name tank/media/Music@pre-07-pilot` fails (absent) and same
   for `fast/appdata/arrs@pre-07-pilot` — refuse if either exists; `zfs snapshot
   tank/media/Music@pre-07-pilot`, capture RC, stop on non-zero; `zfs snapshot
   fast/appdata/arrs@pre-07-pilot`, capture RC, stop on non-zero; list both back with
   `zfs list -t snapshot -H -o name,creation` and assert each name by exact whole-line match on the
   name column; print both `creation` values (the order and the gap are the C11 record).
   **Partial fence (G-06):** if the `tank` snapshot succeeded and the `fast` snapshot failed, the
   program stops there, the grant is NOT deployed, and the executor records `PARTIAL FENCE:
   tank/media/Music@pre-07-pilot present, fast/appdata/arrs@pre-07-pilot absent — <RC and stderr>`
   and writes the ledger `STOP STATE 07-09-PARTIAL-FENCE: tank snapshot held, fast snapshot
   missing, no grant, no album landed`. ⛔ The `tank` snapshot is NOT destroyed — no `zfs [d]estroy`
   on the executor's judgement; the operator's next answer at a re-run of Task 1 decides.
   **On `resume-partial-fence`:** the remote program instead asserts `tank/media/Music@pre-07-pilot`
   PRESENT and `fast/appdata/arrs@pre-07-pilot` ABSENT (refuse otherwise), re-reads `written` on
   `tank/media/Music` (must be 0), takes ONLY `zfs snapshot fast/appdata/arrs@pre-07-pilot`, and
   lists both back exactly as above. **On `resume-post-grant` (P7R2-04):** the remote program takes
   NOTHING — it re-lists both pre-07-pilot names with `zfs list -t snapshot -H -o name,creation`,
   asserts each by exact whole-line match on the name column (`listed back`, recorded in the 06-41
   form without a command-issued line) and re-reads `written` on `tank/media/Music` (must be 0);
   either name absent, or `written` non-zero, is `07-09-FENCE-FAIL`. The fence copies and the G-04
   comparison below are not re-taken on this variant; quote the first run's lines instead. Write at
   this call site, in the program's own comment and in the artifact, the D-16 fence:
   `zfs [r]ollback on fast/appdata/arrs is FORBIDDEN on every branch — recovery is a file copy from
   /mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/beets/config/`. Record in the 06-41 form:
   command issued / exit status / listed back / PASS per snapshot.
   Then, still before the grant: on atlantis copy
   `/mnt/fast/appdata/arrs/beets/config/library.db` and `state.pickle` into
   `/mnt/fast/safety/phase07/fence/` (mkdir -p) and record `sha256sum` of source and copy (equal;
   the fixture values). Also assert the snapshot's own view holds the SAME bytes (G-04):
   `sha256sum /mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/beets/config/library.db
   /mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/beets/config/state.pickle` — each must EQUAL
   the corresponding safety copy's sha256 (compared as strings, recorded side by side); a mismatch
   or an unreadable `.zfs/snapshot` path is a STOP (the file-restore remedy D-16 relies on would
   restore different bytes). `ls -l` is recorded for size/owner only, never as the proof.
   If ANY step of 2 fails: STOP; the grant is not deployed; record; ledger `STOP STATE
   07-09-PARTIAL-FENCE: …` when that is the shape of the failure, otherwise `STOP STATE
   07-09-FENCE-FAIL: <step and reading>; <pre-07-pilot names present, as listed after the
   failure>; no grant; albums landed: none`.

3. **THE GRANT (E3, first write on the library path; on `resume-post-grant` this step runs in full
   — the install is idempotent and `up -d` recreates the stopped service).** **Stopping the
   writer**, wherever a STOP below says so: `docker compose -f stacks/selfhosted/arrs/compose.yaml
   stop <service>` (reversible, touches no file), then assert `docker inspect -f
   '{{.State.Running}}' beets-flask` reads `false` and record `WRITER STOPPED:
   State.Running=false`. Every post-grant STOP below is re-entered through Task 1's post-grant
   reconciliation and `resume-post-grant` (D-18, P7R2-04). On LXC 100: install the repo files over the
   appdata copies preserving the task-1 modes/owners (`install -m <mode> -o <uid> -g <gid>`), i.e.
   `stacks/selfhosted/arrs/beets/config.yaml` → `/mnt/fast/appdata/arrs/beets/config/config.yaml`
   and `stacks/selfhosted/arrs/beets/flask-config.yaml` →
   `/mnt/fast/appdata/arrs/beets/config/beets-flask/config.yaml`; assert each sha256 equals the
   repo file (C9). Then `docker compose -f stacks/selfhosted/arrs/compose.yaml up -d` for the
   beets-flask service only (resolve the service name from `docker compose -f … config --services`
   first and record it). An install sha256 mismatch, a compose error, or `test -r
   /config/config.yaml` not succeeding within the readiness bound is a STOP: stop the writer if it is running; ledger `STOP STATE 07-09-GRANT-FAIL: <step>; fence
   held, writer stopped; no album landed`. Readiness: poll (bounded, 180 s) until BOTH `docker exec -u abc beets-flask
   test -r /config/config.yaml` succeeds AND a NEW `Registering watchdog … for inboxes: [...]` line
   appears after the recreate timestamp; assert that line lists exactly `02-review` and `03-asis`
   (two) and not `01-auto` (C4, D-21). Assert `docker inspect beets-flask` shows
   `/mnt/tank/media → /media RW=true` (D-22) and `/config/config.yaml` still RW=false. Assert the
   effective import keys by running `bash scripts/check-beets-config.sh` from the workstation and
   recording its exit and the `import.copy/move/write` readings — this is the post-grant C1
   reading, and it is a GATE (G-05), not a record: parse each effective value (the YAML boolean
   read from the effective config, not a substring of the output) and require `copy` = true,
   `move` = false, `write` = true; write `POST-GRANT IMPORT KEYS: PASS (copy=true move=false
   write=true)` or `POST-GRANT IMPORT KEYS: FAIL — <key>=<value>`. On FAIL, and on any reading
   that could not be taken: STOP; stop the writer so no staged folder can be imported under `move`;
   record; ledger `STOP STATE 07-09-IMPORT-KEYS-FAIL: fence held, grant deployed but writer
   stopped, no album landed`; the operator decides. Then RE-COUNT inbox emptiness now, after
   `compose up` (G-05): from atlantis, audio-file counts under `_inbox/01-auto`, `_inbox/02-review`,
   `_inbox/03-asis` — all must be 0; write `POST-GRANT INBOX RECOUNT: PASS (01-auto 0, 02-review 0,
   03-asis 0)` or `POST-GRANT INBOX RECOUNT: FAIL — <counts>`; a FAIL is a STOP handled the same
   way (writer stopped, ledger `STOP STATE 07-09-INBOX-NOT-EMPTY: <counts>; fence held, grant
   deployed, writer stopped; no album landed`). If the watchdog line is absent or lists fewer than
   two: STOP — the fence holds; do not revert by hand; stop the writer; record; ledger `STOP STATE
   07-09-WATCHDOG-ABSENT: <the line read, or none>; fence held, grant deployed, writer stopped; no
   album landed` (this STOP had no token before P7R2-04); the operator decides (a rejected config
   kills the watchdog while the UI keeps serving).

4. Run `bash scripts/quick-health-check.sh` END TO END from the workstation once (DEF-06-39-05).
   Record its exit code and every block's one-line verdict. Expected: D-23 arm ✅ RW=true
   deliberately; vendored-drift ✅ (repo = appdata); import sweep ⚠️ UNKNOWN (library empty — the
   vacuity guard, correct); consumers ⚠️ exit 3 (CONF-04 pending); D-04 register green naming the
   exempt count; overall exit 1 for those two could-not-look / pending reasons. Any OTHER red is a
   finding written to the phase `deferred-items.md` — and if it is in the D-03, drift, freeze or
   D-04 block, STOP before any import: stop the writer, record, and write the ledger `STOP STATE
   07-09-QHC-RED: <block and verdict>; fence held, grant deployed, writer stopped; no album
   landed`.

5. Write the success entry (P7R2-08b — a named token, not a borrowed "STOP state" header; it is
   the token plan 07-10 gates on, P7R2-05): ledger `STATE 07-09-GRANTED: fence listed back (<both
   names, creation times>), grant deployed (<commit>), POST-GRANT IMPORT KEYS PASS, inboxes 0, qhc
   exit <n>; albums landed: none`, copied into the run section as its `LEDGER:` line.
Commit the artifacts: `docs(07-09): two-dataset fence taken, D-22 grant deployed (D-16, E3)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-09-fence-and-grant.txt && L=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt && test -s "$L" && test "$(/usr/bin/grep -cE 'zfs [r]ollback' "$A")" -eq 0 && test "$(/usr/bin/grep -cE 'zfs [d]estroy' "$A")" -eq 0 && R="$(/usr/bin/awk '/^## RUN /{b=""} {b=b $0 "\n"} END{printf "%s", b}' "$A")" && D="$(printf '%s' "$R" | /usr/bin/grep -E '^DISPOSITION: ' | tail -n 1 | sed 's/^DISPOSITION: //')" && T="$(LC_ALL=C /usr/bin/grep -oE '^(STOP )?STATE 07-09-[A-Z][A-Z-]*[A-Z]' "$L" | tail -n 1 | /usr/bin/grep -oE '07-09-[A-Z-]+$')" && test -n "$T" && case "$D" in already-granted) test "$T" = 07-09-GRANTED ;; hold|review-first|proceed|grant-both|resume-partial-fence|resume-post-grant) printf '%s' "$R" | /usr/bin/grep -qE "^LEDGER: (STOP )?STATE $T:" && case "$D:$T" in hold:07-09-HOLD|review-first:07-09-REVIEW-FIRST) true ;; hold:*|review-first:*) false ;; *:07-09-GRANTED) printf '%s' "$R" | /usr/bin/grep -qF 'listed back' && printf '%s' "$R" | /usr/bin/grep -qF 'tank/media/Music@pre-07-pilot' && printf '%s' "$R" | /usr/bin/grep -qF 'fast/appdata/arrs@pre-07-pilot' && printf '%s' "$R" | /usr/bin/grep -qE 'RW=true' && printf '%s' "$R" | /usr/bin/grep -qF 'Registering watchdog' && printf '%s' "$R" | /usr/bin/grep -qxF 'POST-GRANT IMPORT KEYS: PASS (copy=true move=false write=true)' && printf '%s' "$R" | /usr/bin/grep -qE '^POST-GRANT INBOX RECOUNT: PASS' && printf '%s' "$R" | /usr/bin/grep -qF 'quick-health-check' ;; *:07-09-IMPORT-KEYS-FAIL) printf '%s' "$R" | /usr/bin/grep -qE '^POST-GRANT IMPORT KEYS: FAIL' && printf '%s' "$R" | /usr/bin/grep -qxF 'WRITER STOPPED: State.Running=false' ;; *:07-09-INBOX-NOT-EMPTY) printf '%s' "$R" | /usr/bin/grep -qE '^POST-GRANT INBOX RECOUNT: FAIL' && printf '%s' "$R" | /usr/bin/grep -qxF 'WRITER STOPPED: State.Running=false' ;; *:07-09-GRANT-FAIL|*:07-09-WATCHDOG-ABSENT|*:07-09-QHC-RED) printf '%s' "$R" | /usr/bin/grep -qxF 'WRITER STOPPED: State.Running=false' ;; *:07-09-FF-FAIL|*:07-09-FENCE-FAIL|*:07-09-PARTIAL-FENCE) true ;; *) false ;; esac ;; *) false ;; esac</automated>
  </verify>
  <acceptance_criteria>
    - Every outcome wrote exactly one named 07-09 token at the start of a ledger line and copied it into this run's section as `LEDGER: …`; the verify reads the ledger's NEWEST 07-09 token and this run's `DISPOSITION:`, never ledger history (P7R2-01) — a historical `07-09-PARTIAL-FENCE` cannot pass a resumed run, whose `STATE 07-09-GRANTED` must carry the full success evidence.
    - On hold/review-first: `STOP STATE 07-09-HOLD` / `07-09-REVIEW-FIRST` states the reconciled state truthfully (never "no fence taken" while a snapshot exists, P7R2-19); the transcript shows no write verb.
    - Every post-grant STOP (`GRANT-FAIL`, `IMPORT-KEYS-FAIL`, `INBOX-NOT-EMPTY`, `WATCHDOG-ABSENT`, `QHC-RED`) recorded `WRITER STOPPED: State.Running=false` and is re-enterable through Task 1's post-grant reconciliation and `resume-post-grant` (P7R2-04); on `resume-post-grant` nothing is snapshotted and both names are re-listed by exact whole-line match.
    - On success: `STATE 07-09-GRANTED: …` (a named success token, not a "STOP state" header — P7R2-08b) with `POST-GRANT INBOX RECOUNT: PASS` in the run section.
    - On proceed/grant-both: host `HEAD` == workstation `HEAD` after `pull --ff-only`; both snapshots taken in ONE ssh invocation, each preceded by an absent-assertion and followed by an exact whole-line listed-back PASS, with both creation times recorded and the order stated.
    - `library.db` / `state.pickle` copies in `/mnt/fast/safety/phase07/fence/` with sha256 equal to their sources AND to the snapshot's own `.zfs/snapshot/pre-07-pilot/beets/config/` view of each (sha256, side by side; `ls -l` is not the proof).
    - A partial fence (tank taken, fast failed) is recorded as `STOP STATE 07-09-PARTIAL-FENCE` with no grant and no destroy; on `resume-partial-fence` only the missing `fast` snapshot is taken, after `written` on `tank/media/Music` read 0.
    - `POST-GRANT IMPORT KEYS: PASS (copy=true move=false write=true)` compared by parsed value, or a FAIL that stopped the writer and wrote `STOP STATE 07-09-IMPORT-KEYS-FAIL`; the three inboxes re-counted as 0 AFTER `compose up`.
    - The D-16 prohibition appears at the fence call site, written `zfs [r]ollback`; the unbracketed rollback and destroy verbs appear 0 times in the transcript (recipes driven against controls in both directions first: a line with the real verb → 1, a line with only the bracketed form → 0).
    - Appdata installs sha256-equal to the repo; watchdog line lists exactly two inboxes, no `01-auto`; `/media` RW=true and `/config/config.yaml` RW=false on beets-flask; `check-beets-config.sh` post-grant readings recorded.
    - `quick-health-check.sh` run end to end, exit code and every block verdict recorded; any unexpected red filed or stopped on as stated.
    - `tank/downloads@pre-phase5` still present (listed after); no import has run (item count still 0).
  </acceptance_criteria>
  <done>The library sits behind a listed-back two-dataset fence and is writable by beets-flask only, with the routine health check reading the new state as deliberate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → atlantis as real root | `zfs` on pools every service depends on. |
| workstation → origin → LXC 100 checkout | The deploy path for compose and scripts. |
| beets-flask → `/media` (now rw) | The first writable library mount. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering / DoS | `fast/appdata/arrs` (Sonarr/Radarr/Lidarr state) | mitigate | `zfs [r]ollback` forbidden on every branch, written at the call site; recovery only by file copy from `.zfs/snapshot/`; the unbracketed verb asserted absent from the transcript by a recipe driven both ways. |
| T-07-09-02 | Tampering | grant without fence | mitigate | Fence and grant in one plan, fence strictly first, both names listed back by exact match before `docker compose up`; any fence failure stops before the grant. |
| T-07-09-03 | Tampering | overwriting an existing snapshot name / partial fence | mitigate | Fail-if-exists assertion per name; RC captured per snapshot; C11 order and creation times recorded; a partial fence is its own ledger state, never destroyed by the executor, resumed only on the operator's `resume-partial-fence` after a CONSISTENT reconciliation (`written` = 0). |
| T-07-09-09 | Tampering | grant deployed with an effective `import.move: yes` | mitigate | Post-grant `copy/move/write` gated by parsed value; FAIL stops the writer (reversible) and holds; inboxes re-counted after `compose up`. |
| T-07-09-10 | Repudiation | snapshot view differs from the safety copy | mitigate | sha256 of the snapshot's `.zfs/snapshot/` view compared with the safety copy before the grant; mismatch is a STOP. |
| T-07-09-11 | Repudiation | a historical stop token passing a later run's verify (P7R2-01) | mitigate | Named token per outcome; per-run `## RUN` sections; Task 2's verify keyed on this run's `DISPOSITION:` and the ledger's NEWEST 07-09 token, with the success arm requiring every grant reading from this run's section. |
| T-07-09-04 | Elevation of privilege | `01-auto` resurrected by a stale appdata config | mitigate | Appdata installs sha256-asserted equal to repo; watchdog line asserted to list exactly two inboxes. |
| T-07-09-05 | Denial of service | a rejected config silently killing the watchdog | mitigate | Readiness gated on the NEW registration line after recreate, never on `.State.Status`; absence is a STOP. |
| T-07-09-06 | Tampering | host checkout divergence | mitigate | Host must be clean and 0-ahead before `pull --ff-only`; failure is a stop before any fence. |
| T-07-09-07 | Repudiation | a grant nobody authorised | mitigate | `checkpoint:decision`, blocking; answer recorded verbatim with UTC time; any other answer is `hold`; `resume-partial-fence` offered only after a CONSISTENT reconciliation. |
| T-07-09-08 | Tampering | `tank/downloads@pre-phase5` | mitigate | Listed before and after; out of scope on every branch. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- Gate answered with measured state, reconciled against the GOVERNING 07-09 token.
- On proceed: fence listed back, then grant; watchdog two inboxes; RW=true; import keys and inbox re-count PASS; qhc run end to end recorded; `STATE 07-09-GRANTED` written.
- Every stop wrote its own named 07-09 token and passes Task 2's verify; every post-grant stop left the writer stopped and is re-enterable (P7R2-04).
</verification>

<success_criteria>
- The library becomes writable only behind a proven two-dataset fence, with every coupling that moves with the grant observed in its new state.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-09-SUMMARY.md` when done.
</output>
