---
phase: 07-pilot-12-albums-end-to-end
plan: 17
type: execute
wave: 13
depends_on: ["07-16"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-17-evidence.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
  - .planning/phases/07-pilot-12-albums-end-to-end/07-TRUST-VERDICT.md
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
  - stacks/selfhosted/arrs/beets.md
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: false
requirements: [IMPT-01, IMPT-02, CONS-04, QUAL-02, QUAL-03, QUAL-04]

must_haves:
  truths:
    - "Criterion 4 / QUAL-02: ONE field-level diff over EVERY pilot file — the concatenated `pre-07-pilot` BEFORE against a `post-07-pilot` AFTER captured over every landed album's FINAL location — is recorded with exit code, AMBIGUOUS counts, the empty-BEFORE counter, every field gained and every field dropped with its reason"
    - "Criterion 3: an ffprobe-vs-beets table for every landed file plus ownership read from atlantis"
    - "Criterion 7 / IMPT-02: `check-music-import.sh` over the final library, exit and findings with their resolution"
    - "Criterion 8: every source folder's manifest re-taken from atlantis and compared with 07-07's, file by file"
    - "Criterion 2: commit ancestry proves `07-SAMPLE.md` preceded the first import artifact"
    - "D-31: the evidence map's 'measured' column is filled with measurements ONLY — no plan writes a verdict; `/gsd-verify 07` scores the eight criteria once; the operator's separate TRUST VERDICT is recorded verbatim in `07-TRUST-VERDICT.md`; the single budgeted code review is recorded as run (at 07-09) or as the one next step"
    - "D-18: the ledger's final STOP STATE names every album that landed and every album held"
    - "beets.md gains a Phase 7 closure section and its head pointer is updated IN THE SAME COMMIT; CLAUDE.md/PROJECT.md's bare 'beets has no undo' claim (DEF-04-01) is amended with what plan 07-11 measured; C1's measured `import.copy` state replaces the stale `import.move: yes`; stale figures corrected only with measured numbers and their dates"
    - "REQUIREMENTS.md is untouched — the CONF-04 box stays unticked, and no Phase 7 box is ticked by a plan"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-17-evidence.txt"
      provides: "The assembled per-criterion measurements"
      contains: "criterion 8"
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/07-TRUST-VERDICT.md"
      provides: "The operator's recorded answer to 'do you believe this flow?'"
      contains: "TRUST VERDICT"
    - path: "stacks/selfhosted/arrs/beets.md"
      provides: "Phase 7 closure section + updated head pointer"
      contains: "Phase 7"
  key_links:
    - from: "07-EVIDENCE-MAP.md measured column"
      to: "artifacts/07-17-evidence.txt sections"
      via: "one measurement per criterion row"
      pattern: "measured"
---

<objective>
Assemble the evidence for `/gsd-verify 07` without scoring it, record what the phase leaves open,
correct the documents this phase has now falsified or measured, and ask the operator the one
question the criteria cannot answer: do you believe this flow?

Purpose: D-31 — the deliverable is trust, not a count, and making the trust verdict a written
artifact is the whole difference; verification scores the criteria once, separately. Every
measurement here is a read: the AFTER capture and the manifests read files; the sweep reads the DB
read-only. No import, no `zfs` write, no MA or Jellyfin write happens in this plan.

Document corrections are limited to claims this phase measured: the "beets has no undo" constraint
(DEF-04-01 assigned it to Phase 7, which owns "undo exercised"), the `import.move: yes` statement
(C1), and the stale backlog/DJ figures (CONTEXT § Deferred: correct them where a plan already
touches the file, never restate an unverified number).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
@.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: assemble the eight criteria's measurements over the final state</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-17-evidence.txt, .planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `07-EVIDENCE-MAP.md` (the pre-registered instrument per criterion — use exactly those).
    - Every Phase 7 artifact 07-09 … 07-16 and the ledger.
    - `artifacts/07-07-source-manifest-before.tsv`.
    - `scripts/diff-music-tags.sh`, `scripts/snapshot-music-tags.sh`, `scripts/check-music-import.sh`.
  </read_first>
  <action>
All reads bounded, RCs recorded. Under headed sections of `07-17-evidence.txt`:
- `criterion 1`: quote the fence listing (07-09) and the three COVERED verdicts + re-run equality
  (07-11); re-list both `pre-07-pilot` snapshots now (exact whole-line).
- `criterion 2`: `git merge-base --is-ancestor <07-SAMPLE.md add commit> <07-10 artifact add commit>`
  exit code; S2/S3 paths.
- `criterion 3`: for EVERY landed file, ffprobe vs beets DB (read-only sqlite) for title, artist,
  album, albumartist, track, disc, MusicBrainz album id where matched, albumtype for DJ; owner from
  atlantis; totals: files, mismatches, non-`568:568`.
- `criterion 4`: `snapshot-music-tags.sh post-07-pilot` over every landed album's FINAL directory
  (one invocation per directory, same BASENAME); record count = total landed files; then
  `diff-music-tags.sh` of the concatenated `pre-07-pilot` restricted to LANDED albums' per-album
  BEFOREs (concatenate only the landed `pre-07-PNN` files — a held album would otherwise read as
  MISSING_AFTER) vs `post-07-pilot`, text and `--json`: exit, MATCHED, MISSING_AFTER, AMBIGUOUS
  BEFORE/AFTER, empty-BEFORE counter, and the full gained / changed / dropped lists, every dropped
  field with its reason (carried from the per-album plans) or `UNEXPLAINED`.
- `criterion 5`: pointer + quoted verdict lines from 07-11.
- `criterion 6`: the two tables from 07-15 quoted, plus the D-28 lines.
- `criterion 7`: `bash /mnt/fast/stacks/scripts/check-music-import.sh` now: exit, summary, each
  finding with its resolution (or "unresolved").
- `criterion 8`: source manifests re-taken from atlantis for all twelve folders; file-by-file
  comparison with 07-07's (sha256 + size); count identical / changed / missing.
- `open items`: E2 (bootleg untested by import), E4 (`@pre-phase5` operator-gated), E6 (first and
  second measurements, each with its own disposition), D-02/D-03 (archive phase and DUPE move not
  enacted — `/gsd-phase` work), deferred DJ key/energy generation, every `DEF-07-*` finding.
Fill ONLY the `measured` column of `07-EVIDENCE-MAP.md` with a pointer to each section and the raw
measured values (exit codes, counts). Write no PASS/FAIL/satisfied word in it. Update the ledger's
final `## STOP STATE` (albums landed; albums held; E5). Commit:
`docs(07-17): assemble Phase 7 evidence for /gsd-verify (no verdict written)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-17-evidence.txt && for n in 1 2 3 4 5 6 7 8; do /usr/bin/grep -qF "criterion $n" "$A" || exit 1; done && /usr/bin/grep -qF 'open items' "$A" && M=.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md && ! /usr/bin/grep -qiE '\| *(PASS|FAIL|SATISFIED|VERIFIED) *\|' "$M" && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Eight criterion sections with measurements from the instruments the map named; the all-pilot QUAL-02 diff recorded in full; the source-manifest comparison file by file.
    - The evidence map's instrument and "satisfies when" columns byte-unchanged from its first commit (compare the rows with `git show <map commit>:…`); only the measured column filled, with no verdict word.
    - Ledger's final STOP STATE lists landed and held albums and E5.
    - `REQUIREMENTS.md` unchanged.
  </acceptance_criteria>
  <done>Everything `/gsd-verify 07` needs is measured and pointed to, and nothing is scored.</done>
</task>

<task type="auto">
  <name>Task 2: the beets.md Phase 7 closure section with its pointer, and the measured corrections to PROJECT.md / CLAUDE.md</name>
  <files>stacks/selfhosted/arrs/beets.md, .planning/PROJECT.md, CLAUDE.md</files>
  <read_first>
    - `stacks/selfhosted/arrs/beets.md` — the head pointer blockquote and its go-forward rule; the `## Phase 6 closed 2026-09-21 — …` section's sub-section shape (`### The five criteria`, `### Still open at Phase 6 close`, `### How to re-run the Phase 6 evidence`); § "One correction to this page's own § The recovery fence"; the DEF-04-01 bullet.
    - `.planning/PROJECT.md` and `CLAUDE.md` — find by text: ``beets has no `undo` command``, `import.move: yes`, the 144-folder figure, the `~46` DJ baseline; check whether CLAUDE.md's Project section is a generated mirror (GSD markers) of PROJECT.md.
    - `artifacts/07-11-p01-undo-rerun.txt` (COVERED verdicts), `artifacts/07-08-d22-measure.txt` (C1), `07-SAMPLE.md` (measured population), and the 2026-09-22 archive inventory in `07-CONTEXT.md` D-01.
    - `scripts/quick-health-check.sh` `D04_INV_RE` (prose must stay non-invocation-shaped; `D04_DOC_BASELINE` stays 2).
  </read_first>
  <action>
beets.md: append `## Phase 7 pilot — <date>: <N> albums landed, undo exercised on tree/DB/state (<verdicts>)`
(heading states measured facts, no verdict word like "passed") with sub-sections `### The eight
criteria — evidence, not verdicts` (one line each pointing at `07-17-evidence.txt`), `### Still open
at Phase 7 close` (the open items list), `### How to re-run the Phase 7 evidence` (the instrument
commands by script name and flags, phrased so the D-04 scan does not count them), and
`### What changed in the operating model` (media rw by D-22/D-23, `01-auto` de-registered until
Phase 8, D-08 routing script, the sweep, the snapshot register). In the SAME commit update the head
pointer blockquote to name this section and its sub-sections by heading text. Amend the § "One
correction to this page's own § The recovery fence" paragraph with a dated line stating what 07-11
measured about `state.pickle`.

PROJECT.md and CLAUDE.md (edit both identically; if CLAUDE.md's section is a generated mirror, edit
within its markers to match PROJECT.md): (1) after the bare "beets has **no `undo` command**" text,
add a dated qualification: true of the CLI; beets-flask rc6 `UNDO IMPORT` measured by plan 07-11 as
covering tree <yes/no>, database <yes/no>, `state.pickle` <yes/no> — close DEF-04-01 by naming
this; (2) replace the "`import.move: yes` is currently set" claim with the C1 measurement (repo,
appdata, effective values and date), keeping the old sentence as a dated retraction; (3) the backlog
denominator: add the measured live figure from `07-SAMPLE.md` with its date beside the 144 (do not
delete the dated 144); (4) the DJ baseline: add "450 distinct DJ releases measured in
`mybook-music-archive` on 2026-09-22 (07-CONTEXT D-01)" beside `~46`, stating they measure different
things (MusicBrainz entries vs releases held). Change nothing else in either file.
Confirm `git grep -n -I -i beet HEAD -- '*.md'` through `D04_INV_RE` still yields the documentation
count 2 after the commit. Commit: `docs(07-17): Phase 7 closure in beets.md + measured corrections (DEF-04-01, C1)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && /usr/bin/grep -qE '^## Phase 7 pilot — ' stacks/selfhosted/arrs/beets.md && /usr/bin/grep -qF '### Still open at Phase 7 close' stacks/selfhosted/arrs/beets.md && /usr/bin/awk 'NR<=60' stacks/selfhosted/arrs/beets.md | /usr/bin/grep -qF 'Phase 7 pilot' && /usr/bin/grep -qF 'UNDO IMPORT' .planning/PROJECT.md && /usr/bin/grep -qF 'UNDO IMPORT' CLAUDE.md && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The closure section exists with the four sub-sections, and the head pointer (within the file's first 60 lines) names it by heading text in the same commit.
    - PROJECT.md and CLAUDE.md carry the dated undo qualification with 07-11's three measured verdicts, the C1 measurement with the old sentence retracted in place, and the two figures with sources and dates; no other line in either file changed (diff body read).
    - The D-04 documentation count is still 2.
  </acceptance_criteria>
  <done>The durable record reflects what Phase 7 measured, with the pointer rule honoured.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: the operator's trust verdict — "do you believe this flow?" — and consent to deploy the final instruments</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/07-TRUST-VERDICT.md</files>
  <read_first>
    - `artifacts/07-17-evidence.txt` (all eight sections and open items), the ledger, `07-EVIDENCE-MAP.md`.
    - `07-CONTEXT.md` D-31 and § specifics (the operator's own standard: trust, not a count).
  </read_first>
  <action>
Present a one-screen summary: albums landed / held; per criterion the measured values (no verdict
words); what surprised (every `DEF-07-*` finding); the open items; the D-23 accepted consequence
(the library is now writable for good; the snapshot fence is the primary control); whether the one
budgeted code review ran (07-09) or is the single next step. Ask the question verbatim: "Do you
believe this flow — would you run Phase 9's batches through it?" Offer `hold` first. Also ask
whether to deploy the final instrument versions to LXC 100 (push + host `pull --ff-only`), since
plans 07-15/07-17 changed committed files after 07-09's deploy.

After the answer: write `07-TRUST-VERDICT.md` with heading `# Phase 7 TRUST VERDICT`, the question,
the operator's answer VERBATIM with UTC time, any reservations they name (each as its own line), and
a closing note that this is separate from `/gsd-verify 07`, which scores the eight criteria once and
is the next step. If deploy was consented: `git push origin main`, host `git -C /mnt/fast/stacks
pull --ff-only`, assert host HEAD == workstation HEAD, record it in the verdict file's appendix.
Commit: `docs(07-17): record the operator's trust verdict (D-31)`.
  </action>
  <decision>Do you believe this flow — would you run Phase 9's batches through it?</decision>
  <context>The criteria are scored separately and once by /gsd-verify 07. This question is the phase's stated deliverable. Reservations are recorded as named lines, not smoothed.</context>
  <options>
    <option id="hold">
      <name>Not ready to answer</name>
      <pros>No verdict is manufactured.</pros>
      <cons>The phase's deliverable stays unrecorded.</cons>
    </option>
    <option id="believe">
      <name>I believe it</name>
      <pros>Recorded as the operator's trust.</pros>
      <cons>None.</cons>
    </option>
    <option id="believe-with-reservations">
      <name>I believe it, with named reservations</name>
      <pros>Trust with its limits on the record.</pros>
      <cons>Each reservation becomes an open item for Phase 8/9 planning.</cons>
    </option>
    <option id="do-not-believe">
      <name>I do not believe it (say why)</name>
      <pros>An honest negative is a result.</pros>
      <cons>Phase 9 should not start until the reason is addressed.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold`, `believe`, `believe-with-reservations: <list>`, or `do-not-believe: <why>`, plus `deploy: yes|no`. A missing deploy answer is `no`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && V=.planning/phases/07-pilot-12-albums-end-to-end/07-TRUST-VERDICT.md && test -s "$V" && /usr/bin/grep -qF '# Phase 7 TRUST VERDICT' "$V" && /usr/bin/grep -qF 'Do you believe this flow' "$V" && /usr/bin/grep -qF '/gsd-verify 07' "$V" && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `07-TRUST-VERDICT.md` holds the question, the verbatim answer with UTC time, each reservation on its own line, and the separation-from-verify note.
    - Deploy performed only on `deploy: yes`, with host HEAD == workstation HEAD recorded.
    - `REQUIREMENTS.md` unchanged; no verification verdict written anywhere by this plan.
  </acceptance_criteria>
  <done>The operator's trust verdict is a written artifact, separate from the criteria's scoring, and `/gsd-verify 07` is the next step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence assembly → the verifier | A self-scored plan would pre-empt `/gsd-verify`. |
| docs → every future session | CLAUDE.md and PROJECT.md are loaded as instructions. |
| workstation → origin → LXC 100 | Final deploy of instruments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17-01 | Repudiation | self-declared verification | mitigate | Evidence map's measured column only; verdict words asserted absent; REQUIREMENTS.md unchanged under `HEAD --`. |
| T-07-17-02 | Tampering | laundering unverified numbers into CLAUDE.md | mitigate | Only figures measured in this phase or with a named dated source; old sentences kept as dated retractions. |
| T-07-17-03 | Tampering | D-04 documentation pin | mitigate | Prose phrased non-invocation-shaped; count re-measured = 2. |
| T-07-17-04 | Tampering | library during evidence capture | mitigate | Reads only (snapshot script read-only, sqlite `mode=ro`, manifests via `sha256sum`); no zfs/MA/Jellyfin write. |
| T-07-17-05 | Repudiation | deploy without consent | mitigate | Push/pull only on an explicit `deploy: yes` in a blocking gate. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- Eight criteria measured; evidence map measured column filled without verdicts.
- beets.md closure + pointer in one commit; PROJECT.md/CLAUDE.md corrections measured and dated.
- Trust verdict recorded verbatim; REQUIREMENTS.md untouched.
</verification>

<success_criteria>
- `/gsd-verify 07` can score the eight criteria from committed evidence, and the operator's belief in the flow is on the record as its own artifact.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-17-SUMMARY.md` when done.
</output>
