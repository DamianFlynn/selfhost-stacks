---
phase: 07-pilot-12-albums-end-to-end
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-beets-config.sh
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-rule2-assertion.txt
autonomous: true
requirements: [IMPT-01]

must_haves:
  truths:
    - "D-30 / E12: the first real `phase06-oracle.sh --run` happens BEFORE the D-22 grant (couplings C7 `assert_media_readonly` and C8 `FIXTURE_LIB_SHA256` both go red after `rw` / first import), and it confirms: both `layer3.before`/`layer3.after` parse with both awk keys matching; the destructive fence REFUSES end to end under the container's `dash`; no `/tmp/p6-mf.*` or `/tmp/p6-taghist.*` survives"
    - "D-30 rider (DEF-06-29-04): `wc -c` of the live `$WORKDIR/arm1.dump` is recorded from a live `check-beets-config.sh` run — the margin GC-01 was measured against, never before written down"
    - "D-09 / E1 / DEF-06-12-01: DJ path rule 2 (`albumtype:=dj disctotal:2..`) is asserted by a named READ-ONLY class assertion — `beet move -p` over one of the seven named `disctotal=2` `dj-mixes` folders in a THROWAWAY library with `albumtype=dj` set — and the rendered paths are checked for the disc-prefixed flat shape; nothing is imported into the real library and the drawn sample is untouched"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt"
      provides: "The first real oracle --run, the four E12 confirmations, the dash canary drive and the arm1.dump size"
      contains: "arm1.dump"
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-rule2-assertion.txt"
      provides: "The rule-2 rendered-path class assertion"
      contains: "DJ/"
  key_links:
    - from: "phase06-oracle.sh --run layer 1 / layer 3"
      to: "the pre-grant estate (RW=false, library.db at fbbdde0c…)"
      via: "sequencing: this plan's run precedes plan 07-09's grant"
      pattern: "RW=false"
---

<objective>
Exercise, for the first time, the Phase 6 hardenings that only a live oracle run can exercise
(D-30 / E12), size the arm-1 dump (DEF-06-29-04), and evaluate the one `paths:` rule no instrument
has ever evaluated (D-09) — all while the library is still `:ro` and `library.db` is still at its
recorded empty-library fixture.

Purpose: C7 and C8 make this a sequencing constraint, not a preference: `phase06-oracle.sh`'s
layer 1 (`assert_media_readonly`) and its `FIXTURE_LIB_SHA256` check both go red the moment
plan 07-09 grants `rw` or the first album lands. Run after the grant, the oracle could only report
a red it was designed to report; run before, it can confirm its four fail-closed constructions on
real material. All four fail CLOSED, which is why they are residue and not blockers — but D-30
still requires the confirmation. D-09 closes what plan 06-11 was told to do and did not: rule 2 is
where a `-1 - ` or `00-01` rendering defect would hide (Phase 3 found one in the retired tagger).

Nothing here writes to `/mnt/tank`. The oracle and the D-09 assertion import only into throwaway
libraries inside `beets-flask` under `/tmp/p6-*` with a `-c` overlay forcing `import.write: no`,
`import.copy: no`, `import.move: no`; the real `library.db` / `state.pickle` hashes are read before
and after (layer 3). ⛔ If `/media` already reads RW=true, or `library.db` is not at
`fbbdde0c…`, this plan STOPS — the grant or an import has happened out of order and the phase's
sequencing is broken; record and escalate, do not adapt.

Output: two artifacts and one line added to `check-beets-config.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the first real `phase06-oracle.sh --run`, pre-grant, with the four E12 confirmations and a dash canary drive</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt</files>
  <read_first>
    - `scripts/phase06-oracle.sh` header (usage, EXIT CODES, PRECEDENCE, THE OVERRIDE CONTRACT, THE THREE-LAYER "WROTE NOTHING" PROOF), the `DESTRUCTIVE-KNOB FENCE`, `SCRATCH_FENCE_SH`, `CLEANUP_PROG`, `remote_sh_c`, `dex_cmd`, `rsh_classify`, `assert_media_readonly`, the layer-3 `layer3.before` / `layer3.after` awk extraction, `FIXTURE_LIB_SHA256`, and the R4-10 comment on which `exit 3` arms skip step 12.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-29-05` (the four hardenings) and `DEF-06-39-02` (no cleanup trap, deliberately).
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-11-oracle-run.txt` — the last real run's shape.
    - `.planning/ROADMAP.md` Phase 7 entry criterion E12 (read by heading text).
  </read_first>
  <action>
Preconditions, each recorded with command and ssh RC (bounded Linux-side, `set -o pipefail` in
piped remote strings): (a) `docker inspect beets-flask` shows `/mnt/tank/media → /media` with
`RW=false`; (b) `docker exec beets-flask sha256sum /config/library.db /config/state.pickle` equals
`fbbdde0c416e72b9884e56c562fd88eaa4da447926e1cb6cc17c3e04aee7da1a` /
`f6a9a1ad7aa553e42e53a8056fa80724785111180a5e2122be4f8732d1e4bc7c`; (c) the container's `/bin/sh`
resolves to dash (`docker exec beets-flask readlink -f /bin/sh`). If (a) or (b) fails → STOP as
stated in the objective.

Run `bash scripts/phase06-oracle.sh --self-test` (record exit and case count), then
`bash scripts/phase06-oracle.sh --run` from the workstation repo root. Record the full transcript
(ANSI-stripped) and the exit code. The oracle's own verdict (0/1/3) is RECORDED, not gated on — it
judges the Phase 6 expected tree, and a red is a finding for `deferred-items.md`, not a reason to
retry.

Then the four E12 confirmations, each an explicit line in the artifact:
1. `layer3.before` and `layer3.after` exist in the run's `$OUT` directory and BOTH awk keys
   (`LIB_SHA_*`, `STATE_SHA_*`) extracted non-empty values on both — quote the four hashes.
2. The fence REFUSES end to end under the container's dash — drive it with a CANARY so a fence
   that fails open deletes only the canary: inside the container create `/tmp/p7-canary/` holding a
   sentinel file `keep`; build the exact `CLEANUP_PROG` text by extracting `SCRATCH_FENCE_SH` and
   `CLEANUP_PROG` from the committed file by symbol (record the sha256 of the extracted program
   text) and run it through the file's own `remote_sh_c` + `dex_cmd` construction with these
   arguments, one at a time: `/tmp/p6-x/../p7-canary`, `/tmp/p7-canary`, `/tmp/p6 /tmp/p7-canary`,
   and the empty string. Each must print `REFUSED` and exit 3, and after all four the sentinel
   `/tmp/p7-canary/keep` must still exist. Then one positive: create empty `/tmp/p6-p7probe` and
   run the program on it → prints `gone`, proving the program executes under dash at all. Finally
   remove `/tmp/p7-canary` with a plain `rm -rf /tmp/p7-canary` issued as a literal (no variable).
3. After the run, list `ls -d /tmp/p6-mf.* /tmp/p6-taghist.* 2>/dev/null` on LXC 100 AND inside
   the container (`docker exec beets-flask sh -c 'ls -d /tmp/p6-mf.* /tmp/p6-taghist.* 2>/dev/null'`)
   and on the workstation's temp directory; each must list nothing. Drive the listing recipe
   first against a deliberately created `/tmp/p6-mf.control` on LXC 100 (observed listed), then
   remove the control by its literal name.
4. GC-15's space-bearing `REAL_LIB_DB` prediction stays a prediction — do NOT drive it against the
   real container (it would need a real path with a space). Record it as "not driven; predicted
   UNKNOWN + exit 3" so it is not mistaken for confirmed.

Re-read the real `library.db` / `state.pickle` hashes after everything and assert them unchanged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt && test -s "$A" && /usr/bin/grep -qF 'layer3.before' "$A" && /usr/bin/grep -qF 'layer3.after' "$A" && /usr/bin/grep -qF 'REFUSED' "$A" && /usr/bin/grep -qF 'p7-canary' "$A" && /usr/bin/grep -qF 'fbbdde0c416e72b9884e56c562fd88eaa4da447926e1cb6cc17c3e04aee7da1a' "$A" && /usr/bin/grep -qE 'RW=false|RW false|"RW": ?false' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - Preconditions (a)–(c) recorded; (a) RW=false and (b) both fixture hashes matched at start.
    - `--self-test` and `--run` exit codes recorded with the full `--run` transcript.
    - Confirmation 1 quotes four non-empty hashes from `layer3.before`/`layer3.after`.
    - Confirmation 2 shows four `REFUSED` + exit 3 results, the sentinel still present afterwards, one positive `gone`, and the sha256 of the extracted program text.
    - Confirmation 3 shows three empty listings, after the listing recipe was driven against a control and observed listing it.
    - Confirmation 4 recorded as not driven, predicted.
    - Real `library.db` / `state.pickle` hashes identical at start and end.
    - No write under `/mnt/tank` (layer 2 of the oracle plus the RW=false mount).
  </acceptance_criteria>
  <done>The oracle has run for real once before the grant, and each E12 hardening is confirmed or explicitly left predicted.</done>
</task>

<task type="auto">
  <name>Task 2: record the live arm-1 dump size from `check-beets-config.sh` (DEF-06-29-04)</name>
  <files>scripts/check-beets-config.sh, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt</files>
  <read_first>
    - `scripts/check-beets-config.sh` — header (READ-ONLY BY CONTRACT, the one named write), `WORKDIR="$(mktemp -d)"`, the `cp "$WORKDIR/exec.out" "$WORKDIR/arm1.dump"` line and the `pass "ARM 1 read via CONFIG_ROUTE=…"` line after it, `run_self_test()` and its D-04 contract over the file's own source.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-29-04`; `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-22-pipefail-141.txt` (the GC-01 margin).
  </read_first>
  <action>
Immediately after the `cp "$WORKDIR/exec.out" "$WORKDIR/arm1.dump"` line add ONE line that prints
the dump's byte size: `info "arm1.dump bytes: $(wc -c <"$WORKDIR/arm1.dump" | tr -d ' ')"` (use the
file's existing informational helper; if the file names it differently, use that helper — read it).
The size must be captured inside the run because `WORKDIR` is a `mktemp -d`. Add no `beet` token.
Run `bash scripts/check-beets-config.sh --self-test` (unchanged case count, exit 0), then a live
`bash scripts/check-beets-config.sh` from the workstation (pre-grant). Record the exit code, the
`arm1.dump bytes:` value, and the GC-01 margin as stated in `06-22-pipefail-141.txt` beside it
(the pipe-buffer figure it was measured against), in a `ARM1 DUMP SIZE` section of
`07-05-oracle-run.txt`. Record the live run's effective `import.copy`, `import.move` and
`import.write` readings if the script prints them — plan 07-08 uses them as the pre-grant C1
reading. Commit: `feat(07-05): record the live arm1.dump size (DEF-06-29-04)` (the script line) and
include the artifact update.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n scripts/check-beets-config.sh && bash scripts/check-beets-config.sh --self-test >/dev/null && test "$(/usr/bin/grep -c 'arm1.dump bytes:' scripts/check-beets-config.sh)" -eq 1 && /usr/bin/grep -qE 'arm1.dump bytes: *[0-9]+' .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-oracle-run.txt</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one line added to `check-beets-config.sh` (diff shows `+1 -0`), containing no `beet` invocation; `--self-test` still exits 0 with its unchanged announced count.
    - The artifact's `ARM1 DUMP SIZE` section carries an integer byte count from a LIVE run, the live run's exit code, and the GC-01 margin beside it.
  </acceptance_criteria>
  <done>The arm-1 dump size is written down for the first time.</done>
</task>

<task type="auto">
  <name>Task 3: D-09 — assert DJ path rule 2 read-only on one named `disctotal=2` folder in a throwaway library</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-rule2-assertion.txt</files>
  <read_first>
    - `stacks/selfhosted/arrs/beets/config.yaml` `paths:` stanza (rule 2 `'albumtype:=dj disctotal:2..': 'DJ/$albumartist/$album%aunique{}/$disc-$track $title'`), `per_disc_numbering: yes`, the `%aunique{}` and numeric-ID landmine comments.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md` § "What this sample does NOT cover" item 1 (the seven named folders) and § instruments (beets' own field view via `mediafile` in beets-flask).
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt` header § "THE RENDERING IS 02-05, NOT 2-05".
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-12-01`.
    - `scripts/phase06-oracle.sh` — how its throwaway import is built: the `-c` overlay contents (`$SCRATCH_OVERLAY` redirecting `library`, `statefile`, `directory`), `-A` as-is, `--set`, then `move -p`, and the `/tmp/p6-*` scratch allow-list.
  </read_first>
  <action>
Folder choice, mechanical: list the seven names from `06-SAMPLE.md` (`Mastermix_Issue_403`,
`Mastermix_Issue_404`, `Mastermix_Issue_413`, `Mastermix_Issue_418_April_2021`,
`VA-Mastermix.Issue.422-2021`, `VA-Mastermix.Issue.427.2CD-2021`, `VA-Mastermix.Issue.429-2022`),
sort them `LC_ALL=C`, take the first, and record the sort. Assert it exists as exactly one
directory under `/mnt/tank/downloads/complete/nzb/dj-mixes/` and read its beets fields
(`disctotal`, `disc`, `albumartist`, `album`, file count) through `mediafile` in beets-flask; if
`disctotal` is not 2 on every file, record it and take the next name in the sort.

Source manifest BEFORE: from atlantis as real root, `sha256sum` of every file in the folder plus
`find -printf '%P\t%s\t%T@\n'`, saved under `/mnt/fast/safety/phase07/rule2/`.

In beets-flask (flask's own 2.12.0 — D-04; the 2.13.1 `beets` container is not used), under
`/tmp/p6-p7rule2/` (inside the oracle's allow-list): write an overlay YAML redirecting `library:
/tmp/p6-p7rule2/lib.db`, `statefile: /tmp/p6-p7rule2/state.pickle`, `directory: /media/Music`, and
setting `import: {write: no, copy: no, move: no}`. Before importing, print the effective config
with `-l /tmp/p6-p7rule2/lib.db -c /tmp/p6-p7rule2/overlay.yaml config` and assert
`import.write: no`, `copy: no`, `move: no`, `library` and `statefile` under `/tmp/p6-p7rule2/`;
STOP if any differs. Then `-l … -c … import -A -q --set albumtype=dj <container path of folder>`,
then `-l … -c … move -p` and capture every ` -> ` line. Every invocation carries both `-l` outside
`/config/library.db` AND `-c` (D-04 compliant; ad hoc, not committed, so the D-04 scan is
unaffected — say so).

Assert over the captured destinations, recording each check's count:
- line count equals the folder's audio file count;
- every destination begins `/media/Music/DJ/`;
- every basename matches `^0[12]-[0-9]{2} .+\.(mp3|flac|wav)$` (disc-prefixed flat, `PaddedInt(2)`
  rendering `02-05`), both disc values `01` and `02` present;
- zero occurrences of `-1 - `, of a basename starting `00-`, of a `Disc [0-9]` or `CD [0-9]`
  directory component, and of a bracketed integer ` [NN]` that is not a four-digit year;
- exactly one album directory.
Drive the negative recipes first against control strings (`/media/Music/DJ/X/Y/-1 - T.mp3`,
`/media/Music/DJ/X/Y/00-01 T.mp3`, `/media/Music/DJ/X/Y/Disc 2/01 T.mp3`) and observe each
detected. Record the rendered paths verbatim.

Afterwards: remove `/tmp/p6-p7rule2` through the oracle's fenced `CLEANUP_PROG` (the same
construction task 1 drove), re-take the source manifest and assert byte-identical to BEFORE
(sha256 and size per file; mtime recorded), and re-read the real `library.db`/`state.pickle`
hashes (unchanged). A rendering defect found here is a FINDING recorded in the phase's
`deferred-items.md` with the rendered path — ⛔ do NOT edit the `paths:` stanza (locked, carried
from Phase 6).
Commit both artifacts: `docs(07-05): first live oracle run, arm1 size, rule-2 class assertion (D-30, D-09)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-05-rule2-assertion.txt && test -s "$A" && /usr/bin/grep -qF ' -> /media/Music/DJ/' "$A" && /usr/bin/grep -qE '/0[12]-[0-9]{2} ' "$A" && /usr/bin/grep -qF 'LC_ALL=C' "$A" && /usr/bin/grep -qiE 'manifest.*(identical|unchanged)' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - The chosen folder is the first of the seven by `LC_ALL=C` (or the next, with the reason), recorded with the sort.
    - The effective-config assertion (write/copy/move all `no`, library and statefile in scratch) is recorded BEFORE the import.
    - Rendered destinations are recorded verbatim; every listed check has a count; the negative recipes were driven against the three controls first.
    - Source manifest identical before and after; real `library.db` / `state.pickle` unchanged.
    - Scratch removed through the fenced cleanup program.
    - No change to `stacks/selfhosted/arrs/beets/config.yaml`: `git diff --exit-code HEAD -- stacks/selfhosted/arrs/beets/config.yaml` exits 0.
  </acceptance_criteria>
  <done>Rule 2 has been evaluated by an instrument for the first time, read-only, with its rendered paths on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle / D-09 imports → `/downloads` (mounted `:rw` in beets-flask) | The vendored config has `import.write: yes`; only the overlay stops a tag write into the source. |
| `CLEANUP_PROG` → container filesystem | An `rm -rf` whose argument is a knob. |
| workstation → LXC 100 → container | Remote execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | `dj-mixes` source files | mitigate | Effective config asserted (write/copy/move `no`) BEFORE import, STOP on mismatch; source sha256 manifest identical before/after. |
| T-07-05-02 | Denial of service | fence failing open under dash | mitigate | Canary design: every refusal case targets a sentinel directory, so a fail-open deletes only the canary; sentinel presence asserted after. |
| T-07-05-03 | Tampering | real `library.db`/`state.pickle` | mitigate | Every invocation carries `-l` throwaway AND `-c` overlay; layer-3 hashes read at start and end. |
| T-07-05-04 | Repudiation | running after the grant and misreading a by-design red | mitigate | Preconditions STOP the plan if `/media` is RW=true or `library.db` is off-fixture. |
| T-07-05-05 | Information disclosure | transcripts | mitigate | No credential used; artifacts screened for keys/tokens/NUL. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- Oracle `--run` recorded pre-grant with the four E12 confirmations.
- arm1.dump byte count recorded from a live run.
- Rule 2 rendered paths asserted; sources and real state unchanged.
</verification>

<success_criteria>
- Every E12 hardening that a live run can exercise has been exercised before the library becomes writable, and rule 2 has been evaluated at least once.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-05-SUMMARY.md` when done.
</output>
