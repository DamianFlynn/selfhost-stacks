---
phase: 07-pilot-12-albums-end-to-end
plan: 12
type: execute
wave: 8
depends_on: ["07-11"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/deferred-items.md
autonomous: false
requirements: [IMPT-01, QUAL-02, QUAL-03, CONS-04]

must_haves:
  truths:
    - "D-10 / G-08: the remaining non-DJ albums (P02–P06, P09–P12) are imported only after P01 cleared its gate and its undo/re-run with a PASSED verdict — the ledger's NEWEST P01 re-run line must read `P01 LANDED (re-run) — EQUALITY VERDICT: PASS` (or `P01 LANDED (re-run-other …) — NEW-IMPORT VERIFIED: PASS`); the word LANDED alone, a FAIL verdict or any `STOP STATE 07-11-…` token after that line is a STOP before staging"
    - "P7R2-10 (operator: mechanical first): BOTH PASS lines are required — the verdict above AND the ledger's newest `QUALITY:` line reading `QUALITY: PASS (P01; …)` (computed by plan 07-11, separate from equality); any other reading → `STOP STATE 07-12-PRECONDITION`. The Task 1 verify computes this precondition from the ledger itself, so a correct stop passes it and a FAIL verdict cannot (P7R2-08a, P7R2-09)"
    - "P7R2-03: disposition completeness is enforced, never inferred — the in-scope P-ID set is derived from 07-SAMPLE.md's `EXECUTION SET 07-12:` line (under `### EXECUTION ORDER`), minus any DJ twin moved to 07-13; every in-scope P-ID has EXACTLY ONE `DISPOSITION PNN:` row (or the single `DISPOSITION: hold-all`), and Task 3 verifies each imported row's `PNN LANDED` — a zero count of imported rows is never read as all-held"
    - "D-20 / G-12: each album is imported through `02-review` and confirmed by the operator in the UI, in the ONE execution order 07-SAMPLE.md fixes — P02, P03, P04, P05, P06, P09, P10, P11, P12 in this plan (P07/P08 follow in 07-13) — with P02 before P04 so the Benson Boone pair's second arrival meets the ambiguity; the order is ASSERTED after the fact from beets' own `albums.added`, not assumed"
    - "D-29: every folder's match is gated recommendation → track count → per-track distance, in that order, with the accepted candidate's `search_limit` rank recorded; S3's chosen candidate's country is recorded (CONF-05 `preferred.countries` exercised on real content)"
    - "D-13: before any `unsorted/` folder is staged, the DEPLOYED `diff-music-tags.sh` is asserted to carry the AMBIGUOUS arm (sha256 equal to the repo) — the condition the amendment names"
    - "Criteria 3, 4, 7, 8 and the Jellyfin half of 6 are recorded per album, as for P01; every FROZEN / APPENDED oracle divergence is classified and never edited (D-07)"
    - "D-28 groundwork: every `%aunique{}` firing in the landed tree is listed (the Benson Boone pair is predicted), and a compliant `move -p` against a COPY of the real library.db shows whether any landed album would re-path under the final collision set"
    - "D-18: the ledger names each album as it lands; a hold at any point is a written STOP state"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt"
      provides: "Per-album D-29 gates, operator answers, criteria readings, oracle classification, aunique list"
      contains: "P12"
  key_links:
    - from: "import order P02 → P04"
      to: "the %aunique{} disambiguator on the Benson Boone pair"
      via: "beets evaluates the ambiguity against the library at import time"
      pattern: "aunique"
---

<objective>
Import the remaining bucket-A albums through the same gated front end, one confirmation per album,
and read every per-album criterion for each.

Purpose: the eleven after the one (D-10). This is where the compilation (S3, `Various Artists/`
via the `comp:` override resolving to `$albumartist`, CONF-03) and `preferred.countries` (CONF-05)
meet real content, where the predicted `%aunique{}` firing happens, and where the `unsorted/` draws
test the flow on the backlog Phase 9 must process.

**Scope rule for DJ twins:** any fresh folder `07-SAMPLE.md` flagged as a `dj-mixes` name twin is
NOT imported here; it moves to plan 07-13 with P07/P08 so DJ routing (D-08) is applied in one place.
State in the artifact which P-IDs this plan covers.

Music Assistant is still not synced (plan 07-15, once, at the end). ⛔ No `Full[R]efresh`.
⛔ `zfs [r]ollback` forbidden on `fast/appdata/arrs` (D-16); no `zfs` write verb in this plan.
Every `beet` call in this plan is either through the beets-flask UI or compliant (`-l` on a COPY
outside `/config/library.db` AND `-c` overlay) — D-27.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md
@.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: assert the D-13 condition, stage the non-DJ albums, read every preview and gate each by D-29</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt</files>
  <read_first>
    - `artifacts/07-11-p01-undo-rerun.txt` (the `EQUALITY VERDICT: PASS` line — a precondition) and the ledger.
    - `07-SAMPLE.md` § THE DRAW — P02–P06, P09–P12 paths, DJ-twin flags, and `### EXECUTION ORDER` with its machine-readable `EXECUTION SET 07-12:` line (P02…P06, P09…P12 here; P07/P08 in 07-13).
    - `artifacts/07-10-p01-import.txt` task 1 — the exact staging and preview-reading method to repeat.
    - `.planning/ROADMAP.md` `## Beyond This Milestone` DUPE row amendment (D-13 condition).
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-country-preference.txt` — CONF-05's expected behaviour.
  </read_first>
  <action>
Preconditions — BOTH PASS lines (G-08, P7R2-10), read from the LEDGER, never from memory: (1) the
ledger's newest line containing `P01 LANDED (re-run` reads exactly `P01 LANDED (re-run) —
EQUALITY VERDICT: PASS` or `P01 LANDED (re-run-other <id>) — NEW-IMPORT VERIFIED: PASS`, and no
`STOP STATE 07-11-…` token appears on a later ledger line; (2) the ledger's newest line beginning
`QUALITY: ` reads `QUALITY: PASS (P01; …)` — plan 07-11's computed quality gate, separate from
equality; the operator's adjudication at 07-11 does not substitute for it. Quote both lines as
`PRECONDITION P01 VERDICT: <line>` and `PRECONDITION P01 QUALITY: <line>`, then write
`PRECONDITION: PASS`. Anything else — `LANDED` without a PASS verdict, a FAIL verdict, a later
07-11 stop token, a missing or FAIL `QUALITY:` line, or a hold token such as `07-11-T1-HOLD` — is a
STOP: stage nothing, write `PRECONDITION: STOP — <what the ledger said>` and the ledger `STOP STATE
07-12-PRECONDITION: <what the ledger said>` at the start of its own line; Task 2 then presents
`hold-all` only and Task 3 writes nothing further. Also: both pre-07-pilot snapshots present;
`_inbox/02-review` holds no audio other than any P01 remnant beets-flask left (record).

**In-scope set (P7R2-03).** Read `07-SAMPLE.md`'s `EXECUTION SET 07-12:` line (under `### EXECUTION
ORDER`, declared by plan 07-06 before the draw). The P-IDs on 07-SAMPLE's `DJ TWINS AMONG FRESH
DRAWS:` line move to plan 07-13. Write two lines, P-IDs space-separated in execution order:
`IN-SCOPE P-IDS: <the rest>` and `MOVED TO 07-13: <exactly the DJ TWINS line's P-IDs | none>`.
Their union must equal the `EXECUTION SET 07-12:` set exactly, and `MOVED TO 07-13:` must equal the
DJ TWINS line (the verify recomputes both).

D-13 condition: on LXC 100 assert `/mnt/fast/stacks/scripts/diff-music-tags.sh` sha256 equals the
repo file and contains `AMBIGUOUS_BEFORE` (comment-stripped). If not → do not stage any `unsorted/`
folder; record; stage only `music/` folders.

Stage each in-scope album exactly as plan 07-10 did (reflink copy from atlantis, chown the staged
copy only, sha256 set equal to its 07-07 manifest rows, source manifest unchanged). Stage all at
once — beets-flask previews each; nothing imports without the operator. Read each preview and apply
D-29 in order per album (recommendation → track count per disc → per-track distance), recording for
each: candidate table (rank ≤ 5, release id, title, album artist, country, discs, tracks/disc,
recommendation, distance, per-track extremes) and the named passing candidate or "none passes". For
S3 (P05) record which candidates carry GB/US and whether the preferred-country candidate ranks
first. For S7 (P06, one file) record whether the UI offers a singleton import and what the frozen
row (`Singles/Cyril/…`, which assumed `-s`) needs. For any album where the match changes
`albumartist`/`album` text relative to the file's own tags, predict the destination directory and
compare with its FROZEN/APPENDED oracle lines now, before import.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && P=.planning/phases/07-pilot-12-albums-end-to-end && A=$P/artifacts/07-12-nine-imports.txt && L=$P/artifacts/07-pilot-ledger.txt && test -s "$A" && LN="$(/usr/bin/grep -nF 'P01 LANDED (re-run' "$L" | tail -n 1)" && SN="$(/usr/bin/grep -nF 'STOP STATE 07-11-' "$L" | tail -n 1 | cut -d: -f1)" && OK=no && if printf '%s' "$LN" | /usr/bin/grep -qE '^[0-9]+:P01 LANDED \(re-run\) — EQUALITY VERDICT: PASS$|^[0-9]+:P01 LANDED \(re-run-other [^)]+\) — NEW-IMPORT VERIFIED: PASS$' && /usr/bin/grep -E '^QUALITY: ' "$L" | tail -n 1 | /usr/bin/grep -qE '^QUALITY: PASS \(P01;' && { test -z "$SN" || test "$SN" -lt "$(printf '%s' "$LN" | cut -d: -f1)"; }; then OK=yes; fi && if [ "$OK" = yes ]; then /usr/bin/grep -qxF 'PRECONDITION: PASS' "$A" && /usr/bin/grep -qE '^PRECONDITION P01 VERDICT: P01 LANDED \(re-run' "$A" && /usr/bin/grep -qE '^PRECONDITION P01 QUALITY: QUALITY: PASS \(P01;' "$A" && /usr/bin/grep -qF 'D-13' "$A" && /usr/bin/grep -qF 'AMBIGUOUS_BEFORE' "$A" && E="$(/usr/bin/grep -E '^EXECUTION SET 07-12: ' $P/07-SAMPLE.md | sed 's/^EXECUTION SET 07-12: //' | tr ' ' '\n' | /usr/bin/grep . | sort)" && I="$( { /usr/bin/grep -E '^IN-SCOPE P-IDS: ' "$A" | tail -n 1 | sed 's/^IN-SCOPE P-IDS: //'; echo; /usr/bin/grep -E '^MOVED TO 07-13: ' "$A" | tail -n 1 | sed 's/^MOVED TO 07-13: //; s/^none$//'; } | tr ' ' '\n' | /usr/bin/grep . | sort)" && test -n "$E" && test "$E" = "$I" && test "$(/usr/bin/grep -E '^MOVED TO 07-13: ' "$A" | tail -n 1 | sed 's/^MOVED TO 07-13: //' | tr ' ' '\n' | /usr/bin/grep . | sort)" = "$(/usr/bin/grep -E '^DJ TWINS AMONG FRESH DRAWS: ' $P/07-SAMPLE.md | sed 's/^DJ TWINS AMONG FRESH DRAWS: //' | tr ' ' '\n' | /usr/bin/grep . | sort)" && for p in $(/usr/bin/grep -E '^IN-SCOPE P-IDS: ' "$A" | tail -n 1 | sed 's/^IN-SCOPE P-IDS: //'); do /usr/bin/grep -qF "$p" "$A" || exit 1; done; else /usr/bin/grep -E 'STOP STATE 07-12-' "$L" | tail -n 1 | /usr/bin/grep -qF 'STOP STATE 07-12-PRECONDITION' && /usr/bin/grep -qE '^PRECONDITION: STOP' "$A"; fi</automated>
  </verify>
  <acceptance_criteria>
    - The ledger's newest P01 verdict line and newest `QUALITY:` line quoted; `PRECONDITION: PASS` only when the verdict is a PASS form with no later 07-11 stop token AND quality reads `QUALITY: PASS (P01; …)`; anything else stopped the plan with `STOP STATE 07-12-PRECONDITION` and nothing staged. The verify recomputes the precondition from the ledger in both directions: a correct stop (e.g. after `07-11-T1-HOLD`, which carries no verdict string) passes, and `EQUALITY VERDICT: FAIL` cannot pass (P7R2-08a, P7R2-09, P7R2-10).
    - `IN-SCOPE P-IDS:` and `MOVED TO 07-13:` lines whose union equals 07-SAMPLE.md's `EXECUTION SET 07-12:` exactly, any DJ twin moved to 07-13 named (P7R2-03).
    - D-13 condition asserted on the deployed script (sha256 + token) before any `unsorted/` staging.
    - Each staged copy's sha256 set equals its manifest rows; every source manifest unchanged.
    - A complete D-29 table and verdict per album; P05's country evidence; P06's singleton handling recorded.
  </acceptance_criteria>
  <done>Every in-scope album is previewed and gated, and none is imported yet.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: gate — the operator imports each album in the declared execution order, or holds any of them</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt</files>
  <read_first>
    - `artifacts/07-12-nine-imports.txt` task-1 sections.
  </read_first>
  <action>
Present one table: P-ID, folder, recommended candidate (or `hold` if none passes D-29), its rank,
its predicted destination vs oracle, and any warning (e.g. P04 is predicted to carry the `%aunique{}`
disambiguator; a MusicBrainz `label` on both may change which). Recommend `hold` for any album with
no passing candidate. The operator imports in the beets-flask UI in the execution order
P02, P03, P04, P05, P06, P09, P10, P11, P12 (pausing between albums is allowed), and replies with
one line per album. Record verbatim with UTC time on an `OPERATOR ANSWER` line, then write EXACTLY
ONE normalised line per P-ID on Task 1's `IN-SCOPE P-IDS:` line — `DISPOSITION PNN: imported`,
`DISPOSITION PNN: imported-other <release id>` or `DISPOSITION PNN: hold` (a P-ID with no answer
line, or an unrecognised one, normalises to `hold`, and still gets its row) — and no row for any
other P-ID; or the single line `DISPOSITION: hold-all` and no per-P-ID rows (G-10 — the verify reads
these, never the free-text answer; P7R2-03 — it requires one row per in-scope P-ID, so a missing row
fails rather than reading as held). After a Task 1 `STOP STATE 07-12-PRECONDITION`, only
`hold-all` is presented.
  </action>
  <decision>Import each in-scope album with its named candidate, in the declared execution order (P02…P06, P09…P12)?</decision>
  <context>Per-album confirmation is D-20's human act. A held album is a successful outcome; it is recorded in the ledger as not landed and criterion 1's "twelve" is then for /gsd-verify to score, not this plan.</context>
  <options>
    <option id="hold-all">
      <name>Hold all — import none of them</name>
      <pros>Nothing further written; P01 remains the only album landed.</pros>
      <cons>Criterion 1's twelve not reached.</cons>
    </option>
    <option id="per-album">
      <name>Per album — reply `PNN imported` / `PNN imported-other <id>` / `PNN hold` for each</name>
      <pros>Exactly D-20's per-album confirmation.</pros>
      <cons>None beyond the imports.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold-all`, or one line per P-ID: `PNN imported`, `PNN imported-other <release id>`, or `PNN hold`. A P-ID with no line is treated as `hold`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt && /usr/bin/grep -qF 'OPERATOR ANSWER' "$A" && if /usr/bin/grep -qxF 'DISPOSITION: hold-all' "$A"; then test "$(/usr/bin/grep -cE '^DISPOSITION P[0-9]{2}: ' "$A")" -eq 0; else S="$(/usr/bin/grep -E '^IN-SCOPE P-IDS: ' "$A" | tail -n 1 | sed 's/^IN-SCOPE P-IDS: //')" && test -n "$S" && for p in $S; do test "$(/usr/bin/grep -cE "^DISPOSITION $p: (imported|imported-other [^ ]+|hold)$" "$A")" -eq 1 || exit 1; done && test "$(/usr/bin/grep -cE '^DISPOSITION P[0-9]{2}: ' "$A")" -eq "$(printf '%s\n' $S | /usr/bin/grep -c .)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Answer recorded verbatim with UTC time; EXACTLY ONE normalised `DISPOSITION PNN:` row per P-ID on `IN-SCOPE P-IDS:` (missing answer = a `hold` row, never a missing row) and no row for any other P-ID — or the single `DISPOSITION: hold-all` with no per-P-ID rows. The verify counts rows per expected P-ID and the total (P7R2-03).
    - The ledger is appended with each held album as "staged, not imported".
  </acceptance_criteria>
  <done>Each in-scope album is imported by the operator or held, per album.</done>
</task>

<task type="auto">
  <name>Task 3: verify every imported album, list the %aunique firings, and update the ledger</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `artifacts/07-10-p01-import.txt` task 3 — the exact per-album verification recipe (criteria 3, 4, 7, 8, oracle, Jellyfin) to repeat.
    - `07-EXPECTED-TREE.txt` — each album's FROZEN or APPENDED lines.
    - `scripts/phase06-oracle.sh` — the throwaway `-l` + `-c` overlay construction (for the compliant `move -p` against a COPY of the real DB).
  </read_first>
  <action>
For EACH imported album, repeat plan 07-10 task 3 verbatim in method, writing `post-07-PNN` and
`/mnt/fast/safety/phase07/diff/PNN/`: DB rows (read-only sqlite); criterion 3 (ffprobe vs DB per
file; owner from atlantis); criterion 4 (`diff-music-tags.sh pre-07-PNN post-07-PNN`, exit,
AMBIGUOUS counts, empty-BEFORE counter, every gained/changed/dropped field, a reason or
`UNEXPLAINED` for each drop); oracle lines diff with each divergence classified
(`MATCH-CHANGED-TAG` / `TEMPLATE-DEFECT`) and filed as `DEF-07-12-NN`; criterion 8 (source manifest
unchanged). Run `check-music-import.sh` once after all imports (record exit, summary, any
finding). Jellyfin FIRST: one targeted `POST /Library/Media/Updated` listing every new file path
(`Created`), address resolved fresh; after ≥ 60 s poll `/Items` and assert per album: exactly one
MusicAlbum, its `AlbumArtist`, child count = file count, and for P05 (S3) `AlbumArtist` is
`Various Artists` with 44 children under ONE album (not 44 one-track artists). ⛔ No
`Full[R]efresh`; Music Assistant untouched.

`%aunique{}` list: from the landed paths, every album directory whose name carries a disambiguator
suffix, plus every pair of albums sharing `(albumartist, album)`; for each, record which
disambiguator fired and its rendered value. Compliant re-path check: copy `/config/library.db` to
`/tmp/p6-p7aunique/lib.db` inside beets-flask (the copy is the `-l`), write an overlay
(`library`/`statefile` in that directory, `import.write: no`), run `-l … -c … move -p` over the whole
library copy and record any ` -> ` line (a landed album that beets would now place elsewhere, e.g.
the first of the Benson Boone pair gaining or losing a suffix after the second arrived) — each such
line is a finding for D-28 in plan 07-15. Remove `/tmp/p6-p7aunique` through the oracle's fenced
cleanup program. Real `library.db` sha256 read before and after the copy-and-check and unchanged by
it.

**Import order, asserted after the fact (G-12):** read-only sqlite `SELECT id, albumartist, album,
added FROM albums` for every album landed by this plan; map each to its P-ID; sort by `added` and
write `ADDED ORDER: PASS — <P-IDs in added order with timestamps>` when the landed P-IDs appear in
the execution order above, or `ADDED ORDER: FAIL — <actual order>` (a finding, `DEF-07-12-NN`: the
`%aunique{}` prediction is then re-read against the ACTUAL order, and the P02-before-P04 premise is
stated as not met). If P02 or P04 was held, write `ADDED ORDER (P02<P04): NOT APPLICABLE — <which
held>` and still assert the rest. The line's form is fixed (P7R2-17 — the verify reads the verdict,
the P-IDs and the finding, not just the heading): `ADDED ORDER: PASS — PNN <added>, PNN <added>, …`
or `ADDED ORDER: FAIL — …` with its `DEF-07-12-NN` in the artifact.

Ledger: one `PNN LANDED` line per imported album (destination, item count, candidate), and a
current `## STOP STATE` block (token `07-12-DONE` or `07-12-PARTIAL: <held P-IDs>`) listing every
album landed so far and every album held.
Commit: `docs(07-12): remaining bucket-A albums imported and verified (D-20, D-29)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-12-nine-imports.txt && L=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt && test "$(/usr/bin/grep -cE 'Full[R]efresh|zfs [r]ollback' "$A")" -eq 0 && git diff --exit-code HEAD -- .planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt && if /usr/bin/grep -qxF 'DISPOSITION: hold-all' "$A"; then true; else S="$(/usr/bin/grep -E '^IN-SCOPE P-IDS: ' "$A" | tail -n 1 | sed 's/^IN-SCOPE P-IDS: //')" && test -n "$S" && I="" && H="" && for p in $S; do d="$(/usr/bin/grep -E "^DISPOSITION $p: " "$A" | tail -n 1 | sed "s/^DISPOSITION $p: //")"; case "$d" in hold) H="$H $p" ;; imported|'imported-other '*) I="$I $p"; /usr/bin/grep -qE "(^|[^A-Z0-9])$p LANDED" "$L" || exit 1 ;; *) exit 1 ;; esac; done && { test -z "$I" || { /usr/bin/grep -qF 'aunique' "$A" && /usr/bin/grep -qF 'check-music-import' "$A" && /usr/bin/grep -qE '^ADDED ORDER: (PASS|FAIL) — P[0-9]{2}' "$A" && { ! /usr/bin/grep -qE '^ADDED ORDER: FAIL' "$A" || /usr/bin/grep -qE 'DEF-07-12-[0-9]+' "$A"; } && case " $H " in *" P02 "*|*" P04 "*) /usr/bin/grep -qE '^ADDED ORDER \(P02<P04\): NOT APPLICABLE — P0[24]' "$A" ;; *) true ;; esac; }; }; fi</automated>
  </verify>
  <acceptance_criteria>
    - Every imported album has criteria 3, 4, 8 readings, an oracle diff with classified divergences, and Jellyfin's one-album reading; P05 reads as ONE album under `Various Artists` in Jellyfin, or the deviation is a filed finding.
    - `check-music-import.sh` run once after the imports, exit and summary recorded.
    - The `%aunique{}` list and the compliant copy-based `move -p` output recorded; real DB unchanged by the check; scratch removed through the fenced cleanup.
    - No frozen or appended oracle line edited; no MA write; unbracketed `FullRefresh` / `zfs rollback` absent from the artifact.
    - An `ADDED ORDER: PASS|FAIL — PNN …` line computed from `albums.added` (FAIL with a filed `DEF-07-12-NN`), plus `ADDED ORDER (P02<P04): NOT APPLICABLE — <held P-ID>` whenever P02 or P04 was held (the verify computes which were held from the dispositions and requires the arm).
    - Ledger lists each landed and each held album; the verify walks the `IN-SCOPE P-IDS:` set and, for every P-ID whose ONE disposition row is `imported…`, requires its `PNN LANDED` ledger line — only `DISPOSITION: hold-all`, or every in-scope row reading `hold`, passes with none (P7R2-03: a zero count of imported rows is never read as all-held).
  </acceptance_criteria>
  <done>The in-scope bucket-A albums are landed and read, with the %aunique behaviour of the real library on record for plan 07-15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator confirms → library writes | Nine more imports. |
| `unsorted/` duplicates → QUAL-02 join | The D-13 exception's condition. |
| copy of library.db → beets `move -p` | A read-only path computation that must not touch the real DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Tampering | ambiguous duplicate join | mitigate | Deployed `diff-music-tags.sh` asserted to carry the AMBIGUOUS arm (sha256 + token) before any `unsorted/` staging; otherwise `unsorted/` is not staged. |
| T-07-12-02 | Tampering | wrong matches at volume | mitigate | Per-album D-29 ordered gate and per-album operator confirmation; per-album verification before the next plan. |
| T-07-12-03 | Tampering | real `library.db` during the aunique check | mitigate | `-l` points at a copy, `-c` overlay; real DB sha256 unchanged before/after; D-04/D-27 compliant. |
| T-07-12-04 | Tampering | Jellyfin metadata | mitigate | Targeted file-scope POST only; `Full[R]efresh` absent. |
| T-07-12-05 | Tampering | sources | mitigate | Copy staging; per-album source manifest re-check. |
| T-07-12-06 | Denial of service | `/tmp/p6-p7aunique` cleanup | mitigate | Removed through the oracle's two-layer fenced cleanup program. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- D-13 condition asserted; per-album D-29 and operator answers; per-album criteria readings; aunique list; ledger current.
</verification>

<success_criteria>
- Every non-DJ pilot album has either landed and been read on every per-album criterion, or been held on the record.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-12-SUMMARY.md` when done.
</output>
