---
phase: 07-pilot-12-albums-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [IMPT-01, IMPT-02, CONS-04, QUAL-02, QUAL-03, QUAL-04]

must_haves:
  truths:
    - "D-31: before anything is imported, a committed map names ONE artifact and ONE instrument for each of the eight Phase 7 success criteria, plus what reads as UNKNOWN for each"
    - "D-31: the map states that `/gsd-verify 07` scores the eight criteria once, that no plan self-declares a verification result, that a separate operator trust verdict is recorded, and that ONE code review of this phase's own changes is budgeted"
    - "D-01/D-02/D-03 are recorded as OUT of scope with their mechanical reason; enacting the inserted archive phase and moving DUPE-01/02 in ROADMAP.md is `/gsd-phase` work and is NOT done here"
    - "D-13: the ROADMAP `Beyond This Milestone` DUPE-01/DUPE-02 row is amended IN BAND to 'any BULK import of `unsorted`', the original text kept visible, the pilot named as the exception, D-11 named as the control, and the narrowing ARGUED rather than merely edited"
    - "Every PATTERNS.md coupling C1 … C11 has a named owning plan in the map's coupling index"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md"
      provides: "The pre-registered artifact + instrument per success criterion, entry-criterion ownership, coupling index, closure protocol"
      contains: "EVIDENCE MAP"
    - path: ".planning/ROADMAP.md"
      provides: "D-13's argued in-band amendment of the DUPE-01/02 `unsorted` prohibition"
      contains: "BULK"
  key_links:
    - from: "07-EVIDENCE-MAP.md criterion rows"
      to: "the artifacts later plans write"
      via: "exact artifact paths named before the run"
      pattern: "artifacts/07-"
    - from: "ROADMAP DUPE-01/02 row amendment"
      to: "scripts/diff-music-tags.sh AMBIGUOUS arm (plan 07-02)"
      via: "the exception holds only while the D-11 control is in place"
      pattern: "D-11"
---

<objective>
Register, before anything is imported, what would close each of Phase 7's eight success criteria
(D-31 item 1), and amend the one roadmap rule the sample would otherwise contradict (D-13).

Purpose: D-31 — "what would close this" is answered while it is still cheap to answer honestly.
Phase 6 took 52 plans and six rounds and still did not close; the lesson kept is that verification
and review are separate acts, and the map makes the verification target explicit before the run.
D-13 — D-06 draws from `unsorted`, and the ROADMAP `Beyond This Milestone` row for DUPE-01/02 reads
"Must precede any import of `unsorted`". The conflict is resolved by an argued amendment, never by a
quiet edit, because narrowing a safety rule to fit the plan is how safety rules die.

Scope, recorded here so no later plan re-derives it:
- **Out of scope (D-01):** `tank/downloads/mybook-music-archive` — mechanical reason: it has no
  QUAL-01 before-state; `scripts/snapshot-music-tags.sh` pins four capture roots by name and the
  archive is none of them, so criterion 4 is uncomputable for any album drawn from it.
- **D-02 / D-03:** the archive gets its own inserted phase between Phase 8 and Phase 9 and
  DUPE-01/DUPE-02 fold into it. ⛔ **Enacting either in `ROADMAP.md` is `/gsd-phase` work and this
  plan does NOT do it** — CONTEXT § Deferred says so explicitly. D-13's edit touches only the
  in-band text of the existing DUPE row.
- **Also out:** bucket B (S4, D-04), any bulk import (Phase 9), E6's mtime re-probe route
  (`DEF-06-45-02`, measured dead — ⛔ do not retry).

Output: `07-EVIDENCE-MAP.md` at the phase root (an INPUT to the run it judges, the same reasoning
`06-EXPECTED-TREE.txt`'s header gives for sitting at the phase root) and one amended ROADMAP row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: write and commit the D-31 evidence map — one artifact and one instrument per criterion, before any import</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § `### Phase 7: Pilot — 12 Albums End to End` — read the whole section by heading text: the eight `**Success Criteria**` items AND the `**Entry criteria inherited from Phase 6**` block E1 … E12, including E6's two separately-dispositioned measurements. Per CONVENTIONS §14, a criteria-only read is not sufficient evidence of absence — read both blocks.
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md` — all of D-01 … D-31, § Carried NOT re-decided, § Deferred.
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-PATTERNS.md` § "Couplings the CONTEXT file list does not name" (C1 … C11).
    - `.planning/REQUIREMENTS.md` — IMPT-01, IMPT-02, CONS-04, QUAL-02, QUAL-03, QUAL-04, and the unticked `- [ ] **CONF-04**` line.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt` header § "WHY THIS FILE IS AT THE PHASE ROOT" — the reasoning this map's location reuses.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-VERIFICATION.md` — the per-criterion table shape the map's rows follow.
  </read_first>
  <action>
Capture `BASE=$(git rev-parse HEAD)` first and write it into the map's header as the commit the map
was authored against.

Write `07-EVIDENCE-MAP.md` with these headed sections (heading text is the anchor other plans cite):

1. `# Phase 7 EVIDENCE MAP — registered before the run (D-31)` plus a header paragraph: this is an
   INPUT, committed before `tank/media/Music@pre-07-pilot` exists; its value is the commit that
   predates the first import; later plans FILL the "measured" column and never edit the
   "instrument" or "satisfies when" columns (a change to either is a finding recorded in
   `.planning/phases/07-pilot-12-albums-end-to-end/deferred-items.md`, not an edit).

2. `## The eight criteria` — a table with columns: criterion number and one-line text | owning
   plan(s) | artifact path | instrument (exact script/command) | reads as SATISFIED-FOR-SCORING
   when | reads as UNKNOWN when | measured (left empty; filled by plan 07-17). The rows, concretely:
   - **1 (fence + rollback):** owners 07-09, 07-11. Artifacts
     `artifacts/07-09-fence-and-grant.txt` and `artifacts/07-11-p01-undo-rerun.txt`. Instrument:
     `zfs list -t snapshot -H -o name` exact whole-line match (`grep -qxF`) for BOTH
     `tank/media/Music@pre-07-pilot` and `fast/appdata/arrs@pre-07-pilot` plus a sha256'd file
     copy of `library.db` and `state.pickle` under `/mnt/fast/safety/phase07/fence/`; the "rollback
     exercised" half is D-15's back-out of the gated first album through beets-flask rc6
     `UNDO IMPORT` (D-14), witnessed on tree (file count 0 at the destination), database
     (read-only sqlite item count for the album 0) and `state.pickle` (decoded `taghistory` no
     longer holding the staged source path). State in the row, in these words or equivalent: the
     roadmap text says "rollback"; D-14 chose `UNDO IMPORT` as the mechanism; this map registers,
     BEFORE the run, that criterion 1's "rollback exercised" is offered as discharged by that
     back-out and that NO `zfs [r]ollback` of `tank/media/Music` is planned — `/gsd-verify 07`
     accepts or rejects that mapping; no plan decides it.
   - **2 (hard shapes named before the run):** owner 07-06. Artifact `07-SAMPLE.md`. Instrument:
     `git merge-base --is-ancestor <commit that added 07-SAMPLE.md> <commit that added
     artifacts/07-10-p01-import.txt>` exits 0, and `07-SAMPLE.md` names S3 (compilation) and S2
     (multi-disc) by folder path.
   - **3 (ffprobe new tags on the file + ownership):** owners 07-10, 07-12, 07-13, 07-17. Artifact
     `artifacts/07-17-evidence.txt` § criterion 3. Instrument: `ffprobe -show_format
     -show_streams` on every imported file compared field-by-field against beets' own row for that
     item (read-only sqlite `file:/config/library.db?mode=ro`) for title, artist, album,
     albumartist, track, disc, and `mb_albumid` where matched; plus `stat -c '%u:%g'` read FROM
     ATLANTIS as real root equals `568:568`. `beet ls` is named as NOT acceptable evidence
     (EPERM is logged as a warning and the import continues).
   - **4 (QUAL-02 no net loss):** owners 07-02, 07-07, 07-10, 07-12, 07-13, 07-17. Artifact
     `artifacts/07-17-evidence.txt` § criterion 4. Instrument: `scripts/diff-music-tags.sh`
     (with plan 07-02's AMBIGUOUS arm) over the `pre-07-pilot` BEFORE capture and the
     `post-07-pilot` AFTER capture; satisfied-for-scoring when exit 0, AMBIGUOUS_BEFORE and
     AMBIGUOUS_AFTER both 0, the zero-BEFORE-fields counter read and stated, every dropped field
     listed with a recorded reason. UNKNOWN when exit 3. State the BEFORE-of-record reasoning:
     Phase 1's `pre-project` capture (2026-08-18) predates the `music/` tree's growth (277 files
     then, 531 at the 06-SAMPLE live pass) and Phase 5's 751 in-place tag writes, so the BEFORE of
     record is the `pre-07-pilot` capture plan 07-07 takes immediately before import, with
     `pre-project` coverage recorded as the cross-check.
   - **5 (undo demonstrated):** owner 07-11. Artifact `artifacts/07-11-p01-undo-rerun.txt`.
     Instrument: the three-part witness above, then the re-run's destination path list and
     `audio_md5` set compared byte-for-byte with the first run's.
   - **6 (both consumers):** owners 07-10 (Jellyfin half of the gated album), 07-15. Artifact
     `artifacts/07-15-consumers.txt`. Instrument: Jellyfin `/Items` per album (one MusicAlbum,
     AlbumArtist, child count, disc numbers) and Music Assistant API per album (album present once,
     album artist, track count, disc numbers); the compilation must read as ONE album under
     `Various Artists`. Order rule carried from Phase 6 D-25: Jellyfin FIRST, Music Assistant
     LAST. ⛔ `FullRefresh` forbidden.
   - **7 (detection sweep):** owners 07-03, 07-17. Artifact `artifacts/07-17-evidence.txt`
     § criterion 7. Instrument: `scripts/check-music-import.sh` exit 0, with its self-test
     controls driven red first; findings, if any, listed with their resolution.
   - **8 (sources still exist):** owners 07-07, 07-17. Artifacts
     `artifacts/07-07-source-manifest-before.tsv` and the after manifest in
     `artifacts/07-17-evidence.txt`. Instrument: per-file `sha256` + size manifest of every
     source folder, read from atlantis, identical before and after.

3. `## Entry criteria E1 … E12 — owner and handling` — one row each: E1 → 07-04 + 07-13 (D-08)
   and 07-05 (D-09 rule 2); E2 → 07-06 (D-26 predicate, read-only; import exercise carried
   forward); E3 → 07-09; E4 → NOT released by this phase (D-17), listed by 07-16's register only;
   E5 → 07-14 (+ 07-07 / 07-15 readings, D-24); E6 → first measurement 07-15 (new-import
   reading), second measurement 07-06 (D-05 stratum) + 07-15, each dispositioned separately,
   `REQUIREMENTS.md` CONF-04 box stays unticked by every plan; E7 → 07-02 (D-11/D-12) + this
   plan (D-13); E8 → 07-15 (D-28); E9 → 07-10, 07-12, 07-13 (D-29); E10 → 07-04 (D-27); E11 →
   07-08 (D-21/D-22); E12 → 07-05 (D-30).

4. `## Coupling index C1 … C11` — one row each naming the owning plan: C1 07-08/07-09; C2 07-08;
   C3 07-08; C4 07-08/07-09; C5 07-08; C6 07-08; C7 07-05 (sequenced before the grant) + 07-08
   (disposition recorded); C8 same as C7; C9 07-09; C10 07-08; C11 07-09.

5. `## Closure protocol (D-31)` — `/gsd-verify 07` scores the eight criteria ONCE at the end; no
   plan writes a verification verdict, ticks a `REQUIREMENTS.md` box or re-scores anything; the
   operator records a separate trust verdict in `07-TRUST-VERDICT.md` (plan 07-17); ONE code
   review of this phase's own changes is budgeted (offered at plan 07-09's gate, before the grant,
   where a finding is cheapest; otherwise run once after execution) — not six rounds.

6. `## Out of scope` — D-01 (with the mechanical reason), D-02/D-03 (and that their ROADMAP
   enactment is `/gsd-phase` work not done here), S4/bucket B, bulk import, the mtime route.

Write any token that a published recipe in this phase counts (e.g. `zfs [r]ollback`,
`zfs [d]estroy`, `Full[R]efresh`) in bracketed form (CONVENTIONS §8). Commit the map on its own:
`docs(07-01): register the Phase 7 evidence map before the run (D-31)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && M=.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md && test -s "$M" && for h in 'EVIDENCE MAP' '## The eight criteria' '## Entry criteria E1' '## Coupling index C1' '## Closure protocol' '## Out of scope'; do /usr/bin/grep -qF "$h" "$M" || { echo "missing: $h"; exit 1; }; done && for c in C1 C2 C3 C4 C5 C6 C7 C8 C9 C10 C11; do /usr/bin/grep -qE "(^|[^0-9A-Z])$c([^0-9]|$)" "$M" || { echo "missing coupling $c"; exit 1; }; done && test "$(/usr/bin/grep -cE 'zfs rollback|zfs destroy|FullRefresh' "$M")" -eq 0 && git diff --exit-code HEAD -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `07-EVIDENCE-MAP.md` exists at the phase root and contains all six headed sections named in the action (exact heading text).
    - The eight-criteria table has exactly eight data rows, each naming an owning plan, an artifact path beginning `artifacts/07-` or `07-`, an instrument, a "satisfied when" and an "UNKNOWN when" cell; the "measured" column is empty in every row.
    - Row 1 states that `UNDO IMPORT` is offered as the "rollback exercised" discharge, that no ZFS rollback of `tank/media/Music` is planned, and that `/gsd-verify 07` accepts or rejects the mapping.
    - Row 4 names `pre-07-pilot` as the BEFORE of record and states why Phase 1's `pre-project` capture is the cross-check rather than the BEFORE.
    - The coupling index names an owning plan for each of C1 … C11 (the verify loop greps each ID).
    - The unbracketed forms of the counted tokens appear zero times: `/usr/bin/grep -cE 'zfs rollback|zfs destroy|FullRefresh'` returns 0 — first driven against a one-line control file containing `zfs rollback` and observed to return 1 (CONVENTIONS §8.3); record both drives in the SUMMARY.
    - `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` exits 0 (no box moved).
    - The map's commit is on its own (`git show --stat HEAD` lists only `07-EVIDENCE-MAP.md`).
  </acceptance_criteria>
  <done>The evidence map is committed before any Phase 7 write, with one artifact and one instrument per criterion, and the closure protocol stated.</done>
</task>

<task type="auto">
  <name>Task 2: amend the DUPE-01/02 `unsorted` prohibition IN BAND, with the argument (D-13)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § `## Beyond This Milestone` — the `| DUPE-01, DUPE-02 | Duplicate reconciliation |` row; locate by that text, never by line number (planner commits shift this file).
    - `.planning/ROADMAP.md` Phase 7 entry criterion E7 (last-wins join) — the defect D-11 fixes.
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md` D-11, D-13, D-06, D-03.
    - `stacks/selfhosted/arrs/beets.yaml` retraction-in-place comment style ("kept visible rather than deleted") — the house style for amending a stated rule.
  </read_first>
  <action>
Edit ONLY the Note cell of the DUPE-01/DUPE-02 row. Keep the original sentence "Must precede any
import of `unsorted` — …" verbatim and visible. Append, inside the same cell, a dated amendment
headed `**Amended 2026-09-25 (Phase 7, D-13):**` that:
1. States the new scope: the rule now reads **"any BULK import of `unsorted`"**; the Phase 7 pilot's
   bounded draw (at most four folders from `music/` ∪ `unsorted/` per D-05/D-06, drawn by plan
   07-06) is the **named exception**, and it is the only one.
2. Argues it, in four clauses: (a) the rule's risk model is Phase 9's wholesale 7,451-file import,
   where one bad duplicate call is unrecoverable at scale; (b) the pilot imports with
   `import.copy` inside a two-dataset snapshot fence (D-16), so its sources are untouched
   (criterion 8) and every landed album is individually reversible (D-14); (c) the control that
   replaces "reconcile first" for the pilot is D-11: `scripts/diff-music-tags.sh` classifies a
   matched `audio_md5` whose duplicate records carry differing tag maps as AMBIGUOUS and exits 3
   UNKNOWN, on the BEFORE and AFTER sides, so a bad duplicate call cannot pass QUAL-02 silently;
   (d) **the exception is conditional** — it holds only while that AMBIGUOUS arm is present in
   the committed script (plan 07-02); if it is removed or regressed the original rule applies to
   the pilot too.
3. States what it does NOT change: Phase 9's bulk import still requires the reconciliation first,
   and D-03 moves DUPE-01/02 into the archive phase inserted between Phase 8 and Phase 9 — that
   move is `/gsd-phase` work and is deliberately not made by this edit.

Keep the table row a single line (the cell may be long; do not break the Markdown table). Do not
touch any other row, the Phase 7 section, or the progress table. Hand-edit; do NOT use any
`roadmap.*` SDK verb (it has wiped narrative cells in this project). Read the diff BODY afterwards.
Commit: `docs(07-01): amend the DUPE unsorted rule to bulk import, pilot excepted (D-13)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && R=.planning/ROADMAP.md && L="$(/usr/bin/grep -F '| DUPE-01, DUPE-02 |' "$R")" && test "$(printf '%s\n' "$L" | wc -l | tr -d ' ')" -eq 1 && printf '%s' "$L" | /usr/bin/grep -qF 'Must precede any import of `unsorted`' && printf '%s' "$L" | /usr/bin/grep -qF 'Amended 2026-09-25 (Phase 7, D-13)' && printf '%s' "$L" | /usr/bin/grep -qF 'BULK' && printf '%s' "$L" | /usr/bin/grep -qF 'D-11' && test "$(git diff HEAD~1 -- "$R" | /usr/bin/grep -c '^-[^-]')" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The DUPE row is still exactly one table line and still contains the original sentence "Must precede any import of `unsorted`" verbatim.
    - The same line contains `Amended 2026-09-25 (Phase 7, D-13)`, the word `BULK`, the pilot named as the exception, `D-11` named as the control, the conditionality clause (exception void if the AMBIGUOUS arm is absent) and the statement that D-03's move is `/gsd-phase` work not done here.
    - The commit's diff of `ROADMAP.md` removes exactly one line (the old row) and adds exactly one (the amended row); no other line of `ROADMAP.md` changes.
    - No `roadmap.*` or `state.*` SDK verb was used (stated in the SUMMARY).
  </acceptance_criteria>
  <done>The `unsorted` prohibition is narrowed to bulk import by an argued, visible, conditional in-band amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning records → every later plan and `/gsd-verify 07` | The map is read as the pre-registered target; a map written after the run would let the evidence choose its own criterion. |
| a safety rule in ROADMAP.md → the executor deciding what may be imported | An unargued narrowing reads as permission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Repudiation | evidence map | mitigate | Committed on its own before plan 07-09 takes the fence; the map's header records its base commit; plan 07-09's gate asserts the map's commit is an ancestor of HEAD before any write. |
| T-07-01-02 | Tampering | criterion instruments after the run | mitigate | The "instrument" and "satisfies when" columns are declared immutable; any change is recorded as a finding in the phase `deferred-items.md`, not an edit. |
| T-07-01-03 | Elevation of privilege | D-13 amendment used as a general licence to import `unsorted` | mitigate | The amendment is scoped to the named pilot draw, conditional on the D-11 control being present, and states Phase 9's bulk rule is unchanged. |
| T-07-01-04 | Information disclosure | committed prose | accept | Documentation only; no host credential, key or token is read or written by this plan. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install of any kind in this plan. |
</threat_model>

<verification>
- `07-EVIDENCE-MAP.md` committed with eight criterion rows, E1–E12 ownership, C1–C11 index, closure protocol and scope-out.
- The DUPE row amended in band, original text visible, argued, conditional.
- `REQUIREMENTS.md` byte-unchanged under `git diff --exit-code HEAD --`.
</verification>

<success_criteria>
- Every success criterion has exactly one pre-registered artifact and instrument before any import.
- The `unsorted` conflict is resolved by argument rather than by edit, and D-02/D-03's roadmap enactment is explicitly left to `/gsd-phase`.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-01-SUMMARY.md` when done.
</output>
