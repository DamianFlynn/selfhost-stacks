---
phase: 07-pilot-12-albums-end-to-end
plan: 14
type: execute
wave: 10
depends_on: ["07-13", "07-06", "07-07"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
autonomous: false
requirements: [CONS-04, QUAL-02]

must_haves:
  truths:
    - "D-24 / E5: `Def Leppard/Def Leppard (2015)/` is repaired in the same `rw` window as a NAMED OPERATION DISTINCT FROM THE TWELVE — measured first, the exact fields and values proposed, applied only on the operator's answer, and verified on the file"
    - "E5: any album directory plan 06-11 reported in the same shape (album folder name == artist folder name) is found by reading 06-11's record and included in the measured set, or its absence is stated with the search used"
    - "The hard-erroring FLAC is identified by measurement (which file, what error) and its disposition is recorded — repaired by a tag rewrite if the error is in the tag block, otherwise recorded, never re-encoded"
    - "QUAL-02 holds for the repair: `diff-music-tags.sh` over a fresh `pre-07-e5` and `post-07-e5` capture shows only the intended field(s) gained/changed and no drop"
    - "The repair writes through `mediafile` inside beets-flask (no `beet` invocation, so no D-04 registration), ownership stays `568:568`, and Jellyfin is told by a targeted file-scope POST — ⛔ never FullRefresh"
    - "⛔ `zfs [r]ollback` forbidden (D-16); recovery of a bad repair is a file copy of the named files out of `tank/media/Music@pre-07-pilot`'s `.zfs/snapshot/`, on the operator's answer"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt"
      provides: "E5 measurement, proposal, operator answer, applied change, verification"
      contains: "Def Leppard"
  key_links:
    - from: "E5 repair"
      to: "D-24 census and DEF-06-45-03"
      via: "the Def Leppard track among the 30 is re-read in plan 07-15"
      pattern: "ArtistItems"
---

<objective>
Repair the live `Def Leppard/Def Leppard (2015)/` defect (D-19a, scheduled here by Phase 6) while the
library is writable, as its own named operation, and set up the D-24 after-reading that tests
`DEF-06-45-03`.

Purpose: PROJECT.md records this album deriving an EMPTY album artist in Music Assistant and
hard-erroring one FLAC, because the album folder name equals the artist folder name and
`missing_album_artist_action: folder_name` only fires when folder and tag agree. It was left
unrepaired because nobody held `rw`. One of the 30 Jellyfin items with a populated `Artists` list
and zero `ArtistItems` is a Def Leppard track; whether the repair moves that item is the first
evidence on whether row 1 of `ARTIST_PROOF_ROWS` is an E5 problem rather than an E6 one.

This is not one of the twelve and is recorded separately in the ledger. The repair edits tags in
place on legacy library files (not a beets import): its fence is `tank/media/Music@pre-07-pilot`
(taken in 07-09, before these files were touched) plus a fresh per-file tag capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/PROJECT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: measure the E5 defect, propose an exact repair, and ask</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt</files>
  <read_first>
    - `.planning/PROJECT.md` § Key Decisions — the `missing_album_artist_action: folder_name` row and its "Live defect found and deliberately not repaired: `Def Leppard/Def Leppard (2015)/`" sentence.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-CONTEXT.md` D-19a; `06-11-SUMMARY.md` and `artifacts/06-11-oracle-run.txt` — search both for any other directory reported in the album-folder == artist-folder shape.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-45-03`.
    - `artifacts/07-07-before-state.txt` § CONSUMER BASELINES (the D-24 census list, including the Def Leppard path).
    - `scripts/check-music-consumers.sh` `ma_login` / `ma_api` / `provider_filter` (MA credentials via `$ENV`, never argv).
  </read_first>
  <action>
Record `E5 MEASUREMENT` (all reads; RCs recorded):
1. The repair set: `Def Leppard/Def Leppard (2015)/` plus any directory 06-11 reported in that shape
   (quote the source line), plus a live search from atlantis for `Music/<A>/<A> (YYYY)/` pairs
   where the album folder minus a trailing ` (YYYY)` equals its parent artist folder (record the
   command and every hit).
2. Per file in the set: name, size, owner/mode from atlantis; `ffprobe` tags (album, albumartist,
   artist, artists, title, track, disc); `ffprobe -v error` stderr and exit code; for FLAC also
   `flac -t` if installed (record "not installed" otherwise) and a `mediafile` open inside
   beets-flask (`docker exec -i beets-flask /venv/bin/python -` reading `MediaFile(path)` fields,
   read-only, no `save()`) — the error text identifies the hard-erroring FLAC and whether it is in
   the tag/metadata blocks or the audio stream.
3. Consumer readings: MA — the album's `album_artist` via the MA API (login per run, password via
   `$ENV` in jq); Jellyfin — the album's `AlbumArtist`, and the Def Leppard track's `Artists` and
   `ArtistItems` (it is in the D-24 census).
4. A fresh capture `bash scripts/snapshot-music-tags.sh pre-07-e5 --roots "<dir>"` per directory.

`PROPOSAL`: the exact change per file — field name, current value, new value — derived from the
measurement (expected: set `albumartist` = the artist folder's value where it is missing/empty;
set `album` only if it disagrees with the intended album name, stated). For the hard-erroring FLAC:
if the error is in the metadata blocks and `mediafile` can open and rewrite it, include it; if the
audio stream is damaged, EXCLUDE it and record it as an unrepaired finding (no re-encode — that
rewrites audio, which this project keeps off). The tool: `mediafile` inside beets-flask
(`MediaFile(path)`, set the named fields, `save()`), run as the container's service user so
ownership is preserved. Recovery if the repair is wrong: file copy of the named files from
`/mnt/tank/media/Music/.zfs/snapshot/pre-07-pilot/…` — ⛔ never `zfs [r]ollback`.
Recommendation stated; `hold` first.
  </action>
  <decision>Apply the proposed E5 tag repair to the named files?</decision>
  <context>Legacy library files, not a beets import; the Music snapshot from 07-09 predates the change and a fresh tag capture is taken. MA and Jellyfin are re-read afterwards (MA in plan 07-15).</context>
  <options>
    <option id="hold">
      <name>Hold — repair nothing</name>
      <pros>No write to legacy files.</pros>
      <cons>E5 stays open as permanent MA noise; DEF-06-45-03 stays untested.</cons>
    </option>
    <option id="apply">
      <name>Apply the proposal exactly as listed</name>
      <pros>Closes the live defect in the window that exists for it.</pros>
      <cons>In-place tag writes to legacy files (fenced).</cons>
    </option>
    <option id="apply-excluding">
      <name>Apply, excluding named files</name>
      <pros>Operator control per file (e.g. the erroring FLAC).</pros>
      <cons>A partial repair; excluded files recorded as open.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold`, `apply`, or `apply-excluding <file,...>`. Any other answer is treated as `hold`. The executor writes the verbatim answer on an `OPERATOR ANSWER` line and then a normalised `DISPOSITION: hold|apply|apply-excluding` line (G-10).</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt && /usr/bin/grep -qF 'E5 MEASUREMENT' "$A" && /usr/bin/grep -qF 'PROPOSAL' "$A" && /usr/bin/grep -qF 'Def Leppard' "$A" && /usr/bin/grep -qF 'OPERATOR ANSWER' "$A" && /usr/bin/grep -qE '^DISPOSITION: (hold|apply|apply-excluding)' "$A" && ! /usr/bin/grep -qE '(password|Token=|api_key)[=:] *[A-Za-z0-9]' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - The repair set derived from 06-11's record AND a live search, with every hit (or "none besides Def Leppard") recorded with its command.
    - Per-file tags, errors and the hard-erroring FLAC identified with the error's location (metadata vs audio).
    - MA and Jellyfin readings recorded without any credential in the artifact.
    - `pre-07-e5` capture taken; proposal lists field/current/new per file; recovery route stated as a file copy.
    - Answer recorded verbatim with UTC time, followed by a normalised `DISPOSITION:` line.
  </acceptance_criteria>
  <done>The defect is measured and an exact repair is proposed and answered.</done>
</task>

<task type="auto">
  <name>Task 2: apply exactly the approved repair and verify it on the file</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `artifacts/07-14-e5-repair.txt` (proposal and answer — re-read).
    - `scripts/diff-music-tags.sh`; `scripts/check-music-consumers.sh` `jf_api`.
  </read_first>
  <action>
On `hold`: record in the ledger "E5: held, not repaired" and stop.
Otherwise: for each approved file, run ONE `docker exec -i -u <service user> beets-flask
/venv/bin/python -` program (fed on stdin, path as argv) that opens `MediaFile`, asserts each
field's CURRENT value equals the proposal's "current" (refuse the file on mismatch — state moved
since the gate), sets exactly the proposed fields, and `save()`s. Record per file the RC and the
before/after field values printed by the program. No other field, file or directory is touched; no
rename.

Verify: `ffprobe` each file (new values present on the file); owner/mode from atlantis unchanged
(`568:568`); `snapshot-music-tags.sh post-07-e5 --roots "<dir>"`; `diff-music-tags.sh pre-07-e5
post-07-e5` → expected exit 0, only the proposed fields in FIELDS_GAINED/CHANGED, zero dropped
(any other change is a finding); `ffprobe -v error` on the previously erroring FLAC (record whether
the error is gone). Jellyfin: one targeted `POST /Library/Media/Updated` listing the repaired files
with `UpdateType` `Modified`, address resolved fresh; after ≥ 60 s read the album's `AlbumArtist`
and the Def Leppard census track's `Artists`/`ArtistItems` (recorded — plan 07-15 takes the D-24
after-reading). ⛔ No `Full[R]efresh`. MA is not synced here (plan 07-15).

Ledger: an `E5 REPAIR (named operation, not one of the twelve)` entry with files, fields, and
verdicts. Commit: `docs(07-14): E5 Def Leppard repair applied and verified (D-24)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-14-e5-repair.txt && /usr/bin/grep -qF 'E5' .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt && { /usr/bin/grep -qE '^DISPOSITION: hold$' "$A" || { /usr/bin/grep -qF 'post-07-e5' "$A" && /usr/bin/grep -qiE 'diff-music-tags' "$A"; }; } && test "$(/usr/bin/grep -cE 'Full[R]efresh|zfs [r]ollback' "$A")" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Each approved file written by one guarded `MediaFile` program with a current-value precondition; per-file RC and before/after values recorded.
    - ffprobe shows the new values; ownership/mode unchanged from atlantis; `diff-music-tags.sh` exit 0 with only the proposed fields changed and zero dropped.
    - The erroring FLAC's post-repair `ffprobe -v error` result recorded.
    - One targeted Jellyfin POST; census track re-read; no MA write; unbracketed `FullRefresh`/`zfs rollback` absent.
    - Ledger records E5 as a named operation distinct from the twelve.
  </acceptance_criteria>
  <done>E5 is repaired on the files (or held), verified by the same no-loss instrument as the pilot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-place tag writes → legacy library files | Not a beets import; no beets undo covers them. |
| MA credentials | Password login per run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14-01 | Tampering | legacy files | mitigate | Fenced by `tank/media/Music@pre-07-pilot` (predates the write) + `pre-07-e5` capture; per-field current-value precondition; only named fields written; recovery by file copy from `.zfs/snapshot/`. |
| T-07-14-02 | Tampering | audio stream of the erroring FLAC | mitigate | No re-encode; damaged audio is recorded, not repaired. |
| T-07-14-03 | Tampering / DoS | `fast/appdata/arrs` or `tank/media/Music` rollback | mitigate | `zfs [r]ollback` forbidden in this plan; the artifact is screened for the unbracketed verb. |
| T-07-14-04 | Information disclosure | MA password / Jellyfin key | mitigate | `$ENV` in jq and process substitution; mode-600 root secrets; artifact screened. |
| T-07-14-05 | Tampering | Jellyfin metadata | mitigate | Targeted file-scope POST only; `Full[R]efresh` absent. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- E5 measured with its repair set; proposal answered; repair applied exactly; diff exit 0 with only intended changes.
</verification>

<success_criteria>
- The live E5 defect is closed on the files in the window that exists for it, without loss, and the D-24 after-reading is ready.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-14-SUMMARY.md` when done.
</output>
