---
phase: 07-pilot-12-albums-end-to-end
plan: 10
type: execute
wave: 6
depends_on: ["07-09"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/deferred-items.md
autonomous: false
requirements: [IMPT-01, CONS-04, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "D-10: the gated first album is P01 `Michael Jackson – The Essential` (S2: 32 FLAC, `disctotal = 2`, `disc` {1,2}, four distinct artists) and nothing else is imported until it has cleared criteria 3, 4, the Jellyfin half of 6, 7 and its frozen oracle rows"
    - "D-20: P01 is imported through `02-review`, confirmed by the operator in the beets-flask UI; no scripted `beet import`, no `01-auto`"
    - "D-29 / E9: the match gate is applied in ORDER — `recommendation`, then track count (per disc), then per-track distance — and the accepted candidate's rank inside `musicbrainz.search_limit` (default 5) is recorded"
    - "Criterion 3: every one of the 32 files is read with `ffprobe` and its tags match beets' own row for that item; ownership read FROM ATLANTIS is `568:568`"
    - "Criterion 4: `diff-music-tags.sh pre-07-P01 post-07-P01` is recorded with its exit code, AMBIGUOUS counts, the empty-BEFORE counter, every field gained and every field dropped with a reason — or the import is rejected"
    - "D-07: P01's 32 FROZEN oracle lines are compared with the actual destinations; every divergence is classified (match changed the tag / template rendered wrongly) and never edited"
    - "Criterion 6, Jellyfin half, FIRST: a targeted Default-mode `POST /Library/Media/Updated` at FILE scope, Jellyfin's address resolved fresh; one album, disc numbering {1,2} intact. ⛔ FullRefresh forbidden. Music Assistant is NOT synced here (it never purges and the fence does not cover its DB) — its half is plan 07-15's, after the undo cycle"
    - "P-01: BEFORE the first library write, Music Assistant's add-on log is read for recurring provider-sync lines over a stated 24 h window (MA 2.11.0b0 exposes no sync-interval setting, measured 2026-09-25); the reading fails closed — `COULD NOT LOOK` is distinct from `NONE SEEN` — and a periodic sync FOUND or a could-not-look STOPs at a hold. No barrier is invented"
    - "Criterion 8 (partial): P01's source folder manifest is unchanged after the import"
    - "P7R2-05: nothing is staged unless the ledger's NEWEST 07-09 token is `07-09-GRANTED` (read by 07-09's own extraction command) AND a FRESH parsed effective reading taken in this plan shows `import.copy=true`, `import.move=false`, `import.write=true`; any other reading is `STOP STATE 07-10-PRECONDITION` — a writer someone restarted after a 07-09 stop cannot walk past 07-09's G-05 gate"
    - "D-18: at any stop the ledger records which albums landed; nothing auto-rolls"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt"
      provides: "Staging, D-29 gate, operator answer, criteria 3/4/6-JF/7/8 readings, oracle comparison for P01"
      contains: "P01"
  key_links:
    - from: "the operator's confirm in 02-review"
      to: "library.db items for P01"
      via: "read-only sqlite item count and paths"
      pattern: "mode=ro"
---

<objective>
Import ONE album — the one chosen to make the gate non-vacuous — through the front end the operator
will own, and verify it against every per-album criterion before anything else is imported.

Purpose: D-10 applies plan 05-09's lesson: a pilot album with nothing to change passes vacuously.
P01 is multi-disc (criterion 6's "disc numbering intact", D-14's flat disc-prefixed naming),
multi-artist, and has enough tag surface that QUAL-02 cannot come back empty. D-15 then backs it out
immediately (plan 07-11) — so this is also the state the undo must restore from and the re-run must
reproduce; record it exactly.

Music Assistant is deliberately NOT synced in this plan: MA never purges stale entries and the ZFS
fence does not cover MA's database, so exposing MA to a state that plan 07-11 is about to undo
would leave an entry nothing can roll back. Jellyfin handles removals on its own scan. MA's half of
criterion 6 for P01 is taken once, in plan 07-15, after the re-run has restored the identical state
(carried Phase 6 D-25: Jellyfin first, Music Assistant last).

⛔ No `zfs [r]ollback` anywhere; on `fast/appdata/arrs` forbidden absolutely (D-16). ⛔ No
`Full[R]efresh`. ⛔ No retry of the mtime route (`DEF-06-45-02`). No album other than P01 is staged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stage P01 into `02-review` by reflink copy and read the preview</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt</files>
  <read_first>
    - `artifacts/07-09-fence-and-grant.txt` (fence names, grant state) and `artifacts/07-pilot-ledger.txt`.
    - `artifacts/07-07-source-manifest-before.tsv` (P01 rows) and `07-SAMPLE.md` (P01 path, 32 files).
    - CLAUDE.md § Constraints — `rsync -a` fails on tank (`mkstemp … Operation not permitted`, exit 23); reflink works only within `tank/downloads`; `chmod` fails EPERM; ownership `568:568`.
    - `stacks/selfhosted/arrs/beets/flask-config.yaml` — `02-review` path `/downloads/complete/nzb/_inbox/02-review`, `debounce_before_autotag: 30`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-12-preview-crosscheck.txt` — how Phase 6 read a `02-review` preview's candidates (endpoint or UI), reused here.
    - `scripts/check-music-consumers.sh` header — the HA route (`HA_SSH_KEY`/`HA_SSH_PORT`/`HA_SSH_USER` in `~/.claude/secrets/ha-deercrest.env` on the workstation, never copied to LXC 100) and `MA_LOCAL_PROVIDER_INSTANCE`; `~/.claude/memory/tools/home-assistant.md` (the `ha` CLI "unauthorized" workaround).
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-REVIEWS.md` P-01 row (what was measured: no sync-interval setting; what was not: whether a periodic sync runs).
  </read_first>
  <action>
Preconditions (recorded, RC read): both pre-07-pilot snapshots present (exact whole-line), beets-flask
`/media` RW=true, watchdog lists two inboxes, `_inbox/02-review` holds no audio, `library.db` items 0.

**How 07-09 ended, and the import keys NOW (P7R2-05) — before anything else.** (1) Read the
ledger's newest 07-09 token with plan 07-09's extraction, verbatim:
`LC_ALL=C /usr/bin/grep -oE '^(STOP )?STATE 07-09-[A-Z][A-Z-]*[A-Z]' <ledger> | tail -n 1` —
write `PRECONDITION 07-09 TOKEN: <token>`; it must be `07-09-GRANTED` (a stop token, `none`, or
anything newer than the grant means 07-09 did not end in a grant, whatever the estate now looks
like). (2) Re-take the effective import keys fresh, exactly as 07-09 Task 2's G-05 gate did:
`bash scripts/check-beets-config.sh` from the workstation (the server-committed config, the value
that governs imports), exit recorded, each of `import.copy/move/write` parsed as the YAML boolean,
never a substring; write `PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)` or
`PRE-STAGING IMPORT KEYS: FAIL — <key>=<value | could not look>`. If (1) is not `07-09-GRANTED`,
(2) is not PASS, or any precondition above is false or a could-not-look: STOP — nothing is
staged, the ledger gets `STOP STATE 07-10-PRECONDITION: <the reading>; fence held; no album staged
or landed` (at the start of its own line), and Task 2's gate is presented with `hold` as its only
option. The MA gate below is not run after this STOP.

**MA PERIODIC-SYNC GATE (P-01) — before any staging, reads only.** From the workstation, over the
HA SSH route (key path from the secrets file, never argv-echoed into the artifact), read the Music
Assistant add-on's log (`ha addons logs <MA add-on slug>`, slug resolved from `ha addons` output
and recorded; bounded Linux-side on the HA end, ssh RC read). The stated window is the 24 h ending
at the read. Record the first and last timestamps and the line count retrieved. Derive the
provider-sync log pattern from the installed MA version's own source or from a sync line of a
recorded Phase 6 manual sync (record where it came from), and DRIVE it against a control line
first (a real sync line → 1, a non-sync line → 0, CONVENTIONS §8.3). Then write exactly one of:
`MA PERIODIC SYNC: NONE SEEN over <first ts> … <last ts> (<n> lines, window 24 h covered)`;
`MA PERIODIC SYNC: FOUND — <each matching line, its time, and the interval between them>` (any
provider-sync line in the window that no recorded operator or executor action explains counts);
or `MA PERIODIC SYNC: COULD NOT LOOK — <reason>` (ssh/`ha` failure, empty output, an unparsable
log, or a retrieved log whose first timestamp is later than the window start — widening the
retrieval by requesting more lines is allowed, shortening the window is not). **FOUND or COULD NOT
LOOK is a STOP:** nothing is staged, the ledger gets (at the start of its own line) `STOP STATE 07-10-MA-PERIODIC-SYNC: <FOUND |
COULD NOT LOOK>; fence held, grant deployed, no album staged or landed`, and Task 2's gate is
presented with `hold` as its only option. The plan does not invent a barrier (no MA setting is
changed, no provider disabled); the operator decides what the barrier is. Only `NONE SEEN`
continues to staging.

Stage from atlantis as real root: `cp -R --reflink=always "<P01 source folder>"
"/mnt/tank/downloads/complete/nzb/_inbox/02-review/"` (no `-a`/`-p` — mode/ACL preservation fails
EPERM on this pool). If reflink is refused (EXDEV / EOPNOTSUPP), record the error verbatim and use a
plain `cp -R` — `tank` has ≥5 T free (re-read in 07-09); say which was used. Then `chown -R 568:568`
on the STAGED copy only (assert the target path begins
`/mnt/tank/downloads/complete/nzb/_inbox/02-review/` before issuing it). Assert the staged copy's
per-file sha256 set equals P01's rows in the 07-07 manifest and that the SOURCE folder's manifest is
unchanged.

Wait for the watchdog (debounce 30 s) and the preview to complete (bounded, poll up to 15 min; a
debounce is not a failure). Read the preview's candidate list the way 06-12 did (beets-flask's own
read endpoint through Traefik/Authelia or from inside the container, read-only; if neither is
reachable, record COULD NOT LOOK and have the operator transcribe the values at task 2). Record for
the top candidates (up to `search_limit` = 5): rank, MusicBrainz release id, title, album artist,
country, media/disc count, track count per disc, `recommendation`, overall distance, per-track
distance extremes and any missing/unmatched tracks. Apply D-29 in ORDER and write the verdict for
each candidate: (1) `recommendation` — `none` refuses; (2) track count — per disc, candidate
tracks == files (16 + 16 or as measured); a mismatch refuses (CLAUDE.md: never accept without a
track-count check); (3) per-track distance — list every track with a non-zero title/length penalty.
Name the candidate that passes all three at the lowest rank, or state that none does.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt && L=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt && test -s "$A" && T9="$(LC_ALL=C /usr/bin/grep -oE '^(STOP )?STATE 07-09-[A-Z][A-Z-]*[A-Z]' "$L" | tail -n 1 | /usr/bin/grep -oE '07-09-[A-Z-]+$' || true)" && T10="$(LC_ALL=C /usr/bin/grep -oE '^STOP STATE 07-10-[A-Z][A-Z-]*[A-Z]' "$L" | tail -n 1 | /usr/bin/grep -oE '07-10-[A-Z-]+$' || true)" && if [ "$T9" = 07-09-GRANTED ] && /usr/bin/grep -qxF 'PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)' "$A" && [ "$T10" != 07-10-PRECONDITION ]; then /usr/bin/grep -qxF 'PRECONDITION 07-09 TOKEN: 07-09-GRANTED' "$A" && /usr/bin/grep -qE '^MA PERIODIC SYNC: (NONE SEEN|FOUND|COULD NOT LOOK)' "$A" && { ! /usr/bin/grep -qE '^MA PERIODIC SYNC: NONE SEEN' "$A" || { /usr/bin/grep -qF 'D-29' "$A" && /usr/bin/grep -qiE 'recommendation' "$A" && /usr/bin/grep -qiE 'reflink' "$A" && /usr/bin/grep -qF '_inbox/02-review' "$A"; }; } && { /usr/bin/grep -qE '^MA PERIODIC SYNC: NONE SEEN' "$A" || test "$T10" = 07-10-MA-PERIODIC-SYNC; }; else test "$T10" = 07-10-PRECONDITION; fi</automated>
  </verify>
  <acceptance_criteria>
    - Preconditions recorded and all true — including `PRECONDITION 07-09 TOKEN: 07-09-GRANTED` (07-09's extraction, the NEWEST 07-09 token) and a fresh `PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)` compared by parsed value (P7R2-05); otherwise `STOP STATE 07-10-PRECONDITION` at the start of a ledger line, nothing staged (the verify computes the 07-09 token itself and requires this STOP whenever the grant or the PASS is missing), and Task 2 offers `hold` only.
    - One `MA PERIODIC SYNC:` line in one of its three forms, with the log's slug, first/last timestamps and line count, the pattern's source and its two-way control drive; on FOUND or COULD NOT LOOK nothing was staged and the ledger holds `STOP STATE 07-10-MA-PERIODIC-SYNC`.
    - The staging method (reflink or plain copy) recorded; staged sha256 set == P01 manifest rows; source manifest unchanged; `chown` targeted only the staged copy.
    - The candidate table and a D-29 verdict per candidate in the stated order, with a named passing candidate or "none passes".
  </acceptance_criteria>
  <done>P01 sits in `02-review` with its preview read and the D-29 gate evaluated.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: gate — import P01 with the named candidate, or hold</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt</files>
  <read_first>
    - `artifacts/07-10-p01-import.txt` task-1 sections.
    - `07-EXPECTED-TREE.txt` — P01's 32 FROZEN lines (the prediction the result will be compared with).
  </read_first>
  <action>
Present: the D-29 table and verdicts; the candidate recommended (lowest-rank candidate passing all
three checks) and its `search_limit` rank; P01's frozen predicted destination directory and the
first and last predicted file names; what a confirm writes (32 copied files under `/media/Music`,
tags written into the COPIES — `import.copy`, sources untouched); that the fence is in place and the
undo is plan 07-11. Recommend `hold` if no candidate passes. The OPERATOR performs the import in the
beets-flask UI (`02-review`, choose the named candidate, confirm) — D-20's per-album confirmation is
the human act; the executor does not press it. Record the answer verbatim with UTC time.
  </action>
  <decision>Import P01 through 02-review with the named candidate?</decision>
  <context>First write into the library. Only P01. MusicBrainz match via preview; as-is is not appropriate for S2 unless no candidate passes D-29 (then hold).</context>
  <options>
    <option id="hold">
      <name>Hold — do not import</name>
      <pros>Always safe; correct if no candidate passes D-29 or anything above is a could-not-look.</pros>
      <cons>The pilot stops here; the ledger records a STOP state with nothing landed.</cons>
    </option>
    <option id="imported">
      <name>Imported — I confirmed the named candidate in the UI</name>
      <pros>Proceeds to verification.</pros>
      <cons>The library now holds one beets-imported album.</cons>
    </option>
    <option id="imported-other">
      <name>Imported with a DIFFERENT candidate (name it)</name>
      <pros>Operator judgement recorded.</pros>
      <cons>Its D-29 verdict must be recorded after the fact; a failed track-count check becomes a finding.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold`, `imported`, or `imported-other <release id>`. Any other answer is treated as `hold`. After any Task 1 STOP (`07-10-PRECONDITION` or `07-10-MA-PERIODIC-SYNC`) only `hold` is accepted.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt && /usr/bin/grep -qE 'OPERATOR ANSWER' "$A" && /usr/bin/grep -qE '^DISPOSITION: (hold|imported|imported-other)' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - The answer is recorded verbatim with a UTC timestamp on an `OPERATOR ANSWER` line, followed by a normalised `DISPOSITION: <option id>` line (unrecognised → `hold`; forced `hold` after any Task 1 STOP — precondition or MA-sync).
    - On `hold`: the ledger gains a STOP STATE entry "P01 staged, not imported; albums landed: none" — or, after a Task 1 STOP, "nothing staged; albums landed: none" (the hold text states what is true, as P7R2-19 requires of 07-09's) — and the plan ends successfully.
  </acceptance_criteria>
  <done>The operator has imported P01 through the chosen front end, or held.</done>
</task>

<task type="auto">
  <name>Task 3: verify P01 — criteria 3, 4, 7, 8, the oracle rows, and Jellyfin first</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `scripts/diff-music-tags.sh`, `scripts/snapshot-music-tags.sh`, `scripts/check-music-import.sh` (deployed at `/mnt/fast/stacks`).
    - `scripts/check-music-consumers.sh` — `jf_api` (key via process substitution), secrets mode gate, `JELLYFIN_ADDR` from `docker inspect jellyfin` resolved per call, `JELLYFIN_MUSIC_LIBRARY_ID`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/artifacts/06-41-conf04-reprobe-drive.txt` / `06-42-conf04-reprobe-after.txt` — the targeted `POST /Library/Media/Updated` body shape at file scope and `LibraryMonitorDelay = 60`.
    - `07-EXPECTED-TREE.txt` P01 lines.
  </read_first>
  <action>
Skip entirely on `hold`. Otherwise, all reads bounded, RCs recorded:

**Beets' view (read-only sqlite `mode=ro` via `docker exec beets-flask /venv/bin/python`):** P01's
album row (id, albumartist, album, mb_albumid, albumtype, year, label, disctotal) and its item rows
(path, title, artist, artists, album, albumartist, track, tracktotal, disc, disctotal,
mb_albumid, mb_trackid). Assert 32 items.

**Criterion 3:** for each item path (host path `/mnt/tank/media/Music/…` = container `/media/Music/…`)
run `ffprobe -v error -show_format -show_streams -of json` on LXC 100 and compare the file's tags
with the DB row for title, artist, album, albumartist, track, disc, and the MusicBrainz album id
(tag key per container: FLAC `MUSICBRAINZ_ALBUMID`); any mismatch is a finding naming field and
file (the EPERM-as-warning class). From ATLANTIS as real root: `stat -c '%u:%g %a'` of every file
and the album directory; expected `568:568` — a different owner is a finding (do NOT chown; it is
adjudicated at plan 07-11's gate as possible hand-repair). Record also the beets-flask log lines
for this import containing `EPERM`, `PermissionError` or `Error writing` (count, driven against a
control string first).

**Criterion 4:** `bash scripts/snapshot-music-tags.sh post-07-P01 --roots "<album dir>"` on LXC
100; then `diff-music-tags.sh pre-07-P01.ndjson.gz post-07-P01.ndjson.gz` (text and `--json`),
scratch beside copies under `/mnt/fast/safety/phase07/diff/P01/`. Record exit code, MATCHED (32),
MISSING_AFTER, AMBIGUOUS_BEFORE/AFTER, the empty-BEFORE counter, and EVERY field gained / changed /
dropped. For each dropped field write its reason (e.g. a rip-tool comment replaced by the
MusicBrainz record) or mark it `UNEXPLAINED` — an unexplained drop is a reject candidate for the
07-11 gate. Exit 3 is UNKNOWN, not pass.

**Oracle (D-07):** extract P01's 32 FROZEN lines from `07-EXPECTED-TREE.txt`; `LC_ALL=C` sort the
32 actual container paths; `diff` them. For every differing line classify: `MATCH-CHANGED-TAG`
(the directory/file differs only because MusicBrainz supplied different `albumartist`/`album`/
`title` text than the file's own tag — show the tag before and after) or `TEMPLATE-DEFECT` (the
rendering differs for the same field values: e.g. not `0[12]-NN`, a `Disc N/` subdir, `-1 - `).
Never edit the frozen lines. Write each finding to the phase `deferred-items.md` as `DEF-07-10-NN`.

**Criterion 7:** run `bash /mnt/fast/stacks/scripts/check-music-import.sh` on LXC 100; record exit
and summary; expected exit 0 with `items read: 32`.

**Criterion 8 (P01):** re-take P01's source manifest from atlantis; assert identical to 07-07's
rows.

**Criterion 6, Jellyfin half (FIRST; MA not touched):** resolve `JELLYFIN_ADDR` fresh from
`docker inspect jellyfin`; send ONE `POST /Library/Media/Updated` with a JSON body listing each of
the 32 file paths as Jellyfin sees them (`/media/Music/…`, the same root `ARTIST_PROOF_ROWS` uses)
with `UpdateType` `Created` — Default mode, file scope, key via process substitution. Record the
HTTP status (204 expected). Wait ≥ 60 s (`LibraryMonitorDelay`) then poll (bounded, up to 20 min)
`/Items` for the album. Assert: exactly one MusicAlbum for P01; its `AlbumArtist`; 32 Audio
children; `ParentIndexNumber` values exactly {1,2}; `IndexNumber` per disc 1..N with no gaps.
Record, for P01 tracks whose `ARTISTS` tag has ≥2 values, the Jellyfin `ArtistItems` count against
the tag count (the first genuine new-import reading bearing on E6's first measurement; recorded,
not scored). ⛔ No `Full[R]efresh`, no per-item refresh, no library-wide scan.

Append to the ledger: P01 LANDED at <time>, destination dir, item count 32, candidate id, and the
verdict lines above. If anything above is a finding, do not proceed to other albums — plan 07-11's
gate adjudicates it. Commit the artifacts: `docs(07-10): P01 imported through 02-review and verified (D-10, D-20)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-10-p01-import.txt && L=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt && { /usr/bin/grep -qE '^DISPOSITION: hold$' "$A" || { for s in 'Criterion 3' 'Criterion 4' 'Criterion 7' 'Criterion 8' 'Jellyfin' 'FROZEN'; do /usr/bin/grep -qF "$s" "$A" || exit 1; done && /usr/bin/grep -qF 'P01 LANDED' "$L"; }; } && test "$(/usr/bin/grep -cE 'Full[R]efresh|zfs [r]ollback' "$A")" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 32 items in `library.db` for P01; ffprobe-vs-DB comparison recorded per file with the mismatch count; ownership from atlantis recorded per file with the count not equal to `568:568`.
    - `diff-music-tags.sh` exit code, MATCHED 32, AMBIGUOUS counts, empty-BEFORE counter, and every gained/changed/dropped field listed; every drop has a reason or is marked UNEXPLAINED.
    - The 32-line oracle diff recorded with each divergence classified; findings filed as `DEF-07-10-NN`; `07-EXPECTED-TREE.txt` unchanged (`git diff --exit-code HEAD -- .planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt`).
    - `check-music-import.sh` exit and summary recorded.
    - P01 source manifest identical to 07-07's rows.
    - Jellyfin: one targeted POST (status recorded), one album, 32 tracks, discs {1,2}; `JELLYFIN_ADDR` resolved in this run, not a literal; MA untouched (no MA write verb in the transcript).
    - The unbracketed `FullRefresh` and `zfs rollback` tokens appear 0 times in the artifact (recipe driven against a control first).
    - Ledger updated with P01 LANDED.
  </acceptance_criteria>
  <done>P01 is in the library, every per-album criterion has a recorded reading, and nothing else has been imported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's UI confirm → a write into the library | The first real import. |
| executor → Jellyfin API with an API key | A write verb (targeted scan) on the consumer. |
| staging copy → `_inbox/02-review` watched by the watchdog | Anything placed there is previewed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Tampering | source folder | mitigate | Staged by copy (reflink or plain), never moved; `import.copy`; source manifest re-taken and asserted identical. |
| T-07-10-02 | Tampering | wrong MusicBrainz match | mitigate | D-29 ordered gate (recommendation → track count → distance) before the operator confirms; track count per disc mandatory. |
| T-07-10-03 | Repudiation | "imported" meaning only "in the DB" | mitigate | Criterion 3 compares `ffprobe` on each file with the DB row; EPERM log lines counted. |
| T-07-10-04 | Tampering | Jellyfin metadata rewrite | mitigate | Only the targeted Default-mode `POST /Library/Media/Updated` at file scope; `Full[R]efresh` absent (grep driven). |
| T-07-10-05 | Tampering | MA stale entries outside the fence | mitigate | MA not synced until after the undo cycle (plan 07-15); MA's add-on log read for periodic provider syncs over 24 h BEFORE the first write — FOUND or COULD NOT LOOK stops at a hold (P-01). |
| T-07-10-06 | Information disclosure | Jellyfin API key | mitigate | `jf_api` pattern — key via process substitution; mode-600 root secrets gate; artifact screened. |
| T-07-10-07 | Tampering | `fast/appdata/arrs` | mitigate | `zfs [r]ollback` forbidden; no zfs write verb in this plan. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- P01 staged by copy, D-29 gate recorded, operator's confirm recorded.
- Criteria 3, 4, 7, 8 (P01) and the Jellyfin half of 6 recorded; oracle diff classified; MA untouched.
</verification>

<success_criteria>
- Exactly one album has landed, and every per-album instrument has read it, before the undo is exercised and before any other album is staged.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-10-SUMMARY.md` when done.
</output>
