# 07-10-p01-import.txt — plan 07-10, P01 `Michael Jackson – The Essential` (D-10, D-20)
# Executor run 1, 2026-09-26, started 15:10:08Z. Workstation and host (/mnt/fast/stacks) both at
# 460dfce, host porcelain 0 lines.

## Task 1 — preconditions (P7R2-05), read before anything else

(1) Ledger's newest 07-09 token, by 07-09's own extraction, verbatim:
    LC_ALL=C /usr/bin/grep -oE '^(STOP )?STATE 07-09-[A-Z][A-Z-]*[A-Z]' artifacts/07-pilot-ledger.txt | tail -n 1
    -> `STATE 07-09-GRANTED`
PRECONDITION 07-09 TOKEN: 07-09-GRANTED

(2) Effective import keys, taken fresh: `bash scripts/check-beets-config.sh` from the workstation,
    15:10Z, EXIT 0, FAILURES total 0, arm 1 (server-committed) not blind, READY_ROUTE
    watchdog-log-line. D-29 before == after (library.db fbbdde0c…, state.pickle f6a9a1ad…).
    Values extracted from the ANSI-stripped output with anchored sed (whole-value capture, compared
    as exact strings, never a substring): copy=[true] move=[false] write=[true].
PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)

(3) Remaining preconditions (each command's RC read):
    - Fence, `zfs list -H -t snapshot` from atlantis (via LXC 100), RC 0; exact whole-line names
      present: `tank/media/Music@pre-07-pilot` (creation 1790434107 = 14:48:27Z) and
      `fast/appdata/arrs@pre-07-pilot` (1790434108 = 14:48:28Z).                     TRUE
    - beets-flask mounts (`docker inspect`, RC 0): /media RW=true, /downloads RW=true,
      /config/config.yaml RW=false, /config/beets-flask/config.yaml RW=false.        TRUE
    - Watchdog registration line (check-beets-config section 1): inboxes
      ['/downloads/complete/nzb/_inbox/02-review', '/downloads/complete/nzb/_inbox/03-asis'] — two.
                                                                                      TRUE
    - Inbox audio from atlantis (find over flac/mp3/m4a/ogg/opus/wav/aiff/wma/ape/wv):
      01-auto 0, 02-review 0, 03-asis 0; `_inbox/02-review` is an empty directory.  TRUE
      (The same ssh also ran an unrelated `zfs get sensitive,…` that is not a valid property
      list; it failed and set the ssh RC to 2. The inbox counts were printed before it and are
      unaffected. Recorded rather than hidden.)
    - library.db via `docker exec -u 568 beets-flask /venv/bin/python`, sqlite `mode=ro`, RC 0:
      items 0, albums 0.                                                               TRUE
    - tank avail 5.26T (atlantis, `zfs list`).
    All preconditions TRUE. No PRECONDITION stop.

## Task 1 — MA PERIODIC-SYNC GATE (P-01), before any staging, reads only

Route, as the plan specifies: from the workstation over the HA SSH route (HA_SSH_* from
~/.claude/secrets/ha-deercrest.env, mode 600; key path not echoed), `ha addons` to resolve the MA
add-on slug, then `ha addons logs <slug>`.

Attempt 1, 15:12Z: `ssh -i <key> -p <port> -o BatchMode=yes -o ConnectTimeout=10
    <user>@<HA_SSH_HOST> 'timeout 30 ha addons …'`
    -> `ssh: connect to host 172.16.1.31 port 22: Operation timed out`, ssh RC 255.
    Consistent with the carried 07-09 observation that 172.16.1.31 is reachable from LXC 100 but
    not from the workstation. Control, 15:15:48Z: TCP 172.16.1.31:22 from LXC 100 is OPEN — the
    route exists only from a host that must not hold the NUC key (check-music-consumers.sh header:
    "DO NOT … copy the NUC SSH key onto LXC 100").
Attempt 2 (HA tailnet address + HA REST as a reachability probe): NOT RUN — refused by this
    session's permission policy before execution. No other route was tried.
Also standing against this route even when reachable: ~/.claude/memory/tools/home-assistant.md
    records that the `ha` CLI returns "unauthorized" from the sysadmin SSH user.

MA add-on slug: NOT RESOLVED. Log lines retrieved: 0. First/last timestamps: none.
Sync-line pattern: NOT DERIVED and NOT DRIVEN — with no log retrieved there is nothing to drive a
control against, and a pattern written without its two-way drive would be the §8.3 trap.
No MA setting was read or changed; no provider was disabled; no barrier was invented.

MA PERIODIC SYNC: COULD NOT LOOK — HA SSH route from the workstation timed out (172.16.1.31:22, ssh RC 255); slug unresolved, 0 log lines retrieved, 24 h window not covered

=> STOP (P-01 fails closed). Nothing staged: `_inbox/02-review` untouched, no `cp`, no `chown`,
   no watchdog event, no preview. D-29 not evaluated (no preview exists). Fence held, grant
   deployed, writer running with /media RW=true and zero items. Ledger line appended:
   `STOP STATE 07-10-MA-PERIODIC-SYNC: COULD NOT LOOK; …`.
   Task 2's gate is presented with `hold` as its only option.

## Task 1 — RUN 2 (2026-09-26, 15:2xZ onward): operator-approved HA REST route

OPERATOR ROUTE ANSWER (Task 1 route only — NOT a Task 2 answer): question "07-10 can't read the
Music Assistant add-on log (needed to measure MA periodic sync before the first import). Which
route should it use?"; options "hold for now" / "HA REST API from workstation" / "I'll paste the
log" / "read from LXC 100 (D-39)"; verbatim answer "HA REST API from workstation"; relayed
verbatim by the orchestrator ~2026-09-26T15:2xZ. Terms: read-only GETs through the Supervisor
proxy only; token never in argv; 401/403/404 on logs = COULD NOT LOOK, no other route.

Token handling (CONVENTIONS §7): the Authorization header was built by the shell builtin printf
from HA_TOKEN_FILE (mode 600) and fed to `curl -H @-` on stdin; never on a command line, never
printed. Only GET was issued; no POST/PUT.

Pre-gate re-reads, run 2 (15:31:27Z):
  07-09 token (07-09's extraction): `STATE 07-09-GRANTED`.
  check-beets-config.sh EXIT 0, FAILURES 0, arm 1 blind 0; copy=[true] move=[false] write=[true]
    (anchored sed on ANSI-stripped output, exact-string compare); watchdog on 02-review + 03-asis.
  Fence (atlantis, exact whole-line grep -x, RC 0): fast/appdata/arrs@pre-07-pilot,
    tank/media/Music@pre-07-pilot. Inboxes 01-auto 0 / 02-review 0 / 03-asis 0.
  beets-flask /media RW=true; library.db (mode=ro) items 0.
PRECONDITION 07-09 TOKEN: 07-09-GRANTED
PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)

Route order as instructed:
  HA_URL (LAN, http://172.16.1.31:8123)  GET /api/hassio/addons -> curl rc 28 (connect timeout 15 s), no HTTP code.
  HA_EXTERNAL_URL (https://hass.deercrest.info) GET /api/hassio/addons -> 401 (empty body).
  HA_TAILNET_URL GET /api/hassio/addons -> 401.
  Control, same header, same two routes: GET /api/ -> 200 on both. The token is valid; the
    Supervisor proxy refuses THIS endpoint to it (admin-only path class). /addons/<slug>/info is
    401 likewise.
Slug: `GET /api/hassio/addons` was refused, so the slug was resolved by probing the logs endpoint
  on HA_EXTERNAL_URL: d5369777_music_assistant_beta/logs -> 200; d5369777_music_assistant/logs ->
  400 (not installed). Slug = `d5369777_music_assistant_beta` (consistent with MA 2.11.0b0).
Log retrieval, 15:30:31Z (all GET, all 200 unless stated):
  /logs (default)                               23 lines
  /logs?lines=100000                            23 lines (identical)
  /logs + Range: entries=:-100000:              23 lines (identical)
  /logs + Range: entries=:-5000:5000            23 lines (identical)
  /logs/boots/0                                 23 lines (identical)
  /logs/boots/-1, /logs/boots/-2                400 "Logs only contain 1 boots"
  /logs/latest                                  401
  Widening was attempted five ways and could not go past 23 lines.
Log clock: the newest line reads 16:16:12 while UTC at retrieval was 15:30:31Z, so the add-on logs
  local time, UTC+1 (IST). Converted below.
  First line: 2026-09-26 12:03:33.400 local = 11:03:33Z   (auth: user login)
  Last line:  2026-09-26 16:16:12.089 local = 15:16:12Z   (music.genre)
  Window stated: 24 h ending at the read, 2026-09-25T15:30:31Z … 2026-09-26T15:30:31Z.
  Retrieved coverage: 11:03:33Z … 15:16:12Z, ~4 h 13 min. The first timestamp is ~19 h 33 min
  later than the window start -> the plan's COULD NOT LOOK clause (shortening the window is not
  allowed).
Content, for the record (not a verdict): 23 lines = 3 user logins, 12 AirPlay player
  register/unavailable lines for BYO-MBP-LY62L726TL, 2 Last.fm recommendation lines, and 4
  `[music_assistant.music.genre] Genre mapping scan completed: 0 items mapped` lines in two pairs,
  at 12:16:12Z and 15:16:12Z — a recurring scheduled job, 3 h apart. Whether the scheduler that
  fires it also fires a provider sync is not answerable from this log.
Sync-line pattern: NOT DERIVED. No Phase 6 artifact records a real MA provider-sync log line
  (LC_ALL=C grep over .planning/phases for `sync task|Sync(ing)? .*completed|music_assistant\.music\]`
  -> 0 hits), and none of the 23 retrieved lines is one, so there is no real sync line to drive a
  control against (CONVENTIONS §8.3). An undriven pattern is not used.

MA PERIODIC SYNC: COULD NOT LOOK — run 2, HA REST (hass.deercrest.info) slug d5369777_music_assistant_beta: log capped at 23 lines, 11:03:33Z … 15:16:12Z, 24 h window not covered (starts ~19.5 h late); sync pattern not derivable (no recorded sync line)

=> STOP again (P-01 fails closed). Nothing staged; no cp, no chown, no preview; D-29 not
   evaluated. Ledger gains a second `STOP STATE 07-10-MA-PERIODIC-SYNC` line. Task 2 is hold-only.

## Task 1 — RUN 3 (2026-09-26, 15:4xZ onward): operator-approved MA API route from LXC 100 (D-39)

OPERATOR ROUTE ANSWER (Task 1 route only — NOT a Task 2 answer): question "The MA add-on log only
holds the current boot (~4 h, since ~11:03Z today); the P-01 gate needs 24 h to see whether MA runs
a periodic provider sync. How should 07-10 proceed?"; options "wait, re-run tomorrow" / "MA API
from LXC 100 (D-39)" / "sync barrier" / "hold (decide later)"; verbatim answer "MA API from LXC
100 (D-39)"; relayed verbatim by the orchestrator ~2026-09-26T15:4xZ. Terms: read-only calls only.

Credential handling: /mnt/fast/secrets/ma-deercrest.env gated on mode `600 root` (read: 600 root),
sourced without `set -a`; login exactly as scripts/check-music-consumers.sh ma_login (jq reads
the pair from its own env, body on stdin); bearer passed as `-H @<(printf …)`; token never printed.
MA_URL http://172.16.1.31:8095, login OK. GET /info: server_version 2.11.0b2 (was 2.11.0b0 when
P-01 was measured on 2026-09-25), schema 68.

Calls made (all reads; the command schema was read first, and every call below is declared with a
read scope — config.core.read / system.read — except logging/get, which is `system.manage`
scope but is documented "Return the application log from file" and takes no parameters):
  GET  /api-docs/commands.json      311 commands. Sync/scheduler-related: music/sync (WRITE, not
                                    called), tasks/list|get|log (read), tasks/run|update_schedule|
                                    set_enabled|cancel|retry|remove|clear_finished (WRITE, not called),
                                    logging/get (read).
  config/providers                  30 providers; filesystem_local--XJaJWNUS enabled.
  config/providers/get (fs local)   11 entries; NO interval/schedule key. The sync toggles are
                                    library_sync_{artists,albums,tracks,playlists,podcasts,
                                    audiobooks} = true. This is why 2026-09-25's reading found "no
                                    sync-interval setting": the interval does not live in provider
                                    config.
  config/core                       domains discovery, streams, webserver, players, metadata,
                                    cache, music, player_queues, tasks.
  config/core/get tasks             max_concurrent_tasks 2, log_level GLOBAL. No schedule key.
  config/core/get music             reset_db (action, not invoked), log_level. No schedule key.
  tasks/list                        23 tasks WITH `schedule` objects — the scheduler lives here.
  logging/get                       179 lines, 20,496 bytes, one server start in it.
  (music/synctasks was also sent in the first read script; MA returned "Invalid Command" — a
  read name that does not exist in 2.11.0b2; logged by MA as an ERROR line at 15:47:40Z.)

POSITIVE CONTROL that the read would show a schedule if one existed: the same tasks/list response
carries enabled schedules on 13 recurring tasks, e.g. `playlist_metadata_cleanup` hourly every 2,
`genre_mapping_scan` daily 04:00, `music_sync_builtin--bbsqELSo_*` hourly every 3 — and its
`genre_mapping_scan` last_run 15:16:11.979Z matches the `Genre mapping scan completed` line the
run-2 add-on log shows at 16:16:11.969 local. The read is not blind.

FINDING — the local filesystem provider IS on a periodic schedule (tasks/list, read 15:48:07Z):
  id music_sync_filesystem_local--XJaJWNUS_artist    schedule hourly every 12, enabled  last_run 2026-09-26T09:16:11.967Z  next_run 2026-09-26T21:16:11.967Z
  id music_sync_filesystem_local--XJaJWNUS_album     schedule hourly every 12, enabled  last_run 2026-09-26T09:16:11.968Z  next_run 2026-09-26T21:16:11.968Z
  id music_sync_filesystem_local--XJaJWNUS_playlist  schedule hourly every 12, enabled  last_run 2026-09-26T09:16:13.756Z  next_run 2026-09-26T21:16:13.756Z
  id music_sync_filesystem_local--XJaJWNUS_track     schedule hourly every 12, enabled  last_run 2026-09-26T09:17:23.383Z  next_run 2026-09-26T21:17:23.383Z
  All four: status success, created_at 2026-09-25T21:15:48Z (the server start), last_run_user_id null
  (scheduler-initiated, not a user).

SYNC-LINE PATTERN — derived from REAL provider-sync lines in MA's own log (logging/get), not
guessed; the log is in local time UTC+1 (same offset measured in run 2):
  PAT = \[music_assistant\.Filesystem \(local disk\)\] Found [0-9]+ changed/new items to process for Filesystem \(local disk\)
  Drive (CONVENTIONS §8.3): a real sync line -> 1; `[music_assistant] Loaded music provider
  Filesystem (local disk)` -> 0; `[music_assistant.music.genre] Genre mapping scan completed` -> 0.
  Over the full log: 5 matches —
    2026-09-25 22:16:13.750 / 22:16:13.906 / 22:17:24.469 local = 21:16:13Z / 21:16:13Z / 21:17:24Z (first run after the 21:15:38Z server start)
    2026-09-26 10:16:15.517 / 10:17:24.521 local            = 09:16:15Z / 09:17:24Z (scheduled run, 12 h later; matches tasks/list last_run)
  Interval between runs: 12 h 00 min. Each reports `Found 0 changed/new items`.
  No recorded operator or executor action explains either run (07-09 ran 14:4x–15:0xZ today; no
  plan has called music/sync since Phase 6).
  Log coverage: 2026-09-25T21:15:38Z (server start) … 2026-09-26T15:48:07Z, ~18.5 h — short of 24 h,
  but FOUND does not depend on the full window: the scheduler state plus two logged runs 12 h apart
  settle it.

MA counts, provider-filtered (music/{albums,tracks,artists}/library_items, provider
filesystem_local--XJaJWNUS, limit 5000; control: a bogus instance id returns 0), 15:49:47Z:
  albums 70, tracks 1244, artists 66 — equal to 07-07's baseline 70 / 1,244 / 66.

OPERATOR REQUEST, NOT EXECUTED: the operator also asked (relayed verbatim ~15:5xZ: "can you trigger
music assistant via the api to do the scan") for ONE manual `music/sync` of the local provider as a
positive control. The guarded script (refused to trigger unless the pre-counts equal the baseline and
providers == [filesystem_local--XJaJWNUS]) was REFUSED by this session's permission classifier
before it ran ("Modify Shared Resources"). Nothing was sent to music/sync; nothing was scp'd for it.
It is now also redundant for P-01: the pattern above was derived and driven from real scheduled
sync lines, which is what the manual sync was meant to supply.

MA PERIODIC SYNC: FOUND — filesystem_local--XJaJWNUS syncs every 12 h by MA's own scheduler (tasks music_sync_filesystem_local--XJaJWNUS_{artist,album,playlist,track}, hourly every 12, enabled); logged runs 2026-09-25T21:16:13Z–21:17:24Z and 2026-09-26T09:16:15Z–09:17:24Z (interval 12 h, 0 items each, no operator/executor action behind them); NEXT RUN 2026-09-26T21:16:11Z

=> STOP (P-01: FOUND is a STOP). Nothing staged; D-29 not evaluated. No MA setting was changed
   and no task schedule was touched — the barrier is the operator's decision. Consequence for the
   plan: any P01 import that is in /media/Music at 21:16Z (or any later 12-hourly run) is picked up
   by MA, outside the fence, before 07-11's undo can run — exactly the T-07-10-05 threat.
   Barrier options available through the API (writes, NOT done): `tasks/set_enabled` false on the
   four music_sync_filesystem_local--XJaJWNUS_* tasks for the pilot's duration, or
   `tasks/update_schedule`. Ledger gains `STOP STATE 07-10-MA-PERIODIC-SYNC: FOUND`.

Run 3 pre-gate re-reads (15:50:47Z): 07-09 token `STATE 07-09-GRANTED`; check-beets-config.sh EXIT 0,
FAILURES 0, copy=[true] move=[false] write=[true]; both `@pre-07-pilot` snapshots present (exact
whole-line count 2); inboxes 01-auto 0 / 02-review 0 / 03-asis 0; beets-flask /media RW=true;
library.db (mode=ro) items 0.
PRE-STAGING IMPORT KEYS: PASS (copy=true move=false write=true)
