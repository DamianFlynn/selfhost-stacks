---
phase: 07-pilot-12-albums-end-to-end
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md
  - .planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-06-draw.txt
autonomous: true
requirements: [QUAL-03, IMPT-01]

must_haves:
  truths:
    - "D-04: the twelve are 6 reused bucket-A rows from 06-SAMPLE.md (three S1 incl. the Benson Boone FLAC/MP3 pair, S2 multi-disc, S3 various-artist compilation, S7 singleton) + both S5 `dj-mixes` folders + 4 fresh draws; S6 is OUT because it is a LIBRARY folder, not an import candidate; S4 is OUT because it is bucket B"
    - "QUAL-03 / criterion 2: the compilation (S3) and the multi-disc release (S2) are NAMED by folder path in a committed document before any import"
    - "D-05: one fresh slot is a declared >=4-artist stratum, target 1, whose target and predicate are COMMITTED before its eligible set is measured; if the set is empty the slot reallocates and E6's second measurement is recorded NOT TAKEN — never satisfied by a 3-artist row"
    - "D-06: the three unreserved fresh slots draw from `music/` and `unsorted/` combined, excluding the Now! `Vol NNN` set, by a seeded, reproducible key over the LC_ALL=C folder path, run twice and diffed identical"
    - "D-01: nothing is drawn from `tank/downloads/mybook-music-archive` or `mac-music-archive` (no QUAL-01 root covers them)"
    - "D-07: the reused rows' expected paths are carried BYTE-IDENTICAL from 06-EXPECTED-TREE.txt at its commit (`git show <commit>:…`), never re-derived; new rows are appended; a frozen row failing later is a FINDING, never silently updated"
    - "D-26 / E2: the album-populated-and-distinct predicate is asserted READ-ONLY on the two S5 folders (the natural `bootleg` candidates); `03-asis` stays unused"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md"
      provides: "The declared population, strata, seed, the twelve named folders with P-IDs and import order"
      contains: "P01"
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt"
      provides: "Frozen rows carried from 06-EXPECTED-TREE.txt by commit + appended rows for the fresh draws"
      contains: "FROZEN"
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-06-draw.txt"
      provides: "Derivation transcript, twice-run diff, eligibility counts, D-26 predicate result"
      contains: "draw-a"
  key_links:
    - from: "07-SAMPLE.md § declaration commit"
      to: "07-SAMPLE.md § draw commit"
      via: "git commit order proves target and predicate preceded the eligible set"
      pattern: "DECLARED BEFORE"
    - from: "07-EXPECTED-TREE.txt FROZEN section"
      to: "06-EXPECTED-TREE.txt at its commit"
      via: "digest of `git show <commit>:<path>` extracted lines vs the working file, two hashers"
      pattern: "git show"
---

<objective>
Name the twelve albums before the run (criterion 2 / QUAL-03), by a draw whose targets and
predicates are committed before its eligible sets are known, and write the oracle the run will be
judged against — frozen where Phase 6 already registered a prediction, appended where it did not.

Purpose: the pilot's value is that it meets the hard shapes on content nobody chose because the
config handles it (Phase 6 D-26). Reusing six Phase 6 rows keeps their predictions registered
before anyone knew the answer (D-07); the fresh draws make the sample say something about
`unsorted/`, the population Phase 9 must process (D-06); the reserved ≥4-artist slot is E6's
second measurement, which has drifted untouched through two rounds (D-05).

Discretion exercised (CONTEXT § Claude's Discretion — seed and derivation):
- **Seed, fixed in this plan text before any measurement:** `gsd-07-pilot-draw-2026-09-25`.
- **Key:** for each eligible folder, `sha256(SEED + "\n" + path_bytes)` where `path_bytes` is the
  full folder path encoded `utf-8` with `surrogateescape` (the same byte string `LC_ALL=C sort`
  orders on, per 06-SAMPLE.md); sort ascending by hex digest; ties (none expected) broken by
  `LC_ALL=C` path order; take the first N. This is the Phase 6 key with a declared seed, so it is
  random within the eligible set (D-05's "drawn randomly") and still reproducible and
  byte-ordered.
- **The DJ pair's frozen rows:** D-07 names six frozen rows; the two S5 folders' 20 lines are ALSO
  in `06-EXPECTED-TREE.txt` at the same commit and are ALSO carried byte-identical — strictly
  stronger than re-deriving them. They are additionally re-derived with the same generator and the
  two compared; a difference is a finding naming a config change since 2026-09-21.

**Read the oracle for what it is:** `06-EXPECTED-TREE.txt` renders each file's OWN tags (as-is,
`-A`) through the `paths:` stanza. The pilot imports through `02-review` with MusicBrainz matching
(D-20), so a matched album may legitimately get different `albumartist`/`album` text. Plans 07-10 /
07-12 / 07-13 classify every divergence as either "the match changed the tag" or "the template
rendered wrongly" — never update a frozen line.

Everything here is read-only against `/mnt/tank` and the real beets state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: commit the population, strata targets, predicates, seed and key BEFORE measuring anything</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md` in full — § The population declared first, § stratum targets, § draw mechanism, § The ten drawn folders, § The predicted `%aunique{}` firing, § What this sample does NOT cover.
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md` D-01, D-04, D-05, D-06, D-07, D-10, D-26.
    - `.planning/ROADMAP.md` Phase 7 E6 (both measurements) — by heading text.
  </read_first>
  <action>
Write `07-SAMPLE.md` with, at this commit, ONLY the declaration — under a heading
`## DECLARED BEFORE THE DRAW (committed before any eligibility was measured)`:
1. The twelve slots and their sources: the six reused 06-SAMPLE rows named by full folder path
   (three S1, S2, S3, S7), the two S5 folders by full path, and four fresh slots F1 … F4.
2. Exclusions with reasons: S6 (`/mnt/tank/media/Music/Katy Perry/Teenage Dream (2010)`) — a
   LIBRARY folder drawn read-only under Phase 6 D-05, already imported, **not an import
   candidate**; S4 (`Vol 001`) — bucket B, and criterion 1 says bucket A; D-01 —
   `mybook-music-archive` and `mac-music-archive` are unreachable by construction (no QUAL-01
   root); staging dirs under `_inbox/`; non-music `nzb/*`.
3. The fresh population (D-06): folders directly under `/mnt/tank/downloads/complete/nzb/music/`
   and `/mnt/tank/downloads/complete/nzb/unsorted/` holding ≥1 audio file (extensions as
   `snapshot-music-tags.sh` `AUDIO_EXT`), EXCLUDING the 115 `VA-Now_That.s_What_I_Call_Music__1-115_2023/Vol NNN`
   folders and their parent, and EXCLUDING the eight reused folders. D-06's figure (162 folders /
   3,236 audio files at 2026-09-21) is stated as the expectation to be re-measured, not assumed.
4. F1 — the ≥4-artist stratum (D-05), target 1: eligible when at least one audio file in the
   folder has ≥4 distinct non-empty entries in beets' own multi-valued `artists` field (read via
   `mediafile` in beets-flask, 06-SAMPLE's third instrument) OR in its raw `ARTISTS` tag split on
   `;`. Reallocation rule: if the eligible set is empty, F1 becomes a fourth D-06 draw and E6's
   second measurement is recorded **NOT TAKEN** in this document — never satisfied by a row with
   fewer than four artists.
5. F2 … F4 — the D-06 population minus F1's draw, target 3, no further predicate.
6. Seed `gsd-07-pilot-draw-2026-09-25` and the key exactly as stated in this plan's objective;
   draw order F1 first, then F2–F4 from the remainder; the determinism rule (run twice, diff).
7. P-IDs and IMPORT ORDER, fixed now because `%aunique{}` is evaluated against the library at
   import time: P01 = S2 (gated first, D-10); P02 = S1 Benson Boone American Heart FLAC;
   P03 = S1 Benson Boone Fireworks & Rollerblades; P04 = S1 Benson Boone American Heart MP3
   (after P02, deliberately, so the pair's second arrival is the one that meets the ambiguity);
   P05 = S3 Now 121; P06 = S7 Cyril; P07 = S5 Mastermix.Issue.420.2021; P08 = S5
   Mastermix.Issue.421.2021; P09 = F1; P10 … P12 = F2 … F4. P-IDs are IDENTIFIERS; the ONE
   EXECUTION ORDER (G-12) is: P01 (plan 07-10, then its undo/re-run in 07-11); then P02, P03, P04,
   P05, P06, P09, P10, P11, P12 (plan 07-12); then P07, P08 and any DJ twin (plan 07-13, so DJ
   routing happens in one place). Write this order verbatim under a `### EXECUTION ORDER` heading,
   followed by two machine-readable lines, exactly (P7R2-03 — plans 07-12 and 07-13 derive their
   expected P-ID sets from these, and their verifies require one disposition row per P-ID):
   `EXECUTION SET 07-12: P02 P03 P04 P05 P06 P09 P10 P11 P12` and `EXECUTION SET 07-13: P07 P08`.
   A fresh draw that task 2 flags as a `dj-mixes` twin is moved from the first set to 07-13 by plan
   07-12 Task 1 (recorded there as `MOVED TO 07-13:`); this declaration is never edited.
   The only order-sensitive constraint is P02 before P04 (the `%aunique{}` prediction); plans 07-12
   and 07-13 assert the order after the fact from beets' `albums.added`, so the prediction is read
   against the order that actually happened.
Commit this alone: `docs(07-06): declare the Phase 7 sample before the draw (D-04, D-05, D-06)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md && /usr/bin/grep -qF 'DECLARED BEFORE THE DRAW' "$S" && /usr/bin/grep -qF 'gsd-07-pilot-draw-2026-09-25' "$S" && /usr/bin/grep -qF 'NOT TAKEN' "$S" && /usr/bin/grep -qF 'Teenage Dream (2010)' "$S" && /usr/bin/grep -qF '### EXECUTION ORDER' "$S" && /usr/bin/grep -qxF 'EXECUTION SET 07-12: P02 P03 P04 P05 P06 P09 P10 P11 P12' "$S" && /usr/bin/grep -qxF 'EXECUTION SET 07-13: P07 P08' "$S" && for p in P01 P02 P03 P04 P05 P06 P07 P08 P09 P10 P11 P12; do /usr/bin/grep -qF "$p" "$S" || exit 1; done && git show --stat HEAD | /usr/bin/grep -qF '07-SAMPLE.md'</automated>
  </verify>
  <acceptance_criteria>
    - The declaration commit contains the seven items above and no eligibility count, no drawn fresh folder, and no measured attribute (read by eye; stated in the SUMMARY).
    - S6 and S4 exclusions each carry their reason; D-01's mechanical reason is stated.
    - P01 … P12 and the ONE execution order (P01; P02–P06, P09–P12; P07, P08) under `### EXECUTION ORDER`, with P02 before P04, are fixed in this commit, with the two exact lines `EXECUTION SET 07-12: P02 P03 P04 P05 P06 P09 P10 P11 P12` and `EXECUTION SET 07-13: P07 P08` beneath it (P7R2-03's consumers read these); no sentence says plans import "in P-ID order".
    - This commit is separate from task 2's (its hash recorded in the SUMMARY as the declaration anchor).
  </acceptance_criteria>
  <done>Targets, predicates, seed and order are on record before any eligible set is known.</done>
</task>

<task type="auto">
  <name>Task 2: measure, draw twice, fill the sample record, and assert the D-26 predicate read-only</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-06-draw.txt</files>
  <read_first>
    - `.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md` as committed by task 1.
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-SAMPLE.md` § draw mechanism (the five fence scripts at `/mnt/fast/safety/phase06/`: `phase06-live-tags.py`, `phase06-folders.py`, `phase06-fields.py`, `phase06-strata.py`, `phase06-expected-tree.py`) and § the D-18 register (raw mutagen frame read for `TKEY` / `TXXX:EnergyLevel`).
    - `stacks/selfhosted/arrs/beets/flask-config.yaml` `03-asis` comment (bootleg groups by existing metadata) and ROADMAP Phase 7 E2.
  </read_first>
  <action>
Work under `/mnt/fast/safety/phase07/draw/` on LXC 100 (never `/tmp`). Reuse the Phase 6 fence
scripts by COPY into that directory (record each copy's sha256 equal to the original); write one new
`phase07-draw.py` implementing the declared key. Run beets' field reads with
`docker exec -i beets-flask /venv/bin/python - <args>` feeding the program on stdin — stage NO file
in `/mnt/fast/appdata/arrs/beets/config/` (it would sit beside the vendored config the drift block
compares).

1. Assert the eight reused folders still exist (from atlantis as real root, `test -d`, exact
   paths). A missing one is a STOP — record it; do not substitute.
2. Inventory the D-06 population live; record folder and audio-file counts against 162 / 3,236.
3. Compute F1's eligible set with the declared predicate; draw F1. If empty, apply the
   reallocation rule and write "E6 second measurement: NOT TAKEN — eligible set empty" into
   `07-SAMPLE.md`.
4. Draw F2–F4 from the remainder. Run the whole derivation twice into `draw-a.json` / `draw-b.json`
   (+ logs) and `diff` them: zero lines of difference required; record both digests.
5. For each drawn fresh folder record whether its folder name collides with a `dj-mixes` folder
   name (the DUPE-01 twin class the D-13 amendment covers) and its Phase 6 stratum by the declared
   precedence `S5 → S7 → S2 → S3 → S1 → other` (information only). Summarise the collisions on ONE
   exact line in `07-SAMPLE.md`'s draw section: `DJ TWINS AMONG FRESH DRAWS: <P-IDs space-separated
   | none>` — plans 07-12 and 07-13 read it to move each twin into 07-13's set (P7R2-03).

Fill `07-SAMPLE.md` below the declaration (do not edit the declaration section) with
`## THE DRAW (measured after the declaration commit)`: the population counts; F1/F2–F4 eligible
counts and draws; the twelve-row table (P-ID, folder path, stratum, files, extensions, `disctotal`,
`disc`, distinct `artist`, `albumartist` tag → as-is guess, `album`, `comp`, and — for F1 — the
≥4-artist file(s) and their `artists` values) in 06-SAMPLE's column style; the DJ-field register
(bpm, `TKEY`, `TXXX:EnergyLevel`, genre, comment counts per folder by raw mutagen frame read); the
predicted `%aunique{}` firings given the import order (the Benson Boone pair at minimum; state
which of P02/P04 is predicted to carry the disambiguator under ordered import and that a
MusicBrainz match may populate `label` on both and change the prediction); § What this sample does
NOT cover (rule 2 — covered read-only by 07-05; zero WAV if still zero; any stratum not reached).

**D-26 predicate, read-only:** for both S5 folders read `album` per file via `mediafile`; assert
every file's `album` is non-empty AND the folder's distinct `album` values number exactly 1 (the
intended grouping is one album per folder). Record per file and the verdict per folder
(TRUE/FALSE). State in `07-SAMPLE.md`: `03-asis`/`bootleg` is NOT used by the pilot (D-20 routes
all twelve through `02-review`), so E2's gate stays untested-by-import and carried; this reading
makes its precondition a tested predicate.

Copy the machine-readable outputs' digests (not the files) into `artifacts/07-06-draw.txt` with the
commands, both digests of the twice-run diff, and the D-26 per-file table.
Commit: `docs(07-06): draw the four fresh folders and record the twelve (D-05, D-06, D-26)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=.planning/phases/07-pilot-12-albums-end-to-end/07-SAMPLE.md && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-06-draw.txt && /usr/bin/grep -qF 'THE DRAW (measured after the declaration commit)' "$S" && /usr/bin/grep -qE '^DJ TWINS AMONG FRESH DRAWS: (none|P(09|1[0-2])( P(09|1[0-2]))*)$' "$S" && /usr/bin/grep -qxF 'EXECUTION SET 07-12: P02 P03 P04 P05 P06 P09 P10 P11 P12' "$S" && /usr/bin/grep -qF 'draw-a' "$A" && /usr/bin/grep -qF 'draw-b' "$A" && /usr/bin/grep -qiE 'D-26' "$A" && ! /usr/bin/grep -qF 'mybook-music-archive/' "$S"</automated>
  </verify>
  <acceptance_criteria>
    - The eight reused folders asserted present by exact path.
    - The live population counts recorded beside 162 / 3,236 with any difference stated.
    - F1's eligible count recorded; either a drawn ≥4-artist folder with the file(s) and `artists` values shown, or the NOT TAKEN statement.
    - `draw-a` and `draw-b` diffed with zero difference, both digests recorded.
    - The twelve-row table is complete; S3 (compilation) and S2 (multi-disc) appear by full folder path.
    - The D-26 per-file `album` table and a TRUE/FALSE verdict for each S5 folder are recorded.
    - One exact `DJ TWINS AMONG FRESH DRAWS: <P-IDs | none>` line (fresh draws P09–P12 only), which plans 07-12/07-13 read to move twins (P7R2-03).
    - No file was staged in `/mnt/fast/appdata/arrs/beets/config/` (a before/after listing of that directory is recorded and identical).
    - The declaration section is byte-identical to task 1's commit: `git show <task-1 commit>:<path>` section digest equals the current section digest.
  </acceptance_criteria>
  <done>The twelve are named, reproducibly, with the ≥4-artist slot either filled or recorded NOT TAKEN, and E2's predicate measured.</done>
</task>

<task type="auto">
  <name>Task 3: write `07-EXPECTED-TREE.txt` — frozen rows by commit, appended rows by the same generator</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-06-draw.txt</files>
  <read_first>
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt` header in full (format contract, the root `/media/Music`, derivation method, preconditions P1–P4, the aunique prediction, 02-05 rendering, per-stratum expectations).
    - CONVENTIONS §10 and its canonical example (plan 06-48): base commit, `git show <BASE>:<path>`, two hash implementations.
    - `/mnt/fast/safety/phase06/audit.json` (on LXC 100) — one `{stratum, rule, source, dest}` record per 06 line: the stratum field is how frozen lines are selected.
  </read_first>
  <action>
Anchor: `ANCHOR=$(git log -1 --format=%H -- .planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt)`
(expected to be the commit whose short hash is `cb9f49a`; record the full hash either way).
Extract `git show "$ANCHOR":.planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt`,
strip `^#` lines, and select the lines whose `audit.json` stratum is S1, S2, S3, S5 or S7 —
excluding S4 (`Vol 001`, 30 lines) and S6 (Katy Perry, 12 lines). Assert the selection has
**132** lines (35 + 32 + 44 + 20 + 1) and that every selected line also matches its path-prefix
expectation (cross-check both selectors agree).

Write `07-EXPECTED-TREE.txt` at the phase root:
- Header (all lines `#`): it is an INPUT committed before the first import; consumers strip `^#`
  before diffing; root `/media/Music` (container path); the anchor commit's full hash; the rule
  that FROZEN lines are never edited and a failing frozen line is a finding (D-07); that the
  oracle renders each file's own tags and the pilot imports with MusicBrainz matching, so every
  divergence is classified "match changed the tag" vs "template rendered wrongly"; the per-P-ID
  line counts; the import-order-dependent `%aunique{}` note from `07-SAMPLE.md`.
- `# ===== FROZEN — carried byte-identical from 06-EXPECTED-TREE.txt @ <anchor> =====` followed by
  the 132 lines exactly as extracted (LC_ALL=C order preserved from the source).
- `# ===== APPENDED — derived 2026-09-2x for P09 … P12 by phase06-expected-tree.py (sha256 …) =====`
  followed by the fresh folders' lines, derived by running the Phase 6 generator (copied, sha256
  equal to the fence original) over the fresh folders' beets field reads with the SAME
  preconditions P1–P4 it assumed (as-is, one library, singleton `-s` for a one-file folder).
  If the generator cannot be driven for a subset without modification, copy it, make the minimal
  input-selection change only, record the diff of the change, and assert the rendering functions
  are byte-identical to the original's.
- Re-derive the S5 lines with the same generator and record in the artifact whether they equal the
  frozen S5 lines (difference = finding naming a config change since the anchor, written to the
  phase `deferred-items.md`; the FROZEN lines are not touched).

Byte-identity proof (CONVENTIONS §10): the digest of the 132 extracted lines from
`git show "$ANCHOR":…` equals the digest of the FROZEN section's non-`#` lines in the working file,
computed with `shasum -a 256` AND `openssl dgst -sha256`, all four values recorded. Assert no
bracketed non-year integer appears in any appended line (the numeric-ID landmine; drive the recipe
against a control `/media/Music/A/B [12]/01 T.mp3` first).
Commit: `docs(07-06): commit the Phase 7 expected tree, frozen rows by commit (D-07)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && T=.planning/phases/07-pilot-12-albums-end-to-end/07-EXPECTED-TREE.txt && test -s "$T" && /usr/bin/grep -qF '===== FROZEN' "$T" && /usr/bin/grep -qF '===== APPENDED' "$T" && ANCHOR=$(git log -1 --format=%H -- .planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt) && /usr/bin/grep -qF "$ANCHOR" "$T" && test "$(/usr/bin/awk '/===== FROZEN/{f=1;next} /===== APPENDED/{f=0} f && !/^#/' "$T" | wc -l | tr -d ' ')" -eq 132 && /usr/bin/awk '/===== FROZEN/{f=1;next} /===== APPENDED/{f=0} f && !/^#/' "$T" | while IFS= read -r l; do git show "$ANCHOR":.planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt | /usr/bin/grep -qxF -- "$l" || { echo "not in anchor: $l"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - The FROZEN section holds exactly 132 non-`#` lines, every one present verbatim in `git show <anchor>:06-EXPECTED-TREE.txt` (the verify loop checks each line).
    - Four digests recorded (two hashers × two sources) and equal pairwise.
    - The APPENDED section's line count equals the four fresh folders' audio-file total from `07-SAMPLE.md`.
    - The S5 re-derivation comparison is recorded (equal, or a finding filed without touching the FROZEN lines).
    - No appended line carries a bracketed non-year integer (recipe driven against the control first).
    - `git diff --exit-code HEAD -- .planning/phases/06-tagger-configuration-and-dry-run/06-EXPECTED-TREE.txt` exits 0.
  </acceptance_criteria>
  <done>The pilot's oracle is committed: 132 lines frozen by commit, the fresh rows appended by the same generator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the draw → what the pilot proves | A hand-picked or post-hoc sample proves the config works on cases chosen because it works. |
| the oracle → the verdict on each landed album | An oracle re-derived from the config it judges cannot fail. |
| beets-flask `docker exec` reads → the real `/downloads` and `/config` | Read-only field reads inside the tagger container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Repudiation | sample selection | mitigate | Declaration committed before measurement; seed fixed in the plan; draw run twice and diffed; commit order recorded. |
| T-07-06-02 | Tampering | frozen oracle rows | mitigate | Extracted from `git show <anchor>:…`, checked line-by-line and by two digests; frozen lines never edited; failures become findings. |
| T-07-06-03 | Tampering | `/mnt/fast/appdata/arrs/beets/config/` | mitigate | Programs fed on stdin, no staged files; directory listing before/after identical. |
| T-07-06-04 | Information disclosure | committed sample record | accept | Folder names and tag values only; no credential read or written. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- Declaration commit precedes the draw commit.
- Twelve folders named with P-IDs and import order; S2 and S3 by path.
- Oracle file: 132 frozen lines proven by commit, appended rows derived by the Phase 6 generator.
- D-26 predicate recorded for both S5 folders.
</verification>

<success_criteria>
- Criterion 2's "named in the plan before the run" is satisfied by a commit that predates every Phase 7 write, and the oracle cannot have been fitted to the result.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-06-SUMMARY.md` when done.
</output>
