---
phase: 07-pilot-12-albums-end-to-end
plan: 11
type: execute
wave: 7
depends_on: ["07-10"]
files_modified:
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt
  - .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt
  - .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md
autonomous: false
requirements: [QUAL-04, IMPT-01]

must_haves:
  truths:
    - "D-15: P01 is backed out IMMEDIATELY after it passes (one album at risk, not twelve), then re-run to the exact state that cleared the gate"
    - "D-14: the undo is beets-flask rc6's `UNDO IMPORT`, performed by the operator in the UI, and it is shown to cover THREE things — the tree (P01 destination holds 0 files), the database (P01 items 0) AND `state.pickle` (its decoded `taghistory` no longer holds the staged P01 source path) — not merely the tree and the DB"
    - "Criterion 5 / QUAL-04: the re-run reaches a good state with NO hand-repair of files or database — its destination path list and `audio_md5` set are byte-identical to the first run's, and its QUAL-02 diff output equals the first run's"
    - "If `UNDO IMPORT` does NOT cover `state.pickle`, that is recorded as a FINDING against D-14, and the only permitted remedy is the operator-chosen FILE RESTORE of `state.pickle` from `fast/appdata/arrs@pre-07-pilot`'s `.zfs/snapshot/` — ⛔ never `zfs [r]ollback` of `fast/appdata/arrs` (D-16)"
    - "D-17: `tank/media/Music@pre-06-41-conf04-reprobe` is destroyed MECHANICALLY — no gate — in the same step that records DEF-06-52-01's trigger as fired (a new snapshot on the Music dataset taken AND rollback exercised, both evidenced); if the trigger has not fired it is held and that is recorded. `tank/downloads@pre-phase5` is not touched"
    - "No plan self-declares a verification verdict: the trigger is recorded as FIRED on its two named events; criterion 1 and criterion 5 are scored by `/gsd-verify 07`"
  artifacts:
    - path: ".planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt"
      provides: "Pre-undo witness, undo witness (tree/DB/state.pickle), re-run equality, D-17 act"
      contains: "taghistory"
    - path: ".planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md"
      provides: "DEF-06-52-01 / DEF-06-45-01 status line recording the D-17 trigger outcome"
      contains: "DEF-06-45-01"
  key_links:
    - from: "UNDO IMPORT"
      to: "state.pickle taghistory"
      via: "decoded read of a COPY of state.pickle before import, after import, after undo, after re-run"
      pattern: "taghistory"
    - from: "D-17 trigger fired"
      to: "zfs destroy of exactly tank/media/Music@pre-06-41-conf04-reprobe"
      via: "same step, literal-name fence"
      pattern: "pre-06-41-conf04-reprobe"
---

<objective>
Prove undo on the cheapest possible album at the cheapest possible moment (D-15): back P01 out with
the mechanism the operator will actually reach for (D-14, `UNDO IMPORT`), prove it reverses the
tree, the database AND beets' `incremental` state, re-run to the identical good state, and then
release the Phase 6 snapshot whose release condition this event is (D-17).

Purpose: criterion 5's own reason — rolling back one of tree/state without the other leaves the
release invisible to a retry — is exactly the half `beets.md` flags: `UNDO IMPORT` was verified once
on tree and database, never on `state.pickle`. If it misses `state.pickle`, a re-run is silently
skipped under `incremental: yes` / `incremental_skip_later: yes`, and the re-run step here is what
detects it.

`state.pickle` is read by DECODING A COPY: `docker exec beets-flask cp /config/state.pickle
/tmp/p7-state.<stage>.pickle` then `/venv/bin/python -c` unpickling the copy and printing whether
the staged P01 source path (the `_inbox/02-review/<folder>` container path, as bytes/str) is in
`state['taghistory']` (and any other top-level key, listed) — the live file is never opened for
writing. Its sha256 is recorded at every stage too.

⛔ `zfs [r]ollback` of `fast/appdata/arrs` is forbidden on every branch (Sonarr/Radarr/Lidarr state);
a file restore from `/mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/beets/config/state.pickle` is
the only database/state remedy, and only on the operator's explicit answer. ⛔ No ZFS rollback of
`tank/media/Music` is planned either (the evidence map registered `UNDO IMPORT` as the rollback
mechanism). ⛔ `tank/downloads@pre-phase5` untouched (E4, D-17).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-CONTEXT.md
@.planning/phases/07-pilot-12-albums-end-to-end/07-EVIDENCE-MAP.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: witness the imported state, adjudicate P01's findings, then ask undo / hold</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt</files>
  <read_first>
    - `artifacts/07-10-p01-import.txt` in full (criteria readings, oracle classification, findings) and the ledger.
    - `stacks/selfhosted/arrs/beets.md` § "One correction to this page's own § The recovery fence" (UNDO IMPORT verified by use once — tree and library, not state) — locate by heading text.
    - `stacks/selfhosted/arrs/beets/config.yaml` `incremental: yes`, `incremental_skip_later: yes` and their CONF-02 comment.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-52-01` (the mechanical trigger wording) and `DEF-06-45-01`.
  </read_first>
  <action>
Record `PRE-UNDO WITNESS`: P01 destination file count (`find … -type f | wc -l` from atlantis, with
`set -o pipefail`); read-only sqlite P01 album and item counts; `library.db` and `state.pickle`
sha256; the decoded `taghistory` membership of the staged P01 source path (expected PRESENT after an
import — if ABSENT, record it: then `state.pickle` never held the path and the undo question is
moot for it, which is itself a finding against the incremental model); the staged `_inbox/02-review`
folder's presence/absence (does beets-flask leave, move or delete it after import? record); Jellyfin
P01 item count.

Then `ADJUDICATION`: list every P01 finding from 07-10 (UNEXPLAINED drops, ffprobe/DB mismatches,
ownership ≠ 568:568, oracle divergences, sweep findings) and the executor's reading of each. Present
the D-15 plan: the operator presses `UNDO IMPORT` for P01 in the beets-flask UI; the executor
witnesses; then the re-run. Recommendation stated. `hold` first.
  </action>
  <decision>Back P01 out now with UNDO IMPORT?</decision>
  <context>D-15 backs out the gated album immediately after it passes, to prove undo with one album at risk. If P01's findings mean it did NOT pass, the undo is still the right act — the re-run can then use a different candidate (answer `undo-rerun-other`).</context>
  <options>
    <option id="hold">
      <name>Hold — leave P01 landed, stop the pilot here</name>
      <pros>Nothing further written; the fence holds.</pros>
      <cons>Criterion 5 is not exercised; the ledger records a STOP state with P01 landed.</cons>
    </option>
    <option id="undo">
      <name>Undo — I will press UNDO IMPORT for P01 now, then re-run with the SAME candidate</name>
      <pros>Proves the undo the operator will use, on one album.</pros>
      <cons>Brief window with P01 absent from the library.</cons>
    </option>
    <option id="undo-rerun-other">
      <name>Undo, then re-run with a DIFFERENT candidate (name it)</name>
      <pros>Undo proven and a flawed first match replaced.</pros>
      <cons>Re-run equality with run 1 no longer applies; the re-run is verified as a new import against criteria 3/4/7 instead.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold`, `undo` (after pressing UNDO IMPORT), or `undo-rerun-other <release id>` (after pressing UNDO IMPORT). Any other answer is treated as `hold`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt && /usr/bin/grep -qF 'PRE-UNDO WITNESS' "$A" && /usr/bin/grep -qF 'ADJUDICATION' "$A" && /usr/bin/grep -qF 'taghistory' "$A" && /usr/bin/grep -qF 'OPERATOR ANSWER' "$A"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-undo witness: file count, DB counts, both sha256, decoded `taghistory` membership, staged-folder state, Jellyfin count — each with its command and RC.
    - Every 07-10 finding listed with the executor's reading.
    - Answer recorded verbatim with UTC time on an `OPERATOR ANSWER` line; on `hold` the ledger gets a STOP STATE naming P01 as landed.
  </acceptance_criteria>
  <done>The imported state is witnessed and the operator has pressed UNDO IMPORT, or held.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: witness the undo on tree, DB and state.pickle, then ask re-run (or restore-then-re-run)</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt</files>
  <read_first>
    - `artifacts/07-11-p01-undo-rerun.txt` task-1 sections.
    - `artifacts/07-09-fence-and-grant.txt` — the fence copy paths and the snapshot names.
  </read_first>
  <action>
Skip on `hold`. Record `UNDO WITNESS`: (tree) P01 destination file count — expected 0; note any empty
directories left behind (`find … -type d -empty`) and whether the parent `Michael Jackson/` directory
remains (record; an empty directory is noted, not hand-removed); (DB) P01 album/item counts —
expected 0; total items — expected 0; (state) `state.pickle` sha256 and decoded `taghistory`
membership of the staged P01 path — expected ABSENT; (source) P01 source manifest identical;
(staging) whether the staged copy in `02-review` still exists. Jellyfin: send one targeted
`POST /Library/Media/Updated` with the 32 paths as `UpdateType` `Deleted` (address resolved fresh)
and, after ≥ 60 s, record that the P01 album is gone from `/Items` (bounded poll).

Verdict lines, one per part: TREE COVERED yes/no, DB COVERED yes/no, STATE COVERED yes/no.
If STATE COVERED is `no`: write `DEF-07-11-01` in the phase `deferred-items.md` — "`UNDO IMPORT`
does not cover `state.pickle`; D-14's claim is falsified for the incremental state" — and present
the restore option. Recommendation stated; `hold` first.
  </action>
  <decision>Re-run P01 now?</decision>
  <context>If the undo covered tree, DB and state, the operator re-imports P01 in 02-review with the same candidate (re-staging it first if the staged copy is gone — the executor re-stages by the same reflink copy). If state was NOT covered, a re-run would be skipped by `incremental`; the sanctioned remedy is a FILE copy of state.pickle out of the arrs snapshot — never a ZFS rollback of that dataset.</context>
  <options>
    <option id="hold">
      <name>Hold — stop with P01 backed out</name>
      <pros>Nothing further written.</pros>
      <cons>"Re-run to a good state" is not demonstrated.</cons>
    </option>
    <option id="rerun">
      <name>Re-run — undo covered all three; re-stage if needed and I will re-import in the UI</name>
      <pros>Completes criterion 5's demonstration.</pros>
      <cons>None beyond the import itself.</cons>
    </option>
    <option id="restore-then-rerun">
      <name>Restore state.pickle from the arrs snapshot by FILE COPY, then re-run</name>
      <pros>The only sanctioned remedy if the undo missed state; covers the gap without touching Sonarr/Radarr/Lidarr.</pros>
      <cons>A restore step outside `UNDO IMPORT`; criterion 5's "no hand-repair" is then for `/gsd-verify 07` to judge, and D-14 stands falsified.</cons>
    </option>
  </options>
  <resume-signal>Reply `hold`, `rerun` (after re-importing in the UI once the executor confirms re-staging), or `restore-then-rerun`. Any other answer is treated as `hold`.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt && { /usr/bin/grep -qiE 'OPERATOR ANSWER.*hold' "$A" || { /usr/bin/grep -qF 'UNDO WITNESS' "$A" && /usr/bin/grep -qE 'STATE COVERED (yes|no)' "$A" && /usr/bin/grep -qE 'TREE COVERED (yes|no)' "$A" && /usr/bin/grep -qE 'DB COVERED (yes|no)' "$A"; }; }</automated>
  </verify>
  <acceptance_criteria>
    - Undo witness recorded for tree, DB, state (sha256 + decoded membership), source, staging and Jellyfin, with the three COVERED verdicts.
    - If STATE COVERED is `no`, `DEF-07-11-01` exists in the phase `deferred-items.md`.
    - No hand removal of files or directories, no DB edit, no `zfs` write verb in the transcript.
    - Answer recorded verbatim with UTC time.
  </acceptance_criteria>
  <done>The undo's coverage of all three parts is measured and the operator has chosen the re-run path.</done>
</task>

<task type="auto">
  <name>Task 3: execute the chosen re-run path, prove equality, and fire (or hold) D-17 mechanically</name>
  <files>.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt, .planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-pilot-ledger.txt, .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md</files>
  <read_first>
    - `artifacts/07-11-p01-undo-rerun.txt` (both gates), `artifacts/07-10-p01-import.txt` (run-1 path list, `audio_md5` set, diff output).
    - `.planning/phases/06-tagger-configuration-and-dry-run/06-52-PLAN.md` Task 2's `release` branch — re-assert presence immediately before; exactly one `zfs destroy` of one full name, no `-r`/`-R`/`-f`, literal fence; re-list and compare full name sets; ZFS frees asynchronously (~20 s) — judge on names, not `avail`.
    - `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md` `DEF-06-52-01` (the trigger: release when Phase 7 Success Criterion 1's two events have both happened).
  </read_first>
  <action>
Skip the re-run on `hold` (ledger STOP STATE: P01 backed out, nothing landed; D-17 trigger NOT fired
— record why).

**On `restore-then-rerun`:** on atlantis as real root, copy
`/mnt/fast/appdata/arrs/.zfs/snapshot/pre-07-pilot/beets/config/state.pickle` over
`/mnt/fast/appdata/arrs/beets/config/state.pickle` preserving owner/mode read beforehand (a single
`cp` naming both literal paths; assert both literals before issuing). Record sha256 after (must
equal the fence copy's). The comment at this call site: `zfs [r]ollback of fast/appdata/arrs is
FORBIDDEN on every branch; this file copy is the only sanctioned restore`. Then proceed as `rerun`.

**Re-run:** if the staged copy is gone, re-stage P01 exactly as plan 07-10 task 1 did (reflink
copy, chown the staged copy only, sha256 set equal). Wait for the preview; confirm the same
candidate is offered at the same rank (record). The operator re-imports in the UI (their `rerun`
answer covers this; wait for their confirmation message before measuring). Then repeat plan 07-10
task 3's measurements for P01 (criteria 3, 4, 7, 8, Jellyfin), writing `post-07-P01-rerun`.

**Equality (criterion 5, "no hand-repair"):** assert (a) the `LC_ALL=C` sorted destination path
list is byte-identical to run 1's; (b) the `audio_md5` set is identical; (c) the `--json` diff
output of `pre-07-P01` vs `post-07-P01-rerun`, with `source_path` fields removed, is identical to
run 1's with the same removal (per-file fields gained/changed/dropped equal); (d) P01's `mb_albumid`
equal. On `undo-rerun-other`, replace (a)–(d) with a full criteria 3/4/7 verification of the new
import and record that run-1 equality does not apply.

**D-17, mechanical, no gate.** The trigger (DEF-06-52-01) has FIRED iff: (i) both pre-07-pilot
snapshots were listed back in `07-09-fence-and-grant.txt` (quote the lines), AND (ii) this plan's
UNDO WITNESS shows TREE COVERED yes, DB COVERED yes, and state covered either by UNDO IMPORT (STATE
COVERED yes) or by the operator-chosen file restore — i.e. "rollback exercised" on tree, DB and
state. Write `D-17 TRIGGER: FIRED` or `D-17 TRIGGER: NOT FIRED — <which event is missing>`.
If FIRED, in the SAME step, on atlantis as real root: re-assert
`tank/media/Music@pre-06-41-conf04-reprobe` present by exact whole-line match; capture the full
`zfs list -t snapshot -H -o name -r tank/media/Music` and `-r tank/downloads` name sets; assert the
literal target string equals `tank/media/Music@pre-06-41-conf04-reprobe` and issue exactly
`zfs destroy tank/media/Music@pre-06-41-conf04-reprobe` (no flags, no glob, no variable expansion
of more than that one name); capture RC; re-list both name sets and assert the set difference is
exactly that one name, `tank/media/Music@pre-07-pilot` and `tank/downloads@pre-phase5` still
present. If NOT FIRED: issue nothing and record the snapshot as held with the missing event named.

Append to `.planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md`, under
`DEF-06-52-01`, a dated status line: trigger FIRED/NOT FIRED on <UTC>, by plan 07-11, destroyed /
held, with `DEF-06-45-01` now CLOSED or still OPEN. Append no other change to that file.

Ledger: P01 LANDED (re-run) with the equality verdict, or the STOP state. Commit:
`docs(07-11): P01 undo proven on tree/DB/state, re-run equal; D-17 trigger recorded`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=.planning/phases/07-pilot-12-albums-end-to-end/artifacts/07-11-p01-undo-rerun.txt && /usr/bin/grep -qE 'D-17 TRIGGER: (FIRED|NOT FIRED)' "$A" && test "$(/usr/bin/grep -cE 'zfs [r]ollback' "$A")" -eq 0 && test "$(/usr/bin/grep -cE 'zfs [d]estroy' "$A")" -le 1 && { ! /usr/bin/grep -qE 'D-17 TRIGGER: FIRED' "$A" || /usr/bin/grep -qF 'pre-06-41-conf04-reprobe' "$A"; } && /usr/bin/grep -qF 'pre-phase5' "$A" && git diff HEAD~1 -- .planning/phases/06-tagger-configuration-and-dry-run/deferred-items.md | /usr/bin/grep -qE '^\+' </automated>
  </verify>
  <acceptance_criteria>
    - On re-run: equality (a)–(d) each recorded as a computed comparison (or the full new-import verification on `undo-rerun-other`).
    - On `restore-then-rerun`: the single `cp` with both literal paths, owner/mode preserved, sha256 equal to the fence copy; no `zfs` write verb used for the restore.
    - `D-17 TRIGGER:` line present with its two events quoted; on FIRED exactly one issued destroy naming the one snapshot, before/after name sets with a one-name difference, pre-07-pilot and pre-phase5 present after; on NOT FIRED zero destroy lines.
    - Verb recipes driven in both directions against controls before being relied on (real verb → 1; bracketed form → 0), recorded.
    - `deferred-items.md` (Phase 6) gains only the dated status line under `DEF-06-52-01`; no existing entry reworded (diff body read).
    - `REQUIREMENTS.md` untouched: `git diff --exit-code HEAD -- .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <done>Undo is demonstrated on tree, DB and state with a byte-equal re-run (or its failure is on record), and the held Phase 6 snapshot has been released or held on its mechanical trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI `UNDO IMPORT` → tree, library.db, state.pickle | A pre-release front end's undo; coverage unproven on state. |
| executor → atlantis `zfs destroy` | Irreversible. |
| restore path → `fast/appdata/arrs` | Shared with Sonarr/Radarr/Lidarr. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering / DoS | `fast/appdata/arrs` | mitigate | `zfs [r]ollback` forbidden, written at the restore call site; restore is a single literal-path file copy, only on the operator's `restore-then-rerun`. |
| T-07-11-02 | Repudiation | undo claimed complete when state was missed | mitigate | Three separate COVERED verdicts; `state.pickle` decoded from a copy and its `taghistory` membership read; failure filed as `DEF-07-11-01`. |
| T-07-11-03 | Data loss | other snapshots on `tank` | mitigate | D-17 destroy only on a fired trigger with both events quoted; presence re-asserted at the act; one literal name, no flags; full before/after name sets compared; pre-07-pilot and pre-phase5 asserted present. |
| T-07-11-04 | Tampering | hand-repair masquerading as re-run | mitigate | No manual file/DB edits permitted; re-run equality proven by path list, `audio_md5` set and diff output. |
| T-07-11-05 | Tampering | `tank/downloads@pre-phase5` | mitigate | Listed before/after; never a target (E4, D-17). |
| T-07-11-06 | Repudiation | operator authorisation | mitigate | Two blocking `checkpoint:decision` gates; answers recorded verbatim with UTC time; anything else is `hold`. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install. |
</threat_model>

<verification>
- Pre-undo and undo witnesses with three COVERED verdicts.
- Re-run equality or new-import verification.
- D-17 trigger recorded; at most one destroy of the one named snapshot.
</verification>

<success_criteria>
- Undo is exercised, not assumed, on all three parts of the state, and the good state is reproduced without hand-repair — or the gap is recorded as a finding with the operator's chosen remedy.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-12-albums-end-to-end/07-11-SUMMARY.md` when done.
</output>
