**/.env

**/secrets/*
**/data/*
**/config/*/secrets/*
.DS_Store
.vscode/
backups/
temp.md
# Local .env backups (contain secrets - DO NOT COMMIT)
*.env.backup

# ===================================================================
# Oracle Platform Archive — Migrated to agent-os vault (2026-06-09)
# ===================================================================
# All content below has been archived to:
# ~/Development/damianflynn/agent-os/Archives/Projects/Oracle-Agent-Platform/
# Branch: archive/oracle-platform-2026
# See: CLEANUP-PLAN.md for migration details

# Mac Mini Oracle agent platform experiments (abandoned)
mac mini/

# Keeper Secrets Manager OAuth experiments (failed)
scripts/keeper-inject-token.py
scripts/keeper-refresh-ms-token.py
scripts/ms-device-code-auth.py

# (Keeper-sh Graph-permissions correspondence moved 2026-09-02 to neocortex
#  clients/innofactor/workloads/concierge/docs/ — ignore rules no longer needed)

# Cerebro LXC notes (AI workload moved to Data platform)
stacks/cerebro/

# MPE ERPNext upstream clone (not committed)
stacks/mpe/erpnext/frappe_docker/

# Python bytecode. scripts/normalise-dj-tags.py (plan 03-05) is this repo's first committed
# Python file, and its own verification command is `python3 -m py_compile`, which drops a
# __pycache__/ directory beside it. Ignored so a verification run cannot dirty the tree.
__pycache__/
*.pyc
