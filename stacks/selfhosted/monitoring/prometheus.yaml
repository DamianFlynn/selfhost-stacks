services:
  prometheus:
    image: prom/prometheus:v3.10.0
    container_name: prometheus
    hostname: prometheus
    restart: unless-stopped
    user: "${PUID}:${PGID}"
    networks:
      - t3_proxy
    # ports:
    #   - "9090:9090"  # Uncomment for direct access or expose via Traefik
    volumes:
      - ${APPDATA_DIR}/monitoring/prometheus:/prometheus
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=${PROMETHEUS_RETENTION:-15d}'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheBASE=/var/lib/lxc/100/r-w
# Copy fixed files
cp $BASE/stacks/selfhomemcp $BASE/stacks/m
cat > $BASE/stacks/selfhosted/monitoring/prometheus.yaml << 'YAML'
services:
  prometheus:
    image: prom/prometheus:v3E}services:
  prometheus:
    image: prom/prometheus:v3.10.0
    conur  promet      image: pht    container_name: prometheus
        hostname: prometheus
    pr    restarts.certresolver    user: "${PUID}:${PGra    networks:
      - t3_proxyid      - t3_pin    # ports:
  
